# Common rules for every Wave 4 author (A2.1 as a standalone paid course)

S = /tmp/claude-0/-home-user-deutschmeister/c2792a5e-db22-5773-87d6-2f95d17dddab/scratchpad
Repo = /home/user/deutschmeister — READ-ONLY for you. Never edit, commit, or run git write commands
there. Write ONLY under the S/wave4/<area>/ folder your brief names. Never touch Supabase.
The sandbox cannot reach supabase.co, goethe.de, deutsch-meister.de; do not try.

1. `S/wave4/level-a2.1.md` is BINDING. Every German string you write must be inside it.
   Banned items are blocking defects. Sentences ≤14 words in learner-facing German.
2. Copy the SHAPE of the Wave 3 artefact your brief names; copy no content.
3. Correct standard German: umlauts/ß, capitalisation of nouns, commas before Nebensätze
   (where a text is allowed one), natural everyday register. No English inside German fields.
4. Self-check line by line against the level file, then run the validator/verify script your brief
   names, then write `<area>/notes.md` (coverage, deliberate exclusions, doubts for the reviewer).
5. Report ≤15 lines: file paths, counts, validator output, open doubts. An adversarial Opus
   reviewer reads your output next; expect a second round.
