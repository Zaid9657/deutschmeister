# Adversarial review — Wave 7 PR A2, author E1 (B1.1 EXTEND), round 2 (delta)

Deliverable re-read in full (not just changed rows): `S/extend/typed-b1.1/{genitive-case,genitive-prepositions,relative-clauses-nom-acc,relative-clauses-dat-gen}.json`, `S/extend/notes-E1.md` ("Round 2 changes"), and — for the out-of-band item — `S/recut/patches.json`.
Baseline: `S/reviews/extend-E1-review-1.md` (FAIL, 9 blocking, 12 minor).

## Validator output (run by the reviewer)

```
$ node scripts/check-grammar-json.mjs <all four absolute paths> --cache grammar-content-cache.json
OK: 4 file(s) validated, no violations.
$ ... one file at a time
genitive-case:            OK: 1 file(s) validated, no violations.
genitive-prepositions:    OK: 1 file(s) validated, no violations.
relative-clauses-nom-acc: OK: 1 file(s) validated, no violations.
relative-clauses-dat-gen: OK: 1 file(s) validated, no violations.
```

## Sweeps re-run from scratch (not trusting the author's report)

- **Banned-connector sweep** (als/bevor/nachdem/solange/sobald/bis/seitdem/obwohl/damit/sodass/so dass/falls/deshalb/deswegen/darum/trotzdem/außerdem/sonst/danach/dabei/dagegen/entweder/sowohl/weder/zwar + the B2 set) over every German field of all four files including every `rule_patches[*].new`: **0 hits**. The "Außerdem fehlen die Kommas!" regression the author reports catching is indeed absent — dat-gen mistake #3 reads "Es fehlen auch die Kommas!".
- **Passiv sweep**: `wird|werden|wurde|wurden|worden|geworden` — **0 occurrences** anywhere in new or patched German. Finding 1 is genuinely gone, not merely reworded around.
- **Partizip-II sweep** (independent of the werden sweep): 7 hits, 6 legitimate (Perfekt `gehört`/`gesprochen`/`gedacht`, and the noun `Geschlecht` twice). The seventh is new — see finding N1.
- **Null-article singular adjective sweep** over every key and acceptable_answer: 1 hit, `des kleinen Hotels` — has a definite article, so not the banned pattern. `laute Musik` is gone.
- **20-word cap** over every sentence of every German field incl. rule content and patch payloads: **0 violations** (longest 19).
- **Duplicate sweep, widened past round 1's**: every new exercise prompt, every new exercise key, every new example sentence and every string inside the new rules (mistake pairs, table cells, dialogue lines) normalised and compared against every live exercise prompt, live exercise key, and live example sentence across **all eight** B1.1 source dumps, plus the four new files against each other: **0 duplicates**. Findings 5 and 9 are closed at the mechanism level, not just for the two rows named.
- **rule_patches**: all five `old` values still **byte- and key-order-exact** against the source dumps, and all five `new` differ from `old`.
- **Structure**: order_index unchanged (16–25 / 9–18 exercises, rules 6/7/8, examples 9–12); 10 typed items per topic; `options: null` throughout; 4×fill_blank + 3×sentence_building + 3×error_correction per topic; every `related_rule_title` resolves to an existing or new rule title; all 8 examples' `grammar_highlight` a substring and `word_breakdown` tokenising the sentence in order.
- **Cold re-solve**: every changed item re-solved before reading its key (gen-case 24; gen-prep 9/10/11/12/14/16/18; nom-acc 16–20, 22; dat-gen 9–13, 15, 17). No key is wrong; the two cue/key contradictions are gone.
- **Out of band**: `S/recut/patches.json` verified against the dumps — all six `old` values byte-exact, and it de-tautologises both `explanation_de` fields ("während = während", "deren = deren") as well as swapping the questions. Both replacement questions are unambiguous against their own option lists (`wegen` is the only Grund; `deren` the only feminine possessive).

## Round-1 findings: closure check

| # | severity (r1) | status | evidence in round 2 |
|---|---|---|---|
| 1 | BLOCKING | **CLOSED** | nom-acc `rule_patches[0].new…content_de`: "Ein Relativsatz gibt mehr Informationen über ein Nomen. **Ein Relativpronomen leitet ihn ein.**" — active, `ihn` correctly masculine for *Relativsatz*, separable `einleiten` correct. Zero `werden` forms remain in the file. |
| 2 | BLOCKING | **CLOSED** | nom-acc oi 20 recast: `[der Nachbar / jeden Abend / Gitarre / spielen / sein / nervig]` → "Der Nachbar, der jeden Abend Gitarre spielt, ist nervig." No adjective before a null-article noun anywhere in the item. |
| 3 | BLOCKING | **CLOSED** | gen-prep oi 11 Q_en now "Despite **the** illness, he keeps working." against key `der`. Cue and key agree. |
| 4 | BLOCKING | **CLOSED** | gen-prep oi 12 now genuinely tests the indefinite: Q_en "…feminine **INDEFINITE** noun (eine Antwort)", key `einer`, explanation "eine Antwort → einer Antwort". |
| 5 | BLOCKING | **CLOSED** | gen-case oi 24 replaced: "Der Name des **klein** Hotels steht am Eingang." → "…des **kleinen** Hotels…". Same phenomenon, no n-Deklination, and the widened duplicate sweep finds no collision with any live row. |
| 6 | BLOCKING | **CLOSED** | dat-gen `rules[2].mistakes[0]` replaced with a real error — wrong "Der Chef, ich arbeite für, ist streng." / correct "Der Chef, für den ich arbeite, ist streng." (preposition stranding, a realistic English calque). The wo nuance moved to `key_insight_de`: "wo ist bei einfachen Orten richtig; bei einer festen Präposition wie bei/mit ist Präposition + Relativpronomen im Schreiben üblicher." That now agrees with example oi 12 and dialogue line 5 instead of contradicting them. Rule title updated to "Common Mistakes: Preposition, dessen/deren, Comma". |
| 7 | BLOCKING | **CLOSED** | gen-prep mistake #2 is now an actual dative — "Statt **dem** Auto nehme ich den Bus." — matching its explanation; mistake #3 replaced with a distinct phenomenon: "Trotz **den** Problemen…" → "Trotz **der** Probleme…", explained as plural genitive vs dative plural. Four distinct pairs. |
| 8 | BLOCKING | **CLOSED** | gen-case `rule_patches[0].new`: "Im Deutschen: Besitz + **Besitzer** (im Genitiv)**:** das Auto des Mannes." — noun capitalised, and the period became a colon so the lowercase `das` is no longer sentence-initial. Both defects gone in one edit. |
| 9 | BLOCKING | **CLOSED** | gen-prep example oi 10 replaced with on-topic content: "Aufgrund der hohen Miete suchen wir eine kleinere Wohnung." (`aufgrund` is on the topic's allowed list and was unused). dat-gen keeps its copy, correctly. No cross-file duplicate remains. |
| 10 | MINOR | **CLOSED** | gen-prep oi 9–12 now spread masc. def. (`des Umzugs`), neut. def. (`des Jahres`), fem. def. (`der Krankheit`), fem. indef. (`einer Antwort`) — four different answers instead of four `der`. |
| 11 | MINOR | **CLOSED** | gen-prep oi 18 `acceptable_answers` now carries all four strings incl. "Wegen des **Staues** komme ich zu spät."; explanation names the variant. gen-case oi 24's replacement noun (Hotel) has a single genitive, so nothing owed there. |
| 12 | MINOR | **PARTLY OPEN** | "Base form: X" removed from all eight guided relative-clause items; the pronoun removed from all three sentence_building brackets. **But** dat-gen oi 15's `question_en` still reads "**deren** + genitive noun (Relativpronomen ergänzen)" — it names the answer in the same breath as asking for it. See N-list. |
| 13 | MINOR | **CLOSED** | nom-acc oi 22 rebuilt: `[die Kollegin / ich / schon lange / kennen / wohnen / in Berlin]` → "Die Kollegin, die ich schon lange kenne, wohnt in Berlin." Bracket order mirrors the target and the sentence no longer echoes live example oi 6. |
| 14 | MINOR | **CLOSED** | "The relative clause goes directly after the noun." present on all six affected items (nom-acc 20/22/24, dat-gen 13/15/17). |
| 15 | MINOR | **CLOSED (with N1 attached)** | gen-prep mistake #1 noun changed Regen → Verkehr, and the explanation now cross-references rule 5 instead of restating it. The cross-reference itself introduced a new defect — N1/N2. |
| 16 | MINOR | **CLOSED** | dat-gen `rule_patches[0].new.description_de`: "dessen **ersetzt sein** (m/n). deren **ersetzt ihr** (f/pl). Zeigt Besitz!" — accurate and contrastive, and it now mirrors the table's own "Sein Auto → dessen Auto / Ihr Haus → deren Haus" rows. |
| 17 | MINOR | **CLOSED** | dat-gen dialogue line 2 highlight is now "mit denen ich **hier** zu tun habe" — a substring of its line. |
| 18 | MINOR | **CLOSED** | dat-gen dialogue line 4: "von dem Sie **gesprochen haben**" (Perfekt), highlight updated to match. |
| 19 | MINOR | **CLOSED** | gen-prep dialogue rebuilt: Frau Klein now asks "Was ist denn los? …", Herr Berger carries the complaint ("Trotz meines Anrufs letzte Woche ist noch nichts passiert."). `Personalmangel` → "der vielen Arbeit"; `Kündigungsfrist` → "innerhalb ihrer Frist". |
| 20 | MINOR | **CLOSED** | gen-prep stage-5 difficulty now 2/3/2/3/2/3; all ten nom-acc items carry a `related_rule_title`, each resolving to a live rule ("Nominative Relative Pronouns (Subject)", "Accusative Relative Pronouns (Object)", "Common Mistakes with Relative Clauses", "Nominative vs Accusative: How to Choose"). |
| 21 | MINOR | **CLOSED** | gen-prep dialogue line 3 now "Können Sie das innerhalb einer Woche **reparieren**?" — the causative `lassen` is gone. |
| D | — | **OUT OF SCOPE, correctly** | Both legacy rows left untouched in the deliverable; `S/recut/patches.json` carries them with byte-exact `old` values and rewritten German that no longer names its own key. Verified. |

**19 of 21 fully closed, 1 partly open (12), 1 closed with a new defect attached (15).**

## New defects introduced in round 2

| # | file:path | severity | quote | why | fix |
|---|---|---|---|---|---|
| N1 | genitive-prepositions.json:`rules[2].content.mistakes[0].explanation_de` | **BLOCKING** | "wegen braucht **wie schon in Regel 5 gezeigt** den Genitiv, nicht den Dativ." | Introduced by the finding-15 fix. `wie schon in Regel 5 gezeigt` is an elliptical passive clause (`wie … gezeigt wurde`) inserted mid-sentence — a Partizip-II adverbial group that a B1.1 learner has no grammar for. `level-b1.1.md` is explicit that every German string must be *readable with only the grammar listed*, bans Passiv outside the receptive text allowance, and sets the rule-text register at "du-form, short sentences". The insertion is also uncomfortable to parse with no commas on either side ("braucht wie schon in Regel 5 gezeigt den Genitiv"); a Partizip group may take commas under §77 E1, and an *Einschub* with none reads as a run-on. This is the only sentence in the four files that a B1.1 learner cannot decode. | Split it, which also kills N2: "wegen braucht den Genitiv, nicht den Dativ. Das steht auch in der Regel zur Umgangssprache." (`explanation_en` correspondingly: "…as the rule on spoken German also says."). |
| N2 | genitive-prepositions.json:`rules[2].content.mistakes[0].explanation_de` / `_en` | MINOR | "in **Regel 5**" / "as **rule 5** already showed" | A cross-reference to an internal `order_index`. That topic's rules run 0–5, so "Regel 5" is the learner's **sixth** card, and the reference silently breaks the moment any rule is inserted or renumbered — the script explicitly never renumbers, so a future insert would leave this pointing at the wrong rule. | Name the rule instead of its number (see N1's fix), or drop the reference — the noun change alone already satisfies finding 15. |
| N3 | genitive-prepositions.json:`rules[0].content.lines[1].highlight` | MINOR | line "Was ist denn los? Ich habe **seit letzter Woche** nichts mehr gehört." with `highlight: "seit letzter Woche"` | Correct German and a substring, but it highlights a **dative** time phrase in the *genitive-prepositions* topic; the dialogue's other five highlights are all genitive-preposition phrases (wegen der Heizung, Trotz meines Anrufs, Während des Winters, Wegen der Kälte, Trotz der vielen Arbeit). The one line whose job is to set up the complaint now points the learner at grammar the topic does not teach. | Either drop the `highlight` on this line or re-cast it around a target preposition, e.g. "Was ist denn los? **Wegen der Heizung** habe ich nichts mehr gehört." |
| N4 | genitive-prepositions.json:`exercises[1]` (oi 10) | MINOR | "Innerhalb ___ Jahres wächst die Firma stark." / Q_en "Within **the** year…" / key `des` | The key is defensible because the cue says "the year", but `innerhalb des Jahres` is stilted German for a duration — the idiomatic form is `innerhalb eines Jahres`, which is exactly what live `genitive-case` exercise oi 9 ("Innerhalb ___ Jahres hat er viel gelernt." → `eines`) and live `genitive-prepositions` example oi 6 already model. The learner therefore meets a near-identical frame with the opposite key. | Change the noun rather than the article: "Innerhalb ___ Gebäudes ist Rauchen verboten." (neuter, definite, natural) — or keep `Jahr` and make the key `eines`, adjusting Q_en to "Within a year". |
| N5 | genitive-prepositions.json:`rules[0].content.lines[2].de` | MINOR | "**Trotz meines Anrufs letzte Woche** ist noch nichts passiert." | A bare accusative time adverbial hung on the end of a genitive noun phrase; it parses, but a native would write "Trotz meines Anrufs **von letzter Woche**" or split it. Cosmetic, in an otherwise much-improved dialogue. | "Ich habe letzte Woche angerufen. Trotz meines Anrufs ist noch nichts passiert." (two sentences, both under the cap). |
| N6 | `S/extend/notes-E1.md`, the four per-topic sections | MINOR (documentation) | e.g. "fixed to \"dessen = wessen (m/n). deren = wessen (f/pl).\"", "(8) common_mistakes, 4 pairs (wegen dem ✗, statt/anstatt, …)", "(8) common_mistakes, 4 pairs (**wo vs. bei der**, …)" | The per-topic sections still describe round-1 state and are contradicted by the Round 2 section further down — including for the two items that were *blocking* (6 and 16). A handoff reader who reads the topic sections and stops gets the retracted content as fact. | Fold the Round 2 corrections back into the per-topic prose, or mark the per-topic sections "(round 1 — superseded below)". |

## VERDICT: FAIL (1 blocking, 6 minor)

What I tried and did not find, this round: no `werden`/`wurde`/`worden` anywhere; no topic-9–12 connector (including the "Außerdem" regression the author self-reports); no sentence over 20 words; no duplicate against any live exercise prompt, live exercise key or live example across all eight B1.1 topics, nor between the four new files; no Präteritum key; all five `rule_patches` `old` values still byte-exact; every changed key re-solved cold and unique; every `related_rule_title` resolving; no null-article singular adjective in any key.

The round was substantive rather than cosmetic: eight of the nine blocking items were fixed at the root (the offending item recast, not the wording nudged), and the two structural ones — the duplicate check and the ban sweep — were widened so the class of defect cannot recur silently. The single remaining blocker is a regression, not a survivor: the cross-reference added to close finding 15 dropped a reduced-passive participial group into learner-facing B1.1 German. It is a one-line fix that also closes N2, and the other five new items are cosmetic. I expect round 3 to be a spot-check, not a re-review: fix N1/N2 in `rules[2].content.mistakes[0]`, close the `deren` leak in dat-gen oi 15's `question_en`, and the deliverable is shippable.
