
# A1.1 — DaF-Fachreview #11, nach Runde 11 (2026-09-12)

Reviewer: DaF-Lehrkraft (Erwachsenenbildung, Integrationskurs) und Prüferin für
Goethe-Zertifikat A1 *Start Deutsch 1*. Elfter Durchgang. Basis:
`REVIEW-daf-2026-09-12.md` (5 BLOCKER · 45 MAJOR), `REVIEW-daf-2-2026-09-12.md`
(1 BLOCKER · 35 MAJOR · 44 MINOR), `REVIEW-daf-3` bis `REVIEW-daf-5` (je
3 BLOCKER · 11 MAJOR), `REVIEW-daf-6` (3 BLOCKER · 7 MAJOR), `REVIEW-daf-7`
(3 BLOCKER · 5 MAJOR), `REVIEW-daf-8` und `REVIEW-daf-9` (je 0 BLOCKER · 5 MAJOR)
und `REVIEW-daf-10-2026-09-12.md` (0 BLOCKER · 4 MAJOR), angewendet in `main` @
`1395d2b` („A1.1 round 11: close DaF review #10 + report", PR #128). Gemessen wurde
ausschließlich der Commit-Stand. **A1.2 ist durch Eigentümerentscheidung vom
2026-09-13 pausiert und nicht Gegenstand dieses Reviews.**

Gemessen, nicht angenommen:

* **Die echte Ziehung für alle drei Versuche**: `planPractice(CURRICULUM_A11, POOL, a)`
  für `a` = 1, 2 **und** 3 importiert, alle **252** ausgelieferten Items
  (**218 verschiedene**) mit `questionDe`/`questionEn`/`options`/`answer`/`accepted`/
  `explanationDe`/`caseSensitive`/`minLektion` ausgedruckt und einzeln gelesen; dazu für
  jeden der **36 Blöcke** `practiceReport()` (`relaxUsed`, **`complete`**, `eligible`), die
  Zahl der echten Drills (`drillsSlug`), die Zahl verschiedener `taskShape`-Schlüssel,
  die größte Antwortschlüssel- und Lemmagruppe und **alle drei Versuchspaare** (1∩2, 2∩3
  **und 3∩1**).
* Eine **eigene, gröbere Rahmenmessung** neben `taskShape`: Klammer ganz entfernt,
  Wortlisten auf eine Marke, `·`-Läufe zusammengezogen, Itemtyp ignoriert — über alle
  36 Blöcke.
* Eine **eigene Lecksuche**: für jeden der 36 Blöcke jede Antwort gegen jeden anderen
  Prompt desselben Siebenerblocks, wortgenau, Klammercues ausgenommen.
* Eine **eigene Situationsmessung**: `relevanceScore(item, ownTerms, ∅)` je Item, also wie
  viele der sieben Aufgaben überhaupt ein Wort aus dem **eigenen** Wortfeld der Lektion
  tragen — 36 Blöcke.
* Eine **Gegenprobe am Motor**: `buildLesson.js` in den Scratchpad kopiert, `fits()`
  instrumentiert (welche Kappe welches Item wann ablehnt), und ein zweiter Lauf mit
  `bestimmter`/`unbestimmter`/`artikel` in `LEMMA_STOPWORDS`, um die Ursache von MAJOR 1
  zu belegen statt zu vermuten.
* `checkAnswer` + `checkOptionsFor` auf **1252 Bewertungsflächen**: 252 gezogene Items,
  der ganze gebaute Pool (**366**), die Handquelle (**219**), **44** getippte
  Checkpoint-Items und **395** Wiederholungskarten gegen `gradeTypedReview` — je mit der
  eigenen Antwort und mit der kleingeschriebenen Probe, gruppiert nach der exakten
  Antwortzeichenkette.
* **Alle vier Checkpoints gebaut**, alle **80** Items ausgedruckt; die **16 Lesen-Aussagen**
  Zeile für Zeile gegen ihren eigenen Text gehalten (Teilzeichenkette, Ein-Token-Abstand,
  Richtig/Falsch-Rhythmus, Falsifikatorart), dazu Dialogzeilen-Mehrfachnutzung und
  Poolitem-Wiederverwendung.
* **Die 54 neuen Handitems und die sechs L10-Neufassungen** einzeln gelesen (Grammatik,
  Natürlichkeit, A1-Tauglichkeit, Situationsbezug, Antwortschlüssel, `accepted`,
  Register der Aufgabenzeile), dazu ihre `taskShape`- und `answerKey`-Schlüssel.
* **Der Konstruktionsguard nachgebaut und auf die Produktionsflächen gerichtet**: dieselben
  drei `CONSTRUCTION_PATTERNS` über alle Diktat- und Vorlesezeilen aller zwölf Lektionen.
* **Der Weg der Versuchszahl, Ende zu Ende**: `attemptFromCompletions`, `localRunCount`,
  `recordLocalLesson` (`runs`, `runStamps`, `trimAttempts`), `planLocalMerge`,
  `mergeLocalProgress`, `logAttempts(createdAt)`, `countCompletedRuns`,
  `LessonPlayerPage` (merge-vor-zählen), und der Weg zurück in die Lektion über
  `CurriculumHomePage` (`to={unlocked || isDone ? hrefFor(...) : null}`).
* Das Curriculum: Dialoge, notices, Kann-Beschreibungen, `examTeile`, Wortfeld, Register,
  pretests, Diktat-/Vorlesefenster, die **zwölf Schreibaufgaben** gegen
  `netlify/functions/_shared/writingTasks.mjs` (Aufgabentext, Leitpunkte, Wortgrenzen
  **deckungsgleich**), die Sprechaufträge (`anrede` in allen zwölf `'Sie'`), die zwölf
  Regelkarten gegen ihre notices.
* `node scripts/validate-curriculum.mjs a1.1` (RULE 10: 14/14 · RULE 11: **62**/62 ·
  RULE 11b: **0, harte Regel** · RULE 12: 0, harte Regel · RULE 13: 4/4 · RULE 6b: 26/26 ·
  RULE 15: 12/12 · RULE 16: 0/0 · RULE 14: 0).
* `npm test`: **835 Tests, 834 grün, 1 übersprungen** (der A1.2-Ziehungstest).
  `npm run lint`: 0 Fehler, 0 Warnungen. `npm run check:duplicates`: OK.

## Urteil in einem Absatz

**Runde 11 hat alle vier MAJOR aus Runde 10 an den Artefakten geschlossen, und die
Formachse ist damit zum ersten Mal wirklich zu.** `taskShape` misst die Aufgabe und nicht
mehr ihre Buchstaben: die fünf buchstabengleichen L1-Sätze aus Runde 10 kollabieren zu
**einem** Schlüssel, die sechs umformulierten L10-Klammern zu **einem**, und meine eigene,
gröbere Rahmenmessung — Klammer ganz weg, Wortliste zu einer Marke, `·`-Läufe
zusammengezogen — findet über **alle 36 Blöcke** als größte Rahmengruppe **3 von 7** statt
5 von 7 in Runde 10. Die sechs L10-Items sind nicht umbenannt, sondern **in zwei echte neue
Operationen umgeschrieben** worden („Bilden Sie den Satz mit dem Verb auf Position 1:
‚Der Zug fährt nach Österreich.'" und „Die Antwort ist: ‚Ja, der Bus hat Verspätung.'
Schreiben Sie die Frage."), und beide sind genau das, was *Start Deutsch 1* Sprechen Teil 3
verlangt. Die Wächter laufen jetzt über `ATTEMPTS` statt über `[1, 2]`, das Wrappaar 3→1
wird geprüft, und ein Test verbietet die Literalliste in der Testdatei selbst — alle 36
Blöcke füllen sieben, mit `relaxUsed = 0`, **7 von 7** verschiedenen Formen und ≥ 4 echten
Drills; die Überschneidung ist in **allen drei** Paaren ≤ 2, und die einzigen Ausnahmen
sind L8s zwei `mustCover`-Items und der neue, gewollte Treffer `extra-a11-l12-08` im
Wrappaar (die Höflichkeitsform, die L12 in jedem Versuch üben MUSS). Die Funktionswörter
sind von einer Liste auf **erste Nennung** umgestellt — das ist die sauberste Schließung
dieser ganzen Reihe, weil sie eine Ausnahme durch eine Messung ersetzt —, der
Konstruktionsguard fängt alle sechs Sonden aus Runde 10, und die Versuchszahl überlebt das
Anmelden: ich habe den Lernenden durchgerechnet (zwei ausgeloggte Durchgänge, Anmeldung,
ein eingeloggter Durchgang) und er bekommt beim vierten Besuch **Versuch 1**, also genau
den Zyklus, den er auch ohne Anmeldung bekommen hätte. **Und trotzdem unterschreibe ich
nicht, und diesmal nicht wegen einer Regel, sondern wegen eines Satzes.** Das neue Item
`extra-a11-l06-14` erklärt: „**Nach brauchen wird ein zu einen**: einen Computer." Das ist
für A1 schlicht falsch — es gilt nur maskulin —, es steht dreimal anders und richtig im
selben Kurs („Nach brauchen wird **maskulin** ein zu einen", `extra-a11-l06-04`; „Nach
haben wird **maskulin** ein zu einen", `extra-a11-l09-07`; und so auch die L6-notice), und
es wird in **Lektion 6, Versuch 3** in **derselben Sieben** ausgeliefert wie
`extra-a11-l06-12` („Ich brauche einen Pause." → „Ich brauche **eine** Pause."), also wie
das Item, das genau die Antwort als Fehler markiert, die die Erklärung verlangt. Daneben
drei systematische Befunde. **Erstens** hat die Formkappe die Situationsachse bezahlt: die
Lemmakappe zählt die Klammerwörter `bestimmter`/`artikel` als **Inhalt**, während
`taskShape` dieselbe Klammer seit dieser Runde als **Kategorie** liest — die zwei Wächter
sind sich uneinig darüber, was eine Klammer ist, und Lektion 5 verliert daran ihr eigenes
Grammatikthema: `practiceReport` meldet für L5 Versuch 2 und 3 **`complete: false`** mit
**2 von 7** Items auf `definite-articles`, fünf kommen aus dem Flohmarkt von Lektion 4,
und **kein einziger Test liest `complete`**. Meine Gegenprobe (dieselben Wörter in die
Stoppliste, sonst nichts) hebt L5 Versuch 1/2 auf 6/7 bzw. 5/7 und das eigene Wortfeld von
5/7 auf 7/7. **Zweitens** beantwortet in **11 von 36 Blöcken** ein Item ein anderes
derselben Sieben: L11 Versuch 1 stellt als Aufgabe 1 „Kommst du am Freitag ___?
(mitkommen)" und als Aufgabe 7 „Bilden Sie die Frage: [mitkommen / du / am Freitag]", deren
Lösung genau dieser Satz ist; L1 Versuch 2 serviert das Paar sogar **gegenseitig**
(„Lesen Sie die Buchstaben: W-I-L-L-K-O-M-M-E-N … Schreiben Sie das Wort: ___" neben „Wie
buchstabiert man Willkommen? ___"). **Drittens** bindet der neue Konstruktionsguard die
Regelkarte, aber nicht den Satz, den der Lernende **schreiben** muss: dieselbe Runde hat
„Meine Schwester ist noch jung." von der L3-Karte gestrichen, weil der Possessivartikel
erst L12 ist — und lässt genau diesen Satz als **Diktat** von Lektion 3 stehen; insgesamt
acht solcher Stellen in vier Lektionen.

**Zahlen: 1 BLOCKER · 3 MAJOR.** Die Blockerfreiheit der Runden 8 bis 10 ist gerissen, und
zwar an einem Satz, den diese Runde selbst geschrieben hat. Die Bedingung aus
`docs/course-standard-2026-09-12.md` §8 (0 BLOCKER / 0 MAJOR) steht weiter aus. MINORs sind
am Ende als acht Zeilen erfasst.

---

## BLOCKER

| # | Feld | Zitat | Problem | Fix (pasteable) |
|---|---|---|---|---|
| 1 | `src/data/lessonPools/a11.extra.json` → `extra-a11-l06-14` (neu in dieser Runde), ausgeliefert in `a11.json`, gezogen in **L6 Versuch 3**; zweite Instanz `4aae7de0-bb31-5533-ac24-027af7598a19` im **selben** Block | `extra-a11-l06-14`: „Korrigieren Sie: „Ich brauche **eine** Computer."" → „Ich brauche einen Computer.", `explanationDe`: „**Nach brauchen wird ein zu einen: einen Computer.**" · im selben Siebener: `extra-a11-l06-12` „Korrigieren Sie: „Ich brauche **einen** Pause."" → „Ich brauche **eine** Pause." und `extra-a11-l06-13` „Ich brauche **einen** Telefon." → „Ich brauche **ein** Telefon." · ebenfalls im selben Siebener: `4aae7de0` „Er **hat** ___ Handy. (unbestimmter Artikel)" → `ein`, `explanationDe`: „Handy ist neutral, deshalb ein, **wie bei maskulinen Nomen**." · die richtige Fassung steht dreimal daneben: `extra-a11-l06-04` „Nach brauchen wird **maskulin** ein zu einen.", `extra-a11-l09-07` „Nach haben wird **maskulin** ein zu einen.", L6-notice „Nach Verben wie brauchen, haben, kaufen wird **maskulin** ein → einen" | **Dem Lernenden wird für A1 etwas Falsches beigebracht, und der Kurs widerlegt es zwei Aufgaben später selbst.** Wer die Erklärung von `extra-a11-l06-14` anwendet, schreibt „Ich brauche **einen** Pause" und „Ich brauche **einen** Telefon" — und genau diese beiden Sätze sind in derselben Sieben die **Fehlersätze**, die er korrigieren soll. Die Regel ist nicht ungenau, sie ist falsch: nach `brauchen` wird **maskulin** `ein` zu `einen`, feminin und neutral bleiben `eine` und `ein`. Das ist kein Randfall, sondern die einzige Kasusform, die A1.1 überhaupt produziert, und sie ist in *Start Deutsch 1* Schreiben Teil 2 und Sprechen Teil 3 prüfbar. Die zweite Instanz ist dieselbe Klasse in der Gegenrichtung: `4aae7de0` steht nach **haben** — wo maskulin `einen` heißt — und begründet das neutrale `ein` ausgerechnet mit „wie bei maskulinen Nomen". Beide Sätze sind gezogen, beide stehen in **L6 Versuch 3**, zusammen mit der korrekten Fassung `extra-a11-l06-04`: der Lernende liest in einer einzigen Lektion dreimal denselben Sachverhalt, zweimal falsch und einmal richtig. Es ist **keine** Einzelzeile: die Klasse heißt „eine Erklärung behauptet eine Regel, ohne die Bedingung zu nennen, unter der sie gilt", und der Pool trägt sie noch an drei weiteren Stellen (`fb88c1bb` „Wir steht **immer** mit haben.", `3c9fff52` „Du steht **immer** mit hast.", beide **gezogen** in L9 Versuch 2; `885e0528` „Wir steht **immer** mit sind." widerspricht dem ersten wörtlich). | **Erstens** die zwei Zeilen reparieren, mit dem Wortlaut, den der Kurs schon dreimal führt: `extra-a11-l06-14.explanationDe` → `"Nach brauchen wird maskulin ein zu einen: einen Computer."`; `4aae7de0.explanationDe` → `"Handy ist neutral: ein Handy. Maskulin heißt es nach haben einen: einen Computer."` **Zweitens** — und das ist die Regel, nicht die Liste — einen Lint über **jede** `explanationDe` des Pools, der Karten und der notices in `tests/lesson-extra-items.test.mjs`: (a) eine Erklärung, die das Muster `/\b(ein|der)\s+(wird|zu)\s+(einen|den)\b/` oder `/nach \w+ wird\b/i` trägt, MUSS `maskulin` enthalten; (b) eine Erklärung darf `immer`/`nie`/`alle` nur in einem Satz führen, der ein Genus, einen Kasus oder eine Person nennt — „Im Plural haben alle Nomen die" bleibt erlaubt, „Wir steht immer mit haben" nicht. **Drittens** die fünf oben gemessenen Sätze als `FALSE_RULE_PROBES`-Fixture in dieselbe Datei, mit der Zusicherung, dass **jeder** gefangen wird: ein Guard, der seine eigene Gegenprobe nicht besteht, ist keine Regel — dieser Satz steht seit Runde 9 in dieser Reihe und gilt auch für Erklärungstexte. **Viertens** eine Konsistenzprobe als Klasse: für jedes Item, dessen Antwort `einen`/`ein`/`eine` ist, muss die Erklärung das Genus des Bezugsnomens nennen; die drei Zwillinge `l06-12/-13/-14` sind der Beweis, dass eine Lektion sonst drei Erklärungen für eine Regel schreibt. |

Die übrigen drei Definitionen sind einzeln gegen den heutigen Stand gemessen und **halten**:

* **„Der Lernende wird auf eine richtige Antwort hin falsch bewertet."** **0**
  Selbstablehnungen über **1252** Bewertungsflächen: 252 gezogene Items, 366 Poolitems,
  219 Handitems, 44 getippte Checkpoint-Items und 395 Wiederholungskarten, jede mit ihrer
  eigenen Antwort über `checkOptionsFor(item)` bzw. `gradeTypedReview`. Die
  Kleinschreibprobe über alle **167** verschiedenen großgeschriebenen Antwortzeichenketten
  findet **acht** Gruppen mit verschiedenen Urteilen, und jede einzelne ist entweder die
  lockere Richtung (das Item führt seine kleine Zwillingsschreibung in `accepted`) oder die
  **richtige** Unterscheidung: `Sie` ist in `extra-a11-l03-08` („Frau Kaya, sprechen ___
  Englisch?", `caseSensitive: true`) hart und in `extra-a11-l03-02` locker; `Ihr` in
  `extra-a11-l12-08/09` hart und in `4299d5ca` („___ seid meine Freunde.") locker; `Ihre`
  in `b269d424` („Ist das ___ Adresse? (Sie, Höflichkeitsform)") hart und in
  `extra-a11-l12-10` („___ Geschenke sind hier. (sie, Plural)") locker. Kein Item lehnt
  seine eigene Lösung ab.
* **„Der Kurs verspricht etwas, das er nicht enthält."** RULE 16 misst **0**, RULE 12 ist
  eine harte Regel und misst **0**; alle 33 `examTeile`-Behauptungen der zwölf Lektionen
  haben eine Fläche in ihrer eigenen Lektion. Die zwölf Schreibaufgaben sind mit der
  Serverbank deckungsgleich (Aufgabentext, Leitpunkte, Wortgrenzen), alle zwölf `taskKey`s
  vorhanden. L12 zieht die Höflichkeitsform `Ihr` jetzt in **allen drei** Versuchen — der
  `mustCover`-Exaktvorzug dieser Runde greift gemessen.
* **„Eine geprüfte Form, die der Kurs nie lehrt."** RULE 11b = **0**, und zum ersten Mal
  ist das keine Buchhaltung: `taughtUpTo` bindet jedes Funktionswort an die erste Lektion,
  deren Dialog oder Notice es **sagt**. Nachgemessen: `dem`, `mit`, `meiner`, `einen` fallen
  alle auf **L3**, weil L3s eigene Dialogzeile 9 „Der Mann **von meiner** Schwester kommt
  auch aus Marokko." lautet — die beiden Items, die Runde 10 als Vorgriff gemeldet hat
  (`extra-a11-l03-12/-15`), sind damit nicht wegdefiniert, sondern **belegt**. RULE 11 ist
  von 49 auf 62 gestiegen; die Begründung („das Maß ist ehrlich geworden, nicht das
  Material schlechter") stimmt bis auf **eine** Zeile: `extra-a11-l01-15: am` ist ein
  **neues** Item dieser Runde, nicht bloß ein aufgedeckter Altbestand (Minor 4).

---

## MAJOR

| Feld | Zitat | Problem | Fix (pasteable) | Schwere |
|---|---|---|---|---|
| `LEMMA_STOPWORDS` in `src/lib/lesson/buildLesson.js:132–166` ↔ `TASK_MARKS`/`instructionMark` (ebd. 247–290) ↔ `practiceReport().complete` (ebd. 700–705) ↔ `tests/lesson-engine.test.mjs:329–353` | Kopfkommentar der Stoppliste: „«Artikel», «Präposition» … sind **deliberately absent**, because they mark a template — and capping a template at `MAX_SAME_LEMMA` is exactly what stops a Lektion turning into four times …" · Kopfkommentar von `TASK_MARKS`, aus derselben Runde: „A rewording is not a new exercise, so the wording never reaches the key: **the whole bracket becomes its category**." · gemessen, `practiceReport()`: **L5 Versuch 2 `complete: false`**, **L5 Versuch 3 `complete: false`**, `relaxUsed = 0`, sonst kein Block · L5 Versuch 3 im Wortlaut: „Bilden Sie den Satz: [das Regal / sein / teuer]", „Ich kaufe ___ Regal.", „___ Fenster ist hier.", „___ Taschen sind alt.", „___ Tisch kostet fünfzehn Euro.", „___ Tasche ist teuer.", „___ Bleistift ist schwarz." — **2 von 7** auf `definite-articles`, **5 von 7** aus dem Flohmarkt-Slice von Lektion 4 · instrumentierte Ablehnungen: `reject extra-a11-l05-11 lemma bestimmter`, `reject extra-a11-l05-02 lemma bestimmter`, `reject extra-a11-l05-09 lemma artikel`, `reject extra-a11-l05-12 lemma artikel` · eigene Situationsmessung (Items mit ≥ 1 Wort aus dem eigenen Wortfeld), Versuche 1/2/3: **L5 5/3/3**, **L7 7/4/2**, **L9 7/4/3**, L2 7/6/4, L12 7/6/4 | **Die Runde hat die Klammer für den einen Wächter zu einer Kategorie gemacht und für den anderen Inhalt gelassen — und die Lektion, deren Grammatik der Artikel IST, zahlt dafür mit ihrem Thema.** Drei Messungen. **(a) Die Lemmakappe zählt die Aufgabenformel.** `bestimmter` und `artikel` sind keine Inhaltswörter, sie sind die Klammer „(bestimmter Artikel)", und `MAX_SAME_LEMMA = 2` erlaubt sie deshalb **zweimal je Lektion**. Von den 17 in L5 servierbaren `definite-articles`-Items tragen **13** genau diese Klammer. Ab dem dritten ist die eigene Grammatik der Lektion gesperrt — nicht weil der Pool leer wäre, sondern weil die Anweisung mitgezählt wird. **(b) Der Motor meldet es, und niemand hört zu.** `pickPracticeItems` schreibt `report.complete = false`, wenn `primaryCount < primaryTarget`; über alle 36 Blöcke ist das genau zweimal der Fall, beide Male L5. Ein `grep` über `tests/` und `scripts/` findet **null** Leser von `.complete` — die Wächter lesen ausschließlich `relaxUsed`, und das steht hier bei **0**, weil der Block ja sieben Items hat. Der Ratchet `MAX_CAP_STARVED_LEKTIONEN = 0` ist also wahr und blind zugleich: er zählt die Lektionen, die ihre Sieben nicht **füllen** können, nicht die, die sie mit dem Stoff der Nachbarlektion füllen. **(c) Der Preis ist der Kurs selbst.** „Im Klassenzimmer" serviert im zweiten und dritten Durchgang Rucksack, Regal, Tasche, Tisch, Lampe und Uhr — den Flohmarkt. „Freizeit und Hobbys" kommt im dritten Durchgang auf **2 von 7** eigenen Wortfeldwörtern (dafür „Das Kind von meiner Schwester …" und „Mein Bruder und ich …"), „Im Café" auf **3 von 7** (dafür „Die Firma braucht eine Verkäuferin." und „Die Kollegin braucht ein Handy."). Der Standard verkauft **situative** Lektionen; es gibt einen Relevanz-**Score**, aber keinen **Boden**, und die Vorversuchssperre räumt die situativen Items zuerst ab. **Die Gegenprobe ist gerechnet**: dieselbe Ziehung mit `bestimmter`/`unbestimmter`/`artikel` in der Stoppliste und sonst keiner Änderung gibt L5 Versuch 1 **6/7** auf der eigenen Grammatik und **7/7** eigenes Wortfeld, Versuch 2 **5/7** und **5/7**, beide `complete: true`. Versuch 3 bleibt auch dann dünn — das ist dann ein echter Inhaltsmangel und keine Kappe. | **Erstens** die Klammer für **beide** Wächter dasselbe sein lassen: in `taskShape` wird die Klammer schon vor der Maske zu `zzmark…` — dieselbe Ersetzung gehört vor `itemLemmas`, d. h. `contentLemmas` liest den Prompt **ohne** `(…)`. Eine Zeile: `export const itemLemmas = (item) => contentLemmas(\`${String(item.questionDe || '').replace(/\([^()]*\)/g, ' ')} ${item.answer || ''}\`);` **Zweitens** `complete` zu einem Wächter machen, mit derselben Begründung wie `relaxUsed`: in `tests/lesson-engine.test.mjs` neben `MAX_CAP_STARVED_LEKTIONEN` ein zweiter Ratchet `MAX_OFF_PRIMARY_BLOCKS = 0` über `practiceReport(plan)` **aller** `ATTEMPTS`, der jeden Block mit `complete === false` auflistet (Lektion, Versuch, `primaryCount`, `primaryTarget`) — ein Motor, der seine eigene Unvollständigkeit protokolliert und nicht gelesen wird, ist kein Bericht, sondern ein Kommentar. **Drittens** einen Situationsboden einziehen, wie es `PRIMARY_MIN` für die Grammatik tut: `export const SITUATION_MIN = 4;` und in `pickPracticeItems` eine Füllstufe vor Schritt 3, die zuerst Items mit `relevanceScore(it, ownTerms, ∅) > 0` nimmt, bis vier erreicht sind — dazu ein Test über alle 36 Blöcke mit Ratchet auf die dann gemessene Zahl, gesenkt nur durch Items, nie durch eine weichere Zahl. **Viertens** die Reparatur, die kein Motor leisten kann: L5 braucht Items auf `definite-articles`, die **nicht** den Rahmen „___ Nomen ist Adjektiv. (bestimmter Artikel)" tragen — von 17 servierbaren tun das acht —, L7 und L9 brauchen Freizeit- bzw. Café-Items für den dritten Durchgang. | MAJOR |
| `pickPracticeItems` in `src/lib/lesson/buildLesson.js:465–660` — es gibt **keine** Prüfung, ob die Antwort eines gewählten Items im Prompt eines anderen steht; kein Test in `tests/lesson-engine.test.mjs` sucht danach | Eigene Lecksuche, wortgenau, Klammercues ausgenommen: **17 Paare in 11 von 36 Blöcken**, dazu ein 18., das der strenge Filter nur am Fragezeichen verliert. Die vier schlimmsten im Wortlaut: · **L11 Versuch 1**, Aufgabe 1 `extra-a11-l11-07` „Kommst du am Freitag ___? (mitkommen)" → `mit`, Aufgabe 7 `extra-a11-l11-01` „Bilden Sie die Frage: [mitkommen / du / am Freitag]" → **„Kommst du am Freitag mit?"** · **L1 Versuch 2**, **gegenseitig**: `61b961a8` „Lesen Sie die Buchstaben: W-I-L-L-K-O-M-M-E-N. Schreiben Sie das Wort: ___" → „Willkommen" und `extra-a11-l01-14` „Wie buchstabiert man **Willkommen**? ___" → „W-I-L-L-K-O-M-M-E-N" · **L11 Versuch 3**: `extra-a11-l11-09` „Was hast du gestern gemacht? — Ich habe ___. (arbeiten, fester Ausdruck)" → `gearbeitet`, daneben `extra-a11-l11-10` „Bilden Sie den Satz: [ich / haben / gestern / **gearbeitet**]" · **L6 Versuch 3**: `extra-a11-l06-04` „Der Chef braucht ___ Computer." → `einen`, daneben `extra-a11-l06-12` „Korrigieren Sie: „Ich brauche **einen** Pause."" · weiter: L1 A1, L2 A3 (2), L3 A3 (2), L6 A1 (2), L8 A1, L12 A1 (3), L12 A3 | **Eine Aufgabe des Siebeners löst eine andere, und dafür gibt es keine Regel.** Der Motor hat fünf Diversitätsachsen (Lemma, Antwortschlüssel, Form, Übertrag, Vorversuch) und keine einzige, die fragt, ob der Lernende die Lösung schon gedruckt vor sich hatte. Drei Größenordnungen. **(a) Das gegenseitige Paar in L1 Versuch 2 ist der Extremfall:** zwei Items derselben Sieben sind exakte Umkehrungen voneinander; welches der Lernende zuerst sieht, entscheidet, dass das andere keine Aufgabe mehr ist. Beide sind `zzbuchstaben`-Items, also genau die Gruppe, die diese Runde zusammengefasst hat — die Formkappe sieht sie als **zwei** Formen (ein `fill_blank:zzbuchstaben schreiben sie das wort _`, ein `fill_blank:wie buchstabiert man · _`) und hat damit recht: es sind zwei Aufgabenformen und **eine** Information. **(b) L11 Versuch 1 ist der häufige Fall:** die Lücke „Kommst du am Freitag ___?" und der Wortsalat, dessen Lösung dieser Satz ist, stehen als Aufgabe 1 und Aufgabe 7 in einer Sieben. Die Satzklammer ist L11s ganzes Thema; sie wird zweimal an **demselben** Satz geübt, einmal davon gratis. **(c) Es ist die Kehrseite von MAJOR 1 und derselbe Mechanismus:** je enger die Formkappe, desto mehr Gewicht bekommt die Zwillingsschreibung („dieselbe Aufgabe anders eingekleidet"), und diese Runde hat **54** neue Items geschrieben, um die engere Kappe zu bedienen. Zwei der drei schlimmsten Lecks laufen über Items aus dieser Runde. | Eine sechste Achse, in `fits()`, wo alle anderen auch stehen. **Erstens** eine Hilfsfunktion neben `answerKey`: `const answerWords = (it) => new Set(String(it.answer||'').toLowerCase().replace(/[^a-zäöüß0-9- ]/g,' ').split(/\s+/).filter((w)=>w.length>2 && !LEMMA_STOPWORDS.has(w)));` und `const promptText = (it) => String(it.questionDe||'').replace(/\([^()]*\)/g,' ').toLowerCase();` **Zweitens** in `fits()`, **vor** der Formkappe und mit ihr zusammen auf Leiterstufe 4 aufgebbar: ein Item passt nicht, wenn **alle** seine Antwortwörter im Prompt eines schon gewählten Items stehen — oder alle Antwortwörter eines gewählten Items in seinem eigenen Prompt. Das fängt beide Richtungen des L1-Paares und das L11-Paar und lässt „Nach Donnerstag kommt ___." neben einem Item mit der Antwort „Donnerstag" durch, wenn dort mehr als ein Wort produziert wird. **Drittens** der Test als Klasse, nicht als Id-Liste: in `tests/lesson-engine.test.mjs` über **alle 36 Blöcke** `assert.deepEqual(leaks, [])` mit einer Fehlermeldung, die Paar, Antwort und Prompt ausschreibt — und die vier oben gemessenen Paare als `ANSWER_LEAK_PROBES`-Fixture, die von der Prüffunktion **alle** gemeldet werden müssen. | MAJOR |
| `tests/rule-card-overrides.test.mjs:1349–1380` (`CONSTRUCTION_PATTERNS`, `introducedAt`) ↔ `src/data/curricula/a11.js` `hoeren.lines` / `sprechen.readAloud` ↔ `scripts/validate-curriculum.mjs` RULE 15 (Ratchet 12) ↔ dem Kartenschnitt dieser Runde in `scripts/rule-card-overrides.mjs` | Der Guard: „A course defers CONSTRUCTIONS … `ein`/`eine` before a noun is Lektion 6, a finite verb with its prefix at the end of the sentence is Lektion 11, `mein`/`dein` before a noun is Lektion 12" · die Wirkung dieser Runde auf die L3-Karte, im Diff: `- 'Beispiele: Wie alt ist er? Er ist zwanzig. **Meine Schwester ist noch jung.**'` / `+ 'Beispiele: Wie alt ist er? Er ist zwanzig.'` · und im Curriculum, unverändert: L3 `hoeren: { kind: 'dictation', lines: [3, 5] }`, Zeile 3 = **„Er ist zwanzig. Meine Schwester ist noch jung."** · eigene Messung, dieselben drei Muster über alle Diktat- und Vorlesefenster: **L3 Diktat Zeile 3** „Meine Schwester" (L12) · **L3 Vorlesen Zeile 1** „ein Bruder", „eine Schwester" (L6), „meine Eltern", „meine Geschwister" (L12) · **L3 Vorlesen Zeile 6** „dein Bruder" (L12) · **L7 Diktat Zeile 2** „Mein Hobby" (L12) · **L8 Vorlesen Zeile 1** „Ich stehe um sechs Uhr auf." (L11) — **acht Stellen in vier Lektionen** | **Der Guard ist an der Stelle gebaut worden, an der der Lernende liest, und nicht an der, an der er produziert — und die Runde hat den Widerspruch mit der Schere gelöst statt mit einer Regel.** Drei Messungen. **(a) Derselbe Satz ist zu schwer zum Lesen und leicht genug zum Schreiben.** „Meine Schwester ist noch jung." darf seit dieser Runde nicht mehr als **Beispiel** auf der L3-Regelkarte stehen, weil der Possessivartikel Lektion 12 ist; als **Diktat** von Lektion 3 muss der Lernende ihn Buchstabe für Buchstabe hinschreiben, und `checkAnswer` bewertet ihn. Eine Karte zu kürzen, deren Satz eine Zeile weiter geprüft wird, ist keine Schließung, sondern eine Verschiebung. **(b) Das Vorlesen ist die härtere Fläche, nicht die weichere.** L8 Vorlesen Zeile 1 ist „Ich stehe um sechs Uhr auf." — exakt die Sonde `Stehen Sie um sechs auf?`, die der neue Guard auf einer L10-Karte **fangen muss** (`DEFERRED_CONSTRUCTION_PROBES`), drei Lektionen vor der Satzklammer, und sie wird bewertet (`readaloud.mjs`). **(c) Die Zahl ist bekannt und wird falsch gelesen.** RULE 15 meldet dieselben zwölf Stellen seit Runde 5 und wurde von Runde 5 bis 10 als „nicht material" durchgewinkt — mit der Begründung, CONTRACT §2 erlaube dem Lernenden, eine Form zu **treffen**, bevor sie gelehrt wird. Das war richtig, **solange derselbe Kurs die Form auch auf einer Karte zeigen durfte**. Seit dieser Runde darf er das nicht mehr, und damit ist RULE 15 keine Toleranzzahl mehr, sondern die Liste der Stellen, an denen der Kurs die eigene Aufschubentscheidung an der teuersten Fläche bricht. | Ein Begriff von „aufgeschobene Konstruktion", nicht zwei. **Erstens** `CONSTRUCTION_PATTERNS` aus der Testdatei in ein eigenes Modul ziehen (`src/data/curricula/constructions.js`, samt `introducedAt`, das die einführende Lektion aus dem Curriculum liest) und aus **drei** Stellen importieren: dem Kartenguard, `scripts/validate-curriculum.mjs` (RULE 15) und dem `minLektion`-Stempel in `scripts/build-lesson-pool.mjs`. Karte, Item und Dialog beantworten dann dieselbe Frage mit demselben Code. **Zweitens** RULE 15 auf die **Produktionsflächen** einschränken und auf die Konstruktionen ausweiten: gemeldet wird eine Zeile, die der Lernende **diktiert oder vorliest** und die ein Muster trägt, dessen Lektion nach der eigenen liegt — heute acht, Ratchet auf 8, und er geht nur durch Fensterverschiebung nach unten. **Drittens** die Fenster verschieben, nicht die Dialoge: L3 `hoeren.lines` von `[3, 5]` auf `[5, 7]` (Zeile 5 „Sie sprechen Arabisch und Deutsch.", Zeile 7 „Ja, er spricht Englisch und Deutsch." — beides genau die Personalpronomen, die L3 lehrt) und `sprechen.readAloud` von `[1, 6]` auf `[2, 7]`; L7 `hoeren.lines` Zeile 2 → Zeile 4; L8 `sprechen.readAloud` Zeile 1 → eine Uhrzeitzeile. Der Dialog bleibt, wie er ist — er darf die Formen **sagen**, das ist CONTRACT §2. **Viertens** der Test als Klasse: `tests/curricula.test.mjs` prüft für jede Lektion jedes Levels, dass keine Zeile in `hoeren.lines ∪ sprechen.readAloud` ein Muster trägt, dessen einführende Lektion später ist — mit den acht heutigen Stellen als Fixture, die alle gemeldet werden müssen. | MAJOR |

---

## Status der Befunde aus Runde 10

Verifiziert an den Artefakten (gebauter Pool, alle drei Ziehungen, gebaute
`ruleCards.mjs`, die vier gebauten Checkpoints, der Kartenindex, Validator-Ausgabe,
Testlauf, Lint, und eine eigene Durchrechnung des Anmeldewegs), nicht am Diff.

| # | Runde-10-Befund | Status | Beleg |
|---|---|---|---|
| 1 | `taskShape` misst den Schlüssel, nicht die Aufgabe: fünf buchstabengleiche L1-Sätze sind fünf Formen, sechs umformulierte L10-Klammern sechs, `alt`/`gut`/`neu` trennen Rahmen, die die Wortzahl trennt | **geschlossen, vollständig, und am Material besser als verlangt** | Alle drei Teilbefunde sind **vor** der Wortmaske geschlossen: `SPELLED_RE` macht eine buchstabierte Folge zu **einem** Token, `TASK_MARKS` (zehn Kategorien, aus 79 Klammern des Pools abgelesen, erste Übereinstimmung gewinnt) macht die Klammer zu ihrer **Kategorie**, `WORD_LIST_RE` + `listMark` machen den Wortsalat zu seiner **Längenklasse**, und `SHAPE_CONTENT_WORDS` zieht `alt`/`gut`/`neu` aus der Stoppliste **nur für die Maske**. Nachgerechnet: die fünf `SPELLED_PROBES` ergeben **einen** Schlüssel, die vier umformulierten Verb-zuerst-Sonden **einen**, `(Adjektiv)` und `(etwas ganz anderes)` **einen**. Meine eigene, gröbere Rahmenmessung — Klammer ganz weg, Liste zu einer Marke, `·`-Läufe zusammengezogen, Typ ignoriert — findet über **alle 36 Blöcke** als größte Gruppe **3 von 7** (L7 A1: drei Konjugationslücken, also die Grammatik der Lektion selbst; L10 A2: drei Wortsalat-Fragen) gegen 5 von 7 in Runde 10. Und die sechs L10-Items sind nicht umetikettiert, sondern **umgeschrieben**: `-17/-19/-21` sind „Bilden Sie den Satz mit dem Verb auf Position 1: „Der Zug fährt nach Österreich."", `-18/-20/-22` sind „Die Antwort ist: „Ja, der Bus hat Verspätung." Schreiben Sie die Frage." — zwei echte, prüfungsnahe Operationen, sauberes Deutsch, und die sachfremde Klammer „(Die Stimme steigt am Ende.)" auf einer geschriebenen Aufgabe ist weg. **Rest:** keiner an dieser Regel; die Nebenwirkung auf die Lemmakappe ist MAJOR 1 und ein anderer Wächter. |
| 2 | Der Zyklus ist drei Versuche lang, die Wächter laufen über `[1, 2]`; L6 A3 gibt sieben Plätze an eine Artikelschablone, L4 A3 fünfmal `kostet`, L8 teilt im Wrappaar fünf von sieben | **geschlossen, vollständig und maschinell gegen Rückfall gesichert** | `ATTEMPTS = Array.from({ length: ATTEMPT_CYCLE }, …)` und `ATTEMPT_PAIRS = ATTEMPTS.map((_, i) => [i, (i + 1) % ATTEMPTS.length])` stehen einmal oben in der Datei; ein eigener Test liest die **Quelle** der Testdatei und verbietet die Literale `of [1, 2])` und `[[0, 1], [1, 2]]`. Eigene Messung über alle 36 Blöcke: **jeder** Block füllt sieben, **jeder** hat `relaxUsed = 0`, **jeder** hat **7 von 7** verschiedenen `taskShape`-Schlüsseln, **jeder** hat ≥ 4 echte Drills (Minimum 4, Maximum 7). Die größte Antwortschlüsselgruppe ist **2** (Kappe 2), die größte Lemmagruppe **2**. Überschneidung in **allen drei** Paaren, 1→2, 2→3 **und 3→1**: L8 je 2 (seine zwei `mustCover`-Items), L12 im Wrappaar 1 (`extra-a11-l12-08`, die Höflichkeitsform — genau der Exaktvorzug, den diese Runde eingebaut hat), sonst **0 in 33 von 36**. L6 A3 zieht heute sechs echte Drills in sieben Formen, L4 A3 `kostet` genau einmal. **Rest:** `practiceReport().complete` wird weiter von niemandem gelesen, und darunter liegt L5 — MAJOR 1. |
| 3 | Der Vorgriffs-Guard nimmt **jedes** Funktionswort aus; „ein und eine stehen bei einer Sache." und die Satzklammer auf der L10-Karte gehen durch, und `minLektion` lässt „von meiner Schwester" in L3 ziehen | **geschlossen, und es ist die sauberste Schließung dieser Reihe** | Die pauschale Lizenz ist weg. `taughtUpTo` bindet ein Funktionswort an die **erste Lektion, deren Dialog oder Notice es sagt** (`functionWordsUsedIn`); frei bleiben nur Zahlen, Partikeln/Konjunktionen/Fragewörter und die Nominativ-Subjektpronomen, **jede der drei Gruppen einzeln gegen den gebauten Pool begründet**. Nachgemessen an den Dialogen: `dem`, `mit`, `meiner`, `einen` → erste Nennung **L3**, `den` → **L4**, `von` → **L2**. Damit ist „Der Mann **von meiner** Schwester kommt auch aus Marokko." nicht wegdefiniert, sondern **L3s eigene Dialogzeile 9**, und `extra-a11-l03-12/-15` sind belegt statt geduldet. Der Kartenguard hat die Ausnahme verloren und einen **Konstruktionsguard** dazubekommen (unbestimmter Artikel + Nomen → L6, Satzklammer → L11, Possessivartikel + Nomen → L12, einführende Lektion **aus dem Curriculum gelesen**, zwei begründete Verengungen für Metasprache und für `ihr`/`Ihr`); die sechs Sätze, an denen Runde 10 den Befund festgemacht hat, stehen als `DEFERRED_CONSTRUCTION_PROBES`-Fixture in der Datei und werden **alle** gefangen. Sieben Kartenzeilen sind daraufhin umgeschrieben worden. RULE 11 ist von 49 auf 62 gestiegen, RULE 11b ist **0**. **Rest:** derselbe Guard gilt für die Karte und nicht für den Satz, den der Lernende diktiert — MAJOR 3; und eine der 13 neuen RULE-11-Zeilen ist neuer Text, nicht aufgedeckter Altbestand (Minor 4). |
| 4 | Die abgeleitete Versuchszahl wird beim Anmelden vernichtet: `mergeLocalProgress` schreibt alle Durchgänge in **einen** `INSERT`, `countCompletedRuns` zählt `created_at` | **geschlossen, vollständig, und Ende zu Ende nachgerechnet** | `recordLocalLesson` markiert jede Antwort mit ihrem `run` und führt `runStamps`; `trimAttempts` kappt **pro Lektion** und wirft **ganze** Durchgänge weg, ältester zuerst (die zweite Messung aus Runde 10); `planLocalMerge` ist rein und liefert **genau `runs` Batches** je Lektion, jeder mit eigenem `createdAt`, ein `runMarkerAttempt()` (`item_id: '__run__'`) dort, wo die Antworten getrimmt wurden; `runStampsFor` erzwingt **streng aufsteigende, verschiedene** Stempel (Gleichstand wird eine Millisekunde vorgeschoben, nie verworfen); `mergeLocalProgress` schreibt die Batches **sequenziell und awaited**; `logAttempts` fällt bei abgelehntem Stempel auf getrennte `now()`-Inserts zurück; `LessonPlayerPage` mischt **vor** dem Zählen, in **einem** Effekt statt in zweien. Der Lernende, durchgerechnet: ausgeloggt Durchgang 1 → `localRunCount 0` → **Versuch 1**; Durchgang 2 → **Versuch 2**; Anmeldung → zwei Batches, zwei `created_at` → `countCompletedRuns = 2` → **Versuch 3** (dasselbe, was er ausgeloggt bekommen hätte); eingeloggter Durchgang 3 → drei Batches → `1 + (3 % 3)` = **Versuch 1**. Der vierte Besuch zieht Versuch 1, der Zyklus wrappt korrekt. **Zum bewusst ausgelassenen Wiederholen-Knopf:** ich halte die Entscheidung für **richtig**. Der Rückblick hat einen Handlungsaufruf (Standard §4), und der Weg zurück existiert und ist nicht die Adresszeile: `CurriculumHomePage` rendert eine fertige Lektion als `done` und **verlinkt sie weiter** (`to={unlocked || isDone ? hrefFor(level, node) : null}`), der Spieler mischt und zählt beim Betreten neu. Ein zweiter CTA auf dem Rückblick hätte den Lernenden im Kreis geschickt, wo die Karte ihn weiterschickt. **Was fehlt, ist ein Satz, kein Knopf** (Minor 8). |

**Zusätzlich geprüft und weiterhin in Ordnung:** die **zwölf Schreibaufgaben**
(Aufgabentext, Leitpunkte bzw. Formularfelder und Wortgrenzen zwischen `a11.js` und
`netlify/functions/_shared/writingTasks.mjs` **deckungsgleich**, alle zwölf `taskKey`s in
der Serverbank; die einzige Differenz ist ein anderer Feldname für dasselbe — `kind`
„mitteilung" im Curriculum, `register` „formell"/„informell" auf dem Server, das eine für
die Oberfläche, das andere für die Zeichenuntergrenze); `anrede` in allen zwölf
Sprechaufträgen `'Sie'`; die zwölf pretests sprechen alle Sie; RULE 14 (Figuren) und
RULE 12 (Kann-Beschreibungen) als harte Regeln auf 0; die vier Checkpoints: **80** Items,
**0** Selbstablehnungen, **32 von 32** verschiedene Poolitems, **keine** doppelt genutzte
Dialogzeile, und die 16 Lesen-Aussagen weiterhin **0** Teilzeichenketten und **0**
Ein-Token-Abstände — was Runde 10 als vollständig geschlossen gemeldet hat, hält;
**395** Wiederholungskarten, **0** Selbstablehnungen gegen `gradeTypedReview`;
`a11.audio.js` weiterhin der leere Manifest-Stub, alle Hörflächen fallen ehrlich auf die
Computerstimme zurück (Eigentümeraufgabe, kein Befund); **RULE 13** (4 Sprechaufträge ohne
Mission) unverändert vertretbar. **Die Erinnerungsmail duzt weiterhin, und das lasse ich
als Eigentümerentscheidung stehen — kein MAJOR**, wie in Runde 5 bis 10; die Notiz darüber
im Kopf von `course-reminder.mjs` fehlt zum siebten Mal (Minor 8).

**Zu den 54 neuen Handitems und den sechs L10-Neufassungen, einzeln gelesen.**
Siebenundvierzig sind gutes, natürliches, A1-taugliches Deutsch mit richtigem Schlüssel und
richtigen `accepted`-Varianten, und die L10-Neufassungen sind die beste Arbeit dieser Runde:
`extra-a11-l10-18` („Die Antwort ist: „Ja, der Zug hat Verspätung." Schreiben Sie die
Frage." → „Hat der Zug Verspätung?") ist eine echte Umkehroperation und keine Umetikettierung,
und `extra-a11-l10-17/-19/-21` machen aus dem Aussagesatz die Entscheidungsfrage — genau die
Operation, die *Start Deutsch 1* Sprechen Teil 3 verlangt, und alle drei tragen denselben
Formschlüssel, sind also von der Kappe auf einen Platz beschränkt. Auch die neuen L8-Items
sitzen: `um` vs. `am` (`-09` bis `-11` gegen `-15`/`-16`) und die beiden Fehlerkorrekturen
„Der Arzt ist **um** Freitag." / „Der Termin ist **um** Dienstag." drillen den Kontrast, an
dem A1-Lernende tatsächlich scheitern. Sieben sind schwächer, und sie zerfallen in drei
Gruppen. **Erstens der falsche Satz:** `extra-a11-l06-14` — BLOCKER 1. **Zweitens die
Zwillinge, jetzt in Serie:** `l08-09/-10/-11` sind dreimal „Der Termin ist ___ acht/neun/
sieben Uhr." → `um`; `l04-22/-23` zweimal „Ich kaufe ___ Lampe/Uhr."; `l04-27/-28` zweimal
„___ Lampen/Taschen sind alt."; `l01-16/-17` zweimal „Gute Morgen/Abend, Frau Kaya!";
`l01-18/-23` zweimal „[ich / buchstabieren / Chakiri|Ana]". Die Formkappe hält sie
zuverlässig auseinander — je Sieben landet höchstens einer —, aber es sind **Items, die auf
den Schlüssel hin geschrieben wurden**, und über drei Durchgänge gesehen füllen sie
Plätze, die situatives Material besser gefüllt hätte (MAJOR 1c). **Drittens die Passform:**
`extra-a11-l04-22/-23/-24` tragen die Klammer „(der, die oder das?)" auf einem
**Akkusativobjekt** („Ich kaufe ___ Lampe."). Die Antworten sind richtig, weil alle drei
Nomen feminin oder neutral sind — die Lücke ist also nur deshalb harmlos, weil kein
maskulines Nomen darin vorkommt, und die L4-notice sagt selbst „Nach **kaufen** wird der zu
**den**". Ein viertes Item dieser Machart mit `der Stuhl` wäre ein Blocker; die Klammer
sollte den festen Ausdruck nennen, nicht das Genus (Minor 5). `extra-a11-l01-22` („Welcher
Buchstabe heißt Eszett? ___" → `ß`, `accepted: ["ß"]`) verlangt ein Zeichen, das auf keiner
nichtdeutschen Tastatur liegt und dessen Großform der Prüfling nicht kennt — sachlich
richtig, praktisch ein Stolperstein (Minor 7).

---

## Bewertungsübersicht (Runde 11, Runde 10 in Klammern)

| Lektion | Deutsch | Niveau | Dialog | Prüfung | Kohärenz | Σ /25 |
|---|---|---|---|---|---|---|
| 1 Hallo, ich bin … | 4 (4) | 4 (4) | 5 (5) | 4 (4) | **4** (3) | **21** (20) |
| 2 Ich bin Studentin | 4 (4) | 4 (4) | 4 (4) | 4 (4) | 4 (4) | **20** (20) |
| 3 Meine Familie | 4 (4) | 4 (4) | 4 (4) | 4 (4) | 4 (4) | **20** (20) |
| 4 Auf dem Flohmarkt | 4 (4) | 4 (4) | 5 (5) | 4 (4) | **4** (3) | **21** (20) |
| 5 Im Klassenzimmer | 5 (5) | 4 (4) | 5 (5) | 4 (4) | **2** (3) | **20** (21) |
| 6 Der erste Tag im Büro | **3** (5) | 4 (4) | 4 (4) | 4 (4) | **3** (2) | **18** (19) |
| 7 Freizeit und Hobbys | 4 (4) | 4 (4) | 5 (5) | 4 (4) | **3** (4) | **20** (21) |
| 8 Termine und Uhrzeit | 4 (4) | 4 (4) | 5 (5) | 4 (4) | **4** (3) | **21** (20) |
| 9 Im Café | 5 (5) | 4 (4) | 5 (5) | 4 (4) | **3** (4) | **21** (22) |
| 10 Am Bahnhof | 5 (5) | 4 (4) | 5 (5) | 4 (4) | **4** (3) | **22** (21) |
| 11 Mein Tag | 4 (4) | 4 (4) | 4 (4) | 4 (4) | **3** (4) | **19** (20) |
| 12 Feste feiern | 5 (5) | 4 (4) | 5 (5) | 4 (4) | 4 (4) | **22** (22) |

Kursmittel **20,4/25** (Runde 10: 20,5 · Runde 9: 20,5 · Runde 8: 20,4 · Runde 7: 20,3 ·
Runde 6: 19,9 · Runde 5: 19,7 · Runde 4: 19,9 · Runde 3: 20,4 · Runde 2: 19,8 · Runde 1:
17,2). Vier Lektionen besser, fünf schlechter, drei gleich.

**Zur Lesart, und sie ist die eigentliche Aussage dieser Runde.** Die Gewinner sind die
Lektionen, die neue Items **und** die neue Formmessung bekommen haben: **L1 (20 → 21)**,
weil die fünf buchstabengleichen Sätze aus Runde 10 nicht mehr ziehbar sind und 14 neue
Items den Buchstabierteil auf Namen, Grüße und die höfliche Frage verbreitern; **L4
(20 → 21)** mit zwanzig neuen Genusitems und **7/7** eigenem Wortfeld in allen drei
Durchgängen; **L8 (20 → 21)** mit zehn neuen `um`/`am`-Items und dem Kontrast, der für
Hören Teil 1 zählt; **L10 (21 → 22)**, weil aus sechs Umetikettierungen zwei echte
Operationen geworden sind. Die Verlierer sind **keine Verschlechterung des
Lektionstextes**, sondern die Rechnung für die engere Formkappe: **L5 (21 → 20)** verliert
zwei Kohärenzpunkte an die Klammer, die für die Lemmakappe Inhalt geblieben ist und der
Lektion in zwei von drei Durchgängen das eigene Grammatikthema nimmt; **L7 (21 → 20)** und
**L9 (22 → 21)** an den dritten Durchgang, der ihr Wortfeld auf 2 von 7 bzw. 3 von 7
zurücklässt; **L11 (20 → 19)** an die beiden Lecks, die in zwei von drei Durchgängen eine
Aufgabe durch eine andere beantworten. **L6 (19 → 18)** ist der einzige echte
Materialverlust und der Grund, warum dieser Bericht einen BLOCKER trägt: die Lektion hat
zehn gute neue Items bekommen (Kohärenz 2 → 3) und **einen falschen Erklärungssatz**, der
in derselben Sieben steht wie das Item, das ihn widerlegt. **Stärkste:** L10 und L12 (je
22). **Schwächste:** L6 (18).

---

## Würde ich meinen Namen jetzt daraufsetzen?

**Auf die Checkpoints, auf die Dialoge, auf die Schreib- und Sprechaufträge und auf den
Motor: ja. Auf den Kurs als Ganzes: nein, und diesmal aus einem Grund, der in elf Runden
nicht vorgekommen ist.**

Ich habe alle vier Punkte aus Runde 10 an den Artefakten nachgeprüft: **alle vier
vollständig geschlossen**, und drei davon so, wie eine Regel geschlossen gehört. Die
Formachse misst jetzt die Aufgabe: fünf buchstabengleiche Sätze sind ein Schlüssel, sechs
Umschreibungen sind ein Schlüssel, und meine eigene, absichtlich gröbere Messung kommt über
alle 36 Blöcke auf höchstens drei gleiche Rahmen in einer Sieben, wo Runde 10 fünf gefunden
hat. Die Wächter laufen über den ganzen Zyklus, und ein Test verbietet die Literalliste, die
den Befund erzeugt hat — ein Guard, der seinen eigenen Rückfall verbietet, ist mehr als eine
Reparatur. Die Funktionswörter sind von einer Liste auf **erste Nennung** umgestellt: das
ist die Schließung, die ich in Runde 10 verlangt habe, in einer besseren Form als die
vorgeschlagene, weil sie keine zweite Liste erfindet, sondern den Dialog fragt. Und die
Versuchszahl überlebt das Anmelden — ich habe den Lernenden Schritt für Schritt
durchgerechnet, zwei Gastdurchgänge, Anmeldung, ein eingeloggter Durchgang, und der vierte
Besuch zieht Versuch 1, also genau den Zyklus, den er ohne Konto bekommen hätte.

Der fehlende Wiederholen-Knopf ist **richtig** entschieden. Der Rückblick hat einen
Handlungsaufruf, der Weg zurück in die Lektion steht auf der Kurskarte, und eine fertige
Lektion bleibt dort anklickbar. Ein zweiter CTA hätte den Lernenden auf der Stelle gehalten,
wo das Produkt ihn weiterschicken will.

**Und trotzdem: der Kurs sagt einem Anfänger in Lektion 6 einen falschen Satz über das
Deutsche, und zwei Aufgaben später bestraft er ihn dafür, ihn geglaubt zu haben.** „Nach
brauchen wird ein zu einen" ist keine Schlamperei in einer Randzeile; es ist die einzige
Kasusform, die A1.1 überhaupt produziert, sie ist in Schreiben Teil 2 und Sprechen Teil 3
prüfbar, die richtige Fassung steht dreimal im selben Repository, und das falsche Item ist
in **dieser** Runde geschrieben worden, um eine Formkappe zu bedienen. Das ist der Preis,
den die Runde bezahlt hat, und er steht in der Bewertungstabelle: **54 neue Handitems sind
sehr viel Text für einen Durchgang, und der Text ist nicht überall so sorgfältig wie der
Motor, in den er eingefüllt wurde.** Zwei der drei MAJOR sind Kehrseiten desselben
Vorgangs — die Lemmakappe zählt weiter eine Klammer mit, die der Formschlüssel seit dieser
Runde als Kategorie liest, und in elf von 36 Blöcken steht die Lösung einer Aufgabe im
Prompt einer anderen derselben Sieben, zweimal in Items, die diese Runde geschrieben hat.
Der dritte ist der ältere Satz in neuer Schärfe: **eine Regel, die nur dort gilt, wo sie
billig zu prüfen ist, ist keine Regel.** Der Konstruktionsguard hat „Meine Schwester ist
noch jung." von der L3-Karte gestrichen und lässt denselben Satz als Diktat derselben
Lektion stehen.

Die Arbeit ist überschaubar und vollständig ausgeschrieben: zwei Erklärungszeilen plus ein
Lint, der behauptete Regeln auf ihre Bedingung prüft, mit den fünf gemessenen Sätzen als
Gegenprobe; eine Zeile in `itemLemmas`, die der Lemmakappe dieselbe Klammer wegnimmt, die
`taskShape` ihr schon weggenommen hat, dazu `complete` als Ratchet und ein
`SITUATION_MIN`-Boden; eine sechste Achse in `fits()`, die fragt, ob die Antwort schon auf
dem Bildschirm stand; und ein gemeinsames Konstruktionsmodul für Karte, Item und
Dialogfenster, gefolgt von vier verschobenen Diktat- und Vorlesefenstern. Das ist wieder
etwa ein Tag. Diesmal bitte ich ausdrücklich **nicht** um neue Items: L5, L7 und L9
brauchen im dritten Durchgang situatives Material, und es sollte langsamer und mit einer
zweiten Lektüre geschrieben werden als die 54 dieser Runde.

---

**Minors (acht Zeilen, nicht erfasst):** (1) Die Buchstabenkarte mischt weiter Groß- und
Kleinschreibung in der Namensliste (`be, ce, de` neben `Jot, Vau, Ypsilon, Zett`, dazu
`W we` im `content` gegen „V heißt Vau, **W heißt We**" in `commonMistakes`), und kein
Item prüft Q — die drei neuen Buchstabenitems dieser Runde fragen Z, Y und ß.
(2) Die `-chen/-lein`-Zeile der L4-Karte („das **Mädchen**, das **Brötchen**") lehrt zum
dritten Mal eine Endungsgruppe, von der der Kurs **null** Wörter enthält (nachgezählt im
gebauten Pool: 0 Items mit `Mädchen` oder `Brötchen`); sie sollte ersatzlos fallen.
(3) Im Lesen-Teil: `a1.1-cp1-lesen-3` („Der **Student** spricht Englisch und Deutsch.",
Schlüssel *Falsch*) ist im Text nicht widerlegt, sondern unerwähnt, und
`a1.1-cp3-lesen-1`/`-4` sind zweimal dieselbe Umformung mit demselben Sprecher und
demselben Verb („Ana hat Durst." / „Ana hat Hunger.") — zwei von vier Items eines Papiers,
unverändert seit Runde 10. (4) Die Begründung zur RULE-11-Anhebung (49 → 62) sagt „none of
them new text"; das stimmt für 13 der 14 Paare, aber `extra-a11-l01-15: am` ist ein Item
**dieser** Runde — die Zeile sollte korrigiert werden, sonst ist der nächste Anlass zum
Anheben leichter zu begründen, als er sein sollte. (5) `extra-a11-l04-22/-23/-24` setzen die
Klammer „(der, die oder das?)" auf ein Akkusativobjekt („Ich kaufe ___ Lampe."); die
L4-Karte nennt den festen Ausdruck („Nach kaufen wird der zu den"), die Klammer sollte es
auch tun. `extra-a11-l12-17` trägt weiter den metasprachlichen Cue „(Adjektiv)" und wird in
L12 Versuch 1 gezogen. (6) Der Topic-Slug `numbers` erzeugt im Wiederholungsindex weiter eine
Doppelkarte: `pattern:numbers` ist Zeichen für Zeichen `pattern:verb-sein`, gemessen über
alle 395 Karten die **einzige** Dublette. (7) Acht gezogene Items tragen als `questionEn`
die deutsche Frage statt einer Übersetzung (`0fe8146d`, `34957c67`, `da9e486f`, `9fd52b13`,
`dd1c3d60`, `e0ac0425`, `1ce4f35f`, `cb35f9d0`); **13** gezogene Einwortantworten
akzeptieren nur die große Schreibung, während andere beide führen; und
`extra-a11-l01-22` verlangt als einzige Antwort das Zeichen `ß`. (8) L6 nutzt Dialogzeile 2
sowohl als Diktat- **als auch** als Vorlesezeile — die einzige Lektion, in der sich die
beiden Fenster überschneiden; `tests/lesson-engine.test.mjs:622` nennt die
Höflichkeitsitems weiter namentlich; `src/pages/A11PhasePage.jsx` duzt weiter auf drei
Zeilen; die Eigentümerentscheidung zur du-Anrede der Erinnerungsmail ist zum siebten Mal
nirgends aufgeschrieben; der Rückblick sagt dem Lernenden nirgends, dass ein zweiter
Durchgang **andere** Aufgaben bringt — drei Ziehungen, die niemand ankündigt; und
`tests/lesson-extra-items-a12.test.mjs:464` ist weiter der einzige übersprungene Test der
Suite — korrekt geführt, aber er sollte mit A1.2 wieder anspringen.

BLOCKER: 1 · MAJOR: 3
