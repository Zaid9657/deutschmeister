
# A1.1 — DaF-Fachreview #6, nach Runde 6 (2026-09-12)

Reviewer: DaF-Lehrkraft (Erwachsenenbildung, Integrationskurs) und Prüferin für
Goethe-Zertifikat A1 *Start Deutsch 1*. Sechster Durchgang. Basis:
`REVIEW-daf-2026-09-12.md` (5 BLOCKER · 45 MAJOR), `REVIEW-daf-2-2026-09-12.md`
(1 BLOCKER · 35 MAJOR · 44 MINOR), `REVIEW-daf-3-2026-09-12.md` (3 BLOCKER · 11 MAJOR),
`REVIEW-daf-4-2026-09-12.md` (3 BLOCKER · 11 MAJOR) und `REVIEW-daf-5-2026-09-12.md`
(3 BLOCKER · 11 MAJOR), angewendet in `origin/main` @ `37b689a`
(„A1.1 round 6: close DaF review #5; A1.2 curriculum draft", PR #121).
Gemessen wurde ausschließlich der Commit-Stand in einem sauberen Worktree; die
unversionierte A1.2-Poolarbeit im Arbeitsbaum ist nicht Gegenstand dieses Reviews.

Gemessen, nicht angenommen:

* `src/data/curricula/a11.js` — alle zwölf Lektionen, jedes Feld (situation,
  handlungsfeld, canDo, examTeile, wortfeld inkl. `wordId`, dialog, pretest, notice,
  phonetik, hoeren, sprechen inkl. `anrede`, schreiben, links, practiceRule).
* **Die echte Ziehung**: `planPractice(CURRICULUM_A11, a11.json, 1)` **und Versuch 2**
  importiert, alle sieben Items jeder Lektion mit
  `questionDe`/`questionEn`/`options`/`answer`/`accepted`/`explanationDe`/`caseSensitive`
  ausgedruckt und einzeln bewertet (**168 Items**). Pool: **351** Items, davon **124**
  handgeschrieben.
* Alle **124** Items in `src/data/lessonPools/a11.extra.json` einzeln gelesen.
* `drillsSlug()` aus `src/data/lessonPools/quality.js` über beide Ziehungen laufen
  lassen und jede Zahl gegen das gehalten, was der Lernende in dem Item **produziert**.
* `checkAnswer` + `tagError` + `isCaseTask` aus `src/lib/lesson/check.js` mit der NEUEN
  Semantik (`caseSensitive` ist nur noch das Item-Flag, `tagSentenceError` liest die
  Differenz) auf die kritischen Items mit je zwei bis vier plausiblen Lernerantworten
  nachgerechnet — und jedes Ergebnis zusätzlich durch `isItemCorrect()` des Checkpoints
  und `gradeTypedReview()` der Wiederholungsseite geschickt.
* `exclusionReason()` über beide Pooldateien: **0** Ausschlussgründe auf 351 + 124 Items.
* Die zwölf A1.1-Karten in der gebauten `netlify/functions/_shared/ruleCards.mjs`.
* Die zwölf Kursaufgaben in `src/data/writingTasks.js` gegen `schreiben` und gegen
  `netlify/functions/evaluate-writing.mjs` (Serverauflösung über `task_key`, `MIN_CHARS`).
* `buildMessage()` in `netlify/functions/course-reminder.mjs`.
* Die Chrome: `src/components/lesson/*.jsx`, `src/pages/lesson/*.jsx`,
  `src/pages/SpeakingPage.jsx` — und der Weg des Sprechauftrags **samt Anrede** bis in den
  System-Prompt (`speaking-session.mjs`, `speaking-turn.mjs`, `_shared/speakingAI.mjs`).
* **Alle vier Checkpoints gebaut**, alle 80 Items ausgedruckt und einzeln bewertet,
  inklusive der benoteten Schreibaufgabe und des Lesen-Lösungsschlüssels; dazu
  `scoreCheckpoint`, `itemCounts`, `ReviewPage.jsx` und `reviewService.buildCardIndex`.
* `node scripts/validate-curriculum.mjs` (RULE 10: 18/262 · RULE 11: 187 · RULE 12: 4 ·
  RULE 13: 4 · RULE 14: 0) und die Implementierung aller fünf Regeln, nicht nur ihre Ausgabe;
  die 187 Tokens auf gezogene / nur im Checkpoint stehende / ungezogene Items aufgeteilt.
* `npm test`: **684/684 grün**. `npm run lint`: 0 Fehler, 0 Warnungen.
  `npm run check:duplicates`: OK.

## Urteil in einem Absatz

**Runde 6 ist die erste Runde dieser Reihe, in der ich keinen einzigen Befund der
Vorrunde wieder aufmachen muss: alle drei Blocker und alle elf MAJOR aus Runde 5 sind
geschlossen, zehn davon als Prädikat mit poolweitem Test, und die Checkpoints — die
Fläche, die in fünf Runden niemand gebaut hatte — sind in einer Runde von der
schwächsten zur zweitbesten Fläche des Kurses geworden.** `missingSentenceArticle()`
fragt nach der Form statt nach dem Typ und hat die drei Satzbauitems repariert;
`cueAnswerMismatch()` und `metalinguisticPrompt()` sind zwei neue Prädikate mit je einem
gemessenen Treffer und einer ehrlich dokumentierten Ablehnung der zu breiten Variante;
`isCaseTask()` ist auf `item.caseSensitive === true` eingedampft und wird jetzt an
**allen drei** Bewertungsstellen übergeben; `tagSentenceError()` liest, *was* sich
unterscheidet (`Ich möchte einen Glas Wasser.` → **Kasus**, nicht mehr Verbstellung); der
Lesen-Teil zieht seinen Lösungsschlüssel und fälscht durch Ersetzung **im selben Text**;
der Schreiben-Teil jedes Checkpoints enthält jetzt die echte, KI-bewertete Kursaufgabe;
`arbeiten` steht im Wortfeld von L7 und der e-Einschub in dessen notice; die Anrede reist
bis in den System-Prompt des Coachs; RULE 11 liest endlich den gebauten Pool und meldet
ehrlich 187 statt beruhigend 9. **Und trotzdem unterschreibe ich nicht, weil die drei
schwersten Befunde dieser Runde zum sechsten Mal aus derselben Quelle stammen — nur ist
die Quelle diesmal enger und benennbar: eine Regel wurde als *Liste* geschlossen, wo sie
als *Prädikat* hätte geschlossen werden müssen, und zwei Optionen von `checkAnswer` sind
weiterhin von der Aufrufstelle abhängig statt vom Item.** Erstens ist `caseSensitive` ein
Opt-in, und genau **fünf** Items haben optiert: `0416dbbe` („Frau Müller, ___ sind sehr
freundlich.", eigene Erklärung: „Sie (großgeschrieben) ist das formelle Sie"), `b269d424`
(„Ist das ___ Adresse? **(Sie, Höflichkeitsform)**") und `10b25f5c` nehmen `sie`/`ihre`/`ihr`
als **typo = richtig**, während `extra-a11-l03-08` mit derselben Frage `wrong` gibt — und
zwei der lockeren Items stehen als `a1.1-cp4-bausteine-1` und `a1.1-cp4-schreiben-2` im
**benoteten Abschlusscheckpoint**; `src/data/curricula/a11.js` enthält das Wort
`caseSensitive` **null Mal**, also ist zusätzlich jede Wiederholungskarte generell
ausgenommen. Zweitens bestraft `extra-a11-l05-09` — wieder ein Reparaturitem der
Vorrunde, wieder in beiden Ziehungen von L5 — die *minimale* Korrektur:
„Korrigieren Sie: „Ein Schere ist hier."" nimmt nur *Die Schere ist hier.*, obwohl
**„Eine Schere ist hier." der Satz ist, in dem genau der eine Fehler behoben ist**;
nachgerechnet `wrong`, Tag **Artikel**. `error_correction` ist der einzige Itemtyp, den
weder `articleCueOnlyInGloss` noch `missingSentenceArticle` erreicht. Drittens
`isItemCorrect()` ruft `checkAnswer` ohne `dictation: true`, während `DictationItem.jsx`
es setzt: `a1.1-cp2-hoeren-1` („Null vier zwei **–** drei drei acht eins.") ist in der
Lektion mit jedem Trennzeichen richtig und im benoteten Hören-Teil nur mit dem
Halbgeviertstrich — auf einer Handytastatur nicht eingebbar. Dazu sieben MAJOR, deren
Schwerpunkt sich verschoben hat: nicht mehr die Bewertung, sondern **was gezogen wird**.
L4 zieht in beiden Versuchen vier Items mit ungelehrten Nomen (Instrument, Freiheit,
Zeitung, Sonne, Kaffee), weil sie weiterhin nur acht eigene Items hat; L12 — die letzte
Lektion — zieht in beiden Versuchen **fünf von sieben** Items auf `mein` und **kein
einziges** der drei höflichen `Ihr`-Items, weil `LEMMA_STOPWORDS` alle Determinative
enthält und `MAX_SAME_LEMMA` deshalb ausgerechnet in den Determinativ-Lektionen blind
ist. Ein Prädikat für die Höflichkeitsform, drei Zeilen im Lesen-Generator, ein
`dictation`-Flag, zwei L4-Items und ein Blick auf die Ziehung von L12 — dann
unterschreibe ich.

**Zahlen:** 3 BLOCKER · 7 MAJOR. Minors sind nicht erfasst (fünf Zeilen am Ende).

---

## BLOCKER

| Feld | Zitat | Problem | Fix (pasteable) | Schwere |
|---|---|---|---|---|
| `isCaseTask()` in `src/lib/lesson/check.js` ↔ `0416dbbe`, `b269d424`, `10b25f5c`, `83bab298`, `c473031c` ↔ `src/services/reviewService.js:143/156/168` | `isCaseTask = (item) => item?.caseSensitive === true` · Items mit dem Flag im ganzen Pool: **5** (`extra-a11-l01-06`, `-l03-08`, `-l12-08`, `-l12-09`, `-l12-16`) · `0416dbbe`: „Frau Müller, ___ sind sehr freundlich.", `accepted: ['Sie']`, `explanationDe: 'Sie (großgeschrieben) ist das formelle Sie für Fremde und Beamte.'` · `83bab298`: „[das / ist / Ihre Adresse] → Schreiben Sie den Satz.", `explanationDe: '… großes I markiert die Höflichkeitsform.'` | **Der Blocker aus Runde 5 wurde als Liste geschlossen, nicht als Klasse — und die Liste ist unvollständig, in beide Richtungen.** Die Entscheidung ist richtig („die Items besitzen das Flag, der Prüfer liest es nur"), aber sie wurde nur auf die drei Items angewandt, die Runde 5 namentlich genannt hat. Nachgerechnet mit `checkAnswer(… , { caseSensitive: isCaseTask(item) })`: `0416dbbe` `sie` → **typo**, `b269d424` („Ist das ___ Adresse? **(Sie, Höflichkeitsform)**") `ihre` → **typo**, `10b25f5c` („Wie ist ___ Name, bitte? **(Sie, Höflichkeitsform)**") `ihr` → **typo** — und `typo` zählt in `PracticeItem`, in `isItemCorrect` und in `gradeTypedReview` als **richtig**. Dieselbe Frage in `extra-a11-l03-08` („Frau Kaya, sprechen ___ Englisch?") liefert für `sie` **wrong**. Der Kurs bewertet damit weiterhin dieselbe Regel an zwei Stellen verschieden — nur ist die Bruchlinie jetzt nicht mehr die Aufrufstelle, sondern die Reihenfolge, in der die Reviews die Items gefunden haben. Am schwersten wiegt der benotete Abschluss: `83bab298` ist **`a1.1-cp4-schreiben-2`** und `c473031c` ist **`a1.1-cp4-bausteine-1`**; gemessen `isItemCorrect(item, 'Das ist ihre Adresse.') === true` und `isItemCorrect(item, 'Sind sie Frau Meier?') === true`, obwohl die Regelkarte `possessive-articles` sagt: „Die höfliche Form ist Ihr und steht **immer** mit großem I." Dritte Messung: `grep -c caseSensitive src/data/curricula/a11.js` = **0**, während `reviewService.buildCardIndex` das Flag aus `word.caseSensitive` / `notice.caseSensitive` / `line.caseSensitive` liest — also ist **jede** Wiederholungskarte fallunempfindlich, auch die Satzkarte „Gut. Wie geht es Ihnen?" aus L1, deren Lektionsitem `caseSensitive: true` trägt. | Das Flag **ableiten** statt es zu setzen, und die Handeintragung als Override behalten. In `quality.js`: `export const POLITE_FORM_RE = /^(Sie\|Ihnen\|Ihr\|Ihre\|Ihren\|Ihrem\|Ihrer\|Ihres)$/;` und `export function politeCaseItem(item) { const acc = [item?.answer, ...(item?.accepted \|\| [])].map((a) => String(a ?? '').trim()).filter(Boolean); if (!acc.length) return false; if (acc.every((a) => !a.includes(' '))) return acc.every((a) => POLITE_FORM_RE.test(a)); return acc.every((a) => a.split(/\s+/).slice(1).some((w) => /^Ihr(e\|en\|em\|er\|es)?$/.test(w.replace(/[.,!?]/g, '')))); }` — die Einwort-Klausel trifft nur, wenn **keine** kleingeschriebene Variante akzeptiert wird (deshalb bleibt `extra-a11-l12-10` mit `['Ihre','ihre']` und `210e89f8` mit `['ihre']` außen vor, und `extra-a11-l03-02` mit `['Sie','sie']` ebenfalls); die Satzklausel ist auf den **Possessiv** verengt, damit ein Wortstellungsitem wie `extra-a11-l10-08` nicht wegen eines Großbuchstabens komplett falsch wird. In `scripts/build-lesson-pool.mjs` im Reparaturlauf `item.caseSensitive = item.caseSensitive === true \|\| politeCaseItem(item);` schreiben; in `reviewService.buildCardIndex` die drei Zeilen auf `politeCaseItem({ accepted })` umstellen, weil die Kurationsdatei das Feld nicht führt. Test in `tests/lesson-pool-rules.test.mjs`: **jedes** Item des gebauten Pools, für das `politeCaseItem` wahr ist, trägt `caseSensitive: true` — und ein Test in `tests/checkpoint.test.mjs`, der das über **alle 80** Checkpoint-Items prüft statt über ein genanntes. | BLOCKER |
| `extra-a11-l05-09` (gezogen L5 #4 in Versuch 1, L5 #3 in Versuch 2) ↔ `articleCueOnlyInGloss()` / `missingSentenceArticle()` in `quality.js` | `type: 'error_correction'` · `questionDe: 'Korrigieren Sie: „Ein Schere ist hier.“'` · `questionEn: 'Fix the article.'` · `answer: 'Die Schere ist hier.'`, `accepted: ['Die Schere ist hier.','Die Schere ist hier']` · `explanationDe: 'Bekannt und feminin: die Schere, nicht ein Schere.'` | **Die Klasse von BLOCKER 1 aus Runde 5, eine Itemform weiter — und wieder greift die neue Regel nicht, weil sie an den Typen hängt, in denen die letzte Instanz gefunden wurde.** `articleCueOnlyInGloss()` verzweigt auf `sentence_building` und `fill_blank`; `error_correction` ist der dritte produktive Typ und wird von keinem der beiden Prädikate erreicht. Der zu korrigierende Satz „Ein Schere ist hier." enthält **genau einen** Fehler — das Genus des unbestimmten Artikels. Die minimale, vollständige Korrektur lautet **„Eine Schere ist hier."**, ein fehlerfreier deutscher Satz. Nachgerechnet: `checkAnswer('Eine Schere ist hier.', accepted)` → **`wrong`**, `tagError` → **Artikel**. Die Aufgabe, den *bestimmten* Artikel zu setzen, steht **nirgends**: der deutsche Prompt sagt nur „Korrigieren Sie:", die englische Glosse nur „Fix the article." — die Absicht steht ausschließlich in der `explanationDe`, die der Lernende erst **nach** der falschen Antwort sieht. Das Item ist die einzige Fehlerkorrektur des Pools mit mehr als einer minimalen Lösung (die anderen dreizehn geprüft: Genusfehler, Konjugationsfehler oder Präpositionsfehler, jeweils eindeutig) — und es wurde in **Runde 5 als Reparatur geschrieben**, als eines der drei L5-Kontrastitems, die den Bekanntheitsunterschied lehren sollen. Damit ist es der dritte Befund in Folge, bei dem der Blocker in der Karte steckt, die den Altbestand ersetzen sollte. Es ist in **beiden** Ziehungen von L5 enthalten. | Die Regel als Frage formulieren — *unterscheidet sich die Musterlösung vom Zitat in genau der Weise, die der deutsche Prompt nennt?* In `quality.js`: `REASON.AMBIGUOUS_CORRECTION` mit `export function ambiguousCorrection(item) { if (String(item?.type) !== 'error_correction') return false; const q = QUOTED_SPAN_RE.exec(String(item.questionDe \|\| '')); if (!q) return false; const src = bare(q[1]).split(/\s+/), tgt = bare(item.answer).split(/\s+/); if (src.length !== tgt.length) return false; const diff = src.map((w, i) => [w, tgt[i]]).filter(([a, b]) => flat(a) !== flat(b)); if (diff.length !== 1) return false; const [from, to] = diff[0]; const fam = (w) => DEFINITE_ARTICLE_ANSWERS.includes(flat(w)) ? 'definite' : INDEFINITE_ARTICLE_ANSWERS.includes(flat(w)) ? 'indefinite' : null; return Boolean(fam(from)) && Boolean(fam(to)) && fam(from) !== fam(to) && !ARTICLE_TASK_CUE_RE.test(String(item.questionDe \|\| '')); }` — also: wer die Artikel**familie** wechseln lässt, muss das im deutschen Prompt sagen. Reparatur im Build wie bei den fill_blank-Items (Formel anhängen) und am Item selbst: `questionDe: 'Korrigieren Sie: „Ein Schere ist hier.“ (mit bestimmtem Artikel)'`, `accepted: ['Die Schere ist hier.','Die Schere ist hier']`. Dazu ein poolweiter 0-Test in `tests/lesson-pool-rules.test.mjs`, wie für die beiden anderen Typen. | BLOCKER |
| `isItemCorrect()` in `src/lib/checkpoint/buildCheckpoint.js:759–761` gegen `src/components/lesson/DictationItem.jsx:39` | `const { result } = checkAnswer(String(answer), item.accepted, { strict, caseSensitive });` — **ohne** `dictation` · dagegen in der Lektion: `checkAnswer(value, [line.de], { strict: false, dictation: true })` · betroffen: `a1.1-cp2-hoeren-1`, `answer: 'Null vier zwei – drei drei acht eins.'` | **Runde 5 hat verlangt, dass `caseSensitive` an alle drei Bewertungsstellen gereicht wird; gereicht wurde `caseSensitive`, nicht die Regel dahinter — die zweite Option ist weiterhin von der Aufrufstelle abhängig.** `normalizeDictation()` existiert genau deshalb, weil „die Gruppierung eine Entscheidung des Schreibenden ist, nicht des Sprechenden, und ein Bindestrich unhörbar ist" (so der Kommentar in `check.js`). Im Checkpoint ist der Bindestrich wieder die Prüfungsfrage. Nachgerechnet an dem einen Diktat des Kurses, das ein Trennzeichen trägt: `'Null vier zwei - drei drei acht eins.'` (normaler Bindestrich statt Halbgeviertstrich) → in der Lektion **`correct`**, im Checkpoint **`isItemCorrect === false`**; `'Null vier zwei drei drei acht eins.'` (ohne Trennzeichen, die Form, die eine Handytastatur nahelegt) ebenso: Lektion `correct`, Checkpoint **falsch**. Der Lernende hat die Telefonnummer richtig gehört und richtig geschrieben. Das trifft den Hören-Abschnitt, also einen der fünf Abschnitte, für die die 40-%-Regel des Standards (§3) gilt, und es ist strukturell, nicht zufällig: **alle zwölf** Diktate der vier Checkpoints sind Dialogzeilen, und ob eine davon ein Trennzeichen oder eine Zifferngruppe enthält, ist eine Entscheidung des Kurationstextes — heute trifft es eine, morgen drei. Dieselbe Lücke besteht in `gradeTypedReview()`, das ebenfalls kein `dictation` kennt; dort ist sie heute folgenlos, weil der Hörmodus der Wiederholungsseite eine Aufdeckkarte ist und nicht getippt wird. | Die Optionen ans **Item** hängen, nicht an den Aufruf. In `check.js` eine Funktion neben `isCaseTask`: `export const isDictationTask = (item) => item?.kind === 'dictation' \|\| item?.type === 'dictation' \|\| item?.stage === 'listening';` — dann `buildCheckpoint.js:760` → `checkAnswer(String(answer), item.accepted, { strict, caseSensitive, dictation: isDictationTask(item) })`, `DictationItem.jsx:39` → derselbe Ausdruck, `reviewGrading.js` → `dictation: isDictationTask(pseudoItem)`. Und der Test, der die Klasse hält (nicht die Instanz), in `tests/checkpoint.test.mjs`: für **jedes** Item aller vier Checkpoints und eine Antwortliste muss gelten `isItemCorrect(item, x) === (checkAnswer(x, item.accepted, { strict: STRICT_TOPIC.test(item.topic \|\| ''), caseSensitive: isCaseTask(item), dictation: isDictationTask(item) }).result !== RESULT.WRONG)` — das ist derselbe Satz wie in Runde 5, diesmal ohne Optionsliste im Testcode: **eine Antwort wird im Kurs überall gleich bewertet, und welche Prüfregeln gelten, entscheidet das Item.** | BLOCKER |

---

## MAJOR

| Feld | Zitat | Problem | Fix (pasteable) | Schwere |
|---|---|---|---|---|
| `src/data/lessonPools/a11.extra.json` — L4 hat **8** Items; Ziehung L4, beide Versuche | Versuch 1: `0ab66073` „___ Instrument ist teuer.", `319b95b6` „___ Mutter kocht **Kaffee**.", `57592d92` „___ **Freiheit** ist wichtig." (*Endung -heit ist immer feminin.*), `d205e58f` „Der **Sonne** ist warm." · Versuch 2 zusätzlich `14894fb2` „___ **Zeitung** ist **interessant**." | **Der einzige Befund dieser Reihe, der in drei Runden hintereinander unverändert steht — und Runde 5 hat die Reparatur wörtlich ausgeschrieben.** L4 („Auf dem Flohmarkt", Handlungsfeld „Einkaufen: nach Gegenständen und Preisen fragen", Wortfeld Tisch, Stuhl, Lampe, Uhr, Tasche, Rucksack, Regal) hat weiterhin genau **acht** handgeschriebene `nouns-gender`-Items, also füllt die Ziehung drei bis vier Plätze aus dem Altbestand. Gemessen an der Ziehung: **vier** der sieben Items in Versuch 1 und **vier** in Versuch 2 enthalten Nomen oder Adjektive, die keine Lektion des Kurses einführt (`Instrument`, `Freiheit`, `Zeitung`, `Sonne`, `Kaffee` — letzteres steht im Wortfeld von L9 —, dazu `interessant`, `wichtig`, `warm`, `kocht`). Die Erklärungen bringen weitere ungelehrte Wörter mit („das Dokument", „der König"). Runde 5 hat `extra-a11-l04-09` und `-10` als fertige Items angegeben; beide fehlen. Der Effekt ist nicht kosmetisch: in *Start Deutsch 1* ist Lesen Teil 2 eine Anzeige mit Alltagsgegenständen, und der Lernende übt hier Genus an abstrakter Lexik, die er nie wiedersieht. | Die beiden Items aus Runde 5 schreiben, plus zwei weitere aus dem eigenen Wortfeld — dann deckt L4 seine Ziehung selbst: `{ id:'extra-a11-l04-09', topic:'nouns-gender', type:'fill_blank', questionDe:'___ Flohmarkt ist am Samstag. (der, die oder das?)', answer:'Der', accepted:['Der','der'] }`, `{ id:'extra-a11-l04-10', type:'error_correction', questionDe:'Korrigieren Sie: „Das Lampe kostet acht Euro.“ (mit bestimmtem Artikel)', answer:'Die Lampe kostet acht Euro.' }`, `{ id:'extra-a11-l04-11', questionDe:'___ Stuhl ist alt, aber billig. (der, die oder das?)', answer:'Der' }`, `{ id:'extra-a11-l04-12', type:'sentence_building', questionDe:'Bilden Sie den Satz: [die Uhr / kosten / zwanzig Euro]', answer:'Die Uhr kostet zwanzig Euro.' }`. Die Regel, die nachrückt, ist MAJOR 2: sobald die Lexik an der **gezogenen** Lektion gemessen wird, fallen `Instrument`, `Freiheit`, `Zeitung` und `Sonne` von selbst aus dem Pool. | MAJOR |
| `lektionAssignment()` in `scripts/validate-curriculum.mjs:530–536`, benutzt von `itemLexis()` (RULE 11) | `return (it) => lektionOfItem(it.id) ?? earliest.get(it.topic) ?? null;` · Ausgabe: „RULE 11 Item-Lexik: **187** ungelehrte Tokens (Ratchet 187)" | **Die Regel liest jetzt den richtigen Pool und misst trotzdem an der falschen Lektion — deshalb ist 187 eine Zahl, die niemand reparieren kann.** Ein generiertes Item wird der **frühesten** Lektion zugeordnet, deren `practiceRule.topics` sein Topic enthält; die Ziehung legt es dorthin, wohin der Plan es legt. Aufgeteilt: von den 187 Tokens (127 Items) stehen **25 in 16 Items, die tatsächlich gezogen werden**, **15 in 11 Items, die nur im Checkpoint stehen**, und **147 in 100 Items, die kein Lernender je sieht**. Und ein Teil der 25 ist Messartefakt: `cbee2a2c` wird `Formular` in **L1** angelastet, obwohl das Item ausschließlich in **L2** gezogen wird, wo `das Formular` im Wortfeld steht; `459b2960` wird `Schwester` in L2 angelastet und in L3 gezogen, wo `die Schwester` im Wortfeld steht. Der Ratchet ist damit ehrlich gesetzt, aber als Reparaturwerkzeug wertlos: eine Runde, die die 16 echten Offender behebt, senkt die Zahl um 25 von 187 und sieht aus, als hätte sie nichts getan. Runde 5 hat den richtigen Satz geschrieben („was der Validator nicht liest, existiert für die Reparaturrunde nicht") — die Fortsetzung lautet: *und was er an der falschen Stelle liest, kann nicht repariert werden.* | Zwei Zahlen statt einer, beide an der Stelle gemessen, an der der Lernende das Item sieht. In `validate-curriculum.mjs` neben `itemLexis` eine `drawnLexis(c, pool)`, die die Zuordnung aus `planPractice(c, pool, 1)` **und** `planPractice(c, pool, 2)` sowie aus `buildCheckpoint()` (Kapitel-Lektion) nimmt: `const drawn = new Map(); for (const a of [1,2]) for (const [nr, items] of planPractice(c, pool, a)) for (const it of items) if (!drawn.has(it.id) \|\| drawn.get(it.id) > nr) drawn.set(it.id, nr);` — dieselbe Tokenmechanik, aber **harte Regel 0** statt Ratchet, weil ein gezogenes Item mit ungelehrter Lexik nichts ist, was man zählen müsste. Den Rest schließt der Build: in `scripts/build-lesson-pool.mjs` ein Ausschlussgrund `REASON.UNTAUGHT_AT_EARLIEST_LEKTION` für Altbestandsitems, deren Inhaltswörter an ihrer frühestmöglichen Ziehungs-Lektion ungelehrt sind — dann kann die Ziehung sie gar nicht erreichen, die 147 ungezogenen Tokens verschwinden mit ihnen, und `MAX_UNTAUGHT_ITEM_TOKENS` wird auf den dann gemessenen Wert gesetzt. Achtung: L4 fällt danach unter `PRIMARY_MIN` — das ist der ehrliche Befund und der Grund, warum MAJOR 1 in derselben Änderung erledigt werden muss. | MAJOR |
| `LEMMA_STOPWORDS` / `MAX_SAME_LEMMA` in `src/lib/lesson/buildLesson.js:61,74–104,236` gegen die Ziehung von L12 | Stoppliste: `'der','die','das',…,'mein','meine','meinen','meinem','meiner','dein','deine',…,'ihr','ihre',…,'unser','unsere','euer','eure'` · Ziehung L12, Versuch 1 (Antworten): `ihre · Mein · Wir feiern unser Fest. · Mein · Mein · Deine · Mein Geburtstag ist im Mai.` · Versuch 2: `Mein · ihre · Mein Geburtstag ist im Mai. · Mein · sein · Mein · Wir feiern unser Fest.` | **Die Abschlusslektion des Kurses drillt in beiden Versuchen fünf von sieben Items auf `mein` und zieht kein einziges der drei höflichen `Ihr`-Items — und die Ursache ist eine Regel, nicht ein Item.** Die Stoppliste entfernt alle Artikel und Possessiva, damit `relevanceScore` nicht jedes „Schreib den Satz"-Item als dieselbe Lexik liest; `MAX_SAME_LEMMA` benutzt dieselbe Lemmamenge (`itemLemmas`). In den drei Lektionen, deren **primäre Grammatik das Determinativ ist** (L5 definite-articles, L6 indefinite-articles, L12 possessive-articles), ist die Vielfaltsgrenze damit blind für genau das, was variieren müsste. Gemessen an L12: Besitzer-Verteilung Versuch 1 `ich` ×5, `sie-Plural` ×1, `wir` ×1, `du` ×1 (`Deine` zählt doppelt, weil zwei Items dieselbe Aussage tragen: „___ Geburtstag ist im Mai. (ich)" und „[mein / Geburtstag / sein / im Mai]"); Versuch 2 `ich` ×4, `er` ×1, `sie` ×1, `wir` ×1. Die notice von L12 lehrt sechs Possessiva **und** die Höflichkeitsform; `extra-a11-l12-08/09/16` — die drei einzigen Items des Kurses, die die Höflichkeitsform prüfen und die deshalb `caseSensitive: true` tragen — sind in **keinem** der beiden Versuche enthalten. Für einen Prüfungskandidaten ist das der teuerste Ausfall des Kurses: `Ihr`/`Ihre` ist in Schreiben Teil 2 (formelle Mitteilung) und Sprechen Teil 3 Bewertungsgegenstand, und L12 ist die Lektion, in der er es zuletzt üben könnte. | Die Vielfaltsgrenze auf das beziehen, was das Item **produzieren lässt**, nicht auf seine Inhaltswörter. In `buildLesson.js` eine zweite Achse neben `itemLemmas`: `const answerKey = (it) => contentLemmas(it.answer).size ? [...contentLemmas(it.answer)].join(' ') : flat(String(it.answer \|\| '')).split(' ')[0];` und in `pickPracticeItems` eine zusätzliche Schranke `MAX_SAME_ANSWER_FORM = 2` über `flat(bare(it.answer)).split(' ')[0]` — das zählt `Mein/mein` als eine Form und kappt sie bei zwei. Zusätzlich für die Determinativ-Lektionen ein `coverRule`, das analog zu `PRIMARY_MIN` arbeitet: `practiceRule.mustCover: ['mein','dein','sein','ihr','unser','Ihr']` auf L12 (`['der','die','das']` auf L5, `['ein','eine','einen']` auf L6), und in `pickPracticeItems` nach der Primärquote je eine noch nicht gedeckte Form bevorzugen. Test in `tests/lesson-engine.test.mjs`: für L12 muss die Ziehung in **beiden** Versuchen mindestens vier verschiedene Besitzer und mindestens ein `caseSensitive`-Item enthalten. | MAJOR |
| `changedDetail()` / `contentWords()` in `src/lib/checkpoint/buildCheckpoint.js:349–391` — gebauter Checkpoint 3, Lesen 2 | `vocab`-Zweig: `if (allowVocab && vocab.includes(word)) return pickOther(vocab, word, rng);` · erzeugt: `promptDe: 'Steht das im Text? „Tim: Spielst du jede **Frühstück** Fußball?“'` gegen den Text „… Tim: Spielst du jede **Woche** Fußball? …" | **Der neue Falsifikationsgenerator ist die richtige Idee und hat keine Kongruenzschranke: er ersetzt ein beliebiges Inhaltsnomen durch ein beliebiges anderes des Kapitels.** Die Zahlen- und Wochentagszweige sind sauber gebaut (`eins` und `null` sind bewusst ausgenommen, „weil eine falsche Aussage falsch sein muss, nicht kaputt" — genau der richtige Satz); der `vocab`-Zweig, der greift, wenn ein Fenster weder Zahl noch Wochentag noch Namen enthält, hat diese Schranke nicht. Gemessen über alle vier gebauten Checkpoints: von acht `Falsch`-Aussagen sind sechs einwandfreie Detailänderungen (`sechs`→`fünf`, `zwanzig`→`siebzig`, `Ana`→`Lena`, `Mittwoch`→`Sonntag`, `zwanzig`→`drei`, `vier`→`achtzig`), eine ist schief („**Siebzehn** vier zwei" als Telefonnummer, die ziffernweise gesprochen wird) und **eine ist kein Deutsch**: *jede Frühstück* (das Frühstück ist neutral, `jede` ist feminin). Im benoteten Lesen-Teil gibt das dem Lernenden die Lösung über die Form statt über den Inhalt — also genau die Schwäche, die der Generator beseitigen sollte, nur von der anderen Seite — und es zeigt ihm nebenbei eine falsche Form. | Die Ersetzung auf Wörter derselben Klasse verengen, aus Daten, die schon da sind: das Wortfeld führt `article` und `plural`. In `contentWords()` statt eines Wortsatzes eine Map `wort → { article, plural }` aus `lektion.wortfeld` der Kapitel-Lektionen aufbauen, und in `changedDetail()`: `if (allowVocab && vocab.has(word)) { const a = vocab.get(word); const same = [...vocab.keys()].filter((w) => w !== word && vocab.get(w).article === a.article); return same.length ? pickOther(same, word, rng) : null; }` — kein Kandidat, kein Item (der Aufrufer überspringt bereits sauber). Dazu für den Zifferngruppen-Fall: Zahlwörter, die Teil einer Telefonnummer sind (zwei oder mehr Zahlwörter in Folge), nur gegen andere **einstellige** Zahlwörter tauschen. Test in `tests/checkpoint.test.mjs`: jede `Falsch`-Aussage aller vier Checkpoints unterscheidet sich vom Quelltext in genau einem Wort, **und** das ersetzte und das ersetzende Wort tragen denselben Artikel. | MAJOR |
| `dd86dc8a-49b8-5d48-8a6a-2606fd90b0fd` als `a1.1-cp2-schreiben-1` | `promptDe: 'Schreiben Sie den Satz: [Honig / ist / gut] **(mit bestimmtem Artikel)**'` · `answer: 'Der Honig ist gut.'` · `explanationDe: 'Endungen auf -ig sind meist maskulin: der Honig, der **König**.'` · `source: 'chapter'` | **Die Bewertungshälfte des Blockers aus Runde 5 ist geschlossen, die Lexikhälfte nicht — und Runde 5 hat den Austausch wörtlich verlangt („`dd86dc8a` ersetzen — `Honig` und `König` stehen in keiner Lektion").** Nachgerechnet: mit der angehängten Aufgabenformel ist `Honig ist gut.` **begründet** falsch (Tag: Artikel, nicht mehr Verbstellung — auch das ist repariert). Es bleibt: das Item steht als **erste** von drei Schreiben-Aufgaben im **benoteten** Checkpoint 2, `Honig` steht in keinem Wortfeld des Kurses (Kapitel 2 sind Flohmarkt, Klassenzimmer, Büro), seine Erklärung führt mit `König` ein zweites ungelehrtes Wort ein, und `source: 'chapter'` behauptet ausdrücklich, es gehöre zu diesem Kapitel. Dasselbe in kleiner Münze im selben Checkpoint: `0691d3fb` („___ **Fahrer** kommt **gleich**.") und `2140796b` („___ Stühle sind **bequem**.") in den Sprachbausteinen, `extra-a11-l12-11` („Im **Januar** ist ___ Geburtstag.") in Checkpoint 4. Elf der 80 Checkpoint-Items tragen ungelehrte Lexik — im Test, der über „Sicher" entscheidet. | Kurzfristig ersetzen, aus dem Wortfeld des Kapitels: `{ id:'extra-a11-l04-12', type:'sentence_building', topic:'nouns-gender', questionDe:'Bilden Sie den Satz: [die Uhr / kosten / zwanzig Euro]', answer:'Die Uhr kostet zwanzig Euro.' }` (MAJOR 1) — dann zieht `buildSchreiben` es statt `dd86dc8a`, weil es ein `sentence_building` desselben `primarySlug` ist. Dauerhaft die Regel: `buildCheckpoint` darf kein Item mit `source: 'chapter'` ziehen, dessen Lexik am Kapitelende ungelehrt ist. Konkret in `buildCheckpoint.js` einen Filter in `drawPool`/`byTopics`, gespeist von derselben Funktion, die MAJOR 2 einführt (`untaughtTokens(item, knownUpTo(chapterEnd))`), und ein Test in `tests/checkpoint.test.mjs`, der über **alle 80** Items 0 ungelehrte Tokens verlangt. | MAJOR |
| `extra-a11-l02-09` bis `-12` in `a11.extra.json`, `topic: 'verb-sein'` | `'Ergänzen Sie: Meine Telefonnummer im Formular ist null eins ___ sechs. (7)'` → `sieben` · `-10` „(3)" → `drei` · `-11` „(8)" → `acht` · `-12` „Mein Vorname hat ___ Buchstaben. (10)" → `zehn` | **Die Zahlwörter sind wieder da — und sie sind genau so abgelegt, wie Runde 5 ausdrücklich gewarnt hat.** Die Lücke, die Runde 3 durch Löschen geschlossen und Runde 5 als MAJOR 10 wieder geöffnet hat, ist mit vier guten Items gefüllt: die Zahl steht als Ziffer im **deutschen** Prompt, der Rahmen kommt aus dem eigenen Dialog, und `extra-a11-l02-09` ist in beiden Ziehungen von L2 enthalten. Sie tragen aber `topic: 'verb-sein'`, weil L2s `practiceRule.topics` nur `verb-sein` und `alphabet-pronunciation` kennt. Folge, nachgerechnet mit `tagError`: `sieber` → **Konjugation**, `sechs` → **Konjugation**, `seven` → **Konjugation**, `siben` → typo/**Konjugation** — die letzte Klausel von `tagError` greift auf `/verb\|sein\|haben\|present\|separable\|conjug/` im Topic. Ein Lernender, der Zahlwörter falsch schreibt, bekommt in `CheckpointPage.jsx:412` „Konjugation" als Diagnose angezeigt und bekommt aus `remediationSet` weitere `verb-sein`-Items. Runde 5 hat genau das vorhergesagt und ein eigenes Topic verlangt („und er ist mit einem eigenen Topic behoben statt mit dem Löschen"). `-12` steht zusätzlich als `a1.1-cp2-bausteine-6` im benoteten Checkpoint. | Das Topic anlegen, das der Kurs ohnehin braucht (Hören Teil 1 von SD1 ist fast nur Zahlen, Uhrzeiten, Telefonnummern): die vier Items auf `topic: 'numbers'` umschreiben, `practiceRule.topics` von L2 auf `['verb-sein','alphabet-pronunciation','numbers']` erweitern (L2 zieht bereits 5/7 primär, die Quote hält), `drillsSlug` um einen `numbers`-Eintrag ergänzen (`DRILLS.numbers = (item, { expected }) => expected.some((a) => NUMBER_WORDS.includes(flat(a)))`), und in `tagError` vor die Verbklausel: `if (item?.topic === 'numbers') return 'Rechtschreibung';`. `ERROR_TAGS` bleibt unverändert — ein Zahlwortfehler ist eine Schreibung, kein neunter Tag. Test in `tests/check-answer.test.mjs` mit den vier oben gemessenen Eingaben. | MAJOR |
| `extra-a11-l10-06` (gezogen L10 #7, Versuch 2) | `questionDe: '___ du morgen mit dem Bus?'` · `accepted: ['Fährst','fährst','Kommst','kommst','**Gehst**','**gehst**']` · `explanationDe: 'du fährst wird in der Frage zu Fährst du.'` | **Das Item akzeptiert einen Satz, der kein Deutsch ist — in der Lektion, deren Handlungsfeld „Mobilität und Verkehrsmittel" heißt.** Nachgerechnet: `checkAnswer('Gehst', accepted)` → **`correct`**. „Gehst du morgen mit dem Bus?" ist falsch: man fährt mit dem Bus oder kommt mit dem Bus, man geht nicht mit ihm (`gehen` schließt das Verkehrsmittel aus — das ist der Kontrast, den A1-Lernende mit romanischer oder slawischer Erstsprache am häufigsten verfehlen). Runde 5 hat `EQUIVALENT_VERBS` gelobt, und zu Recht: die Mechanik in `build-lesson-pool.mjs:534–578` ist schmal, kommentiert und mit `EQUIVALENT_VERB_IDS = {}` ehrlich leer. Der Fehler sitzt eine Ebene darüber: die Gruppe `fahren: ['gehen','kommen']` gilt **pro Lemma**, nicht **pro Rahmen**, und der Auslöser ist die Heuristik „das Item lässt schon zwei Lemmata zu" (`if (lemmas.size < 2 && !optedIn) return;`) — eine Vermutung über die Absicht des Autors. Hier war die Absicht `fahren`/`kommen`; `gehen` ist dazugekommen und steht inzwischen in der handgeschriebenen Datei. Das Item ist in Versuch 2 gezogen; ein Lernender, der `Gehst` tippt, bekommt ein grünes Häkchen auf einen Fehler. | Die Heuristik streichen und die Gruppe rahmenabhängig machen. In `build-lesson-pool.mjs`: `if (!optedIn) return;` (also nur noch `EQUIVALENT_VERB_IDS` entscheidet — die Datei sagt selbst, leer sei der ehrliche Zustand), und `EQUIVALENT_VERBS` auf den Rahmen einschränken: `const PP_BLOCKS = { gehen: /\bmit (dem\|der) \w+/i };` — ein Lemma wird nicht ergänzt, wenn sein Blocker auf `questionDe` passt. Am Item sofort: `accepted: ['Fährst','fährst','Kommst','kommst']`. Test in `tests/lesson-extra-items.test.mjs`: kein Item des gebauten Pools akzeptiert eine Form von `gehen`, wenn sein Prompt ein `mit dem/der`-Präpositionalobjekt trägt. | MAJOR |

---

## Status der Befunde aus Runde 5

Verifiziert an den Artefakten (Pool, beide Ziehungen, gebaute `ruleCards.mjs`, die vier
gebauten Checkpoints, Validator-Ausgabe, Testlauf, Lint), nicht am Diff.

### Die drei BLOCKER

1. **Drei Satzbauitems, deren Artikel nur in der englischen Glosse steht
   (`dd86dc8a`, `670eaadb`, `cd6d471e`)** — **gelandet, und zwar als Prädikat statt als
   Typ.** `missingSentenceArticle(item)` fragt: steht der Artikel, den die Antwort tragen
   muss, vor einem großgeschriebenen Wort, und nennt ihn die Cue-Liste oder die
   Aufgabenformel? `articleCueOnlyInGloss` verzweigt darauf für `sentence_building`.
   Gemessen: `exclusionReason()` über beide Pooldateien meldet **0** Treffer auf 351 + 124
   Items; die drei Items tragen jetzt „(mit bestimmtem Artikel)" im **deutschen** Prompt.
   Nachgerechnet an `dd86dc8a`: `Honig ist gut.` ist begründet `wrong` und der Tag lautet
   **Artikel**, nicht mehr Verbstellung. Die Narrowing-Entscheidung (nur vor einem
   großgeschriebenen Folgewort, damit „Wir kaufen heute **ein**." nicht als fehlender
   Artikel gelesen wird) ist gemessen und im Code begründet. **Rest:** das Prädikat kennt
   `error_correction` nicht (BLOCKER 2 oben) und die Lexik von `dd86dc8a` ist unverändert
   (MAJOR 5 oben).
2. **`extra-a11-l05-08` bestraft die Antwort, die seine eigene Aufgabenformel verlangt** —
   **gelandet, als Regel mit poolweitem Test.** Das Item lautet jetzt „Ist hier ein Heft?
   — Ja, und ___ **Heft** ist grün. (bestimmter Artikel)" mit `accepted: ['das','Das']`;
   nachgerechnet sind `das` und `Das` **correct**. Dazu `REASON.CUE_ANSWER_MISMATCH` mit
   `cueAnswerMismatch()` in beide Richtungen und `tests/lesson-pool-rules.test.mjs:473`
   mit 0 über den ganzen Pool. Die Rückrichtung („die Antwort ist ein blanker Artikel,
   die Formel nennt ein Verb") hat heute keinen Treffer, und der Kommentar sagt das
   ausdrücklich — das ist die richtige Art, eine Regel ohne Instanz zu führen.
3. **`isCaseTask` entscheidet über die Antwortform; der Schalter erreicht nur
   `PracticeItem`** — **gelandet, aber als Liste (siehe BLOCKER 1 oben).** Die
   Mechanik ist richtig: `POLITE_POSSESSIVE_RE` und der Slug-Zweig sind gestrichen,
   `isCaseTask(item)` ist `item.caseSensitive === true` und nichts sonst, und der Schalter
   wird an **allen drei** Stellen übergeben — `PracticeItem.jsx:84`,
   `buildCheckpoint.js:759` und neu `src/lib/checkpoint/reviewGrading.js`, eine eigene
   Datei mit einem Kopfkommentar, der die Entscheidung vollständig aufschreibt.
   Nachgerechnet: `extra-a11-l12-10` („(sie, Plural)", `accepted: ['Ihre','ihre']`) nimmt
   `ihre` jetzt als **correct** — der Fehlbefund ist weg; `extra-a11-l03-08` und
   `extra-a11-l01-06` tragen das Flag und werten `sie`/`ihnen` als **wrong**, überall
   gleich. Offen ist nur, **welche** Items das Flag tragen: fünf, und mindestens fünf
   weitere hätten es verdient, zwei davon im benoteten Checkpoint 4.

### Die elf MAJOR

| # | Runde-5-Befund | Status | Beleg |
|---|---|---|---|
| 1 | `DRILLS['yes-no-questions']` zählt jedes Item mit Fragezeichen in der Antwort; L10 misst 4/7 und produziert 2/7 | **gelandet, vollständig** | Die Klausel ist an die Herkunft des Fragezeichens gebunden. Nachgezählt in **beiden** Ziehungen: L10 misst jetzt **6/7**, und alle sechs sind echte Frageproduktionen („[fahren / ihr / mit dem Auto / in die Schweiz]", „[sein / die Abfahrt / vom Gleis vier]" …); die beiden Kongruenzitems `-10`/`-11` und die fill_blank-Items mit fertiger Frage zählen nicht mehr (`extra-a11-l10-04`, `-06` → `drills=false`). Die zweite `DRILLS`-Kopie in `tests/lesson-extra-items.test.mjs` ist aufgelöst — der Test importiert `drillsSlug` aus `quality.js`. |
| 2 | RULE 11 liest nur `a11.extra.json` und damit 119 von 347 Items | **gelandet, mit einer neuen Kante** | `itemLexis(c, extraItems, poolItems)` liest den gebauten Pool, die gebaute Fassung eines handgeschriebenen Items gewinnt, und die Zahl ist von 9 auf **187** gesprungen — eine ehrliche Zahl, mit Offender-Liste im Report, und der Ratchet ist auf den gemessenen Wert gesetzt. Die Kante ist MAJOR 2 oben: die Zuordnung ist die **früheste** Topic-Lektion, nicht die gezogene. |
| 3 | `arbeiten` wird in L7 gedrillt, steht aber im Wortfeld von L11, und der e-Einschub steht in keiner notice | **gelandet, und zwar so, wie ich es empfohlen hatte** | `arbeiten` steht im Wortfeld von **L7** (`a11.js`), und die notice von L7 lautet jetzt „… Endet der Stamm auf **-t**, kommt ein **e** dazu: du arbeit**e**st, er arbeit**e**t." — 51 Wörter, also unter der Grenze, und der Vokalwechsel-Satz ist entsprechend gekürzt. `898a0861` und `58dd25ce` sind damit gedeckt; L11 behält `arbeiten` im Perfekt-Chunk und verweist über die canDo-Zeile darauf. |
| 4 | `89f19829` („Welche Endung ist IMMER feminin?", „-ung ist 100% feminin ohne Ausnahmen") | **gelandet, als zwei Regeln** | `metalinguisticPrompt()` (`^Welche Endung`/`^Welches Genus` **plus** ein Optionssatz aus blanken Endungen) und ein Test, der jeder Erklärung des Pools eine Prozentangabe verbietet — dieselbe Auflage, die schon für die Regelkarten galt. Das Item ist aus beiden Ziehungen verschwunden. Der Kommentar nennt die **abgelehnten** Muster und warum (vier legitime Items stehen ein Wort daneben) — das ist der Maßstab, den ich seit Runde 4 verlange. **Rest:** der Ausfall ist nicht gedeckt, L4 hat weiter acht eigene Items (MAJOR 1 oben). |
| 5 | `buildLesen`: derselbe Lösungsschlüssel R–F–R–F in allen vier Checkpoints, Falschaussage aus einer anderen Lektion | **gelandet, mit einer Kante** | Gebaut und ausgezählt: cp1 **F–R–F–R**, cp2 **F–R–R–F**, cp3 **R–F–F–R**, cp4 **F–R–R–F** — je zwei von jedem, kein wiederkehrendes Muster, mit Test. Die Falschaussage stammt jetzt aus **demselben** Text mit genau einem geänderten Detail („sieben **sechs**" → „sieben **fünf**", „Gleis **vier**" → „Gleis **achtzig**"), `hint` ist entfernt, und `REPLACEMENT_NUMBERS` schließt `eins` und `null` aus, „weil eine falsche Aussage falsch sein muss, nicht kaputt". Genau diese Schranke fehlt dem Vokabelzweig (MAJOR 4 oben). |
| 6 | `buildSchreiben`: Grammatikdrill mit erfundenem Textsorten-Etikett, cp4 dreimal `yes-no-questions` | **gelandet, vollständig und besser als verlangt** | Jeder Checkpoint trägt jetzt **die echte Kursaufgabe** seines Kapitels (`a11-l03`, `a11-l06`, `a11-l09`, `a11-l12`), gerendert durch `GradedWriting` und bewertet von `evaluate-writing` — mit `optional: true` und einer sauber begründeten Regel: ohne Grader-Verdikt ist die Aufgabe *nicht versucht* statt falsch, und der Abschnitt rechnet über seine zwei Drills (`itemCounts`/`itemIsScored`, nachgerechnet). Die zwei Drills kommen aus **verschiedenen** Lektionen des Kapitels (cp4: yes-no-questions + possessive-articles statt dreimal L10), `register` ist auf den Drills **null** und nur auf der echten Aufgabe gesetzt. Serverseitig geprüft: `evaluate-writing` löst die Aufgabe über `task_key` selbst auf, der Client-`kind` steuert nur Formular-vs-Textfeld — die `MIN_CHARS`-Ableitung bleibt korrekt. |
| 7 | `CheckpointPage`/`ReviewPage` duzen, der Registertest prüft nur `a11.js` | **gelandet, vollständig** | Die vier genannten Zeichenketten siezen („… zählt in **Ihr** Sprechen-Ergebnis", „im Format **Ihrer** Prüfung", „**Melden Sie sich** an …", „sobald **Sie** eine Lektion abschließen"), und der Test in `tests/course-player.test.mjs:101` liest jetzt `src/components/lesson/*.jsx` **und alle** `src/pages/lesson/*.jsx` und prüft jede Zeile gegen eine du-Tokenliste. Nachgemessen mit einem eigenen Grep über beide Verzeichnisse **und** `SpeakingPage.jsx`: **null** Fundstellen außerhalb des Datenfelds `anrede`. |
| 8 | Der Sprechauftrag reist ohne die Anrede, die er verlangt | **gelandet, als Datenfeld** | `sprechen.open.anrede` existiert in allen **zwölf** Lektionen (durchgehend `'Sie'`, mit einem Kommentar an L7, warum auch beim Hobbythema die Prüfungsanrede gilt), `SpeakingStage:53` → `SpeakingPage:177/277` → `normalizeCourseTask` → `courseTaskColumns` (persistiert in `scenario`) → `taskFromSession` → `buildTeacherSystemPrompt:172–174`, wo der Prompt „ANREDE: Sprich den Lernenden mit Sie an und spiele die Rolle, die die Aufgabe verlangt (z. B. Kellner, Beamtin)" erhält. Der ganze Weg ist nachgeprüft, inklusive des Folgeturns. |
| 9 | L3 `lines[9]` macht Ana verheiratet, während das Formular derselben Lektion sie als ledig führt | **gelandet, mit Regel** | Die Zeile lautet jetzt wörtlich die von mir vorgeschlagene: „Ja, ein Baby. **Der Mann von meiner Schwester** kommt auch aus Marokko." — `der Mann` bleibt gedeckt, Ana bleibt ledig. Dazu **RULE 14** (`personaConsistency`, `PERSONAS_A11`) als **harte** Regel ohne Ratchet, die Familienstand, Herkunft und Beruf der wiederkehrenden Figuren zwischen `dialog` und `schreiben` vergleicht; Ausgabe: **0**. |
| 10 | RULE 12: L2 übt kein Zahlwort, L9 keine höfliche Frage | **gelandet, beidseitig** | L9: `extra-a11-l09-09` („Im Café: ___ Sie auch Kuchen? (haben, höflich)") existiert und ist in **beiden** Ziehungen von L9 enthalten. L2: vier Zahlwortitems (`-09` bis `-12`), Zahl als Ziffer im deutschen Prompt, Rahmen aus dem eigenen Dialog; `-09` ist in beiden Ziehungen. `MAX_UNREHEARSED_CANDOS` ist von 6 auf **4** gesenkt, die verbleibenden vier (L1, L4, L7, L12) stehen jeweils im Dialog. **Rest:** das Topic (MAJOR 6 oben). |
| 11 | `tagError` bucht jeden `sentence_building`-Fehler als Verbstellung | **gelandet, vollständig** | `tagSentenceError()` liest die Mengendifferenz beider Sätze, mit einer im Kopf dokumentierten Entscheidungsreihenfolge (gleiche Wörter/andere Reihenfolge → Verbstellung; ein Wort zu viel → Artikel/Wortschatz; genau ein abweichendes Wort → Artikel/Kasus, Konjugation, Rechtschreibung, Wortschatz). Nachgerechnet an allen drei Paaren, die ich in Runde 5 gemessen habe: *Ich möchte **einen** Glas Wasser.* → **Kasus** (war Verbstellung), *Honig ist gut.* → **Artikel** (war Verbstellung), *Der Honig **gut ist**.* → **Verbstellung** (richtig geblieben). `tests/check-answer.test.mjs` pinnt die Paare. |

Zusätzlich geprüft und weiterhin in Ordnung: die **zwölf Regelkarten** in der gebauten
`ruleCards.mjs` (je genau eine `English:`-Zeile, keine Prozentangabe, drei Fehlerbeispiele
je Karte, Lexik aus der eigenen Lektion; die Genuskarte hedgt ihre Endungsgruppen jetzt
korrekt als „reliable groups, not a complete system"); die **zwölf Schreibaufgaben**
(Titel, Aufgabentext, Leitpunkte und Wortgrenzen deckungsgleich mit `a11.js`, sechs
Formulare mit Quelltext und fünf Feldern, sechs Mitteilungen mit drei Leitpunkten, ein
zweiter servertauglicher Zwilling in `netlify/functions/_shared/writingTasks.mjs`,
duplikatsgeprüft); `evaluate-writing.mjs` (Aufgabe aus dem Server-Bank über `task_key`,
`MIN_CHARS = { formular: 12 }`, Identität aus dem JWT); `scoreCheckpoint` (60 % gesamt,
40 % je benotetem Abschnitt, Sprechen nur bei durchgehender Mikrofonbewertung —
nachgerechnet, die Regeln sind an den Grenzen getestet); und **`buildMessage()`** in
`course-reminder.mjs` — A1-Deutsch, keine Behauptung über das Verhalten des Lernenden,
Betreff und CTA aus der Lektion abgeleitet, Lektionstitel gegen `CURRICULUM_A11` gepinnt,
die Auswahl aus **einer** Definition (`public.course_reminder_candidates()`) statt neu
abgeleitet. **Die E-Mail duzt weiterhin, und das lasse ich als Eigentümerentscheidung
stehen — kein MAJOR**, aus denselben Gründen wie in Runde 5. Die Notiz im Kopf der Datei,
um die ich gebeten hatte, fehlt aber (Minor 5). Testlauf: **684 von 684 grün**, Lint 0/0,
Duplikatsprüfung OK.

---

## Bewertungsübersicht (Runde 6, Runde 5 in Klammern)

| Lektion | Deutsch | Niveau | Dialog | Prüfung | Kohärenz | Σ /25 |
|---|---|---|---|---|---|---|
| 1 Hallo, ich bin … | 4 (4) | 4 (4) | 5 (5) | 4 (4) | 4 (4) | **21** (21) |
| 2 Ich bin Studentin | 4 (4) | 4 (4) | 4 (4) | **4** (3) | 3 (3) | **19** (18) |
| 3 Meine Familie | 4 (4) | 4 (4) | **4** (3) | 4 (4) | 3 (3) | **19** (18) |
| 4 Auf dem Flohmarkt | 3 (3) | 3 (3) | 5 (5) | 3 (3) | 3 (3) | **17** (17) |
| 5 Im Klassenzimmer | 5 (5) | 4 (4) | 5 (5) | **2** (3) | 4 (4) | **20** (21) |
| 6 Der erste Tag im Büro | 5 (5) | 4 (4) | 4 (4) | 4 (4) | 3 (3) | **20** (20) |
| 7 Freizeit und Hobbys | 4 (4) | **4** (3) | 5 (5) | **4** (3) | 4 (4) | **21** (19) |
| 8 Termine und Uhrzeit | 4 (4) | 4 (4) | 5 (5) | 4 (4) | 4 (4) | **21** (21) |
| 9 Im Café | 5 (5) | 4 (4) | 5 (5) | **4** (3) | 4 (4) | **22** (21) |
| 10 Am Bahnhof | **4** (5) | 4 (4) | 5 (5) | **4** (3) | 3 (3) | **20** (20) |
| 11 Mein Tag | 4 (4) | 4 (4) | 4 (4) | 4 (4) | 4 (4) | **20** (20) |
| 12 Feste feiern | 5 (5) | 4 (4) | 5 (5) | **2** (3) | 3 (3) | **19** (20) |

Kursmittel **19,9/25** (Runde 5: 19,7 · Runde 4: 19,9 · Runde 3: 20,4 · Runde 2: 19,8 ·
Runde 1: 17,2). Vier Lektionen besser, zwei schlechter, sechs gleich.

**Zur Lesart:** die Gewinner sind Arbeit dieser Runde, und sie sind alle vom selben Typ —
eine Kann-Beschreibung, die vorher nur behauptet war, wird jetzt geübt. **L7 (19 → 21)**
ist der größte Sprung: `arbeiten` gehört jetzt zur Lektion und der e-Einschub steht in
ihrer notice, also drillt sie zum ersten Mal nur Formen, die sie selbst eingeführt hat.
**L9 (21 → 22)**, die stärkste Lektion des Kurses, hat die höfliche Frage bekommen, die
ihr Sprechen-Teil-3-Auftrag verlangt. **L2 (18 → 19)** übt endlich Zahlwörter — der Punkt
bleibt bei 4 und nicht 5, weil sie im falschen Topic liegen und ihr Fehler als
Konjugation gebucht wird. **L3 (18 → 19)** hat den eingefügten Widerspruch verloren und
mit RULE 14 eine Regel bekommen, die ihn nicht wiederkommen lässt. Die beiden Verlierer
sind keine Verschlechterung des Materials, sondern Befunde, die diese Runde zum ersten
Mal gemessen hat: **L5 (21 → 20)**, weil ihr Reparaturitem `extra-a11-l05-09` die
minimale Korrektur bestraft und in beiden Ziehungen steht, und **L12 (20 → 19)**, weil
ihre Ziehung fünf von sieben Plätzen an `mein` gibt und die Höflichkeitsform, die diese
Lektion prüfungsrelevant macht, in keinem der beiden Versuche vorkommt.
**Stärkste:** L9 (22), dann L1, L7 und L8 (je 21) — L8 zum dritten Mal in Folge ohne
einen einzigen eigenen Befund.
**Schwächste:** L4 (17), zum dritten Mal, aus demselben Grund und mit derselben,
längst ausformulierten Reparatur: zu wenig eigene Items.

---

## Würde ich meinen Namen jetzt daraufsetzen?

**Noch nicht — aber die Lücke ist zum ersten Mal kleiner als eine Runde Arbeit, und sie
ist zum ersten Mal vollständig als Prädikat beschreibbar.** Ich habe alle vierzehn Punkte
aus Runde 5 an den Artefakten nachgeprüft: drei Blocker geschlossen (zwei vollständig als
Regel mit poolweitem Test, einer als richtige Mechanik mit unvollständiger Liste), elf
MAJOR geschlossen, davon zehn vollständig. Zwei Dinge sind in dieser Runde qualitativ
passiert, und beide verdienen es, benannt zu werden. Erstens: **die Checkpoints sind
gebaut worden.** Der Lesen-Teil fälscht jetzt durch Ersetzung im eigenen Text, der
Schreiben-Teil enthält die echte, KI-bewertete Kursaufgabe mit einer sauber begründeten
Optional-Regel, der Lösungsschlüssel wird gezogen — und der Abschnitt, den in fünf Runden
niemand angesehen hatte, ist in einer Runde von der schwächsten zur zweitbesten Fläche
des Kurses geworden. Zweitens: **`metalinguisticPrompt` nennt die Muster, die abgelehnt
wurden, und warum.** Der Kommentar sagt, dass `^Welcher Artikel passt zu der Endung` null
Treffer hätte und trotzdem nicht geschrieben wurde, weil vier legitime Items ein Wort
daneben stehen. Das ist genau die Denkbewegung, die diese Reihe seit Runde 2 einfordert:
eine Regel wird an der Frage gemessen, die sie beantwortet, und an den Instanzen, die sie
**nicht** treffen darf.

**Was offen bleibt, ist zum sechsten Mal dieselbe Kante — aber sie hat sich verschoben,
und das ist ein Fortschritt, den ich festhalten will.** Runde 5 schrieb: „eine Regel muss
an der Frage hängen, die sie beantwortet, nicht am Feld, in dem die letzte Instanz
gefunden wurde." Runde 6 hat das zweimal getan (`missingSentenceArticle`,
`cueAnswerMismatch`) und einmal nicht: `caseSensitive` ist kein Prädikat geworden,
sondern ein Opt-in, und opted haben genau die Items, die in Runde 5 namentlich im Bericht
standen. Die Lehre dieser Runde ist deshalb die engste bisher und zugleich die
allgemeinste: **ein Flag, das ein Item tragen muss, ist keine Regel, solange niemand
prüft, welche Items es tragen müssten.** Ein Opt-in-Feld verschiebt die Frage nur von der
Bewertungsstelle zum Kurationsschritt; die Antwort muss trotzdem irgendwo als Prädikat
stehen, mit einem poolweiten Test, sonst ist die nächste Instanz wieder ein Bericht
später. Dasselbe in der zweiten Hälfte von BLOCKER 3: `checkAnswer` hat vier Optionen,
zwei davon (`spelling`, `strict`) leitet es selbst aus dem Item ab, zwei (`caseSensitive`,
`dictation`) bekommt es von der Aufrufstelle — und genau die beiden letzteren sind die,
die auseinanderlaufen. Der Satz, den ich als Test formuliert sehen will, lautet deshalb
nicht mehr „eine Antwort wird überall gleich bewertet", sondern: **welche Prüfregeln für
eine Antwort gelten, entscheidet das Item und nichts sonst.**

Der zweite Satz, der aus dieser Runde mitgeht, betrifft nicht die Bewertung, sondern die
Auswahl: **was nicht gezogen wird, ist nicht gelehrt.** Alle drei großen MAJOR dieser
Runde sind Ziehungsbefunde. L4 zieht vier ungelehrte Nomen, weil sie zu wenige eigene
Items hat. L12 zieht fünfmal `mein` und nie `Ihr`, weil die Vielfaltsgrenze die
Determinative aus der Stoppliste nicht sieht — die Lektion, deren ganzes Thema das
Determinativ ist, ist für ihre eigene Diversitätsregel unsichtbar. Und RULE 11 misst
187 Tokens an einer Lektion, in der die Items gar nicht erscheinen, und ist damit eine
ehrliche Zahl, die keine Reparatur führen kann. Fünf Runden lang war die Frage „ist das
Item richtig"; ab hier ist die Frage „bekommt dieser Lernende in diesen sieben Items das,
was diese Lektion verspricht" — und die einzige Art, sie zu beantworten, ist, die Ziehung
zur Messgröße zu machen, so wie Runde 5 den gebauten Pool zur Messgröße gemacht hat.

Die Arbeit ist überschaubar und vollständig ausgeschrieben: ein Prädikat
`politeCaseItem` plus eine Zeile im Build, ein Prädikat `ambiguousCorrection` plus eine
Aufgabenformel, ein `isDictationTask` an drei Aufrufstellen, eine Kongruenzschranke im
Lesen-Generator, vier L4-Items, ein Zahlen-Topic, eine gestrichene Heuristik in
`widenEquivalentVerbs`, und eine Deckungsregel für die Determinativ-Lektionen. Das ist ein
Tag. Danach unterschreibe ich für alle zwölf.

---

**Minors (fünf Zeilen, nicht erfasst):** (1) Die Buchstabenkarte schreibt weiter `Q ku`
statt `Qu` und mischt Groß-/Kleinschreibung (`be, ce, de` neben `Jot, Vau, Zett`), und
kein Item prüft Q; die Karte `verb-sein` schreibt weiter „Der Wohnort: Ich bin in
Bremen.", während der eigene Dialog „Ich **wohne** in Bremen" sagt. (2) `situation` von L9
trägt weiter die Redaktionsnotiz „(die Einladung kommt in Lektion 12)" — und sie wird dem
zahlenden Lernenden in `CurriculumHomePage.jsx:191` angezeigt. (3) `extra-a11-l08-06`
(„___ Abend habe ich Zeit." → `Am`) hat als einziges Präpositionsitem keine
Aufgabenformel, während sein Zwilling `8970aaea` „(Präposition für Tageszeit)" trägt —
*Heute* und *Jeden* sind beide `wrong`, Tag Wortschatz; in Versuch 2 werden beide Zwillinge
im selben Siebenerblock gezogen. (4) Drei Items heißen *Anna* neben der Kursfigur *Ana*,
eines davon (`aae6d30e`) im Schreiben-Abschnitt von Checkpoint 4; `extra-a11-l04-08` bucht
*Der Stuhl kostet **12** Euro.* als **Wortschatz** statt als Schreibweise; L3 `lines[9]`
führt mit „von meiner Schwester" einen Dativ ein, den A1.1 nicht lehrt. (5) Die
Eigentümerentscheidung zur Anrede der Erinnerungsmail ist weiterhin nirgends
aufgeschrieben — ein Satz im Kopf von `course-reminder.mjs` und eine Ausnahmezeile im
Registertest, sonst schließt eine spätere Runde sie versehentlich mit.

BLOCKER: 3 · MAJOR: 7
