
# A1.1 — DaF-Fachreview #8, nach Runde 8 (2026-09-12)

Reviewer: DaF-Lehrkraft (Erwachsenenbildung, Integrationskurs) und Prüferin für
Goethe-Zertifikat A1 *Start Deutsch 1*. Achter Durchgang. Basis:
`REVIEW-daf-2026-09-12.md` (5 BLOCKER · 45 MAJOR), `REVIEW-daf-2-2026-09-12.md`
(1 BLOCKER · 35 MAJOR · 44 MINOR), `REVIEW-daf-3-2026-09-12.md` (3 BLOCKER · 11 MAJOR),
`REVIEW-daf-4-2026-09-12.md` (3 BLOCKER · 11 MAJOR), `REVIEW-daf-5-2026-09-12.md`
(3 BLOCKER · 11 MAJOR), `REVIEW-daf-6-2026-09-12.md` (3 BLOCKER · 7 MAJOR) und
`REVIEW-daf-7-2026-09-12.md` (3 BLOCKER · 5 MAJOR), angewendet in `main` @ `e7c56a0`
(„A1.1 round 8: close DaF review #7 + report", PR #125). Gemessen wurde ausschließlich
der Commit-Stand. **A1.2 ist durch Eigentümerentscheidung vom 2026-09-13 pausiert und
nicht Gegenstand dieses Reviews.**

Gemessen, nicht angenommen:

* `src/data/curricula/a11.js` — alle zwölf Lektionen, jedes Feld (situation,
  handlungsfeld, canDo, examTeile, wortfeld inkl. `wordId`, dialog, pretest, notice,
  phonetik, hoeren, sprechen inkl. `anrede` und `readAloud`, schreiben inkl. `sample`,
  links, practiceRule inkl. `mustCover`).
* **Die echte Ziehung**: `planPractice(CURRICULUM_A11, a11.json, 1)` **und Versuch 2**
  importiert, alle sieben Items jeder Lektion mit
  `questionDe`/`questionEn`/`options`/`answer`/`accepted`/`explanationDe`/`caseSensitive`
  und dem von `checkOptionsFor(item)` abgeleiteten Optionsobjekt ausgedruckt und einzeln
  bewertet (**168 Items, 104 verschiedene**). Pool: **291** Items, davon **144**
  handgeschrieben, und **alle 144** sind im gebauten Pool angekommen (Runde 7: 289/140).
* `drillsSlug()` aus `src/data/lessonPools/quality.js` über beide Ziehungen, **mit dem
  tatsächlichen Primärslug** jeder Lektion; dazu `taskShape()` und
  `MAX_SAME_TASK_SHAPE` aus `buildLesson.js` über alle 24 Siebenerblöcke.
* `checkAnswer` + `tagError` + `checkOptionsFor` aus `src/lib/lesson/check.js` auf
  gezogene und Checkpoint-Items mit je drei bis sechs plausiblen Lernerantworten
  nachgerechnet; dazu drei poolweite Gegenproben: „jede akzeptierte Antwort wird
  akzeptiert" (**0 Selbstablehnungen** über 168 + 80 Items), Kreuzvergleich
  Lektion ↔ Checkpoint über die 23 geteilten Poolitems und sechs Antwortvarianten je
  Item (**0 Abweichungen**) und ein Robustheitslauf über alle **44** getippten
  Checkpoint-Items (Punkt weg, klein, `ß`→`ss`, Doppelblank: **0** Fehlurteile).
* `exclusionReason()`, `ambiguousCorrection()`, `untaughtForm()`, `politeCaseItem()`
  über **beide** Pooldateien (291 + 144): **0** Ausschlussgründe, **0** mehrdeutige
  Fehlerkorrekturen, **0** ungelehrte Antwortformen, **0** Items, die das
  Höflichkeitsprädikat erfüllen, ohne das Flag zu tragen.
* `buildCardIndex()` aus `src/services/reviewService.js`: **395** Karten, jede gegen
  `gradeTypedReview()` mit der klein geschriebenen Variante ihrer eigenen Antwort
  (**0 Selbstablehnungen**); die **18** Karten mit einer höflichen Form einzeln.
* **Alle vier Checkpoints gebaut**, alle 80 Items ausgedruckt und einzeln bewertet,
  inklusive Lösungsschlüssel, Falsifikation, Lexikfilter, benoteter Schreibaufgabe;
  dazu eine Überschneidungsmessung Diktat ↔ Vorlesen ↔ Lesetext über alle vier.
* Die zwölf A1.1-Karten in der gebauten `netlify/functions/_shared/ruleCards.mjs`
  Zeile für Zeile gegen die notice ihrer eigenen Lektion (Titel, fette Formen,
  Beispielsätze, Lexik) — und der Test, der sie prüft.
* `netlify/functions/evaluate-writing.mjs` (`courseAllowanceFor`,
  `CHECKPOINTS_PER_COURSE`, `courseTaskKeyPrefix`, `WRITING_LIMITS`), der
  429-Zweig in `src/components/lesson/GradedWriting.jsx`, `_shared/writingTasks.mjs`
  gegen `src/data/writingTasks.js`, `_shared/speakingAI.mjs` (`normalizeAnrede`,
  `buildTeacherSystemPrompt`) und `score-readaloud.mjs`.
* Die Chrome: **alle** `src/components/lesson/*.jsx`, `src/pages/lesson/*.jsx`,
  `src/pages/SpeakingPage.jsx` — **und** `src/components/course/*.jsx` sowie
  `src/pages/CurriculumHomePage.jsx`, die der Registertest nicht liest.
* `node scripts/validate-curriculum.mjs a1.1` (RULE 10: 14/262 · RULE 11: 61 informativ ·
  RULE 11b: 4 · RULE 12: **0** · RULE 13: 4 · RULE 6b: 26 · RULE 15: 12 · RULE 16: **0** ·
  RULE 14: 0) und die Implementierung jeder Regel gegen ihre Ratchet-Zahl.
* `npm test`: **750/751 grün, 1 übersprungen** (der A1.2-Ziehungstest).
  `npm run lint`: 0 Fehler, 0 Warnungen. `npm run check:duplicates`: OK.

## Urteil in einem Absatz

**Runde 8 ist die erste Runde dieser Reihe ohne einen einzigen BLOCKER — alle drei
Blocker und alle fünf MAJOR aus Runde 7 sind an den Artefakten geschlossen, und
keiner von ihnen ist in einer anderen Gestalt zurückgekommen.** Das
Höflichkeitsprädikat liest jetzt die **Stelle, an der die Entscheidung fällt**:
`politeCaseItem()` hat Satzgrenze, Cue-Regex und ein Informell-Veto bekommen, die drei
falsch positiven Items aus Runde 7 (`dd1c3d60`, `b19be5e4`, `4299d5ca`) tragen das Flag
nicht mehr, im gebauten Pool trägt es **genau** noch, wer es verdient (sieben Items,
darunter `extra-a11-l12-16` als dokumentierter Override), **0** Items erfüllen das
Prädikat ohne Flag — und `reviewService.caseFlag()` hat für Satzkarten ein eigenes,
bewusst unabhängiges Prädikat, mit dem die Karte „Gut. Wie geht es Ihnen?" `ihnen` jetzt
**wrong** wertet, genau wie `extra-a11-l01-06`; von den 15 lockeren Karten der Vorrunde
sind acht übrig, und die vier sauberen darunter sind der funktionierende Carve-out
(satzinitiales `Sie` = *sie*). Das Schreibkontingent ist **abgeleitet** statt abgetippt:
`courseAllowanceFor(taskKey)` = Kursaufgaben + `CHECKPOINTS_PER_COURSE`, also 16 für
A1.1, der 429 hat in `GradedWriting.jsx` einen eigenen, sichtbaren Satz bekommen, und
`marketing.js` führt beide Zahlen mit Provenienz. `deinen` ist aus Checkpoint 4
verschwunden — `UNTAUGHT_ANSWER_FORMS` und `NEXT_LEVEL_RE` nehmen jede selbsterklärte
Vorschau aus dem Pool, gemessen **0** Treffer über 291 + 144 Items. „Ihre Antwort"
steht auf jedem Übungsbildschirm, `CHROME_FILES` ist ein Glob mit einer
Ausnahmeliste, die ein eigener Test wörtlich nachprüft. Der Falsifikator tauscht kein
Nomen mehr in einer Verbalphrase: „jede Kellnerin Fußball" ist weg, alle acht
`Falsch`-Aussagen der vier Checkpoints sind saubere Detailänderungen. RULE 16 fragt
nach der **Fläche** statt nach dem Feld und steht bei **0**, weil L2 die unbelegte
Behauptung „Lesen Teil 1" abgelegt hat. Und die Ziehung ist so gut wie nie: **7 von 7
verschiedenen Aufgabenformen in allen 24 Blöcken**, kein Item zweimal in einem Versuch,
L6 bei 5/7 und 4/7 statt 4/4, L12 mit der Höflichkeitsform **und** dem guten Wunsch in
**beiden** Versuchen. **Und trotzdem unterschreibe ich noch nicht, weil diese Runde
zum ersten Mal eine Fläche sichtbar gemacht hat, die acht Berichte lang niemand
gemessen hat: die zwölf Regelkarten.** Der A1.2-Durchgang hat vier Regeln dafür
geschrieben — jeder Beispielsatz eine Zeile seiner Lektion, keine Lexik außerhalb des
kumulativen Wortschatzes, jede fette Notice-Form auf der Karte, keine zurückgestellte
Form gelehrt — und A1.1 dann mit `LEGACY_BUDGET = { 'a1.1': { examples: 47,
boldForms: 17, titles: 10 } }` bei seiner **gemessenen Schuld** festgeschrieben, mit dem
ehrlichen Kommentar „Skipping a1.1 is how a finding survives four rounds in this repo"
darüber — nur ist ein Ratchet auf dem Ist-Stand kein Ausweg daraus, sondern dieselbe
Lücke mit einer Zahl davor. Nachgerechnet: die Karte von **L5** eröffnet mit
`ein und eine stehen bei einer neuen Sache` und baut ihr erstes Fehlerbeispiel darauf,
also auf dem unbestimmten Artikel, den erst **L6** einführt; die Karte von **L10**
lehrt mit „Sie stehen früh auf. → Stehen Sie früh auf? Die Vorsilbe bleibt am Ende."
die Satzklammer aus **L11**; die Karte von **L9** lässt `Ich möchte …` weg, das die
eigene notice im **Titel** führt, der pretest verlangt und der Sprechauftrag braucht;
die Karte von **L11** zeigt **keine** ihrer fünf fetten Formen (`schlafen`, `schläft`,
`Gemacht`, `gearbeitet`, `Ich rufe dich an`); die Karte von **L4** erklärt das Genus an
*Freiheit*, *Möglichkeit*, *Freundschaft*, *Mädchen* und *Brötchen* — fünf Wörter, die
in keinem Wortfeld des Kurses stehen; und **zehn von zwölf** Karten tragen einen anderen
Titel als die notice, die sie zusammenfassen. Dazu vier weitere MAJOR, deren Schwerpunkt
wieder gewandert ist — diesmal auf die **Ränder** der geprüften Flächen: ein
Checkpoint-Bauer, der Hören, Lesen und Sprechen unabhängig aus demselben Dialogtopf
zieht (in Checkpoint 4 steht **eine** Zeile dreimal in **einem** Test); ein
Registertest, der statt einer Handliste jetzt zwei Verzeichnisse globt — und das dritte
nicht, in dem `ExamDatePlan.jsx` den Lernenden auf der Kurs-Startseite viermal duzt;
drei Ratchets, die **über** ihrem heute gemessenen Wert stehen und deren
Dokumentationskommentar eine Zahl nennt, die das Skript nicht mehr druckt; und das
Höflichkeitsprädikat, das Items und Satzkarten erreicht, aber weder Musterkarten noch
die **benoteten** Diktate von Checkpoint 1.

**Zahlen: 0 BLOCKER · 5 MAJOR.** Der Kurs hat damit zum ersten Mal die
Blockerfreiheit erreicht, die `docs/course-standard-2026-09-12.md` §8 verlangt — die
zweite Hälfte der Bedingung (0 MAJOR) steht noch aus. MINORs sind am Ende als sechs
Zeilen erfasst.

---

## BLOCKER

**Keine.** Zum ersten Mal in acht Durchgängen.

Ich habe die vier Definitionen einzeln gegen den heutigen Stand geprüft, statt die
Abwesenheit zu behaupten:

* **„Der Lernende wird auf eine richtige Antwort hin falsch bewertet."** Drei
  poolweite Läufe: 0 Selbstablehnungen über 168 gezogene und 80 Checkpoint-Items;
  0 Abweichungen im Kreuzvergleich Lektion ↔ Checkpoint über die 23 geteilten
  Poolitems mit je sechs Antwortformen; 0 Fehlurteile im Robustheitslauf über alle 44
  getippten Checkpoint-Items (fehlender Schlusspunkt, Kleinschreibung, `ß`→`ss`,
  Doppelblank). Die Fallempfindlichkeit trifft heute genau sieben Items, und jedes
  einzelne nennt seinen Anlass im eigenen Text („Frau Kaya", „Höflichkeitsform",
  „Formal address"). Die verbleibende Abweichung geht in die **lockere** Richtung
  (MAJOR 5) und bestraft niemanden.
* **„Dem Lernenden wird für A1 etwas Falsches beigebracht."** Die zwölf notices, die
  zwölf Dialoge und die 80 Checkpoint-Items enthalten nach meiner Durchsicht keine
  falsche Aussage über das Deutsche. Die Regelkarten enthalten **Verfrühtes** und
  **Unvollständiges** (MAJOR 1), aber nichts Falsches — bis auf eine Zeile, die seit
  Runde 7 als MINOR geführt wird und dort bleibt (`Der Wohnort: Ich bin in Bremen.`,
  Minor 2).
* **„Der Kurs verspricht etwas, das er nicht enthält."** RULE 16 steht bei **0**: jede
  der 12 × 6 `examTeile`-Behauptungen hat eine Fläche in ihrer eigenen Lektion, L2 hat
  die unbelegte „Lesen Teil 1" abgelegt, und alle zwölf *Start Deutsch 1*-Teile sind
  über die zwölf Lektionen abgedeckt. Das Schreibkontingent deckt seit Runde 8 die
  sechzehn Flächen, die der Kurs wirklich hat.
* **„Eine geprüfte Form, die der Kurs nie lehrt."** `untaughtForm()` über beide
  Pooldateien: **0**. `deinen` und sein Zwilling `64680d9b` sind aus dem Pool und aus
  Checkpoint 4 verschwunden; an der Stelle steht jetzt `extra-a11-l12-13`
  („[mein / Geburtstag / sein / im Mai]"), eine Form, die die notice von L12 lehrt.

---

## MAJOR

| Feld | Zitat | Problem | Fix (pasteable) | Schwere |
|---|---|---|---|---|
| `LEGACY_BUDGET` in `tests/rule-card-overrides.test.mjs:654–657` ↔ die zwölf A1.1-Karten in `netlify/functions/_shared/ruleCards.mjs` ↔ `notice` jeder Lektion in `src/data/curricula/a11.js` | `const LEGACY_BUDGET = { 'a1.1': { examples: 47, boldForms: 17, titles: 10 }, 'a1.2': { examples: 0, boldForms: 0, titles: 0 } };` · Kommentar darüber: „**A1.1 IS A RATCHET, NOT AN EXEMPTION.** … Skipping a1.1 is how a finding survives four rounds in this repo" · Karte L5: „**ein und eine stehen bei einer neuen Sache.** … Neu: ein Heft, eine Schere, ein Lineal." · Karte L10: „Sie stehen früh auf. → Stehen Sie früh auf? **Die Vorsilbe bleibt am Ende.**" · Karte L9 (Titel): „haben: Hunger, Durst und Zeit" gegen notice-Titel „haben und der Chunk **„Ich möchte …"**" · Karte L4: „-ung, -heit, -keit, -schaft → die: die Entschuldigung, die **Freiheit**, die **Möglichkeit**, die **Freundschaft**. -chen, -lein → das: das **Mädchen**, das **Brötchen**." | **Der A1.2-Durchgang hat für die Regelkarten vier Regeln geschrieben und A1.1 dann auf seiner gemessenen Schuld eingefroren — und die Schuld ist inhaltlich, nicht kosmetisch.** Die Karte ist kein Beiwerk: `explain-answer.mjs` erdet die „Erklär mir das"-Antwort auf ihr, und der Lernende ruft sie genau dann auf, wenn er die Regel **nicht** verstanden hat. Vier Befunde, jeder einzeln nachgerechnet. **(a) Zwei Karten lehren die Grammatik der nächsten Lektion.** L5 (`definite-articles`) eröffnet mit dem **unbestimmten** Artikel und baut ihr erstes Fehlerbeispiel darauf („Das ist eine Schere. Eine Schere ist hier." → „Die Schere ist hier."); `ein/eine` kommt in **L6**, und die notice von L5 erwähnt es mit keinem Wort. L10 (`yes-no-questions`) lehrt mit „Sie stehen früh auf. → Stehen Sie früh auf? Die Vorsilbe bleibt am Ende." die Satzklammer aus **L11**. Das ist genau Regel (d) des A1.2-Guards — „no form the notice explicitly defers is taught" —, nur eine Lektion früher als die Vorschau, die Runde 7 aus dem Pool geworfen hat. **(b) Karten lassen die Lehrpunkte ihrer eigenen notice weg.** Eigene Messung über die zwölf Paare: **18** fett gesetzte Notice-Formen fehlen auf der Karte (der Test zählt 17). Die teuersten: **L11** zeigt **keine** ihrer fünf (`schlafen`, `schläft`, `Gemacht`, `gearbeitet`, `Ich rufe dich an`) — und der pretest derselben Lektion verlangt „Ich habe gearbeitet."; **L9** lässt `Ich möchte …` weg, das im notice-**Titel** steht, im pretest-Modell steht („Ich möchte einen Kaffee, bitte."), in vier Poolitems produziert wird und in Sprechen Teil 3 die Bestellformel ist; **L8** lässt beide Fragen weg, die ihre notice fett setzt (`Wie spät ist es?`, `Hast du Zeit?`). **(c) Eine Karte erklärt mit Lexik, die der Kurs nie hat.** L4 begründet das Genus an *Freiheit*, *Möglichkeit*, *Freundschaft*, *Mädchen*, *Brötchen* — fünf Wörter, die in **keinem** der 262 Wortfeldeinträge stehen; das ist Regel (b) des Guards, und es ist derselbe Befund, den Runde 6 für den Pool als „nie gelehrte Lexik" geschlossen hat, auf der Erklärfläche. **(d) Zehn von zwölf Karten tragen einen anderen Titel als die notice**, die sie zusammenfassen (L2 und L11 sind die beiden Ausnahmen) — der Lernende klickt auf „Ihre Antwort erklären" unter „Das Alphabet: buchstabieren" und bekommt „Die Buchstabennamen und das Buchstabieren". | Das Budget ist richtig gebaut — es ist nur nie bezahlt worden, und die vier Teilzahlungen sind klein und einzeln beweisbar. **Erstens** die beiden Vorgriffe streichen, das ist die einzige Änderung, die Deutsch betrifft: in `scripts/rule-card-overrides.mjs` bei `definite-articles` die Eröffnungszeile durch die notice-Eröffnung ersetzen (`Der bestimmte Artikel zeigt: Wir wissen, welche Sache gemeint ist.`) und das erste Fehlerbeispiel gegen eines aus dem Dialog tauschen („Wo ist Wörterbuch?" steht schon da), bei `yes-no-questions` die Zeile `Sie stehen früh auf. → Stehen Sie früh auf?` ersatzlos entfernen — die Karte hat vier weitere Paare. **Zweitens** die fehlenden fetten Formen ergänzen, je eine Zeile: `separable-verbs-intro` bekommt „schlafen wechselt den Vokal: er schläft." und „Gemacht und gearbeitet stehen als Ganzes: Ich habe gearbeitet."; `verb-haben` bekommt „Man bestellt mit dem festen Ausdruck Ich möchte …: Ich möchte einen Kaffee, bitte." (der Satz steht schon fast da, ohne den Chunk zu benennen); `time-and-dates` bekommt „Die Frage lautet: Wie spät ist es? — und Hast du Zeit? ist eine feste Wendung." **Drittens** in `nouns-gender` die fünf kursfremden Nomen gegen Wortfeldnomen desselben Musters tauschen (`die Entschuldigung`, `die Wohnung`, `die Zeitung` → alle im Wortfeld; `-chen/-lein` ganz streichen, der Kurs hat kein einziges). **Viertens** `titleDe` = `notice.title`, mechanisch für alle zwölf. Und dann **die Regel statt der Zahl**: `LEGACY_BUDGET['a1.1']` auf `{ examples: 0, boldForms: 0, titles: 0 }` setzen — gleich a1.2 —, damit die vier Regeln für **beide** Level hart sind; solange `examples` noch nicht bei 0 ankommt, den Wert nach jeder Runde auf den **gemessenen** Stand nachziehen und im Kommentar die Zahl mitziehen, die das Skript druckt. Ein Ratchet auf dem Ist-Stand, der eine Runde lang nicht fällt, ist eine Ausnahme mit einer Zahl davor. | MAJOR |
| `buildHoeren()` / `buildLesen()` / `buildSprechen()` in `src/lib/checkpoint/buildCheckpoint.js:281, 699, 926` — `ctx` in `buildCheckpoint():972–983` | `const lines = shuffle(dialogLines(chapter), rng); take(lines, 3).forEach(…)` (Hören) · `const source = preferred.length >= 2 ? preferred : dialogLines(chapter); return take(shuffle(source, rng), SECTION_COUNTS.sprechen)` (Sprechen) · `ctx` führt `usedPoolIds: new Set()` — und **keine** Menge benutzter Dialogzeilen | **Die drei Fertigkeitsteile eines Checkpoints ziehen unabhängig aus demselben Dialogtopf, und niemand merkt sich, welche Zeile schon dran war.** Der Bauer hat eine Sperrliste — `usedPoolIds` —, aber sie gilt nur für **Poolitems**, also für Sprachbausteine und Schreiben. Zeilen sind nicht gesperrt. Gemessen über alle vier gebauten Checkpoints: in **Checkpoint 4**, dem benoteten Abschlusstest, steht die Zeile L11/5 „**Ja, ich kaufe am Freitag ein. Kommst du mit?**" **dreimal** — als erstes Diktat (`a1.1-cp4-hoeren-1`), als zweites Vorlesen (`a1.1-cp4-sprechen-2`) und wörtlich im Lesetext von `a1.1-cp4-lesen-2`. Der Lernende tippt den Satz, liest ihn dann laut vor und findet ihn im Lesetext wieder: drei von zwanzig Items desselben Tests hängen an einer Zeile. Und das ist kein Einzelfall — in **allen vier** Checkpoints steht mindestens eine Diktat- oder Vorlesezeile wörtlich in einem Lesetext desselben Tests (CP1: 2, CP2: 3, CP3: 0, CP4: 4; zusammen **neun** Überschneidungen). Für den Lesen-Teil ist das teuer: `a1.1-cp4-lesen-2` fragt „Steht das im Text?" zu einem Satz, dessen Nachbarzeile der Lernende zwei Minuten vorher selbst getippt hat — das Item misst Erinnerung, nicht Lesen, und *Start Deutsch 1* Lesen Teil 1 misst Lesen. Dazu zwei kleinere Messwerte derselben Ursache: **Checkpoint 3** baut zwei seiner vier Lesen-Items (`-lesen-1` und `-lesen-4`) aus **demselben** Textfenster und beide mit dem Schlüssel `Richtig`, und das Poolitem `0ef58eff` („___ bin hier." → `Ich`) steht als Sprachbaustein in **Checkpoint 1 und Checkpoint 3**. | Dieselbe Sperrliste, eine Ebene höher — der Mechanismus steht schon da, er deckt nur die falsche Sorte Gegenstand ab. In `buildCheckpoint()` neben `usedPoolIds` ein zweites Set anlegen: `usedLineKeys: new Set()`, und die Reihenfolge der drei Bauer ist bereits die richtige (Hören, Lesen, Sprechen). In `buildHoeren` vor dem `take`: `const lines = shuffle(dialogLines(chapter), rng).filter((l) => !ctx.usedLineKeys.has(\`${l.lektionId}#${l.idx}\`));` und nach der Wahl jedes Item `ctx.usedLineKeys.add(...)`; in `buildLesen` dasselbe für **alle** Zeilen des gewählten Fensters, nicht nur für die zitierte — das Fenster ist es, was der Lernende liest; in `buildSprechen` dasselbe Filter auf `preferred` **und** auf den Fallback, mit dem bestehenden Rückfall auf ungefilterte Zeilen, wenn zu wenige übrig bleiben (der Bauer kann seit Runde 8 auf andere Fenster und andere Lektionen ausweichen, also läuft kein Abschnitt leer). Zusätzlich `usedPoolIds` **über die vier Checkpoints hinweg** führen: in `CURRICULUM_A11.checkpoints.reduce(...)` ein gemeinsames Set durchreichen, dann kann `0ef58eff` nicht zweimal antreten. Test in `tests/checkpoint.test.mjs`, als Klasse: für **jeden** Checkpoint jedes Curriculums muss gelten, dass die Menge aus Diktatzeilen, Vorlesezeilen und **allen** Zeilen der Lesetexte so viele Elemente hat wie ihre Summe — **keine Zeile zweimal in einem Test** —, und dass `new Set(items.map((i) => i.poolItemId).filter(Boolean)).size` über alle vier Checkpoints gleich ihrer Gesamtzahl ist. | MAJOR |
| `CHROME_FILES` in `tests/course-player.test.mjs:83–90` ↔ `src/components/course/ExamDatePlan.jsx:84, 86, 131, 136` (gerendert von `src/pages/CurriculumHomePage.jsx:135`) | `const CHROME_FILES = [ ...readdirSync(join(ROOT, 'src/components/lesson'))…, ...readdirSync(join(ROOT, 'src/pages/lesson'))…, 'src/pages/SpeakingPage.jsx' ];` · dagegen `ExamDatePlan.jsx`: „**Wann ist deine Prüfung?**" · „Mit einem Datum zeigt **dir** der Kurs, wie viele Lektionen pro Woche reichen. **Du** kannst es jederzeit ändern." · „**Dein** Prüfungsdatum liegt hinter **dir**" · „**Setz** ein neues Datum, wenn **du** einen neuen Termin **hast** — der Kurs bleibt genau da, wo **du** bist." | **Runde 7 hat verlangt, die Handliste durch einen Glob zu ersetzen; Runde 8 hat zwei Verzeichnisse geglobt — und die Kurs-Chrome liegt in drei.** `src/components/course/` ist das dritte, und darin duzt `ExamDatePlan.jsx` auf **vier** Zeilen. Das ist nicht irgendein Bildschirm: `CurriculumHomePage.jsx` rendert den Prüfungsdatum-Plan unter der Weiter-Karte, also auf der Seite, über die **jede** Lektion betreten wird, und die Überschrift „Wann ist **deine** Prüfung?" ist das Erste, was ein Lernender ohne gesetztes Datum dort liest. Zwei Klicks weiter siezt ihn derselbe Kurs durchgehend: `sprechen.open.anrede` steht in **allen zwölf** Lektionen auf `'Sie'`, `buildTeacherSystemPrompt` siezt, jede Aufgabenstellung siezt, und seit Runde 8 steht über **jedem** Eingabefeld „Ihre Antwort". Die du-Tokenliste des Tests (`/\b(du|Du|dir|Dir|…|Deine[mnrs]?|…|hast|…|Setz…)\b/`) trifft alle vier Zeilen einwandfrei — sie wird nur nie auf die Datei angewendet. Der Satz, den Runde 7 als ihre Lehre aufgeschrieben hat („eine Regel, die als Liste geschrieben ist, prüft nur, woran der letzte Bericht gedacht hat"), gilt für eine Liste von **Verzeichnissen** genauso wie für eine Liste von Dateien; gegengeprüft habe ich den Rest: `SaveProgressCard.jsx` und `CurriculumHomePage.jsx` sind registerneutral, es ist genau diese eine Datei. | Vier Zeilen Text, eine Zeile Regel. Am Text, in `ExamDatePlan.jsx`: „Wann ist **Ihre** Prüfung?", „Mit einem Datum zeigt **Ihnen** der Kurs, wie viele Lektionen pro Woche reichen. **Sie können** es jederzeit ändern.", „**Ihr** Prüfungsdatum liegt hinter **Ihnen**", „**Setzen Sie** ein neues Datum, wenn **Sie** einen neuen Termin **haben** — der Kurs bleibt genau da, wo **Sie** sind." An der Regel, in `tests/course-player.test.mjs`: die Verzeichnisliste selbst aus dem Dateisystem holen statt sie zu schreiben — `const CHROME_DIRS = ['src/components/lesson', 'src/pages/lesson', 'src/components/course'];` ist wieder eine Liste, also besser: `const CHROME_FILES = [...walk(join(ROOT, 'src')).filter((f) => f.endsWith('.jsx') && /(lesson|course|Curriculum|Checkpoint|Review|Speaking)/.test(f))]` — **jede** Datei, die eine Kursfläche rendert, gefunden am Pfad, nicht an einer Aufzählung. Die bestehende `REGISTER_EXEMPT`-Konstante und der Test, der jede Ausnahmezeile wörtlich nachprüft, bleiben unverändert; sie sind der richtige Ort für eine begründete Ausnahme. | MAJOR |
| `MAX_UNCOVERED_WORTFELD` / `MAX_UNREHEARSED_CANDOS` / `MAX_UNTAUGHT_DRAWN_TOKENS` in `scripts/validate-curriculum.mjs:245, 271, 327` gegen die heutige Ausgabe von `node scripts/validate-curriculum.mjs a1.1` | Ausgabe: „RULE 10 Wortfeld-Deckung: **14** ungenutzt von 262 (**Ratchet 18**)" · „RULE 11b gezogene Item-Lexik: **4** ungelehrte Tokens (**Ratchet 5**)" · „RULE 12 Kann-Beschreibungen ohne Übung: **0** (**Ratchet 2**)" · dagegen die Kommentare: „35 → 19, and **18** when re-measured 2026-09-13" · „**16 → 5**, in three generated items" · „Re-measured 2026-09-13: **2**." · und, eine Regel weiter, für RULE 16: „L2 dropped the Teil on 2026-09-13 (round 8) and **the ratchet closed at 0**" | **Drei der acht Ratchets stehen über ihrem heute gemessenen Wert, und ihre Dokumentationskommentare nennen jeweils eine Zahl, die das Skript nicht mehr druckt.** Ein Ratchet mit Luft ist keine Regel: RULE 12 misst **0** und erlaubt **2**, also dürfen zwei Kann-Beschreibungen wieder unbeübt werden, ohne dass CI ein Wort sagt — und genau eine davon, L12s „Ich kann mich verabschieden und gute Wünsche aussprechen.", war in Runde 7 MAJOR 4, ist in Runde 8 mit `readAloud: [0, 9]` und zwei neuen Items **teuer erkauft** worden und ist jetzt wieder unbewacht. Dasselbe bei RULE 10 (14 gemessen, 18 erlaubt: vier Wortfeldeinträge dürfen still verwaisen) und RULE 11b (4 gemessen, 5 erlaubt). Dass es anders geht, steht drei Regeln weiter in derselben Datei: RULE 16 ist auf **0** geschlossen worden, in derselben Runde, mit derselben Begründung. Der Befund ist nicht, dass die Zahlen falsch sind — sie sind konservativ, und konservativ ist die richtige Richtung, wenn man sie nicht nachgemessen hat. Der Befund ist, dass die Datei ihre eigene Maxime an dieser Stelle verletzt: „eine Ratchet-Zahl, die man nicht nachrechnen kann, ist keine Messung" (`validate-curriculum.mjs:604`, DaF review #4). Wer den Kommentar liest, rechnet 18 nach und findet 14. | Den Ratchet aus der **Messung** ziehen, statt ihn daneben zu führen — derselbe Satz wie beim Schreibkontingent in Runde 7, eine Datei weiter. Nach jedem Lauf, der eine Regel senkt, im selben Commit: `MAX_UNCOVERED_WORTFELD = 14`, `MAX_UNTAUGHT_DRAWN_TOKENS = 4`, `MAX_UNREHEARSED_CANDOS = 0` — und die Null, wie bei RULE 14 und jetzt RULE 16, als **harte Regel ohne Ratchet** schreiben, damit „0" nicht als Zahl verhandelbar bleibt. Damit das nicht wieder driftet, die Regel maschinell machen statt menschlich, in `tests/curricula.test.mjs`: für **jede** Regel des Validators muss `gemessen === ratchet` gelten, mit einer einzigen erlaubten Ausnahmerichtung — `gemessen < ratchet` **schlägt fehl** mit der Meldung „RULE n misst m, der Ratchet steht auf r: senken Sie ihn im selben Commit". Ein Ratchet, den ein Test auf den Ist-Stand zwingt, kann nur noch fallen, und der Kommentar kann nicht mehr eine andere Zahl behaupten als die Ausgabe. | MAJOR |
| `caseFlag()` in `src/services/reviewService.js:171–178` ↔ `buildCardIndex()` (Karten der Art `pattern:` und `word:`) ↔ `checkOptionsFor()` auf den Diktaten in `src/lib/checkpoint/buildCheckpoint.js:281` | `const caseFlag = (entry, accepted, { sentence = false } = {}) => ( entry?.caseSensitive === true \|\| politeCaseItem({ answer: entry?.answer, accepted: acc }) \|\| (sentence && acc.some(politeSentence)) );` — `{ sentence: true }` wird **nur** an der Satzkartenstelle übergeben · gemessen mit `gradeTypedReview`: `sentence:a1.1-l01:2` „Buchstabieren Sie bitte Chakiri." → klein = **wrong**, `pattern:alphabet-pronunciation` mit **demselben Satz** als erstem `accepted` → klein = **typo, ok: true** · `a1.1-cp1-hoeren-2` (Diktat, benotet) „Gut. Wie geht es Ihnen?" → `gut. wie geht es ihnen?` = **typo, richtig**, während `extra-a11-l01-06` dieselbe Form **wrong** wertet | **Das neue Satzprädikat ist richtig gebaut und wird an genau einer der drei Kartenarten übergeben — und an keinem der benoteten Diktate.** `politeSentence()` ist die Antwort auf Runde 7 („nicht das erste Wort seines Satzes") und funktioniert: von den 18 Karten mit höflicher Form tragen jetzt **10** das Flag, die vier locker gebliebenen **Satz**karten sind der korrekte Carve-out (satzinitiales `Sie` = *sie*: „Sie sprechen Arabisch und Deutsch.", „Das ist eine Lampe. Sie kostet acht Euro."). Was fehlt, ist die Übergabe. `caseFlag(entry, accepted)` ohne `{ sentence: true }` steht an den `word:`- und `pattern:`-Stellen, und Musterkarten führen als `accepted` **dieselben Dialogzeilen**, die die Satzkarten führen: **drei Zeichenketten werden im selben Kurs zweimal verschieden bewertet** — „Buchstabieren Sie bitte Chakiri." (`sentence:a1.1-l01:2` wrong / `pattern:alphabet-pronunciation` ok), „Was sind Sie von Beruf?" (`sentence:a1.1-l02:6` wrong / `pattern:verb-sein` **und** `pattern:numbers` ok) und „Haben Sie auch Hunger? Wir haben Suppe, Salat und Kuchen." (`sentence:a1.1-l09:2` wrong / `pattern:verb-haben` ok). Die dritte, schwerere Fläche ist der **Checkpoint**: zwei der drei Diktate von Checkpoint 1 — `a1.1-cp1-hoeren-2` „Gut. Wie geht es Ihnen?" und `a1.1-cp1-hoeren-3` „Bitte füllen Sie das Formular aus." — tragen eine nicht-satzinitiale Höflichkeitsform, sind `caseSensitive: false`, und die durchgehend klein geschriebene Abschrift zählt dort als **richtig**, im **benoteten** Test. Das ist genau die Bruchlinie, die Runde 7 als BLOCKER 1 beschrieben hat, nur in die lockere Richtung und auf den zwei Flächen, die Runde 8 nicht angefasst hat — niemand wird falsch bewertet, aber die Regel, die L1 und L12 einführen und deren Großbuchstabe in *Start Deutsch 1* Schreiben Teil 2 bewertet wird, ist im eigenen Kurs an drei von fünf Stellen folgenlos. | Das Prädikat einmal an einer Stelle, nicht dreimal an Aufrufstellen. In `reviewService.js` die Satzklausel in `caseFlag` **unbedingt** machen — `politeSentence` prüft die Stellung im Satz und ist auf einer Einwortantwort ohnehin falsch, also kostet es nichts: `const caseFlag = (entry, accepted) => entry?.caseSensitive === true \|\| politeCaseItem({ answer: entry?.answer, accepted: acc }) \|\| acc.some(politeSentence);` — und die Option `{ sentence }` samt ihrer drei Aufrufstellen ersatzlos streichen, damit keine Kartenart sie vergessen kann. Im Checkpoint dieselbe Frage am **Item** beantworten statt an der Sektion: in `buildHoeren` (und `buildSprechen`, wo es heute folgenlos ist, morgen aber nicht) `caseSensitive: politeSentence(line.de)` setzen, sodass `checkOptionsFor()` es wie bei jedem Poolitem abholt — die Vier-Optionen-Regel aus Runde 7 bleibt damit unangetastet, das Item entscheidet weiterhin allein. Test in `tests/checkpoint.test.mjs`, als Klasse und über **alle** Flächen, nicht als Liste: für jede Karte aus `buildCardIndex(CURRICULUM_A11)`, jedes Item des gebauten Pools und jedes getippte Item aller vier Checkpoints muss gelten — wenn die Antwort eine nicht-satzinitiale Höflichkeitsform enthält, ist die klein geschriebene Variante **nicht** `ok`; und wenn sie eine satzinitiale trägt, ist sie es. **Dieselbe Form wird überall gleich bewertet, und die Stellung entscheidet, nicht die Datei.** | MAJOR |

---

## Status der Befunde aus Runde 7

Verifiziert an den Artefakten (gebauter Pool, beide Ziehungen, gebaute
`ruleCards.mjs`, die vier gebauten Checkpoints, der Kartenindex der
Wiederholungsseite, Validator-Ausgabe, Testlauf, Lint), nicht am Diff.

### Die drei BLOCKER

1. **`politeCaseItem()` als Formregex — drei falsch positive Items, 15 falsch
   negative Wiederholungskarten** — **geschlossen, in beide Richtungen und
   nachgemessen.** Das Prädikat hat die Satzgrenze bekommen, die ich verlangt hatte,
   dazu zwei Dinge, um die ich **nicht** gebeten hatte und die es besser machen: ein
   `INFORMAL_VETO_RE` („informell", „duzen", „du-Form") und ein `namesLowercase()`,
   das ein Item ausschließt, dessen eigener Text die kleine Form als Ziel benennt.
   Nachgerechnet: die drei falsch positiven Items (`dd1c3d60`, `b19be5e4`,
   `4299d5ca`) tragen das Flag **nicht mehr**; im gebauten Pool tragen es genau
   **sieben** Items, und jedes einzelne nennt „Frau Kaya", „Höflichkeitsform" oder
   „Formal address" in seinem eigenen Text; **0** Items erfüllen das Prädikat, ohne
   das Flag zu tragen; `extra-a11-l12-16` bleibt die einzige, im Code begründete
   Handeintragung. Die Kartenhälfte hat ein **eigenes**, bewusst unabhängiges
   Prädikat (`politeSentence` in `reviewService.js`) mit einem ausgeschriebenen
   Kommentar darüber, warum es nicht dasselbe sein darf — und die Karte, an der
   Runde 7 den Befund festgemacht hat, ist repariert: `sentence:a1.1-l01:6`
   („Gut. Wie geht es Ihnen?") wertet `ihnen` jetzt **wrong**, wie
   `extra-a11-l01-06`. Von 15 lockeren Karten sind acht übrig, vier davon sind der
   funktionierende Carve-out. **Rest:** die vier Musterkarten und die zwei benoteten
   Diktate, an die das Prädikat nie übergeben wird — MAJOR 5 oben, in der lockeren
   Richtung, und deshalb kein Blocker mehr.
2. **`COURSE_WRITING_FREE_LIFETIME = 12` gegen sechzehn Schreibflächen** —
   **geschlossen, genau wie verlangt, und einen Schritt weiter.**
   `courseAllowanceFor(taskKey)` leitet die Zahl aus `courseWritingTasks(level).length
   + CHECKPOINTS_PER_COURSE` ab, gibt für `a11-l01` **16**, und `LEVEL_OF_PREFIX`
   macht sie kursweise statt global. Der Kommentar über der Konstante schreibt den
   ganzen Befund auf, samt dem Satz, warum er unsichtbar war („the checkpoint item is
   `optional: true`, so `itemCounts()` drops it"). Der 429 hat in
   `GradedWriting.jsx:92` seinen eigenen Satz bekommen — „Ihr Schreibkontingent für
   diesen Kurs ist aufgebraucht." — und er ist vom Kontingentsatz der Prüfungsaufgaben
   unterschieden. `marketing.js` führt **beide** Zahlen (12 und 16) mit Provenienz und
   Datum, `tests/claims.test.mjs` parst sie weiter aus der Funktion heraus, und
   `tests/writing-course.test.mjs` pinnt die Klasse („die Erlaubnis wächst mit dem
   Kurs statt neben ihm") über jedes Level mit Curriculum.
3. **`a1.1-cp4-bausteine-4` verlangt `deinen`, das der Kurs A1.2 zuschreibt** —
   **geschlossen, als Regel mit zwei Prädikaten.** `untaughtForm()` steht in
   `quality.js` und prüft zwei Dinge: `NEXT_LEVEL_RE` (das Item nennt sich selbst
   Vorschau — und die Kasusnamen sind mit aufgenommen, was ich nicht verlangt hatte
   und was richtig ist) und `UNTAUGHT_ANSWER_FORMS`, die 24 Kasusformen der sechs
   Possessiva. Gemessen über **beide** Pooldateien, 291 + 144 Items: **0** Treffer.
   `03bd1113` und der Zwilling `64680d9b` sind aus dem Pool verschwunden; an der
   Stelle von Checkpoint 4 steht jetzt `extra-a11-l12-13`
   („[mein / Geburtstag / sein / im Mai]" → `Mein Geburtstag ist im Mai.`). Die
   Tag-Lücke ist mit geschlossen: `extra-a11-l12-08` gibt für `Ihre` jetzt **Kasus**,
   nicht mehr Rechtschreibung. Ein Reststück, das ich als MINOR führe: dieselbe Form
   `meinen` steht weiterhin in `schreiben.sample` von L12 („Ich feiere am Freitag
   **meinen** Geburtstag."), also in der Musterlösung, die `GradedWriting.jsx:293`
   dem Lernenden **nach** der benoteten Abschlussaufgabe zeigt — das Gate liest
   Poolitems und keine andere Lernerfläche.

### Die fünf MAJOR

| # | Runde-7-Befund | Status | Beleg |
|---|---|---|---|
| 1 | `PracticeItem.jsx` schreibt „Deine Antwort" auf 84 Bildschirmen; `CHROME_FILES` ist eine Handliste | **gelandet, teilweise** | „Ihre Antwort" steht in `PracticeItem.jsx:138`; `CHROME_FILES` globt `src/components/lesson` und `src/pages/lesson` statt dreizehn Dateien aufzuzählen, und ein zweiter Test prüft jede `REGISTER_EXEMPT`-Zeile wörtlich nach, damit eine Ausnahme nicht still driftet. Eigener Grep über beide Verzeichnisse: **null** du-Fundstellen. **Rest:** der Glob liest zwei von drei Kurs-Verzeichnissen — `src/components/course/ExamDatePlan.jsx` duzt viermal auf der Kurs-Startseite (MAJOR 3 oben). |
| 2 | `changedDetail()` fälscht semantisch kaputt („Spielst du jede **Kellnerin** Fußball?") | **gelandet, vollständig** | Der `vocab`-Zweig tauscht kein bloßes Objektnomen in einer Verbalphrase mehr (Commit: „falsifier swaps nouns only in a noun-phrase slot"), und der Bauer weicht bei einem fehlgeschlagenen Pass auf andere Fenster und andere Lektionen aus, statt kurz zu liefern. Ausgezählt über alle vier gebauten Checkpoints: **acht** `Falsch`-Aussagen, **alle acht** saubere Detailänderungen (`sieben`→`eins`, `zwanzig`→`siebzig`, `Null`→`Sechs`, `Ana`→`Lena`, `Tim`→`Ana`, `acht`→`sieben`, `zwanzig`→`drei`, `vier`→`achtzig`) und keine einzige, die den Satz zerstört. Die Verteilung Richtig/Falsch ist 2:2 in CP1, CP2 und CP4 und 2:2 in CP3. |
| 3 | RULE 16 misst am Feld `links` statt an der Fläche; L2 behauptet „Lesen Teil 1" ohne Leseübung | **gelandet, vollständig und als harte Null** | `EXAM_TEIL_COVERS` fragt nach der Fläche der Teil-Familie; die zwei Messartefakte (L5, L11 mit eigener Diktatfläche) sind verschwunden, und der eine echte Befund ist **repariert statt umgedeutet**: L2 trägt heute `examTeile: ["Sprechen Teil 1","Hören Teil 1","Schreiben Teil 2"]` — die Leseklausel ist weg, die Textsorte, die die Lektion wirklich schreibt (Mitteilung), ist benannt. RULE 16 meldet **0**, `MAX_UNBACKED_EXAM_TEILE = 0`. Gegengeprüft: alle zwölf *Start Deutsch 1*-Teile sind über die zwölf Lektionen weiterhin abgedeckt. |
| 4 | L12s letzte Kann-Beschreibung („verabschieden und gute Wünsche") wird nirgends geübt | **gelandet, vollständig** | `sprechen.readAloud` von L12 steht auf `[0, 9]`, Zeile 9 ist „Ja! Mach's gut, Lena. Bis bald!" — der Lernende **spricht** den Wunsch jetzt und `score-readaloud` bewertet ihn. Dazu die zwei Items, die ich vorgeschlagen hatte, beide im Pool und beide gezogen: `extra-a11-l12-17` („Mach's gut und ___ Tag noch!" → `schönen`) in **beiden** Versuchen, `extra-a11-l12-18` („[ich / grüßen / dein / Mama]") in Versuch 2. `grüßen` und `Schönen Tag noch` sind aus RULE 10 verschwunden (18 → 14 ungenutzte Wortfeldeinträge), RULE 12 misst **0**. Und die Regel ist mitgewandert: `rehearsalText()` liest den Dialog nur noch durch `hoeren.lines` und `sprechen.readAloud`. **Kante:** der Ratchet steht weiter auf 2 (MAJOR 4 oben). |
| 5 | L6 zieht zwei fast identische Berufskorrekturen; `answerKey` misst die Lexik, nicht die Aufgabe | **gelandet, besser als verlangt** | `taskShape()` und `MAX_SAME_TASK_SHAPE = 1` sind gebaut — und nachgemessen über **alle 24** Siebenerblöcke (zwölf Lektionen × zwei Versuche): **7 von 7 verschiedenen Aufgabenformen in jedem einzelnen**, und **kein** Item kommt innerhalb eines Versuchs in zwei Lektionen vor. `extra-a11-l06-09` ist von einer zweiten artikellöschenden Berufskorrektur in ein Fill-Blank umgeschrieben, das `eine` **produziert** („Die Firma braucht ___ Verkäuferin."); L6 steht damit in Versuch 1 auf **5/7** statt 4/7. Der Test ist als Klasse formuliert („höchstens eine artikellöschende Berufskorrektur pro Ziehung") und benutzt für den Formvergleich einen Inline-Zwilling statt des ausgelieferten Items. **Kante:** Versuch 2 von L6 steht weiter auf 4/7, weil `1ed2c78f` und zwei `definite-articles`-Items drei der sieben Plätze nehmen — auf `PRIMARY_MIN`, also kein neuer Befund, aber der niedrigste Wert des Kurses. |

Zusätzlich geprüft und weiterhin in Ordnung: die **zwölf Schreibaufgaben** (Titel,
Aufgabentext, Leitpunkte, Wortgrenzen deckungsgleich mit `a11.js`, der servertaugliche
Zwilling in `netlify/functions/_shared/writingTasks.mjs` mit identischen `taskKey`s);
`evaluate-writing` (Identität aus dem JWT, Aufgabe über `task_key` aus der Serverbank,
`MIN_CHARS`, `getTier`); der Weg des Sprechauftrags **samt `anrede`** bis in
`buildTeacherSystemPrompt` (`normalizeAnrede` fällt auf `'Sie'` zurück, alle zwölf
Lektionen stehen auf `'Sie'`, der System-Prompt sagt „Sprich den Lernenden mit Sie an");
`score-readaloud` (Worterkennung, im UI ehrlich *Verständlichkeit* genannt, sauberer
Fallback auf die Selbstbestätigung ohne `OPENAI_API_KEY`); `scoreCheckpoint`
(60 % gesamt, 40 % je benotetem Abschnitt); der Lexikfilter der Checkpoints
(`untaughtAt` über alle 80 Items: **0** ungelehrte Tokens); der Robustheitslauf über die
44 getippten Checkpoint-Items (**0** Fehlurteile). `a11.audio.js` ist weiterhin der
leere Manifest-Stub, und alle Hörflächen fallen ehrlich auf die Computerstimme zurück —
Eigentümeraufgabe, kein Befund. **Die Erinnerungsmail duzt weiterhin, und das lasse ich
als Eigentümerentscheidung stehen — kein MAJOR**, wie in Runde 5, 6 und 7; die Notiz
darüber im Kopf von `course-reminder.mjs` fehlt zum vierten Mal (Minor 5).

**Zu den Ratchets, die ich einzeln beurteilt habe:** **RULE 15** (12 Vorgriffe) ist
unverändert und weiterhin **nicht material** — acht der zwölf stehen in
Vorlese­zeilen (Aussprache, keine Formwahl), die vier Diktatzeilen lassen eine gehörte
Form abschreiben. **RULE 13** (4 Sprechaufträge ohne Mission: L7, L10, L11, L12) ist
unverändert und bleibt vertretbar, weil der Auftrag über `saveCourseContext` vollständig
mitreist. **RULE 6b** (26 Notice-Formen ohne Beleg) und **RULE 10** (14 ungenutzte
Wortfeldeinträge) sind ehrlich gemessen; ihre materielle Überschneidung mit den
Regelkarten steht oben als MAJOR 1.

---

## Bewertungsübersicht (Runde 8, Runde 7 in Klammern)

| Lektion | Deutsch | Niveau | Dialog | Prüfung | Kohärenz | Σ /25 |
|---|---|---|---|---|---|---|
| 1 Hallo, ich bin … | 4 (4) | 4 (4) | 5 (5) | **4** (3) | 4 (4) | **21** (20) |
| 2 Ich bin Studentin | 4 (4) | 4 (4) | 4 (4) | 4 (4) | **4** (3) | **20** (19) |
| 3 Meine Familie | 4 (4) | 4 (4) | 4 (4) | 4 (4) | 3 (3) | **19** (19) |
| 4 Auf dem Flohmarkt | 4 (4) | 4 (4) | 5 (5) | 4 (4) | 4 (4) | **21** (21) |
| 5 Im Klassenzimmer | 5 (5) | **3** (4) | 5 (5) | 4 (4) | 3 (3) | **20** (21) |
| 6 Der erste Tag im Büro | 5 (5) | 4 (4) | 4 (4) | **4** (3) | 3 (3) | **20** (19) |
| 7 Freizeit und Hobbys | 4 (4) | 4 (4) | 5 (5) | 4 (4) | 4 (4) | **21** (21) |
| 8 Termine und Uhrzeit | 4 (4) | 4 (4) | 5 (5) | 4 (4) | 4 (4) | **21** (21) |
| 9 Im Café | 5 (5) | 4 (4) | 5 (5) | 4 (4) | **3** (4) | **21** (22) |
| 10 Am Bahnhof | 5 (5) | **3** (4) | 5 (5) | 4 (4) | 3 (3) | **20** (21) |
| 11 Mein Tag | 4 (4) | 4 (4) | 4 (4) | 4 (4) | **3** (4) | **19** (20) |
| 12 Feste feiern | 5 (5) | 4 (4) | 5 (5) | **4** (3) | **4** (3) | **22** (20) |

Kursmittel **20,4/25** (Runde 7: 20,3 · Runde 6: 19,9 · Runde 5: 19,7 · Runde 4: 19,9 ·
Runde 3: 20,4 · Runde 2: 19,8 · Runde 1: 17,2). Fünf Lektionen besser, vier schlechter,
drei gleich — und der Mittelwert erreicht zum ersten Mal den Höchststand von Runde 3
auf durchgehend **gemessenen** Flächen.

**Zur Lesart:** **L12 (20 → 22)** ist der größte Sprung und der verdienteste: die letzte
Kann-Beschreibung wird jetzt gesprochen (`readAloud: [0, 9]`) **und** produziert (zwei
neue Items, eines davon in beiden Versuchen), der Akkusativ-Vorgriff ist aus dem
benoteten Checkpoint 4 verschwunden, beide Versuche ziehen die Höflichkeitsform, und
ihre Regelkarte ist die einzige neben L2, die **keine** fette Notice-Form auslässt.
**L1 (20 → 21)**, weil die Satzkarte „Gut. Wie geht es Ihnen?" `ihnen` jetzt so wertet
wie das Lektionsitem — die Bruchlinie, an der Runde 7 diese Lektion abgewertet hat, ist
weg (die Musterkarte und das CP1-Diktat bleiben locker, das kostet den fünften Punkt bei
Prüfung, nicht den vierten). **L2 (19 → 20)**, weil sie die Behauptung „Lesen Teil 1"
abgelegt hat, die sie nicht einlösen konnte, und dafür die Textsorte nennt, die sie
wirklich schreibt. **L6 (19 → 20)**, weil `extra-a11-l06-09` jetzt `eine` produziert
statt den Artikel ein zweites Mal zu löschen und die Lektion damit in Versuch 1 auf 5/7
kommt. Die vier Verlierer sind **keine** Verschlechterung des Materials, sondern zum
ersten Mal gemessene Befunde auf einer Fläche, die sieben Berichte lang nur auf
Englischzeilen und Prozentangaben geprüft wurde: **L5 (21 → 20)** und **L10 (21 → 20)**,
weil ihre Regelkarte jeweils mit der Grammatik der **nächsten** Lektion erklärt — L5 mit
`ein/eine`, L10 mit der Satzklammer —, was den Niveaupunkt kostet; **L9 (22 → 21)**,
weil ihre Karte `Ich möchte …` weglässt, den Chunk, den die eigene notice im Titel
führt, der pretest verlangt und Sprechen Teil 3 braucht; **L11 (20 → 19)**, weil ihre
Karte **keine** ihrer fünf fetten Notice-Formen zeigt, darunter die beiden
Perfekt-Chunks, die ihr eigener pretest abfragt.
**Stärkste:** L12 (22), dann L1, L4, L7, L8 und L9 (je 21) — L8 zum fünften Mal in Folge
ohne einen einzigen eigenen Materialbefund.
**Schwächste:** L3 und L11 (je 19) — L3 unverändert wegen der Mischziehung (4/7 auf dem
eigenen Slug in **beiden** Versuchen, drei Plätze an `verb-sein`), L11 wegen ihrer
Regelkarte.

---

## Würde ich meinen Namen jetzt daraufsetzen?

**Auf das Kursmaterial: ja. Auf den Kurs als Ganzes: noch nicht — und der Abstand ist
kleiner als je zuvor.** Ich habe alle acht Punkte aus Runde 7 an den Artefakten
nachgeprüft: drei Blocker geschlossen, fünf MAJOR, davon vier vollständig und als Regel
mit poolweitem Test. **Zum ersten Mal in acht Durchgängen finde ich keinen einzigen
BLOCKER**, und ich habe die vier Definitionen einzeln gegen den Stand geprüft, statt die
Abwesenheit zu behaupten — die drei poolweiten Gegenproben (0 Selbstablehnungen, 0
Kreuzabweichungen, 0 Fehlurteile im Robustheitslauf) stehen oben im Methodenteil, und
jede ist wiederholbar.

Drei Dinge sind in dieser Runde qualitativ passiert. Erstens: **`politeCaseItem()` ist
zum zweiten Mal umgeschrieben worden und diesmal richtig.** Es liest nicht mehr die
Buchstabenfolge der Antwort, sondern ihre **Stellung im Satz**, es hat ein Veto für
Items, die sich selbst informell nennen, und es hat ein zweites Veto für Items, die die
kleine Form im eigenen Text als Ziel benennen — zwei Sicherungen, um die ich nicht
gebeten hatte. Das Ergebnis ist nachgemessen und nicht behauptet: 0 Fehlstellen, 0
falsch Positive, ein dokumentierter Override. Zweitens: **`courseAllowanceFor()` ist die
saubere Anwendung von `CLAUDE.md`s ältester Regel auf ein Limit.** Die Zahl wächst mit
dem Kurs, ein Test hält sie über jedes Level, und der 429 hat aufgehört, wie ein
Netzfehler auszusehen. Drittens, und das ist die Arbeit, die man am wenigsten sieht:
**die Ziehung ist fertig.** 7 von 7 verschiedenen Aufgabenformen in allen 24 Blöcken,
kein Item zweimal in einem Versuch, und die drei Lektionen, die acht Berichte lang
wackelten (L4, L6, L12), ziehen heute aus ihrem eigenen Wortfeld und auf ihrer eigenen
Grammatik.

**Was offen bleibt, ist zum achten Mal dieselbe Kante — aber sie ist jetzt vollständig
aus dem Kursmaterial heraus und sitzt in den Flächen daneben.** Der Satz, der aus dieser
Runde mitgeht, steht als Kommentar in der Testdatei, die ihn verletzt: **„Skipping a1.1
is how a finding survives four rounds in this repo."** Das ist richtig — und ein Ratchet,
der auf dem Ist-Stand einer nie geprüften Fläche eingefroren wird, ist dasselbe
Überspringen mit einer Zahl davor. Die zwölf Regelkarten sind die letzte große Fläche
dieses Kurses, die noch nie gegen ihre eigene Lektion gehalten wurde; die Regeln dafür
sind seit dieser Runde geschrieben, sie gelten für A1.2 bei 0 und für A1.1 bei 47/17/10,
und das Ergebnis ist nachrechenbar: zwei Karten lehren die Grammatik der nächsten
Lektion, eine erklärt das Genus an fünf Wörtern, die der Kurs nie hat, drei lassen die
Formel weg, die ihre eigene notice fett setzt, und zehn von zwölf heißen anders als das,
worauf der Lernende geklickt hat.

Der zweite Satz ist die Verallgemeinerung von drei der fünf MAJOR und er ist eine
Fortschreibung von Runde 7: **eine Regel, die als Liste geschrieben ist, prüft nur,
woran der letzte Bericht gedacht hat — und eine Liste von Verzeichnissen, eine Liste von
Kartenarten und eine Ratchet-Zahl über dem Ist-Stand sind Listen.** `CHROME_FILES` globt
zwei Verzeichnisse und lässt das dritte aus; das dritte enthält den Bildschirm, den der
Lernende vor **jeder** Lektion sieht, und darauf steht „Wann ist **deine** Prüfung?".
`caseFlag()` bekommt sein Satzprädikat an einer von drei Kartenarten übergeben, und die
zwei, die es nicht bekommen, führen dieselben Dialogzeilen. Drei Ratchets stehen über
ihrer gemessenen Zahl, darunter der, der genau die Kann-Beschreibung bewacht, die diese
Runde teuer repariert hat. Und der Checkpoint-Bauer führt eine Sperrliste für Poolitems
und keine für Dialogzeilen — weshalb im benoteten Abschlusstest **eine** Zeile dreimal
steht.

Die Arbeit ist überschaubar und vollständig ausgeschrieben: zwei Zeilen aus zwei
Regelkarten streichen, vier Zeilen in drei andere hinein, fünf Nomen tauschen, zwölf
Titel gleichsetzen und `LEGACY_BUDGET['a1.1']` auf die gemessene Zahl ziehen; ein
`usedLineKeys`-Set im Checkpoint-Kontext plus ein gemeinsames `usedPoolIds` über die
vier Checkpoints; vier Sätze in `ExamDatePlan.jsx` und ein Glob, der die Kursflächen am
Pfad findet; drei Ratchet-Zahlen und ein Test, der `gemessen === ratchet` erzwingt; eine
unbedingte Satzklausel in `caseFlag` und ein `caseSensitive` am Checkpoint-Diktat. Das
ist ein Tag, wie beim letzten Mal — und zum ersten Mal ist keine einzige Änderung am
Lektionstext dabei, weil das Material inzwischen gut ist. Danach unterschreibe ich für
alle zwölf.

---

**Minors (sechs Zeilen, nicht erfasst):** (1) Die Buchstabenkarte mischt weiter Groß-
und Kleinschreibung der Buchstabennamen (`be, ce, de` neben `Jot, Vau, Zett`), schreibt
`Q ku`, und kein Item prüft Q; ihre Frageformel lautet „Wie schreibt man das?", während
die notice von L1 „**Wie buchstabiert man das?**" fett setzt. (2) Die Karte `verb-sein`
schreibt weiter „Der Wohnort: **Ich bin in Bremen.**", während der eigene Dialog „Ich
**wohne** in Bremen" sagt — dritte Runde, und in Sprechen Teil 1 ist „Wo wohnen Sie?"
eine der sechs Pflichtangaben. (3) `schreiben.sample` von L12 benutzt weiter „Ich feiere
am Freitag **meinen** Geburtstag" und den Imperativ „**Komm** um acht Uhr!" — genau die
Form, die `UNTAUGHT_ANSWER_FORMS` seit dieser Runde aus jedem Poolitem entfernt, und sie
steht in der Musterlösung neben der benoteten Aufgabe von Checkpoint 4; das Gate liest
Poolitems und keine andere Lernerfläche. (4) `tagError` bucht einen Zahlwortfehler
weiter als **Wortschatz** statt als Rechtschreibung (`sieber` → Wortschatz) und einen
Höflichkeitsfehler als **Rechtschreibung** (`ihnen` → Rechtschreibung) — eine
Registerentscheidung wird dem Lernenden als Tippfehler angezeigt; folgenlos für die
Remediation, die über `topic` zieht. (5) Die `situation` von L9 trägt weiter die
Redaktionsnotiz „(die Einladung kommt in Lektion 12)" und wird in
`CurriculumHomePage.jsx` angezeigt; `a1.1-cp4-lesen-4` fälscht weiter „Gleis vier" zu
„Gleis **achtzig**" (formal korrekt, aber kein Bahnhof hat Gleis 80);
`a1.1-cp4-bausteine-2` schreibt die Uhrzeit als Ziffer („um **8** Uhr"), obwohl der Kurs
Zahlwörter lehrt; `extra-a11-l12-17` trägt den metasprachlichen Cue „(Adjektiv)",
obwohl `metalinguisticPrompt()` als Prädikat existiert; und
`tests/lesson-engine.test.mjs:439` nennt die drei Höflichkeitsitems weiter **namentlich**
(`const POLITE = ['extra-a11-l12-08', …]`), obwohl `politeCaseItem` exportiert und im
Pool gestempelt ist — eine Liste, wo ein Prädikat bereitliegt. (6) Die
Eigentümerentscheidung zur du-Anrede der Erinnerungsmail ist weiterhin nirgends
aufgeschrieben (zum vierten Mal erbeten), und `tests/lesson-extra-items-a12.test.mjs:453`
ist der einzige übersprungene Test der Suite — mit Arbeitsauftrag in der Begründung,
also korrekt geführt, aber er sollte mit A1.2 wieder anspringen.

BLOCKER: 0 · MAJOR: 5
