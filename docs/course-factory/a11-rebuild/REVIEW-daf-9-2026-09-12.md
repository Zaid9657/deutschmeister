
# A1.1 — DaF-Fachreview #9, nach Runde 9 (2026-09-12)

Reviewer: DaF-Lehrkraft (Erwachsenenbildung, Integrationskurs) und Prüferin für
Goethe-Zertifikat A1 *Start Deutsch 1*. Neunter Durchgang. Basis:
`REVIEW-daf-2026-09-12.md` (5 BLOCKER · 45 MAJOR), `REVIEW-daf-2-2026-09-12.md`
(1 BLOCKER · 35 MAJOR · 44 MINOR), `REVIEW-daf-3` bis `REVIEW-daf-5` (je
3 BLOCKER · 11 MAJOR), `REVIEW-daf-6-2026-09-12.md` (3 BLOCKER · 7 MAJOR),
`REVIEW-daf-7-2026-09-12.md` (3 BLOCKER · 5 MAJOR) und
`REVIEW-daf-8-2026-09-12.md` (0 BLOCKER · 5 MAJOR), angewendet in `main` @
`ae754e6` („A1.1 round 9: close DaF review #8 + report", PR #126). Gemessen wurde
ausschließlich der Commit-Stand. **A1.2 ist durch Eigentümerentscheidung vom
2026-09-13 pausiert und nicht Gegenstand dieses Reviews.**

Gemessen, nicht angenommen:

* **Die echte Ziehung**: `planPractice(CURRICULUM_A11, a11.json, 1)` **und Versuch 2**
  importiert, alle **168** gezogenen Items (davon **104 verschiedene**) mit
  `questionDe`/`questionEn`/`options`/`answer`/`accepted`/`explanationDe`/`caseSensitive`
  und dem von `checkOptionsFor(item)` abgeleiteten Optionsobjekt ausgedruckt und einzeln
  bewertet. Pool: **291** Items, davon **144** handgeschrieben, alle 144 im gebauten
  Pool. **Neu in dieser Runde und zum ersten Mal überhaupt gemessen: der Vergleich
  Versuch 1 ↔ Versuch 2 Item für Item** (62 von 84 identisch) und der Weg des
  Parameters `attempt` bis in die Seite, die ihn setzt.
* `taskShape()` und `MAX_SAME_TASK_SHAPE` über alle 24 Siebenerblöcke — **und
  zusätzlich eine eigene Rahmenmessung**, die jedes Inhaltswort maskiert statt nur die
  langen (4/7 in L4, in beiden Versuchen).
* `checkAnswer` + `tagError` + `checkOptionsFor` auf gezogenen und Checkpoint-Items:
  **0 Selbstablehnungen** über 168 gezogene und 56 getippte Checkpoint-Items;
  ein Kreuzvergleich über **742 Bewertungsflächen** (291 Poolitems + 56 getippte
  Checkpoint-Items + 395 Wiederholungskarten), gruppiert nach der exakten
  Antwortzeichenkette.
* `exclusionReason()`, `ambiguousCorrection()`, `untaughtForm()`, `metalinguisticPrompt()`,
  `politeCaseItem()` über **beide** Pooldateien (291 + 144): **0/0/0/0**, und beim
  Höflichkeitsprädikat **0** Items, die es erfüllen, ohne das Flag zu tragen
  (`extra-a11-l12-16` bleibt der eine dokumentierte Override).
* `buildCardIndex()`: **395** Karten, jede gegen `gradeTypedReview()` mit ihrer eigenen
  Antwort (**0 Selbstablehnungen**); die **18** Karten mit einer höflichen Form einzeln.
* **Alle vier Checkpoints gebaut**, alle 80 Items ausgedruckt und einzeln bewertet;
  dazu drei eigene Messungen: Zeilenüberschneidung Diktat ↔ Vorlesen ↔ Lesetext
  (**0** in allen vier Papieren), Poolitem-Wiederverwendung über die vier Checkpoints
  (**32 von 32 verschieden**) und — neu — der **Abgleich jeder Lesen-Aussage gegen
  ihren eigenen Text** (16 von 16 Items).
* **Die zwölf Regelkarten** in der gebauten `netlify/functions/_shared/ruleCards.mjs`
  Zeile für Zeile gegen notice, Dialog, Wortfeld und pretest ihrer Lektion; dazu die
  vier Guards in `tests/rule-card-overrides.test.mjs` **nachgebaut und gegen die
  Runde-8-Fassungen der L5-/L7-/L10-Karten laufen gelassen**, plus 34 Sondierungswörter
  gegen den Lexik-Guard.
* Das Curriculum: Dialoge, notices, Kann-Beschreibungen, `examTeile`, Wortfeld, Figuren,
  Register, pretests, Diktat-/Vorlesefenster, Schreibaufgaben gegen
  `netlify/functions/_shared/writingTasks.mjs` (zwölf Aufgaben, Text, Leitpunkte,
  Felder, Wortgrenzen **deckungsgleich**), Sprechaufträge über `_shared/speakingAI.mjs`.
* `node scripts/validate-curriculum.mjs a1.1` (RULE 10: 14/14 · RULE 11: 61/61 ·
  RULE 11b: 4/4 · RULE 12: **0, harte Regel** · RULE 13: 4/4 · RULE 6b: 26/26 ·
  RULE 15: 12/12 · RULE 16: **0/0** · RULE 14: 0) — **jede Ratchet-Zahl gleich ihrer
  Messung**; dazu eine eigene Nachmessung der 26 RULE-6b-Formen gegen Dialog, Ziehung,
  Pool, Karte und Checkpoint.
* `npm test`: **758 Tests, 757 grün, 1 übersprungen** (der A1.2-Ziehungstest).
  `npm run lint`: 0 Fehler, 0 Warnungen. `npm run check:duplicates`: OK.

## Urteil in einem Absatz

**Runde 9 hat alle fünf MAJOR aus Runde 8 an den Artefakten geschlossen, und die
Schließungen sind gut: die zwölf Regelkarten sind zum ersten Mal Karten ihrer eigenen
Lektion, `LEGACY_BUDGET` ist gelöscht statt auf null gesetzt, kein Dialogsatz steht mehr
zweimal in einem Checkpoint, kein Poolitem in zwei Checkpoints, `ExamDatePlan.jsx` siezt
wörtlich wie verlangt, alle sieben Ratchets stehen exakt auf ihrer Messung und RULE 12 ist
eine harte Regel ohne Zahl, und `caseFlag()` hat seine Aufrufoption verloren, sodass
Wortkarte, Musterkarte, Satzkarte, Poolitem und benotetes Diktat dieselbe Form gleich
bewerten.** Nachgerechnet: L5 lehrt kein `ein/eine` mehr, L10 keine Satzklammer, L7 kein
`fahren`, L9 führt „Ich möchte …" als Chunk, L11 zeigt alle fünf fetten Formen, und
**alle zwölf** Kartentitel sind die Titel ihrer notice; 0 Zeilenüberschneidungen in vier
Papieren (Runde 8: neun), 32 von 32 Poolitems verschieden, `a1.1-cp1-hoeren-2`
(„Gut. Wie geht es Ihnen?") wertet `ihnen` im benoteten Diktat jetzt **wrong**, genau wie
`extra-a11-l01-06`. **Und trotzdem ist dies nicht die Runde, in der ich unterschreibe —
weil diese Runde zum ersten Mal gemessen hat, was der Lernende beim *zweiten* Anlauf
bekommt, und die Antwort lautet: dasselbe.** `src/pages/lesson/LessonPlayerPage.jsx:46`
schreibt `const [attempt] = useState(1);` — **ohne Setter**. Der Parameter, auf den der
ganze Ziehungsapparat gesät ist (`seedFor(level, nr, attempt)`), dessen Dateikopf
verspricht „a retry gives a **DIFFERENT** seven", den acht Berichte in beiden Ausprägungen
vermessen haben und den `tests/lesson-engine.test.mjs:94` als Eigenschaft pinnt, hat in der
ausgelieferten Anwendung **genau einen Wert**. Wer eine Lektion wiederholt, bekommt
dieselben sieben Aufgaben, für immer — und selbst wenn Versuch 2 erreichbar wäre, sind
**62 von 84 Items (74 %) identisch**, in **L2 und L8 alle sieben**. Der Preis ist in L8 mit
dem Finger nachzuzählen: die notice setzt **halb** neun fett und schreibt „(= 8.30!)", die
Regelkarte gibt ihm zwei Zeilen und einen eigenen Fehlerfall, der Pool hält **vier**
Items dafür bereit — und in beiden Versuchen wird **keines** davon gezogen, ebenso wenig
die offizielle Uhrzeit („acht Uhr dreißig", `2e4e344c`), obwohl L8 „Hören Teil 1"
beansprucht; stattdessen steht „Viertel nach" **zweimal** in denselben sieben. Der zweite
Befund erklärt den ersten: `taskShape()` behält die kurzen Nomen im Schlüssel, deshalb
sind „___ Uhr ist alt. (der, die oder das?)" und „___ Stuhl ist alt. (der, die oder das?)"
zwei verschiedene „Aufgabenformen" — `MAX_SAME_TASK_SHAPE = 1` greift damit in **keinem
einzigen** der 24 Blöcke, und die 7/7, die Runde 8 als Beweis gefeiert hat, dass „die
Ziehung fertig" ist, ist eine Eigenschaft des Schlüssels: L4 zieht in **beiden** Versuchen
**vier** Items desselben Rahmens. Dazu drei weitere: der **Lesen-Teil aller vier
Checkpoints** ist ein Zeichenkettenvergleich — acht der sechzehn Aussagen stehen
buchstabengleich im abgedruckten Text (Schlüssel *Richtig*, Erklärung wörtlich „Der Satz
steht genau so im Text."), die anderen acht sind dieselbe Zeile mit **einem** getauschten
Wort, 2:2 in jedem Papier, sodass „finde den Satz" 4 von 4 Punkten bringt, ohne ein Wort zu
verstehen; `politeCaseItem()` liest die Selbstauskunft eines Items **nur bei
Einwortantworten**, weshalb drei Items, deren deutsche Aufgabenstellung wörtlich
„Schreiben Sie die **höfliche** Frage" lautet, das kleingeschriebene `sie` als
Tippfehler durchwinken, während `Ihr…` im selben Kurs hart ist; und die vier Guards, die
die Regelkarten in Zukunft halten sollen, hätten den Befund, aus dem sie entstanden sind,
**nicht gefunden** — der Lexik-Guard lässt 15 von 16 kursfremden Formen durch, darunter
`hätte`, `dürfte` und `gegangen`, der Vorgriffs-Guard hat in **zehn von zwölf** Lektionen
überhaupt keine Form zu bewachen und feuert in keiner, und die *Regelsatz*-Zeile einer
Karte — genau die Zeile „ein und eine stehen bei einer neuen Sache.", an der Runde 8 den
Befund festgemacht hat — ist als Metasprache vom Beispielsatz-Guard ausgenommen.

**Zahlen: 0 BLOCKER · 5 MAJOR.** Die Blockerfreiheit aus Runde 8 hält und ist in dieser
Runde erneut gegen alle vier Definitionen geprüft worden, nicht behauptet. Die zweite
Hälfte der Bedingung aus `docs/course-standard-2026-09-12.md` §8 (0 MAJOR) steht weiter
aus. MINORs sind am Ende als sieben Zeilen erfasst.

---

## BLOCKER

**Keine.** Zum zweiten Mal.

Ich habe die vier Definitionen wieder einzeln gegen den heutigen Stand gemessen, statt die
Abwesenheit zu behaupten:

* **„Der Lernende wird auf eine richtige Antwort hin falsch bewertet."** **0**
  Selbstablehnungen über 168 gezogene und 56 getippte Checkpoint-Items. Der
  Kreuzvergleich über **742** Bewertungsflächen, gruppiert nach der **exakten**
  Antwortzeichenkette, findet neun Gruppen mit verschiedenen Urteilen — und **jede
  einzelne geht in die lockere Richtung** (`correct` statt `typo`, weil das eine Item
  seine kleine Zwillingsschreibung in `accepted` führt und das andere nicht) oder ist die
  **richtige** Unterscheidung: `Sie` ist in `extra-a11-l03-08` („Frau Kaya, sprechen ___
  Englisch?") hart und in `dd1c3d60` („___ ist meine Mutter.") locker, `Ihr` in
  `extra-a11-l12-08` hart und in `4299d5ca` („___ seid meine Freunde.", Erklärung:
  „informell") locker, `Ihre` in `b269d424` (Höflichkeitsform) hart und in
  `extra-a11-l12-10` („sie, Plural") locker. Niemand wird auf eine richtige Antwort hin
  bestraft. Die verbleibende Abweichung ist MAJOR 4 und ist zu **mild**.
* **„Dem Lernenden wird für A1 etwas Falsches beigebracht."** Die zwölf notices, die
  zwölf Dialoge, die **neu geschriebenen** zwölf Regelkarten und die 80
  Checkpoint-Items enthalten nach meiner Durchsicht keine falsche Aussage über das
  Deutsche. Die beiden Zeilen, die Runde 7 und 8 als MINOR geführt haben, sind **weg**:
  `verb-sein` schreibt heute „Beispiele: Ich komme aus Marokko. **Ich wohne in Bremen.**"
  statt „Ich bin in Bremen." Übrig bleibt eine einzige anfechtbare Zeile
  (`alphabet-pronunciation`: „**Ü ist ein eigener Buchstabe.**" — das Alphabet hat 26
  Buchstaben, die Umlaute sind keine eigenen; die eigene `content`-Zeile sagt es mit
  „Dazu kommen die **Formen** Ä …" richtig), Minor 1.
* **„Der Kurs verspricht etwas, das er nicht enthält."** RULE 16 misst **0** und hat
  keinen Ratchet mehr; alle 12 × 6 `examTeile`-Behauptungen haben eine Fläche in ihrer
  eigenen Lektion, alle zwölf *Start Deutsch 1*-Teile sind abgedeckt. Der Schreibumfang
  deckt die sechzehn Flächen. **Grenzfall, den ich bewusst nicht hochstufe:** dass die
  Uhrzeit-Formen `halb` und `acht Uhr dreißig` nie geübt werden (MAJOR 1), ist ein
  Übungsloch, kein gebrochenes Versprechen — der Kurs **lehrt** beide, in notice und
  Karte.
* **„Eine geprüfte Form, die der Kurs nie lehrt."** `untaughtForm()` über beide
  Pooldateien: **0**. `NEXT_LEVEL_RE` und `UNTAUGHT_ANSWER_FORMS` halten; im gebauten
  Pool steht keine Possessivform des nächsten Levels, in Checkpoint 4 keine Vorschau.

---

## MAJOR

| Feld | Zitat | Problem | Fix (pasteable) | Schwere |
|---|---|---|---|---|
| `src/pages/lesson/LessonPlayerPage.jsx:46` ↔ `src/lib/lesson/buildLesson.js:6–7, 31–39, 432` ↔ `tests/lesson-engine.test.mjs:94–101` | `const [attempt] = useState(1);` — **kein Setter, kein zweiter Wert, nirgends** · Dateikopf von `buildLesson.js`: „The 7 controlled-practice items are drawn with a seeded PRNG keyed on (level, lektion nr, attempt) … **a retry gives a DIFFERENT seven**" · Test: `const attempt2 = pickPracticeItems(POOL, rule, seedFor('a1.1', 1, 2)).map((i) => i.id); assert.notDeepEqual(a, attempt2, 'attempt 2 must not repeat attempt 1');` · gemessen an L8: notice „**halb** neun (= 8.30!)", Karte „Die Uhrzeit halb neun ist 8.30 und nicht 9.30." + eigener Fehlerfall — Ziehung Versuch 1 **und** 2: `extra-a11-l08-07, extra-a11-l08-06, 8970aaea, extra-a11-l08-05, extra-a11-l08-08, 69162961, extra-a11-l08-02`, **dieselben sieben** | **Die zweite Ziehung ist in der ausgelieferten Anwendung nicht erreichbar, und dort, wo sie es wäre, ist sie zu 74 % die erste.** Drei Messungen, jede einzeln nachrechenbar. **(a) `attempt` ist verdrahtet.** Der einzige Produktionsaufruf von `buildLesson` steht in `LessonPlayerPage.jsx:72` und bekommt `attempt` aus `useState(1)` ohne Setter; es gibt keinen zweiten Aufrufer, keinen Query-Parameter und keine gespeicherte Versuchszahl. Wer eine Lektion wiederholt — und die Wiederholung ist im Standard §3 der Remediationsweg — bekommt **dieselben sieben Items**, unbegrenzt oft. **(b) Selbst Versuch 2 wäre kaum ein zweiter.** Item für Item über alle zwölf Lektionen: **62 von 84 identisch**; L2 **7/7**, L8 **7/7**, L3 6/7, L7 6/7, L10 6/7, L1 5/7, L6 5/7, L9 5/7. Der Test, der „a retry gives a different seven" pinnt, prüft **eine** Lektion, über eine **andere** Funktion (`pickPracticeItems` statt `planPractice`) und mit `notDeepEqual` auf ein **geordnetes** Array — eine Umsortierung derselben sieben genügt ihm, und genau das liefert der Bauer in L2 und L8. **(c) Der Preis ist inhaltlich und trifft die teuerste Stelle des Kurses.** In L8, der Uhrzeit-Lektion, nehmen `um`/`am` vier der sieben Plätze (`l08-02`, `l08-06`, `8970aaea`, `l08-07`), „Viertel nach" **zwei** (`l08-05` für 8.15, `69162961` für 2:15 — dieselbe Antwortform zweimal), und der Pool hält ungezogen bereit: `extra-a11-l08-03` („Es ist ___ neun. (8.30)" → `halb`), `36f2bd1a`, `a8759f56`, `3e14842d` und `2e4e344c` („Es ist ___. (14:30, offiziell)" → `vierzehn Uhr dreißig`). Über zwölf Lektionen à sieben Items **und** vier Checkpoints à zwanzig produziert oder erkennt der Lernende **kein einziges Mal** „halb neun" oder die offizielle Uhrzeit — die beiden Formen, die *Start Deutsch 1* Hören Teil 1 und 2 abfragen und die die eigene notice fett setzt. Dasselbe gilt für „Viertel vor" (`f12532bf`, `extra-a11-l08-04`), obwohl die eigene Dialogzeile 3 es sagt. | Zwei Zeilen Produktcode und eine Regel. **Erstens**, `LessonPlayerPage.jsx`: den Versuch aus dem Fortschritt lesen statt ihn zu setzen — `const [attempt, setAttempt] = useState(1);` und beim Betreten einer bereits abgeschlossenen Lektion `setAttempt(completedCount + 1)` (die Zahl steht in `lesson_attempts`/`recordLocalLesson` schon), plus im Abschlussbildschirm (`RecapStage.jsx`) einen Knopf „Noch einmal üben", der `setAttempt(a => a + 1)` ruft und `stageIndex` zurücksetzt. **Zweitens** die Zahl der Versuche nicht bei zwei enden lassen: `seedFor` ist auf jedem `attempt` definiert, also `attempt = 1 + (abgeschlossene Durchgänge mod N)`. **Drittens** die Regel, und sie gehört in `tests/lesson-engine.test.mjs` als Klasse über **alle zwölf** Lektionen und über `planPractice`, nicht über `pickPracticeItems`: für jede Lektion und jedes Versuchspaar (1,2) und (2,3) muss `new Set([...a].filter(x => b.has(x))).size <= 3` gelten — **höchstens drei der sieben Items dürfen wiederkehren** —, und der Vergleich muss auf **Mengen** laufen, nicht auf Arrays, damit eine Umsortierung nicht mehr als Erfolg zählt. Dazu eine zweite Regel, die das Loch in L8 schließt und allgemein ist: für jede Lektion muss **jede fett gesetzte notice-Form, die der Pool in einem Item produziert, in mindestens einem der ersten drei Versuche gezogen werden** — das ist RULE 6b eine Ebene weiter (siehe MINOR 3) und macht `halb` ziehbar, statt es nur vorrätig zu halten. | MAJOR |
| `taskShape()` in `src/lib/lesson/buildLesson.js:203–204` ↔ `MAX_SAME_TASK_SHAPE = 1` (ebd. 78–88) | `export const taskShape = (item) => \`${(item && item.type) \|\| ''}:${withGaps(bare(item && item.questionDe)).replace(/[a-zäöüß]+in\b\|[a-zäöüß]{6,}/g, '·')}\`;` · Kommentar darüber: „**`taskShape` collapses exactly that**: one per Lektion, so the second seat goes to a different kind of task." · gemessene Schlüssel in L4, Versuch 2: `"fill_blank:_ uhr ist alt der die oder das"`, `"fill_blank:_ stuhl ist alt der die oder das"`, `"fill_blank:_ preis ist zwölf euro der die oder das"`, `"fill_blank:_ · ist teuer der die oder das"` — **vier verschiedene Schlüssel, eine Aufgabe** | **Die dritte Diversitätsachse, die Runde 8 als Antwort auf DaF #7 MAJOR 5 gebaut hat, greift in keinem einzigen der 24 Blöcke.** Der Ersetzungsausdruck maskiert nur Wörter ab **sechs** Buchstaben und `-in`-Ableitungen; `uhr`, `stuhl`, `regal`, `preis`, `bild`, `buch`, `stift` sind kürzer und bleiben im Schlüssel stehen. Damit ist „___ Uhr ist alt. (der, die oder das?)" eine andere „Aufgabenform" als „___ Stuhl ist alt. (der, die oder das?)", und die Obergrenze 1 wird nie erreicht. Gegenprobe mit einem Schlüssel, der **jedes** Inhaltswort maskiert: L4 steht in **beiden** Versuchen auf **4/7** desselben Rahmens (`fill_blank:_ · ist · der die oder das`), L8 auf 5/7, L9 auf 5/7 (Versuch 2), L12 auf 5/7 (Versuch 2), L5 auf 6/7 — gegenüber dem gemeldeten **7/7 in allen 24 Blöcken**. Der Lernende in L4 füllt also viermal hintereinander denselben Satzrahmen mit einem anderen Nomen, und der Schutz, der das verhindern soll, meldet Vollständigkeit. Das ist dieselbe Befundklasse wie MAJOR 1: eine Regel, die **eine Instanz** geschlossen hat (L6s zwei Berufskorrekturen, die beide **lange** Nomen tragen und deshalb kollabieren) und **ihre Klasse nicht**. | Die Maskierung an die **Wortart** binden statt an die Wortlänge, in `taskShape`: erst jedes Token gegen `LEMMA_STOPWORDS` prüfen (die Liste steht zwei Bildschirme höher und enthält genau die Funktionswörter, die den Rahmen ausmachen) und alles, was **nicht** darin steht und keine Aufgabenformel ist, durch `·` ersetzen — `bare(q).split(/\s+/).map(w => (w === '_' \|\| LEMMA_STOPWORDS.has(w) \|\| TASK_WORDS.has(w)) ? w : '·').join(' ')`. Dann ist „___ Uhr ist alt." derselbe Schlüssel wie „___ Stuhl ist alt.", und `MAX_SAME_TASK_SHAPE = 1` bindet zum ersten Mal. Weil das die Ziehung sofort verengt, in denselben Commit eine gemessene Obergrenze statt einer geratenen: `MAX_SAME_TASK_SHAPE = 2` einführen, laufen lassen, und den Wert **auf die Messung ziehen**, wie es `tests/curricula.test.mjs:483` für die Validator-Ratchets bereits erzwingt. Test in `tests/lesson-engine.test.mjs`, als Klasse: für **jede** Lektion und **jeden** Versuch muss der größte Rahmen ≤ `MAX_SAME_TASK_SHAPE` sein, mit der Fehlermeldung, die die Items auflistet — und ein zweiter Test, der die Maskierung selbst prüft, indem er zwei Inline-Zwillinge („___ Uhr ist alt. (der, die oder das?)" / „___ Stuhl ist alt. (der, die oder das?)") vergleicht und **Gleichheit** verlangt. | MAJOR |
| `buildLesen()` in `src/lib/checkpoint/buildCheckpoint.js:763–800`, Zeile `let statement = quoted.de; let explanationDe = 'Der Satz steht genau so im Text.';` | `a1.1-cp4-lesen-2`: promptDe „Steht das im Text? „**Lena: Und wann stehst du morgens auf?**"" · text: „**Lena: Und wann stehst du morgens auf?** Tim: Ich stehe um sechs auf. Ich bin müde. Lena: Kaufst du heute ein?" · answer `Richtig` · explanationDe „Der Satz steht genau so im Text." · dagegen `a1.1-cp4-lesen-1`: Aussage „Ana: Kostet die Fahrkarte **acht** Euro?" gegen Text „… Kostet die Fahrkarte **zwanzig** Euro? …", answer `Falsch` | **Der Lesen-Teil aller vier Checkpoints ist ein Zeichenkettenvergleich, und der Kurs schreibt das in seine eigene Erklärung.** Ausgezählt über alle **16** Lesen-Items der vier Papiere: **acht** Aussagen stehen **buchstabengleich** im abgedruckten Text — inklusive des vorangestellten Sprechernamens („Ana: ", „Frau Kaya: ") — und tragen den Schlüssel *Richtig*; die anderen **acht** sind dieselbe Zeile mit **genau einem** getauschten Wort (`zwanzig`→`acht`, `vier`→`sieben`, `sechs`→`eins`, `Tim,`→`Ana,`, `Lena,`→`Paul,`, `Handy?`→`Büro?`, `siebzig`→`zwanzig`, `Lena`→`Ana`) und tragen *Falsch*. Die Verteilung ist in **jedem** Papier 2:2. Die Strategie „suche die Zeile; finde ich sie Buchstabe für Buchstabe, ist es Richtig" liefert damit **4 von 4** in jedem Checkpoint, ohne dass ein Wort Deutsch verstanden werden muss — in einem Abschnitt, den die 40-%-Regel zum Durchfallgrund machen kann. Runde 5 hat an dieser Funktion den Schlüsselrhythmus repariert und Runde 7 den Falsifikator, aber die *Richtig*-Hälfte ist seit dem ersten Bau `statement = quoted.de`, und niemand hat sie gegen ihren eigenen Text gehalten. *Start Deutsch 1* Lesen Teil 1 prüft etwas anderes: die Aussage ist eine **Umformulierung** („Frau Müller kommt am Montag."), und genau die Umformulierung ist das Lesen. Zwei kleinere Messwerte derselben Ursache: zwei der acht *Falsch*-Aussagen tauschen eine **Anrede** (`cp3-lesen-2`, `cp3-lesen-3`) statt einer Tatsache — wer den Text nicht liest, hört trotzdem, dass Lena Tim anspricht; und **jede** Aussage trägt den Sprechernamen mit, was den Suchraum auf eine von drei Zeilen verkleinert. | Die *Richtig*-Hälfte muss dieselbe Arbeit leisten wie die *Falsch*-Hälfte: eine **Aussage über** den Text, nicht ein Zitat aus ihm. Der Bauer hat das Material dafür schon — `speakerNames(chapter)` und `contentWords(chapter)` stehen im `ctxWords`. In `buildLesen`, für `wantRichtig`: aus der gewählten Zeile eine Aussage in der **3. Person** bilden statt sie zu zitieren — `Lena: Ich stehe um sechs auf.` → `Lena steht um sechs auf.` —, den Sprechernamen **aus der Aussage entfernen** und das Verb aus der Zeile übernehmen; wo die Umformung nicht mechanisch gelingt (Frage, Imperativ), auf die nächste Zeile des Fensters ausweichen, wie der Falsifikator es bereits tut. Die *Falsch*-Hälfte erbt dieselbe Umformung **und danach** die Detailänderung, damit beide Hälften gleich aussehen und die Länge kein Signal ist. Zusätzlich zwei harte Kleinigkeiten: kein Sprechername mehr in der Aussage, und `changedDetail` darf keine Anrede tauschen (`names` nur als Satzsubjekt, nicht im Vokativ). Test in `tests/checkpoint.test.mjs`, als Klasse über **jeden** Checkpoint **jedes** Curriculums: **keine** Lesen-Aussage darf als Teilzeichenkette in ihrem eigenen Text vorkommen (nach Normalisierung von Leerraum und Anführungszeichen), und keine darf sich von einer Textzeile in genau einem Token unterscheiden, **ohne** dass dieses Token ein Inhaltswort ist. Die Meldung nennt Item-Id, Aussage und Text. | MAJOR |
| `politeCaseItem()` in `src/data/lessonPools/quality.js:697–715`, Klausel 4 · `POLITE_CUE_RE` (ebd. 663) | `if (acc.every((a) => !a.includes(' '))) { … return POLITE_CUE_RE.test(text) \|\| acc.every((a) => UNAMBIGUOUS_POLITE_RE.test(a)); }` · `// 4. Sentences are read as sentences.` · `return acc.every(politeSentenceAnswer);` — **die Selbstauskunft `text` wird hier nicht mehr gelesen** · gemessen: `78abfd45` „Schreiben Sie die **höfliche** Frage: [Sie / in Berlin / wohnen]" → `Wohnen Sie in Berlin`, `caseSensitive: false`, klein = **typo** · `6f0fbd89` „Schreiben Sie die **höfliche** Frage: [Sie / Kinder / haben]" → `Haben Sie Kinder`, klein = **typo** · `extra-a11-l10-08` „Bilden Sie die **höfliche** Frage: [fahren / Sie / morgen / nach Deutschland]", explanationDe „**Höfliche** Frage mit Sie: Verb zuerst, dann Sie.", klein = **typo** · dagegen `83bab298` „[das / ist / Ihre Adresse]" → **wrong** | **Das Prädikat fragt ein Item nach seiner eigenen Aufgabenstellung — aber nur, wenn die Antwort ein einziges Wort ist.** Bei einer Satzantwort entscheidet allein `politeSentenceAnswer()`, also die **Stellung** der Form im Satz, und `POLITE_CUE_RE` — das Regex, das „höflich", „Höflichkeitsform", „formal address" und „Frau X" erkennt — wird nie gefragt. Folge, gemessen über alle 742 Flächen: von acht Satzantworten mit nicht-satzinitialer Höflichkeitsform tragen **sechs** einen Höflichkeits-Cue im eigenen Text, und **vier** davon sind trotzdem locker. Der Kurs trennt damit nach der **Form**, nicht nach der **Aufgabe**: `Ihre Adresse` ist hart (weil `/^Ihr(e|en|em|er|es)?$/` greift), `Sie` im selben Satztyp ist weich — und drei der vier lockeren Items sind genau die, deren deutsche Aufgabenstellung das Wort **höflich** enthält. Ein Lernender, der „Wohnen sie in Berlin?" schreibt, hat die Aufgabe „Schreiben Sie die höfliche Frage" **nicht** gelöst (der Satz heißt dann „Wohnen sie …?" = die Leute) und bekommt „fast richtig". Der PR-Kommentar in `reviewService.js` nennt `extra-a11-l10-08` ausdrücklich als gewollten Carve-out mit der Begründung „a bare non-initial `Sie` **with no address cue**" — die Begründung trifft auf dieses Item nachweislich nicht zu: der Cue steht zweimal in seinem eigenen Text. In *Start Deutsch 1* Schreiben Teil 2 und in jeder Mitteilung des Kurses ist genau dieser Großbuchstabe bewertet. | Die Selbstauskunft aus der Einwortklausel herausziehen, damit sie für **jede** Antwortgestalt gilt — vier Zeilen in `quality.js`: vor Klausel 3 ein gemeinsames `const declaresPolite = POLITE_CUE_RE.test(text) && !INFORMAL_VETO_RE.test(text);`, in Klausel 4 dann `return declaresPolite \|\| acc.every(politeSentenceAnswer);`. Damit wird **kein** Item strenger, das seine Aufgabe nicht selbst als höflich bezeichnet — insbesondere bleibt das Diktat `a1.1-cp1-hoeren-3` („Bitte füllen Sie das Formular aus.") **locker**, und das ist richtig so (siehe „Zum bekannten Rest" unten). Streng werden genau die drei Items, deren Aufgabenstellung „höfliche Frage" verlangt. Test in `tests/lesson-engine.test.mjs`, als Klasse und ohne Id-Liste: für **jedes** Item beider Pooldateien, **jede** Karte aus `buildCardIndex` und **jedes** getippte Item aller vier Checkpoints muss gelten — nennt der eigene Text die Aufgabe höflich (`POLITE_CUE_RE` ohne `INFORMAL_VETO_RE`) **und** trägt die Antwort eine Höflichkeitsform, dann ist die klein geschriebene Variante **nicht** `ok`. Dazu die Gegenrichtung, damit die Regel nicht überschießt: trägt der Text **keinen** Cue und steht die Form satzinitial, muss sie `ok` sein. **Die Aufgabe entscheidet, nicht die Wortgestalt.** | MAJOR |
| `tests/rule-card-overrides.test.mjs:638–653` (die vier Guards), 789–846 (Guard b), 900–929 (Guard d), 716–726 (`exampleSentences`) | Kommentar: „(b) **NO WORD FROM OUTSIDE THE COURSE-SO-FAR** … (d) **NO FORM THE NOTICE EXPLICITLY DEFERS IS TAUGHT**" · Guard b, Abdeckungstest: `token === form \|\| token.startsWith(form) \|\| token.endsWith(form) \|\| form.startsWith(token) \|\| commonPrefix(token, form) >= 4` · Guard d: `const DEFERRAL_RE = /(\S+)\s+(?:kommt\|folgt)\s+(?:erst\s+)?(?:in\s+Lektion\s+\d+\|in\s+A1\.\d)/g;` · `exampleSentences`: filtert jedes Segment weg, das `META_SENTENCE_VERB` trifft (`steht\|stehen\|wird\|…`) | **Die vier Guards halten die zwölf Karten von heute, aber nicht die Klasse, aus der sie entstanden sind — nachgebaut und gegen die Runde-8-Fassungen laufen gelassen.** Drei Messungen. **(a) Guard (b) beißt nicht.** Der Abdeckungstest akzeptiert jeden Suffix- und Präfixtreffer und jede Vier-Zeichen-Gemeinsamkeit; 16 kursfremde Kleinschreibungen gegen den Stand von L5 geprüft, **15 gehen durch**: `repariert`, `verhandelt`, `unterschreibt`, `gewöhnlich`, `zuverlässig`, `geschwommen`, `gegangen` — und `müsste`, `dürfte`, `wäre`, `hätte`, also Konjunktiv II, den weder A1.1 noch A1.2 kennt. Die **Nomen**hälfte (der ältere Wortfeld-Test) beißt dagegen wirklich: `Schmetterling`, `Waschmaschine`, `Zahnarzt`, `Gewerkschaft` werden gefangen — die Lücke ist also präzise die der Verben, Adjektive und Adverbien, und genau dort sitzt Grammatik. **(b) Guard (d) hat in zehn von zwölf Lektionen nichts zu bewachen.** Das Regex liest das Wort **vor** „kommt/folgt"; in L4, L5, L9 und L11 steht dort eine Klammer mit einem Metabegriff („(Akkusativ) kommt in A1.2", „(Dativ) folgen in A1.2", „(Perfekt) kommt in A1.2"), der von `META_NOUNS` sofort weggefiltert wird; in L9 fehlt „kommt/folgt" ganz („gehört zu den Modalverben in A1.2"). Übrig bleiben **zwei** Treffer, und beide sind Fehlzündungen: `eins` (aus „Die Uhrzeit um eins folgt in Lektion 8") und `haben` (aus „haben kommt in Lektion 9"). Der Guard feuert heute auf **keine** A1.1-Karte — er kann nicht. **(c) Der Regelsatz einer Karte ist vom Beispielsatz-Guard ausgenommen.** `exampleSentences` verwirft jedes Segment mit einem Metasprache-Verb; „ein und eine **stehen** bei einer neuen Sache." — die Eröffnungszeile, an der Runde 8 MAJOR 1 festgemacht hat — trifft `stehen` und wird nie geprüft. Nachgerechnet: von den Runde-8-Zeilen fängt Guard (a) die **Beispiele** („Das ist eine Schere.", „Stehen Sie früh auf?", „ich fahre, du fährst, er fährt."), aber **keinen** der drei Regelsätze, und Guard (d) fängt **nichts**. Eine Karte darf also weiterhin die Grammatik der nächsten Lektion **behaupten**, solange sie kein Beispiel dafür erfindet. | Der Guard muss die Frage stellen, die der Befund gestellt hat: **lehrt diese Karte eine Form, die der Kurs später einführt?** Das Material dafür liegt im Validator — `producedBeforeTaught()` (RULE 15) kennt für jede Form die Lektion, die sie einführt. **Erstens**, eine fünfte Regel in `tests/rule-card-overrides.test.mjs`: jede Karte wird mit demselben Prädikat über ihren **ganzen** Text geprüft (Titel, `content`, `commonMistakes[].correct`, `.explanationDe`) — keine Form, deren einführende Lektion **nach** der eigenen liegt, in keinem Segment, Metasprache **ausgenommen nur** dort, wo der Satz die Verschiebung ausdrücklich nennt (`NAMES_DEFERRAL`). Das hätte L5 (`ein/eine`, L6), L10 (Satzklammer, L11) und L7 (`fahren`, L10) **alle drei** gefangen, in der Regelsatz-Zeile. **Zweitens** Guard (b) schärfen: die Vier-Zeichen-Toleranz und `endsWith` fallen ersatzlos weg (sie war für „hilft/hilfst" gedacht — dafür genügt `token.startsWith(form) && form.length >= 4`), und die Ausnahmen, die dann auffliegen, wandern einzeln und **mit Begründung** in `CARD_LANGUAGE`, so wie `GENDER_ENDING_EXAMPLES` es vormacht. Gegenprobe im selben Test: die 16 Sondierungsformen oben stehen als Fixture in der Datei und **müssen** alle gefangen werden — ein Guard, der seine eigene Gegenprobe nicht besteht, ist keine Regel. **Drittens** `DEFERRAL_RE` von der Wortstellung lösen: den Satz ab „kommt/folgt" rückwärts nach der **letzten fett gesetzten oder geklammerten Form** absuchen statt nach dem unmittelbar vorangehenden Token, und wenn dort ein Metabegriff steht, die Klausel als **Konzept**-Verschiebung führen und die zugehörigen Formen aus der notice ziehen. | MAJOR |

---

## Status der Befunde aus Runde 8

Verifiziert an den Artefakten (gebauter Pool, beide Ziehungen, gebaute `ruleCards.mjs`,
die vier gebauten Checkpoints, der Kartenindex der Wiederholungsseite, Validator-Ausgabe,
Testlauf, Lint), nicht am Diff.

| # | Runde-8-Befund | Status | Beleg |
|---|---|---|---|
| 1 | Die zwölf Regelkarten stehen auf `LEGACY_BUDGET` (47 Beispiele, 17 fette Formen, 10 Titel) statt auf den vier Guards; zwei Karten lehren die nächste Lektion, eine erklärt mit kursfremder Lexik, zehn Titel weichen ab | **geschlossen am Material, teilweise an der Regel** | `LEGACY_BUDGET` ist **gelöscht**, nicht auf 0 gesetzt, mit der ausgeschriebenen Begründung „an allowance that exists is an allowance a future level asks for". Nachgemessen an der gebauten `ruleCards.mjs`: **alle zwölf** Titel sind heute der Titel ihrer notice (Runde 8: zwei von zwölf); `definite-articles` eröffnet mit „Der bestimmte Artikel zeigt: Wir wissen, welche Sache gemeint ist." und nennt `ein/eine` **nicht mehr**; `yes-no-questions` hat die Satzklammer-Zeile verloren; `present-tense-regular` konjugiert `fahren` nicht mehr; `verb-haben` führt „Ich möchte … ist hier eine feste Wendung." **und** „So bestellt man: Ich möchte einen Kaffee, bitte."; `separable-verbs-intro` zeigt **alle fünf** fetten Formen (`schlafen`, `schläft`, `Gemacht`, `gearbeitet`, `Ich rufe dich an`); `time-and-dates` hat „Wie spät ist es?" und „Hast du Zeit?". Eigene Nachzählung: **0** fehlende fette notice-Formen über alle zwölf Paare. Die fünf kursfremden Nomen der L4-Karte (*Freiheit*, *Möglichkeit*, *Freundschaft*, *Mädchen*, *Brötchen*) sind **geblieben**, jetzt aber als `GENDER_ENDING_EXAMPLES` mit ausgeschriebener Begründung allowlisted — das akzeptiere ich für `-heit/-keit/-schaft` (eine Endungsregel braucht Wörter mit der Endung) und führe die `-chen/-lein`-Zeile als Minor 2. **Rest:** die Guards selbst halten die Klasse nicht (MAJOR 5 oben). |
| 2 | Hören, Lesen und Sprechen ziehen unabhängig aus demselben Dialogtopf; neun Zeilenüberschneidungen, eine Zeile dreimal in Checkpoint 4; `0ef58eff` in zwei Checkpoints | **geschlossen, vollständig und genau wie verlangt** | `ctx.usedLineKeys` steht in `buildCheckpoint():1110–1114` mit dem Kommentar, der die Reihenfolge begründet („Hören fills it, Lesen honours and extends it with **every line of the windows it prints**, Sprechen reads it last"); `lesenCandidates()` filtert Fenster über `windowLineKeys`. Eigene Messung über alle vier gebauten Papiere: **0** Überschneidungen zwischen Diktatzeilen, Vorlesezeilen und den Zeilen der Lesetexte (Runde 8: 9), und **keine** Zeile zweimal in einem Papier. `poolIdsBefore()` reicht die benutzten Poolitems über die Kapitel weiter: **32 von 32** Poolitem-Ids in den vier Checkpoints sind verschieden. Abschnittsgrößen unverändert 5/4/6/3/2. |
| 3 | `CHROME_FILES` globt zwei von drei Kurs-Verzeichnissen; `ExamDatePlan.jsx` duzt viermal auf der Kurs-Startseite | **geschlossen, vollständig und einen Schritt weiter** | `ExamDatePlan.jsx` siezt **wörtlich** wie vorgeschlagen: „Wann ist **Ihre** Prüfung?" (Z. 84), „Mit einem Datum zeigt **Ihnen** der Kurs … **Sie können** es jederzeit ändern." (86), „**Ihr** Prüfungsdatum liegt hinter **Ihnen**" (131), „**Setzen Sie** ein neues Datum, wenn **Sie** einen neuen Termin **haben** …" (136). `CHROME_DIRS` führt jetzt `src/components/lesson`, `src/components/course` **und** `src/components/ui`, und darüber steht die bessere Hälfte: ein eigener Test liest die **Import-Zeilen** von `CurriculumHomePage.jsx` und aller `src/pages/lesson/*.jsx` und verlangt, dass jedes importierte `components/<dir>` geglobt ist — ein viertes Verzeichnis fällt beim **ersten Import** auf, nicht erst beim ersten `du`. Eigener Grep über alle vier Verzeichnisse plus `src/pages/lesson` und `SpeakingPage.jsx`: **null** du-Fundstellen. `REGISTER_EXEMPT` führt weiterhin genau eine, wörtlich nachgeprüfte Ausnahme. **Rest, den ich als MINOR führe:** `src/pages/A11PhasePage.jsx` (Route `/a1-1-phase`, verlinkt von `/level/a1.1`) duzt auf drei Zeilen — das ist der **ältere** 28-Tage-Plan, nicht der wiederaufgebaute Lektionskurs, und deshalb kein MAJOR (Minor 4). |
| 4 | Drei Ratchets stehen über ihrer gemessenen Zahl; die Kommentare nennen Zahlen, die das Skript nicht mehr druckt | **geschlossen, besser als verlangt** | Verlangt war die Angleichung von drei Ratchets. Geliefert: RULE 10 18 → **14**, RULE 11b 5 → **4**, RULE 12 → **harte Regel ohne Konstante** (`MAX_UNREHEARSED_CANDOS` existiert nicht mehr, `ratchets.unrehearsedCanDos` auch nicht), und ein Test (`tests/curricula.test.mjs:483`, „a ratchet with slack is not a rule") erzwingt `gemessen === Ratchet` für **alle sieben** verbliebenen Regeln, nicht nur für die drei genannten. Nachgerechnet gegen die Ausgabe von `node scripts/validate-curriculum.mjs a1.1`: 10 = 14/14, 11 = 61/61, 11b = 4/4, 13 = 4/4, 6b = 26/26, 15 = 12/12, 16 = 0/0 — **jede Zahl deckungsgleich**, jeder Doku-Kommentar mit Datum. |
| 5 | `caseFlag()` bekommt sein Satzprädikat an einer von drei Kartenarten; die benoteten Diktate von Checkpoint 1 sind locker; drei Zeichenketten werden im selben Kurs zweimal verschieden bewertet | **geschlossen für Karten und Diktate; die Satzantwort mit Cue bleibt offen** | `caseFlag(entry, accepted)` hat die Option `{ sentence }` **ersatzlos verloren** und ruft nur noch `politeCaseItem` — mit einem Kommentar darüber, der den Befund ausschreibt („A flag that depends on WHICH CARD KIND quotes the string is not a rule about German"). Nachgemessen: die drei Zeichenketten sind **einig** („Buchstabieren Sie bitte Chakiri." ist als `pattern:` **und** als `sentence:`-Karte locker, „Was sind Sie von Beruf?" auf allen drei Karten locker — beides der korrekte Carve-out, weil die Höflichkeitsform satzinitial bzw. ohne Cue steht); `sentence:a1.1-l01:6` („Gut. Wie geht es Ihnen?") ist **hart**, ebenso `sentence:a1.1-l02:8` und `sentence:a1.1-l06:1`. `buildHoeren`/`buildSprechen` stempeln über `lineIsCaseTask(de)`: `a1.1-cp1-hoeren-2` trägt `caseSensitive: true` und wertet „gut. wie geht es ihnen?" im **benoteten** Diktat **wrong**, wie das Lektionsitem. Kreuzvergleich über 742 Flächen: **0** Fälle, in denen dieselbe Zeichenkette mit derselben Aufgabenlage verschieden bewertet wird. **Rest:** das Prädikat liest die Selbstauskunft des Items nur bei Einwortantworten — MAJOR 4 oben. |

**Zum bekannten Rest, den der PR ehrlich protokolliert hat** („Bitte füllen Sie das
Formular aus." bleibt auf jeder Fläche locker, gleiche Klasse wie `extra-a11-l10-08`):
**das ist in dieser Gestalt vertretbar und kein Befund — für das Diktat.** Die Zeile
nennt keine Person, ihre Aufgabenstellung ist „Hören Sie zu und schreiben Sie den Satz.",
und „Bitte füllen sie das Formular aus." ist eine reale, wenn auch schiefe Lesart
(*sie* = die Leute). Ein Lernender, der eine gehörte Zeile abschreibt, kann den
Großbuchstaben **nicht hören**; ihn im benoteten Diktat zu bestrafen wäre die falsche
Richtung. **Für `extra-a11-l10-08` und seine zwei Zwillinge gilt das nicht**, und zwar
aus einem Grund, der in der PR-Begründung falsch steht: diese Items haben sehr wohl einen
Cue — ihre eigene deutsche Aufgabenstellung lautet „Schreiben Sie die **höfliche**
Frage", das Item ist also **geschriebene** Produktion mit ausgesprochener Registeraufgabe.
Die Trennlinie ist deshalb nicht „Satz gegen Wort", sondern „nennt die Aufgabe das
Register oder nicht" — und genau die zieht MAJOR 4.

**Zusätzlich geprüft und weiterhin in Ordnung:** die **zwölf Schreibaufgaben** (Titel,
Aufgabentext, Leitpunkte bzw. Formularfelder, Wortgrenzen deckungsgleich zwischen
`a11.js` und dem servertauglichen Zwilling in `netlify/functions/_shared/writingTasks.mjs`,
alle zwölf `taskKey`s identisch); der Weg des Sprechauftrags samt `anrede` bis in
`buildTeacherSystemPrompt` (`normalizeAnrede` fällt auf `'Sie'`, alle zwölf Lektionen
stehen auf `'Sie'`, der System-Prompt sagt „Sprich den Lernenden mit Sie an");
`explain-answer.mjs` (Systemprompt = A1-Regel ≤ 60 Wörter **plus** die Karte als
Grundlage — die Karte ist Erdung, kein Lesetext, weshalb ihre Metasprache in
`commonMistakes` nur Minor 5 ist); `scoreCheckpoint` (60 % gesamt, 40 % je benotetem
Abschnitt, das benotete Schreiben als einziges `optional`); der Lexikfilter der
Checkpoints (`untaughtAt` über alle 80 Items: **0**); `score-readaloud`;
`a11.audio.js` ist weiterhin der leere Manifest-Stub und alle Hörflächen fallen ehrlich
auf die Computerstimme zurück — Eigentümeraufgabe, kein Befund. **Die Erinnerungsmail
duzt weiterhin, und das lasse ich als Eigentümerentscheidung stehen — kein MAJOR**, wie
in Runde 5 bis 8; die Notiz darüber im Kopf von `course-reminder.mjs` fehlt zum fünften
Mal (Minor 7).

**Zu den Ratchets, die ich einzeln beurteilt habe:** **RULE 15** (12 Vorgriffe) ist
unverändert und weiterhin **nicht material** — acht stehen in Vorlesezeilen (Aussprache,
keine Formwahl), vier in Diktaten (eine gehörte Form abschreiben). **RULE 13** (4
Sprechaufträge ohne Mission: L7, L10, L11, L12) bleibt vertretbar, weil der Auftrag über
`saveCourseContext` vollständig mitreist. **RULE 6b** (26 notice-Formen ohne Beleg) habe
ich zum ersten Mal aufgeschlüsselt, und die Zahl verbirgt zwei sehr verschiedene Zustände:
**21** der 26 Formen kommen sehr wohl im Pool vor (`bist`, `seid`, `hast`, `habt`,
`fährst`, `unser` …), **fünf** kommen in **keiner** Lektion, **keinem** Poolitem und
**keinem** Checkpoint vor und stehen nur in notice und Karte: „**acht Uhr dreißig**" (L8),
„**schlafen**" (L11), „**euer**", „**unsere**", „**eure**" (L12). Der Ratchet zählt beide
Sorten zusammen — siehe Minor 3.

---

## Bewertungsübersicht (Runde 9, Runde 8 in Klammern)

| Lektion | Deutsch | Niveau | Dialog | Prüfung | Kohärenz | Σ /25 |
|---|---|---|---|---|---|---|
| 1 Hallo, ich bin … | 4 (4) | 4 (4) | 5 (5) | 4 (4) | 4 (4) | **21** (21) |
| 2 Ich bin Studentin | 4 (4) | 4 (4) | 4 (4) | 4 (4) | **3** (4) | **19** (20) |
| 3 Meine Familie | 4 (4) | 4 (4) | 4 (4) | 4 (4) | 3 (3) | **19** (19) |
| 4 Auf dem Flohmarkt | 4 (4) | 4 (4) | 5 (5) | 4 (4) | **3** (4) | **20** (21) |
| 5 Im Klassenzimmer | 5 (5) | **4** (3) | 5 (5) | 4 (4) | 3 (3) | **21** (20) |
| 6 Der erste Tag im Büro | 5 (5) | 4 (4) | 4 (4) | 4 (4) | 3 (3) | **20** (20) |
| 7 Freizeit und Hobbys | 4 (4) | 4 (4) | 5 (5) | 4 (4) | 4 (4) | **21** (21) |
| 8 Termine und Uhrzeit | 4 (4) | 4 (4) | 5 (5) | **3** (4) | 4 (4) | **20** (21) |
| 9 Im Café | 5 (5) | 4 (4) | 5 (5) | 4 (4) | **4** (3) | **22** (21) |
| 10 Am Bahnhof | 5 (5) | **4** (3) | 5 (5) | **3** (4) | 4 (3) | **21** (20) |
| 11 Mein Tag | 4 (4) | 4 (4) | 4 (4) | 4 (4) | **4** (3) | **20** (19) |
| 12 Feste feiern | 5 (5) | 4 (4) | 5 (5) | 4 (4) | 4 (4) | **22** (22) |

Kursmittel **20,5/25** (Runde 8: 20,4 · Runde 7: 20,3 · Runde 6: 19,9 · Runde 5: 19,7 ·
Runde 4: 19,9 · Runde 3: 20,4 · Runde 2: 19,8 · Runde 1: 17,2). Fünf Lektionen besser,
drei schlechter, vier gleich — und der Mittelwert ist der höchste der Reihe.

**Zur Lesart:** Die vier Gewinner sind alle die Regelkarten. **L5 (20 → 21)** und
**L10 (20 → 21)** bekommen den Niveaupunkt zurück, den Runde 8 ihnen genommen hatte:
ihre Karten lehren nicht mehr die Grammatik der nächsten Lektion. **L9 (21 → 22)**, weil
ihre Karte „Ich möchte …" jetzt als Chunk benennt **und** die Bestellformel zeigt — der
Chunk, den die eigene notice im Titel führt und Sprechen Teil 3 braucht. **L11 (19 → 20)**,
weil ihre Karte alle fünf fetten notice-Formen trägt, darunter die beiden Perfekt-Chunks,
die ihr eigener pretest abfragt. L10 verliert dafür einen Prüfungspunkt an
`extra-a11-l10-08` (MAJOR 4) und steht damit netto auf 21. Die drei Verlierer sind **keine
Verschlechterung des Materials**, sondern die erste Messung der Ziehung über die
Versuchsgrenze hinweg: **L8 (21 → 20)** verliert den Prüfungspunkt, weil die Lektion, die
„halb neun (= 8.30!)" fett setzt und „Hören Teil 1" beansprucht, diese Form in **keinem**
Versuch und **keinem** Checkpoint ziehen kann, während „Viertel nach" zweimal in denselben
sieben steht; **L4 (21 → 20)** und **L2 (20 → 19)** verlieren den Kohärenzpunkt — L4, weil
**vier** ihrer sieben Items in **beiden** Versuchen derselbe Satzrahmen sind
(„___ N ist ADJ. (der, die oder das?)"), L2, weil beide Versuche **dieselben sieben**
Items ziehen.
**Stärkste:** L9 und L12 (je 22), dann L1, L5, L7 und L10 (je 21).
**Schwächste:** L2 und L3 (je 19) — L3 unverändert wegen der Mischziehung (4/7 auf dem
eigenen Slug in beiden Versuchen, drei Plätze an `verb-sein`), L2 neu wegen der
identischen Wiederholung.

---

## Würde ich meinen Namen jetzt daraufsetzen?

**Auf das Kursmaterial: ja, und deutlicher als beim letzten Mal. Auf den Kurs als Ganzes:
noch nicht — und der Abstand ist zum ersten Mal nicht kleiner geworden, sondern anders.**
Ich habe alle fünf Punkte aus Runde 8 an den Artefakten nachgeprüft: **fünf von fünf
geschlossen**, vier davon vollständig und als Regel mit poolweitem Test, einer
(Regelkarten) am Material vollständig und an der Regel zur Hälfte. Zum zweiten Mal in
neun Durchgängen finde ich **keinen einzigen BLOCKER**, und ich habe die vier Definitionen
wieder einzeln gegen den Stand gemessen statt die Abwesenheit zu behaupten — 0
Selbstablehnungen über 224 Bewertungen, 0 widersprüchliche Urteile über 742
Bewertungsflächen bei exakter Zeichenkettengruppierung, 0 ungelehrte Antwortformen, 0
ungedeckte Prüfungsteile.

Drei Dinge sind in dieser Runde qualitativ passiert. Erstens: **die zwölf Regelkarten
sind endlich Karten ihres eigenen Kurses.** Jeder Beispielsatz ist eine Zeile, die die
Lektion wirklich sagt, jeder Titel ist der Titel seiner notice, keine fette Notice-Form
fehlt, und `LEGACY_BUDGET` wurde **gelöscht** statt genullt — mit dem richtigen Satz
darüber, dass eine Erlaubnis, die existiert, von der nächsten Ebene eingefordert wird.
Zweitens: **der Checkpoint führt jetzt Buch über das, was er ausgibt.** `usedLineKeys`
reicht von Hören über Lesen bis Sprechen, `poolIdsBefore` über die vier Papiere — neun
Überschneidungen auf null, 32 von 32 Poolitems verschieden, ohne dass ein Abschnitt kurz
liefert. Drittens, und das ist die stillste Arbeit: **die Ratchets sind keine
Verhandlungsmasse mehr.** Ein Test zwingt jede der sieben Zahlen auf ihre Messung, RULE 12
hat seine Konstante ganz verloren, und der Satz aus Runde 8 — eine Ratchet-Zahl, die man
nicht nachrechnen kann, ist keine Messung — ist jetzt maschinell wahr.

**Was offen bleibt, ist zum ersten Mal in dieser Reihe nicht die Fläche, die der letzte
Bericht benannt hat, sondern die Fläche, die alle neun Berichte falsch vermessen haben.**
Acht Reviews haben „Versuch 1 und Versuch 2" importiert und 168 Items ausgedruckt; keiner
hat gefragt, ob ein Lernender Versuch 2 je sieht. Er sieht ihn nicht:
`const [attempt] = useState(1);`, ohne Setter, in der einzigen Seite, die die Lektion
baut. Der ganze Apparat darüber — ein gesäter PRNG auf `(level, nr, attempt)`, ein
Dateikopf, der „a retry gives a DIFFERENT seven" verspricht, ein Test, der es pinnt, und
neun Berichte, die es nachgerechnet haben — beschreibt eine Eigenschaft, die die
Anwendung nicht hat. Und die Messung, die ich stattdessen gemacht habe, sagt, dass sie
auch dann kaum da wäre: 62 von 84 Items identisch, L2 und L8 vollständig.

Der Satz, der aus dieser Runde mitgeht, ist deshalb eine Verschärfung des Satzes aus
Runde 7 und 8. Dort hieß er: eine Regel, die als Liste geschrieben ist, prüft nur, woran
der letzte Bericht gedacht hat. Jetzt heißt er: **eine Regel, die auf einem Schlüssel
misst, prüft nur, was der Schlüssel unterscheidet — und ein Test, der einen Parameter
prüft, den die Anwendung nie ändert, prüft gar nichts.** `taskShape` behält die kurzen
Nomen und meldet darum 7 von 7 Aufgabenformen, wo vier Items derselbe Satz mit einem
anderen Wort sind. Der Lesen-Guard des Checkpoints prüft den Schlüsselrhythmus und den
Falsifikator und nie die *Richtig*-Hälfte, die seit dem ersten Bau ein Zitat ist — und
schreibt dem Lernenden sogar in die Erklärung, dass sie es ist. `politeCaseItem` liest die
Selbstauskunft eines Items und hört bei der ersten Leerstelle in der Antwort auf. Die vier
Kartenguards prüfen Beispielsätze und lassen den Regelsatz durch, also genau die Zeile,
die den Befund ausgelöst hat, und ihr Lexiktest akzeptiert `hätte` und `gegangen` in einer
A1.1-Karte. Keine dieser vier Regeln ist falsch geschrieben; alle vier messen eine Stelle
neben der, an der die Entscheidung fällt.

Die Arbeit ist wieder überschaubar und vollständig ausgeschrieben: ein Setter und ein
Knopf „Noch einmal üben" plus ein Mengen- statt Array-Test über alle zwölf Lektionen und
zwei Versuchspaare; eine Maskierung in `taskShape`, die an `LEMMA_STOPWORDS` hängt statt
an der Wortlänge, mit einem auf die Messung gezogenen Limit; eine Umformung der
*Richtig*-Aussage in die 3. Person plus ein Test, der verbietet, dass eine Lesen-Aussage
in ihrem eigenen Text steht; vier Zeilen in `politeCaseItem`, die die Selbstauskunft aus
der Einwortklausel herausziehen; und drei Schärfungen an den Kartenguards, von denen die
wichtigste — „keine Form, deren einführende Lektion nach der eigenen liegt" — das Material
des Validators wiederverwendet, das seit RULE 15 da ist. Das ist wieder etwa ein Tag, und
es ist zum zweiten Mal in Folge **keine einzige Änderung am Lektionstext**. Danach
unterschreibe ich für alle zwölf.

---

**Minors (sieben Zeilen, nicht erfasst):** (1) Die Buchstabenkarte schreibt weiter
„**Ü ist ein eigener Buchstabe.**", während ihre eigene `content`-Zeile korrekt von den
„**Formen** Ä a-Umlaut, Ö o-Umlaut, Ü u-Umlaut, ß Eszett" spricht; dazu mischt die
Namensliste weiter Groß- und Kleinschreibung (`be, ce, de` neben `Jot, Vau, Zett`,
`Q ku`), und kein Item prüft Q. (2) Die `-chen/-lein`-Zeile der L4-Karte („das **Mädchen**,
das **Brötchen**") lehrt eine Endungsgruppe, von der der Kurs **kein einziges** Wort
enthält — die `-heit/-keit/-schaft`-Beispiele sind als Endungsdemonstration vertretbar,
diese Zeile ist es nicht und sollte ersatzlos fallen. (3) RULE 6b zählt zwei verschiedene
Zustände in einer Zahl: 21 der 26 Formen kommen im Pool vor, fünf (`acht Uhr dreißig`,
`schlafen`, `euer`, `unsere`, `eure`) in **keiner** Lektion, **keinem** Poolitem und
**keinem** Checkpoint — die Regel sollte nach „im Pool vorhanden" und „nirgends vorhanden"
trennen und nur die zweite Sorte ratchen. (4) `src/pages/A11PhasePage.jsx` duzt auf drei
Zeilen („**Dein** Fortschritt", „Arbeite in **deinem** Tempo", „den Status siehst **du**")
und ist über `/level/a1.1` erreichbar; es ist der ältere 28-Tage-Plan und nicht der
Lektionskurs, aber es trägt „A1.1" im Titel, und der abgeleitete Verzeichnis-Guard des
Registertests leitet nur aus den zwei Einstiegspunkten des **neuen** Kurses ab. (5) Die
Regelkarten führen **738 von 1619** deutschen Wörtern (46 %) in `commonMistakes`, und
weder das 120-Wort-Limit noch die Pflicht zur englischen Stützzeile gilt dort; darum steht
in `yes-no-questions` der Satz „Gesprochen gibt es auch die Frage mit gleicher Wortfolge
und **steigender Stimme**; in Übungen schreibt man die Frage …" — zwei Partizipialattribute
ohne Stütze. Folgenlos, weil `explain-answer.mjs` die Karte nur als Erdung nutzt und in
≤ 60 A1-Wörtern antwortet, aber das Limit sollte den ganzen Kartentext zählen. (6) Der
Topic-Slug `numbers` existiert im Pool viermal (`extra-a11-l02-09` bis `-12`), hat **keine**
Regelkarte (`RULE_CARDS` kennt nur `numbers-counting`, und das ist die A1.2-Karte) und
erzeugt im Wiederholungsindex eine **Doppelkarte**: `pattern:numbers` ist Zeichen für
Zeichen `pattern:verb-sein`. Eines der vier Items steht im benoteten Checkpoint 4
(`a1.1-cp4-bausteine-6`); „Erklär mir das" ist dort ungeerdet. (7) Vier gezogene Items
tragen als `questionEn` die deutsche Frage statt einer Übersetzung (`e0ac0425`,
`1ce4f35f`, `34957c67`, `0fe8146d`); acht gezogene Einwortantworten akzeptieren nur die
große Schreibung, während 36 beide führen, sodass derselbe Ein-Buchstaben-Verrutscher in
L4 einmal `correct` und einmal `typo` ist; `a1.1-cp4-bausteine-2` schreibt die Uhrzeit
weiter als Ziffer („um **8** Uhr"); `extra-a11-l12-17` trägt weiter den metasprachlichen
Cue „(Adjektiv)"; `tests/lesson-engine.test.mjs:439` nennt die Höflichkeitsitems weiter
namentlich; die Eigentümerentscheidung zur du-Anrede der Erinnerungsmail ist zum fünften
Mal nirgends aufgeschrieben; und `tests/lesson-extra-items-a12.test.mjs:453` ist der
einzige übersprungene Test der Suite — korrekt geführt, aber er sollte mit A1.2 wieder
anspringen.

BLOCKER: 0 · MAJOR: 5
