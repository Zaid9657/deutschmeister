
# A1.1 — DaF-Fachreview #12, nach Runde 12 (2026-09-12)

Reviewer: DaF-Lehrkraft (Erwachsenenbildung, Integrationskurs) und Prüferin für
Goethe-Zertifikat A1 *Start Deutsch 1*. Zwölfter Durchgang. Basis:
`REVIEW-daf-2026-09-12.md` (5 BLOCKER · 45 MAJOR), `REVIEW-daf-2-2026-09-12.md`
(1 BLOCKER · 35 MAJOR · 44 MINOR), `REVIEW-daf-3` bis `REVIEW-daf-5` (je
3 BLOCKER · 11 MAJOR), `REVIEW-daf-6` (3 BLOCKER · 7 MAJOR), `REVIEW-daf-7`
(3 BLOCKER · 5 MAJOR), `REVIEW-daf-8` und `REVIEW-daf-9` (je 0 BLOCKER · 5 MAJOR),
`REVIEW-daf-10` (0 BLOCKER · 4 MAJOR) und `REVIEW-daf-11-2026-09-12.md`
(1 BLOCKER · 3 MAJOR), angewendet in `main` @ `cbcac8e` („A1.1 round 12: close DaF
review #11 + report", PR #129). Gemessen wurde ausschließlich der Commit-Stand.
**A1.2 ist durch Eigentümerentscheidung vom 2026-09-13 pausiert und nicht
Gegenstand dieses Reviews.**

Gemessen, nicht angenommen:

* **Die echte Ziehung für alle drei Versuche**: `planPractice(CURRICULUM_A11, POOL, a)`
  für `a` = 1, 2 und 3, alle **252** ausgelieferten Items (**218 verschiedene** aus
  einem Pool von **374**) mit `questionDe`/`questionEn`/`options`/`answer`/`accepted`/
  `explanationDe`/`caseSensitive`/`minLektion` ausgedruckt und einzeln gelesen; je Block
  die Zahl verschiedener `taskShape`-Schlüssel, die größte Antwortschlüssel- und
  Lemmagruppe, `relaxUsed` und alle drei Versuchspaare (1∩2, 2∩3 **und 3∩1**).
* **Eine eigene Situationsmessung**: `relevanceScore(item, ownTerms, ∅)` je Item über
  alle 36 Blöcke — wie viele der sieben Aufgaben ein Wort aus dem **eigenen** Wortfeld
  der Lektion tragen.
* **Zwei Lecksuchen nebeneinander**: die eingebaute (`leaksAnswer` über alle 36 Blöcke)
  und eine eigene, absichtlich gröbere, die auch Funktionswortantworten zählt — um zu
  sehen, was die dokumentierte Ausnahme der neuen Achse durchlässt.
* **Der Checker gegen 911 Bewertungsflächen**: 252 gezogene Items, der ganze gebaute
  Pool (374), die Handquelle, alle getippten Checkpoint-Items — je mit der eigenen
  Antwort, mit **jeder** `accepted`-Variante und mit der kleingeschriebenen Probe,
  gruppiert nach der exakten Antwortzeichenkette; dazu **395** Wiederholungskarten
  gegen `gradeTypedReview`.
* **Eine eigene Alternativantwortprobe**, die diese Reihe bisher nicht gefahren hat:
  für jedes Wortsalat-Item mit einer Zeitangabe oder einer Ortsangabe in der Klammer
  die **zweite** korrekte Wortstellung, und für jede Fehlerkorrektur ohne Achsencue die
  **zweite** minimale Reparatur — beide gegen `checkAnswer` mit `checkOptionsFor(item)`.
* **Alle vier Checkpoints gebaut**, alle **80** Items ausgedruckt, die 16
  Lesen-Aussagen gegen ihren eigenen Text gehalten, jedes Poolitem gegen sein
  `minLektion` und das Kapitelfenster seines Checkpoints geprüft.
* **Der neue Wächter gegen seine eigene Fläche**: `unconditionedRuleSentence` über alle
  374 gebauten Poolitems, über die zwölf notices **und** über die zwölf Regelkarten von
  `netlify/functions/_shared/ruleCards.mjs` — also auch dort, wo er nicht läuft.
* **Die Modelltexte, zum ersten Mal gegen den eigenen Formcheck**: `scoreWriting(schreiben,
  schreiben.sample)` für alle zwölf Schreibaufgaben, dazu die zwölf pretest-Modelle gegen
  ihren eigenen Prompt und gegen die Figurenfakten ihres eigenen Dialogs.
* **Die verschobenen Fenster als gebaute Lektion**: `buildLesson(...)` für alle zwölf
  Lektionen, die Diktatzeilen und die Vorlesezeilen als Text verglichen.
* Das Curriculum: Dialoge, notices, Kann-Beschreibungen, `examTeile`, Wortfeld, Register,
  pretests, die zwölf Schreibaufgaben gegen `netlify/functions/_shared/writingTasks.mjs`,
  die Sprechaufträge (`anrede` in allen zwölf `'Sie'`), die zwölf Regelkarten gegen ihre
  notices.
* `node scripts/validate-curriculum.mjs a1.1` (RULE 10: 14/14 · RULE 11: **59**/59 ·
  RULE 11b: **0, harte Regel** · RULE 12: 0, harte Regel · RULE 13: 4/4 · RULE 6b: 26/26 ·
  RULE 15: **3**/3 · **RULE 15b: 0, harte Regel** · RULE 16: 0/0 · RULE 14: 0).
* `npm test`: **854 Tests, 853 grün, 1 übersprungen** (der A1.2-Ziehungstest).
  `npm run lint`: 0 Fehler, 0 Warnungen. `npm run check:duplicates`: OK.

## Urteil in einem Absatz

**Runde 12 hat den BLOCKER und zwei der drei MAJOR aus Runde 11 sauber geschlossen, und
die beiden Motorschließungen sind die beste Arbeit seit dem Umbau der Formachse.** Die
falsche Regel ist nicht als Zeile repariert, sondern als Klasse: `REASON.UNCONDITIONED_RULE`
liest jede Erklärung in vier Klauseln (Transformation, absoluter Quantor, Genusvergleich,
elliptisches „deshalb <Artikel>"), die Bedingung darf ein Genus, ein Kasus, ein Skopus **oder
eine Person** sein — und ausgerechnet die Person **nicht** in einer Paarung („Wir steht
immer mit haben."), was genau der Satz ist, an dem Runde 11 den Befund festgemacht hat.
Ich habe alle 374 gebauten Poolitems durch den Wächter geschickt: **0 Treffer**, elf
Cacheitems sind draußen, `extra-a11-l06-14` liest jetzt „Computer ist maskulin: Nach
brauchen wird maskulin ein zu einen.", und ein zweiter Guard verbietet, dass zwei
Erklärungen denselben Auslöser auf zwei Formen abbilden. Die Lemmakappe ignoriert die
Klammer: L5 steigt von 5/3/3 auf **5/7/5** eigene Wortfeldtreffer, `complete` wird gelesen
(`MAX_OFF_PRIMARY_BLOCKS = 0`), und die sechste Achse funktioniert — meine Nachmessung mit
`leaksAnswer` über alle 36 Blöcke findet **0** Paare, wo Runde 11 siebzehn gefunden hat.
RULE 15b ist eine harte Null. **Und trotzdem unterschreibe ich nicht, und diesmal aus zwei
Gründen, die beide dasselbe sind: der Kurs bewertet richtige Antworten als falsch.**
**Erstens die Wortstellung.** `extra-a11-l11-10` heißt „Bilden Sie den Satz: [ich / haben /
gestern / gearbeitet]" und akzeptiert nur „Ich habe gestern gearbeitet." — „**Gestern habe
ich gearbeitet.**" ist `wrong`. Derselbe Kurs sagt im pretest **derselben Lektion 11**, die
Antwort dürfe mit „Gestern habe ich" beginnen (`accepted: ['Ich habe', 'Gestern habe ich']`),
und **derselbe Pool** führt drei Cacheitems, die die vorangestellte Zeitangabe ausdrücklich
akzeptieren („Um 7 Uhr stehe ich auf.", „Heute kaufen wir ein.", „Um 8 Uhr holt er dich
ab."). Vier gezogene Handitems tun es nicht, und **drei davon sind benotete
Checkpoint-Aufgaben** (`a1.1-cp4-bausteine-2`, `a1.1-cp3-schreiben-1`, `a1.1-cp1-schreiben-1`).
**Zweitens die Fehlerkorrektur ohne Achse.** „Korrigieren Sie: „Du habt Durst."" — das neue
`extra-a11-l09-14` dieser Runde — hat zwei minimale Reparaturen, „Du **hast** Durst." und
„**Ihr** habt Durst.", und nur die erste zählt; dieselbe Form steht zweimal in **einer**
Sieben von L10 („Sind die Abfahrt …?", „Haben der Fahrer …?", beide auch über das Nomen
reparierbar). Der Kurs hat für genau diese Klasse seit Runde 7 eine Regel
(`ambiguousCorrection`: eine Korrektur darf sich von ihrem Zitat nur so unterscheiden, wie
der deutsche Prompt es benennt) — sie prüft die Artikelfamilie und nicht die Kongruenz.
Daneben vier systematische Befunde, von denen zwei die Rechnung dieser Runde sind. **Der
größte ist eine Fläche, die zwölf Runden lang niemand gemessen hat:** alle **sechs**
Mitteilungs-Beispieltexte fallen durch die Formcheck-Liste, die der Kurs dem Lernenden
einen Absatz darüber zeigt — 24, 15, 17, 17, 14 und 15 Wörter gegen „25–45 Wörter" —, und
Runde 12 hat den einzigen, der die Länge bestand, mit heruntergerissen (L2: 26 → **24**).
**Zweitens** ist RULE 15b mit den Modelltexten bezahlt worden: die L3-Modellantwort lautet
seit dieser Runde „Das ist die Schwester. Sie ist zwanzig." — kein Possessivartikel, also
keine Antwort mehr auf „Sprechen Sie über eine Person in **Ihrer** Familie", und sachlich
im Widerspruch zum Dialog derselben Lektion, in dem der **Bruder** zwanzig ist und die
Schwester „noch jung". **Drittens** teilen Diktat und Vorlesen nach den verschobenen
Fenstern in **drei von zwölf** Lektionen dieselbe Zeile — L3 und L8 sind in dieser Runde
entstanden. **Viertens** fehlt der Situationsboden weiter: „Freizeit und Hobbys" serviert
im dritten Durchgang **2 von 7** Aufgaben aus dem eigenen Wortfeld, dafür „Ihr ___
'Tschüss'.", „Du ___ in Berlin." und zweimal Familie.

**Zahlen: 2 BLOCKER · 4 MAJOR.** Die Bedingung aus `docs/course-standard-2026-09-12.md` §8
(0 BLOCKER / 0 MAJOR) steht weiter aus. MINORs sind am Ende als acht Zeilen erfasst.

---

## BLOCKER

| # | Feld | Zitat | Problem | Fix (pasteable) |
|---|---|---|---|---|
| 1 | `src/data/lessonPools/a11.extra.json` → `extra-a11-l11-10`, `extra-a11-l08-08`, `extra-a11-l03-07`, `extra-a11-l12-13` (und `extra-a11-l07-05`), alle **gezogen**; `extra-a11-l11-10` = `a1.1-cp4-bausteine-2`, `extra-a11-l08-08` = `a1.1-cp3-schreiben-1`, `extra-a11-l03-07` = `a1.1-cp1-schreiben-1`, `extra-a11-l07-05` = `a1.1-cp3-schreiben-2` — also **vier benotete Checkpoint-Aufgaben** | `extra-a11-l11-10`: „Bilden Sie den Satz: **[ich / haben / gestern / gearbeitet]**" → `accepted: ["Ich habe gestern gearbeitet.", "Ich habe gestern gearbeitet"]`. Gemessen mit `checkAnswer(…, checkOptionsFor(item))`: „Ich habe gestern gearbeitet." → `correct`, „**Gestern habe ich gearbeitet.**" → **`wrong`** · derselbe Kurs, `src/data/curricula/a11.js`, L11 `pretest.accepted`: `['Ich habe', '**Gestern habe ich**']` · derselbe Pool, drei Cacheitems derselben Bauart, die die vorangestellte Angabe **führen**: `96b41b81` „Schreiben Sie den Satz: [ich / aufstehen / um 7 Uhr]" → `["Ich stehe um 7 Uhr auf", "**Um 7 Uhr stehe ich auf.**", "Um 7 Uhr stehe ich auf"]`, `1958acf0` „[wir / einkaufen / heute]" → `[…, "**Heute kaufen wir ein.**", …]`, `51d74698` „[er / abholen / dich / um 8 Uhr]" → `[…, "**Um 8 Uhr holt er dich ab.**", …]` · die drei weiteren: `extra-a11-l08-08` „[der Termin / sein / am Dienstag / um acht Uhr]" — „Am Dienstag um acht Uhr ist der Termin." → `wrong`; `extra-a11-l12-13` „[mein / Geburtstag / sein / im Mai]" — „**Im Mai ist mein Geburtstag.**" → `wrong`; `extra-a11-l03-07` „[wir / kommen / aus Marokko]" — „Aus Marokko kommen wir." → `wrong` | **Der Lernende schreibt einen richtigen deutschen Satz aus genau den vorgegebenen Wörtern und wird dafür als falsch bewertet — in der Übung und im benoteten Checkpoint.** Drei Messungen. **(a) Die abgelehnte Stellung ist die vom Kurs gelehrte.** Die L11-Regelkarte sagt „Das Verb steht auf **Position 2**, die Vorsilbe am Ende." In „Gestern habe ich gearbeitet." steht `habe` auf Position 2. Es gibt keine Regel im ganzen Kurs, die die vorangestellte Zeitangabe verbietet, und der Wortsalat trägt keinen Ordnungscue — die Aufgabenzeile heißt „Bilden Sie den Satz", nicht „beginnen Sie mit dem Subjekt". **(b) Der Kurs widerspricht sich innerhalb einer Lektion.** In Lektion 11 sagt Schritt 1 dem Lernenden, „Gestern habe ich …" sei ein guter Anfang (`pretest.accepted`), und Schritt 4 sagt ihm, derselbe Satz sei falsch. Dazwischen liegen vier Bildschirme. **(c) Es ist kein Einzelfall, sondern zwei Schreibweisen desselben Aufgabentyps im selben Pool.** Die **Cacheitems** dieser Bauart führen die vorangestellte Angabe in `accepted`, die **Handitems des Rebuilds** nicht — und gezogen werden ausschließlich die Handitems (die drei großzügigen Cacheitems stehen in keiner der 36 Sieben). Der Lernende sieht also nur die enge Fassung, und zwar viermal, davon dreimal als benotete Prüfungsaufgabe. Bei `extra-a11-l12-13` ist die abgelehnte Fassung sogar die natürlichere Antwort auf die Situation der Lektion („Wann ist dein Geburtstag?" — „**Im Mai ist mein Geburtstag.**"). In *Start Deutsch 1* Schreiben Teil 2 und Sprechen Teil 3 wird die vorangestellte Zeitangabe nicht nur akzeptiert, sie ist die häufigste Lernerproduktion. | **Erstens** die Regel dort, wo die anderen Achsen auch stehen: eine Funktion `frontableOrders(item)` neben `answerWords` in `src/lib/lesson/buildLesson.js` bzw. besser in `src/data/lessonPools/quality.js`, die aus einem `sentence_building`-Item mit Klammerliste die **zweite** Stellung bildet, wenn genau ein Listenglied eine Zeit- oder Ortsangabe ist (`/^(gestern|heute|morgen|am \w+|um .+|im \w+|jeden Tag|jede Woche|nächste Woche|letzte Woche)$/i`), und sie in `accepted` verlangt — Verb bleibt Position 2, Subjekt rückt dahinter. **Zweitens** — und das ist die Regel, nicht die Liste — ein Guard in `tests/lesson-extra-items.test.mjs`, der über den **gebauten Pool** läuft: für jedes `sentence_building`-Item, dessen Klammer eine solche Angabe enthält und dessen Antwort ein Aussagesatz ist, muss `accepted` **beide** Stellungen führen; die drei Cacheitems `96b41b81` / `1958acf0` / `51d74698` als `FRONTING_PROBES`-Fixture, die vom Prädikat **alle** als „hat beide" gemeldet werden müssen, und die vier gemessenen Handitems als die Liste, die auf 0 fällt. **Drittens** dieselbe Prüfung über die vier Checkpoints (`buildCheckpoint`), weil ein benotetes Item die teuerste Fläche ist. **Viertens** die Gegenprobe, die den Guard ehrlich hält: er darf **keine** Frage und keinen Satz ohne Angabe erfassen — `extra-a11-l11-01` („Bilden Sie die Frage: [mitkommen / du / am Freitag]") und `extra-a11-l09-16` müssen unberührt bleiben, beide als Negativfixture. |
| 2 | `src/data/lessonPools/a11.extra.json` → `extra-a11-l09-14` (**neu in dieser Runde**, gezogen in L9 Versuch 2), `extra-a11-l10-10` und `extra-a11-l10-11` (beide gezogen, **in derselben Sieben**, L10 Versuch 2) ↔ `ambiguousCorrection` in `src/data/lessonPools/quality.js:799` ↔ `tests/lesson-extra-items.test.mjs:642` | `extra-a11-l09-14`: „Korrigieren Sie: „**Du habt** Durst."" → `"Du hast Durst."`, `explanationDe`: „Bei du ist die Form hast, nicht habt." — gemessen: „**Ihr habt Durst.**" → `wrong` · `extra-a11-l10-10`: „Korrigieren Sie: „**Sind die Abfahrt** um neun Uhr?"" → `"Ist die Abfahrt um neun Uhr?"` — „**Sind die Abfahrten um neun Uhr?**" → `wrong` · `extra-a11-l10-11`: „Korrigieren Sie: „**Haben der Fahrer** Verspätung?"" → `"Hat der Fahrer Verspätung?"` — „**Haben die Fahrer Verspätung?**" → `wrong` · zum Vergleich die Fassung, die der Kurs selbst als richtig gelernt hat: **jede** Artikelkorrektur trägt ihren Cue, `extra-a11-l05-23` „Korrigieren Sie: „Die Schlüssel ist gelb." **(mit bestimmtem Artikel)**", `extra-a11-l04-39`, `extra-a11-l04-40` ebenso | **Eine Korrekturaufgabe mit zwei minimalen Reparaturen bewertet eine davon als falsch, und der Kurs hat für genau diese Klasse seit Runde 7 eine Regel, die die Kongruenzachse nicht kennt.** Drei Messungen. **(a) Beide Reparaturen sind minimal, korrekt und im Sinne der Lektion.** „Du habt Durst." wird durch **ein** Token repariert, entweder am Verb (`hast`) oder am Subjekt (`Ihr`) — beide Sätze sind richtiges Deutsch, beide zeigen die haben-Konjugation, die L9 lehrt, und der deutsche Prompt sagt nicht, welche Seite falsch ist. Dasselbe zweimal in einer einzigen Sieben von L10: `die Abfahrt`/`die Abfahrten` und `der Fahrer`/`die Fahrer` sind beide reguläre Plurale, die L4 gelehrt hat („Im Plural haben alle Nomen die"). **(b) Die Regel existiert und greift eine Achse zu kurz.** `ambiguousCorrection` (quality.js, Runde 7) prüft, ob das Modell das **Artikelfamilien**-Fenster verlässt, und `tests/lesson-extra-items.test.mjs:642` läuft über **alle** `error_correction`-Items — und meldet für diese drei nichts, weil zwischen `habt` und `hast` keine Artikelfamilie liegt. Das ist der Satz, der seit Runde 9 in dieser Reihe steht: eine Regel, die eine Instanz schließt und ihre Klasse nicht, ist keine Regel. **(c) Der Kurs weiß, wie es geht — an 15 anderen Stellen.** Jede Genuskorrektur des Pools trägt „(mit bestimmtem Artikel)"; genau dieser Cue ist in Runde 6 eingeführt worden, weil `extra-a11-l05-09` sonst eine richtige Antwort bestraft hätte. Die drei Kongruenzkorrekturen haben keinen. `extra-a11-l09-14` ist in **dieser** Runde geschrieben worden, um Checkpoint 2 und L9 Versuch 3 nachzufüllen. | **Erstens** die drei Zeilen mit dem Cue versehen, den der Kurs schon führt: `extra-a11-l09-14.questionDe` → `"Korrigieren Sie das Verb: „Du habt Durst.“"`; `extra-a11-l10-10` → `"Korrigieren Sie das Verb: „Sind die Abfahrt um neun Uhr?“"`; `extra-a11-l10-11` → `"Korrigieren Sie das Verb: „Haben der Fahrer Verspätung?“"`. **Zweitens** die Achse in `quality.js` neben `ambiguousCorrection`: `agreementAmbiguity(item)` — das Zitat enthält ein Subjekt (Pronomen oder Artikel+Nomen) und eine finite Verbform, die nicht dazu kongruiert, und das Modell ändert **das Verb**, während eine Änderung **des Subjekts** (Person tauschen, Plural bilden) denselben Satz mit einem Token repariert; dann MUSS der deutsche Prompt das geänderte Element benennen (`/\b(verb|subjekt|artikel|nomen)\b/i` in der Aufgabenzeile), sonst `REASON`. **Drittens** der Test als Klasse in `tests/lesson-extra-items.test.mjs`, über den **gebauten** Pool und über alle `buildCheckpoint`-Items, mit den drei gemessenen Items als `AGREEMENT_PROBES`-Fixture, die **alle** gefangen werden müssen, und `extra-a11-l07-08` („Lena spielen am Wochenende Fußball.", nur am Verb reparierbar) als Negativfixture. **Viertens** die allgemeine Form des Satzes, die beide BLOCKER teilen und die ich als Regel für die nächste Runde bitte: **eine Aufgabe, deren deutsche Anweisung die Lösung nicht eindeutig macht, gehört nicht in den Pool — und „eindeutig" heißt gemessen, nicht geglaubt.** Ein Guard, der für jedes typisierte Item eine zweite, mechanisch erzeugte Kandidatenantwort baut (zweite Wortstellung, zweite minimale Reparatur) und verlangt, dass sie entweder in `accepted` steht oder vom Prompt ausgeschlossen ist, schließt beide BLOCKER in einem Stück. |

Die übrigen zwei Definitionen sind einzeln gegen den heutigen Stand gemessen und **halten**:

* **„Der Kurs verspricht etwas, das er nicht enthält."** RULE 16 misst **0**, RULE 12 ist
  eine harte Regel und misst **0**; alle `examTeile`-Behauptungen der zwölf Lektionen haben
  eine Fläche in ihrer eigenen Lektion, alle zwölf `taskKey`s sind in der Serverbank, die
  zwölf Sprechaufträge stehen alle auf `anrede: 'Sie'`. Der Grenzfall ist MAJOR 1: die
  Wortzahl „25–45 Wörter" wird vom Kurs selbst nicht eingehalten — aber sie wird vom
  Lernenden auch nicht eingefordert, sondern nur angezeigt; das ist Qualität, nicht
  Versprechen.
* **„Eine geprüfte Form, die der Kurs nie lehrt."** RULE 11b = **0**, RULE 15b = **0**
  (harte Regel, `src/data/curricula/constructions.js` als gemeinsames Modul, Zwilling
  driftgeschützt), RULE 15 von 12 auf **3** gefallen, und die drei Reste sind inhaltlich
  gebunden und in der eigenen notice **angekündigt**: L6 Zeile 9 „Die Pause ist um eins."
  (notice: „Die Uhrzeit **um eins** folgt in Lektion 8"), L8 Zeilen 6 und 7 „Hast du
  nachmittags Zeit?" / „Am Dienstag habe ich abends Zeit." (notice: „**Hast du Zeit?** ist
  eine feste Wendung; haben kommt in Lektion 9"). Das ist CONTRACT §2 und kein Befund.
  Auch die Checkpoints halten jetzt die Grenze: **kein einziges** Poolitem eines
  Checkpoints trägt ein `minLektion` jenseits seines Kapitelfensters (cp1 ≤ 3, cp2 ≤ 6,
  cp3 ≤ 9, cp4 ≤ 12, alle 32 geprüft).

---

## MAJOR

| Feld | Zitat | Problem | Fix (pasteable) | Schwere |
|---|---|---|---|---|
| `src/data/curricula/a11.js` → `schreiben.sample` der Lektionen 2, 4, 6, 8, 10, 12 ↔ `scoreWriting` in `src/lib/lesson/writing.js:66–83` ↔ `src/components/lesson/GradedWriting.jsx:266–294` | Der Bildschirm, in dieser Reihenfolge: erst die Liste `{ key: 'length', label: '25–45 Wörter', ok: count >= min && count <= max }`, darunter „Diese Checkliste prüft nur die Form (**Länge**, Punkte, Anrede und Gruß)", darunter `{done && task?.sample && (… <p className="…">{task.sample}</p>)}` unter der Überschrift **„Beispieltext"** · gemessen, `scoreWriting(schreiben, schreiben.sample)` für alle sechs Mitteilungen: **L2 24 Wörter · L4 15 · L6 17 · L8 17 · L10 14 · L12 15**, Minimum **25** — `ok: false` in **sechs von sechs**, jedes Mal mit `FAIL: length` · L2 im Wortlaut, vor und nach dieser Runde: `- 'Sehr geehrte Damen und Herren, ich heiße Ana Chakiri. **Mein Geburtsdatum ist** 3. Mai 1998. …'` (**26** Wörter, `length ok: true`) / `+ 'Sehr geehrte Damen und Herren, ich heiße Ana Chakiri**, Geburtsdatum** 3. Mai 1998. …'` (**24**, `false`) | **Der Kurs zeigt dem Lernenden als Muster genau den Text, den er ihm eine Zeile vorher als zu kurz angestrichen hat — in allen sechs Mitteilungen, und seit dieser Runde ohne Ausnahme.** Drei Messungen. **(a) Es ist dieselbe Funktion.** Der Formcheck und der Beispieltext stehen in **einem** Bildschirm; die Liste zählt Wörter mit `countWords`, und dieselbe Funktion auf den `sample` angewandt gibt 14 bis 24. Ein Lernender, der den Beispieltext abschreibt, bekommt das rote Kreuz, das er gerade weggeklickt hat. **(b) Der Beispieltext ist auch inhaltlich unter der Aufgabe.** L10 verlangt drei Leitpunkte („Warum Sie schreiben", „Wann Sie kommen", „Was die Kollegin bis dahin machen soll") und modelliert zwei; L6 verlangt „Ihre Telefonnummer" und modelliert „**Die** Nummer ist null vier zwei." — 14 bis 17 Wörter reichen für drei Leitpunkte plus Anrede plus Gruß schlicht nicht. In *Start Deutsch 1* Schreiben Teil 2 ist die Untergrenze von rund 30 Wörtern das, woran Prüflinge scheitern; ein Muster mit 14 Wörtern trainiert den Fehler. **(c) Runde 12 hat den einzigen gerissen, der hielt.** L2 war mit 26 Wörtern der einzige Beispieltext oberhalb seines Minimums; die RULE-15b-Reparatur hat „Mein Geburtsdatum ist" durch die verblose Apposition „, Geburtsdatum" ersetzt und damit zwei Wörter **und** den einzigen vollständigen Satz dieser Zeile entfernt. Eine Regel, die den Mustertext kürzt, ohne dass irgendetwas die Länge des Mustertextes misst, muss das tun. | **Erstens** eine Regel, die in einer Zeile steht und alles abdeckt: in `tests/curricula.test.mjs`, über **jedes** Curriculum und **jede** Lektion — `for (const l of CURRICULUM.lektionen) { const s = l.schreiben; if (!s) continue; assert.ok(scoreWriting(s, s.sample).ok, \`L${l.nr}: der Beispieltext besteht die eigene Checkliste nicht: ${JSON.stringify(scoreWriting(s, s.sample).checks.filter((c) => !c.ok).map((c) => c.key))}\`); }` — das prüft Länge, Anrede, Gruß und **jeden** Leitpunkt in einem, mit genau dem Code, den der Lernende sieht. **Zweitens** die sechs Mustertexte neu schreiben, nicht verlängern: je drei vollständige Sätze zu den drei Leitpunkten, Anrede und Gruß, 27–35 Wörter, nur aus dem Wortfeld der eigenen und früherer Lektionen — L6 z. B. „Guten Tag, Frau Berg! Ich brauche einen Computer und ein Telefon. Meine Handynummer ist null vier zwei – drei drei acht eins. Am Montag bin ich ab neun Uhr im Büro. Viele Grüße, Ana" (Possessivartikel: siehe MAJOR 2 — hier gehört das Fenster verschoben, nicht der Satz zerstört). **Drittens** dieselbe Zusicherung für die **Formulare**: `scoreWriting` prüft dort nur, ob jedes Feld gefüllt ist, und alle sechs bestehen — der Test muss beide Zweige laufen, sonst hält er nur die Hälfte. **Viertens** die Zeile, die diese Runde gerissen hat, in den Guard der RULE-15b-Reparatur aufnehmen: ein Modelltext, den ein Guard ändert, wird im selben Lauf gegen seine eigene Aufgabe gemessen. | MAJOR |
| `src/data/curricula/a11.js` → L3 `pretest.model` und L6 `schreiben.sample` (beide **in dieser Runde geändert**, um RULE 15b zu erfüllen) ↔ `src/components/lesson/PretestStage.jsx:59–63` ↔ RULE 14 in `scripts/validate-curriculum.mjs:1605` | Der Diff dieser Runde: `- model: 'Mein Bruder ist zwanzig.'` / `+ model: '**Das ist die Schwester. Sie ist zwanzig.**'` — bei unverändertem Prompt „Sprechen Sie über eine Person in **Ihrer** Familie." und unveränderter `accepted: ['Mein', 'Meine', 'Das ist', 'Er ist', 'Sie ist']` · der Dialog **derselben Lektion 3**, Zeile 4: „**Er ist zwanzig.** Meine Schwester ist **noch jung**." · die Oberfläche: `<p className="font-data …">Modellantwort</p>` … `<p className="mt-2 text-[1.125rem] font-semibold text-ink">{pretest.model}</p>` plus „Anhören" · dazu `- sample: '… Ich brauche einen Computer. **Meine** Nummer ist null vier zwei. …'` / `+ '… **Die** Nummer ist null vier zwei. …'` gegen den unveränderten Leitpunkt „**Ihre** Telefonnummer" | **Die Runde hat einen Wächter über Produktionstexte gebaut und ihn dort, wo er anschlug, mit schlechterem Deutsch bedient statt mit einem verschobenen Fenster.** Drei Messungen. **(a) Die Modellantwort beantwortet ihre eigene Aufgabe nicht mehr.** Gefragt ist eine Person in **Ihrer** Familie; „Das ist die Schwester." nennt keinen Besitzer, und der bestimmte Artikel vor einem Verwandtschaftsnomen ohne vorher eingeführten Bezug ist im Deutschen nicht idiomatisch — ein Prüfling, der das in Sprechen Teil 2 sagt, bekommt eine Rückfrage. Der Satz ist als Modell **vorgelesen** (`playLine(id, 'pretest', pretest.model)`), also die Fläche, die der Lernende nachspricht. **(b) Er widerspricht dem Dialog derselben Lektion.** Im Dialog ist der **Bruder** zwanzig und die Schwester „noch jung"; das Modell macht die **Schwester** zwanzig. Runde 12 hat aus „Mein Bruder ist zwanzig." das Genus getauscht und die Zahl stehen lassen. RULE 14 (Figuren-Widersprüche, harte Regel, misst 0) vergleicht ausdrücklich **Dialogzeile und Schreiben-Text** — die pretest-Modelle liegen außerhalb ihres Fensters, und deshalb meldet sie nichts. **(c) Dieselbe Bewegung an der zweiten Stelle.** „Meine Nummer" → „Die Nummer" nimmt dem Beispieltext den Bezug auf den Leitpunkt „Ihre Telefonnummer"; der Text erfüllt den Punkt jetzt weder für den Formcheck noch für einen Leser. Beide Änderungen waren **nötig** — Possessivartikel sind Lektion 12 —, aber die richtige Antwort darauf ist dieselbe wie bei den Diktatfenstern: die **Aufgabe** verschieben, nicht den Satz verstümmeln. Für L3 heißt das: das pretest-Thema der Lektion ist das Personalpronomen, also „Der Bruder ist zwanzig. **Er** ist Student." — kein Possessivartikel, richtiges Deutsch, und es übt genau, was L3 lehrt. | **Erstens** die zwei Zeilen reparieren: L3 `pretest` → `{ promptDe: 'Sprechen Sie über eine Person in Ihrer Familie.', model: 'Der Bruder ist zwanzig. Er ist Student.', accepted: ['Mein', 'Meine', 'Das ist', 'Er ist', 'Sie ist', 'Der Bruder', 'Die Schwester'] }`; L6 `sample` → „… Ich brauche einen Computer. Die Handynummer ist null vier zwei – drei drei acht eins. …" (zusammen mit MAJOR 1). **Zweitens** RULE 14 auf **alle** Texte ausdehnen, die der Lernende als Muster liest: `pretest.model`, `schreiben.sample` und `notice.examples` in dasselbe Fenster wie Dialog und Schreiben-Text — die Figurenfakten sind dieselben, die Fläche ist dieselbe, und die Zahl `zwanzig` neben der falschen Person ist genau der Fehler, für den die Regel 2026-09 geschrieben wurde. **Drittens** eine Zusicherung, die den Modelltext an seine Aufgabe bindet: in `tests/curricula.test.mjs` muss `pretest.model` mit einem Präfix aus seiner eigenen `pretest.accepted`-Liste beginnen **und** — wenn der Prompt ein Possessivpronomen der Anrede trägt (`/\b(Ihre[rmns]?|Ihr|deine?)\b/`) — eine Person benennen, statt sie mit einem bloßen bestimmten Artikel einzuführen: `assert.doesNotMatch(model, /^(Der|Die|Das)\s+(Bruder|Schwester|Mutter|Vater|Sohn|Tochter|Kind|Mama|Papa|Familie)\b/)` — mit dem heutigen L3-Modell als Fixture, die gefangen werden **muss**. **Viertens** die Regel hinter der Regel, als Satz in den Kopf von `src/data/curricula/constructions.js`: **ein Guard darf einen Modelltext ablehnen, aber nie verbessern — die Reparatur ist ein anderes Fenster oder ein anderer Satz, und beide werden gegen die Aufgabe gemessen, die den Text trägt.** | MAJOR |
| `src/data/curricula/a11.js` → `hoeren.lines` und `sprechen.readAloud` der Lektionen 3, 6 und 8 ↔ `buildLesson.js:939–955` — es gibt **keine** Prüfung, ob die beiden Fenster dieselbe Zeile ziehen | Der Diff dieser Runde: L3 `- hoeren: { … lines: [3, 5] }` / `+ lines: [5, 7]` und `- readAloud: [1, 6]` / `+ readAloud: [2, **7**]`; L8 `- readAloud: [1, 6]` / `+ readAloud: [**3**, 6]` bei unverändertem `hoeren.lines: [3, 7]` · gemessen an der **gebauten** Lektion (`buildLesson(...)`, `stages.find(s => s.key === 'dictation'/'speaking')`): **L3** Diktat `["Sie sprechen Arabisch und Deutsch.", "**Ja, er spricht Englisch und Deutsch.**"]` / Vorlesen `["Wie alt ist er?", "**Ja, er spricht Englisch und Deutsch.**"]` · **L8** Diktat `["**Der Termin ist am Montag um Viertel vor acht.**", "Am Dienstag habe ich abends Zeit."]` / Vorlesen `["**Der Termin ist am Montag um Viertel vor acht.**", "Und am Dienstag? Hast du nachmittags Zeit?"]` · **L6** (alt, Runde-11-Minor 8) Zeile „Danke. Ich brauche einen Computer und ein Telefon." in beiden · die übrigen neun Lektionen: keine Überschneidung | **Zwei der neun Schritte einer Lektion arbeiten in drei von zwölf Lektionen am selben Satz, und zwei dieser drei sind in dieser Runde entstanden.** Drei Messungen. **(a) Der eine Schritt verschenkt den anderen.** Schritt „Hören und schreiben" lässt den Satz Buchstabe für Buchstabe tippen und bewertet ihn; Schritt „Sprechen" lässt denselben Satz vorlesen und bewertet ihn (`readaloud.mjs`). Wer diktiert hat, liest den Satz vom eigenen Bildschirm ab; wer vorgelesen hat, kennt das Diktat auswendig. Beides ist Zeit, die der Standard als zwei Fertigkeiten verkauft. **(b) Es ist die Kehrseite von MAJOR 3 der letzten Runde und derselbe Mechanismus wie dort.** RULE 15b verbietet eine aufgeschobene Konstruktion in einem Produktionstext, also sind die Fenster gewandert — und niemand hat gemessen, wohin. Bei L8 ist die neue Vorlesezeile ausgerechnet die Diktatzeile, bei L3 dieselbe Bewegung. Der Guard, den Runde 11 verlangt hat, prüft die **Vorgriffe** in diesen Fenstern; er prüft nicht die **Fenster**. **(c) Die Zahl ist klein und die Regel ist eine Zeile.** Neun von zwölf Lektionen halten die Trennung ohne Zutun, also ist sie die Norm des Materials und nicht eine neue Forderung — sie steht nur nirgends. | **Erstens** die drei Fenster trennen, jeweils in der Richtung, die inhaltlich trägt: L3 `sprechen.readAloud` `[2, 7]` → `[2, 5]` (Zeile 6 „Sie sprechen Arabisch und Deutsch." ist nach RULE 15b frei und lehrt `sie`); L8 `sprechen.readAloud` `[3, 6]` → `[6, 8]` (Zeile 7 und 9 sind beide Wochentagszeilen und damit L8s eigenes Thema); L6 `sprechen.readAloud` `[2, 6]` → `[4, 6]`. **Zweitens** die Regel als Test, in `tests/curricula.test.mjs`, über **jedes** Curriculum und **jede** Lektion: `const d = new Set(l.hoeren?.lines || []); const r = new Set(l.sprechen?.readAloud || []); assert.deepEqual([...d].filter((i) => r.has(i)), [], \`L${l.nr}: Diktat und Vorlesen teilen Zeile ${…}\`);` — harte Null, kein Ratchet, weil neun von zwölf sie heute schon erfüllen und die drei Ausnahmen Fehler sind, keine Kompromisse. **Drittens** denselben Lauf gegen den **gebauten** Stapel statt gegen die Indizes (`buildLesson(...)`, Zeilentexte vergleichen), weil zwei verschiedene Indizes denselben Text tragen können und ein Index-Vergleich das nicht sähe. **Viertens** die Verschiebung an RULE 15b koppeln: wer ein Fenster verschiebt, lässt beide Tests in einem Lauf laufen — der Grund, warum diese zwei Überschneidungen entstanden sind, ist, dass es den zweiten Test nicht gab. | MAJOR |
| `pickPracticeItems` in `src/lib/lesson/buildLesson.js:562–660` — `relevanceScore` **sortiert**, aber es gibt keinen Boden; `SITUATION_MIN` existiert nicht (`grep -rn "SITUATION_MIN" src scripts tests` → 0 Treffer) ↔ Runde-11-MAJOR 1, Teil „Drittens" | Eigene Situationsmessung (Items mit ≥ 1 Wort aus dem **eigenen** Wortfeld), Versuche 1/2/3: **L7 7/4/2** · L2 7/5/4 · L3 7/6/4 · L5 5/7/5 · L6 7/5/5 · L9 7/5/5 · L12 7/6/5 · L7 Versuch 3 im Wortlaut: „Ihr ___ „Tschüss". (sagen)", „Bilden Sie den Satz: [wir / tanzen / zusammen]", „Was ___ ihr? (machen)", „**Das Kind von meiner Schwester ist ein Baby.** ___ ist noch jung.", „**Du ___ in Berlin.** (wohnen)", „**Mein Bruder und ich sind zusammen hier.** ___ sprechen Deutsch.", „Ich ___ Fußball. (spielen)" — zwei von sieben aus „Freizeit und Hobbys", zwei davon `personal-pronouns` aus Lektion 3 · zum Vergleich die geschlossene Hälfte: L5 stieg von 5/3/3 (Runde 11) auf **5/7/5**, L9 von 7/4/3 auf **7/5/5** | **Die Runde hat die beiden Lektionen, die Runde 11 namentlich genannt hat, mit Items bezahlt und die dritte ausgelassen — also die Instanzen geschlossen und die Klasse nicht.** Drei Messungen. **(a) Die Schließung ist echt, aber sie ist Vorrat, kein Wächter.** L5 bekam acht neue Kursraum-Items und steht bei 5/7/5; L9 bekam fünf Café-Items und steht bei 7/5/5. **L7 stand in Runde 11 bei 7/4/2 und steht heute bei 7/4/2** — identisch, weil es keine L7-Items gab und nichts danach fragt. **(b) Der Motor hat die Zahl und liest sie nicht.** `relevanceScore(item, ownTerms, earlierTerms)` läuft in `pickPracticeItems` als **Sortierschlüssel** (Zeile 626); es gibt einen `PRIMARY_MIN = 4` für den Grammatikslug und keinen Boden für die Situation. `practiceReport` meldet `complete` für die Grammatik, nicht für das Wortfeld — der Ratchet `MAX_OFF_PRIMARY_BLOCKS = 0` ist wahr und blind an derselben Stelle wie sein Vorgänger. **(c) Der Preis ist der Verkaufsversprechen des Standards.** `docs/course-standard-2026-09-12.md` §2.2 verkauft zwölf **situative** Lektionen. Der dritte Durchgang von „Freizeit und Hobbys" handelt von Tschüss, Berlin, einem Baby und dem Bruder; das eigene Thema kommt zweimal vor. Für den Lernenden, der eine Lektion wiederholt, ist der dritte Durchgang der, an dem er merkt, ob das Produkt eine Situation oder eine Grammatikbank ist. | **Erstens** den Boden, den Runde 11 schon ausgeschrieben hat, tatsächlich einziehen: `export const SITUATION_MIN = 4;` in `buildLesson.js`, und in `pickPracticeItems` vor Schritt 3 eine Füllstufe, die zuerst Items mit `relevanceScore(it, ownTerms, new Set()) > 0` setzt, bis vier erreicht sind — auf der Nachgeberleiter **nach** der Formkappe und **vor** der Vorversuchssperre, damit sie einen Block nie unter sieben drückt. **Zweitens** die Zahl zu einem gelesenen Bericht machen: `practiceReport` bekommt `situationCount` neben `primaryCount`, und `tests/lesson-engine.test.mjs` bekommt neben `MAX_OFF_PRIMARY_BLOCKS` einen `MAX_OFF_SITUATION_BLOCKS`, der jeden Block mit `situationCount < SITUATION_MIN` mit Lektion, Versuch und Zahl auflistet — Ratchet auf die dann gemessene Zahl, gesenkt nur durch Items, nie durch eine weichere Zahl. **Drittens** die Reparatur, die kein Motor leisten kann und die ich diesmal ausdrücklich **nur für L7** erbitte: sechs bis acht Items aus dem Freizeit-Wortfeld (Kino, Fußball, schwimmen, kochen, tanzen, Wochenende, Musik, Sport, gern) in Formen, die L7 noch nicht besitzt — langsam geschrieben und zweimal gelesen, nicht in einem Schwung wie die 54 der Runde 11, aus denen die beiden BLOCKER dieses Berichts stammen. **Viertens** die Gegenprobe, die den Ratchet ehrlich hält: derselbe Lauf für alle 36 Blöcke, ausgedruckt als Tabelle im Testfehler, damit die nächste Runde sieht, welche Lektion sie bezahlt hat und welche nicht. | MAJOR |

---

## Status der Befunde aus Runde 11

Verifiziert an den Artefakten (gebauter Pool, alle drei Ziehungen, gebaute
`ruleCards.mjs`, die vier gebauten Checkpoints, die gebauten Lektionsstapel,
Validator-Ausgabe, Testlauf, Lint, Duplikatprüfung), nicht am Diff.

| # | Runde-11-Befund | Status | Beleg |
|---|---|---|---|
| B1 | `extra-a11-l06-14` erklärt „Nach brauchen wird ein zu einen" — falsch für A1, ausgeliefert neben dem Item, das genau das als Fehler markiert; vier weitere Cacheitems derselben Klasse | **geschlossen, vollständig, und als Klasse statt als Liste** | `REASON.UNCONDITIONED_RULE` steht in `quality.js` mit vier Klauseln, jede gegen die 481 Erklärungen des Bestands gemessen, bevor sie geschrieben wurde: Transformation (`/nach \w+ wird/`, `/wird … zu <Artikel>/`), absoluter Quantor, Genusvergleich („wie bei maskulinen Nomen" in einem Satz, der ein **anderes** Genus genannt hat) und elliptisches „deshalb <Artikel>". `namesCondition` akzeptiert Genus, Kasus, Skopus **und Person** — und die Person ausdrücklich **nicht** in einer Paarung (`PAIRING_RE`), was genau „Wir steht **immer** mit haben." fängt und „wir wechselt den Vokal **nie**" (A1.2-Entwurf, korrekt) durchlässt. Eigene Nachmessung: **0 von 374** gebauten Poolitems und **0 von 12** notices tragen einen Treffer; die elf gemessenen Cacheitems sind aus dem Pool, ihre Topics behalten ≥ 7 Items; `extra-a11-l06-14.explanationDe` lautet „**Computer ist maskulin: Nach brauchen wird maskulin ein zu einen.**", `4aae7de0` ist ersetzt durch `extra-a11-l06-22` „Er hat ___ Handy. (unbestimmter Artikel)" mit „**Handy ist neutral: ein Handy.**". Ein zweiter Guard (`tests/lesson-pool-rules.test.mjs`) prüft, dass **kein** Auslöser des Pools auf zwei Formen abbildet, mit der von Runde 11 genannten Gegenprobe als eigenem Testfall. Die Item wird **nicht** repariert, sondern ausgeschlossen, und ein Handitem stoppt den Build mit seiner Id — die einzig richtige Entscheidung, weil die fehlende Bedingung Autorschaft ist. **Rest:** der Lint läuft über Poolitems, nicht über Karten und notices, wie Runde 11 es formuliert hat; ich habe beide gemessen — 0 materielle Treffer, aber zwei **wahre** Kartensätze („Der Artikel steht immer vor dem Nomen", „Die höfliche Form ist Ihr und steht immer mit großem I") würden das Prädikat auslösen, also braucht die Ausweitung erst ein Carve-out (Minor 4). |
| M1 | Die Lemmakappe zählt die Klammer als Inhalt, `complete` liest niemand, es gibt keinen Situationsboden | **teilweise: (a) und (b) geschlossen, (c) offen** | **(a)** `itemLemmas` lemmatisiert den Prompt mit entfernter Klammer (`stripInstruction`), der Kopfkommentar schreibt die Messung mit, und ein Test prüft beide Richtungen (`bestimmter`/`artikel` weg, `fenster` bleibt; vier Klammerformen ergeben je genau ein Lemma). Gemessen: L5 steigt von 5/3/3 auf **5/7/5** eigene Wortfeldtreffer, jeder der 36 Blöcke hat `relaxUsed = 0`, **7 von 7** verschiedene Formen, größte Lemmagruppe 2, größte Antwortschlüsselgruppe 2, Überschneidung in allen drei Paaren 0 außer L8 (2, `mustCover`) und L12 im Wrappaar (1, die Höflichkeitsform). **(b)** `practiceReport().complete` wird gelesen: `MAX_OFF_PRIMARY_BLOCKS = 0` in `tests/lesson-engine.test.mjs`, `relaxUsed === 0` ist hart assertiert, und L5 Versuch 3 ist über acht neue Items (`extra-a11-l05-17`…`-24`) aus dem eigenen Kursraum-Wortfeld auf fünf eigene Slugitems gehoben. **(c)** `SITUATION_MIN` existiert nicht; L7 Versuch 3 steht unverändert bei **2 von 7** — MAJOR 4. |
| M2 | In 11 von 36 Blöcken beantwortet ein Item ein anderes derselben Sieben | **geschlossen, mit einer begründeten und dokumentierten Ausnahme** | Die sechste Achse steht in `fits()`, wo die anderen fünf auch stehen: `answerWords` (Funktionswörter und Kurzfragmente weg), `promptWords` (Klammer weg, ganze Tokens), `leaksAnswer` in **beiden** Richtungen und über **alle** `accepted`-Formen, zuletzt auf der Nachgeberleiter, und der `mustCover`-Durchgang respektiert sie. Eigene Nachmessung mit der Produktionsfunktion über alle 36 Blöcke: **0 Paare**. Die vier von Runde 11 zitierten Paare stehen als `ANSWER_LEAK_PROBES`-Fixture und müssen alle gemeldet werden, drei Negativsonden halten den Guard ehrlich („(mitkommen)" verrät nichts, `gearbeitet` verrät `arbeite` nicht, ein Teilüberlapp bleibt erlaubt), und der Blocklauf assertiert `deepEqual(rows, [])` statt eines Ratchets. **Rest:** die dokumentierte Vier-Buchstaben-Grenze lässt Funktionswortantworten durch; meine gröbere Messung findet dadurch noch 58 Paare, von denen **genau eines** inhaltlich zählt — L11 Versuch 3: `extra-a11-l11-04` „Kaufst du heute ___? (einkaufen)" → `ein` neben `8a2be339` „___ ihr heute **ein**? (einkaufen)" (Minor 5). |
| M3 | Der Konstruktionsguard bindet die Karte, aber nicht den Satz, den der Lernende diktiert oder vorliest — acht Stellen in vier Lektionen | **geschlossen an der Zahl, bezahlt an den Texten** | `src/data/curricula/constructions.js` ist das gemeinsame Modul (Muster + `introducedAt` aus `primarySlug` gelesen), driftgeschützter Zwilling unter `astro-site/`, importiert vom Kartenguard **und** vom Validator; **RULE 15b misst 0 als harte Regel** über jeden Text, den der Lernende produziert — Diktat, Vorlesen, pretest-Modell und Schreiben-Muster. RULE 15 fällt von 12 auf **3**, und die drei Reste sind inhaltlich gebunden und in der eigenen notice angekündigt. Drei Fenster sind verschoben (L3 `hoeren [3,5] → [5,7]` und `readAloud [1,6] → [2,7]`, L7 `hoeren [2,6] → [3,6]`, L8 `readAloud [1,6] → [3,6]`), **kein Dialog ist umgeschrieben** — das ist CONTRACT §2 und genau richtig. **Rest, und es sind zwei:** vier **Modelltexte** sind stattdessen geändert worden, zwei davon ins Unidiomatische bzw. gegen die Figurenfakten (MAJOR 2), und einer unter die eigene Wortuntergrenze (MAJOR 1); und die verschobenen Fenster lassen in **L3 und L8** Diktat und Vorlesen dieselbe Zeile ziehen (MAJOR 3). Die Fenster selbst sind pädagogisch gut gewählt: L3 diktiert jetzt „Sie sprechen Arabisch und Deutsch." und „Ja, er spricht Englisch und Deutsch." — beides genau die Personalpronomen, die L3 lehrt. |

**Zusätzlich geprüft und weiterhin in Ordnung:** die **zwölf Schreibaufgaben** sind mit der
Serverbank deckungsgleich (Aufgabentext, Leitpunkte bzw. Formularfelder, Wortgrenzen), alle
zwölf `taskKey`s vorhanden; `anrede` in allen zwölf Sprechaufträgen `'Sie'`, die zwölf
pretests sprechen alle Sie; RULE 14 und RULE 12 als harte Regeln auf 0; RULE 13 (4
Sprechaufträge ohne Mission) unverändert vertretbar; die vier Checkpoints: **80** Items,
**0** Selbstablehnungen über 911 Bewertungsflächen (252 gezogene, 374 Poolitems, die
Handquelle und die getippten Checkpoint-Items, jeweils mit **jeder** `accepted`-Variante),
**32 von 32** verschiedene Poolitems, **keine** doppelt genutzte Dialogzeile, **kein**
Poolitem jenseits seines Kapitelfensters (das ist die Neuerung dieser Runde und sie hält),
und die 16 Lesen-Aussagen weiterhin ohne Teilzeichenkette und ohne Ein-Token-Abstand;
**395** Wiederholungskarten, **0** Selbstablehnungen gegen `gradeTypedReview`;
`a11.audio.js` weiterhin der leere Manifest-Stub, alle Hörflächen fallen ehrlich auf die
Computerstimme zurück (Eigentümeraufgabe, kein Befund). **Die Erinnerungsmail duzt weiterhin,
und das lasse ich als Eigentümerentscheidung stehen — kein MAJOR**, wie in Runde 5 bis 11;
die Notiz darüber im Kopf von `course-reminder.mjs` fehlt zum achten Mal (Minor 8).

**Zu den 14 neuen Handitems, einzeln gelesen.** Dreizehn sind gutes, natürliches,
A1-taugliches Deutsch mit richtigem Schlüssel. Die acht L5-Items sind die beste Arbeit
dieser Runde und genau das, was Runde 11 verlangt hat — acht **verschiedene** Rahmen statt
achtmal „___ Nomen ist Adjektiv. (bestimmter Artikel)": das Paar unbestimmt→bestimmt
(`extra-a11-l05-17` „**Ein** Bild ist hier. ___ Bild ist blau."), die Rückfrage
(`-18` „Wie ist ___ Tür? — Sie ist weiß."), die Entscheidungsfrage (`-19` „Ist ___ Lineal
hier?"), der Zwei-Nomen-Rahmen (`-21` „Hier sind ein Buch und ein Heft. ___ Buch ist
rot."), die zitierte Frage (`-22` „Tim fragt: Wo ist ___ Papier?"), der Zimmer-Rahmen
(`-24`) und zwei Korrekturen mit ganzem Satz als Antwort (`-20`, `-23`). Alle acht nutzen
ausschließlich L5s eigenes Kursraum-Wortfeld, und die Wirkung ist gemessen: Versuch 2 von
7/7 eigener Wortfeldtreffer, Versuch 3 von 3/7 auf 5/7. Die fünf L9-Items decken `hat`,
`haben`, `hast` (als Korrektur), `habt` (als Frage) und `hat` im Wortsalat — genau die
Lücke, die der dritte Durchgang hatte. **Das eine schwächere ist `extra-a11-l09-14`**
(„Korrigieren Sie: „Du habt Durst."") — BLOCKER 2: zwei minimale Reparaturen, eine
gewertet. `extra-a11-l06-22` ist die saubere Ersetzung des in Runde 11 gerügten
`4aae7de0` und erklärt das neutrale `ein` ohne Seitenblick auf ein anderes Genus.

---

## Bewertungsübersicht (Runde 12, Runde 11 in Klammern)

| Lektion | Deutsch | Niveau | Dialog | Prüfung | Kohärenz | Σ /25 |
|---|---|---|---|---|---|---|
| 1 Hallo, ich bin … | 4 (4) | 4 (4) | 5 (5) | 4 (4) | 4 (4) | **21** (21) |
| 2 Ich bin Studentin | 4 (4) | 4 (4) | 4 (4) | 4 (4) | **3** (4) | **19** (20) |
| 3 Meine Familie | **3** (4) | 4 (4) | 4 (4) | **3** (4) | **3** (4) | **17** (20) |
| 4 Auf dem Flohmarkt | 4 (4) | 4 (4) | 5 (5) | 4 (4) | 4 (4) | **21** (21) |
| 5 Im Klassenzimmer | 5 (5) | 4 (4) | 5 (5) | 4 (4) | **4** (2) | **22** (20) |
| 6 Der erste Tag im Büro | **5** (3) | 4 (4) | 4 (4) | 4 (4) | **4** (3) | **21** (18) |
| 7 Freizeit und Hobbys | 4 (4) | 4 (4) | 5 (5) | **3** (4) | 3 (3) | **19** (20) |
| 8 Termine und Uhrzeit | 4 (4) | 4 (4) | 5 (5) | **3** (4) | **3** (4) | **19** (21) |
| 9 Im Café | **4** (5) | 4 (4) | 5 (5) | 4 (4) | **4** (3) | **21** (21) |
| 10 Am Bahnhof | **4** (5) | 4 (4) | 5 (5) | 4 (4) | 4 (4) | **21** (22) |
| 11 Mein Tag | 4 (4) | 4 (4) | 4 (4) | **3** (4) | **4** (3) | **19** (19) |
| 12 Feste feiern | 5 (5) | 4 (4) | 5 (5) | **3** (4) | 4 (4) | **21** (22) |

Kursmittel **20,1/25** (Runde 11: 20,4 · Runde 10: 20,5 · Runde 9: 20,5 · Runde 8: 20,4 ·
Runde 7: 20,3 · Runde 6: 19,9 · Runde 5: 19,7 · Runde 4: 19,9 · Runde 3: 20,4 · Runde 2:
19,8 · Runde 1: 17,2). Zwei Lektionen besser, sechs schlechter, vier gleich.

**Zur Lesart, und sie unterscheidet zwei Arten von Minus.** Die Gewinner sind die beiden
Lektionen, an denen die Motorschließungen tatsächlich ankommen: **L6 (18 → 21)**, weil der
falsche Erklärungssatz nicht nur repariert, sondern als Klasse unmöglich gemacht ist, und
weil `extra-a11-l06-22` die zweite Instanz sauber ersetzt; **L5 (20 → 22)**, weil die
Lemmakappe die Klammer nicht mehr mitzählt und acht neue Items aus dem eigenen Kursraum
dazukommen — die Lektion serviert jetzt in allen drei Durchgängen ihr eigenes Thema statt
des Flohmarkts von Lektion 4. Bei den Verlierern muss man zwei Gruppen trennen, und ich
sage das ausdrücklich, weil sonst die Tabelle den Fortschritt dieser Runde verdeckt.
**Neu entstanden** sind nur drei: **L3 (20 → 17)** — die Modellantwort widerspricht dem
eigenen Dialog und beantwortet die eigene Aufgabe nicht mehr, dazu die neue
Diktat/Vorlesen-Überschneidung; **L8 (21 → 19)** — dieselbe Überschneidung plus der
Wortstellungsbefund im benoteten Checkpoint 3; **L2 (20 → 19)** — der einzige
Beispieltext, der seine Wortuntergrenze hielt, hält sie nicht mehr. **Neu gemessen, nicht
neu entstanden** sind die übrigen: **L7 (20 → 19)**, **L9 (21 → 21)**, **L10 (22 → 21)**,
**L11 (19 → 19)** und **L12 (22 → 21)** verlieren an den beiden BLOCKERn, und beide
Befunde sind älter als diese Runde — die Wortstellungsenge steht seit dem ersten
Handitem-Batch im Pool, die Korrekturambiguität von L10 seit Runde 7. Dass sie erst jetzt
auftauchen, liegt an einer Probe, die diese Reihe zwölf Runden lang nicht gefahren hat:
**nicht zu fragen, ob der Kurs seine eigene Antwort akzeptiert, sondern ob er die zweite
richtige akzeptiert.** **Stärkste:** L5 (22). **Schwächste:** L3 (17).

---

## Würde ich meinen Namen jetzt daraufsetzen?

**Auf den Motor, auf die Dialoge, auf die Checkpoints und auf die Regelkarten: ja, und auf
den Motor mit mehr Überzeugung als je in dieser Reihe. Auf den Kurs als Ganzes: nein.**

Ich habe alle vier Punkte aus Runde 11 an den Artefakten nachgeprüft: der BLOCKER und zwei
MAJOR **vollständig geschlossen**, der dritte zur Hälfte. Und die Art der Schließung ist
das, was diese Runde auszeichnet. `REASON.UNCONDITIONED_RULE` ist kein Filter mit einer
Liste, sondern ein Prädikat mit vier Klauseln, jede gegen den Bestand gemessen, **bevor**
sie geschrieben wurde, mit einer Ausnahmebehandlung, die aus der Gegenrichtung belegt ist
(die Person zählt als Bedingung, außer in einer Paarung — und der A1.2-Entwurf liefert den
Satz, der beweist, dass die Ausnahme nötig ist). Der Kurs kann eine falsche Regel jetzt
nicht mehr **aussprechen**, und er repariert sie nicht, sondern wirft sie hinaus, weil die
fehlende Bedingung Autorschaft ist und kein Build sie erraten darf. Die sechste
Diversitätsachse steht dort, wo die anderen fünf stehen, mit Gegenproben in beide
Richtungen, und meine eigene Nachmessung über alle 36 Blöcke findet null. RULE 15b ist eine
harte Null über **jede** Zeile, die der Lernende produziert, und die Muster leben in einem
Modul, das Karte, Item und Dialogfenster gemeinsam lesen. Das sind drei Regeln mit drei
Tests, und keine einzige davon ist eine Id-Liste.

**Und trotzdem: der Kurs sagt einem Lernenden zweimal, seine richtige Antwort sei falsch,
und einmal davon im benoteten Prüfungsteil.** „Gestern habe ich gearbeitet." ist ein
untadeliger deutscher Satz aus genau den vier Wörtern, die die Aufgabe vorgibt; das Verb
steht auf Position 2, wie die Regelkarte derselben Lektion es verlangt; der pretest
derselben Lektion nennt diesen Anfang als guten Anfang; drei Items desselben Pools führen
die vorangestellte Zeitangabe in `accepted`. Er ist `wrong` — in Lektion 11 und in
Checkpoint 4. Und „Ihr habt Durst." ist die zweite minimale Reparatur von „Du habt Durst.",
für die der Kurs seit Runde 7 eine Regel hat, die an der Kongruenz vorbeisieht. Das ist
derselbe Fehler wie der BLOCKER der letzten Runde, nur von der anderen Seite: damals stand
eine Regel ohne ihre Bedingung in einer Erklärung, jetzt steht eine Aufgabe ohne ihre
Eindeutigkeit im Prompt. **Beide Male hat der Kurs mehr behauptet, als der deutsche Text
hergibt.**

Die drei weiteren Befunde sind kleiner und zwei davon sind die Rechnung dieser Runde. Dass
alle sechs Mitteilungs-Beispieltexte durch die Checkliste fallen, die einen Absatz höher
steht, ist zwölf Runden lang niemandem aufgefallen — mir eingeschlossen —, und es ist die
billigste Reparatur in diesem Bericht: eine Zeile Test, der `scoreWriting` auf den eigenen
Mustertext anwendet, und sechs neu geschriebene Texte. Dass die RULE-15b-Reparatur die
L3-Modellantwort gegen den eigenen Dialog gestellt hat, ist der Preis dafür, dass ein Guard
Texte ablehnen kann, die niemand misst. Und die zwei neuen Diktat/Vorlesen-Überschneidungen
sind das, was passiert, wenn man Fenster verschiebt, ohne zu prüfen, wo sie landen.

Die Arbeit ist überschaubar und vollständig ausgeschrieben: ein Guard, der zu jedem
typisierten Item die zweite richtige Antwort mechanisch erzeugt und verlangt, dass sie
entweder akzeptiert oder vom Prompt ausgeschlossen ist — das schließt beide BLOCKER in
einem Stück; ein Test, der `scoreWriting(schreiben, schreiben.sample)` grün verlangt, plus
sechs neue Mustertexte; eine Zeile, die Diktat- und Vorlesefenster disjunkt hält, plus drei
verschobene Indizes; RULE 14 über `pretest.model` und `schreiben.sample` ausgedehnt; und
`SITUATION_MIN` mit seinem Ratchet, dazu sechs bis acht Freizeit-Items für Lektion 7. Das
ist wieder etwa ein Tag. **Und wie in Runde 11 bitte ich ausdrücklich nicht um viele neue
Items:** die acht L5-Items dieser Runde zeigen, wie gut ein kleiner, langsam geschriebener
Batch sein kann, und das eine schwächere Item derselben Runde ist einer der beiden BLOCKER
dieses Berichts.

---

**Minors (acht Zeilen, nicht erfasst):** (1) Die Buchstabenkarte mischt weiter Groß- und
Kleinschreibung in der Namensliste (`be, ce, de` neben `Jot, Vau, Ypsilon, Zett`, dazu
`W we` im `content` gegen „W heißt **We**" in `commonMistakes`), und **kein Item des ganzen
Pools prüft Q** (gemessen: 0 Treffer auf `Buchstabe Q`). (2) Die `-chen/-lein`-Zeile der
L4-Karte („das **Mädchen**, das **Brötchen**") lehrt zum vierten Mal eine Endungsgruppe, von
der der Kurs **null** Wörter enthält (nachgezählt im gebauten Pool: 0 Items mit `Mädchen`
oder `Brötchen`); sie sollte ersatzlos fallen. (3) Im Lesen-Teil unverändert seit Runde 10:
`a1.1-cp1-lesen-3` („Der **Student** spricht Englisch und Deutsch.", Schlüssel *Falsch*) ist
im Text nicht widerlegt, sondern unerwähnt, und `a1.1-cp3-lesen-1`/`-4` sind zweimal dieselbe
Umformung mit derselben Sprecherin und demselben Verb („Ana hat Durst." / „Ana hat Hunger.")
— zwei von vier Items eines Papiers. (4) Der `UNCONDITIONED_RULE`-Lint läuft nur über
Poolitems; Runde 11 hat ausdrücklich „des Pools, **der Karten und der notices**" verlangt.
Ich habe beide gemessen: 0 materielle Treffer, aber zwei **wahre** Kartensätze
(`definite-articles`: „Der Artikel steht **immer** vor dem Nomen"; `possessive-articles`:
„Die höfliche Form ist Ihr und steht **immer** mit großem I") lösen das Prädikat aus — die
Ausweitung braucht also erst ein Carve-out für „steht immer vor/mit <Wortart>", dann den
Lauf. (5) Die Vier-Buchstaben-Grenze der neuen Leckachse ist dokumentiert und richtig, hat
aber genau einen inhaltlichen Rest: L11 Versuch 3 serviert `extra-a11-l11-04` („Kaufst du
heute ___? (einkaufen)" → `ein`) neben `8a2be339` („___ ihr heute **ein**? (einkaufen)"),
also zwei Hälften derselben Satzklammer, von denen eine die andere druckt. (6) Der
Topic-Slug `numbers` erzeugt im Wiederholungsindex weiter eine Doppelkarte: `pattern:numbers`
ist Zeichen für Zeichen `pattern:verb-sein`, gemessen über alle **395** Karten die einzige
Dublette. (7) **Sieben** gezogene Items tragen als `questionEn` die deutsche Frage statt
einer Übersetzung (`34957c67`, `0fe8146d`, `dd1c3d60`, `cb35f9d0`, `da9e486f`, `9fd52b13`,
`e0ac0425`); und **22** gezogene Einwortantworten akzeptieren nur die große Schreibung,
während die Handitems derselben Lage beide führen — dieselbe Antwort `die` ist auf 25 Items
`correct` und auf einem `typo`, `Der` auf 27 `correct` und auf zweien `typo`. Ein Retry ist
kein Fehlurteil, aber es sind zwei Urteile für eine Lage. (8) L3, L6 und L8 nutzen je eine
Dialogzeile doppelt (MAJOR 3); `extra-a11-l11-10` erklärt das Perfekt als „habe + gearbeitet
ist ein **fester Ausdruck für gestern**", was die L11-notice deckt („Die Regel dazu (Perfekt)
kommt in A1.2"), die Erklärung selbst aber nicht sagt; `a11.audio.js` ist weiter der leere
Manifest-Stub; `tests/lesson-extra-items-a12.test.mjs:464` ist weiter der einzige
übersprungene Test der Suite — korrekt geführt, aber er sollte mit A1.2 wieder anspringen;
der Rückblick sagt dem Lernenden nirgends, dass ein zweiter Durchgang **andere** Aufgaben
bringt; und die Eigentümerentscheidung zur du-Anrede der Erinnerungsmail ist zum achten Mal
nirgends aufgeschrieben.

BLOCKER: 2 · MAJOR: 4
