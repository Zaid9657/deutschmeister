
# A1.1 — DaF-Fachreview #10, nach Runde 10 (2026-09-12)

Reviewer: DaF-Lehrkraft (Erwachsenenbildung, Integrationskurs) und Prüferin für
Goethe-Zertifikat A1 *Start Deutsch 1*. Zehnter Durchgang. Basis:
`REVIEW-daf-2026-09-12.md` (5 BLOCKER · 45 MAJOR), `REVIEW-daf-2-2026-09-12.md`
(1 BLOCKER · 35 MAJOR · 44 MINOR), `REVIEW-daf-3` bis `REVIEW-daf-5` (je
3 BLOCKER · 11 MAJOR), `REVIEW-daf-6` (3 BLOCKER · 7 MAJOR), `REVIEW-daf-7`
(3 BLOCKER · 5 MAJOR), `REVIEW-daf-8` (0 BLOCKER · 5 MAJOR) und
`REVIEW-daf-9-2026-09-12.md` (0 BLOCKER · 5 MAJOR), angewendet in `main` @
`47860e6` („A1.1 round 10: close DaF review #9 + report", PR #127). Gemessen wurde
ausschließlich der Commit-Stand. **A1.2 ist durch Eigentümerentscheidung vom
2026-09-13 pausiert und nicht Gegenstand dieses Reviews.**

Gemessen, nicht angenommen:

* **Die echte Ziehung für alle drei Versuche**: `planPractice(CURRICULUM_A11, POOL, a)`
  für `a` = 1, 2 **und** 3 importiert, alle **252** ausgelieferten Items
  (**209 verschiedene**) mit `questionDe`/`questionEn`/`options`/`answer`/`accepted`/
  `explanationDe`/`caseSensitive`/`minLektion` ausgedruckt und einzeln bewertet; dazu für
  jeden der **36 Blöcke** `practiceReport()` (`relaxUsed`, `complete`, `eligible`), die
  Zahl der echten Drills (`drillsSlug`), die Zahl verschiedener `taskShape`-Schlüssel und
  **alle drei Versuchspaare** (1∩2, 2∩3 **und 3∩1**).
* Eine **eigene, gröbere Rahmenmessung** neben `taskShape`: derselbe Satzrahmen ohne die
  Klammeranweisung und ohne die Wortlänge, über alle 36 Blöcke.
* `checkAnswer` + `checkOptionsFor` auf **1180 Bewertungsflächen**: 252 gezogene Items,
  der ganze gebaute Pool (**312**), die Handquelle (**165**), **56** getippte
  Checkpoint-Items und **395** Wiederholungskarten gegen `gradeTypedReview` — je mit der
  eigenen Antwort und mit der kleingeschriebenen Probe, gruppiert nach der exakten
  Antwortzeichenkette.
* `politeCaseItem()`, `untaughtForm()`, `exclusionReason()`, `ambiguousCorrection()`,
  `metalinguisticPrompt()` über beide Pooldateien und über die Checkpoints.
* **Alle vier Checkpoints gebaut**, alle 80 Items ausgedruckt; die **16 Lesen-Aussagen**
  Zeile für Zeile gegen ihren eigenen Text gehalten (Teilzeichenkette, Ein-Token-Abstand,
  Richtig/Falsch-Rhythmus, Falsifikatorart), dazu Zeilenüberschneidung
  Diktat ↔ Vorlesen ↔ Lesetext und Poolitem-Wiederverwendung.
* **Die 21 neuen Handitems** in `a11.extra.json` einzeln gelesen (Grammatik,
  Natürlichkeit, A1-Tauglichkeit, Situationsbezug, Antwortschlüssel, `accepted`), dazu
  ihre `taskShape`- und `answerKey`-Schlüssel.
* **Die vier Kartenguards nachgebaut und mit eigenen Injektionen beschossen**: die
  Runde-8-Regressionszeilen, 14 kursfremde Formen und acht Sätze aus der Grammatik
  späterer Lektionen, jeweils in eine echte Karte eingesetzt und durch `outsideLexis()`
  und `levelDeferralOffenders()` geschickt.
* **Der Weg der Versuchszahl**: `attemptFromCompletions`, `LessonPlayerPage.jsx`,
  `countCompletedRuns` (Supabase), `localRunCount`/`recordLocalLesson` (signiert aus),
  **und die Zusammenführung beim Anmelden** (`mergeLocalProgress`).
* Das Curriculum: Dialoge, notices, Kann-Beschreibungen, `examTeile`, Wortfeld, Figuren,
  Register, pretests, Diktat-/Vorlesefenster, die **zwölf Schreibaufgaben** gegen
  `netlify/functions/_shared/writingTasks.mjs` (Aufgabentext, Leitpunkte bzw.
  Formularfelder, Wortgrenzen **deckungsgleich**), die Sprechaufträge (`anrede` in allen
  zwölf Lektionen `'Sie'`).
* `node scripts/validate-curriculum.mjs a1.1` (RULE 10: 14/14 · RULE 11: **49**/49 ·
  RULE 11b: **0, harte Regel** · RULE 12: 0, harte Regel · RULE 13: 4/4 · RULE 6b: 26/26 ·
  RULE 15: 12/12 · RULE 16: 0/0 · RULE 14: 0).
* `npm test`: **822 Tests, 821 grün, 1 übersprungen** (der A1.2-Ziehungstest).
  `npm run lint`: 0 Fehler, 0 Warnungen. `npm run check:duplicates`: OK.

## Urteil in einem Absatz

**Runde 10 hat alle fünf MAJOR aus Runde 9 an den Artefakten geschlossen, und drei der
fünf Schließungen sind die besten dieser ganzen Reihe.** Die Versuchszahl ist keine
Konstante mehr, sondern wird aus den abgeschlossenen Durchgängen abgeleitet
(`attemptFromCompletions`, Zyklus 3), jeder Versuch schließt die Ziehungen der früheren
aus, und die Überschneidung fällt gemessen von **62/84 auf 0** in zehn von zwölf Lektionen
(L8: 2, und das sind genau seine zwei `mustCover`-Schlüssel). L8 produziert „**halb**"
(`extra-a11-l08-03`) und die offizielle Uhrzeit (`2e4e344c`, „vierzehn Uhr dreißig") jetzt
in **allen drei** Versuchen. Der Lesen-Teil aller vier Checkpoints ist **kein
Zeichenkettenvergleich mehr**: gemessen über alle 16 Aussagen steht **keine einzige** als
Teilzeichenkette in ihrem eigenen Text, **keine** unterscheidet sich von einer Textzeile in
genau einem Token, und was dort jetzt steht — „Die Lampe kostet acht Euro." aus „Das ist
eine Lampe. **Sie** kostet acht Euro.", „Ana steht um sechs Uhr auf." aus Lenas „Ich stehe
um sechs Uhr auf." — ist echte Lesearbeit in der Gestalt von *Start Deutsch 1* Lesen
Teil 1. `politeCaseItem` liest die Selbstauskunft eines Items für **jede** Antwortgestalt:
über Pool, Karten und Checkpoints **0** Items, die ihre Aufgabe höflich nennen und den
Großbuchstaben verzeihen, und **0**, die ohne Prädikat streng sind. Die Lexik-Hälfte der
Kartenguards beißt zum ersten Mal wirklich — von den 14 kursfremden Sondierungsformen aus
Runde 9 (`repariert`, `verhandelt`, `gewöhnlich`, `wäre`, `müsste`, `dürfte`, `geschwommen`
…) wird **jede** gefangen, wo Runde 9 15 von 16 durchgehen sah. **Und trotzdem
unterschreibe ich nicht, und zwar aus einem Grund, der diesmal nicht neu ist, sondern zum
dritten Mal derselbe: die Regel wird an der Stelle gemessen, an der sie bequem zu messen
ist, und nicht an der, an der die Entscheidung fällt.** `taskShape` ist von der Wortlänge
auf die Wortart umgestellt worden und meldet in **allen 36 Blöcken 7 von 7** verschiedenen
Aufgabenformen — während Lektion 1 im dritten Versuch **fünf buchstabengleiche Sätze**
zieht („Lesen Sie die Buchstaben: T-S-C-H-Ü-S-S. Schreiben Sie das Wort: ___", dann
B-U-C-H-S-T-A-B-I-E-R-E-N, B-U-C-H-S-T-A-B-E, G-R-U-ß, D-A-N-K-E), die nur deshalb fünf
verschiedene Schlüssel tragen, weil das **buchstabierte Wort im Prompt steht** und seine
Buchstabenzahl in den Schlüssel wandert. Sechs der 21 neuen Handitems sind dieselbe
Aufgabe in sechs verschieden formulierten Klammern („(Das Verb steht zuerst.)",
„(Beginnen Sie mit dem Verb.)", „(Die Stimme steigt am Ende.)", „(Zuerst das Verb, dann
das Subjekt.)", „(Das Verb steht vorn.)", „(höflich mit Sie)") — und L10 zieht im ersten
Versuch **fünf** davon. Der zweite Befund ist die Kehrseite des Erfolgs: der Zyklus ist
jetzt **drei** Versuche lang, aber der Mengen-Ratchet `MAX_CAP_STARVED_LEKTIONEN = 0` misst
**nur Versuch 1**, die Antwortschlüssel- und Formkappen nur die Versuche **1 und 2**, und
der Überschneidungstest nur die Paare **1→2 und 2→3** — nicht **3→1**, obwohl der Zyklus
wrappt. Gemessen im nicht gemessenen Bereich: L6 Versuch 3 gibt **sieben von sieben**
Plätzen an dieselbe Schablone „___ + Nomen. ((un)bestimmter Artikel)" und den
Antwortschlüssel `eine` **dreimal** (Kappe 2), L4 Versuch 3 fünfmal `kostet`, L1 Versuch 3
fünfmal `Buchstaben lesen`, und L8 wiederholt zwischen Versuch 3 und dem darauf folgenden
Versuch 1 **fünf von sieben** Items. Dazu zwei weitere: die neue, level-genaue
Vorgriffsregel für die Regelkarten nimmt **jedes Funktionswort** aus — und genau dort
liegen die Verschiebungen des Kurses, weshalb der Satz, an dem Runde 8 und Runde 9 den
Befund festgemacht haben („ein und eine stehen bei einer Sache." auf der L5-Karte) und die
Satzklammer auf der L10-Karte („Ich kaufe am Freitag ein.") **beide unbeanstandet
durchgehen**, und dieselbe Ausnahme lässt den neuen `minLektion`-Filter „Das Kind **von
meiner** Schwester …" in Lektion **3** ziehen; und die abgeleitete Versuchszahl, das
Herzstück dieser Runde, wird beim **Anmelden vernichtet** — `mergeLocalProgress` schreibt
alle Antworten einer Lektion in **einen** `INSERT`, und `countCompletedRuns` zählt
`created_at`-Werte, also wird aus drei Durchgängen einer.

**Zahlen: 0 BLOCKER · 4 MAJOR.** Die Blockerfreiheit hält zum dritten Mal und ist erneut
gegen alle vier Definitionen gemessen worden. Die zweite Hälfte der Bedingung aus
`docs/course-standard-2026-09-12.md` §8 (0 MAJOR) steht weiter aus. MINORs sind am Ende als
acht Zeilen erfasst.

---

## BLOCKER

**Keine.** Zum dritten Mal. Die vier Definitionen einzeln gegen den heutigen Stand
gemessen statt behauptet:

* **„Der Lernende wird auf eine richtige Antwort hin falsch bewertet."** **0**
  Selbstablehnungen über **1180** Bewertungsflächen: 252 gezogene Items, 312 Poolitems,
  165 Handitems, 56 getippte Checkpoint-Items und 395 Wiederholungskarten, jede mit ihrer
  eigenen Antwort und über `checkOptionsFor(item)` bzw. `gradeTypedReview`. Die
  Kleinschreibprobe über alle 156 verschiedenen großgeschriebenen Antwortzeichenketten
  findet **zehn** Gruppen mit verschiedenen Urteilen, und **jede einzelne** ist entweder
  die lockere Richtung (`correct` statt `typo`, weil das eine Item seine kleine
  Zwillingsschreibung in `accepted` führt) oder die **richtige** Unterscheidung: `Sie` ist
  in `extra-a11-l03-08` („Frau Kaya, sprechen ___ Englisch?", `caseSensitive: true`) hart
  und in `extra-a11-l03-02` („Meine Schwester ist jung. ___ spricht Deutsch.") locker;
  `Ihr` in `extra-a11-l12-08/09` hart und in `4299d5ca` („___ seid meine Freunde.",
  „informell") locker. Die einzige Zeile, die zwischen Quelle und gebautem Pool anders
  urteilt, ist `extra-a11-l10-18` — in `a11.extra.json` noch locker, im **ausgelieferten**
  `a11.json` hart, weil der Build den Stempel setzt; ausgeliefert wird der harte.
* **„Dem Lernenden wird für A1 etwas Falsches beigebracht."** Die zwölf notices, die zwölf
  Dialoge, die zwölf Regelkarten, die 21 neuen Handitems und die 80 Checkpoint-Items
  enthalten nach meiner Durchsicht **keine falsche Aussage über das Deutsche**. Die
  Runde-9-Zeile ist repariert: `alphabet-pronunciation` schreibt heute „**Ü ist ein
  Buchstabe mit Umlaut. Man sagt u-Umlaut.**" statt „Ü ist ein eigener Buchstabe."
  Geprüft habe ich diesmal ausdrücklich die neuen Konjugationen des Lesen-Bauers
  (`VERB_3SG`): `duscht`, `frühstückt`, `steht … auf`, `kostet`, `braucht`, `kauft`,
  `spielt` — alle korrekt, `isst`, `schläft`, `lädt`, `liest`, `spricht`, `fährt` und
  `sieht` stehen als Tabelleneinträge statt als Suffixregel, was genau richtig ist.
* **„Der Kurs verspricht etwas, das er nicht enthält."** RULE 16 misst **0**, RULE 12 ist
  eine harte Regel und misst **0**; alle 33 `examTeile`-Behauptungen der zwölf Lektionen haben eine Fläche
  in ihrer eigenen Lektion. Der Grenzfall aus Runde 9 ist **weg**: L8 produziert „halb"
  und die offizielle Uhrzeit in allen drei Versuchen, nachgezählt an der Ziehung.
* **„Eine geprüfte Form, die der Kurs nie lehrt."** `untaughtForm()` über beide
  Pooldateien: **0**. `minLektion` filtert vor jeder Kappe und wird nie gelockert;
  nachgerechnet über alle **36** Blöcke: **0** Items, deren `minLektion` über ihrer
  Lektion liegt. Die eine Form, die ich dagegen gehalten habe — `schönen` in
  `extra-a11-l12-17` — steht als Chunk „Schönen Tag noch" im Wortfeld von L12 und ist damit
  gelehrt (die metasprachliche Klammer „(Adjektiv)" bleibt Minor 5). Die Vorgriffe, die ich
  in MAJOR 3 messe (`von meiner Schwester`, `mit dem Bus`), stehen ausschließlich im
  **Prompt**, nie im Antwortschlüssel — rezeptiv, nicht geprüft, und deshalb MAJOR und
  nicht BLOCKER.

---

## MAJOR

| Feld | Zitat | Problem | Fix (pasteable) | Schwere |
|---|---|---|---|---|
| `taskShape()` in `src/lib/lesson/buildLesson.js:259–265` ↔ `MAX_SAME_TASK_SHAPE = 1` (ebd. 92) ↔ `tests/lesson-engine.test.mjs:408–433` und `:480–515` | Kopfkommentar: „The mask is bound to WORD CLASS instead … So the two sentences above are one key, and **the cap binds for the first time**." · Gezogen, L1 Versuch 3, sieben Items, davon **fünf buchstabengleich**: `dfa7474b` „Lesen Sie die Buchstaben: **T-S-C-H-Ü-S-S**. Schreiben Sie das Wort: ___", `8bb13847` „… **B-U-C-H-S-T-A-B-I-E-R-E-N** …", `1d19c92e` „… **B-U-C-H-S-T-A-B-E** …", `448a9516` „… **G-R-U-ß** …", `d662cf8b` „… **D-A-N-K-E** …" · ihre Schlüssel: `"fill_blank:· · · · · · · schreiben sie das wort _"`, `"… · · · · · · · · · · · · · …"`, `"… · · · · · · · · · …"`, `"… · · · · …"`, `"… · · · · · …"` · Test-Zusicherung im selben Lauf: `assert.equal(new Set(items.map(taskShape)).size, PRACTICE_SIZE, 'L… repeats a task shape')` — **bestanden** | **Die dritte Diversitätsachse misst zum dritten Mal den Schlüssel und nicht die Aufgabe, und diesmal ist der Preis mit dem Finger nachzuzählen.** Drei Messungen. **(a) Fünf identische Sätze sind fünf „Aufgabenformen".** `bare()` schneidet bei der ersten Formel-Doppelpunktstelle ab, der Rest ist das **buchstabierte Wort**, jeder Buchstabe ein eigenes Token, jedes Token ein `·` — also entscheidet die **Buchstabenzahl** des Lösungsworts über die Gleichheit zweier Aufgaben. Der Lernende füllt in seinem dritten Durchgang durch Lektion 1 fünfmal hintereinander denselben Satz aus; die Kappe meldet 7/7. **(b) Die Klammer ist zur Stellschraube geworden.** Von den 21 neuen Handitems sind **sechs** (`extra-a11-l10-17` bis `-22`) dieselbe Aufgabe — Wortliste → Ja/Nein-Frage, Verb auf Position 1 — und unterscheiden sich in nichts als der Formulierung ihrer Klammer. `TASK_WORDS` und `LEMMA_STOPWORDS` bleiben im Schlüssel, also ergeben „(Das Verb steht zuerst.)" und „(Das Verb steht vorn.)" zwei Formen. Gezogen: L10 Versuch 1 = `l10-08`, `l10-15`, `l10-20`, `l10-19`, `l10-09` — **fünf von sieben** sind „bilden Sie aus den Wörtern die Ja/Nein-Frage"; Versuch 2 = `l10-07`, `l10-14`, `l10-12`, `l10-13`, `l10-16` — wieder fünf. Eine der sechs Klammern ist zudem sachfremd: `extra-a11-l10-20` trägt „**(Die Stimme steigt am Ende.)**" auf einer **geschriebenen** Wortstellungsaufgabe. **(c) Der Rahmen, an dem Runde 9 den Befund festgemacht hat, ist nur für ein Adjektivpaar geschlossen.** `alt`, `gut` und `neu` stehen in `LEMMA_STOPWORDS`, `teuer` und `zwölf` nicht — also kollabieren „___ Uhr ist **alt**." und „___ Stuhl ist **alt**." (das Paar, das der neue Unit-Test als Gleichheit pinnt), aber „___ Rucksack ist **teuer**.", „___ Regal ist **alt**.", „___ Rucksack ist **gut**." und „___ Preis ist **zwölf** Euro." sind vier Schlüssel für **einen** Rahmen. Gemessen mit einem Schlüssel ohne Klammer und ohne Wortlänge: L4 zieht in Versuch 1 und 2 je **3/7** desselben Rahmens, L5 in Versuch 3 **4/7**, L1 in Versuch 3 **5/7** — gegenüber der gemeldeten Vollständigkeit in **allen 36** Blöcken. | Der Schlüssel darf nicht mehr wissen, **wie viele** Inhaltswörter ein Rahmen trägt und **wie** seine Anweisung formuliert ist. Drei Zeilen in `taskShape`: (1) die Klammeranweisung **vor** der Maskierung entfernen und durch **eine** normalisierte Aufgabenmarke ersetzen — `const instruction = TASK_MARKS.find((m) => m.re.test(q))?.key ?? ''` mit `TASK_MARKS` = {`gender` (der/die/das?), `article` (bestimmter/unbestimmter Artikel), `verbFirst` (Verb zuerst/vorn/Position 1/Stimme steigt), `polite`, `time`} —, sodass sechs Umschreibungen **eine** Marke sind; (2) Läufe von `·` zu **einem** `·` zusammenziehen (`.replace(/(?:· )+·/g, '·')`), sodass die Wortzahl nicht mehr trennt; (3) `alt`, `gut`, `neu` aus `LEMMA_STOPWORDS` herausnehmen — sie sind Inhaltswörter und gehören in die Maske, nicht in den Rahmen. Danach die Kappe **auf die Messung ziehen**, wie `tests/curricula.test.mjs:490` es für die Validator-Ratchets erzwingt. Dazu zwei Tests, beide als Klasse: **erstens** in `tests/lesson-engine.test.mjs` eine Fixture mit den fünf L1-Buchstabieritems und die Zusicherung `new Set(five.map(taskShape)).size === 1` sowie dieselbe für die sechs `l10-17…22` — **ein Guard, der seine eigene Gegenprobe nicht besteht, ist keine Regel**, und diese Gegenprobe ist nicht erfunden, sondern gezogen; **zweitens** über **alle 36 Blöcke** (nicht 24) `größte Rahmengruppe ≤ MAX_SAME_TASK_SHAPE`, mit einer Fehlermeldung, die die Items auflistet. | MAJOR |
| `tests/lesson-engine.test.mjs:294` (`MAX_CAP_STARVED_LEKTIONEN = 0`), `:297` (`planPractice(…, 1)`), `:335` und `:417` (`for (const attempt of [1, 2])`), `:480–500` (`for (const [a, b] of [[0, 1], [1, 2]])`) ↔ `ATTEMPT_CYCLE = 3` in `src/lib/lesson/buildLesson.js:110` ↔ `scripts/validate-curriculum.mjs:802` | Doku über dem Ratchet: „BACK TO 0 later in round 10, by content and not by a looser cap … **It may never be raised.**" · gemessen über `practiceReport()`, Versuch 3: **L1** `relaxUsed=3`, `lesen ×5`, `buchstaben ×5` · **L4** `relaxUsed=3`, `kostet ×5`, `euro ×4` · **L5** `relaxUsed=2` · **L6** `relaxUsed=2`, `artikel ×7`, `unbestimmter ×5`, Antwortschlüssel `eine ×3` (Kappe 2) · **L8** `relaxUsed=3` · L6 Versuch 3 im Wortlaut: „___ Buch ist rot. (bestimmter Artikel)", „Das ist ___ Computer. (unbestimmter Artikel)", „___ Computer ist neu. (bestimmter Artikel)", „Hier ist ___ Kollegin. (unbestimmter Artikel)", „Das ist ___ Firma in Berlin. (unbestimmter Artikel)", „Er hat ___ Handy. (unbestimmter Artikel)", „Wir machen ___ Pause. (unbestimmter Artikel)" · Überschneidung **3∩1**: L8 = **5**, L1 = 1, L4 = 1 | **Die Runde hat den Zyklus auf drei Versuche verlängert und die Wächter bei zwei stehen lassen.** Vier Messungen. **(a) Die Mädchen-Regel (`MAX_SAME_LEMMA = 2`, seit Review #2) wird auf Versuch 1 geprüft**, und nur dort ist sie wahr. In Versuch 3 bricht sie in **vier** Lektionen, und der Ratchet, der das melden soll, liest denselben Plan 1. **(b) Die Antwortschlüssel- und die Formkappe laufen über `[1, 2]`.** L6 Versuch 3 produziert dreimal `eine` — der „mein"-Regel-Test, der genau das verbieten soll, sieht diesen Block nie. **(c) Der Preis ist inhaltlich und trifft die drei Lektionen, deren Grammatik der Artikel IST.** L6 Versuch 3 ist **siebenmal dieselbe Schablone**; ein Lernender, der Lektion 6 zum dritten Mal macht, füllt sieben Lücken mit vier verschiedenen Wörtern (`ein`, `eine`, `Das`, `Der`) und liest siebenmal dieselbe Klammer. L4 Versuch 3 sagt fünfmal `kostet` — genau der Befund, den der Commit-Text dieser Runde mit den drei preislosen Neuitems `l04-19/20/21` als geschlossen meldet („L4 now fills seven with every cap honoured (`relaxUsed` 0)"): **das gilt für Versuch 1**, und in Versuch 3 wird keines der drei gezogen. **(d) Der Zyklus wrappt, der Überschneidungstest nicht.** `THE SECOND DRAW IS A SECOND DRAW` prüft die Paare 1→2 und 2→3 gegen `MAX_SHARED = 2`; das Paar **3→1** ist im Zyklus 1→2→3→1 genauso aufeinanderfolgend und wird nicht geprüft — L8 teilt dort **fünf von sieben**. Die Ursache ist in allen vier Fällen dieselbe und steht in `pickPracticeItems`: die Leiter `LADDER` (Z. 500) gibt auf Stufe 3 die **Vorversuchssperre** und auf Stufe 2 die Lemma- und Antwortschlüsselkappe auf — richtig so, wenn die Scheibe keine legale Sieben hergibt, nur sieht es niemand, weil kein Test über Versuch 3 läuft. | Die Wächter an `ATTEMPT_CYCLE` binden statt an eine Literalliste, und zwar **maschinell**: in `tests/lesson-engine.test.mjs` ein gemeinsames `const ATTEMPTS = Array.from({ length: ATTEMPT_CYCLE }, (_, i) => i + 1);` und `[1, 2]` **überall** dagegen ersetzen (Zeilen 335, 417, 445, 627) — dazu ein eigener Test, der die Datei liest und `for (const attempt of [1, 2])` **verbietet**, weil genau diese Zeile den Befund erzeugt hat. `MAX_CAP_STARVED_LEKTIONEN` über **alle** Versuche zählen, auf die dann gemessene Zahl ziehen und mit derselben Begründung wie heute versehen („nur durch Items zu senken, nie durch eine weichere Kappe") — die Reparatur sind Items auf `alphabet-pronunciation`, `nouns-gender` und `indefinite-articles`, die **nicht** die Schablone ihres Slugs tragen. Den Überschneidungstest über **alle** zyklisch aufeinanderfolgenden Paare laufen lassen: `[[0,1],[1,2],[2,0]]`, mit demselben `MAX_SHARED = 2` und der Ausnahme, die schon dokumentiert ist (`mustCover`-Schlüssel zählen nicht mit, weil die Lektion sie jedes Mal üben MUSS) — dann ist L8s 5 ein Befund über drei Items und nicht über fünf. Schließlich `scripts/validate-curriculum.mjs:802` (`drawnAssignment`) ebenfalls auf `ATTEMPT_CYCLE` umstellen, damit RULE 11b die Blöcke misst, die es zu filtern behauptet. | MAJOR |
| `tests/rule-card-overrides.test.mjs:1240` (`levelDeferralOffenders`) ↔ `src/data/curricula/a11.js` `FUNCTION_WORDS` (159 Einträge) ↔ `scripts/validate-curriculum.mjs:822–836` (`taughtUpTo`) | Der Carve-out: `if (!/\s/.test(form) && functionWords.has(form.toLowerCase())) continue;` mit der Begründung „A FUNCTION_WORD is one a learner may MEET before it is taught (CONTRACT §2)" · nachgebaut und beschossen — **gefangen**: `hätte`, `gegangen`, `repariert`, `verhandelt`, `unterschreibt`, `gewöhnlich`, `zuverlässig`, `geschwommen`, `wäre`, `müsste`, `dürfte`, `ich fahre, du fährst, er fährt.` (L7), `Die Satzklammer hält den Satz zusammen.` (L10), `Ich möchte einen Kaffee.` (L3) · **nicht gefangen**, je in eine echte Karte eingesetzt: `ein und eine stehen bei einer Sache.` (L5 → `lexis=[] defer=0`), `Man sagt ein Stuhl und eine Lampe.` (L5 → 0/0), `Wir haben kein Buch.` (L5 → 0/0), `Stehen Sie um sechs auf?` (L10 → 0/0), `Ich kaufe am Freitag ein.` (L10 → 0/0), `Das ist mein Buch.` (L3 → nur `buch`) · `FUNCTION_WORDS` enthält `meiner`, `meinem`, `meinen`, `deinen`, `ihren`, `einen`, `einem`, `dem`, `den` | **Der fünfte Guard, den Runde 9 verlangt hat, ist gebaut — und nimmt genau die Wortklasse aus, in der die Verschiebungen dieses Kurses liegen.** Drei Messungen. **(a) Zwei der drei Fälle, aus denen der Guard entstanden ist, gehen weiter durch.** Runde 9 hat wörtlich geschrieben, der neue Guard müsse „L5 (`ein/eine`, L6), L10 (Satzklammer, L11) und L7 (`fahren`, L10) **alle drei** gefangen" haben. Injiziert und gemessen: **L7 wird gefangen** (dreimal, weil `fährt` ein Inhaltsverb ist), **L5 und L10 nicht** — weder die Regelsatzzeile über `ein/eine` noch die Satzklammer als **Beispielsatz**. Der Mutationstest, der den L10-Fall pinnt, injiziert das **Wort** „Satzklammer" (ein `META_NOUN`); er prüft nie, ob eine Karte die **Konstruktion** zeigen darf. **(b) Dieselbe Ausnahme steckt im neuen `minLektion`-Filter.** `taughtUpTo` speist sich aus Wortfeld, Notice und `seedVocabulary` — und `seedVocabulary` enthält `FUNCTION_WORDS`, also **alle** kasusflektierten Determinative ab Lektion 1. Gemessen an der echten Ziehung: `extra-a11-l03-12` („Der Mann **von meiner** Schwester kommt aus Marokko.") und `extra-a11-l03-15` („Das Kind **von meiner** Schwester ist ein Baby.") — beide **neu in dieser Runde** — tragen `minLektion: 3` und werden in L3 gezogen, obwohl der Kurs den Possessivartikel erst in L12 und den Dativ gar nicht lehrt („Sätze wie *auf dem Tisch* (Dativ) folgen in A1.2", L5-notice). Ebenso `extra-a11-l10-06/07` („___ du morgen **mit dem** Bus?", „… **mit dem** Auto …") in L10, wo `mit` + Dativ erst die A1.2-Karte `dative-prepositions-intro` einführt. `RULE 11b = 0` ist damit **wahr im Buchstaben und blind im Kern**: die Regel misst Wörter und der Kurs verschiebt Formen. **(c) Es ist dieselbe Befundklasse wie MAJOR 1 und 2**, an einer dritten Regel: eine Prüfung, die dort misst, wo sie leicht zu messen ist. Das Material von heute ist sauber — L5 nennt `ein/eine` nicht, L10 zeigt keine Satzklammer —, aber der Guard, der das halten soll, hält es nicht. | `FUNCTION_WORDS` ist **keine** Liste gleichartiger Wörter, und genau das ist zu trennen. **Erstens** die Liste aufteilen: `FUNCTION_WORDS_FREE` (Konjunktionen, Fragewörter, `ja`/`nein`/`auch`/`nicht`, die Nominativformen der Artikel — ein Lernender darf sie jederzeit lesen) und `FUNCTION_WORDS_TAUGHT` (jede **kasusflektierte** Determinativform: `einen`, `einem`, `einer`, `dem`, `den`, `des`, `meiner`, `meinem`, `meinen`, `deinen`, `ihren` …). Nur die erste Hälfte bleibt im Carve-out von `levelDeferralOffenders` und in `seedVocabulary`; die zweite wird wie jedes andere Wort an die Lektion gebunden, die sie einführt (`den` L4 als Chunk nach `kaufen`, `einen` L6 nach `brauchen/haben/kaufen`, alles andere `null`). **Zweitens** eine sechste Regel im Kartentest, die die **Konstruktion** statt des Wortes prüft: für jede Karte jedes Levels darf kein Segment auf `CONSTRUCTION_PATTERNS` passen, deren einführende Lektion nach der eigenen liegt — heute genügen drei Muster: Satzklammer (`/\b(kaufe|stehe|rufe|bringe|mache)\b.*\b(ein|auf|an|mit|aus)\b\s*[.?!]/` → L11), unbestimmter Artikel vor Nomen (`/\b(ein|eine|kein|keine)\s+[A-ZÄÖÜ]/` → L6) und Possessivartikel vor Nomen (`/\b(mein|dein|sein|unser|euer)e?\s+[A-ZÄÖÜ]/` → L12). **Drittens** die Gegenprobe in dieselbe Datei, als Fixture und nicht als Prosa: die sechs oben gemessenen Sätze stehen als `DEFERRED_CONSTRUCTION_PROBES` im Test und **müssen** alle gefangen werden. **Viertens** dieselben drei Muster in `minLektionIndex()` verwenden, damit Pool und Karte dieselbe Frage beantworten — dann stempelt `extra-a11-l03-12` auf `12`, nicht auf `3`, und die Ziehung von L3 kommt ohne den Dativ aus. | MAJOR |
| `src/lib/course/localProgress.js:108–135` (`mergeLocalProgress`) ↔ `:81` (`runs`) ↔ `src/services/lessonService.js:74–120` (`logAttempts`, `countCompletedRuns`) ↔ `src/pages/lesson/LessonPlayerPage.jsx:46–92` | `recordLocalLesson` schreibt den Zähler: `const runs = Math.max(0, Number(previous && previous.runs) \|\| (previous && previous.status ? 1 : 0)) + 1;` · `mergeLocalProgress` liest ihn **nicht**: `await completeLesson(userId, { level, lektionId, accuracy: Number(row.accuracy) \|\| 0, status: … }); const attempts = (store.attempts \|\| []).filter((a) => a.lektionId === lektionId); if (attempts.length) await logAttempts(userId, { level: store.level, lektionId }, attempts);` — danach `clearLocalProgress()` · `countCompletedRuns`: „`logAttempts` writes one run's answers in a **single INSERT**, and `created_at` defaults to now() … the number of DISTINCT `created_at` values … **is** the number of completed runs" | **Die Versuchszahl, die diese Runde gebaut hat, wird beim Anmelden auf 1 zurückgesetzt — für jede Lektion, still, und der Zähler, der sie retten würde, liegt daneben.** A1.1 ist das freie Level (`App.jsx`, `LevelSubscriptionGuard` a1.1-Zweig), ein ausgeloggter Besucher kann **alle zwölf** Lektionen beliebig oft spielen, und `recordLocalLesson` zählt jeden Durchgang mit. Beim ersten Rendern mit einem Benutzer führt `mergeLocalProgress` den Stand zusammen: pro Lektion **ein** `completeLesson` und **ein** `logAttempts` mit allen lokal gespeicherten Antworten — also **ein** `created_at`, also **ein** Durchgang, egal ob es drei waren. `clearLocalProgress()` löscht danach den einzigen Ort, an dem `runs` stand. Folge, an der Ableitung nachgerechnet: wer eine Lektion ausgeloggt **zweimal** gemacht hat (gesehen: Versuch 1 und 2), steht nach der Anmeldung auf `attemptFromCompletions(1) = 2` und bekommt **die Sieben, die er gerade hatte, noch einmal**; wer sie dreimal gemacht hat, bekommt Versuch 2 statt Versuch 1. Das ist genau der Zustand, den Runde 9 als MAJOR 1 gemeldet hat („Wer eine Lektion wiederholt, bekommt dieselben sieben Aufgaben"), nur verschoben auf den einen Moment, an dem das Produkt den Lernenden am wenigsten verlieren will. Zwei kleinere Messwerte derselben Ursache: `store.attempts` ist global auf `MAX_ATTEMPTS = 400` gekappt, sodass bei einem langen Gastbesuch die Antworten der frühen Lektionen verdrängt werden und `logAttempts` gar nicht läuft — dann greift der Fallback `runs \|\| (completed ? 1 : 0)`, wieder **1**; und `LessonPlayerPage` liest die Zahl **einmal** beim Betreten (der Effekt hängt an `[preview, user, curriculum.level, lektion.id]`), sodass ein Wiederholen ohne Verlassen der Seite denselben Versuch behält — der Abschlussbildschirm hat den von Runde 9 vorgeschlagenen Knopf „Noch einmal üben" nicht bekommen, und ohne ihn ist der einzige Weg zu Versuch 2 das Verlassen und erneute Betreten der Lektion. | Die Zahl mitnehmen, statt sie neu zu raten. **Erstens** in `mergeLocalProgress` die Durchgänge **einzeln** schreiben: `for (let run = 0; run < Math.max(1, Number(row.runs) \|\| 1); run += 1) await logAttempts(userId, { level: store.level, lektionId }, run === 0 ? attempts : [{ itemId: '__merged__', stage: 'merge', correct: true }]);` — ein Platzhalter-Batch je zusätzlichem Durchgang, weil die Zählung auf `created_at` beruht und nicht auf dem Inhalt; sauberer, aber teurer: eine Spalte `runs` in `lesson_progress` und `countCompletedRuns` liest `max(distinct created_at, lesson_progress.runs)`. **Zweitens** `MAX_ATTEMPTS` von einer globalen auf eine **pro Lektion** gezählte Grenze umstellen, damit kein Zusammenführungsbatch leer ist. **Drittens** den Knopf nachziehen, den Runde 9 verlangt hat: in `RecapStage` „Noch einmal üben", der `setAttempt(a => a + 1)` und `setStageIndex(0)` ruft — die Wiederholung ist nach Standard §3 der Remediationsweg, und sie soll nicht über die Adresszeile gehen. Test, als Klasse und ohne Netz: eine Fixture für den lokalen Speicher (`{ level: 'a1.1', lektionen: { x: { status: 'complete', runs: 3 } }, attempts: [...] }`), ein Fake-`logAttempts`, das seine Aufrufe zählt, und die Zusicherung **`Aufrufe === runs`**; dazu eine Eigenschaftsprüfung über `attemptFromCompletions`: **für jedes n gilt, dass der nach dem Zusammenführen abgeleitete Versuch derselbe ist wie der davor abgeleitete.** Genau dieser Satz ist heute falsch. | MAJOR |

---

## Status der Befunde aus Runde 9

Verifiziert an den Artefakten (gebauter Pool, alle drei Ziehungen, gebaute
`ruleCards.mjs`, die vier gebauten Checkpoints, der Kartenindex, Validator-Ausgabe,
Testlauf, Lint), nicht am Diff.

| # | Runde-9-Befund | Status | Beleg |
|---|---|---|---|
| 1 | `LessonPlayerPage.jsx:46` hält `const [attempt] = useState(1);` ohne Setter — die zweite Ziehung ist nicht erreichbar, und selbst Versuch 2 wiederholt 62 von 84 Items; L8 zieht „halb" und die offizielle Uhrzeit in keinem Versuch | **geschlossen, in der Sache vollständig; ein Rest beim Anmelden** | `attempt` ist abgeleitet: `attemptFromCompletions(completed, ATTEMPT_CYCLE)` = `1 + (n % 3)`, gespeist signiert-ein aus `countCompletedRuns` (verschiedene `created_at` in `lesson_attempts`), signiert-aus aus `localRunCount`. `planPractice` plant jeden Versuch **gegen die Vereinigung der früheren** (`priorByNr`), und `pickPracticeItems` gibt diese Sperre erst auf Leiterstufe 3 auf. Eigene Messung über alle zwölf Lektionen: **1∩2 = 0** in zwölf von zwölf, **2∩3 = 0** in elf von zwölf; die einzige Ausnahme ist L8 mit je **2**, und das sind exakt seine zwei `mustCover`-Items (`extra-a11-l08-03` → `halb`, `2e4e344c` → `vierzehn Uhr dreißig`) — die Deckungspassage überstimmt die Sperre absichtlich und dokumentiert das. Damit ist auch die zweite Hälfte des Befunds erledigt: **„halb" und die offizielle Uhrzeit werden in allen drei Versuchen produziert**, „Viertel vor" (`f12532bf`) und „Viertel nach" ebenfalls. Alle 36 Blöcke füllen sieben, alle mit ≥ 4 echten Drills (`drillsSlug`), Minimum 4, Maximum 7. **Rest:** das Paar 3→1 wird nicht geprüft (MAJOR 2), der Knopf „Noch einmal üben" fehlt und die Zahl überlebt das Anmelden nicht (MAJOR 4). |
| 2 | `taskShape` maskiert nur Wörter ab sechs Buchstaben; die Kappe greift in keinem der 24 Blöcke, L4 zieht 4/7 desselben Rahmens | **am Material verbessert, an der Regel nicht geschlossen** | Die Maske hängt jetzt an der Wortart (`LEMMA_STOPWORDS` + `TASK_WORDS` + Lücke), das Paar aus dem Bericht („___ Uhr ist alt." / „___ Stuhl ist alt.") kollabiert wirklich, und ein Unit-Test pinnt es. Gemessen ist der Rahmen in L4 auch besser geworden: Versuch 1 und 2 stehen auf **3/7** statt 4/7. Aber der Schlüssel trennt weiter nach **Wortzahl** und nach **Klammerformulierung**, und beides ist in dieser Runde neu ausgenutzt worden — fünf buchstabengleiche L1-Items als fünf Formen, sechs neue L10-Items als sechs Formen. Das ist MAJOR 1 oben. |
| 3 | Der Lesen-Teil aller vier Checkpoints ist ein Zeichenkettenvergleich: acht Aussagen stehen buchstabengleich im Text, acht sind dieselbe Zeile mit einem getauschten Wort | **geschlossen, vollständig und besser als verlangt** | `buildLesen` bildet die Aussage jetzt in drei mechanischen Umformungen: 1. → 3. Person mit dem Sprecher als Subjekt (`VERB_3SG`, eine **Tabelle** statt einer Suffixregel, mit `isst`, `schläft`, `lädt`, `liest`, `fährt`), „Mein X" → „Der X von \<Name\>", Subjektpronomen → sein Antezedens im gedruckten Fenster; `REPORT_STOP` verwirft jede Zeile, die ihr Gegenüber anredet, für eine Gruppe spricht oder „bitte" sagt. Eigene Messung über alle **16** Aussagen der vier Papiere: **0** stehen als Teilzeichenkette in ihrem Text, **0** unterscheiden sich von einer Textzeile in genau einem Token. Was dort steht, ist echte Lesearbeit: `cp2-lesen-3` „**Die Lampe** kostet acht Euro." gegen „Das ist eine Lampe. **Sie** kostet acht Euro." (Antezedens), `cp3-lesen-3` „**Ana** steht um sechs Uhr auf." gegen Lenas „**Ich** stehe um sechs Uhr auf." (Falsch, Sprecherwechsel, trennbares Verb korrekt gebildet), `cp4-lesen-4` „Tim duscht und frühstückt jeden Tag." (Falsch). Die acht Falsifikatoren verteilen sich jetzt auf drei Arten (Zahl, Nomen, Sprecher) statt auf eine, das Verhältnis Richtig/Falsch bleibt 2:2 je Papier, und der wörtliche Fallback ist laut `tests/checkpoint.test.mjs` für A1.1 unerreichbar gepinnt. Zusätzlich nachgemessen: **0** Zeilenüberschneidungen Diktat ↔ Vorlesen ↔ Lesetext in allen vier Papieren, **32 von 32** Poolitems verschieden. **Rest, Minor 3:** `cp1-lesen-3` („Der **Student** spricht Englisch und Deutsch." für „der **Bruder**") ist nicht falsch, sondern im Text unerwähnt, und `cp3-lesen-1`/`-4` sind zweimal dieselbe Umformung („Ana hat Durst." / „Ana hat Hunger."). |
| 4 | `politeCaseItem()` liest die Selbstauskunft eines Items nur bei Einwortantworten; drei Items, deren Aufgabe „Schreiben Sie die **höfliche** Frage" lautet, verzeihen das kleine `sie` | **geschlossen, vollständig und als Klasse ohne Id-Liste** | Klausel 3 ist herausgezogen (`const declaresPolite = POLITE_CUE_RE.test(text) && !INFORMAL_VETO_RE.test(text);`) und Klausel 5 lautet `if (declaresPolite && acc.every(carriesPoliteForm)) return true;`. Eigene Messung über den gebauten Pool **und** die 56 getippten Checkpoint-Items: **0** Items, die ihre Aufgabe höflich nennen und trotzdem locker sind; **0** Items, die ohne das Prädikat streng sind (`POLITE_OVERRIDES` ist **leer**, auch für `extra-a11-l12-16`). Nachgeprüft an den drei genannten: `extra-a11-l10-08` → `caseSensitive: true`, kleines `sie` = **wrong**; ebenso das neue `extra-a11-l10-18` („[haben / Sie / eine Fahrkarte] (höflich mit Sie)"). Und die Gegenrichtung hält: `a1.1-cp1-hoeren-3` („Bitte füllen Sie das Formular aus.", Diktat ohne Cue) bleibt **locker**, was richtig ist — ein gehörter Großbuchstabe ist nicht hörbar. Der Test läuft über beide Pooldateien ohne einen einzigen Item-Namen. |
| 5 | Die vier Kartenguards halten die Karten von heute, aber nicht ihre Klasse: 15 von 16 kursfremden Formen gehen durch, der Vorgriffs-Guard feuert auf keine Karte, der Regelsatz ist vom Beispielsatz-Guard ausgenommen | **zur Hälfte geschlossen** | Die **Lexik**-Hälfte ist repariert und belegt es selbst: `INFLECTION_TAIL` löst die Vier-Zeichen-Toleranz ab, `OUT_OF_COURSE_PROBES` steht als Fixture in der Datei, und ein eigener Test verlangt, dass **alle** Sonden gefangen werden („a guard that does not pass its own counter-probe is not a rule"), mit `deinen` als einziger, begründeter Ausnahme. Eigene Injektion, unabhängig nachgebaut: **14 von 14** kursfremden Formen werden gefangen, darunter der ganze Konjunktiv II und das Partizip II. Der **Vorgriffs**-Guard ist von der Wortstellung gelöst und liest jetzt die **fett gesetzten Formen und Titelnomen aller späteren Lektionen** (`laterNoticeForms`) statt eines Regex hinter „kommt/folgt"; injiziert feuert er auf `ich fahre, du fährst, er fährt.` (L7), auf `Die Satzklammer …` (L10) und auf `Ich möchte einen Kaffee.` (L3), und fünf Mutationstests halten ihn. **Rest:** der Carve-out für Funktionswörter lässt die beiden Sätze durch, an denen der Befund hing — MAJOR 3 oben. Die `-chen/-lein`-Zeile der L4-Karte steht weiter (Minor 2). |

**Zusätzlich geprüft und weiterhin in Ordnung:** die **zwölf Schreibaufgaben** (Aufgabentext,
Leitpunkte bzw. Formularfelder und Wortgrenzen zwischen `a11.js` und
`netlify/functions/_shared/writingTasks.mjs` **deckungsgleich**, alle zwölf `taskKey`s in
der Serverbank); `anrede` in allen zwölf Sprechaufträgen `'Sie'`; RULE 14 (Figuren) und
RULE 12 (Kann-Beschreibungen) als harte Regeln auf 0; der Ratchet-Gleichheitstest
(`tests/curricula.test.mjs:490`) über RULE 10/11/13/6b/15/16 — **jede Zahl deckungsgleich**,
RULE 11 von 61 auf **49** gesunken; `a11.audio.js` weiterhin der leere Manifest-Stub, alle
Hörflächen fallen ehrlich auf die Computerstimme zurück (Eigentümeraufgabe, kein Befund);
**RULE 15** (12 Vorgriffe in Diktat/Vorlesen) unverändert und weiterhin nicht material;
**RULE 13** (4 Sprechaufträge ohne Mission) unverändert vertretbar. **Die Erinnerungsmail
duzt weiterhin, und das lasse ich als Eigentümerentscheidung stehen — kein MAJOR**, wie in
Runde 5 bis 9; die Notiz darüber im Kopf von `course-reminder.mjs` fehlt zum sechsten Mal
(Minor 8).

**Zu den 21 neuen Handitems, einzeln gelesen.** Fünfzehn sind gutes, natürliches,
A1-taugliches Deutsch mit richtigem Schlüssel und richtigen `accepted`-Varianten, und
mehrere sind auffallend gut gebaut: `extra-a11-l09-11` („Ana, ___ du Hunger oder Durst?
(haben)" → `Hast`) sitzt mitten im Café-Wortfeld, `extra-a11-l02-14` („Lena und Tim, ___
ihr Studenten? (sein)" → `Seid`) übt die Form, die RULE 6b für L2 als unbelegt führt, und
`extra-a11-l10-21` („[sein / das Auto / neu]" → „Ist das Auto neu?") ist genau die Inversion,
die *Start Deutsch 1* Sprechen Teil 3 verlangt. Sechs sind schwächer, und sie zerfallen in
zwei Gruppen. **Erstens die Zwillinge:** `l03-09`/`l03-15` (beide „___ ist noch jung." → `Es`,
Erklärung „das Baby/das Kind ist neutral, darum es."), `l03-10`/`l03-16` (beide „… und ich
… ___ sprechen Deutsch." → `Wir`) und `l03-11`/`l03-14` (beide „Lena und Tim, … ___ …?" →
`ihr`) sind drei Paare, die sich in einem Inhaltswort unterscheiden — geschrieben, um drei
Ziehungen zu füllen, und der Formwächter sieht sie nicht (MAJOR 1). **Zweitens die
Passform:** `extra-a11-l03-09` beantwortet „**Wie alt** ist das Baby?" mit „Es ist noch
jung." — auf eine Alterfrage ist das keine Antwort, sondern ein Ausweichen, und ich würde
es im Unterricht anstreichen; `extra-a11-l10-20` trägt die Klammer „(Die Stimme steigt am
Ende.)" auf einer geschriebenen Aufgabe; und `l03-12`/`l03-15` bringen mit „von **meiner**
Schwester" den Dativ nach L3 (MAJOR 3). `extra-a11-l04-21` („___ Tische sind alt." → `Die`)
ist inhaltlich gedeckt — die L4-notice sagt „Im Plural haben alle Nomen **die**" — und
`extra-a11-l09-10` („Die Kellnerin ___ Kuchen und Tee.") ist im Café-Kontext hinnehmbar,
wenn auch blass.

---

## Bewertungsübersicht (Runde 10, Runde 9 in Klammern)

| Lektion | Deutsch | Niveau | Dialog | Prüfung | Kohärenz | Σ /25 |
|---|---|---|---|---|---|---|
| 1 Hallo, ich bin … | 4 (4) | 4 (4) | 5 (5) | 4 (4) | **3** (4) | **20** (21) |
| 2 Ich bin Studentin | 4 (4) | 4 (4) | 4 (4) | 4 (4) | **4** (3) | **20** (19) |
| 3 Meine Familie | 4 (4) | 4 (4) | 4 (4) | 4 (4) | **4** (3) | **20** (19) |
| 4 Auf dem Flohmarkt | 4 (4) | 4 (4) | 5 (5) | 4 (4) | 3 (3) | **20** (20) |
| 5 Im Klassenzimmer | 5 (5) | 4 (4) | 5 (5) | 4 (4) | 3 (3) | **21** (21) |
| 6 Der erste Tag im Büro | 5 (5) | 4 (4) | 4 (4) | 4 (4) | **2** (3) | **19** (20) |
| 7 Freizeit und Hobbys | 4 (4) | 4 (4) | 5 (5) | 4 (4) | 4 (4) | **21** (21) |
| 8 Termine und Uhrzeit | 4 (4) | 4 (4) | 5 (5) | **4** (3) | **3** (4) | **20** (20) |
| 9 Im Café | 5 (5) | 4 (4) | 5 (5) | 4 (4) | 4 (4) | **22** (22) |
| 10 Am Bahnhof | 5 (5) | 4 (4) | 5 (5) | **4** (3) | **3** (4) | **21** (21) |
| 11 Mein Tag | 4 (4) | 4 (4) | 4 (4) | 4 (4) | 4 (4) | **20** (20) |
| 12 Feste feiern | 5 (5) | 4 (4) | 5 (5) | 4 (4) | 4 (4) | **22** (22) |

Kursmittel **20,5/25** (Runde 9: 20,5 · Runde 8: 20,4 · Runde 7: 20,3 · Runde 6: 19,9 ·
Runde 5: 19,7 · Runde 4: 19,9 · Runde 3: 20,4 · Runde 2: 19,8 · Runde 1: 17,2). Vier
Lektionen besser, vier schlechter, vier gleich — **und der Mittelwert steht still, zum
ersten Mal in dieser Reihe.**

**Zur Lesart, und sie ist die eigentliche Aussage dieser Runde.** Die Gewinner sind
echte Reparaturen: **L2 (19 → 20)** und **L3 (19 → 20)** bekommen den Kohärenzpunkt
zurück, den Runde 9 der identischen Wiederholung genommen hatte — beide ziehen jetzt drei
Versuche ohne eine einzige Überschneidung, L3 mit acht neuen Pronomenitems, L2 mit zwei
neuen `sein`-Items. **L8 (Prüfung 3 → 4)**, weil die Lektion, die „halb neun (= 8.30!)"
fett setzt, diese Form nun in jedem Versuch produziert, dazu die offizielle Uhrzeit.
**L10 (Prüfung 3 → 4)**, weil `extra-a11-l10-08` und der neue Zwilling `-18` das kleine
`sie` jetzt hart werten — die Form, auf die *Start Deutsch 1* Schreiben Teil 2 schaut.
Die Verlierer sind **keine Verschlechterung des Materials**, sondern die erste Messung des
**dritten** Versuchs: **L1 (21 → 20)** verliert den Kohärenzpunkt an die fünf
buchstabengleichen Sätze in Versuch 3, **L6 (20 → 19)** an die sieben Items derselben
Artikel-Schablone, **L8 (Kohärenz 4 → 3)** an die fünf von sieben, die es zwischen
Versuch 3 und dem folgenden Versuch 1 wiederholt, **L10 (Kohärenz 4 → 3)** an die fünf
Wortlistenaufgaben in einer Sieben. Plus und Minus heben sich exakt auf, und das ist kein
Zufall: **die Runde hat ihre Formvielfalt mit Items bezahlt, die auf den Schlüssel hin
geschrieben wurden.** **Stärkste:** L9 und L12 (je 22). **Schwächste:** L6 (19).

---

## Würde ich meinen Namen jetzt daraufsetzen?

**Auf das Kursmaterial: ja, und auf die Checkpoints zum ersten Mal ohne Vorbehalt. Auf den
Kurs als Ganzes: noch nicht.** Ich habe alle fünf Punkte aus Runde 9 an den Artefakten
nachgeprüft: **vier von fünf vollständig geschlossen**, der fünfte zur Hälfte. Zum dritten
Mal in zehn Durchgängen finde ich **keinen einzigen BLOCKER**, und ich habe die vier
Definitionen wieder einzeln gemessen statt die Abwesenheit zu behaupten — 0
Selbstablehnungen über 1180 Bewertungsflächen, 0 widersprüchliche Urteile bei exakter
Zeichenkettengruppierung, 0 ungelehrte Antwortformen, 0 ungedeckte Prüfungsteile, 0 Items
vor ihrer `minLektion`.

Drei Dinge sind qualitativ passiert. Erstens: **die Wiederholung ist eine Wiederholung
geworden.** Neun Berichte haben „Versuch 2" vermessen, ohne zu fragen, ob ihn jemand
sieht; jetzt leitet die Anwendung die Zahl aus dem ab, was der Lernende getan hat, jeder
Versuch schließt die früheren aus, und die Überschneidung fällt von 62 von 84 auf 0 in
zehn von zwölf Lektionen. Zweitens: **der Lesen-Teil prüft zum ersten Mal Lesen.** Aus
einem Zitat mit einem getauschten Wort ist eine Aussage **über** den Text geworden — 1. →
3. Person, Possessiv → „der X von \<Name\>", Pronomen → Antezedens —, abgesichert durch
eine Konjugationstabelle statt einer Suffixmaschine und durch das Lexikon der Lektion, die
das Papier schließt. Keine der sechzehn Aussagen steht mehr in ihrem eigenen Text.
Drittens: **die Guards haben angefangen, ihre eigenen Gegenproben zu bestehen.** Die
Sondierungsformen stehen als Fixture in der Testdatei, fünf Mutationstests brechen je eine
Karte absichtlich, und der Satz aus Runde 9 — ein Guard, der seine eigene Gegenprobe nicht
besteht, ist keine Regel — ist an einer Stelle maschinell wahr geworden.

**Was offen bleibt, ist dreimal derselbe Satz, und deshalb schreibe ich ihn diesmal als
Regel und nicht als Beobachtung: eine Prüfung, die neben der Entscheidung misst, wird von
dem Material bezahlt, das man schreibt, um sie zu bestehen.** `taskShape` misst die
Buchstabenzahl eines Lösungsworts und die Formulierung einer Klammer, also sind fünf
buchstabengleiche Sätze fünf Aufgabenformen und sechs Umschreibungen derselben Anweisung
sechs — und die sechs Umschreibungen sind **in dieser Runde geschrieben worden**, weil der
Test sieben verschiedene Schlüssel verlangt. Die Kappen und der Überschneidungstest messen
die Versuche 1 und 2, der Zyklus ist drei lang und wrappt, also steht der dritte Versuch
außerhalb jeder Regel und ist prompt der schlechteste. Der Vorgriffs-Guard nimmt
Funktionswörter aus, also stehen ausgerechnet die Verschiebungen des Kurses — Akkusativ,
Dativ, Possessivartikel — außerhalb. Und die Versuchszahl, das Herzstück der Runde, wird
beim Anmelden von einer Zusammenführung vernichtet, die den Zähler danebenliegen lässt.
Keine dieser vier Regeln ist falsch geschrieben; alle vier messen eine Stelle neben der,
an der die Entscheidung fällt — und drei von ihnen sind **in dieser Runde** dorthin
geschrieben worden.

Die Arbeit ist wieder überschaubar und vollständig ausgeschrieben: drei Zeilen in
`taskShape` (Klammer zu einer Marke, `·`-Läufe zusammenziehen, `alt`/`gut`/`neu` aus der
Stoppliste) mit den gezogenen Fünfergruppen als Fixture; eine Konstante `ATTEMPTS` statt
vier Literallisten `[1, 2]`, das Paar 3→1 im Überschneidungstest und ein Ratchet, der alle
36 Blöcke zählt; eine Aufteilung von `FUNCTION_WORDS` in „darf man lesen" und „muss
gelehrt sein", plus drei Konstruktionsmuster, die Karte und `minLektion` gemeinsam lesen;
und ein Zusammenführungsschritt, der `runs` mitnimmt, samt einer Eigenschaftsprüfung, die
verlangt, dass der abgeleitete Versuch das Anmelden überlebt. Das ist wieder etwa ein Tag,
und es ist zum dritten Mal in Folge **keine einzige Änderung am Lektionstext** — mit einer
Ausnahme, um die ich bitte: `extra-a11-l03-09` sollte auf eine Frage antworten, die es
beantwortet.

---

**Minors (acht Zeilen, nicht erfasst):** (1) Die Buchstabenkarte mischt weiter Groß- und
Kleinschreibung in der Namensliste (`be, ce, de` neben `Jot, Vau, Ypsilon, Zett`, dazu
`W we` in `content` gegen „W heißt **We**" in `commonMistakes`), und kein Item prüft Q.
(2) Die `-chen/-lein`-Zeile der L4-Karte („das **Mädchen**, das **Brötchen**") lehrt zum
zweiten Mal eine Endungsgruppe, von der der Kurs kein einziges Wort enthält, und ist per
`GENDER_ENDING_EXAMPLES` erlaubt; die `-heit/-keit/-schaft`-Beispiele sind vertretbar,
diese Zeile sollte ersatzlos fallen. (3) Im Lesen-Teil: `a1.1-cp1-lesen-3` („Der **Student**
spricht Englisch und Deutsch.", Schlüssel *Falsch*) ist im Text nicht widerlegt, sondern
unerwähnt — ein Nomentausch auf einen anderen Referenten ist schwächer als ein
Faktentausch; und `a1.1-cp3-lesen-1`/`-4` sind zweimal dieselbe Umformung mit demselben
Sprecher und demselben Verb („Ana hat Durst." / „Ana hat Hunger."), zwei von vier Items
eines Papiers. (4) RULE 6b zählt weiter zwei Zustände in einer Zahl; von den 26 Formen
kommen nach wie vor fünf in keiner Lektion, keinem Poolitem und keinem Checkpoint vor
(`acht Uhr dreißig`, `schlafen`, `euer`, `unsere`, `eure`) — „vierzehn Uhr dreißig" deckt
die offizielle Uhrzeit **als Form**, aber nicht die fett gesetzte Zeichenkette.
(5) `extra-a11-l12-17` trägt weiter den metasprachlichen Cue „(Adjektiv)" und wird in L12
Versuch 1 gezogen. (6) Der Topic-Slug `numbers` erzeugt im Wiederholungsindex weiter eine
Doppelkarte: `pattern:numbers` ist Zeichen für Zeichen `pattern:verb-sein` („Was sind Sie
von Beruf?"). (7) Zehn gezogene Items tragen als `questionEn` die deutsche Frage statt
einer Übersetzung (`0fe8146d`, `dd1c3d60`, `e0ac0425`, `1ce4f35f`, `404c4a60`, `34957c67`,
`5d184615`, `da9e486f`, `9fd52b13`, `cb35f9d0` — Runde 9 zählte vier, weil sie zwei
Versuche maß); 15 gezogene Einwortantworten akzeptieren nur die große Schreibung, während
andere beide führen; die L2-Musterlösung schreibt „Mein Geburtsdatum ist **3. Mai 1998**",
also eine Ordnungszahl und einen Monatsnamen, zehn Lektionen bevor der Kurs Monate lehrt
und ein Level bevor er Ordnungszahlen lehrt — der Leitpunkt „Ihr Geburtsdatum" ist
prüfungsecht, die Musterlösung sollte Ziffern schreiben. (8)
`tests/lesson-engine.test.mjs:622` nennt die drei Höflichkeitsitems weiter namentlich
(`const POLITE = ['extra-a11-l12-08', …]`); `src/pages/A11PhasePage.jsx` duzt weiter auf
drei Zeilen; die Eigentümerentscheidung zur du-Anrede der Erinnerungsmail ist zum sechsten
Mal nirgends aufgeschrieben; und `tests/lesson-extra-items-a12.test.mjs:453` ist weiter
der einzige übersprungene Test der Suite — korrekt geführt, aber er sollte mit A1.2 wieder
anspringen.

BLOCKER: 0 · MAJOR: 4
