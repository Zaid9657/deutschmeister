
# A1.1 — DaF-Fachreview #4, nach Runde 4 (2026-09-12)

Reviewer: DaF-Lehrkraft (Erwachsenenbildung, Integrationskurs) und Prüferin für
Goethe-Zertifikat A1 *Start Deutsch 1*. Vierter Durchgang. Basis:
`REVIEW-daf-2026-09-12.md` (5 BLOCKER · 45 MAJOR), `REVIEW-daf-2-2026-09-12.md`
(1 BLOCKER · 35 MAJOR · 44 MINOR) und `REVIEW-daf-3-2026-09-12.md`
(3 BLOCKER · 11 MAJOR), angewendet in `origin/main` @ `a8fee42`
(„A1.1 round 4: close DaF review #3 as rules", PR #119).

Gemessen, nicht angenommen:

* `src/data/curricula/a11.js` — alle zwölf Lektionen, jedes Feld (dialog, wortfeld,
  notice, pretest, phonetik, hoeren, sprechen, schreiben, practiceRule, links).
* **Die echte Ziehung**: `planPractice(CURRICULUM_A11, a11.json, 1)` **und Versuch 2**
  importiert und die sieben Items jeder Lektion mit
  `questionDe`/`questionEn`/`options`/`answer`/`accepted` ausgedruckt und einzeln
  bewertet (**168 Items**). Pool: **344** Items, davon **116** handgeschrieben.
* Alle **116** Items in `src/data/lessonPools/a11.extra.json` einzeln gelesen.
* `drillsSlug()` aus `src/data/lessonPools/quality.js` über beide Ziehungen laufen
  lassen und die Zahlen gegen `PRIMARY_MIN = 4` gehalten.
* `checkAnswer` aus `src/lib/lesson/check.js` mit `strict: STRICT_TOPIC.test(topic)`
  auf 27 Items mit je zwei bis fünf plausiblen Lernerantworten nachgerechnet,
  zusätzlich `tagError` für jede verworfene Antwort.
* Die zwölf A1.1-Karten in `netlify/functions/_shared/ruleCards.mjs` (also das, was
  „Erklär mir das" wirklich bekommt), gegen `scripts/rule-card-overrides.mjs` und
  gegen die notices.
* Die zwölf Kursaufgaben in `src/data/writingTasks.js` gegen `schreiben` und gegen
  `netlify/functions/evaluate-writing.mjs`.
* `buildMessage()` in `netlify/functions/course-reminder.mjs`.
* `node scripts/validate-curriculum.mjs` (RULE 10: 35/262 · RULE 11: 15) und die
  Implementierung beider Regeln, nicht nur ihre Ausgabe.
* `src/components/lesson/*.jsx` + `src/pages/lesson/LessonPlayerPage.jsx` — die Kopie,
  die um die Items herum steht.
* `npm test`: **581/581 grün**.

## Urteil in einem Absatz

**Runde 4 hat zum ersten Mal getan, was seit Runde 2 verlangt wird: sie hat die
Befunde als Regeln geschlossen, und die Regeln greifen messbar.** Der teuerste
Blocker ist weg, und zwar nicht per ID-Liste: `REASON.VERB_CUE_ONLY_IN_GLOSS` plus
ein Reparaturlauf im Build zieht die Klammerangabe in den deutschen Prompt — von 220
getippten Lückenitems haben 96 keine Klammer, und **genau eines** davon hat ein
Vollverb als Antwort (das diktierte `heißen`); L7 zieht jetzt „Ich ___ viel.
(arbeiten)". `statementNoTask()` hat den Aussagesatz-Typ entfernt, die Buchstabenkarte
schreibt die Namen orthografisch (Zett, Ypsilon, Jot, Vau, Ix), die Genus-Karte hat
die falsche -er-Regel verloren, die drei nicht trennbaren L11-Items sind durch
abholen/mitkommen/anrufen ersetzt, L8 lehrt Samstag und Sonntag jetzt in einer eigenen
Dialogzeile und L7 verlangt „am Wochenende", die Schreibaufgabe von L11 heißt wieder wie
ihre Lektion — mit einem Test, der das festhält. Im ganzen Pool gibt es 0
du-Imperative. **Und trotzdem unterschreibe ich auch diesmal nicht, weil die drei
schwersten Befunde dieser Runde alle drei aus derselben Quelle stammen wie in Runde 3:
aus dem, was noch niemand gemessen hat.** Erstens bestraft die *freie* Lektion 1 in
jedem Versuch zwei ihrer sieben Items eine richtige Antwort: `extra-a11-l01-04/05`
akzeptieren ausschließlich die Bindestrichschreibung, `checkAnswer` gibt für „HALLO"
`wrong` und `tagError` schreibt **Wortschatz** ins Fehlerprofil — der Lernende hat die
Buchstaben richtig genannt. Zweitens steht in **42** getippten Items der drei
aufeinanderfolgenden Lektionen 4, 5 und 6 der Artikel, den sie hören wollen, nur in der
**englischen** Glosse: „___ Tafel ist grün." / „The blackboard is green." → nur `Die`;
„Eine Tafel ist grün." ist fehlerfreies Deutsch und wird mit `wrong` + Tag **Artikel**
quittiert, „Das ist das Büro." gegen erwartetes `ein` sogar mit Tag **Kasus**. Das ist
Blocker 1 aus Runde 3, eine Wortart weiter — dieselbe Bauart, dieselbe Begründung,
nur diesmal beim Artikel statt beim Verb, und die neue Regel greift nicht, weil sie auf
`verb:` in der Glosse hört. Drittens ist die Groß-/Kleinschreibung im ganzen Kurs
**nicht prüfbar**: `normalizeAnswer` senkt vor dem Vergleich, also ist „ihr Geschenk,
Frau Kaya" für `extra-a11-l12-08` **richtig** — in der Lektion, deren notice „**Ihr**
Büro (Sie, höflich — immer groß!)" schreibt und deren Regelkarte den Fall als häufigsten
Fehler führt. Die Übung sagt Ja zu dem Fehler, den die Erklärung verbietet. Dazu elf
MAJOR, deren gemeinsamer Nenner wieder eine unbeobachtete Fläche ist: sieben der zwölf
Regelkarten sind noch die Karten von vor dem Umbau, vier Lektionen schicken den
Lernenden mit einem Sprechauftrag los, den die Sprechseite nie erhält, und die beiden
neuen Ratchets messen die falsche Datei. Zwei `accepted`-Listen ergänzen, eine
Artikel-Aufgabenformel in 42 Prompts, ein Flag für Groß-/Kleinschreibung in `check.js` —
dann ist das ein Tag, und dann unterschreibe ich.

**Zahlen:** 3 BLOCKER · 11 MAJOR. Minors sind nicht erfasst (fünf Zeilen am Ende).

---

## BLOCKER

| Feld | Zitat | Problem | Fix (pasteable) | Schwere |
|---|---|---|---|---|
| practice, gezogen L1 #3 und #6 in **beiden** Versuchen (`extra-a11-l01-04`, `extra-a11-l01-05`) | `questionDe: 'Buchstabieren Sie den Gruß: Hallo → ___'`, `answer: 'H-A-L-L-O'`, `accepted: ['H-A-L-L-O','h-a-l-l-o']` · `'Buchstabieren Sie das Wort: Tschüss → ___'`, `accepted: ['T-S-C-H-Ü-S-S','t-s-c-h-ü-s-s']` | **Zwei der sieben Items der kostenlosen ersten Lektion bestrafen eine richtige Antwort — dieselbe Klasse, die Runde 2 und Runde 3 als schwerste Altlast benannt haben, diesmal in einem handgeschriebenen Item.** Nachgerechnet mit `checkAnswer`: `H-A-L-L-O` → `correct`, **`HALLO` → `wrong`, `H A L L O` → `wrong`**; `stripPunct` in `check.js` entfernt `.,!?;:"'`, aber **keine Bindestriche**, also bleibt die Trennzeichenwahl des Aufgabenschreibers eine Prüfungsfrage. `tagError` liefert für „HALLO" **Wortschatz** — dem Lernenden wird eine Wortschatzlücke ins Profil geschrieben, weil er die Buchstaben ohne Bindestrich untereinander gesetzt hat. Das ist das erste, was ein Kaufinteressent vom Produkt sieht: die Lektion, die als Einstieg verschenkt wird, in beiden Ziehungen, zweimal. Dass der Dialog `C-H-A-K-I-R-I` modelliert, entschuldigt es nicht — ein Modell ist keine Aufgabenstellung, und das Item sagt nirgends „mit Bindestrichen". Nur diese beiden Items im ganzen Pool haben eine Buchstabenfolge als Antwort; es ist also kein Altbestand, sondern Handarbeit dieser Rebuild-Reihe. | Nicht die beiden IDs, sondern die Vergleichsregel: `check.js` bekommt neben `dictation` ein `spelling`-Flag, das Trennzeichen zwischen Einzelbuchstaben faltet — `const normalizeSpelling = (t) => String(t ?? '').replace(/[‐-―−-]/g, ' ').replace(/(?<=\b[A-Za-zÄÖÜäöüß])\s+(?=[A-Za-zÄÖÜäöüß]\b)/g, '');` — gesetzt für jedes Item, dessen `answer` auf `/^\p{L}([-\s]\p{L})+$/u` passt, plus ein Test in `tests/lesson-engine.test.mjs`, der `HALLO`, `H A L L O` und `H-A-L-L-O` alle drei auf `correct` pinnt. Sofortmaßnahme für die Ziehung: `accepted: ['H-A-L-L-O','h-a-l-l-o','HALLO','Hallo','H A L L O']` bzw. `['T-S-C-H-Ü-S-S','t-s-c-h-ü-s-s','TSCHÜSS','Tschüss']`. | BLOCKER |
| practice, gezogen L4 #1/#3/#5, L5 #1–#7, L6 #1/#3/#5/#6 (u. a. `extra-a11-l05-01`, `extra-a11-l04-06`, `78558634`, `extra-a11-l06-01`) | `'___ Tafel ist grün.'` / `questionEn: 'The blackboard is green.'` → `accepted: ['Die','die']` · `'___ Fenster ist offen.'` / `'The ___ (window) is open.'` → `['Das']` · `'Das ist ___ Büro.'` / `'That is an office.'` → `['ein']` | **Der Artikel, den das Item hören will, steht ausschließlich im englischen `questionEn` — die Bauart von BLOCKER 1 aus Runde 3, eine Wortart weiter, und die neue Regel greift nicht, weil `VERB_CUE_RE` auf `verb:` hört.** Gemessen: **30** getippte Items, deren Antwort ein blanker bestimmter Artikel ist, und **12** mit blankem `ein/eine/einen`, alle ohne Klammerhinweis im deutschen Prompt — zusammen **42**, verteilt auf die drei aufeinanderfolgenden Lektionen 4, 5 und 6, also auf ihre **primären** Übungsreihen. „Eine Tafel ist grün.", „Ein Fenster ist offen.", „Das ist das Büro." sind fehlerfreie deutsche Sätze aus dem Wortfeld derselben Lektion. Mit `checkAnswer` nachgerechnet: `Eine` gegen `['Die','die']` → `wrong`, `tagError` → **Artikel**; `Ein` gegen `['Das']` → `wrong`, Tag **Artikel**; `das` gegen `['ein']` → `wrong`, Tag **Kasus** — ein Kasusfehler, wo keiner passiert ist, und zwar bei `STRICT_TOPIC`, also ohne Tippfehlertoleranz. Der Lernende sieht die englische Glosse zwar (PracticeItem.jsx:101 rendert `questionEn` unter dem Prompt) — aber das ist genau die Entschuldigung, die Runde 3 für das Verb nicht gelten ließ, und der Kopfsatz von `quality.js` lautet „THE ANSWER MUST FOLLOW FROM THE GERMAN PROMPT". Für einen Integrationskurs-Teilnehmer ohne Englisch ist die Aufgabe unlösbar; die Generat-Items der gleichen Topics lösen es richtig und bieten `['Die','Der','Das']` als Chips an. | Regel, nicht IDs. In `scripts/build-lesson-pool.mjs` ein Reparaturlauf wie der für `VERB_CUE_ONLY_IN_GLOSS`: ein getipptes `fill_blank`, dessen `answer` nach Normalisierung in `{der,die,das,den,dem}` bzw. `{ein,eine,einen}` liegt und dessen `questionDe` keine Klammer trägt, bekommt die Aufgabenformel angehängt — `'___ Tafel ist grün. (bestimmter Artikel)'`, `'Das ist ___ Büro. (unbestimmter Artikel)'`. In `quality.js` dazu `REASON.ARTICLE_CUE_ONLY_IN_GLOSS` mit `articleCueOnlyInGloss(item)` (gleiche Struktur wie `verbCueOnlyInGloss`, Prädikat auf der Antwort statt auf der Glosse) und ein Test in `tests/lesson-pool-rules.test.mjs`, der über den **ganzen** Pool läuft und 0 verlangt. | BLOCKER |
| `src/lib/lesson/check.js` ↔ L12 notice ↔ `extra-a11-l12-08/09/10/16`, Regelkarte `possessive-articles` | notice L12: `'**Ihr** Büro (Sie, höflich — immer groß!)'` · `extra-a11-l12-08`: `'Ist das ___ Geschenk, Frau Kaya? (Sie)'` → `accepted: ['Ihr']`, `explanationDe: 'Höflich: Ihr Geschenk — immer mit großem I.'` · Karte `commonMistakes[2]`: `wrong: 'Wie ist ihr Name?'` → `correct: 'Wie ist Ihr Name?'` | **Der Kurs lehrt eine Groß-/Kleinschreibungsregel, prüft sie in vier Items und einer Regelkarte — und die Engine kann sie nicht sehen.** `checkAnswer` normalisiert über `normalizeAnswer` auf Kleinschreibung, bevor verglichen wird; `strict` schaltet nur die Levenshtein-Toleranz ab. Nachgerechnet: `'ihr'` gegen `['Ihr']` → **`correct`**; `'ihr papa kommt auch.'` gegen `['Ihr Papa kommt auch.']` → **`correct`**; und weil es keine Wortart-Ausnahme gibt, auch `'die schere ist hier.'` → `correct` und `'ich bin lehrer.'` → `correct`. Der Lernende schreibt „ihr Geschenk, Frau Kaya" und bekommt ein grünes Häkchen mit der Erläuterung „immer mit großem I" darunter — die Übung widerspricht ihrer eigenen Erklärung, also genau die Konstruktion, die in Runde 3 BLOCKER 3 war (Karte gegen Item), nur in die andere Richtung. Für eine Prüfungsvorbereitung ist das nicht nur inkonsistent: Groß-/Kleinschreibung ist im Schreiben Teil 2 von *Start Deutsch 1* Bewertungssubstanz, und der Kurs trainiert sie dem Lernenden systematisch ab. Betroffen sind **vier** handgeschriebene L12-Items (08, 09, 10, 16), die Regelkarte und jede Substantivgroßschreibung in allen 47 Satzantworten des Pools. | `check.js` bekommt eine dritte Option: `checkAnswer(user, expected, { strict, dictation, caseSensitive })`, die den Vergleich vor `normalizeAnswer` zusätzlich auf Gleichheit der Groß-/Kleinschreibung prüft und bei Abweichung `RESULT.WRONG` mit `expected` zurückgibt. Gesetzt wird sie dort, wo die Schreibung die Aufgabe **ist**: in `PracticeItem.jsx` für jedes Item, dessen `accepted` zwei Formen enthält, die sich **nur** in der Groß-/Kleinschreibung unterscheiden — und für `possessive-articles`-Items, deren Antwort auf `/^(Ihr\|Ihre)$/` passt, wird die klein geschriebene Variante aus `accepted` **entfernt** (`extra-a11-l12-08/09`: `accepted: ['Ihr']` bleibt, aber `caseSensitive: true`). Dazu `tagError` um `'Rechtschreibung'` für reine Fallfehler und ein Test in `tests/lesson-engine.test.mjs`, der `'ihr'` gegen `['Ihr']` auf `wrong` pinnt. | BLOCKER |

---

## MAJOR

| Feld | Zitat | Problem | Fix (pasteable) | Schwere |
|---|---|---|---|---|
| `netlify/functions/_shared/ruleCards.mjs`, sieben der zwölf A1.1-Slugs | `personal-pronouns`: `'German \| English \| When to use'`, `commonMistakes[0].wrong: 'Using du with your teacher'` · `definite-articles`: `'Gender \| Singular \| Plural \| Example …'`, `commonMistakes: []` · `verb-haben`: `'Du hast Recht. (You are right.)'`, `'Ihr habt Glück!'` | **Vier Karten wurden überschrieben, acht nicht — und sieben davon sind noch die halb englischen Tabellen von vor dem Umbau.** Ausgelesen aus der gebauten `ruleCards.mjs`, also aus dem, was `explain-answer.mjs` als „Grundlage — die Regel dieses Kurses" in den System-Prompt legt: `definite-articles`, `personal-pronouns`, `verb-sein`, `verb-haben`, `indefinite-articles`, `present-tense-regular`, `possessive-articles` tragen englische Spaltenköpfe und englische Glossen; `definite-articles` hat **keinen einzigen** `commonMistakes`-Eintrag, also fällt die Hälfte der Karte für die Lektion 5 aus. Schlimmer als das Englisch ist der Inhalt: die Karten reden über einen anderen Kurs. `definite-articles` lehrt Pluralbildung (`der Tisch → die Tische`, `das Buch → die Bücher`), die A1.1 nirgends lehrt und deren notice ausdrücklich nur „im Plural immer **die**" sagt; `verb-haben` übt an `Recht`, `Glück`, `Auto`, `Hund`, während Lektion 9 im Café sitzt; `verb-sein` an `müde`, `lustig`, `groß`; `indefinite-articles` an `Katze`. Die Stilregel steht im Kopf von `rule-card-overrides.mjs` selbst („Controlled A1 German, one short English support line per card") — sie gilt für vier von zwölf Karten. | Die restlichen acht Karten in `RULE_CARD_OVERRIDES` nachziehen, mit der Lexik der jeweiligen Lektion (L5-Karte an Tafel/Heft/Schere, L9 an Kaffee/Hunger/Durst), jede mit drei `commonMistakes` in deutschen Sätzen. Und die Regel, die verhindert, dass die nächste Runde wieder nur die auffälligen ersetzt: ein Test in `tests/rule-card-overrides.test.mjs`, der für die zwölf A1.1-Slugs (a) `commonMistakes.length >= 2`, (b) `hasEnglish(card.content)` aus `quality.js` nur für die eine erlaubte `English:`-Zeile, und (c) jedes Beispielnomen der Karte gegen das kumulierte Wortfeld aus `validate-curriculum.mjs` prüft. | MAJOR |
| `src/components/lesson/PretestStage.jsx:36/53` gegen `a11.js` `pretest.promptDe` | Bildschirm „Schritt 1 · Erst probieren", Überschrift: `'Wie heißen Sie? Antworten Sie mit einem Satz.'` — Eingabefeld direkt darunter: `placeholder="Schreib einfach, was du kannst."` | **Zwei Anreden auf demselben Bildschirm, zwei Zeilen auseinander, in der kostenlosen Lektion 1.** Das ist der sichtbare Rest von Befund C aus Runde 2: der Pool ist normalisiert (gemessen: **68** Sie-Aufgabenformeln, **0** du-Imperative), die Kopie ist es nicht — und die Trennlinie läuft mitten durch `a11.js`. Alle zwölf `pretest.promptDe`, alle zwölf `schreiben.taskDe` und alle zwölf `sprechen.open.promptDe` siezen; alle `notice.bodyDe` duzen („Beim Buchstabieren sagst **du** jeden Buchstaben einzeln.", „**Lerne** es immer mit", „Antworten kannst **du** zuerst mit hier und da"), und die vier handgeschriebenen Regelkarten duzen mit („**Antworte** mit einem ganzen Satz", „**Lerne** jedes Nomen mit dem Artikel"), ebenso die ganze Chrome (`WortfeldStage`: „**Tippe** auf eine Karte", `ReadAloudLine`: „**Dein** Browser", `course-reminder`: „**du** lernst Deutsch"). Für erwachsene Prüfungskandidaten ist die Anrede kein Detail: sie ist in Sprechen Teil 3 und Schreiben Teil 2 selbst Prüfungsgegenstand, und ein Kurs, der auf einem Bildschirm beides tut, lehrt sie nicht. | Eine Entscheidung, einmal aufgeschrieben, dann erzwungen. Vorschlag, weil der Kurs eine Prüfungsvorbereitung für Erwachsene ist: **Aufgaben und Bedienung siezen, nur der Dialog duzt** (dort sprechen Lena und Tim miteinander). Konkret: `PretestStage.jsx:53` → `placeholder="Schreiben Sie einfach, was Sie können."`; `WortfeldStage.jsx:37` → `"Tippen Sie auf eine Karte für die Übersetzung, auf den Lautsprecher zum Hören."`; `ReadAloudLine.jsx:33–36` und `GradedWriting.jsx:89` auf Sie; die zwölf `notice.bodyDe` und die vier Karten auf die unpersönliche Form („Beim Buchstabieren sagt man jeden Buchstaben einzeln."). Gehalten wird es von einem Test in `tests/curricula.test.mjs`, der `notice.bodyDe`, `pretest.promptDe`, `schreiben.taskDe` und `sprechen.open.promptDe` gegen `/\b(du\|dir\|dein\w*\|Schreib\|Lerne\|Tippe\|Antworte)\b/` prüft. | MAJOR |
| `src/data/lessonPools/a11.json`, 17 Items (gezogen L1 #5 V1, L1 #1/#4/#7 V2, L2 #6 V1+V2) | `'Buchstabiert: E-N-T-S-C-H-U-L-D-I-G-U-N-G. Schreiben Sie das Wort: ___'` · `'Buchstabiert: F-O-R-M-U-L-A-R. Schreiben Sie das Wort mit Artikel: ___'` | **Der Register-Fix aus Runde 3 ist zu 39 von 40 ausgeführt; der Rest ist ausgerechnet der, den die Review namentlich genannt hat.** Die Liste lautete „`Schreib → Schreiben Sie` … und `'Buchstabiert:'` → `'Hören Sie:'`" — die sieben Verben sind normalisiert, `Buchstabiert:` steht noch **17-mal** im ausgelieferten Pool, in vier der sieben gezogenen L1-Items von Versuch 2. Und es ist nicht nur Register: es ist sachlich falsch. Im Player wird nichts gesprochen — `PracticeItem.jsx` rendert Text, kein Audio —, der Lernende **liest** die Buchstabenfolge. „Buchstabiert:" behauptet einen Hörvorgang, den es nicht gibt, in derselben Zeile, in der „Schreiben Sie das Wort" siezt und die Buchstabenfolge schon dasteht. | Da nichts gehört wird, nicht `Hören Sie:`, sondern die Wahrheit: in `scripts/build-lesson-pool.mjs` im Normalisierungslauf `questionDe.replace(/^Buchstabiert:\s*/, 'Lesen Sie die Buchstaben: ')`. Danach `node scripts/build-lesson-pool.mjs a1.1` und in `tests/lesson-pool-rules.test.mjs` ein Test, der `/^Buchstabiert:/` im ganzen Pool auf 0 pinnt — dieselbe Form wie der Test, der die du-Imperative auf 0 hält. | MAJOR |
| `a11.js` `sprechen.open.missionOrder` für L7, L10, L11, L12 gegen `src/components/lesson/SpeakingStage.jsx:35/85` | `missionOrder: null` · daneben auf dem Bildschirm: `promptDe: 'Fragen und antworten Sie zum Thema Freizeit: Hobby? Musik? Wochenende?'` und `'Der Sprach-Coach hört zu und gibt dir automatisch eine Rückmeldung.'` | **Vier Lektionen zeigen einen Sprechauftrag, versprechen dazu eine automatische Rückmeldung und schicken den Lernenden dann auf eine Seite, die den Auftrag nie erhält.** `speakingHref` hängt `&mission=` nur an, wenn `missionOrder` gesetzt ist; bei `null` landet der Lernende auf `/speaking?level=a1.1` — der allgemeinen Sprechseite mit irgendeiner Mission des Levels. Der Prompt, den er zwei Zeilen vorher gelesen hat, wird nicht mitgegeben: `saveCourseContext` trägt `level`, `code`, `itemId`, `title`, `returnTo` — nicht `open.promptDe`, nicht `hintWords`, nicht `teil`. Betroffen sind vier von zwölf Lektionen, darunter **die letzten beiden**, und mit L12 („Laden Sie eine Kollegin zu Ihrem Fest ein") die einzige Sprechen-Teil-3-Übung des Kursabschlusses. Das ist die Stelle, an der der Standard (§5, „der Moat: nobody else grades every exam task instantly") und der Bildschirmtext dasselbe behaupten und das Produkt es nicht hält. | Zwei Zeilen und eine Regel. `SpeakingStage.jsx`: `saveCourseContext({ …, openPrompt: open.promptDe, openTeil: open.teil, hintWords: open.hintWords })` und auf der Sprechseite den mitgegebenen Prompt benutzen, wenn keine Mission gesetzt ist. Und in `scripts/validate-curriculum.mjs` eine RULE: `sprechen.open` ohne `missionOrder` ist nur zulässig, wenn der Prompt mitreist — bis dahin ein Ratchet `MAX_MISSIONLESS_LEKTIONEN = 4`, der gesenkt und nie erhöht werden darf, mit der Liste im Report wie bei RULE 10. | MAJOR |
| `wortfeld` L3 und L6 gegen jeden Input derselben Lektion (RULE-10-Ausgabe) | L3: `die Geschwister`, `der Sohn`, `die Tochter`, `der Mann`, `das Baby`, `das Einzelkind` · L6: `der Kollege`, `der Ingenieur`, `die Ingenieurin`, `der Verkäufer` | **Der Ratchet ist erfüllt (35 von 262), aber der Rest liegt nicht verstreut, sondern genau dort, wo die Kann-Beschreibung ihn braucht.** Sechs von 24 Wortfeldeinträgen der Familien-Lektion kommen in keiner Dialogzeile, keiner notice, keiner Schreibaufgabe und keinem Item von L3 vor — und es sind die sechs, die ein erwachsener Integrationskurs-Teilnehmer als erste braucht: Sohn, Tochter, Mann, Geschwister. Die Lektion verspricht „Ich kann über meine Familie sprechen" und trainiert Sprechen Teil 2 mit dem Prompt „Bruder? Schwester? Kinder?"; wer verheiratet ist und Kinder hat, findet die Wörter in der Liste und nirgends im Kurs. In L6 dasselbe im Handlungsfeld „Arbeit und Beruf": der Dialog nennt keinen einzigen Beruf, während vier Berufsbezeichnungen im Wortfeld stehen — `die Verkäuferin` wird nur gerettet, weil ein Item sie zufällig braucht. Die Karteikarten sagen dem Lernenden „gelernt", der Kurs hat sie nie gezeigt. | Zwei Dialogzeilen, kein Umbau. L3 `lines[1]` → `{ speaker:'Ana', de:'Ja. Das sind meine Eltern und meine Geschwister: mein Bruder und meine Schwester.', en:'Yes. Those are my parents and my siblings: my brother and my sister.' }` und `lines[8]` → `{ speaker:'Lena', de:'Und dein Bruder? Hat er einen Sohn oder eine Tochter?', en:'And your brother? Does he have a son or a daughter?' }` (dann `lines[9]` als Antwort „Ja, ein Baby. Woher kommt ihr? — Wir kommen aus Marokko."). L6 `lines[3]` → `{ speaker:'Herr Weber', de:'Mein Kollege ist Ingenieur. Frau Berg ist Verkäuferin.', en:'My colleague is an engineer. Mrs Berg is a saleswoman.' }`. Danach `MAX_UNCOVERED_WORTFELD` auf 25 senken. | MAJOR |
| `canDo` L11 #2 und L2 #5 gegen alle Übungsslots derselben Lektion; gerendert in `astro-site/src/pages/courses/[level].astro:159` | L11: `'Ich kann mit zwei festen Ausdrücken sagen, was ich gestern gemacht habe.'` · L2: `'Ich kann ein einfaches Formular mit meinen Daten ausfüllen.'` | **Zwei Kann-Beschreibungen stehen im öffentlichen 12×6-Raster — also in dem, was der Käufer vor dem Kauf prüft — und werden in ihrer Lektion nirgends geübt.** L11: die Perfekt-Chunks sind der Grund, warum die Lektion in §2.2 des Standards existiert („Gestern: Tagesablauf, Perfekt mit haben — INTRO ONLY"). Gemessen stehen `gemacht`/`gearbeitet` in **einer** Dialogzeile und in der notice; der Pretest fragt nach `aufstehen`, die Schreibaufgabe ist ein Wochenplan-Formular, der Sprechauftrag nennt „aufstehen? arbeiten? einkaufen?", und in **beiden** Ziehungen ist kein einziges der sieben Items ein Perfekt-Item. Der Lernende liest den Chunk einmal und produziert ihn nie. L2: die Lektion sitzt im Handlungsfeld „Ämter und Behörden", ihr Dialog sagt „Bitte füllen Sie das Formular aus" — und ihre Schreibaufgabe ist eine Mitteilung (`register: 'formell'`, Schreiben Teil 2). Das Formular, das die Kann-Beschreibung verspricht, füllt der Lernende in L1, L3, L5, L7, L9 und L11 aus, nur nicht in der Lektion, die es ankündigt. | Für L11 zwei handgeschriebene Items in `a11.extra.json` (Topic `separable-verbs-intro`, damit sie gezogen werden, mit Chunk-Cue im deutschen Prompt): `{ id:'extra-a11-l11-09', questionDe:'Was hast du gestern gemacht? — Ich habe ___. (arbeiten, fester Ausdruck)', answer:'gearbeitet', accepted:['gearbeitet'] }` und `{ id:'extra-a11-l11-10', questionDe:'Bilden Sie den Satz: [ich / haben / gestern / gearbeitet]', answer:'Ich habe gestern gearbeitet.' }`; zusätzlich `pretest.promptDe: 'Sagen Sie, was Sie gestern gemacht haben.'`, `model: 'Ich habe gearbeitet.'`, `accepted: ['Ich habe','Gestern habe ich']`. Für L2 entweder die Kann-Beschreibung streichen oder `schreiben` auf `formular` umstellen (Quelltext wie in L1/L3, `fields: ['Familienname','Vorname','Geburtsdatum','Staatsangehörigkeit','Familienstand']`). Und die Regel: RULE 12 in `scripts/validate-curriculum.mjs` — jede `canDo`-Zeile muss ein Schlüsselwort mit mindestens einem Übungsslot der Lektion teilen (pretest, schreiben, sprechen.open, notice oder einem Item ihrer `extra`-Gruppe); Verstöße als Ratchet mit Offender-Liste. | MAJOR |
| `drillsSlug()` in `src/data/lessonPools/quality.js` gegen `tests/lesson-engine.test.mjs:193` | `'nouns-gender': ({ expected, options, answer }) => expected.some((a) => isOneOf(a, DEF_NOM)) \|\| /^(der\|die\|das)\s/i.test(answer) \|\| …` · `'yes-no-questions': … \|\| (/^\s*_{2,}/.test(q) && /\?/.test(q))` | **Das neue Messinstrument aus Runde 3 ist an drei Stellen zu großzügig, und an zweien hebt es genau die Lektion über `PRIMARY_MIN`, die der Befund treffen sollte.** (a) `nouns-gender` und `definite-articles` sind dasselbe Prädikat mit einem Wort Unterschied (`DEF_NOM` ⊂ `DEF_ALL`) — ein Item kann nicht zeigen, ob es Genus oder Artikelform übt, weil beide dieselbe Form produzieren. (b) `/^(der\|die\|das)\s/i.test(answer)` lässt jedes Korrekturitem zählen, dessen korrigierter Satz mit „Das" **beginnt**: `extra-a11-l04-07` („Korrigieren Sie: „Das ist eine Tisch."" → „Das ist ein Tisch.") zählt als `nouns-gender`, obwohl es `ein/eine` übt — den Slug von Lektion **6**. Ohne dieses Item steht L4 in beiden Ziehungen bei 3 von 7. (c) `yes-no-questions` zählt jede Lücke an Position 1 vor einem Fragezeichen; damit misst L10 in beiden Ziehungen 7/7, obwohl fünf dieser sieben Items die Inversion **vorgeben** und nur eine Verbform abfragen („___ du eine Fahrkarte für morgen? (haben)" → Hast). Die Wortstellung, die der Lektionstitel und `primarySlug` versprechen, produziert der Lernende in höchstens zwei der sieben Items. Der Nebenbefund: `possessive-articles` ist umgekehrt zu streng (`POSSESSIVE_RE` prüft den ganzen String, also zählt „Wir feiern unser Fest." **nicht**) — die Zahl ist in beide Richtungen ungenau. | `DRILLS['nouns-gender']` auf das einschränken, was Genus **zeigt**: nur Items, deren Prompt das Nomen ohne Artikel trägt und deren erwartete Antwort ein blanker Nominativartikel ist (`expected.some((a) => isOneOf(a, DEF_NOM))`), die `answer`-Präfix-Klausel streichen. `DRILLS['yes-no-questions']` um die Vorgabe bereinigen: die Klausel `(/^\s*_{2,}/.test(q) && /\?/.test(q))` streichen, sodass nur zählt, wer die ganze Frage schreibt oder Ja/Nein wählt. `DRILLS['possessive-articles']` um `containsOneOf` ergänzen. Danach fallen L4 und L10 unter `PRIMARY_MIN` — das ist der ehrliche Befund, und `tests/lesson-engine.test.mjs:201` macht ihn sichtbar. | MAJOR |
| L4 `practiceRule` / notice gegen L5 `practiceRule` / notice | L4 zieht `'___ Tasche kostet dreizehn Euro.'` → `Die`, `'___ Rucksack ist teuer.'` → `Der` · L5 zieht `'___ Tafel ist grün.'` → `Die`, `'___ Stift ist blau.'` → `Der` | **Zwei aufeinanderfolgende Lektionen üben dieselbe Handlung in derselben Satzform, und der Lernende kann nicht erkennen, was in Lektion 5 neu ist.** Das ist die inhaltliche Kehrseite von MAJOR 7: `nouns-gender` (L4) und `definite-articles` (L5) sind im Kurs zwei Slugs, in der Übung eine Aufgabe — „setze der/die/das vor das Nomen". Die notices versuchen zu trennen (L4: „Das Genus ist Teil des Wortes — lerne es immer mit"; L5: „Der bestimmte Artikel zeigt: Wir wissen, welche Sache gemeint ist"), aber kein einziges der 14 gezogenen Items prüft diesen Unterschied. Nach der Logik des Standards (§2.1, die Situation trägt die Grammatik) müsste L5 die **Bekanntheit** üben — hier/da, „Wo ist das Wörterbuch?" —, und genau das steht im Dialog und im Pretest und in keinem Item. | L5 vom Artikelsetzen auf das umstellen, was seine eigene notice und sein Pretest lehren: drei handgeschriebene Items, die den bestimmten Artikel im **Kontrast** üben — `{ questionDe: 'Ist das ein Heft? — Ja, und ___ Heft ist grün.', answer: 'das' }`, `{ questionDe: 'Wo ist ___ Wörterbuch? — Hier.', answer: 'das' }`, `{ questionDe: 'Korrigieren Sie: „Ein Schere ist hier."', answer: 'Die Schere ist hier.' }` — und `practiceRule.topics` von L4 auf `['nouns-gender']` verengen, damit die reinen Genus-Items dort bleiben, wo sie hingehören. | MAJOR |
| `scripts/validate-curriculum.mjs`, `wortfeldCoverage()` und `itemLexis()` | `wortfeldCoverage(c, extraItems = loadExtraItems())` — liest `a11.extra.json` · `ITEM_FORMULA_RE = new RegExp(['Bilden Sie den Satz', …, 'die Frage', 'den Satz', 'das Wort', 'die Zahl', 'als Wort', 'richtig', 'normalen Wortfolge'].join('\|'), 'g')` | **Die beiden neuen Ratchets messen nicht die Fläche, die der Lernende sieht — und der Repo-Grundsatz dieser Codebasis ist „measure before you claim".** (a) RULE 10 zählt als „Input" die Dialogzeilen plus die **handgeschriebenen** Items; die Generat-Items des ausgelieferten `a11.json` zählen nicht. Deshalb stehen `das Fenster`, `die Wohnung`, `braun` auf der Offender-Liste, obwohl die Ziehung von L5 „___ Fenster ist offen.", „___ Wohnung ist ruhig." und „___ Tisch ist braun." enthält — die Zahl 35 ist also in beide Richtungen falsch, zu streng bei L5 und (siehe MAJOR 5) zu milde als Gesamturteil. (b) Zwei der 35 Einträge (`die Zahlen 0–10`, `die Zahlen 11–100`) sind Meta-Einträge, die niemals wörtlich vorkommen **können**; der Ratchet trägt also eine Schuld, die nicht tilgbar ist. (c) RULE 11 streicht über `ITEM_FORMULA_RE` nicht nur Aufgabenformeln, sondern die **Inhaltswörter** `richtig`, `die Frage`, `den Satz`, `das Wort`, `die Zahl` aus **jedem** Prompt — deshalb sieht sie `extra-a11-l10-03` („___ Gleis vier **richtig**?") nicht, obwohl `richtig` in keinem Wortfeld des Kurses steht. Eine Ratchet-Zahl, die man nicht nachrechnen kann, ist keine Messung, sondern eine Zusicherung. | (a) `wortfeldCoverage` die gebaute Datei lesen lassen: zweites Argument `poolItems = JSON.parse(readFileSync(new URL('../src/data/lessonPools/a11.json', import.meta.url),'utf8')).items` und die Items der Lektion über `practiceRule.topics` zuordnen. (b) Meta-Einträge auskennzeichnen: `{ de: 'die Zahlen 0–10', meta: true }` und in `coverageForms` `if (w.meta) return new Set();`. (c) `ITEM_FORMULA_RE` auf die vollständigen Formeln beschränken — `'Schreiben Sie die Frage richtig'`, `'Schreiben Sie die Frage in der normalen Wortfolge'`, `'Bilden Sie den Satz'`, `'Bilden Sie die Frage'`, `'Schreiben Sie das Wort'`, `'Schreiben Sie die Zahl als Wort'` — und die nackten Wörter streichen. Danach beide Ratchets neu messen und auf den gemessenen Wert setzen. | MAJOR |
| `accepted`-Listen im handgeschriebenen Pool, quer | `extra-a11-l10-01`: `'___ der Zug nach Österreich?'` → `['Fährt','fährt','Geht','geht','Kommt','kommt']` · `extra-a11-l10-06`: `'___ du morgen mit dem Bus?'` → `['Fährst','fährst','Kommst','kommst']` · L10 `pretest.accepted`: `['Fährt','Geht','Kommt']` | **`accepted` ist der einzige Teil der Datenschicht ohne Regel — und er widerspricht sich innerhalb einer Lektion.** Zwei Items mit demselben Rahmen und demselben Verb lassen unterschiedliche Alternativen zu: `Geht` ist in `-01` richtig und in `-06` falsch (mit `checkAnswer` nachgerechnet: `wrong`, Tag Verbstellung), obwohl der Pretest derselben Lektion `Geht` ausdrücklich akzeptiert. Dasselbe Muster an anderen Stellen: `extra-a11-l08-05` („Es ist ___ acht. (8.15)") nimmt nur `Viertel nach`, `69162961` („Es ist ___. (2:15, umgangssprachlich)") nur `Viertel nach zwei` — die offizielle Form „zwei Uhr fünfzehn", die die eigene Regelkarte als gleichwertig lehrt, ist in beiden `wrong`, was hier durch das Wort „umgangssprachlich" gedeckt ist, im ersten Item aber nicht. Der Fix aus Runde 2 (`Fährt/Geht/Kommt`) wurde für ein Item gemacht und ist deshalb eine Insel geblieben — dieselbe Mechanik wie die ID-Listen, die diese Reihe dreimal gerügt hat. | Die Alternativen aus **einer** Quelle ableiten statt sie pro Item zu tippen: in `scripts/build-lesson-pool.mjs` eine Tabelle `EQUIVALENT_VERBS = { fahren: ['gehen','kommen'], … }` und `EQUIVALENT_TIME = { 'Viertel nach acht': ['acht Uhr fünfzehn'] }`, die beim Bauen auf jedes Item mit passender Antwortform angewandt wird; plus ein Test in `tests/lesson-extra-items.test.mjs`, der für Items mit identischem `answer`-Lemma innerhalb einer Lektion gleiche `accepted`-Mengen verlangt. Sofortmaßnahme: `extra-a11-l10-06` → `accepted: ['Fährst','fährst','Kommst','kommst','Gehst','gehst']`. | MAJOR |
| `extra-a11-l11-01` und `extra-a11-l11-06` gegen das Wortfeld von L10/L11 (gezogen L11 #4 in Versuch 2) | `'Bilden Sie die Frage: [umsteigen / wir / jeden Tag]'` → `'Steigen wir jeden Tag um?'` · `'Ich hole Lena um acht ___. (abholen)'` → `ab` | **Das Ersatzitem für die drei nicht trennbaren Verben ist ein Satz, den niemand sagt, mit einem Verb aus der Vorlektion und einem Stamm, den der Kurs nie zeigt.** `umsteigen` steht im Wortfeld von **L10** (Bahnhof), nicht L11; der Simplex `steigen` steht nirgends (RULE 11 führt ihn als ungelehrtes Token); und „Steigen wir jeden Tag um?" ist keine Äußerung aus dem Handlungsfeld „Alltag organisieren" — im Alltag steigt niemand jeden Tag um und fragt sich das auch nicht. Das Item ist in Versuch 2 gezogen, also nicht theoretisch. Der Befund selbst ist klein; er zählt, weil er zeigt, dass die **Reparaturitems** dieser Runde wieder ohne dieselbe Prüfung geschrieben wurden wie der Altpool — der Satz dieser Reihe, der jetzt zum vierten Mal gilt. | Ein Satz aus dem eigenen Dialog, dessen Verb im eigenen Wortfeld steht: `{ id:'extra-a11-l11-01', questionDe:'Bilden Sie die Frage: [mitkommen / du / am Freitag]', answer:'Kommst du am Freitag mit?', accepted:['Kommst du am Freitag mit?','Kommst du am Freitag mit'], explanationDe:'mitkommen: kommst steht vorn, mit steht am Ende.' }` — die Zeile „Ja, ich kaufe am Freitag ein. Kommst du mit?" steht im Dialog von L11. Und die Regel, die RULE 11 dafür bräuchte: sie läuft schon, sie ist nur ein Ratchet — `MAX_UNTAUGHT_ITEM_TOKENS` von 15 auf 10 senken, sobald `Steigen`, `liegt`, `offen`, `klein`, `schön`, `neu` ersetzt sind. | MAJOR |

---

## Status der Befunde aus Runde 3

Verifiziert an den Artefakten (Pool, Ziehung, gebaute `ruleCards.mjs`, Validator-Ausgabe,
Testlauf), nicht am Diff.

### Die drei BLOCKER

1. **Das Vollverb steht nur in der englischen Glosse (36 Items, 2 in der Ziehung)** —
   **gelandet, und zwar als Regel.** `REASON.VERB_CUE_ONLY_IN_GLOSS` +
   `verbCueOnlyInGloss()` + `parseVerbCue()` in `quality.js`, mit einem Reparaturlauf im
   Build, der die Klammerangabe in den deutschen Prompt zieht. Nachgemessen im
   ausgelieferten `a11.json`: 220 getippte `fill_blank`-Items, davon 96 ohne Klammer im
   deutschen Prompt — und **genau eines** davon hat ein Vollverb als Antwort, das
   Diktatitem `9d61145f` („Buchstabiert: H-E-I-ß-E-N"), wo die Antwort aus den Buchstaben
   folgt. L7 zieht in beiden Versuchen „Ich ___ viel. **(arbeiten)**", L11 „Wir ___ heute
   ein. **(einkaufen)**". Die Klasse ist zu.
2. **Der Aussagesatz, der nichts fragt (`d44e8128`)** — **gelandet.** `statementNoTask()`
   + `TASK_FORMULA_RE` + `QUOTED_SPAN_RE`; `d44e8128` und `160867f8` sind im gebauten Pool
   nicht mehr vorhanden. L7 #2 ist jetzt „Anna ist meine Freundin. ___ ist freundlich." →
   `Sie` — eine echte Lücke mit deutschem Kontext. (Die Namenskollision *Anna* / *Ana*
   lebt in drei Items weiter; siehe Minors.)
3. **Die Buchstabenkarte mit Lautbehelfen** — **gelandet, mit einem Rest.** Die Karte
   liefert jetzt `C ce, J Jot, V Vau, X ix, Y Ypsilon, Z Zett` und
   `commonMistakes[0]: 'Wie heißt der Buchstabe Z? — Zed.' → 'Zett.'`, exakt die
   Schreibungen, die `559f4c15` und `extra-a11-l01-01/02/03` akzeptieren (nachgerechnet:
   `Zett` → `correct`, `tset` → `wrong` — die Karte führt jetzt nicht mehr in die Falle).
   Rest: **`Q ku`** statt `Q qu`; kein Item prüft Q, deshalb Minor und nicht Major.

### Die elf MAJOR

| # | Runde-3-Befund | Status | Beleg |
|---|---|---|---|
| 1 | `nouns-gender`-Karte: „-er bei Personen → der" | **gelandet** | Gebaute `ruleCards.mjs`: „-er bei **Berufen von Männern** → der: der Lehrer, der Verkäufer, der Kellner. **Vorsicht: die Mutter, die Schwester, die Tochter.**"; `der Student` ist weg, `commonMistakes[2]` umgeschrieben. |
| 2 | L3-Possessivitems mit Endung (`extra-a11-l03-04/08`) | **gelandet** | Im ganzen Pool 0 Items der Gruppe `extra-a11-l03-*` mit Topic `possessive-articles`. `-04` ist jetzt „Mein Vater und meine Mutter kommen aus Marokko. ___ sprechen Arabisch." → `Sie`, `-08` „Frau Kaya, sprechen ___ Englisch?" → `Sie`. Beide in der Ziehung, beide gedeckt. |
| 3 | `extra-a11-l03-05` „Du ___ Englisch. (sprechen)" — Vokalwechsel vor L7 | **gelandet** | `-05` ist jetzt „Ana und ich sprechen Deutsch. ___ kommen aus Marokko." → `Wir`. `-06` („Mein Vater ___ Deutsch." → spricht) blieb, wie die Review es zuließ; die notice von L3 nennt weiterhin nur die er-Form mit Verweis auf L7. |
| 4 | „`topic` ist ein Routing-Label" — die 4-von-7-Zahl ist nominal | **gelandet, aber die Messung ist ungenau** | `drillsSlug()` existiert, `tests/lesson-engine.test.mjs:193` misst damit, und in **beiden** Ziehungen liegen alle zwölf Lektionen bei ≥ 4 (L4: 4, L7: 4/5, L12: 5, Rest 5–7). Die Zahlenwahrheit hängt jetzt am Prädikat — und das ist an drei Stellen zu großzügig: siehe MAJOR 7. Die beiden namentlich genannten L2-Zahlwortitems sind ersatzlos ersetzt. |
| 5 | Drei nicht trennbare Verben in der Satzklammer-Lektion | **gelandet** | `frühstücken` und `duschen` kommen im ganzen Pool **null**-mal vor; `extra-a11-l11-06/07/08` sind jetzt abholen / mitkommen / anrufen. L11 misst 6/7 in beiden Ziehungen. |
| 6 | Register: 39 du-Imperative gegen 30 Sie | **halb gelandet** | Gemessen im ausgelieferten Pool: **68 Sie-Formeln, 0 du-Imperative.** Offen: `Buchstabiert:` ×17 (MAJOR 3) und die Chrome/notices/Karten, die durchgehend duzen, während jede Aufgabe siezt (MAJOR 2). |
| 7 | ≈50 Wortfeldeinträge ohne Input, dicht in L2 und L9 | **halb gelandet** | RULE 10 misst jetzt 35/262 und läuft im Validator. L9 ist gelöst (Brot, Tasse, Frühstück, Mineralwasser, Orangensaft, Bier, Kellnerin stehen in Dialogzeilen), L2 über die neuen `leitpunkte` (Geburtsdatum, Staatsangehörigkeit, Familienstand) statt über das vorgeschlagene Formular — lexikalisch gedeckt, die Kann-Beschreibung nicht (MAJOR 6). Neuer Schwerpunkt: L3 und L6 (MAJOR 5). |
| 8 | `Samstag`/`Sonntag` in L8 gelehrt, in L7 verlangt | **gelandet, beidseitig** | L8 `lines[9]`: „Am Mittwoch beim Arzt. **Am Samstag und am Sonntag** habe ich frei!"; L7 `taskDe`/`sample`/`fields` und `extra-a11-l07-02` stehen auf „am Wochenende", mit Begründung im Kommentar von `writingTasks.js`. |
| 9 | `extra-a11-l08-06` „___ Vormittag habe ich Zeit." | **gelandet** | Jetzt „___ **Abend** habe ich Zeit." → `Am`; in beiden Ziehungen von L8 enthalten, durch Wortfeld und Dialogzeile 7 gedeckt. |
| 10 | Schreibaufgabe L11 heißt „Gestern und heute" | **gelandet, mit Test** | `src/data/writingTasks.js` und die byte-identische Kopie: `title: 'Lektion 11: Mein Tag'`; `tests/writing-course.test.mjs:155` pint jeden `a11-l*`-Titel auf `Lektion <nr>: <Lektionstitel>`. |
| 11 | `extra-a11-l10-16` „Korrigieren Sie: „Kommt pünktlich der Bus?"" | **gelandet** | Jetzt „**Schreiben Sie die Frage in der normalen Wortfolge**: „Kommt pünktlich der Bus?"" — der mögliche Satz wird nicht mehr zum Fehler erklärt. |

Zusätzlich geprüft und weiterhin in Ordnung: **`buildMessage()`** in
`netlify/functions/course-reminder.mjs` — A1-Deutsch, keine Behauptung über das
Verhalten des Lernenden, namensabhängige Anrede mit korrekter Groß-/Kleinschreibung des
folgenden Pronomens, Betreff und CTA aus der Lektion abgeleitet. Kein Befund auf dieser
Datei. Ebenso in Ordnung: `evaluate-writing.mjs` (`MIN_CHARS = { formular: 12 }`, sonst
30, aus `task.register` abgeleitet; A1-Feedback-Auflage für `goethe_a1`), die zwölf
Schreibaufgaben (6 Formular mit Quelltext und fünf Feldern, 6 Mitteilungen mit drei
Leitpunkten, Wortfenster 5–40 / 25–45, deckungsgleich mit `a11.js`), und der Testlauf:
**581 von 581 grün**.

---

## Bewertungsübersicht (Runde 4, Runde 3 in Klammern)

| Lektion | Deutsch | Niveau | Dialog | Prüfung | Kohärenz | Σ /25 |
|---|---|---|---|---|---|---|
| 1 Hallo, ich bin … | **4** (3) | 4 (4) | 5 (5) | **3** (4) | 4 (4) | **20** (20) |
| 2 Ich bin Studentin | 4 (4) | 4 (4) | 4 (4) | 4 (4) | 3 (3) | **19** (19) |
| 3 Meine Familie | 4 (4) | **4** (3) | 4 (4) | 4 (4) | **3** (4) | **19** (19) |
| 4 Auf dem Flohmarkt | 4 (4) | 4 (4) | 5 (5) | **3** (4) | **3** (4) | **19** (21) |
| 5 Im Klassenzimmer | 5 (5) | 4 (4) | 5 (5) | **3** (4) | 4 (4) | **21** (22) |
| 6 Der erste Tag im Büro | 5 (5) | 4 (4) | 4 (4) | **3** (4) | **3** (4) | **19** (21) |
| 7 Freizeit und Hobbys | 4 (4) | **4** (2) | 5 (5) | 4 (4) | **4** (3) | **21** (18) |
| 8 Termine und Uhrzeit | 4 (4) | 4 (4) | 5 (5) | 4 (4) | 4 (4) | **21** (21) |
| 9 Im Café | 5 (5) | 4 (4) | 5 (5) | 4 (4) | 4 (4) | **22** (22) |
| 10 Am Bahnhof | 5 (5) | 4 (4) | 5 (5) | **3** (4) | **3** (4) | **20** (22) |
| 11 Mein Tag | 4 (4) | **4** (3) | 4 (4) | **3** (4) | 3 (3) | **18** (18) |
| 12 Feste feiern | 5 (5) | 4 (4) | 5 (5) | **3** (4) | **3** (4) | **20** (22) |

Kursmittel **19,9/25** (Runde 3: 20,4 · Runde 2: 19,8 · Runde 1: 17,2). Zwei Lektionen
besser, sechs schlechter, vier gleich.

**Wichtig zur Lesart des Rückgangs:** in L4, L5, L6 und L12 hat sich in dieser Runde
**nichts geändert** — gemessen wurde etwas, das in Runde 3 niemand gemessen hat (die 42
Artikel-Items und die Fallblindheit des Vergleichs). Der Kurs ist nicht schlechter
geworden; er ist genauer vermessen. Umgekehrt ist der Sprung von **L7 (18 → 21)** echt:
beide Blocker-Items sind weg, `Samstag` wird nicht mehr eine Lektion zu früh verlangt,
und die Ziehung ist durchgehend gedeckt — die Lektion, die in Runde 3 die schlechteste
war, ist jetzt im oberen Drittel. **L3 (Niveau 3 → 4)** ist der zweite saubere Gewinn:
die beiden Vorgriffe in den neu geschriebenen Items sind ersetzt, die Kohärenz fällt
dafür auf 3, weil sechs Familienwörter nur in der Liste stehen.
**Stärkste:** L9 (22) — die einzige Lektion ohne einen Befund dieser Runde.
**Schwächste:** L11 (18) — die Kann-Beschreibung, die der Lektion ihren Platz im
Standard gibt, wird in keinem Item, keiner Aufgabe und keinem Sprechauftrag geübt.

---

## Würde ich meinen Namen jetzt daraufsetzen?

**Noch nicht — aber diesmal aus einem anderen Grund als beim letzten Mal, und das ist
der Fortschritt.** Runde 3 endete mit dem Satz „eine Klasse, die per ID-Liste geschlossen
wird, bleibt offen". Runde 4 hat das verstanden: der teuerste Blocker ist nicht als drei
IDs, sondern als Prädikat, Reparaturlauf, Reason-Code und Pool-weiter Test geschlossen
worden, und die Messung bestätigt es über alle 344 Items. Ebenso der Aussagesatz-Typ,
ebenso die du-Imperative (39 → 0), ebenso die Karte, deren Buchstabennamen jetzt exakt
das sind, was die Items akzeptieren. Das ist zum ersten Mal in dieser Reihe eine Runde,
in der ich einen alten Befund nicht wieder aufmachen muss.

**Was offen bleibt, ist die Fläche, die noch nie jemand angesehen hat.** Alle drei
Blocker dieser Runde liegen dort: in zwei handgeschriebenen Items der freien Lektion, die
seit Runde 2 im Kurs stehen und nie nachgerechnet wurden; in 42 Artikel-Items, die
strukturell identisch mit dem Verb-Blocker sind, aber von seiner Regel nicht erfasst
werden, weil die Regel auf das Wort `verb:` hört statt auf die Form „der deutsche Prompt
bestimmt die Antwort nicht"; und in `check.js`, dem Stück Code, das über jede einzelne
Antwort des Kurses entscheidet und das in vier Reviews noch nie gegen die Inhalte
gehalten wurde, die es prüft. Die Lehre ist deshalb enger als die letzte: **eine Regel,
die eine Klasse an ihrem Vokabular erkennt statt an ihrer Form, schließt eine Instanz und
nicht die Klasse.** `verbCueOnlyInGloss` ist die richtige Regel mit dem falschen
Ansatzpunkt — die Frage lautet nicht „steht `verb:` in der Glosse", sondern „gibt es eine
zweite Antwort, die im deutschen Prompt genauso gut steht"; an dieser Frage fallen das
Verb, der Artikel und alles Weitere gemeinsam. Der zweite Satz, der aus dieser Runde
mitgeht: **was gemessen wird, wird repariert — und was der Validator nicht liest,
existiert für die Reparaturrunde nicht.** RULE 10 und RULE 11 sind genau die Antwort auf
den Schlusssatz von Runde 3 gewesen, und sie lesen `a11.extra.json` statt `a11.json`,
also die Hälfte des Kurses. Vier Lektionen schicken den Lernenden mit einem Auftrag zur
Sprechseite, den sie nicht mitgeben; sieben Regelkarten reden noch über den Kurs von
vorgestern. Beides hätte ein Test gefunden, den es nicht gibt.

Die Arbeit ist überschaubar: drei `accepted`-Listen und ein Trennzeichen-Flag, eine
Aufgabenformel in 42 Prompts (per Reparaturlauf, nicht per Hand), ein
`caseSensitive`-Schalter mit vier Aufrufstellen. Das ist ein Tag. Danach unterschreibe
ich für alle zwölf.

---

**Minors (fünf Zeilen, nicht erfasst):** (1) Die Buchstabenkarte schreibt `Q ku` statt
`Qu` — der einzige Rest der Respelling-Manier; kein Item prüft Q, und die übrigen
25 Namen sind korrekt, aber uneinheitlich groß/klein geschrieben (`be, ce, de` neben
`Jot, Vau, Zett`). (2) **23** Items zeigen den deutschen Prompt ein zweites Mal als
„Glosse" (`0fe8146d`, `dd1c3d60`, `28215564` sind in der Ziehung), drei
Multiple-Choice-Glossen sind halb übersetzt („___ table is brown."). (3) Drei Items
heißen *Anna* neben der Kursfigur *Ana* (eines in der L7-Ziehung), dazu die sonst
namenlosen *Tom*, *Frau Müller* im Generat-Pool. (4) Die notice von L12 nennt `es → sein`
nicht, das gezogene MC-Item `41e1c650` („Das Kind hat ___ Buch. (es)") und die Regelkarte
schon; ungelehrt in Items außerdem `Januar`, `klein`, `schön`, `offen`, `neu`, `liegt`
(RULE-11-Liste). (5) `situation` von L9 trägt eine Redaktionsnotiz („Essen und Trinken:
etwas bestellen **(die Einladung kommt in Lektion 12)**") — gerendert wird das Feld
derzeit nicht, aber es ist ein lernendenseitiges Feld.

BLOCKER: 3 · MAJOR: 11
