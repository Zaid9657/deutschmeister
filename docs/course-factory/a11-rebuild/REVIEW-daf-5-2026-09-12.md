
# A1.1 — DaF-Fachreview #5, nach Runde 5 (2026-09-12)

Reviewer: DaF-Lehrkraft (Erwachsenenbildung, Integrationskurs) und Prüferin für
Goethe-Zertifikat A1 *Start Deutsch 1*. Fünfter Durchgang. Basis:
`REVIEW-daf-2026-09-12.md` (5 BLOCKER · 45 MAJOR), `REVIEW-daf-2-2026-09-12.md`
(1 BLOCKER · 35 MAJOR · 44 MINOR), `REVIEW-daf-3-2026-09-12.md` (3 BLOCKER · 11 MAJOR)
und `REVIEW-daf-4-2026-09-12.md` (3 BLOCKER · 11 MAJOR), angewendet in `origin/main`
@ `7d8b161` („A1.1 round 5: close DaF review #4", PR #120).

Gemessen, nicht angenommen:

* `src/data/curricula/a11.js` — alle zwölf Lektionen, jedes Feld (situation,
  handlungsfeld, canDo, examTeile, wortfeld, dialog, pretest, notice, phonetik,
  hoeren, sprechen, schreiben, links, practiceRule).
* **Die echte Ziehung**: `planPractice(CURRICULUM_A11, a11.json, 1)` **und Versuch 2**
  importiert, alle sieben Items jeder Lektion mit
  `questionDe`/`questionEn`/`options`/`answer`/`accepted`/`explanationDe` ausgedruckt
  und einzeln bewertet (**168 Items**). Pool: **347** Items, davon **119**
  handgeschrieben.
* Alle **119** Items in `src/data/lessonPools/a11.extra.json` einzeln gelesen.
* `drillsSlug()` aus `src/data/lessonPools/quality.js` über beide Ziehungen laufen
  lassen und jede Zahl gegen das gehalten, was der Lernende in dem Item **produziert**.
* `checkAnswer` + `tagError` + `isCaseTask` aus `src/lib/lesson/check.js` auf 20 Items
  mit je zwei bis vier plausiblen Lernerantworten nachgerechnet — mit der NEUEN
  Semantik (`spellingApplies` faltet Trennzeichen, ein reiner Fallfehler ist TYPO,
  bei `caseSensitive`/`POLITE_POSSESSIVE_RE` WRONG).
* Die zwölf A1.1-Karten in der gebauten `netlify/functions/_shared/ruleCards.mjs`
  gegen `scripts/rule-card-overrides.mjs`, gegen die notices und gegen die Items.
* Die zwölf Kursaufgaben in `src/data/writingTasks.js` gegen `schreiben` und gegen
  `netlify/functions/evaluate-writing.mjs`.
* `buildMessage()` in `netlify/functions/course-reminder.mjs`.
* Die Chrome: `src/components/lesson/*.jsx`, `src/pages/lesson/*.jsx`,
  `src/pages/SpeakingPage.jsx` — und der Weg des Sprechauftrags bis in den
  System-Prompt (`speaking-session.mjs`, `speaking-turn.mjs`, `_shared/speakingAI.mjs`).
* **Die Checkpoints wirklich gebaut**: `buildCheckpoint()` für alle vier Checkpoints,
  alle 80 Items ausgedruckt, und `isItemCorrect()` gegen `checkAnswer` in der Lektion
  gehalten. Dazu `ReviewPage.jsx`.
* `node scripts/validate-curriculum.mjs` (RULE 10: 19/262 · RULE 11: 9 · RULE 12: 6 ·
  RULE 13: 4) und die Implementierung aller vier Regeln, nicht nur ihre Ausgabe.
* `npm test`: **619/619 grün**.

## Urteil in einem Absatz

**Runde 5 ist die sauberste Runde dieser Reihe: alle drei Blocker und alle elf MAJOR
aus Runde 4 sind geschlossen, zehn davon als Regel mit Test, und keiner davon muss ich
wieder aufmachen.** `normalizeSpelling` faltet die Trennzeichen, also ist `HALLO` jetzt
so richtig wie `H-A-L-L-O`; `articleCueOnlyInGloss` + der Reparaturlauf haben die
Aufgabenformel in die 42 Artikel-Prompts gezogen, und L5 zieht „___ Tafel ist grün.
**(bestimmter Artikel)**"; `caseSensitive` existiert und die höfliche Form `Ihr` wird in
der Lektion endlich falsch gewertet, wenn sie klein geschrieben wird; alle zwölf
Regelkarten sind neu, deutsch, aus der Lexik ihrer eigenen Lektion und mit drei
Fehlerbeispielen; der Sprechauftrag reist bis in den System-Prompt des Coachs; vier
Ratchets messen jetzt die gebaute Datei statt der Absicht. **Und trotzdem unterschreibe
ich auch diesmal nicht, weil die drei schwersten Befunde dieser Runde zum fünften Mal
aus derselben Quelle stammen: die Regel wurde an der Bauform der zuletzt gefundenen
Instanz festgemacht statt an der Frage, die sie beantworten soll.** Erstens greift
`articleCueOnlyInGloss` nur auf `type === 'fill_blank'` — deshalb leben **drei**
Satzbauitems weiter, bei denen der Artikel erfunden werden muss und nur die englische
Glosse sagt welcher: `dd86dc8a` „Schreiben Sie den Satz: [Honig / ist / gut]" → nur
*Der Honig ist gut.*, obwohl **„Honig ist gut." fehlerfreies Deutsch ist** (Stoffname
ohne Artikel); nachgerechnet `wrong`, Tag **Verbstellung**. Das Item ist in Versuch 2
die erste Aufgabe von Lektion 4, seine beiden Zwillinge (`[Blume / ist / schön]`,
`[Schrank / ist / neu]`) sind **zwei der drei Schreiben-Aufgaben von Checkpoint 2**,
also im benoteten Test. Zweitens bestraft ausgerechnet das Item, das Runde 4 als
Kontrastübung bestellt hat, die Antwort, die seine eigene Aufgabenformel verlangt:
`extra-a11-l05-08` „Ist das ein Heft? — Ja, und ___ ist grün. **(bestimmter Artikel)**"
nimmt nur `das Heft`; wer den bestimmten Artikel einsetzt und `das` schreibt, bekommt
`wrong` und den Tag **Wortschatz**. Drittens entscheidet über die Groß-/Kleinschreibung
jetzt ein Regex auf der Antwortform (`POLITE_POSSESSIVE_RE`) statt die Frage „ist die
Schreibung hier die Aufgabe": er trifft `extra-a11-l12-10` „___ Geschenke sind hier.
**(sie, Plural)**", wo `Ihre` nur satzinitial groß ist und die eigene Erklärung
„sie im Plural → **ihre**" lautet — `ihre` ist dort `wrong`; er verfehlt das höfliche
`Sie`/`Ihnen` in L1 und L3, das dieselbe Regel trainiert; **und er wird nur von
`PracticeItem.jsx` übergeben, nicht von `buildCheckpoint.js` und nicht von
`ReviewPage.jsx` — dasselbe Item ist in Checkpoint 4 mit `ihre` richtig und in der
Lektion falsch.** Dazu elf MAJOR, deren gemeinsamer Nenner wieder eine unbeobachtete
Fläche ist: die Checkpoints hat in fünf Runden niemand gebaut (ihr Lesen-Teil hat in
allen vier Checkpoints denselben Lösungsschlüssel Richtig–Falsch–Richtig–Falsch), RULE 11
liest weiterhin nur `a11.extra.json` und damit die Hälfte des Kurses nicht, und L7 drillt
in beiden Ziehungen zweimal `arbeiten` — ein Verb, das erst in Lektion 11 ins Wortfeld
kommt. Drei `accepted`-Listen, ein Prädikat, das nach der Form statt nach dem Typ fragt,
und zwei Zeilen `caseSensitive: isCaseTask(item)` in Checkpoint und Review — dann ist das
ein halber Tag, und dann unterschreibe ich.

**Zahlen:** 3 BLOCKER · 11 MAJOR. Minors sind nicht erfasst (fünf Zeilen am Ende).

---

## BLOCKER

| Feld | Zitat | Problem | Fix (pasteable) | Schwere |
|---|---|---|---|---|
| `dd86dc8a-49b8-5d48-8a6a-2606fd90b0fd` (gezogen L4 #1, Versuch 2), `670eaadb-ca17-52d3-a62b-220ef80b8828`, `cd6d471e-cd91-5863-b5db-eeb2cbce2f2e` (beide **Checkpoint 2, Schreiben 1 und 3**) ↔ `articleCueOnlyInGloss()` in `quality.js` | `questionDe: 'Schreiben Sie den Satz: [Honig / ist / gut]'` · `questionEn: 'Write the sentence: [honey / is / good] — add the right definite article.'` · `answer: 'Der Honig ist gut.'` — ebenso `[Blume / ist / schön]` → `Die Blume ist schön.` und `[Schrank / ist / neu]` → `Der Schrank ist neu.` | **Die Klasse von BLOCKER 2 aus Runde 4, eine Itemform weiter — und die neue Regel greift nicht, weil sie auf `type === 'fill_blank'` hört.** Die Aufgabe „setzen Sie den richtigen bestimmten Artikel davor" steht in **allen drei** Items ausschließlich im englischen `questionEn`; die deutsche Cue-Liste nennt nur `[Honig / ist / gut]`. Mit `checkAnswer` nachgerechnet: **`Honig ist gut.` → `wrong`, `tagError` → Verbstellung** — und „Honig ist gut." ist als Stoffname ohne Artikel fehlerfreies, sogar idiomatisches Deutsch; bei den beiden anderen ist „Eine Blume ist schön." / „Ein Schrank ist neu." die zweite richtige Lösung, die der deutsche Prompt genauso hergibt. Der Lernende bekommt einen **Wortstellungsfehler** ins Fehlerprofil geschrieben, den er nicht gemacht hat, und zwar dort, wo es zählt: `dd86dc8a` ist in Versuch 2 die **erste** Aufgabe von Lektion 4, `670eaadb` und `cd6d471e` sind **zwei der drei Schreiben-Aufgaben von Checkpoint 2**, also im benoteten Abschnitt, der über „Sicher" entscheidet. Dazu ist `Honig` in keinem Wortfeld des Kurses (die Lektion spielt auf dem Flohmarkt), und die Erklärung erfindet eine Endungsregel mit einem zweiten ungelehrten Wort: „Endungen auf -ig sind meist maskulin: der Honig, der **König**." | Die Regel nach der **Form** fragen, nicht nach dem Typ. In `quality.js`: `articleCueOnlyInGloss` um die Satzbauitems erweitern — `if (String(item.type) === 'sentence_building') { const cues = (BRACKET_LIST_RE.exec(q)||[])[1]||''; const lead = flat(bare(item.answer)).split(' ')[0]; return DEFINITE_ARTICLE_ANSWERS.includes(lead) && !flat(cues).split(/[^a-z]+/).includes(lead); }` — also: der Artikel, den die Antwort verlangt, muss in der Cue-Liste stehen. Im Reparaturlauf von `scripts/build-lesson-pool.mjs` den Artikel in die Liste ziehen (`'[der Honig / ist / gut]'`) oder die Aufgabenformel anhängen (`'… (mit bestimmtem Artikel)'`), und in `tests/lesson-pool-rules.test.mjs` denselben Pool-weiten 0-Test wie für die fill_blank-Hälfte. Zusätzlich: `dd86dc8a` ersetzen — `Honig` und `König` stehen in keiner Lektion; das Wortfeld von L4 hat Tisch, Stuhl, Lampe, Uhr, Tasche, Rucksack, Regal. | BLOCKER |
| `extra-a11-l05-08` (gezogen L5 #7, Versuch 2) | `questionDe: 'Ist das ein Heft? — Ja, und ___ ist grün. (bestimmter Artikel)'` · `answer: 'das Heft'`, `accepted: ['das Heft','Das Heft']` · `explanationDe: 'Erst ein Heft, dann bekannt: das Heft.'` | **Das Item, das Runde 4 als Kontrastübung bestellt hat (MAJOR 8), bestraft genau die Antwort, die seine eigene Aufgabenformel verlangt.** Die Klammer sagt „(bestimmter Artikel)" — also ein Wort, `das`. Mit `checkAnswer` nachgerechnet (`strict = true`, weil `STRICT_TOPIC` auf `definite-articles` greift, also ohne Tippfehlertoleranz): **`das` → `wrong`, `Das` → `wrong`, `tagError` → Wortschatz.** Und „Ja, und das ist grün." ist ein vollkommen korrekter deutscher Satz — das Demonstrativum nimmt das Heft auf. Dem Lernenden wird eine **Wortschatzlücke** protokolliert, weil er die Aufgabenformel befolgt hat. Das ist wörtlich die Konstruktion, die seit Runde 2 dreimal Blocker war, diesmal in einem Item, das als **Reparatur** geschrieben wurde — derselbe Befund wie in Runde 3 („der Blocker steckt in der Karte, die den Altbestand ersetzen sollte") und in Runde 4 („die Reparaturitems sind nicht gegen dieselben Regeln geprüft worden"). Das Schwesteritem `extra-a11-l05-06` („Wo ist ___ Wörterbuch? — Hier. (bestimmter Artikel)" → `das`) macht es richtig, im selben Block, zwei Zeilen weiter. | Sofort: `accepted: ['das Heft','Das Heft','das','Das']`. Dauerhaft die Regel, die diese Klasse schließt — die Aufgabenformel und die Antwortform müssen zusammenpassen: in `quality.js` ein `REASON.CUE_ANSWER_MISMATCH` mit `cueAnswerMismatch(item)`, das `true` liefert, wenn `questionDe` `ARTICLE_CUE.definite`/`.indefinite` trägt und **keine** akzeptierte Antwort ein blanker Artikel ist (bzw. umgekehrt), plus ein Pool-weiter 0-Test in `tests/lesson-pool-rules.test.mjs`. Die Formel gehört dann ans Item angepasst: `'Ist das ein Heft? — Ja, und ___ Heft ist grün. (bestimmter Artikel)'` → `accepted: ['das','Das']`. | BLOCKER |
| `isCaseTask()` / `POLITE_POSSESSIVE_RE` in `src/lib/lesson/check.js` ↔ `extra-a11-l12-10` ↔ `src/lib/checkpoint/buildCheckpoint.js:489` und `src/pages/lesson/ReviewPage.jsx:151` | `POLITE_POSSESSIVE_RE = /^Ihr(e\|en)?$/` · `extra-a11-l12-10`: `'___ Geschenke sind hier. (sie, Plural)'`, `accepted: ['Ihre']`, `explanationDe: 'sie im Plural → ihre, im Plural mit -e.'` · `buildCheckpoint.js`: `checkAnswer(String(answer), item.accepted, { strict })` — **ohne** `caseSensitive` | **Der Schalter, der BLOCKER 3 aus Runde 4 schließen sollte, trifft das falsche Item und erreicht die beiden anderen Bewertungsstellen nicht.** Drei Messungen: **(a)** `isCaseTask` entscheidet über einen Regex auf der Antwortform, nicht über die Aufgabe. `extra-a11-l12-10` ist kein Höflichkeitsitem — die Klammer sagt `(sie, Plural)`, die Erklärung sagt „→ **ihre**" — dort ist das große I nur die Satzanfangs-Großschreibung. Nachgerechnet: **`ihre` → `wrong`, `reason: 'case'`, Tag Rechtschreibung**; der Lernende schreibt die Form, die die Erklärung unter dem Item nennt, und bekommt ein rotes X. **(b)** Umgekehrt verfehlt derselbe Regex die Stellen, wo die Schreibung wirklich die Aufgabe ist: `extra-a11-l03-08` („Frau Kaya, sprechen ___ Englisch?", `explanationDe: 'Sie mit großem S ist die höfliche Form.'`) und `extra-a11-l01-06` („Wie geht es ___?" → `Ihnen`) liefern für `sie` bzw. `ihnen` nur `typo` — dieselbe Höflichkeitsregel, drei Lektionen früher, mit einem Wiederholungsversuch statt einem Fehler. **(c)** Am schwersten: `caseSensitive` wird ausschließlich von `PracticeItem.jsx` übergeben. `buildCheckpoint.isItemCorrect` und `ReviewPage.checkTyped` rufen `checkAnswer` ohne den Schalter, und beide zählen `RESULT.TYPO` als richtig. Gemessen an genau diesem Item, das als `a1.1-cp4-bausteine-4` im Checkpoint steht: **`isItemCorrect(item, 'ihre') === true`** — dieselbe Eingabe, dasselbe Item, in der Lektion falsch und im benoteten Checkpoint richtig. Das ist die Stelle, an der der Kurs zwei Bewertungen für eine Antwort hat. | Drei Schritte. (1) Die Aufgabe markieren statt sie zu raten: `caseSensitive: true` im Item (das Feld existiert und wird von `isCaseTask` zuerst gelesen) für `extra-a11-l12-08/09/16` und für `extra-a11-l03-08`, `extra-a11-l01-06`; `POLITE_POSSESSIVE_RE` und den Slug-Zweig aus `isCaseTask` **streichen** — dann trifft der Schalter nur noch, wo jemand ihn gesetzt hat, und `extra-a11-l12-10` verliert ihn. (2) `buildCheckpoint.js:489` → `checkAnswer(String(answer), item.accepted, { strict, caseSensitive: isCaseTask(item) })` (das Item trägt `poolItemId`/`topic`, also reicht das Item selbst) und `ReviewPage.jsx:151` ebenso für Karten, deren `accepted` aus einem `caseSensitive`-Item stammt. (3) Ein Test in `tests/checkpoint.test.mjs`, der für jedes Baustein-Item `isItemCorrect(item, x) === (checkAnswer(x, item.accepted, {strict, caseSensitive: isCaseTask(item)}).result !== RESULT.WRONG)` über eine Antwortliste pinnt — die Regel lautet: **eine Antwort wird im Kurs überall gleich bewertet.** | BLOCKER |

---

## MAJOR

| Feld | Zitat | Problem | Fix (pasteable) | Schwere |
|---|---|---|---|---|
| `DRILLS['yes-no-questions']` in `quality.js` gegen die Ziehung von L10 | `expected.some((a) => /\?\s*$/.test(String(a).trim())) \|\| options.some(…) \|\| /(bilden sie\|bilde\|schreiben sie\|schreib)\s+…frage/i.test(q)` · gezogen: `extra-a11-l10-11` „Korrigieren Sie: „Sind der Bahnhof weit?"" → `Ist der Bahnhof weit?` | **Runde 4 hat die zu großzügige Klausel gestrichen und eine zweite stehen lassen: die erste Klausel zählt jedes Item, dessen Antwort mit einem Fragezeichen endet — auch wenn die Wortstellung vorgegeben ist und nur die Verbform korrigiert wird.** Nachgezählt in **beiden** Ziehungen: `drillsSlug` meldet für L10 4 von 7; davon lassen genau **zwei** den Lernenden die Inversion produzieren (`extra-a11-l10-07/12`, „Bilden Sie die Frage: [fahren / der Bus / nach Bremen]"). Die anderen beiden (`-10`, `-11`) sind Subjekt-Verb-Kongruenz — „Haben der Zug … Verspätung?" → „Hat …", „Sind der Bahnhof weit?" → „Ist …" —, die Frageform steht schon da. Die drei nicht zählenden Items geben sie ebenfalls vor. Die Wortstellung, die Lektionstitel, `primarySlug` und die Regelkarte versprechen, produziert der Lernende in **2 von 7** Items; die Messzahl sagt 4 und hält damit den Ratchet `PRIMARY_MIN` für erfüllt. Dasselbe Prädikat misst `tests/lesson-engine.test.mjs` — und eine zweite, byte-ähnliche Kopie von `DRILLS` steht in `tests/lesson-extra-items.test.mjs:173–230`, die bei der nächsten Änderung auseinanderläuft. | Die erste Klausel an die Herkunft des Fragezeichens binden: `expected.some((a) => /\?\s*$/.test(String(a).trim())) && !/\?/.test(q)` — eine Frage zählt nur, wenn der Prompt **keine** fertige Frage enthält, also der Lernende sie baut. Danach misst L10 2/7, fällt unter `PRIMARY_MIN` — das ist der ehrliche Befund —, und die Lücke füllen zwei Items aus dem eigenen Dialog: `{ id:'extra-a11-l10-17', type:'sentence_building', questionDe:'Bilden Sie die Frage: [kosten / die Fahrkarte / zwanzig Euro]', answer:'Kostet die Fahrkarte zwanzig Euro?' }` und `{ id:'extra-a11-l10-18', questionDe:'Bilden Sie die Frage: [sein / der Bahnhof / weit]', answer:'Ist der Bahnhof weit?' }`. Und die Doppelung auflösen: `tests/lesson-extra-items.test.mjs` importiert `drillsSlug` aus `quality.js`, statt es zu kopieren. | MAJOR |
| `itemLexis()` in `scripts/validate-curriculum.mjs:304` | `export function itemLexis(c, extraItems = loadExtraItems()) { … for (const it of extraItems) { const nr = lektionOfItem(it.id); if (!nr) continue; …` | **RULE 10 hat den Fix aus Runde 4 bekommen und liest jetzt den gebauten Pool; RULE 11 nicht — sie liest weiter nur `a11.extra.json`, weil sie die Lektion aus dem **Id-Präfix** ableitet.** Die 228 Generat-Items haben UUIDs, also fällt jedes von ihnen durch `if (!nr) continue`. Die Ratchet-Zahl 9 beschreibt damit 119 von 347 Items. Nachgemessen mit derselben Mechanik, aber über `practiceRule.topics` zugeordnet und nur auf die **gezogenen** Generat-Items angewandt: **21 ungelehrte Tokens**, unter anderem `Honig` (L4), `Freiheit` (L4), `Instrument` (L4), `Endung`/`feminin`/`-ung`/`-chen` (L4, siehe MAJOR 4), `Kaffee` in L4 (Wortfeld von L9), `Deutschkurs` (L8), `interessant` (L5) und `arbeiten` in L7 (Wortfeld von L11, siehe MAJOR 3). Derselbe blinde Fleck sitzt in `buildCheckpoint`: Checkpoint 2 (Lektion 4–6) zieht `___ Wein ist gut.` mit der Erklärung „Alkoholische Getränke außer Bier sind maskulin: der Wein, der **Whisky**." und markiert es als `source: 'chapter'`. Das ist wörtlich der Schlusssatz von Runde 4: **was der Validator nicht liest, existiert für die Reparaturrunde nicht.** | `itemLexis` dieselbe Zuordnung geben, die `wortfeldCoverage` seit dieser Runde benutzt: zweites Argument `poolItems = loadPoolItems()`, Lektion über `practiceRule.topics` (`const lektionOfTopic = new Map(c.lektionen.flatMap((l) => (l.practiceRule?.topics||[]).map((t) => [t, l.nr])))`), und für ein Topic, das in mehreren Lektionen läuft, die **früheste** Lektion nehmen (das ist die strengere und richtige Lesart). Danach RULE 11 neu messen und `MAX_UNTAUGHT_ITEM_TOKENS` auf den gemessenen Wert setzen — mit der Offender-Liste im Report wie bei RULE 10. Und dieselbe Prüfung in `tests/checkpoint.test.mjs` über die 80 Checkpoint-Items. | MAJOR |
| `898a0861` und `58dd25ce` (gezogen L7 #3 bzw. #1, beide Versuche) gegen `wortfeld` L7/L11 und die notice von L7 | `'Ich ___ viel. (arbeiten)'` → `arbeite` · `'Du ___ viel.'`, `options: ['arbeitst','arbeite','arbeitest','arbeiten']` → `arbeitest`, `explanationDe: 'Stamm endet auf -t, deshalb -e- einfügen: arbeitest.'` | **`arbeiten` steht im Wortfeld von Lektion 11 (`a11.js:1147`), wird aber in Lektion 7 zweimal gedrillt — und das zweite Item lehrt den e-Einschub, den die notice von L7 nicht kennt.** Runde 3 hat das benannt („`arbeiten` ist in L7 doppelt falsch platziert: der e-Einschub steht in keiner notice"), Runde 4 hat es als erledigt verbucht, weil die Klammerangabe jetzt im deutschen Prompt steht — das war der Blocker, nicht dieser Befund. Die notice von L7 lautet vollständig „ich spiel**e**, du spiel**st** … **gern** steht nach dem Verb … einige Verben wechseln den Vokal". Kein Wort über `-t`/`-d` am Stammende. Die Regelkarte `present-tense-regular` hat den Satz („Endet der Stamm auf -t oder -d, kommt ein e dazu"), aber die Karte sieht nur, wer auf „Erklär mir das" drückt — nachdem er falsch geantwortet hat. Von den vier Items, die in Versuch 1 für L7 als primär zählen, drillen damit **zwei** ein Verb, das die Lektion nicht eingeführt hat, an einer Regel, die sie nicht erklärt. | Eines von beidem, in derselben Änderung — und ich empfehle das erste: die notice um einen Halbsatz erweitern und `arbeiten` ins Wortfeld von L7 ziehen (die Lektion heißt „Freizeit und Hobbys" und ihr Dialog kontrastiert frei/arbeiten ohnehin nicht — dann lieber ins Wortfeld von L7 **und** L11 nicht doppeln, sondern L7 zuweisen, L11 verweist darauf). notice L7 → „… wir spiel**en**, ihr spiel**t**, sie/Sie spiel**en**. Endet der Stamm auf **-t**, kommt ein **e** dazu: du arbeit**e**st, er arbeit**e**t." (bleibt unter 60 Wörtern, wenn der Vokalwechsel-Satz auf „sprechen → er **spricht**" kürzt). Alternativ beide Items auf ein Verb aus dem L7-Wortfeld ziehen (`spielen`, `hören`, `kochen`, `tanzen`). Die Regel dazu ist MAJOR 2: sobald RULE 11 den gebauten Pool liest, fällt `arbeiten` in L7 von selbst auf die Offender-Liste. | MAJOR |
| `89f19829-11d0-4a96-9cd4-f4b26d40b6ff` (gezogen L4 #5 bzw. #6, **beide** Versuche), daneben `57592d92` | `questionDe: 'Welche Endung ist IMMER feminin?'`, `options: ['-er','-um','-chen','-ung']`, `answer: '-ung'`, `explanationDe: '-ung ist 100% feminin ohne Ausnahmen.'` · `'___ Freiheit ist wichtig.'`, `explanationDe: 'Endung -heit ist immer feminin.'` | **Ein Item der Ziehung stellt eine metasprachliche Frage und beantwortet sie mit einer Behauptung, die falsch ist.** Sachlich: das **Suffix** -ung ist feminin, aber „100 % ohne Ausnahmen" gilt nicht für Wörter **auf** -ung — der Ursprung, der Schwung, der Sprung, der Dung sind maskulin. Ein A1.1-Lernender kann diese Unterscheidung nicht treffen und lernt einen Satz, den er später zurücknehmen muss. Pädagogisch: die Frage verlangt Grammatikterminologie (*Endung*, *feminin*) in einer Lektion, deren Handlungsfeld „Einkaufen: nach Gegenständen und Preisen fragen" ist und deren Wortfeld Tisch, Stuhl, Lampe, Uhr, Tasche heißt; `Freiheit` und `Instrument` kommen aus derselben Ecke. Und es ist eine Doppelmoral im eigenen Repo: `tests/rule-card-overrides.test.mjs` verbietet jeder Regelkarte eine Prozentangabe („a percentage has no source on an A1 rule card") — dasselbe Item darf im Übungsblock stehen und sogar zweimal gezogen werden. `nouns-gender` hat in L4 nur acht handgeschriebene Items, deshalb rutschen die Generatreste hoch. | Zwei Zeilen und eine Regel. (1) Die Prozentbehauptung streichen: `explanationDe: 'Nomen auf -ung sind die-Wörter: die Entschuldigung, die Rechnung.'` (2) Die Metafrage aus der Ziehung nehmen — in `quality.js` ein `REASON.METALINGUISTIC_PROMPT` mit `/^Welche Endung\|^Welches Genus\|^Welcher Artikel passt zu der Endung/i` auf `questionDe` **und** einem Optionssatz aus blanken Endungen (`options.every((o) => /^-/.test(o))`); der Kurs lehrt Genus als Teil des Wortes, das sagt seine eigene notice („Das Genus ist Teil des Wortes"). (3) Den Ausfall decken: zwei handgeschriebene L4-Items aus dem eigenen Wortfeld, `{ id:'extra-a11-l04-09', questionDe:'___ Flohmarkt ist am Samstag. (der, die oder das?)', answer:'Der' }` und `{ id:'extra-a11-l04-10', type:'error_correction', questionDe:'Korrigieren Sie: „Das Lampe kostet acht Euro."', answer:'Die Lampe kostet acht Euro.' }`. Und dieselbe Prozent-Regel, die für die Karten gilt, in `tests/lesson-pool-rules.test.mjs` für den Pool. | MAJOR |
| `buildLesen()` in `src/lib/checkpoint/buildCheckpoint.js`, alle vier Checkpoints | cp1–cp4, Position 1–4, ausnahmslos: `answer: 'Richtig'`, `'Falsch'`, `'Richtig'`, `'Falsch'` · `promptDe: 'Steht das im Text? „Frau Wolf: Der Tisch kostet fünfzehn Euro."'` mit `explanationDe: 'Dieser Satz steht nicht in diesem Text — er kommt aus einer anderen Lektion.'` | **Der Lesen-Teil der benoteten Checkpoints hat in allen vier Checkpoints denselben Lösungsschlüssel und prüft Zeichenkettenvergleich statt Leseverstehen.** Gebaut und ausgedruckt: 16 Items, 16-mal dieselbe Schablone, viermal dasselbe Muster R–F–R–F. Ein Lernender, der Checkpoint 1 gemacht hat, holt in den Checkpoints 2–4 vier von vier Punkten, ohne den Text zu lesen — und der Lesen-Abschnitt ist einer der fünf, für die die 40-%-Regel des Standards (§3) gilt, also entscheidet er über Bestehen und über den Mastery-Sprung auf „Sicher". Inhaltlich ist es kein Lesen Teil 1/2/3 von *Start Deutsch 1*: dort steht ein kurzer Alltagstext (Anzeige, Schild, E-Mail) mit einer Aussage, die man **inhaltlich** prüfen muss. Hier ist der „Text" ein Ausschnitt von drei Zeilen aus dem Dialog derselben Lektion, und die Prüffrage ist eine Zeile daraus — wörtlich, mit Sprecherpräfix („Ana: …"). Wer den Satz wiedererkennt, hat recht; wer ihn versteht, hat keinen Vorteil. Der `hint` nennt zusätzlich die Lektion. | Erst die Zufälligkeit, dann den Typ. (1) In `buildLesen` die Wahrheitswerte ziehen statt sie zu setzen: `const truth = seededShuffle([true,false,true,false], ctx.rng);` — eine Zeile, und das Rate-Muster ist weg; dazu ein Test in `tests/checkpoint.test.mjs`, der über die vier Checkpoints **mindestens zwei verschiedene** Reihenfolgen verlangt. (2) Den Typ an die Prüfung binden: die falsche Aussage nicht aus einer anderen Lektion zitieren, sondern aus **demselben** Text mit einer geänderten Angabe (Preis, Uhrzeit, Wochentag, Name) — `'Steht das im Text? „Der Tisch kostet fünfzig Euro."'` gegen einen Text, der fünfzehn sagt. Das ist derselbe Generator mit einer Ersetzungstabelle über die Zahlen und Wochentage des Dialogs, und es prüft zum ersten Mal Verstehen. (3) `hint` auf `null`, solange er die Lösung mitliefert. | MAJOR |
| `buildSchreiben()` in `buildCheckpoint.js`, Checkpoint 2 und 4 | cp2: `Schreiben Sie den Satz: [Blume / ist / schön]` · `Bilden Sie den Satz: [das / Bild / sein / schön]` · `Schreiben Sie den Satz: [Schrank / ist / neu]` — dazu `"register":"mitteilung"` bzw. `"formular"` auf jedem · cp4: dreimal `yes-no-questions` („[das / Anna / sein]", „[du / Anna / heißen]", „[du / am Samstag / mitkommen]") | **Der Schreiben-Abschnitt der Checkpoints ist Grammatikdrill mit einem erfundenen Textsorten-Etikett.** Drei Messungen: (a) **Checkpoint 2** besteht aus drei fast identischen Sätzen des Musters „X ist schön/neu", und **zwei davon sind die Blocker-Items von oben** — im benoteten Test wird also die Textsorten-Note an Items vergeben, die eine richtige Antwort bestrafen. (b) **Checkpoint 4**, der Abschluss des Kurses, zieht dreimal `yes-no-questions`, also den Slug von Lektion **10**; aus L11 und L12 — Tagesablauf, Einladung, Possessivartikel — kommt keine einzige Schreiben-Aufgabe, und zweimal heißt die Figur *Anna* neben der Kursfigur *Ana*. (c) Das Feld `register` wird auf jedes dieser Items geschrieben (`'mitteilung'`, `'formular'`), obwohl kein Item eine Textsorte hat; `evaluate-writing.mjs` leitet aus demselben Feldnamen die Zeichenuntergrenze ab, der Name ist also besetzt. Der Standard (§3) verlangt für den Checkpoint „3 Schreiben" im Prüfungsformat, und `CheckpointPage.jsx` beschriftet den Abschnitt so — der Kurs hat zwölf echte, KI-bewertete Schreibaufgaben in `writingTasks.js` und benutzt im Test keine davon. | Den Abschnitt an das binden, was der Kurs schon kann: `buildSchreiben` zieht **eine** Aufgabe aus `writingTasks.js` (die des zuletzt abgeschlossenen Kapitels, `register` echt) und schickt sie durch `evaluate-writing.mjs` wie `GradedWriting.jsx`, plus zwei Satzbauitems aus verschiedenen Lektionen des Kapitels — `const slugs = new Set(chapter.map((l) => l.primarySlug)); … pickOne(slug)` statt dreimal aus demselben Topf. Bis die KI-Aufgabe im Checkpoint hängt, mindestens: `register: null` auf den Drill-Items (kein falsches Etikett), die Blocker-Items ersetzen, und ein Test in `tests/checkpoint.test.mjs`, der für jeden Checkpoint **drei verschiedene `topic`** im Schreiben-Abschnitt verlangt. | MAJOR |
| `src/pages/lesson/CheckpointPage.jsx:169/429` und `src/pages/lesson/ReviewPage.jsx:179/263` gegen `tests/curricula.test.mjs:269` | `'Bewertet — Verständlichkeit zählt in dein Sprechen-Ergebnis.'` · `'20 Aufgaben aus … — im Format deiner Prüfung.'` · `'Melde dich an, damit deine Wiederholungen gespeichert werden.'` · `'Neue Karten kommen, sobald du eine Lektion abschließt.'` | **Die Registerentscheidung aus Runde 4 ist in der Lektion durchgezogen und in den beiden Bildschirmen daneben nicht — und der Test, der sie hält, prüft nur `a11.js`.** Gemessen: `PretestStage`, `WortfeldStage`, `ReadAloudLine`, `GradedWriting`, `SpeakingStage` siezen jetzt durchgehend (der Platzhalter heißt „Schreiben Sie einfach, was Sie können."), alle zwölf notices sprechen unpersönlich, der Pool hat 0 du-Imperative, und `tests/curricula.test.mjs:269` pinnt genau die vier Felder von `a11.js`. Außerhalb dieser vier Felder gilt die Entscheidung nicht: die **Checkpoint-Seite** — der benotete Test, den der Standard als Kern des Produkts führt — duzt zweimal, die **Wiederholungsseite** zweimal. Für einen Lernenden ist das derselbe Bruch wie in Runde 4, nur einen Klick weiter: Schritt 5 der Lektion siezt ihn, der Test danach duzt ihn. Und für einen Prüfungskandidaten ist es nicht kosmetisch — die Anrede ist in Sprechen Teil 3 und Schreiben Teil 2 selbst Bewertungsgegenstand. | Vier Zeichenketten und ein größerer Testgeltungsbereich. `CheckpointPage.jsx:169` → `'Bewertet — Verständlichkeit zählt in Ihr Sprechen-Ergebnis.'`; `:429` → `'… im Format Ihrer Prüfung.'`; `ReviewPage.jsx:179` → `'Melden Sie sich an, damit Ihre Wiederholungen gespeichert werden.'`; `:263` → `'Neue Karten kommen, sobald Sie eine Lektion abschließen.'`. Dazu den Test aus der Datenschicht in die Chrome ziehen: in `tests/curricula.test.mjs` (oder besser `tests/course-player.test.mjs`) die Quelltexte von `src/components/lesson/*.jsx` und `src/pages/lesson/*.jsx` lesen, alle String-Literale extrahieren und gegen dasselbe `DUZEN` prüfen, mit einer schmalen Ausnahmeliste für Dialogzitate. Genau dieser Test hätte die vier Stellen gefunden. | MAJOR |
| `buildTeacherSystemPrompt()` in `netlify/functions/_shared/speakingAI.mjs` gegen `sprechen.open` von L8, L9, L12 | `CONVERSATION_RULES` (7 Regeln, keine über die Anrede) · `'Du bist eine warme, geduldige Deutschlehrerin …'` · `DEINE AUFGABE (${courseTask.teil}) — dein Gegenüber bearbeitet gerade genau diese Aufgabe: "Laden Sie eine Kollegin zu Ihrem Fest ein und reagieren Sie auf die Antwort."` | **Der Sprechauftrag erreicht den Coach jetzt wirklich — aber ohne die Rolle, die er verlangt.** Der Weg ist nachgeprüft und richtig gebaut: `SpeakingStage.saveCourseContext` legt `openPrompt`/`openTeil`/`hintWords` ab, `SpeakingPage` liest sie (`:170`), schickt sie als `taskPrompt`/`taskTeil`/`taskHintWords`, `parseCourseTask` validiert und begrenzt sie, `courseTaskColumns` persistiert sie in `topic`/`scenario`, `taskFromSession` baut sie für jeden Folgeturn zurück — die vier missionslosen Lektionen bekommen ihre Aufgabe. Was der Prompt **nicht** sagt: ob der Coach duzt oder siezt. Bei drei der vier Aufgaben ist das die Aufgabe selbst: L12 „Laden Sie eine **Kollegin** zu Ihrem Fest ein" ist Sprechen Teil 3 in der Sie-Situation, ebenso L8 („Bitten Sie um einen Termin") und L9 („Bitten Sie im Café um ein Glas Wasser"); der Kurs hat die Höflichkeitsform in L1, L3 und L12 eigens gelehrt und in `extra-a11-l12-08/09/16` geprüft. Ein Coach, der „Hast du Lust?" antwortet, macht die einzige mündliche Prüfungsübung des Kursabschlusses unbrauchbar — und er ist, seit Runde 5, der einzige Bildschirm des Kurses, der nicht siezt und dafür keine Entscheidung hat. | Die Rolle mitschicken, aus Daten, nicht aus dem Modellgefühl. In `a11.js` bekommt `sprechen.open` ein Feld `anrede: 'Sie' \| 'du'` (L1–L12: Sie außer dort, wo der Auftrag ausdrücklich unter Freunden spielt); `SpeakingStage` gibt es in `saveCourseContext` weiter, `SpeakingPage` als `taskAnrede`, `normalizeCourseTask` validiert es auf die zwei Werte, und `buildTeacherSystemPrompt` hängt an: `` parts.push(`ANREDE: Sprich dein Gegenüber durchgehend mit ${task.anrede === 'du' ? 'du' : 'Sie'} an. Die Aufgabe verlangt diese Form; wechsle sie nie.`) ``. Test in `tests/speaking-course-task.test.mjs`: für jede der zwölf Lektionen muss der gebaute System-Prompt die Anredezeile enthalten und zu `a11.js` passen. | MAJOR |
| `a11.js` L3 `dialog.lines[9]` gegen `writingTasks.js` `a11-l03` und `a11-l02` | `{ speaker:'Ana', de:'Ja, ein Baby. Mein Mann und ich kommen aus Marokko.' }` — auf Lenas Frage „Und dein Bruder? Hat er einen Sohn oder eine Tochter?" · dagegen `a11-l03`: `'Ana Chakiri ist **ledig**. Sie kommt aus Marokko …'` und `a11-l02` sample: `'… Ich bin ledig.'` | **Der Deckungs-Ratchet wurde erfüllt, indem das fehlende Wort in eine Zeile geschoben wurde — und die Zeile widerspricht jetzt der Schreibaufgabe derselben Lektion.** RULE 10 ist von 35 auf 19 gefallen, und die sechs Familienwörter, die Runde 4 namentlich genannt hat, stehen im Input: `die Geschwister`, `der Sohn`, `die Tochter` in `lines[1]` und `lines[8]`, sauber. `der Mann` ist anders untergebracht: die Antwort auf eine Frage nach dem Kind des Bruders lautet „Ja, ein Baby. **Mein Mann und ich kommen aus Marokko.**" — zwei Sätze ohne Zusammenhang, und der zweite macht Ana verheiratet, während das Formular derselben Lektion sie als **ledig** führt und der Mustertext von L2 „Ich bin ledig" schreibt. Für einen A1-Lernenden, der genau diesen Dialog nachspricht (`readAloud [1,6]`) und danach das Formular ausfüllt, ist das ein Widerspruch in derselben Sitzung; für einen Prüfer ist es das Kennzeichen einer Deckungsliste, die abgearbeitet wurde. Dieselbe Bauart, milder, in L6 `lines[3]`: „Hier sind ein Computer und ein Telefon. **Ihr Kollege ist Ingenieur.**" | Die Zeile inhaltlich auflösen statt das Wort zu platzieren: `lines[8]` → `{ speaker:'Lena', de:'Und dein Bruder? Ist er verheiratet?', en:'And your brother? Is he married?' }` und `lines[9]` → `{ speaker:'Ana', de:'Ja. Seine Frau heißt Nadia. Sein Mann? Nein — er hat eine Frau und ein Baby.', … }` ist zu lang; kürzer und sauber: `lines[8]` → `'Und dein Bruder? Hat er einen Sohn oder eine Tochter?'` bleibt, `lines[9]` → `{ speaker:'Ana', de:'Ja, ein Baby. Der Mann von meiner Schwester kommt auch aus Marokko.', en:'Yes, a baby. My sister’s husband is also from Morocco.' }` — `der Mann` ist gedeckt, Ana bleibt ledig. Und die Regel: in `validate-curriculum.mjs` neben RULE 10 eine RULE 14, die die Personenangaben von `dialog` und `schreiben` derselben Lektion vergleicht (Familienstand, Herkunft, Beruf der wiederkehrenden Figuren aus einer kleinen Tabelle) — der Widerspruch ist sonst nur mit dem Auge zu finden. | MAJOR |
| RULE 12 in `validate-curriculum.mjs` (Ratchet 6) gegen `astro-site/src/pages/courses/[level].astro` | L2: `'Ich kann Zahlen von null bis zehn verstehen und sagen.'` · L9: `'Ich kann höflich fragen, ob es etwas gibt.'` · dazu L1, L4, L7, L12 · `wortfeld` L2: `{ de: 'die Zahlen 0–10', meta: true }`, L4: `{ de: 'die Zahlen 11–100', meta: true }` | **Die neue Regel misst das Richtige, und was sie misst, ist an zwei Stellen Prüfungssubstanz — beide im öffentlichen 12×6-Raster, das der Käufer vor dem Kauf prüft.** L2: der Kurs übt **kein einziges Zahlwort**. Runde 3 hat die beiden Zahlwortitems (`extra-a11-l02-04/05`, „Schreiben Sie die Zahl als Wort: 7 → ___") als falsch getaggt entfernt, Runde 4 und 5 haben sie nicht ersetzt; gleichzeitig sind `die Zahlen 0–10` und `die Zahlen 11–100` seit dieser Runde `meta: true` und damit auch von RULE 10 ausgenommen. Die beiden größten Wortschatzblöcke des Kurses sind also gleichzeitig von der Deckungsprüfung befreit **und** ungeübt — während *Start Deutsch 1* im Hören Teil 1 fast ausschließlich Zahlen, Uhrzeiten und Telefonnummern abfragt und die Kann-Beschreibung sie verspricht. L9: „höflich fragen, ob es etwas gibt" ist Sprechen Teil 3 und steht in keinem Item, keinem Pretest und keiner Schreibaufgabe; der Dialog hat „Haben Sie auch Hunger?", die Übung hat es nicht. Die übrigen vier (L1 begrüßen/verabschieden, L4 „was ist das?", L7 „wann ich frei habe", L12 „gute Wünsche") stehen wenigstens im Dialog. | Für L2 vier Items, die die Zahl aus dem deutschen Prompt ableiten und nicht aus einer Ziffer im Englischen — `{ id:'extra-a11-l02-09', topic:'verb-sein', questionDe:'Schreiben Sie die Zahl als Wort: Meine Telefonnummer ist 0 1 7 6. → null eins ___ sechs', answer:'sieben' }` ist zu verschachtelt; besser aus dem eigenen Dialog: `{ questionDe:'Ergänzen Sie: Meine Telefonnummer ist null eins ___ sechs. (7)', answer:'sieben', accepted:['sieben'] }` und drei Geschwister für 3, 8, 10; `practiceRule.topics` von L2 um `'time-and-dates'` erweitern oder — sauberer — ein eigenes `numbers`-Topic anlegen, damit `tagError` einen Zahlwortfehler nicht als **Konjugation** bucht (das war der Grund, warum Runde 3 sie geworfen hat, und er ist mit einem eigenen Topic behoben statt mit dem Löschen). Für L9 ein Item auf die feste Wendung: `{ id:'extra-a11-l09-09', topic:'verb-haben', questionDe:'Fragen Sie höflich im Café: ___ Sie auch Kuchen? (haben)', answer:'Haben' }`. Danach `MAX_UNREHEARSED_CANDOS` auf 4 senken. | MAJOR |
| `tagError()` in `src/lib/lesson/check.js` gegen jedes `sentence_building`-Item | `if (item?.type === 'sentence_building' \|\| item?.topic === 'yes-no-questions') return 'Verbstellung';` | **Jeder Fehler in einem Satzbauitem wird als Wortstellungsfehler protokolliert, egal was schiefging — und diese Etiketten sind das, woraus der Kurs seine Remediation baut.** Nachgerechnet an gezogenen Items: `extra-a11-l09-05` „Bilden Sie den Satz: [ich / möchten / ein Glas Wasser]", Eingabe *Ich möchte **einen** Glas Wasser.* → Tag **Verbstellung** (es ist ein Artikel-/Kasusfehler); `extra-a11-l04-08`, Eingabe *Der Stuhl kostet **12** Euro.* → **Verbstellung** (es ist eine Schreibweise); `dd86dc8a`, Eingabe *Honig ist gut.* → **Verbstellung** (es ist der fehlende Artikel). Im ganzen Pool tragen 20 Items diesen Typ, in den Checkpoints ist es der komplette Schreiben-Abschnitt. `scoreCheckpoint` sammelt die Tags in `errorTags`, `CheckpointPage.jsx:467` zeigt sie dem Lernenden als Diagnose an, und der Remediationssatz zwischen zwei Versuchen wird daraus gezogen. Ein Lernender, der dreimal den Artikel vergisst, bekommt gesagt, er habe ein Wortstellungsproblem, und übt danach Wortstellung. Der Standard (§3) zählt die acht Tags einzeln auf, weil sie verschiedene Übungen nach sich ziehen. | Die Position des Fehlers lesen, bevor der Typ entscheidet. In `tagError` vor die Typklausel: Wörter beider Sätze vergleichen — sind es dieselben Wörter in anderer Reihenfolge, ist es `Verbstellung`; unterscheidet sich genau ein Wort und ist das erwartete ein Artikel-/Possessivwort, greift die `ARTICLES`-Klausel, die schon darüber steht; unterscheidet sich genau ein Wort mit Levenshtein ≤ 2, ist es `Rechtschreibung`. `const sameBag = (a,b) => { const s=(t)=>t.split(' ').sort().join(' '); return s(a)===s(b); };` und `if (item?.type === 'sentence_building') return sameBag(usr, exp) ? 'Verbstellung' : diffTag(usr, exp);`. Dazu ein Test in `tests/check-answer.test.mjs` mit den drei oben gemessenen Paaren. | MAJOR |

---

## Status der Befunde aus Runde 4

Verifiziert an den Artefakten (Pool, Ziehung, gebaute `ruleCards.mjs`, gebaute
Checkpoints, Validator-Ausgabe, Testlauf), nicht am Diff.

### Die drei BLOCKER

1. **`extra-a11-l01-04/05` bestrafen `HALLO` (Trennzeichen als Prüfungsfrage)** —
   **gelandet, und zwar als Regel.** `normalizeSpelling()` + `SPELLED_OUT_RE` +
   `spellingApplies()` in `check.js` falten jede Trennzeichenvariante, und der Modus
   schaltet sich selbst ein, sobald eine akzeptierte Antwort wie eine Buchstabenfolge
   aussieht. Nachgerechnet an der Ziehung von L1: `HALLO`, `H A L L O`, `H-A-L-L-O`,
   `hallo` → alle **`correct`**; `TSCHÜSS`, `T S C H Ü S S`, `Tschuess` ebenso. Die
   `accepted`-Listen sind zusätzlich erweitert (Gürtel und Hosenträger, in Ordnung).
   Die freie Lektion 1 bestraft in keiner der beiden Ziehungen mehr eine richtige Antwort.
2. **42 Artikel-Items, deren Artikel nur in der englischen Glosse steht** —
   **gelandet, als Regel, mit Pool-weitem Test.** `REASON.ARTICLE_CUE_ONLY_IN_GLOSS`,
   `articleAnswerKind()`, `articleCueOnlyInGloss()`, `ARTICLE_CUE` und der Reparaturlauf
   im Build; `tests/lesson-pool-rules.test.mjs:300` verlangt 0 über den ganzen Pool.
   Nachgemessen an der Ziehung: L5 zieht „___ Tafel ist grün. **(bestimmter Artikel)**",
   L6 „Ich brauche ___ E-Mail-Adresse. **(unbestimmter Artikel)**", L4 „___ Rucksack ist
   teuer. **(der, die oder das?)**". `Eine` gegen `['Die','die']` ist damit begründet
   falsch. **Rest: die Regel hört auf `fill_blank`** — drei Satzbauitems derselben
   Bauart leben weiter (BLOCKER 1 oben), und `extra-a11-l05-08` hat die Formel bekommen,
   ohne dass die `accepted`-Liste dazu passt (BLOCKER 2).
3. **Groß-/Kleinschreibung ist nicht prüfbar (`ihr Geschenk` = richtig)** —
   **halb gelandet.** `caseSensitive`, `caseOnlyDiff()`, `isCaseTask()`,
   `reason: 'case'`, `CASE_HINT` in `PracticeItem.jsx` und `'Rechtschreibung'` in
   `tagError` existieren; nachgerechnet ist `ihr` gegen `['Ihr']` in
   `extra-a11-l12-08` jetzt **`wrong`**, und ein reiner Fallfehler anderswo ist `typo`
   statt stillschweigend richtig — das ist genau, was der Standard §3 verlangt. Offen
   sind die drei Hälften, die BLOCKER 3 oben beschreibt: das Prädikat trifft
   `extra-a11-l12-10` (kein Höflichkeitsitem), verfehlt `Sie`/`Ihnen` in L1/L3, und
   `buildCheckpoint`/`ReviewPage` übergeben den Schalter nicht.

### Die elf MAJOR

| # | Runde-4-Befund | Status | Beleg |
|---|---|---|---|
| 1 | Sieben von zwölf Regelkarten waren die halb englischen Tabellen von vor dem Umbau | **gelandet, vollständig** | Alle **zwölf** A1.1-Slugs stehen in `RULE_CARD_OVERRIDES` und in der gebauten `ruleCards.mjs`. Stichproben: `definite-articles` hat jetzt drei `commonMistakes` und lehrt den Bekanntheitskontrast an Heft/Schere/Lineal statt Pluralbildung; `verb-haben` sitzt im Café (Hunger, Durst, Suppe, Kellnerin); `indefinite-articles` an Computer/Pause/Verkäuferin; `personal-pronouns` ohne englische Spaltenköpfe. Je genau eine `English:`-Zeile, keine Prozentangabe, keine Lautvergleiche — von `tests/rule-card-overrides.test.mjs` (13 Tests) gepinnt. |
| 2 | Zwei Anreden auf einem Bildschirm (`PretestStage` duzt, die Aufgabe siezt) | **gelandet für `a11.js` und die Lektions-Chrome, offen daneben** | Die Entscheidung ist im Kopf von `rule-card-overrides.mjs` aufgeschrieben („TASKS AND EXPLANATIONS USE Sie OR THE IMPERSONAL `man`; only the dialogue duzt"), alle zwölf notices sprechen unpersönlich, die vier geprüften Felder von `a11.js` sind durch `tests/curricula.test.mjs:269` gepinnt, und die Lektions-Chrome siezt („Schreiben Sie einfach, was Sie können.", „Tippen Sie auf eine Karte …", „Der Sprach-Coach hört zu und gibt **Ihnen** …"). Offen: `CheckpointPage` und `ReviewPage` (MAJOR 7 oben) und der Sprech-Coach (MAJOR 8). |
| 3 | `Buchstabiert:` ×17 — ein Hörvorgang, den es nicht gibt | **gelandet** | Im ausgelieferten Pool **0**-mal `Buchstabiert:`; die 17 Items lauten „**Lesen Sie die Buchstaben:** E-N-T-S-C-H-U-L-D-I-G-U-N-G. Schreiben Sie das Wort: ___", vier davon in der L1-Ziehung von Versuch 2. `tests/lesson-pool-rules.test.mjs:285` pinnt es auf 0. |
| 4 | Vier Lektionen schicken den Lernenden mit einem Sprechauftrag los, den die Sprechseite nie erhält | **gelandet, als echter Serverweg** | `saveCourseContext({ …, openPrompt, openTeil, hintWords })` → `SpeakingPage:170` → `taskPrompt/taskTeil/taskHintWords` → `parseCourseTask` (validiert, 300 Zeichen, 8 Hilfswörter) → `courseTaskColumns` persistiert in `speaking_sessions.topic/.scenario` → `taskFromSession` baut sie je Turn zurück → `buildTeacherSystemPrompt` legt sie als „DEINE AUFGABE (Sprechen Teil 3)" in den System-Prompt. Eine Mission gewinnt weiterhin. RULE 13 mit `MAX_MISSIONLESS_LEKTIONEN = 4` läuft im Validator. Rest: die Anrede reist nicht mit (MAJOR 8). |
| 5 | L3 und L6: Wortfeldeinträge ohne Input, genau dort, wo die Kann-Beschreibung sie braucht | **gelandet, mit einer neuen Nebenwirkung** | RULE 10 misst **19 von 262** (Ratchet 19, war 35). L3 hat nur noch `das Einzelkind`, L6 keinen Eintrag mehr; `die Geschwister`, `der Sohn`, `die Tochter` stehen in `lines[1]`/`lines[8]`, `der Kollege`/`Ingenieur` in L6 `lines[3]`. Die Nebenwirkung ist MAJOR 9 oben: `der Mann` wurde in eine Zeile geschoben, die Ana verheiratet macht, während ihr Formular sie als ledig führt. |
| 6 | `canDo` L11 #2 (Perfekt-Chunks) und L2 #5 (Formular) werden nirgends geübt | **gelandet, beidseitig, mit Regel** | L11: `extra-a11-l11-09` („Was hast du gestern gemacht? — Ich habe ___. (arbeiten, fester Ausdruck)") und `-10` („[ich / haben / gestern / gearbeitet]") existieren, der Pretest lautet jetzt „Sagen Sie, was Sie gestern gemacht haben." mit `model: 'Ich habe gearbeitet.'`, und `-09` ist in **beiden** Ziehungen von L11 enthalten. L2: die Formular-Kann-Beschreibung ist gestrichen und durch „Ich kann in einer kurzen Nachricht Angaben zu meiner Person machen." ersetzt, was die Mitteilung deckt. RULE 12 existiert und läuft (Ratchet 6, Rest siehe MAJOR 10). |
| 7 | `drillsSlug()` an drei Stellen zu großzügig bzw. zu streng | **zu zwei Dritteln gelandet** | `nouns-gender`: die Klausel `/^(der\|die\|das)\s/` auf dem Antwortpräfix ist weg — `extra-a11-l04-07` zählt nicht mehr für L4. `possessive-articles`: `containsPossessive()` ergänzt, „Wir feiern unser Fest." zählt. `yes-no-questions`: die Lückenklausel ist weg, die Fragezeichenklausel nicht — L10 misst weiter 4/7, produziert aber 2/7 (MAJOR 1 oben). Gemessene Wirkung über beide Ziehungen: alle zwölf Lektionen ≥ 4, L6/L7/L10/L11 genau an der Grenze. |
| 8 | L4 und L5 üben dieselbe Handlung; L5 müsste die Bekanntheit üben | **gelandet** | `practiceRule.topics` von L4 ist auf `['nouns-gender']` verengt, L5 hat die drei Kontrastitems bekommen (`-06` „Wo ist ___ Wörterbuch? — Hier.", `-08` „Ist das ein Heft? — Ja, und ___ ist grün.", `-09` „Korrigieren Sie: „Ein Schere ist hier.""), `DEF_ARTICLE_CUE_RE` erkennt die Kontrastform, und die Karte `definite-articles` lehrt jetzt genau diesen Unterschied. Vorbehalt: `-08` ist BLOCKER 2 oben. |
| 9 | Die beiden Ratchets messen die falsche Datei | **zu zwei Dritteln gelandet** | (a) `wortfeldCoverage(c, extraItems, poolItems = loadPoolItems())` liest den **gebauten** Pool — `das Fenster`, `die Wohnung`, `braun` sind von der Liste verschwunden. (b) `if (w.meta) return new Set();` in `coverageForms`, die beiden Zahlen-Metaeinträge sind ausgenommen. (c) `ITEM_FORMULA_RE` streicht nur noch vollständige Formeln, und `extra-a11-l10-03: richtig` steht deshalb jetzt auf der RULE-11-Liste. Offen: RULE 11 liest weiterhin nur `a11.extra.json` (MAJOR 2 oben). |
| 10 | `accepted` widerspricht sich innerhalb einer Lektion (`Geht` in `-01` richtig, in `-06` falsch) | **gelandet, als Regel** | `EQUIVALENT_VERBS = { fahren: ['gehen','kommen'] }` + `FAHREN_FORMS` + `widenEquivalentVerbs()` in `scripts/build-lesson-pool.mjs`, angewandt nur dort, wo das Item selbst schon zwei Lemmata zulässt — bewusst schmal, mit der richtigen Begründung im Kommentar („Ich gehe nach Berlin ist nicht Ich fahre nach Berlin"), und `EQUIVALENT_VERB_IDS` ist leer („Empty is the honest state"). Nachgemessen: `extra-a11-l10-06` nimmt jetzt `Gehst/gehst` zusätzlich. Die Uhrzeit-Hälfte ist gegenstandslos, weil „acht Uhr fünfzehn" in den Rahmen „Es ist ___ acht." gar nicht passt. |
| 11 | `extra-a11-l11-01` („Steigen wir jeden Tag um?") | **gelandet** | Das Item lautet jetzt „Bilden Sie die Frage: [**mitkommen** / du / am Freitag]" → „Kommst du am Freitag mit?", exakt der Satz aus `lines[5]` des eigenen Dialogs, und es ist in beiden Ziehungen von L11 enthalten. `MAX_UNTAUGHT_ITEM_TOKENS` ist von 15 auf **9** gesenkt; `Steigen` steht nicht mehr auf der Liste. |

Zusätzlich geprüft und weiterhin in Ordnung: **`buildMessage()`** in
`netlify/functions/course-reminder.mjs` — A1-Deutsch, keine Behauptung über das
Verhalten des Lernenden, namensabhängige Anrede mit korrekter Groß-/Kleinschreibung des
folgenden Pronomens, Betreff und CTA aus der Lektion abgeleitet, und die kopierten
Lektionstitel sind durch `tests/course-reminder.test.mjs:183` gegen `CURRICULUM_A11`
gepinnt. **Zur Registerfrage: die E-Mail duzt, und das lasse ich als
Eigentümerentscheidung stehen — kein MAJOR.** Begründung: die Erinnerung ist weder
Aufgabe noch Bedienoberfläche, sondern Beziehungstext vor der Lektion; sie modelliert
keine Prüfungssituation, sie ist in sich konsistent, und die Du-Ansprache in
Lernerinnerungen ist bei deutschen Anbietern der Normalfall. Die Entscheidung sollte
allerdings dort stehen, wo die andere steht — ein Satz im Kopf von `course-reminder.mjs`
(„Die Erinnerung duzt; Aufgaben und Chrome siezen — Eigentümerentscheidung
2026-09-12") und eine Ausnahmezeile im Register-Test, sonst schließt sie die nächste
Runde versehentlich mit. Ebenfalls in Ordnung: `evaluate-writing.mjs`
(`MIN_CHARS = { formular: 12 }`, sonst 30, aus `task.register` abgeleitet;
A1-Feedback-Auflage für `goethe_a1`) und die zwölf Schreibaufgaben (6 Formular mit
Quelltext und fünf Feldern, 6 Mitteilungen mit drei Leitpunkten, 5–40 / 25–45 Wörter,
Titel und Text deckungsgleich mit `a11.js`, `register` dreiwertig und in
`evaluate-writing.mjs` benutzt). Testlauf: **619 von 619 grün.**

---

## Bewertungsübersicht (Runde 5, Runde 4 in Klammern)

| Lektion | Deutsch | Niveau | Dialog | Prüfung | Kohärenz | Σ /25 |
|---|---|---|---|---|---|---|
| 1 Hallo, ich bin … | 4 (4) | 4 (4) | 5 (5) | **4** (3) | 4 (4) | **21** (20) |
| 2 Ich bin Studentin | 4 (4) | 4 (4) | 4 (4) | **3** (4) | 3 (3) | **18** (19) |
| 3 Meine Familie | 4 (4) | 4 (4) | **3** (4) | 4 (4) | 3 (3) | **18** (19) |
| 4 Auf dem Flohmarkt | **3** (4) | **3** (4) | 5 (5) | 3 (3) | 3 (3) | **17** (19) |
| 5 Im Klassenzimmer | 5 (5) | 4 (4) | 5 (5) | 3 (3) | 4 (4) | **21** (21) |
| 6 Der erste Tag im Büro | 5 (5) | 4 (4) | 4 (4) | **4** (3) | 3 (3) | **20** (19) |
| 7 Freizeit und Hobbys | 4 (4) | **3** (4) | 5 (5) | **3** (4) | 4 (4) | **19** (21) |
| 8 Termine und Uhrzeit | 4 (4) | 4 (4) | 5 (5) | 4 (4) | 4 (4) | **21** (21) |
| 9 Im Café | 5 (5) | 4 (4) | 5 (5) | **3** (4) | 4 (4) | **21** (22) |
| 10 Am Bahnhof | 5 (5) | 4 (4) | 5 (5) | 3 (3) | 3 (3) | **20** (20) |
| 11 Mein Tag | 4 (4) | 4 (4) | 4 (4) | **4** (3) | **4** (3) | **20** (18) |
| 12 Feste feiern | 5 (5) | 4 (4) | 5 (5) | 3 (3) | 3 (3) | **20** (20) |

Kursmittel **19,7/25** (Runde 4: 19,9 · Runde 3: 20,4 · Runde 2: 19,8 · Runde 1: 17,2).
Vier Lektionen besser, vier schlechter, vier gleich.

**Zur Lesart:** die vier Gewinner sind echte Arbeit dieser Runde. **L11 (18 → 20)** ist
der größte Sprung: die Kann-Beschreibung, die der Lektion ihren Platz im Standard gibt,
wird jetzt in zwei Items, im Pretest und im Modell geübt, und `mitkommen` hat das
Umsteige-Item ersetzt. **L1 (20 → 21)** hat den Trennzeichen-Blocker verloren, **L6
(19 → 20)** die Artikelblindheit. **L4 (19 → 17)** und **L7 (21 → 19)** fallen dagegen
aus einem Grund, den ich benennen will, weil er sich wiederholt: in beiden Lektionen hat
sich in dieser Runde **nichts geändert** — gemessen wurde, was die Ziehung an
Generat-Items zieht, sobald die handgeschriebene Hälfte nicht ausreicht (L4 hat acht
eigene `nouns-gender`-Items, L7 acht `present-tense-regular`-Items, und beide Lektionen
füllen den Rest aus dem Altbestand: `Honig`, `Freiheit`, die Endungsfrage mit der
falschen Prozentangabe, zweimal `arbeiten`). **L3 (19 → 18)** ist die einzige
Verschlechterung, die diese Runde selbst verursacht hat: die eingefügte Dialogzeile.
**Stärkste:** L5, L8, L9 und L1 (je 21) — L8 zum zweiten Mal ohne einen einzigen
eigenen Befund, mit 7/7 gedeckten, ehrlich gemessenen Items.
**Schwächste:** L4 (17) — ein falscher Grammatiksatz, zwei ungelehrte Nomen und der
Satzbau-Blocker, alle drei aus derselben Quelle: zu wenig eigene Items.

---

## Würde ich meinen Namen jetzt daraufsetzen?

**Noch nicht — aber zum ersten Mal in dieser Reihe steht kein alter Befund mehr offen,
und das ist ein anderer Zustand als in allen vier Runden davor.** Ich habe alle
vierzehn Punkte aus Runde 4 an den Artefakten nachgeprüft: drei Blocker geschlossen
(einer vollständig als Regel, einer als Regel mit einem Restbereich, einer halb), elf
MAJOR geschlossen, davon neun vollständig. Die Arbeitsweise, die Runde 4 angefangen
hat, ist jetzt der Normalfall: `normalizeSpelling`, `articleCueOnlyInGloss`,
`EQUIVALENT_VERBS`, `isCaseTask` und die vier Validator-Regeln sind Prädikate mit Tests
über den ganzen Pool, keine ID-Listen — und `EQUIVALENT_VERB_IDS = {}` mit dem Kommentar
„Empty is the honest state" ist der Satz, auf den diese Reihe seit Runde 2 hinarbeitet.

**Was offen bleibt, ist zum fünften Mal dieselbe Kante derselben Regeln.** Runde 4 hat
geschrieben: „eine Regel, die eine Klasse an ihrem Vokabular erkennt statt an ihrer
Form, schließt eine Instanz und nicht die Klasse." Runde 5 hat die Regel geschrieben —
und sie an `type === 'fill_blank'` gebunden, also an die Bauform der Instanz, die Runde
4 gefunden hatte. Deshalb steht der Artikel bei drei Satzbauitems weiter nur im
Englischen, und eines davon ist die erste Aufgabe von Lektion 4 und zwei davon stehen im
benoteten Checkpoint 2. Dasselbe beim Fallschalter: die Frage lautet nicht „sieht die
Antwort aus wie `Ihr`", sondern „ist die Schreibung hier die Aufgabe" — das erste
Prädikat trifft ein Item, das keine Höflichkeitsform meint, und verfehlt zwei, die es
tun. **Die Lehre dieser Runde ist deshalb enger als die letzte: eine Regel muss an der
Frage hängen, die sie beantwortet, nicht am Feld, in dem die letzte Instanz gefunden
wurde.** Konkret prüfbar: jede der drei Regeln oben lässt sich als ein Satz formulieren,
der nirgends einen Itemtyp nennt — „der deutsche Prompt bestimmt die Antwort", „die
Aufgabenformel und die akzeptierte Antwort haben dieselbe Form", „eine Antwort wird
überall im Kurs gleich bewertet". Der dritte Satz ist der, den ich am meisten vermisse:
`checkAnswer` wird an drei Stellen gerufen und an dreien mit verschiedenen Optionen.

Der zweite Satz, der aus dieser Runde mitgeht, ist der alte in neuer Gestalt: **die
Fläche, die noch niemand gebaut hat, ist die nächste Fehlerquelle.** Ich habe in dieser
Runde zum ersten Mal alle vier Checkpoints wirklich gebaut und ihre 80 Items gelesen.
Dort steht der Lösungsschlüssel Richtig–Falsch–Richtig–Falsch viermal hintereinander,
dort ist der Schreiben-Abschnitt Grammatikdrill mit einem erfundenen Textsorten-Etikett,
dort werden Items als „chapter" geführt, deren Wortschatz drei Lektionen später kommt,
und dort ist dasselbe Item richtig, das die Lektion falsch wertet. Nichts davon ist in
vier Reviews aufgefallen, weil niemand `buildCheckpoint()` aufgerufen hat — so wie bis
Runde 3 niemand `planPractice()` aufgerufen hatte. Der Kurs ist genau so gut, wie er
gemessen ist.

Die Arbeit ist überschaubar: ein Prädikat, das nach der Form statt nach dem Typ fragt,
zwei `accepted`-Listen, drei Zeilen `caseSensitive: isCaseTask(item)` an den beiden
anderen Aufrufstellen, eine Zeile `seededShuffle` im Lesen-Generator. Das ist ein halber
Tag. Danach unterschreibe ich für alle zwölf.

---

**Minors (fünf Zeilen, nicht erfasst):** (1) Die Buchstabenkarte schreibt weiter `Q ku`
statt `Qu` und mischt Groß-/Kleinschreibung (`be, ce, de` neben `Jot, Vau, Zett`; im
Fehlerbeispiel `We`, in der Liste `we`); kein Item prüft Q. (2) **23** Items zeigen den
deutschen Prompt ein zweites Mal als „Glosse" (`0fe8146d`, `dd1c3d60` sind in der
Ziehung), und die Karte `verb-sein` schreibt „Der Wohnort: Ich bin in Bremen." — der
eigene Dialog sagt „Ich **wohne** in Bremen". (3) Drei Items heißen *Anna* neben der
Kursfigur *Ana*, zwei davon im Schreiben-Abschnitt von Checkpoint 4; dazu die sonst
namenlosen *Tom*, *Frau Müller*. (4) `extra-a11-l08-06` („___ Abend habe ich Zeit." →
`Am`) hat als einziges Präpositionsitem keine Aufgabenformel, während sein Zwilling
`8970aaea` „(Präposition für Tageszeit)" trägt — *Heute Abend* und *Jeden Abend* sind
beide `wrong`. (5) `situation` von L9 trägt weiter die Redaktionsnotiz „(die Einladung
kommt in Lektion 12)", und `tests/lesson-extra-items.test.mjs:173–230` hält eine zweite
Kopie von `DRILLS`, die bei der nächsten Änderung von `quality.js` auseinanderläuft.

BLOCKER: 3 · MAJOR: 11
