
# A1.1 — DaF-Fachreview #7, nach Runde 7 (2026-09-12)

Reviewer: DaF-Lehrkraft (Erwachsenenbildung, Integrationskurs) und Prüferin für
Goethe-Zertifikat A1 *Start Deutsch 1*. Siebter Durchgang. Basis:
`REVIEW-daf-2026-09-12.md` (5 BLOCKER · 45 MAJOR), `REVIEW-daf-2-2026-09-12.md`
(1 BLOCKER · 35 MAJOR · 44 MINOR), `REVIEW-daf-3-2026-09-12.md` (3 BLOCKER · 11 MAJOR),
`REVIEW-daf-4-2026-09-12.md` (3 BLOCKER · 11 MAJOR), `REVIEW-daf-5-2026-09-12.md`
(3 BLOCKER · 11 MAJOR) und `REVIEW-daf-6-2026-09-12.md` (3 BLOCKER · 7 MAJOR),
angewendet in `origin/main` @ `223c6bd` („A1.1 round 7: close DaF review #6;
A1.2 round 2", PR #123). Gemessen wurde ausschließlich der Commit-Stand in einem
sauberen Worktree; die unversionierte A1.2-Arbeit im Arbeitsbaum ist nicht
Gegenstand dieses Reviews.

Gemessen, nicht angenommen:

* `src/data/curricula/a11.js` — alle zwölf Lektionen, jedes Feld (situation,
  handlungsfeld, canDo, examTeile, wortfeld inkl. `wordId`, dialog, pretest, notice,
  phonetik, hoeren, sprechen inkl. `anrede`, schreiben, links, practiceRule inkl.
  des neuen `mustCover`).
* **Die echte Ziehung**: `planPractice(CURRICULUM_A11, a11.json, 1)` **und Versuch 2**
  importiert, alle sieben Items jeder Lektion mit
  `questionDe`/`questionEn`/`options`/`answer`/`accepted`/`explanationDe`/`caseSensitive`
  und dem von `checkOptionsFor(item)` abgeleiteten Optionsobjekt ausgedruckt und
  einzeln bewertet (**168 Items**, 102 verschiedene). Pool: **289** Items — nach dem
  Never-taught-Lexis-Gate 62 weniger als in Runde 6 —, davon **140** handgeschrieben,
  und **alle 140** sind im gebauten Pool angekommen (Runde 6: 124).
* Alle **140** Items in `src/data/lessonPools/a11.extra.json` einzeln gelesen.
* `drillsSlug()` aus `src/data/lessonPools/quality.js` über beide Ziehungen laufen
  lassen — **mit dem tatsächlichen Primärslug** jeder Lektion, nicht mit `undefined`
  (`drillsSlug` gibt für einen unbekannten Slug `true` zurück, eine Messung ohne
  Slug misst also nichts).
* `checkAnswer` + `tagError` + `checkOptionsFor` aus `src/lib/lesson/check.js` auf
  jedes gezogene und jedes Checkpoint-Item mit je drei bis sechs plausiblen
  Lernerantworten nachgerechnet; zusätzlich ein poolweiter Kreuzvergleich
  Lektion ↔ Checkpoint (**0 Abweichungen**) und ein Selbsttest „jede akzeptierte
  Antwort wird akzeptiert" (**0 Selbstablehnungen**, Lektion wie Checkpoint).
* `exclusionReason()`, `politeCaseItem()`, `ambiguousCorrection()` über beide
  Pooldateien: **0** Ausschlussgründe, **0** mehrdeutige Fehlerkorrekturen.
* Die zwölf A1.1-Karten in der gebauten `netlify/functions/_shared/ruleCards.mjs`.
* Die zwölf Kursaufgaben in `src/data/writingTasks.js` gegen `schreiben`, gegen
  `netlify/functions/_shared/writingTasks.mjs` und gegen
  `netlify/functions/evaluate-writing.mjs` (`courseTaskKeyPrefix`, `MIN_CHARS`,
  `COURSE_WRITING_FREE_LIFETIME`, `WRITING_LIMITS`, `getTier`).
* `buildMessage()` in `netlify/functions/course-reminder.mjs`.
* Die Chrome: **alle** `src/components/lesson/*.jsx` und `src/pages/lesson/*.jsx`
  zeilenweise gegen die du-Tokenliste des Registertests — und den Registertest selbst.
* Der Weg des Sprechauftrags **samt Anrede** bis in den System-Prompt
  (`SpeakingStage` → `SpeakingPage` → `normalizeCourseTask` → `courseTaskColumns` →
  `taskFromSession` → `buildTeacherSystemPrompt`).
* **Alle vier Checkpoints gebaut**, alle 80 Items ausgedruckt und einzeln bewertet,
  inklusive Lösungsschlüssel, Falsifikation, Lexikfilter, benoteter Schreibaufgabe,
  `scoreCheckpoint`, `itemCounts`/`itemIsScored` und `remediationSet`.
* `buildCardIndex()` aus `src/services/reviewService.js`: **395** Karten, jede
  gegen `gradeTypedReview()` mit der klein geschriebenen Variante ihrer eigenen
  Antwort geprüft.
* `node scripts/validate-curriculum.mjs` (RULE 10: 15/262 · RULE 11: 61 informativ ·
  RULE 11b: 3 · RULE 12: 4 · RULE 13: 4 · RULE 6b: 26 · RULE 15: 12 · RULE 16: 3 ·
  RULE 14: 0) und die Implementierung jeder Regel, nicht nur ihre Ausgabe.
* `npm test`: **724/724 grün**. `npm run lint`: 0 Fehler, 0 Warnungen.
  `npm run check:duplicates`: OK.

## Urteil in einem Absatz

**Runde 7 hat mehr geschlossen als jede Runde vor ihr — alle drei Blocker und alle
sieben MAJOR aus Runde 6 sind an den Artefakten nachweisbar erledigt, und zwei
davon sind die besten Regeln, die dieses Projekt bisher geschrieben hat.** Der
Never-taught-Lexis-Ausschluss im Build nimmt 62 Altbestandsitems aus dem Pool, RULE 11
fällt von 187 auf 61 und bekommt mit **RULE 11b** endlich die Zahl, die zählt — drei
ungelehrte Tokens in der **gezogenen** Lektion, und alle drei sind harmlos
(zweimal das Wort *Deutsch*, einmal ein rezeptives `arbeitet`); `checkOptionsFor(item)`
bündelt alle vier Prüfoptionen am Item, wird an allen vier Bewertungsstellen
aufgerufen und von einem Test bewacht, der eine handgebaute Optionsliste an der
Aufrufstelle verbietet — nachgerechnet: `a1.1-cp2-hoeren-1` nimmt jetzt jede
Trennzeichenform, und über alle 80 Checkpoint- und 102 gezogenen Items gibt es
**null** Bewertungsabweichungen zwischen Lektion und Test; `ambiguousCorrection()`
ist ein sauberes Prädikat, `extra-a11-l05-09` trägt seine Aufgabenformel;
L4 hat zehn eigene Items bekommen und zieht in beiden Versuchen **ausschließlich**
aus dem eigenen Wortfeld; L12 zieht dank `mustCover: ['Ihr']` und
`MAX_SAME_ANSWER_KEY` in **beiden** Versuchen die Höflichkeitsform und vier bis
fünf verschiedene Besitzer; `extra-a11-l10-06` akzeptiert kein `Gehst` mehr; die
Zahlwörter haben ihr eigenes Topic. **Und trotzdem unterschreibe ich nicht, weil
die drei schwersten Befunde dieser Runde zum siebten Mal aus derselben Quelle
stammen — nur sitzt die Quelle diesmal nicht mehr in der Bewertung, sondern in dem,
was *neben* der geprüften Regel steht.** Erstens ist aus `caseSensitive` ein
Prädikat geworden, aber es ist **genau der Formregex, den Runde 5 aus `isCaseTask`
entfernt hat**, eine Ebene höher wieder eingebaut: `politeCaseItem()` liest die
Gestalt der Antwort, deshalb trifft es drei Items, deren eigene Erklärung
„**informell**" sagt, und es trifft **nicht** `Sie`/`Ihnen` im Satz — **15
Wiederholungskarten** bleiben fallunempfindlich, darunter genau die Karte
„Gut. Wie geht es Ihnen?", die der Kommentar in `reviewService.js` als repariert
**benennt**: das Lektionsitem wertet `ihnen` **wrong**, die Karte wertet es
**typo = richtig**. Zweitens rechnet der Kurs gegen sich selbst: die
Eigentümerentscheidung „zwölf lebenslang, **eine pro Lektion**" steht unverändert in
`evaluate-writing.mjs`, während Runde 6 **vier weitere** Schreibaufgaben auf
**denselben** `task_key`s hinzugefügt hat — 16 nötige Einreichungen gegen ein Limit
von 12, also bekommt jeder kostenlose und jeder abgelaufene Lernende auf die
benotete Schreibaufgabe **aller vier** Checkpoints ein `429`, und weil die Aufgabe
`optional: true` ist, verschwindet sie lautlos aus der Wertung. Drittens steht in
`a1.1-cp4-bausteine-4` des **benoteten Abschlusscheckpoints** die Form `deinen` —
ein Item, dessen eigene Erklärung sie „Vorschau auf den Akkusativ (**A1.2**)" nennt;
wer `dein` schreibt, also genau das, was die notice und die Regelkarte von L12
lehren („maskulin ohne Endung"), bekommt **falsch**, und der Fehler wird ihm als
**Rechtschreibung** angezeigt. Dazu fünf MAJOR, deren Schwerpunkt wieder gewandert
ist: nicht mehr die Ziehung, sondern **die Prüfung neben der Prüfung** — ein
Registertest, der seine Dateien aus einer Handliste nimmt und deshalb
`PracticeItem.jsx` nicht liest, wo seit jeher „**Deine** Antwort" steht; eine
Kongruenzschranke im Lesen-Generator, die die Morphologie sichert und die Semantik
nicht („Spielst du jede **Kellnerin** Fußball?"); eine RULE 16, die nur `links`
liest und deshalb zwei Messartefakte und einen echten Befund gleich laut meldet.
Ein Prädikat mit Satzgrenze, eine Zahl in einer Umgebungsvariablen, ein Item aus dem
Checkpoint, ein Glob statt einer Liste, drei Zeilen im Falsifikator und ein Blick
auf L6 — dann unterschreibe ich.

**Zahlen:** 3 BLOCKER · 5 MAJOR. Minors sind nicht erfasst (fünf Zeilen am Ende).

---

## BLOCKER

| Feld | Zitat | Problem | Fix (pasteable) | Schwere |
|---|---|---|---|---|
| `politeCaseItem()` in `src/data/lessonPools/quality.js:571–576` ↔ `caseFlag()` in `src/services/reviewService.js:148–149` ↔ `isCaseTask()` in `src/lib/lesson/check.js` | `if (acc.every((a) => !a.includes(' '))) return acc.every((a) => POLITE_FORM_RE.test(a));` · `return acc.every((a) => a.split(/\s+/).slice(1).some((w) => /^Ihr(e\|en\|em\|er\|es)?$/.test(w.replace(/[.,!?]/g, ''))));` · Kommentar in `reviewService.js:136–139`: „die L1-Satzkarte „Gut. Wie geht es Ihnen?" **verzieh** `ihnen`, obwohl das Lektionsitem für dieselbe Form (`extra-a11-l01-06`) sie falsch wertet" | **Der Blocker aus Runde 6 wurde als Prädikat geschlossen — aber als genau der Formregex, den Runde 5 aus `isCaseTask` entfernt hat, eine Ebene höher.** `check.js` schreibt selbst auf, warum das nicht geht: „ein Regex auf die Antwortform kann ein höfliches `Ihr` nicht von einem großgeschriebenen Satzanfang unterscheiden." `politeCaseItem` tut genau das, und beide Fehlerrichtungen sind gemessen. **Falsch positiv:** `dd1c3d60` („___ ist meine Mutter.", `['Sie']`, Erklärung „Mutter = weiblich → **sie**"), `b19be5e4` („Die Tasche ist teuer. ___ ist schön.", Erklärung „deshalb wird sie zu **sie**") und `4299d5ca` („___ seid meine Freunde.", `['Ihr']`, Erklärung „**Ihr** spricht mehrere Personen **informell** an", `questionEn`: „= you all, **informal** plural") tragen jetzt `caseSensitive: true` — drei Items, deren eigener Text sagt, dass hier nichts Höfliches steht. **Falsch negativ, und das ist die schwere Hälfte:** die Satzklausel ist auf `Ihr…` verengt, also erreicht sie `Sie` und `Ihnen` im Satz **nie**. `buildCardIndex()` liefert 395 Karten; **15** davon führen eine höfliche Form und sind fallunempfindlich, gemessen mit `gradeTypedReview`: `sentence:a1.1-l01:6` „Gut. Wie geht es **Ihnen**?" → `ihnen` = **typo, ok: true**, während `extra-a11-l01-06` mit derselben Form **wrong** gibt; dazu `sentence:a1.1-l01:2` und `pattern:alphabet-pronunciation` („Buchstabieren **Sie** bitte Chakiri."), `sentence:a1.1-l02:2` („Bitte füllen **Sie** das Formular aus."), `pattern:verb-sein`, `pattern:numbers`, `sentence:a1.1-l02:6` („Was sind **Sie** von Beruf?"), `pattern:verb-haben`, `sentence:a1.1-l09:0/2/4` und `word:079b8258` („Wie geht es **Ihnen**?"). Der Kommentar in `reviewService.js` benennt die L1-Karte als den Grund, warum das Prädikat eingeführt wurde — und sie ist der Beleg, dass es sie nicht erreicht. Eine Regel wird damit im Kurs weiterhin an zwei Stellen verschieden bewertet, nur ist die Bruchlinie jetzt die Wortstellung der Antwort. | Die Frage stellen, die die Regel beantwortet: *steht die höfliche Form dort, wo der Großbuchstabe eine Entscheidung ist — also **nicht am Satzanfang**?* In `quality.js`: `export const POLITE_CUE_RE = /Höflichkeitsform\|höflich\|formell\|Formal address\|\(Sie[,)]/;` und `export function politeCaseItem(item) { const acc = [item?.answer, ...(item?.accepted \|\| [])].map((a) => String(a ?? '').trim()).filter(Boolean); if (!acc.length) return false; if (acc.some((a) => acc.some((b) => a !== b && a.toLowerCase() === b.toLowerCase()))) return false; const cue = POLITE_CUE_RE.test([item?.questionDe, item?.questionEn, item?.explanationDe].join(' ')); const midSentence = acc.some((a) => a.split(/(?<=[.!?])\s+/).some((s) => s.split(/\s+/).slice(1).some((w) => POLITE_FORM_RE.test(w.replace(/[.,!?]/g, ''))))); return midSentence \|\| (cue && acc.every((a) => POLITE_FORM_RE.test(a))); }` — die Satzgrenze (`(?<=[.!?])`) ist das ganze Argument: „**Sie** kostet acht Euro." nach einem Punkt ist ein Satzanfang, „Wie geht es **Ihnen**?" nicht. Nachgerechnet gegen den heutigen Bestand: die drei falsch positiven Items fallen weg (keine Cue, Einwortform ohne Cue), `extra-a11-l12-10` (`['Ihre','ihre']`) und `210e89f8` bleiben draußen (Doppelschreibung bzw. „(sie, Plural)"), `extra-a11-l12-16` bleibt drin (Cue „höfliches Ihr" in der Erklärung), und die 15 Karten kommen herein. Dazu ein Test in `tests/checkpoint.test.mjs`, der **nicht** eine Liste nennt: für **jede** Karte aus `buildCardIndex(CURRICULUM_A11)` und **jedes** Item des gebauten Pools muss gelten `gradeTypedReview(key, accepted, lower(accepted[0]), { caseSensitive }).ok === (checkAnswer(lower(...), accepted, checkOptionsFor(item)).result !== RESULT.WRONG)` — dieselbe Form wird überall gleich bewertet, und **ob** die Großschreibung zählt, entscheidet die Stellung der Form, nicht die Datei, in der der Lernende sie trifft. | BLOCKER |
| `COURSE_WRITING_FREE_LIFETIME` in `netlify/functions/evaluate-writing.mjs:38` gegen `gradedWritingItem()` / `chapterWritingTask()` in `src/lib/checkpoint/buildCheckpoint.js:654–700` | `const COURSE_WRITING_FREE_LIFETIME = 12;` · Kommentar: „Owner decision 2026-09-12: **zwölf lebenslang, eine pro Lektion**." · `usedQuery.like('task_key', courseTaskKeyPrefix(task_key) + '%')` · `courseWritingTasks('a1.1').length === 12` · die vier Checkpoint-Aufgaben posten `a11-l03`, `a11-l06`, `a11-l09`, `a11-l12` | **Der Kurs verlangt sechzehn Einreichungen und erlaubt zwölf — und niemand bemerkt es, weil der Fehlschlag als „nicht versucht" gewertet wird.** Die Eigentümerentscheidung wurde getroffen, als der Kurs zwölf Schreibflächen hatte. Runde 6 hat vier weitere gebaut — die benotete Kursaufgabe in jedem Checkpoint — und sie posten über `GradedWriting.jsx:94–97` an dieselbe Funktion mit **demselben** `task_key` wie die Lektion. Die Zählung ist `task_key LIKE 'a11-%'`, lebenslang. Nachgerechnet an den committeten Konstanten: ein Lernender im Tier `free_trial` oder `free_expired` bekommt `courseAllowance = true` und damit `limit = 12`; nach der zwölften Lektion ist das Kontingent erschöpft, und jede der vier Checkpoint-Aufgaben antwortet `429 limit_reached`. Ein zahlender Lernender mit Abo (`pro`) fällt auf 20 **pro Monat** und ist nicht betroffen — betroffen ist genau die Gruppe, für die die Seite den Kurs als **kostenlos** bewirbt (so der Kommentar in derselben Datei), und jeder Kurskäufer ohne Abo, der als `free_expired` geführt wird. Die Folge ist unsichtbar: `itemCounts()` nimmt ein `optional: true`-Item ohne Graderverdikt aus dem Abschnitt, der Schreiben-Teil schrumpft von drei auf zwei Items, und die zwei verbleibenden sind genau die Grammatikdrills, die Runde 6 als „Grammatikdrill mit erfundenem Textsorten-Etikett" abgeschafft hatte. Der Prüfungsteil, den Runde 6 als ihre beste Arbeit bezeichnet, ist für den Normalfall des Kurses arithmetisch nicht erreichbar, und kein Test, kein Log und kein Bildschirm sagt es. | Die Zahl aus der Lektionenzahl **ableiten**, nicht neben ihr führen — derselbe Satz, den `CLAUDE.md` für Preise und Claims schreibt. In `evaluate-writing.mjs`: `import { courseWritingTasks } from './_shared/writingTasks.mjs';` und `const courseAllowanceFor = (taskKey) => { const level = LEVEL_OF_PREFIX[courseTaskKeyPrefix(taskKey)]; const n = courseWritingTasks(level).length; return n + CHECKPOINTS_PER_COURSE; };` mit `export const CHECKPOINTS_PER_COURSE = 4;` — also „eine pro Lektion **plus eine pro Checkpoint**", 16 für A1.1, und die Zahl wächst mit dem Kurs statt neben ihm. `src/data/marketing.js` liest sie weiterhin aus dieser Datei, `tests/claims.test.mjs` parst sie weiterhin heraus. Dazu der Test, der die Klasse hält, in `tests/writing-course.test.mjs`: für jedes Level mit einem Curriculum muss `courseAllowanceFor('<prefix>l01') >= courseWritingTasks(level).length + curriculum.checkpoints.filter((cp) => chapterWritingTask(chapterLektionen(curriculum, cp), level)).length` gelten — **der Kurs darf nie mehr Schreibflächen haben, als sein Kontingent hergibt.** Und in `GradedWriting.jsx` den `429`-Fall sichtbar machen (heute fällt er in denselben stillen Zweig wie ein Netzfehler): eine Zeile „Ihr Schreibkontingent für diesen Kurs ist aufgebraucht." statt eines leeren Feldes. | BLOCKER |
| `03bd1113-6ab0-589d-b0d6-12f03d5c1952` als **`a1.1-cp4-bausteine-4`** (gebauter Checkpoint 4, Sprachbausteine) ↔ `notice` von L12 in `src/data/curricula/a11.js` ↔ `tagError()` in `src/lib/lesson/check.js` | `promptDe: 'Hast du ___ Schlüssel? (du — **Vorschau Akkusativ**)'` · `answer: 'deinen'` · `explanationDe: 'Vorschau auf den Akkusativ (**A1.2**): nach haben bekommt maskulin die Endung -en: deinen, nicht dein.'` · notice L12: „**mein** Geschenk, **dein** Bruder … Vor femininen Nomen und im Plural kommt **-e** dazu" · Regelkarte `possessive-articles`: „Das -e steht **nur** vor femininen Nomen und im Plural." | **Der benotete Abschlusstest prüft eine Form, die der Kurs ausdrücklich dem nächsten Niveau zuschreibt — und bestraft die Antwort, die seine eigene Regelkarte lehrt.** Das Item ist eines von **sechs** Sprachbausteinen in Checkpoint 4, also 16,7 % eines Abschnitts, dessen Bestehensgrenze bei 40 % liegt. `topic: 'possessive-articles'` fällt unter `STRICT_TOPIC`, es gibt also keine Typo-Toleranz. Nachgerechnet mit `isItemCorrect`: `deinen` → **true**; `dein` → **false**; `deine` → **false**; `Dein` → **false**. „`dein` Schlüssel" ist genau das, was L12 für ein maskulines Nomen lehrt (die notice nennt sechs Possessiva und eine Endungsregel, den Akkusativ nennt sie **nicht**; die einzige Stelle im ganzen Kurs, die `-en` erklärt, ist die notice von L6 — und die spricht von `ein → einen` und sagt selbst: „die Regel kommt in **A1.2**"). Der Lernende bekommt also im Abschlusstest ein Rot auf die Regel, die er richtig angewendet hat. Zweiter Befund am selben Item: `tagError(item, 'dein', 'deinen')` liefert **`Rechtschreibung`** — `ARTICLES` kennt `dein` und `deine`, aber nicht `deinen`, also greift keine Artikelklausel, und die Levenshtein-Distanz 2 entscheidet. Ein Kasusfehler wird dem Lernenden als Schreibfehler angezeigt und geht so in `errorTags` und in die Chips von `CheckpointPage:416` ein. Der Zwilling `64680d9b` („Ich habe ___ Bruder gern. (ich — Vorschau Akkusativ)" → `meinen`) steht unverändert im Pool und ist über `practiceRule.topics` von L12 ziehbar — das ist keine Instanz, das ist eine Klasse. | Die Regel, die fehlt, ist die Umkehrung von RULE 6b: **kein gezogenes und kein Checkpoint-Item darf eine Form verlangen, die keine notice und keine Regelkarte des Kurses einführt.** In `scripts/build-lesson-pool.mjs` ein Ausschlussgrund neben dem Lexis-Gate: `REASON.UNTAUGHT_FORM` mit `const NEXT_LEVEL_RE = /Vorschau\|A1\.2\|kommt in A1/i;` und `if (NEXT_LEVEL_RE.test([item.questionDe, item.explanationDe, item.hint].join(' '))) return REASON.UNTAUGHT_FORM;` — ein Item, das sich selbst als Vorgriff bezeichnet, gehört nicht in den Pool, aus dem gezogen und geprüft wird; beide Zwillinge fallen damit heraus, und Checkpoint 4 zieht an dieser Stelle das nächste `possessive-articles`-Item (`extra-a11-l12-05`, „___ Gäste kommen um acht. (ich)" → `Meine`, Plural-Endung, gedeckt). In `check.js` zusätzlich die Tag-Lücke schließen: `ARTICLES` um die Akkusativ-/Dativformen der Possessiva ergänzen (`'meinen','deinen','seinen','ihren','unseren','euren','meinem','deinem','seinem','ihrem','Ihren','Ihrem'`), damit `articleTag()` greift und ein Endungsfehler **Kasus** heißt. Test in `tests/lesson-pool-rules.test.mjs`: **kein** Item des gebauten Pools trägt `NEXT_LEVEL_RE` in Prompt, Hinweis oder Erklärung — und in `tests/checkpoint.test.mjs` über **alle 80** Items derselbe 0-Test. | BLOCKER |

---

## MAJOR

| Feld | Zitat | Problem | Fix (pasteable) | Schwere |
|---|---|---|---|---|
| `src/components/lesson/PracticeItem.jsx:138` gegen `CHROME_FILES` in `tests/course-player.test.mjs:76–94` | `<label htmlFor={'answer-' + item.id} …>` **`Deine Antwort`** `</label>` · dagegen `src/pages/lesson/CheckpointPage.jsx:210`: `<label className="sr-only" …>` **`Ihre Antwort`** `</label>` · `CHROME_FILES` zählt dreizehn Komponenten **namentlich** auf und globt danach nur `src/pages/lesson/*.jsx` | **Der meistgesehene Text des Kurses duzt — und der Test, der das verhindern soll, liest die Datei nicht, weil sie in einer Handliste fehlt.** `PracticeItem.jsx` rendert jedes getippte Übungsitem: sieben pro Lektion, zwölf Lektionen, also **84 Bildschirme**, plus jede Wiederholung. Über der Eingabe steht „Deine Antwort", während `sprechen.open.anrede` in **allen zwölf** Lektionen `'Sie'` ist, der Sprechcoach per System-Prompt siezt, jede Aufgabenstellung siezt („Bilden Sie den Satz", „Korrigieren Sie") und der Checkpoint an derselben Stelle „**Ihre** Antwort" schreibt. Zwei Bildschirme desselben Kurses, zwei Register, zwei Zeilen voneinander entfernt. Runde 6 hat diesen Befund als MAJOR 7 **geschlossen** und dabei gemessen: „Nachgemessen mit einem eigenen Grep über beide Verzeichnisse: **null** Fundstellen." Nachgerechnet: die du-Tokenliste des Tests (`/\b(du\|Du\|…\|Deine[mnrs]?\|…)\b/`) trifft „Deine Antwort" einwandfrei — sie wird nur nie darauf angewendet, weil `CHROME_FILES` die dreizehn Komponenten **aufzählt** und `PracticeItem.jsx` nicht darunter ist (die Datei steht in derselben Testdatei, aber in `GRADING_SITES`, Zeile 112). Das ist derselbe Satz, den Runde 6 als ihre Lehre aufgeschrieben hat, in der Testschicht: eine Regel, die eine Liste ist, prüft nur, woran der letzte Bericht gedacht hat. | Zwei Zeilen, eine am Text, eine an der Regel. Am Text: `Ihre Antwort` in `PracticeItem.jsx:138` (und den `placeholder` `'Ganzer Satz'` unverändert lassen — er ist registerneutral). An der Regel, in `tests/course-player.test.mjs`: `const CHROME_FILES = [ ...readdirSync(join(ROOT, 'src/components/lesson')).filter((f) => f.endsWith('.jsx')).map((f) => 'src/components/lesson/' + f), ...readdirSync(join(ROOT, 'src/pages/lesson')).filter((f) => f.endsWith('.jsx')).map((f) => 'src/pages/lesson/' + f), 'src/pages/SpeakingPage.jsx', ];` — **jede** Datei der beiden Verzeichnisse, keine Aufzählung; eine neue Komponente ist damit ab ihrem ersten Commit abgedeckt. Wenn eine Ausnahme nötig wird (heute keine), gehört sie als benannte `REGISTER_EXEMPT`-Konstante mit Begründung daneben, nicht als Lücke in der Aufzählung. | MAJOR |
| `changedDetail()` in `src/lib/checkpoint/buildCheckpoint.js:441–460`, `vocab`-Zweig — gebauter Checkpoint 3, Lesen 2 | `if (allowVocab && vocab.has(word)) { const { article } = vocab.get(word); const same = [...vocab.keys()].filter((w) => w !== word && vocab.get(w).article === article); return same.length ? pickOther(same, word, rng) : null; }` · erzeugt `promptDe: 'Steht das im Text? „Tim: Spielst du jede **Kellnerin** Fußball?“'` gegen den Text „… Spielst du jede **Woche** Fußball? …" | **Die Kongruenzschranke ist eingebaut und tut genau, was Runde 6 verlangt hat — sie sichert die Morphologie und lässt die Semantik offen.** `die Woche` und `die Kellnerin` tragen beide `article: 'die'`, der Tausch ist also formal einwandfrei: „jede Kellnerin" ist grammatisch korrektes Deutsch, anders als das „jede Frühstück" der Vorrunde. Nur ist „Spielst du jede Kellnerin Fußball?" keine **falsche Aussage über den Text**, sondern ein kaputter Satz — und der Kommentar über der Funktion nennt selbst den Maßstab: „eine falsche Aussage muss falsch sein, nicht kaputt". Der Lernende löst das Item wieder, ohne den Text zu lesen, nur diesmal über die Bedeutung statt über die Endung. Ausgezählt über alle vier gebauten Checkpoints: von **acht** `Falsch`-Aussagen sind sieben saubere Detailänderungen (`sechs`→`eins`, `zwanzig`→`siebzig`, `Ana`→`Lena`, `Null`→`Sechs` — der Zifferngruppenzweig ist jetzt richtig auf einstellige Zahlwörter beschränkt —, `Mittwoch`→`Sonntag`, `zwanzig`→`drei`, `vier`→`achtzig`) und **eine** ist dieser Satz. Der Ursprung ist derselbe wie vorher: `jede Woche` ist eine adverbiale Zeitangabe im Akkusativ, `Woche` steht dort nicht als Sache, sondern als Zeiteinheit — und die Wortfeld-Map kennt nur `article` und `plural`, also kann sie Zeit nicht von Ding unterscheiden. Der Test in `tests/checkpoint.test.mjs:230` prüft exakt die Artikelgleichheit und ist deshalb grün. | Den Zweig dort nicht greifen lassen, wo das Nomen keine Sache ist — erkennbar an seinem Determinativ, und das steht im Fenster. In `changedDetail()` eine Vorbedingung aus dem Aufrufer durchreichen: `changedDetail(word, ctx, rng, { allowVocab, digitGroup, determiner })`, wobei `determiner` das Wort links vom Treffer ist, und dann `if (allowVocab && vocab.has(word)) { if (/^(jede\|jeden\|jedes\|alle\|allen)$/i.test(String(determiner \|\| ''))) return null; … }` — ein Nomen nach `jede/jeden/jedes/alle` ist eine Zeit- oder Mengenangabe, kein Gegenstand, und ein Tausch dort ist nie eine Detailänderung. Der Aufrufer überspringt bei `null` bereits sauber, und der Zahlen-, Wochentag- und Namenszweig deckt sieben der acht Aussagen ohnehin ab; für Kapitel 3 rückt dann der Wochentag- oder Zahlentausch aus demselben Text nach. Test in `tests/checkpoint.test.mjs` neben dem bestehenden Artikeltest: jede `Falsch`-Aussage aller vier Checkpoints unterscheidet sich in genau einem Wort **und** dieses Wort steht in keinem `jede/jeden/jedes/alle`-Syntagma. | MAJOR |
| `examTeile` ↔ `links` in `src/data/curricula/a11.js`, gelesen von RULE 16 in `scripts/validate-curriculum.mjs` | Ausgabe: „RULE 16 ungedeckte Prüfungsteile: 3 (Ratchet 3) — L2 „Lesen Teil 1" — `links.readingOrder is null` · L5 „Hören Teil 1" — `links.listeningExercise is null` · L11 „Hören Teil 3" — `links.listeningExercise is null`" · dagegen L5: `hoeren: { kind: 'dictation', lines: [1, 5] }`, L11: `hoeren: { kind: 'dictation', lines: [3, 7] }` | **Zwei der drei Befunde sind Messartefakte, einer ist echt — und weil die Regel sie gleich laut meldet, sieht der Ratchet 3 wie ein Restposten aus, den man stehen lassen kann.** RULE 16 fragt nur nach `links.listeningExercise` bzw. `links.readingOrder`, also nach der **Verknüpfung mit einer externen Übung**. L5 und L11 haben aber je eine eigene Hörfläche: das Diktat über zwei Dialogzeilen, dieselbe Fläche, aus der die Checkpoints ihren Hören-Teil bauen. Für die Deckung von „Hören Teil 1"/„Hören Teil 3" ist das die relevante Übung, und die Regel sieht sie nicht — sie misst am falschen Feld, genau wie RULE 11 in Runde 6 an der falschen Lektion gemessen hat. **L2 dagegen ist echt:** die Lektion behauptet auf der Syllabusseite „Lesen Teil 1", hat `readingOrder: null`, keine Lesefläche im Neun-Schritte-Ablauf und keine einzige Zeile, die der Lernende als Text **liest** statt als Dialog zu hören — während *Start Deutsch 1* in Lesen Teil 1 zwei kurze Alltagstexte mit Richtig/Falsch prüft. Der Kurs verspricht dem zahlenden Lernenden an dieser Stelle eine Prüfungsvorbereitung, die die Lektion nicht enthält. Das ist derselbe Claim-ohne-Beleg, den `CLAUDE.md` für Preise und Nutzerzahlen verbietet, nur auf der Kursseite. | Die Regel auf die Frage umstellen, die sie beantwortet — *hat die Lektion eine Fläche für diesen Prüfungsteil?* — und dann den einen echten Befund reparieren. In `validate-curriculum.mjs`, in der Deckungsfunktion von RULE 16: `const covers = { 'Hören': (l) => Boolean(l.links?.listeningExercise) \|\| Boolean(l.hoeren?.lines?.length), 'Lesen': (l) => Boolean(l.links?.readingOrder), 'Sprechen': (l) => Boolean(l.sprechen?.open \|\| l.sprechen?.readAloud?.length), 'Schreiben': (l) => Boolean(l.schreiben?.taskKey) };` und der Teil gilt als gedeckt, wenn `covers[teil.split(' ')[0]]?.(l)` wahr ist. Danach meldet RULE 16 **1**, und der Ratchet wird auf 1 gesetzt, nicht auf 3. Für L2 dann eine von zwei Entscheidungen, beide vertretbar, aber eine muss getroffen werden: entweder `links.readingOrder` auf die vorhandene Leselektion setzen (L2s Thema „Angaben zur Person" passt auf `readingOrder: 1`, das L1 bereits benutzt — eine zweite Lektion darf dieselbe Leseübung verlinken), oder `'Lesen Teil 1'` aus `examTeile` von L2 streichen und in `canDo`/`schreiben` belassen, was die Lektion wirklich übt. Test in `tests/curricula.test.mjs`: kein `examTeile`-Eintrag einer Lektion ohne die zugehörige Fläche, als **harte** Regel ohne Ratchet, so wie RULE 14. | MAJOR |
| L12 `canDo[3]` in `src/data/curricula/a11.js` ↔ RULE 10 ↔ RULE 12 | `canDo: [… , 'Ich kann mich verabschieden und gute Wünsche aussprechen.']` · RULE 12: „L12 „Ich kann mich verabschieden und gute Wünsche aussprechen."" · RULE 10: „L12 **grüßen** · L12 **Bis später**" (ungenutzt) · `sprechen.readAloud: [0, 5]`, `hoeren.lines: [1, 3]` · Dialogzeile **9**: „Ja! **Mach's gut**, Lena. **Bis bald**!" | **Die letzte Kann-Beschreibung des Kurses wird an keiner Stelle geübt — und das ist die einzige der vier RULE-12-Zeilen, für die das gilt.** Ich habe alle vier einzeln an der Lektion nachgeprüft, statt sie als Ratchet zu übernehmen: L4 („Ich kann fragen, was ein Gegenstand ist.") ist durch Dialogzeile 0 gedeckt, die in `readAloud: [0,5]` steht — der Lernende **spricht** sie; L7 („Ich kann sagen, wann ich frei habe.") ist durch Zeile 4 gedeckt, ebenfalls im `readAloud: [1,4]`; L1 („begrüßen und verabschieden") ist halb gedeckt — die Begrüßung durch Zeile 0 im readAloud und durch `extra-a11-l01-08`, die Verabschiedung nur durch das Buchstabieritem `extra-a11-l01-05` („Tschüss"). **L12 ist gar nicht gedeckt:** die Verabschiedung und der gute Wunsch stehen ausschließlich in Zeile 9, und die liegt **außerhalb** von `readAloud: [0,5]` **und** außerhalb von `hoeren.lines: [1,3]` — der Lernende liest sie einmal stumm im Dialog und danach nie wieder. Kein Poolitem produziert einen Wunsch (alle 16 L12-Items sind Possessivartikel), kein `hintWord` des Sprechauftrags nennt einen („einladen", „der Geburtstag", „feiern"), und **drei** der Wortfeldeinträge, die genau dafür da sind — `grüßen`, `Bis später`, dazu `Schönen Tag noch` —, gehören zu den 15 ungenutzten aus RULE 10. Für die Prüfung ist das teuer: in *Start Deutsch 1* endet Sprechen Teil 3 mit einer Reaktion, und die Schlusshöflichkeit ist Bewertungsgegenstand — L12 ist die Lektion, in der der Kandidat sie zuletzt üben könnte. | Zwei Zeilen Daten, kein neuer Mechanismus. Erstens den Diktat-/Vorlesebereich auf die Schlusszeile ausdehnen: in `a11.js` bei L12 `sprechen: { readAloud: [0, 9], … }` — Zeile 9 ist „Ja! Mach's gut, Lena. Bis bald!", also genau der Wunsch, und `readAloud` ist die Fläche, auf der ihn `score-readaloud` bewertet. Zweitens ein Item, das ihn **produzieren** lässt, in `a11.extra.json`: `{ id:'extra-a11-l12-17', topic:'possessive-articles', type:'fill_blank', questionDe:'Zum Abschied am Telefon: „Mach's gut und ___ Tag noch!" (Adjektiv)', answer:'schönen', accepted:['schönen','Schönen'] }` — deckt zugleich `Schönen Tag noch` aus RULE 10 — und `{ id:'extra-a11-l12-18', topic:'possessive-articles', type:'sentence_building', questionDe:'Bilden Sie den Satz: [ich / grüßen / dein / Mama]', answer:'Ich grüße deine Mama.', accepted:['Ich grüße deine Mama.','Ich grüße deine Mama'] }` — deckt `grüßen` und bleibt auf dem Primärslug, die Quote hält. Die Regel, die nachrückt, ist die von MAJOR 3: RULE 12 muss die Deckung an den **Produktionsflächen** messen (`hoeren.lines`, `sprechen.readAloud`, Poolitems), nicht am bloßen Vorkommen im Dialog — sonst zählt eine stumm gelesene Zeile als Übung, und genau daran sind die vier Ratchet-Einträge dieser Runde gemessen worden. | MAJOR |
| Ziehung von L6, beide Versuche ↔ `answerKey()` / `MAX_SAME_ANSWER_KEY` in `src/lib/lesson/buildLesson.js:76,148,274–290` ↔ `extra-a11-l02-08` | Versuch 1, L6: `extra-a11-l06-05` · `c68e4835` · **`1ed2c78f`** („Ich bin **eine Lehrerin**." → *Ich bin Lehrerin.*) · `extra-a11-l06-11` · `extra-a11-l06-04` · `extra-a11-l06-10` · **`extra-a11-l06-09`** („Ich bin **eine Verkäuferin**." → *Ich bin Verkäuferin.*) · Versuch 2: dieselben sieben, andere Reihenfolge · dazu `extra-a11-l02-08` („Ich bin **ein Lehrer**." → *Ich bin Lehrer.*), gezogen in **beiden** Versuchen von L2 | **Die Lektion, die den unbestimmten Artikel einführt, lässt ihn in beiden Versuchen nur viermal von sieben produzieren — und zwei der sieben Plätze gehen an dieselbe Aufgabe mit einem anderen Beruf.** Mit dem tatsächlichen Primärslug gemessen (`drillsSlug(item, 'indefinite-articles')`): L6 kommt in **beiden** Versuchen auf **4/7** — der niedrigste Wert des Kurses, genau auf `PRIMARY_MIN`. Der Grund ist kein Zufall: `1ed2c78f` und `extra-a11-l06-09` sind Fehlerkorrekturen, deren richtige Antwort den Artikel **entfernt** („Ich bin Lehrerin.", „Ich bin Verkäuferin."), also produziert der Lernende dort kein `ein/eine/einen`. Inhaltlich sind das gute Kontrastitems — sie stehen auch so in der Regelkarte („Der Beruf steht ohne Artikel"). Es sind aber **zwei** davon im selben Siebenerblock, mit identischem Prompt-Muster, identischer Regel und einer Antwort, die sich nur im Beruf unterscheidet; und dieselbe Aufgabe hat der Lernende in L2 als `extra-a11-l02-08` schon einmal gelöst, in beiden Versuchen. Dreimal derselbe Satz in einem Zwölf-Lektionen-Kurs. `MAX_SAME_ANSWER_KEY = 2` greift nicht, weil `answerKey()` über `contentLemmas(it.answer)` läuft und `lehrerin` ≠ `verkäuferin` ist — die Vielfaltsschranke misst die Inhaltswörter der Antwort, nicht die **Aufgabe**. Das ist dieselbe Kante wie in Runde 6 bei L12, nur eine Achse weiter: dort war die Lexik gleich und die Aufgabe verschieden, hier ist die Aufgabe gleich und die Lexik verschieden. | Eine zweite, gröbere Achse neben `answerKey`: die **Aufgabenform**. In `buildLesson.js`: `export const taskShape = (it) => it.type + ':' + flat(bare(String(it.questionDe \|\| ''))).replace(/[a-zäöüß]+in\b\|[a-zäöüß]{6,}/g, '·');` — Inhaltswörter werden zu Platzhaltern, die Satzschablone bleibt, also tragen „Ich bin eine **Lehrerin**." und „Ich bin eine **Verkäuferin**." denselben Schlüssel; dazu `export const MAX_SAME_TASK_SHAPE = 1;` und in `pickPracticeItems` dieselbe Schranke wie für `answerKeyCount`. Nachgerechnet an der heutigen Ziehung: L6 verliert eines der beiden Berufsitems und nimmt `extra-a11-l06-01` („Das ist ___ Büro.") oder `-06` („Ich habe ___ Handynummer für dich.") auf, beide auf dem Primärslug, beide produzierend — L6 kommt damit auf 5/7. Zusätzlich `extra-a11-l02-08` aus L2 in die Lektion holen, in die es gehört, oder in L6 gegen `1ed2c78f` austauschen, damit derselbe Satz nicht dreimal vorkommt. Test in `tests/lesson-engine.test.mjs`, als Klasse und nicht als Instanz: für **jede** Lektion und **beide** Versuche muss `new Set(drawn.map(taskShape)).size >= 6` gelten — und, was Runde 5 für L10 einzeln erzwungen hat, jetzt für alle zwölf: `drawn.filter((it) => drillsSlug(it, primarySlug)).length >= 4` **auf der Ziehung**, nicht nur auf dem handgeschriebenen Batch (die heutige Zusicherung in `tests/lesson-extra-items.test.mjs:356` misst den Batch; die Ziehung hält sie derzeit mit 4–7, aber nichts prüft das). | MAJOR |

---

## Status der Befunde aus Runde 6

Verifiziert an den Artefakten (gebauter Pool, beide Ziehungen, gebaute
`ruleCards.mjs`, die vier gebauten Checkpoints, der Kartenindex der
Wiederholungsseite, Validator-Ausgabe, Testlauf, Lint), nicht am Diff.

### Die drei BLOCKER

1. **`isCaseTask` als Liste geschlossen; fünf Items optiert, mindestens fünf
   weitere hätten es verdient, zwei davon im benoteten Checkpoint 4** —
   **gelandet, mit einer neuen Bruchlinie.** `politeCaseItem()` existiert, wird im
   Reparaturlauf von `build-lesson-pool.mjs` gestempelt und von
   `reviewService.caseFlag()` mitbenutzt; im gebauten Pool tragen jetzt **zehn**
   Items das Flag statt fünf, darunter `b269d424` und `83bab298`. Gemessen: kein
   Item des Pools erfüllt das Prädikat, ohne das Flag zu tragen (0 Fehlstellen),
   und `extra-a11-l12-16` ist die einzige Handeintragung, die als Override
   stehenbleibt — dokumentiert. Die beiden in Runde 6 genannten Checkpoint-Items
   sind aus Checkpoint 4 verschwunden (`a1.1-cp4-bausteine-1` ist jetzt
   „___ Sie in Berlin? (wohnen, Sie)", `-schreiben-2` „[fahren / ihr / mit dem
   Auto …]"). **Rest:** das Prädikat ist ein Formregex und deckt drei falsch
   positive und 15 falsch negative Fälle — BLOCKER 1 oben.
2. **`extra-a11-l05-09` bestraft die minimale Korrektur** — **gelandet, als Regel
   mit poolweitem Test.** Das Item lautet jetzt „Korrigieren Sie: „Ein Schere ist
   hier." **(mit bestimmtem Artikel)**", `questionEn` „Fix the article — the task
   names the definite article."; `ambiguousCorrection()` und `articleFamilySwap()`
   stehen in `quality.js` mit einer im Kommentar ausgeschriebenen Ablehnung der
   zu breiten Variante, `minimalArticleCorrection()` kann die Alternativlösung
   sogar berechnen und `ein` bewusst **nicht** (es ist *der* oder *das*, und ein
   Build-Schritt darf kein Genus wählen — genau die richtige Stelle zum
   Aufhören). Gemessen über den ganzen Pool: **0** mehrdeutige Fehlerkorrekturen
   bei 16 `error_correction`-Items; die drei anderen Artikelkorrekturen
   (`extra-a11-l04-10`, `-17`, `extra-a11-l05-12`) tragen dieselbe Formel.
3. **`isItemCorrect()` ruft `checkAnswer` ohne `dictation`** — **gelandet, und
   zwar als die allgemeine Fassung des Problems.** `isDictationTask(item)` und
   `checkOptionsFor(item)` stehen in `check.js`; alle vier Bewertungsstellen
   (`PracticeItem.jsx:85`, `DictationItem.jsx:46`, `buildCheckpoint.js:840`,
   `reviewGrading.js`) rufen `checkAnswer(…, checkOptionsFor(item))`, und
   `tests/course-player.test.mjs:119` verbietet ein literales Optionsobjekt an
   **jeder** dieser Stellen. Nachgerechnet an `a1.1-cp2-hoeren-1`:
   `Null vier zwei - drei drei acht eins.`, `Null vier zwei drei drei acht eins.`
   und `null vier zwei drei drei acht eins` sind jetzt **alle** richtig. Und der
   Kreuztest, den ich in Runde 6 verlangt hatte, habe ich selbst gefahren: über
   **alle** Items, die in Lektion und Checkpoint vorkommen, und je bis zu sechs
   Antwortformen — **0 Abweichungen**. Das ist der sauberste Fix dieser Reihe.

### Die sieben MAJOR

| # | Runde-6-Befund | Status | Beleg |
|---|---|---|---|
| 1 | L4 hat 8 eigene Items, zieht vier Items mit ungelehrten Nomen (*Instrument*, *Freiheit*, *Zeitung*, *Sonne*, *Kaffee*) | **gelandet, vollständig** | L4 hat jetzt **18** eigene Items (`-09` bis `-18`, die beiden aus Runde 5 wörtlich). Beide Ziehungen bestehen **ausschließlich** aus L4-Items und **ausschließlich** aus dem eigenen Wortfeld: Flohmarkt, Uhr, Regal, Stuhl, Tasche, Rucksack, Preis, Lampe. `0ab66073`, `57592d92`, `14894fb2`, `d205e58f` sind aus dem Pool verschwunden (Lexis-Gate im Build). RULE 11 meldet für L4 nur noch `Kaffee`/`kocht`/`Wein` auf zwei ungezogenen Items. |
| 2 | RULE 11 misst an der frühestmöglichen statt an der gezogenen Lektion; 187 Tokens, davon 147 auf nie gezogenen Items | **gelandet, besser als verlangt** | Zwei Zahlen statt einer: RULE 11 heißt jetzt ausdrücklich „(ganzer Pool, **informativ**)" und steht bei **61**, und **RULE 11b** („gezogene Item-Lexik") misst über die echte Ziehung und meldet **3** (Ratchet 5). Dazu der Ausschluss im Build, der 62 Altbestandsitems entfernt hat (351 → 289). Ich habe die drei Resttreffer einzeln geprüft: `Deutsch` in `559f4c15` und `85386a42` (der Name der Zielsprache, in beiden Fällen zusätzlich in der englischen Glosse) und `arbeitet` in `c68e4835` — ein Multiple-Choice, dessen Antwort `Die` ist, das Verb wird also nur gelesen, und `arbeiten` kommt eine Lektion später. **Keiner der drei ist material.** |
| 3 | L12 zieht fünf von sieben Items auf `mein` und kein einziges `Ihr`-Item; `LEMMA_STOPWORDS` macht die Determinativlektionen für `MAX_SAME_LEMMA` blind | **gelandet, vollständig** | `answerKey()`, `MAX_SAME_ANSWER_KEY = 2` und `practiceRule.mustCover` sind gebaut; L12 trägt `mustCover: ['Ihr']`. Nachgezählt in **beiden** Versuchen: Versuch 1 `sie-Pl · wir · ich · **Sie** · ich · du · ich`, Versuch 2 `ich · sie-Pl · wir · ich · es · **Sie** · ich` — je ein `caseSensitive`-Item (`extra-a11-l12-08` bzw. `-09`), fünf bzw. sechs verschiedene Besitzer, `mein` höchstens dreimal. Der teuerste Ausfall des Kurses ist geschlossen. |
| 4 | `changedDetail`s `vocab`-Zweig hat keine Kongruenzschranke („jede **Frühstück**") | **gelandet, halb** | Die Schranke ist da und richtig gebaut: `contentWords()` liest `article` und `plural` aus dem Wortfeld statt aus den Dialogzeilen, Plural nur gegen Plural, und `DIGIT_NUMBERS` beschränkt den Tausch in einer Zifferngruppe auf einstellige Zahlwörter — „Sechs vier zwei – drei drei acht eins" statt „Siebzehn vier zwei". Sieben der acht `Falsch`-Aussagen sind sauber. **Rest:** die Semantik (MAJOR 2 oben). |
| 5 | `dd86dc8a` („[Honig / ist / gut]") als `a1.1-cp2-schreiben-1`; elf der 80 Checkpoint-Items mit ungelehrter Lexik | **gelandet, vollständig und als Regel** | `src/lib/checkpoint/lexis.js` ist neu — eine bewusst und ausführlich begründete Kopie der Lexis-Maschinerie des Validators für den Browser-Bundle, mit der Auflage „der Validator ist die Quelle, eine Änderung dort gehört im selben Commit hierher" und einem Test, der das Ergebnis misst statt der Gleichheit. `untaughtAt(curriculum, checkpoint.afterLektion)` speist `buildCheckpoint`; gemessen über alle 80 Items: **0** ungelehrte Tokens in der Lexik, für die das Item selbst haftet. `dd86dc8a` ist in keinem Checkpoint mehr, `a1.1-cp2-schreiben-1/2` sind jetzt „[das / Bild / sein / gelb]" und „[der Stuhl / kosten / zwölf Euro]", beide aus dem Wortfeld des Kapitels. `0691d3fb`, `2140796b` und `extra-a11-l12-11` sind ebenfalls verschwunden. |
| 6 | Die vier Zahlwortitems liegen unter `topic: 'verb-sein'`, ein Zahlwortfehler wird als **Konjugation** gebucht | **gelandet, mit einer Kante** | `extra-a11-l02-09` bis `-12` tragen `topic: 'numbers'`, L2s `practiceRule.topics` ist `['verb-sein','alphabet-pronunciation','numbers']`, L2 zieht weiterhin 5/7 primär, und `tests/lesson-extra-items.test.mjs:530` verbietet den Zahlwortitems, `verb-sein` zu drillen. Nachgerechnet: `sieber`, `sechs`, `seven`, `siben` → **nicht** mehr Konjugation. **Kante:** der Tag lautet `Wortschatz`, nicht `Rechtschreibung` wie empfohlen (`isNumberAnswer(exp)` steht vor der Verbklausel). Folgenlos für die Remediation, die über `topic` zieht, nicht über den Tag — deshalb nur eine Minorzeile. |
| 7 | `extra-a11-l10-06` akzeptiert `Gehst` („Gehst du morgen mit dem Bus?") | **gelandet, vollständig** | `accepted: ['Fährst','fährst','Kommst','kommst']` — `gehen` ist weg. Die Heuristik „das Item lässt schon zwei Lemmata zu" ist gestrichen, `EQUIVALENT_VERB_IDS` entscheidet allein, und die Rahmenbindung ist gebaut. Gegenprobe an `extra-a11-l10-01` („___ der Zug nach Österreich?", `['Fährt','fährt','Geht','geht','Kommt','kommt']`): hier ist `Geht` korrektes gesprochenes Deutsch und es gibt kein `mit dem/der`-Präpositionalobjekt — die Regel greift also genau dort, wo sie soll, und nicht daneben. |

Zusätzlich geprüft und weiterhin in Ordnung: die **zwölf Regelkarten** in der
gebauten `ruleCards.mjs` (je genau eine `English:`-Zeile, keine einzige
Prozentangabe, drei Fehlerbeispiele je Karte; die Possessivkarte sagt „Die
höfliche Form ist Ihr und steht **immer** mit großem I", und L12 zieht sie
jetzt auch); die **zwölf Schreibaufgaben** (Titel, Aufgabentext, Leitpunkte,
Wortgrenzen deckungsgleich mit `a11.js`, der servertaugliche Zwilling in
`netlify/functions/_shared/writingTasks.mjs` byte-identisch); `evaluate-writing`
(Identität aus dem JWT, Aufgabe über `task_key` aus der Serverbank,
`MIN_CHARS = { formular: 12 }`, und `courseTaskKeyPrefix()` mit einem
vorbildlichen Kommentar darüber, warum ein festes `'a11-'` A1.2 stillgelegt
hätte) — **außer der Kontingentrechnung, BLOCKER 2**; `scoreCheckpoint`
(60 % gesamt, 40 % je benotetem Abschnitt, Sprechen nur bei durchgehender
Mikrofonbewertung, `itemCounts`/`itemIsScored` an den Grenzen nachgerechnet);
`remediationSet` (zieht nach **Topic**, nicht nach Tag, und nimmt nichts wieder,
was im Test schon stand); der Weg des Sprechauftrags **samt `anrede`** bis in
`buildTeacherSystemPrompt` — unverändert korrekt, `sprechen.open.anrede` steht in
allen zwölf Lektionen auf `'Sie'`; und **`buildMessage()`** in
`course-reminder.mjs` — A1-Deutsch, keine Behauptung über das Verhalten des
Lernenden, Auswahl aus **einer** Definition (`public.course_reminder_candidates()`),
20-Stunden-Boden in der SQL, Re-Read vor dem Claim.
**Die E-Mail duzt weiterhin, und das lasse ich als Eigentümerentscheidung stehen —
kein MAJOR**, wie in Runde 5 und 6. Die Notiz im Kopf der Datei, um die ich
zweimal gebeten habe, fehlt weiterhin (Minor 5).

**Zu den übrigen Ratchets, die ich einzeln beurteilt habe:** **RULE 15** (12
Vorgriffe) ist ehrlich gemessen — `productionLines()` liest genau `hoeren.lines`
und `sprechen.readAloud` —, aber **keiner der zwölf ist material**, und der Grund
ist systematisch: acht der zwölf stehen in **Vorlese**zeilen (L3/1, L3/6, L8/1,
L8/6), und Vorlesen ist Aussprache, keine Formwahl; die vier Diktatzeilen (L3/3
„**Meine** Schwester", L6/9 „um eins" — von der eigenen notice ausdrücklich
angekündigt —, L7/2 „**Mein** Hobby / am Wochenende", L8/7 „**habe** ich") lassen
den Lernenden eine gehörte Form **abschreiben**, nicht wählen. Die Regel wäre
schärfer und der Ratchet reparierbar, wenn sie nur `hoeren.lines` läse: dann meldet
sie 5 statt 12, und jeder Treffer ist eine echte Produktionsentscheidung.
**RULE 13** (4 Sprechaufträge ohne Mission: L7, L10, L11, L12) ist unverändert und
bleibt vertretbar — die vier haben `missionOrder: null`, ihr Auftrag reist aber
über `saveCourseContext` vollständig mit, das ist in `tests/course-player.test.mjs`
gepinnt. **RULE 6b** (26 Notice-Formen ohne Beleg) und **RULE 10** (15 ungenutzte
Wortfeldeinträge) überschneiden sich an einer Stelle materiell, und die steht
oben als MAJOR 4.

---

## Bewertungsübersicht (Runde 7, Runde 6 in Klammern)

| Lektion | Deutsch | Niveau | Dialog | Prüfung | Kohärenz | Σ /25 |
|---|---|---|---|---|---|---|
| 1 Hallo, ich bin … | 4 (4) | 4 (4) | 5 (5) | **3** (4) | 4 (4) | **20** (21) |
| 2 Ich bin Studentin | 4 (4) | 4 (4) | 4 (4) | 4 (4) | 3 (3) | **19** (19) |
| 3 Meine Familie | 4 (4) | 4 (4) | 4 (4) | 4 (4) | 3 (3) | **19** (19) |
| 4 Auf dem Flohmarkt | **4** (3) | **4** (3) | 5 (5) | **4** (3) | **4** (3) | **21** (17) |
| 5 Im Klassenzimmer | 5 (5) | 4 (4) | 5 (5) | **4** (2) | 3 (4) | **21** (20) |
| 6 Der erste Tag im Büro | 5 (5) | 4 (4) | 4 (4) | **3** (4) | 3 (3) | **19** (20) |
| 7 Freizeit und Hobbys | 4 (4) | 4 (4) | 5 (5) | 4 (4) | 4 (4) | **21** (21) |
| 8 Termine und Uhrzeit | 4 (4) | 4 (4) | 5 (5) | 4 (4) | 4 (4) | **21** (21) |
| 9 Im Café | 5 (5) | 4 (4) | 5 (5) | 4 (4) | 4 (4) | **22** (22) |
| 10 Am Bahnhof | **5** (4) | 4 (4) | 5 (5) | 4 (4) | 3 (3) | **21** (20) |
| 11 Mein Tag | 4 (4) | 4 (4) | 4 (4) | 4 (4) | 4 (4) | **20** (20) |
| 12 Feste feiern | 5 (5) | 4 (4) | 5 (5) | **3** (2) | 3 (3) | **20** (19) |

Kursmittel **20,3/25** (Runde 6: 19,9 · Runde 5: 19,7 · Runde 4: 19,9 ·
Runde 3: 20,4 · Runde 2: 19,8 · Runde 1: 17,2). Fünf Lektionen besser, zwei
schlechter, fünf gleich — und der Mittelwert liegt zum ersten Mal über dem
bisherigen Höchststand von Runde 3, der damals auf ungemessenen Flächen beruhte.

**Zur Lesart:** **L4 (17 → 21)** ist der größte Sprung dieser Reihe und der
verdienteste: drei Runden lang die schwächste Lektion mit acht eigenen Items,
jetzt achtzehn, und beide Ziehungen bestehen ausschließlich aus dem eigenen
Wortfeld — Genus wird an Tisch, Uhr, Regal und Tasche geübt, nicht mehr an
*Freiheit* und *Instrument*. **L5 (20 → 21)**, weil ihr Reparaturitem seine
Aufgabenformel bekommen hat und die minimale Korrektur nicht mehr bestraft; der
Punkt bei Kohärenz fällt von 4 auf 3, weil sie in beiden Versuchen zwei bis drei
ihrer sieben Plätze aus L4 füllt. **L10 (20 → 21)**, weil `Gehst du mit dem Bus`
nicht mehr akzeptiert wird — der Deutschpunkt kehrt auf 5 zurück. **L12 (19 → 20)**,
weil `mustCover` die Höflichkeitsform in **beiden** Versuchen zieht; sie bleibt
bei Prüfung 3 statt 4, weil ihre letzte Kann-Beschreibung nirgends geübt wird
(MAJOR 4) und weil der Akkusativ-Vorgriff aus ihrem Topic in den benoteten
Checkpoint 4 wandert (BLOCKER 3). Die beiden Verlierer sind keine
Verschlechterung des Materials, sondern zum ersten Mal gemessene Befunde:
**L1 (21 → 20)**, weil ihre Satzkarte „Gut. Wie geht es Ihnen?" dieselbe Form
anders bewertet als ihr eigenes Item `extra-a11-l01-06` — die Lektion, in der die
Höflichkeitsform eingeführt wird, ist die, an der der Bruch sichtbar wird —, und
**L6 (20 → 19)**, weil sie in beiden Versuchen nur 4 von 7 Items den unbestimmten
Artikel produzieren lässt und zwei Plätze an dieselbe Aufgabe gehen.
**Stärkste:** L9 (22), dann L4, L5, L7, L8 und L10 (je 21) — L8 zum vierten Mal
in Folge ohne einen einzigen eigenen Befund.
**Schwächste:** L2, L3 und L6 (je 19) — L2 wegen des unbelegten „Lesen Teil 1"
(MAJOR 3), L3 unverändert wegen der Mischziehung mit `verb-sein` (4/7 auf dem
eigenen Slug), L6 wegen der Dopplung.

---

## Würde ich meinen Namen jetzt daraufsetzen?

**Noch nicht — aber zum ersten Mal nicht wegen des Kursmaterials.** Ich habe alle
zehn Punkte aus Runde 6 an den Artefakten nachgeprüft: drei Blocker geschlossen
und sieben MAJOR, davon fünf vollständig und als Regel mit poolweitem Test. Drei
Dinge sind in dieser Runde qualitativ passiert, und alle drei verdienen es,
benannt zu werden. Erstens: **`checkOptionsFor(item)` ist der beste Fix dieser
Reihe.** Die Frage „welche Prüfregeln gelten für diese Antwort" hat jetzt genau
eine Antwort, sie steht am Item, und ein Test verbietet der Aufrufstelle, sich
eine eigene zu bauen. Ich habe die Behauptung nicht geglaubt, sondern nachgemessen
— über alle Items, die in Lektion und Checkpoint vorkommen, mit bis zu sechs
Antwortformen je Item: null Abweichungen. Zweitens: **der Build wirft jetzt weg.**
62 Altbestandsitems haben den Pool verlassen, weil ihre Lexik an ihrer
frühestmöglichen Lektion ungelehrt ist; RULE 11 sagt seither ehrlich 61 und
markiert sich selbst als informativ, und RULE 11b misst die Zahl, die zählt, an
der Ziehung. Das ist die Umsetzung von Runde 6s zweitem Satz — *was nicht gezogen
wird, ist nicht gelehrt* — und sie ist vollständig. Drittens: **`lexis.js` ist die
ehrlichste Kopie, die ich in diesem Repository gesehen habe.** Sie sagt im Kopf,
dass sie eine Kopie ist, warum sie eine sein muss, welche Funktionen kopiert sind
und welche nicht, wer die Quelle ist — und sie sichert sich nicht durch einen
Gleichheitstest, sondern durch eine **Messung des Ergebnisses**, die eine Drift
als falsche Zahl sichtbar macht statt als schweigende Gleichheit.

**Was offen bleibt, ist zum siebten Mal dieselbe Kante — aber sie ist wieder ein
Stück weitergewandert, und diesmal aus dem Produkt heraus.** Runde 6 schrieb: „ein
Flag, das ein Item tragen muss, ist keine Regel, solange niemand prüft, welche
Items es tragen müssten." Runde 7 hat diese Prüfung gebaut — und sie mit **genau
dem Formregex** beantwortet, den Runde 5 aus `isCaseTask` entfernt hatte, mit
derselben Begründung, die noch heute im Kommentar von `check.js` steht. Ein
Prädikat, das die **Gestalt der Antwort** liest, kann die Frage nicht beantworten,
die es stellt; es kann sie nur eine Ebene höher stellen. Der Satz, der aus dieser
Runde mitgeht, lautet deshalb: **ein Prädikat, das nur die Antwort liest, hat
dieselbe Blindheit wie der Regex, den es ersetzt hat — es muss die Stelle lesen,
an der die Entscheidung fällt** (hier: die Satzposition der Form, nicht ihre
Buchstabenfolge).

Der zweite Satz ist neu, und er ist der Grund, warum ich diese Runde trotz ihrer
Qualität nicht unterschreibe: **eine Regel, die als Liste geschrieben ist, prüft
nur, woran der letzte Bericht gedacht hat — und das gilt für Tests genauso wie für
Items.** `CHROME_FILES` zählt dreizehn Komponenten auf und lässt die vierzehnte
aus; die vierzehnte ist die, die der Lernende 84-mal sieht, und auf ihr steht
„Deine Antwort". RULE 16 liest ein Feld statt einer Frage und meldet deshalb zwei
Artefakte und einen echten Befund gleich laut. Die Drill-Zusicherung „≥4 der
gezogenen 7" ist auf dem Batch festgeschrieben und auf der Ziehung nur für L10.
Und die Kontingentrechnung für die Schreibaufgaben ist die reinste Form davon: die
Zahl 12 steht als Literal neben einem Kurs, der inzwischen sechzehn Schreibflächen
hat, und niemand rechnet nach, weil `optional: true` den Fehlschlag in Schweigen
verwandelt. Das ist exakt die Regel, die `CLAUDE.md` für Preise und Claims
aufstellt — *ableiten, nie abtippen* — angewendet auf ein Limit.

Die Arbeit ist überschaubar und vollständig ausgeschrieben: eine Satzgrenze in
`politeCaseItem` plus ein Karten- und Poolweiter Test, eine abgeleitete
Kontingentzahl plus ein sichtbarer 429, ein Ausschlussgrund für selbsterklärte
Vorgriffe plus zwölf Wörter in `ARTICLES`, ein Glob statt einer Handliste plus ein
Label, eine Determinativ-Schranke im Falsifikator, ein `covers`-Objekt in RULE 16,
zwei L12-Items und ein `readAloud`-Index, eine `taskShape`-Achse in der Ziehung.
Das ist ein Tag, wie beim letzten Mal — nur diesmal ohne eine einzige Änderung am
Kursmaterial selbst, das inzwischen gut ist. Danach unterschreibe ich für alle
zwölf.

---

**Minors (fünf Zeilen, nicht erfasst):** (1) Die Buchstabenkarte schreibt weiter
`Q ku` statt `Qu` und mischt Groß-/Kleinschreibung (`be, ce, de` neben `Jot, Vau,
Zett`), kein Item prüft Q; die Karte `verb-sein` schreibt weiter „Der Wohnort: Ich
bin in Bremen.", während der eigene Dialog „Ich **wohne** in Bremen" sagt. (2) Die
`situation` von L9 trägt weiter die Redaktionsnotiz „(die Einladung kommt in
Lektion 12)" und wird dem zahlenden Lernenden in `CurriculumHomePage.jsx` angezeigt.
(3) `tagError` bucht einen Zahlwortfehler als **Wortschatz** statt als
**Rechtschreibung** (`sieber` → Wortschatz); folgenlos für die Remediation, die
über `topic` zieht, aber die Diagnose-Chips des Checkpoints zeigen es dem
Lernenden an. (4) L12s `schreiben.sample` benutzt mit „Ich feiere am Freitag
**meinen** Geburtstag" und dem Imperativ „**Komm** um acht Uhr!" zwei Formen, die
der Kurs nicht lehrt — und diese Musterlösung steht neben der benoteten Aufgabe
von Checkpoint 4; `a1.1-cp4-lesen-4` fälscht „Gleis vier" zu „Gleis **achtzig**",
formal korrekt, aber kein Bahnhof hat Gleis 80. (5) Die Eigentümerentscheidung zur
Anrede der Erinnerungsmail ist weiterhin nirgends aufgeschrieben — ein Satz im Kopf
von `course-reminder.mjs` und eine Ausnahmezeile im Registertest, sonst schließt
eine spätere Runde sie versehentlich mit (zum dritten Mal erbeten).

BLOCKER: 3 · MAJOR: 5
