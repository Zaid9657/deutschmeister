
# A1.1 — DaF-Fachreview #13, nach Runde 13 (2026-09-12)

Reviewer: DaF-Lehrkraft (Erwachsenenbildung, Integrationskurs) und Prüferin für
Goethe-Zertifikat A1 *Start Deutsch 1*. Dreizehnter Durchgang. Basis:
`REVIEW-daf-2026-09-12.md` (5 BLOCKER · 45 MAJOR), `REVIEW-daf-2-2026-09-12.md`
(1 BLOCKER · 35 MAJOR · 44 MINOR), `REVIEW-daf-3` bis `REVIEW-daf-5` (je
3 BLOCKER · 11 MAJOR), `REVIEW-daf-6` (3 BLOCKER · 7 MAJOR), `REVIEW-daf-7`
(3 BLOCKER · 5 MAJOR), `REVIEW-daf-8` und `REVIEW-daf-9` (je 0 BLOCKER · 5 MAJOR),
`REVIEW-daf-10` (0 BLOCKER · 4 MAJOR), `REVIEW-daf-11` (1 BLOCKER · 3 MAJOR) und
`REVIEW-daf-12-2026-09-12.md` (2 BLOCKER · 4 MAJOR), angewendet in `main` @
`6aabf4e` („A1.1 round 13: close DaF review #12 + report", PR #130). Gemessen wurde
ausschließlich der Commit-Stand. **A1.2 ist durch Eigentümerentscheidung vom
2026-09-13 pausiert und nicht Gegenstand dieses Reviews.**

Gemessen, nicht angenommen:

* **Die echte Ziehung für alle drei Versuche**: `planPractice(CURRICULUM_A11, POOL, a)`
  für `a` = 1, 2 und 3, alle **252** ausgelieferten Items (**227 verschiedene** aus
  einem Pool von **374**) mit `questionDe`/`questionEn`/`options`/`answer`/`accepted`/
  `explanationDe`/`caseSensitive`/`minLektion` ausgedruckt und einzeln gelesen; je Block
  die Zahl verschiedener `taskShape`-Schlüssel (**7 von 7 in allen 36 Blöcken**), die
  größte Antwortschlüsselgruppe (**2**), die größte Lemmagruppe (**2**), `relaxUsed`
  (**0 in allen 36**) und alle drei Versuchspaare (1∩2 = 2, 2∩3 = 2, 3∩1 = 3).
* **Der neue Situationsboden als Zahl, nicht als Zusicherung**: `relevanceScore(item,
  ownTerms, ∅)` je Item über alle 36 Blöcke, daneben `practiceReport().situationCount`
  und `complete` — beide Messungen Block für Block verglichen.
* **Die abgeleiteten V2-Stellungen, Zeichen für Zeichen**: alle vier Items, denen der
  Build eine vorangestellte Angabe schreibt (Buildausgabe „fronted word orders accepted
  (REVIEW #12 BLOCKER 1): 4"), mit **allen sechs** erzeugten Formen gelesen und auf
  Grammatikalität geprüft; dazu `frontableOrders` über **alle 374** Poolitems und alle
  **80** Checkpoint-Items.
* **Eine eigene Zweitantwortprobe, die diese Reihe so noch nicht gefahren hat**: für
  **jedes** `error_correction`-Item des Bestands die zweite minimale Reparatur von Hand
  gebildet (nicht nur die Kongruenzachse, die Runde 13 gebaut hat), und für jedes
  `sentence_building`-Item die im Deutschen übliche Alternativstellung — beide gegen
  `checkAnswer(…, checkOptionsFor(item))`.
* **Der Checker gegen 2 463 Bewertungsflächen**: 252 gezogene Items, der ganze gebaute
  Pool (374), die Handquelle (233) und alle getippten Checkpoint-Items — je mit der
  eigenen Antwort und mit **jeder** `accepted`-Variante; dazu **670** Flächen aus **395**
  Wiederholungskarten gegen `gradeTypedReview`.
* **Alle vier Checkpoints gebaut**, alle **80** Items ausgedruckt, die 16 Lesen-Aussagen
  gegen ihren eigenen Text gehalten, jede Diktat- und Vorlesezeile gegen das Kapitelfenster
  ihres Checkpoints geprüft, dazu realistische Lernertippfehler auf den Diktaten.
* **Die zwölf Mustertexte und die zwölf Modellantworten gegen DREI Maßstäbe**, nicht
  gegen einen: (1) `scoreWriting(task, sample)` mit Fenster und Leitpunkten aus der
  Serverbank (das ist RULE 17), (2) `untaughtTokens(text, levelLexicon('a1.1'))` — die
  Lexisfrage, die der Kurs jedem Poolitem stellt und keinem Mustertext, und (3)
  `constructionHits` gegen die rohe Regex **ohne** ihren `skip()`.
* **Eine eigene Formcheckprobe**: drei selbst geschriebene, prüfungsgerechte
  A1-Mitteilungen (L2, L8, L10), die alle Leitpunkte beantworten, durch `scoreWriting`.
* Das Curriculum: Dialoge, notices, Kann-Beschreibungen, `examTeile`, Wortfeld, Register,
  pretests, die zwölf Schreibaufgaben gegen `netlify/functions/_shared/writingTasks.mjs`,
  die Sprechaufträge und `netlify/functions/_shared/speakingAI.mjs`, die zwölf Regelkarten
  aus `netlify/functions/_shared/ruleCards.mjs` gegen ihre notices.
* `node scripts/validate-curriculum.mjs a1.1` (RULE 10: 14/14 · RULE 11: 59/59 ·
  RULE 11b: **0, harte Regel** · RULE 12: 0, harte Regel · RULE 13: 4/4 · RULE 6b:
  **23**/23 · RULE 15: 3/3 · RULE 15b: **0, harte Regel** · RULE 16: 0/0 ·
  **RULE 17: 0, harte Regel** · **RULE 18: 1/1** · **RULE 19: 0, harte Regel** ·
  RULE 14: 0).
* `npm test`: **868 Tests, 867 grün, 1 übersprungen** (weiterhin nur der
  A1.2-Ziehungstest). `npm run lint`: 0 Fehler, 0 Warnungen. `npm run check:duplicates`: OK.
  `node scripts/build-lesson-pool.mjs a1.1` reproduziert den committeten Pool.

## Urteil in einem Absatz

**Runde 13 hat beide BLOCKER und drei der vier MAJOR aus Runde 12 geschlossen, und die
Motorarbeit ist wieder tadellos.** `frontableOrders` ist kein Flickwerk, sondern ein
Ableiter mit drei begründeten Verengungen; ich habe alle **sechs** erzeugten Formen
gelesen, und **keine einzige ist falsches Deutsch** — „Gestern habe ich gearbeitet.",
„Im Mai ist mein Geburtstag.", „Aus Marokko kommen wir." und die drei Stellungen von
„Der Termin ist am Dienstag um acht Uhr." sind alle korrekt, das Verb bleibt überall auf
Position 2, und die Gegenproben (Frage, angabeloser Satz, bereits invertierter Kanon)
greifen. `agreementAmbiguity` liest die beiden unregelmäßigen Paradigmen und die
regelmäßige Endung, wirft Determinantenpaare bewusst zur Nachbarregel, und die drei
gerügten Items heißen jetzt „Korrigieren Sie **das Verb**: …". `SITUATION_MIN = 4`
steht, und die Zahl ist echt: **kein einziger der 36 Blöcke** liegt darunter, L7 Versuch 3
steigt von 2/7 auf 4/7, alle 36 melden `complete`, `relaxUsed = 0`, sieben verschiedene
Aufgabenformen, null Antwortlecks. RULE 17, 18 und 19 existieren, und RULE 19 hat die
L3-Modellantwort an die Figurenfakten zurückgeholt. Über 2 463 Bewertungsflächen findet
der Checker **null** Selbstablehnungen. **Und trotzdem unterschreibe ich nicht, und
diesmal ist der Grund härter als in Runde 12: beide neuen BLOCKER sind derselbe Befund
wie der geschlossene, eine Achse weiter — der Kurs hat die Instanz geschlossen und die
Klasse offen gelassen, und zwar in genau der Form, vor der Runde 12 im vierten Punkt
ihres zweiten BLOCKERs gewarnt hat.** **Erstens die Wortstellung, zum zweiten Mal.**
`extra-a11-l02-07` heißt „Bilden Sie den Satz: [ich / sein / Lehrer / **von Beruf**]",
und „**Von Beruf bin ich Lehrer.**" ist `wrong` — während der pretest **derselben
Lektion 2**, vier Bildschirme vorher, `accepted: ['Ich bin', 'Ich arbeite als',
'**Von Beruf bin ich**']` führt. Das ist Zeile für Zeile das Argument, mit dem Runde 12
den L11-BLOCKER belegt hat; es steht in derselben Datei, in demselben Feld, und die neue
Regel greift nicht, weil `FRONTABLE_ADVERBIAL_RE` eine **geschlossene Liste** ist und
`von` nicht darauf steht. Das Item ist außerdem `a1.1-cp1-bausteine-2` — **eine benotete
Checkpoint-Aufgabe**. **Zweitens die Korrekturambiguität, zum zweiten Mal.** „Korrigieren
Sie: „Das ist **ein Chefin**."" hat zwei minimale Reparaturen: „Das ist **eine** Chefin."
und „Das ist ein **Chef**." — und der Kurs lehrt `der Chef` und `die Chefin` **in
derselben Wortfeldzeile derselben Lektion 6**. Dasselbe bei `Kollege/Kollegin` und
`Verkäufer/Verkäuferin`; alle drei Items sind gezogen, alle drei werten die zweite
Reparatur `wrong`. `agreementAmbiguity` gibt Artikelkorrekturen ausdrücklich an
`ambiguousCorrection` ab, und die sieht nur den Wechsel der Artikel**familie**
(bestimmt ↔ unbestimmt) — `ein → eine` bleibt in der Familie. Daneben drei systematische
Befunde, und alle drei betreffen dieselbe Fläche: **die Texte, die der Kurs als Muster
zeigt, sind die am schlechtesten gemessene Fläche des ganzen Produkts.** Der größte:
`leitpunktKeyword` zieht aus jedem Leitpunkt **ein** Token und sucht es als Teilzeichenkette
— eine von mir geschriebene, prüfungsgerechte L2-Mitteilung („Ich heiße Ana Chakiri. Ich
bin am 3. Mai 1998 geboren. Ich komme aus Marokko und bin Marokkanerin. Ich bin ledig.",
30 Wörter, alle drei Leitpunkte beantwortet) bekommt **lp0, lp1 und lp2 rot**, und Runde 13
hat darauf nicht den Checker repariert, sondern den Mustertext um die Schlüsselwörter
herumgebaut: „**Der Nachname ist** Chakiri. … **Das Land ist** Marokko … **Der
Familienstand:** Ich bin ledig." Das schreibt kein Mensch. **Zweitens** misst **keine
Regel** die Lexis eines Mustertextes: fünf der sechs neu geschriebenen Mitteilungen
benutzen Wörter, die der Kurs nie lehrt, und die L8-Mitteilung trägt dreimal
Adjektivdeklination („**Ein neuer Termin** … **der neue** Tag und **die neue** Uhrzeit")
— in genau der Lektion, deren Dialogzeile mit dem Kommentar „**NOT** „Ich habe einen
guten Wecker": that is Adjektivdeklination im Akkusativ, a fifth Vorgriff the course never
names" davor bewahrt wurde. **Drittens** ist RULE 15b keine harte Null, sondern eine harte
Null **über eine Liste von vierzehn Verbstämmen**: „Bitte **füllen** Sie das Formular
**aus**." steht als **Vorlesezeile in Lektion 2** und als Diktat in **Checkpoint 1** —
Satzklammer aus Lektion 11 plus ein Sie-Imperativ, den A1.1 nirgends lehrt — und der Guard
verwirft den Treffer, weil `füll` nicht auf seiner Liste steht. Derselbe Satz mit `kaufen`
wird gefangen; ich habe beide gemessen.

**Zahlen: 2 BLOCKER · 3 MAJOR.** Die Bedingung aus `docs/course-standard-2026-09-12.md` §8
(0 BLOCKER / 0 MAJOR) steht weiter aus. MINORs sind am Ende als acht Zeilen erfasst.

---

## BLOCKER

| # | Feld | Zitat | Problem | Fix (pasteable) |
|---|---|---|---|---|
| 1 | `src/data/lessonPools/a11.extra.json` → `extra-a11-l02-07`, **gezogen** (Versuch 1 L2, Versuch 2 L3) und gebaut als **`a1.1-cp1-bausteine-2`**, also eine **benotete Checkpoint-Aufgabe** ↔ `src/data/curricula/a11.js` L2 `pretest.accepted` ↔ `FRONTABLE_ADVERBIAL_RE` in `src/data/lessonPools/quality.js:877` | `extra-a11-l02-07`: „Bilden Sie den Satz: **[ich / sein / Lehrer / von Beruf]**" → `accepted: ["Ich bin Lehrer von Beruf.", "Ich bin Lehrer von Beruf", "Ich bin von Beruf Lehrer.", "Ich bin von Beruf Lehrer"]` · gemessen mit `checkAnswer(…, checkOptionsFor(item))`: „Ich bin Lehrer von Beruf." → `correct`, „Ich bin von Beruf Lehrer." → `correct`, „**Von Beruf bin ich Lehrer.**" → **`wrong`** · dasselbe für das benotete `a1.1-cp1-bausteine-2` (`accepted` identisch plus die Dublette, die der Checkpoint-Bauer anhängt): „**Von Beruf bin ich Lehrer.**" → **`wrong`** · derselbe Kurs, `src/data/curricula/a11.js`, **Lektion 2** `pretest`: `{ promptDe: 'Was sind Sie von Beruf? Antworten Sie mit einem Satz.', model: 'Ich bin Studentin.', accepted: ['Ich bin', 'Ich arbeite als', '**Von Beruf bin ich**'] }` · gemessen: `frontableOrders(extra-a11-l02-07)` → **`[]`**, weil `von` nicht in `FRONTABLE_ADVERBIAL_RE` steht (die Liste führt `am`, `um`, `im`, `in`, `aus`, `nach`, `bei`, `gestern`, `heute`, `morgen`, `jeden Tag`, `jede Woche`, `nächste/letzte Woche`) | **Der Kurs sagt dem Lernenden in Schritt 1 der Lektion, „Von Beruf bin ich …" sei eine gute Antwort, und wertet denselben Anfang in Schritt 4 und im benoteten Checkpoint 1 als falsch. Das ist Wort für Wort der BLOCKER 1 der Runde 12, eine Präposition weiter.** Drei Messungen. **(a) Die abgelehnte Stellung ist die vom Kurs selbst genannte.** Runde 12 hat ihren BLOCKER an genau dieser Konstellation festgemacht: die vorangestellte Angabe steht in `pretest.accepted` derselben Lektion und ist im Item `wrong`. Bei L11 (`'Gestern habe ich'`) ist das geschlossen; bei L2 (`'Von Beruf bin ich'`) steht es unverändert, obwohl die Beweiszeile im **selben Feld derselben Datei** liegt und Runde 13 diese Datei angefasst hat. **(b) Die Stellung ist nicht markiert, sondern die übliche.** „Von Beruf bin ich Lehrer." ist die Standardantwort auf „Was sind Sie von Beruf?" und steht so in jedem A1-Lehrwerk; in *Start Deutsch 1* Sprechen Teil 1 („Beruf") ist sie die häufigste Lernerproduktion überhaupt. Das Verb steht auf Position 2, wie es die Regelkarte verlangt. Der Klammereintrag `von Beruf` ist ein **eigenes Listenglied** der Aufgabe — der Lernende soll ihn als Baustein behandeln, und genau das tut er, wenn er ihn nach vorn zieht. **(c) Die Regel scheitert nicht am Prinzip, sondern an einer geschlossenen Liste.** `frontableOrders` ist sauber gebaut: drei dokumentierte Verengungen, Gegenproben in beide Richtungen, Lauf über den gebauten Pool **und** über alle vier Checkpoints. Aber ihr Herz ist `FRONTABLE_ADVERBIAL_RE`, eine handgeschriebene Alternativenliste, und der Kommentar sagt das auch („Deliberately a closed list of A1.1 shapes"). Eine Liste, die ein Autor pflegt, hat genau die Eigenschaft, die diese Reihe seit Runde 9 bemängelt: sie schließt die gemessenen Fälle und nicht ihre Klasse. Der Beleg ist, dass die Liste `aus …` enthält (für `extra-a11-l03-07`, das die Messung nannte) und `von …` nicht (für das Item, das sie nicht nannte). | **Erstens** die Liste dort erweitern, wo der Kurs sie selbst belegt: `FRONTABLE_ADVERBIAL_RE` um `von\s+\S+` und `mit\s+\S+\s+\S+` ergänzen — aber das ist nur die Zeile. **Zweitens** die Regel, die die Liste überflüssig macht und die ich als eigentlichen Fix erbitte: **jeder Klammerchunk, der weder das Subjekt noch das finite Verb noch ein Objekt mit Artikel ist, ist frontingfähig** — d. h. `frontableOrders` leitet die Kandidatenspanne aus der Aufgabe ab (Klammerglieder, die im Kanon **nicht** an Position 1 oder 2 stehen und mit einer Präposition oder einem Adverb beginnen) statt aus einer Formenliste; die heutige Liste bleibt als Positivfixture, damit die Erweiterung nichts verliert. **Drittens** die Zusicherung, die den Befund dieser Runde unmöglich macht, und sie kostet vier Zeilen: in `tests/curricula.test.mjs`, über **jedes** Curriculum und **jede** Lektion — für jeden `pretest.accepted`-Eintrag, der **nicht** mit dem Subjekt beginnt (`/^(Ich|Du|Er|Sie|Es|Wir|Ihr|Mein|Dein|Der|Die|Das)\b/` trifft nicht), muss **jedes** `sentence_building`-Item derselben Lektion, dessen Antwort dieselbe Angabe enthält, diese Stellung in `accepted` führen. Das ist genau die Brücke, die beide Instanzen verbindet, und sie liest die Daten statt einer Liste. **Viertens** der Lauf über die vier Checkpoints mit `assert`, nicht mit `graded >= 3`: die heutige Zusicherung („only ${graded} checkpoint items have a second word order — the review measured three") zählt, was die Regel findet, und kann deshalb nie melden, was sie nicht findet. |
| 2 | `src/data/lessonPools/a11.extra.json` → `extra-a11-l06-18` (gezogen, Versuch 2 L6), `extra-a11-l06-19` und `extra-a11-l06-11` (beide gezogen, **in derselben Sieben**, Versuch 1 L6) ↔ `ambiguousCorrection` in `src/data/lessonPools/quality.js:802` ↔ `agreementAmbiguity` ebenda, Zeile 1089 (`DETERMINER_FORM_RE`) ↔ `src/data/curricula/a11.js` L6 `wortfeld` | `extra-a11-l06-18`: „Korrigieren Sie: „**Das ist ein Chefin.**"" → `"Das ist eine Chefin."` — gemessen: „**Das ist ein Chef.**" → **`wrong`** · `extra-a11-l06-19`: „Korrigieren Sie: „Das ist ein Verkäuferin."" → „**Das ist ein Verkäufer.**" → **`wrong`** · `extra-a11-l06-11`: „Korrigieren Sie: „Das ist ein Kollegin im Büro."" → „**Das ist ein Kollege im Büro.**" → **`wrong`** · das Wortfeld **derselben Lektion 6**, unmittelbar benachbarte Zeilen: `{ de: 'der Chef', … en: 'boss (m)' }`, `{ de: 'die Chefin', … en: 'boss (f)' }`, `{ de: 'der Kollege' }`, `{ de: 'die Kollegin' }`, `{ de: 'der Verkäufer' }`, `{ de: 'die Verkäuferin' }` · gemessen: `ambiguousCorrection(item)` → **`false`** für alle drei, `agreementAmbiguity(item)` → **`null`** für alle drei · zum Vergleich die Fassung, die Runde 13 selbst als richtig gebaut hat: `extra-a11-l09-14` heißt jetzt „Korrigieren Sie **das Verb**: „Du habt Durst."", `extra-a11-l10-10` und `-11` ebenso | **Eine Korrekturaufgabe mit zwei minimalen Reparaturen wertet eine davon als falsch — dieselbe Klasse wie BLOCKER 2 der Runde 12, eine Achse weiter, und die neue Regel gibt diese Achse ausdrücklich ab.** Drei Messungen. **(a) Beide Reparaturen sind minimal, korrekt und vom Kurs gelehrt.** „Das ist ein Chefin." wird durch **ein** Token repariert, entweder am Artikel (`ein → eine`) oder am Nomen (`Chefin → Chef`). Beide Sätze sind richtiges Deutsch, beide zeigen den unbestimmten Artikel, den L6 lehrt, und der deutsche Prompt sagt nicht, welche Seite falsch ist — er trägt **keinen** Cue, während jede Genuskorrektur der Lektionen 4 und 5 „(mit bestimmtem Artikel)" trägt. Das Entscheidende: der Kurs lehrt `der Chef` und `die Chefin` als **Paar in derselben Wortfeldzeile**, mit den Glossen „boss (m)" und „boss (f)" — er sagt dem Lernenden also selbst, dass beide Formen existieren und gleich gut sind. Wer „ein" für richtig und „Chefin" für den Tippfehler hält, hat die Lektion verstanden, nicht verfehlt. **(b) Die Regel existiert, kennt die Achse aber nicht, und zwar mit Vorsatz.** `agreementAmbiguity` (Runde 13) hat eine ausdrückliche Klausel: `if (DETERMINER_FORM_RE.test(bare(from)) || DETERMINER_FORM_RE.test(bare(to))) return null;` mit dem Kommentar „which is the neighbouring rule's business". Die Nachbarregel `ambiguousCorrection` (Runde 7) prüft, ob die Korrektur die Artikel**familie** wechselt (`ARTICLE_FAMILY(from) !== ARTICLE_FAMILY(to)`) — `ein → eine` bleibt in der Familie `indefinite`, also meldet auch sie nichts. Zwischen den beiden Regeln liegt ein Loch, und drei gezogene Items liegen darin. Das ist derselbe Satz wie in Runde 9, 11 und 12: eine Regel, die eine Instanz schließt und ihre Klasse nicht, ist keine Regel. **(c) Runde 12 hat genau diesen Fix bestellt.** Punkt „Viertens" ihres BLOCKERs 2 lautet: „ein Guard, der für jedes typisierte Item eine zweite, mechanisch erzeugte Kandidatenantwort baut (zweite Wortstellung, **zweite minimale Reparatur**) und verlangt, dass sie entweder in `accepted` steht oder vom Prompt ausgeschlossen ist, schließt beide BLOCKER in einem Stück." Gebaut wurden zwei **Achsen** statt eines Generators — und beide BLOCKER dieses Berichts sind die dritte und vierte Achse. | **Erstens** die drei Zeilen mit dem Cue versehen, den der Kurs schon führt: `extra-a11-l06-18.questionDe` → `"Korrigieren Sie den Artikel: „Das ist ein Chefin.“"`; `extra-a11-l06-19` → `"Korrigieren Sie den Artikel: „Das ist ein Verkäuferin.“"`; `extra-a11-l06-11` → `"Korrigieren Sie den Artikel: „Das ist ein Kollegin im Büro.“"` (`agreementAmbiguity` erkennt `artikel` bereits als Cue, `ambiguousCorrection` über `ARTICLE_TASK_CUE_RE` ebenso — die Zeile passt also ohne weiteren Code). **Zweitens** die fehlende Achse als Prädikat, neben die beiden anderen in `quality.js`: `genderPairAmbiguity(item)` — die Korrektur ändert **einen Artikel**, und das **Nomen** des Zitats hat im Wortfeld des Levels einen Genuspartner, der sich von ihm nur durch ein Suffix unterscheidet (`-in`, `-e`, `-frau`/`-mann`), sodass ein Tausch des Nomens denselben Satz mit einem Token repariert; dann MUSS der Prompt das geänderte Element benennen. Die Partnertabelle wird aus dem Wortfeld **gelesen** (`wortfeldTerms` plus ein Suffixvergleich), nicht getippt — sonst ist es wieder eine Liste. **Drittens** — und das ist der Fix, der diese Reihe beendet statt sie fortzusetzen — der Generator, den Runde 12 bestellt hat, als **eine** Zusicherung in `tests/lesson-extra-items.test.mjs`: für jedes `error_correction`-Item des gebauten Pools und jedes Checkpoint-Items werden **alle** Ein-Token-Varianten des Zitats gebildet, die aus dem Wortfeld des Levels oder aus einem Paradigma des Levels stammen; jede Variante, die ein grammatischer deutscher Satz ist, muss entweder in `accepted` stehen oder vom Prompt ausgeschlossen sein. Der Bestand liefert die Fixtures für beide Richtungen: die drei L6-Items und die drei Verbitems müssen gefangen werden, `extra-a11-l07-08` („Lena spielen am Wochenende Fußball.") und `extra-a11-l06-21` („Das ist ein Firma in Berlin.", `die Firma` hat keinen Genuspartner) dürfen es nicht. **Viertens** die Leseregel dazu in den Kopf von `quality.js`: **eine Aufgabe, deren deutsche Anweisung die Lösung nicht eindeutig macht, gehört nicht in den Pool — und „eindeutig" heißt: kein Wort des eigenen Wortfelds erzeugt eine zweite richtige Antwort.** |

Die übrigen zwei Definitionen sind einzeln gegen den heutigen Stand gemessen und **halten**:

* **„Der Kurs verspricht etwas, das er nicht enthält."** RULE 16 misst **0**, RULE 12 ist
  eine harte Regel und misst **0**; alle `examTeile`-Behauptungen der zwölf Lektionen haben
  eine Fläche in ihrer eigenen Lektion, alle zwölf `taskKey`s sind in der Serverbank, die
  zwölf Sprechaufträge stehen alle auf `anrede: 'Sie'` und `speakingAI.mjs` setzt das
  durch (`normalizeAnrede`, Default `'Sie'`, und die ANREDE-Zeile im Systemprompt).
  Der Grenzfall ist MAJOR 1: die Leitpunktzeilen des Formchecks behaupten etwas über den
  Text des Lernenden, das nicht stimmt — aber sie vergeben keinen Punkt, und die Karte
  sagt selbst „Formcheck, keine KI-Bewertung". Deshalb MAJOR und nicht BLOCKER.
* **„Eine geprüfte Form, die der Kurs nie lehrt."** RULE 11b = **0** über die echte
  Ziehung, RULE 15 von 12 auf 3 und die drei Reste in ihrer eigenen notice angekündigt,
  RULE 15b nominell 0. **Der Grenzfall ist MAJOR 3, und er ist knapp:** „Bitte füllen Sie
  das Formular aus." ist Vorlesezeile in L2 und Diktat in Checkpoint 1 und trägt zwei
  Formen, die A1.1 dort nicht lehrt (Satzklammer → L11; Sie-Imperativ → nirgends). Ich
  werte es als MAJOR und nicht als BLOCKER, weil Diktat und Vorlesen **Nachbildung** sind
  und nicht freie Produktion — der Lernende muss die Regel nicht kennen, um den Satz zu
  schreiben. Der Befund ist die **falsche Null**, nicht der Satz. Auch die Checkpoints
  halten die Grenze sonst: kein Poolitem jenseits seines Kapitelfensters (32 geprüft),
  und die 16 Lesen-Aussagen bleiben ohne Teilzeichenkette und ohne Ein-Token-Abstand.

---

## MAJOR

| Feld | Zitat | Problem | Fix (pasteable) | Schwere |
|---|---|---|---|---|
| `leitpunktKeyword` in `src/lib/lesson/writing.js:33–43` ↔ `scoreWriting` ebenda, Zeile 84–89 ↔ `src/components/lesson/GradedWriting.jsx:255–285` ↔ `src/data/curricula/a11.js` → `schreiben.sample` von L2, L8 und L10 | Die Funktion: `words(leitpunkt).map(w => w.replace(…)).find(w => w.length > 2 && !STOPWORDS.has(w.toLowerCase()))` — **ein** Token je Leitpunkt, dann `lower.includes(kw.toLowerCase())` · gemessen, die abgeleiteten Schlüsselwörter der drei L2-Leitpunkte: `["Name", "Land", "Familienstand"]` · meine eigene, prüfungsgerechte L2-Mitteilung (30 Wörter, Anrede, Gruß, **alle drei** Leitpunkte beantwortet): „Sehr geehrte Damen und Herren, ich heiße Ana Chakiri. Ich bin am 3. Mai 1998 geboren. Ich komme aus Marokko und bin Marokkanerin. Ich bin ledig. Viele Grüße, Ana Chakiri" → `length:ok anrede:ok gruss:ok **lp0:FAIL lp1:FAIL lp2:FAIL**` · dasselbe bei L8 („Neuer Tag und neue Uhrzeit" → Schlüssel `Neuer`): „… Geht es am Dienstag um halb neun? Das ist der **neue** Tag und die **neue** Uhrzeit. …" → `lp1:FAIL` · bei L10 („Wann Sie kommen" → `kommen`): „Ich bin erst um zehn Uhr da." → `lp1:FAIL` · und der Mustertext, den Runde 13 daraufhin geschrieben hat: „Sehr geehrte Damen und Herren, ich bin Ana Chakiri. **Der Nachname ist Chakiri.** Das Geburtsdatum ist der 3. Mai 1998. Ich komme aus Marokko. **Das Land ist Marokko** und die Staatsangehörigkeit ist marokkanisch. **Der Familienstand: Ich bin ledig.** Viele Grüße, Ana" | **Der Formcheck sagt einem Lernenden, ein Leitpunkt fehle, den sein Text beantwortet — und Runde 13 hat darauf den Mustertext an den Checker angepasst statt den Checker an die Sprache.** Drei Messungen. **(a) Das Urteil ist falsch, nicht streng.** Die Zeile auf dem Bildschirm lautet wörtlich „**Ihr Name und Ihr Geburtsdatum — fehlt noch**" über einem Text, der mit „ich heiße Ana Chakiri" beginnt und ein Geburtsdatum nennt. Das ist keine Formstrenge, das ist eine falsche Aussage über eine richtige Antwort. Sie ist nicht benotet (`itemIsScored` verlangt `graded === true` vom Server, die Karte sagt „Formcheck, keine KI-Bewertung") — deshalb MAJOR und nicht BLOCKER —, aber sie ist das **einzige** Feedback, das ein abgemeldeter, offline oder über dem Freikontingent stehender Lernender bekommt, und `mechanicalResult.leitpunktCheck` reicht genau diese Booleans weiter. **(b) Es ist ein struktureller Fehler, kein unglücklicher Leitpunkt.** Ein Leitpunkt ist eine **Aufforderung** („Ihr Land und Ihre Staatsangehörigkeit"), die Antwort ist eine **Aussage** („Ich komme aus Marokko und bin Marokkanerin"). Die beiden teilen fast nie ein Token — das Verfahren funktioniert nur da, wo der Leitpunkt zufällig mit einem Verb beginnt (L4: `kaufen`/`kostet`/`treffen`, drei Treffer). Gemessen über die sechs A1.1-Mitteilungen: 6 von 18 Leitpunkten haben ein robustes Verbschlüsselwort, 2 haben **gar keins** (werden seit Runde 13 stillschweigend fallen gelassen), und 10 hängen an einem Substantiv, das eine natürliche Antwort umschreibt. **(c) Die Reparatur der Runde geht in die falsche Richtung, und man sieht es dem Text an.** Um `Name`, `Land` und `Familienstand` unterzubringen, schreibt der Mustertext „Der Nachname ist Chakiri." (nachdem er „ich bin Ana Chakiri" gesagt hat), „Das Land ist Marokko" (nachdem er „Ich komme aus Marokko" gesagt hat) und „Der Familienstand: Ich bin ledig." — ein Doppelpunktkonstrukt aus einem **Formular**, kein Satz einer Mitteilung. In *Start Deutsch 1* Schreiben Teil 2 kostet das Punkte bei „Kommunikative Gestaltung": der Prüfling soll schreiben, nicht Felder benennen. Der Kurs zeigt dem Lernenden als Muster einen Text, der die Prüfung schlechter besteht als der Text, den derselbe Lernende von sich aus geschrieben hätte. | **Erstens** das Prädikat, das eine Aufforderung auf eine Aussage abbildet, statt eines Tokens: `leitpunktKeywords(leitpunkt)` gibt **alle** Inhaltswörter zurück (nicht das erste), und der Check ist erfüllt, wenn **eines** davon oder **eine seiner Entsprechungen** im Text steht. Die Entsprechungen werden aus dem Wortfeld des Levels gelesen, nicht getippt: `Name` ↔ `heißen`/`Vorname`/`Nachname`, `Land` ↔ `kommen aus`/die Ländernamen des Levels, `Familienstand` ↔ `ledig`/`verheiratet` (beide stehen im Leitpunkt selbst: „Ihr Familienstand: **ledig oder verheiratet**" — der Text des Leitpunkts liefert seine eigenen Synonyme und niemand liest sie). **Zweitens** die Zusicherung, die den Befund misst statt ihn zu glauben: in `tests/curricula.test.mjs` eine Fixture von **je einer handgeschriebenen, prüfungsgerechten Antwort pro Mitteilung** (die drei oben sind gemessen und können übernommen werden), die `scoreWriting(task, antwort).ok === true` erfüllen **muss** — das ist die Gegenrichtung zu RULE 17, und sie ist die wichtigere: RULE 17 fragt „besteht mein Mustertext meinen Checker", diese fragt „besteht eine richtige Antwort meinen Checker", und nur die zweite kann eine falsche Regel entlarven. **Drittens** die zwei Mustertexte zurückbauen, sobald der Checker steht: L2 ohne „Der Nachname ist Chakiri." und „Das Land ist Marokko", L8 ohne „Das ist der neue Tag und die neue Uhrzeit." — ein Mustertext darf nie ein Wort enthalten, das nur für den Checker dasteht, und das ist die Regel, die in den Kopf von `writing.js` gehört. **Viertens** die zwei stillschweigend fallen gelassenen Leitpunkte sichtbar machen: eine Zeile „Warum Sie schreiben — **prüft die KI**" statt gar keiner Zeile; die Aufgabe zeigt drei Leitpunkte und die Liste zeigt zwei, und niemand sagt dem Lernenden warum. | MAJOR |
| `src/data/curricula/a11.js` → `schreiben.sample` von L2, L4, L8, L10 und L12 (fünf der sechs Mitteilungen, **alle in dieser Runde neu geschrieben**) ↔ `scripts/validate-curriculum.mjs` → `modelTextsPassOwnChecklist` (RULE 17) misst nur `scoreWriting`; `lexisScan`/`untaughtTokens` laufen über Poolitems und **über keinen Mustertext** | Gemessen mit dem Maßstab des Kurses selbst, `untaughtTokens(text, levelLexicon('a1.1'))` — die Frage, die `minLektion` jedem der 374 Poolitems stellt: **L2** → `geehrte, Damen, Herren, **marokkanisch**` · **L4** → `**treffen, uns**` · **L8** → `**neuer, neue**` · **L10** → `**erst**, Vielen, Dank` · **L12** → `**meinen**` · L6 → **0** (der einzige saubere) · zur Kontrolle derselbe Lauf über **alle 84 Dialogzeilen und alle zwölf `pretest.model`**: **0 Verstöße**, jede Zeile ist bei ihrer eigenen Lektion lexikalisch gedeckt · die L8-Mitteilung im Wortlaut: „… **Ein neuer Termin**: Geht es am Dienstag um halb neun? Das ist **der neue** Tag und **die neue** Uhrzeit. …" · die Dialogzeile **derselben Lektion 8**, mit ihrem Kommentar: `// NOT „Ich habe einen guten Wecker“: that is Adjektivdeklination im Akkusativ, a fifth Vorgriff the course never names, in the Lektion whose exam part is Hören Teil 1.` → `{ speaker: 'Lena', de: 'Ja, immer. Mein Wecker ist gut.' }` · und `meinen` in L12: der Build wirft zwei Cacheitems genau dafür hinaus — `untaught-form: 64680d9b possessive-articles · Ich habe ___ Bruder gern. (ich — **Vorschau Akkusativ**) → meinen` · die Commit-Nachricht der Runde: „six samples rewritten (35–41 words, **cumulative lexis, no deferred construction**)" | **Die Mustertexte sind die einzige Produktionsfläche des Kurses, die keine Lexisregel liest — und in der Runde, die sie neu geschrieben hat, sind fünf von sechs an Wörtern hängen geblieben, die der Kurs nie lehrt.** Drei Messungen. **(a) Der Kurs hat den Maßstab und wendet ihn hier nicht an.** `minLektion` beantwortet für **jedes** Poolitem die Frage „ist jedes Wort bis Lektion n gelehrt?" und verhindert damit RULE 11b konstruktiv; `lexisScan` läuft über Pool und Handquelle; RULE 15b läuft über Diktat, Vorlesen, `pretest.model` **und** `schreiben.sample` — aber nur für drei Konstruktionsmuster, nie für Wörter. RULE 17, die neue Regel für Mustertexte, misst Länge, Anrede, Gruß und Leitpunkte. Zwischen beiden fällt die Wortfrage durch. Dass die **Dialoge** sie mit 0 bestehen und die **Mustertexte** nicht, ist der Beweis, dass es keine Frage des Anspruchs ist, sondern eine Frage der Messung. **(b) Der L8-Fall ist der teuerste und der peinlichste.** Adjektivdeklination ist die eine Konstruktion, die dieser Kurs namentlich ablehnt, und der Kommentar dazu steht **acht Zeilen über** dem betroffenen Mustertext, in derselben Lektion. Der Mustertext trägt sie dreimal: `ein neuer Termin` (stark, Maskulinum Nominativ), `der neue Tag` und `die neue Uhrzeit` (beide schwach). Ein Lernender, der dieses Muster abschreibt, produziert eine Form, für die ihm der Kurs nie eine Regel gegeben hat — und die er in Lektion 8 auch nicht braucht, weil „Geht es am Dienstag um halb neun?" den Leitpunkt schon beantwortet. **(c) `meinen` ist der Fall, an dem man sieht, dass es eine Klasse ist.** Der Build wirft Poolitems, deren Antwort `meinen` oder `deinen` verlangt, mit dem Grund „Vorschau Akkusativ" hinaus — der Kurs weiß also, dass er den Akkusativ des Possessivartikels nicht lehrt (die L12-notice zeigt `mein`, `meine`, `unser`, `unsere`, `euer`, `eure` und kein einziges `-n`). Und derselbe Kurs schreibt in den Beispieltext derselben Lektion „Am Freitag feiern wir **meinen** Geburtstag." Die Übung ist gesperrt, das Muster nicht. | **Erstens** die Regel, und sie ist eine Zeile, weil der Maßstab schon existiert: in `scripts/validate-curriculum.mjs`, als Erweiterung von RULE 17 — für jede Lektion `untaughtTokens({ questionDe: '', answer: l.schreiben.sample }, knownUpTo.get(l.nr), spec)` und `untaughtTokens(…, l.pretest.model, …)` müssen **leer** sein, harte Null wie RULE 17 selbst, mit einer `LICENSED_CHUNKS`-Ausnahme für die Anrede- und Grußformeln, die eine Mitteilung braucht und kein Wortfeld führt (`Sehr geehrte Damen und Herren`, `Vielen Dank`, `Mit freundlichen Grüßen`) — als **Chunk**, nicht als Einzelwörter, damit `marokkanisch` nicht mit durchrutscht. **Zweitens** RULE 15b um das Muster erweitern, das L8 erwischt: `adjective-declension` in `src/data/curricula/constructions.js`, `re: /\b(?:ein|eine|der|die|das|den|dem)\s+[a-zäöüß]+(?:e|en|er|es)\s+[A-ZÄÖÜ]/g`, `introducedAt` → `null` (A1.1 lehrt sie nie, also ist sie in jeder Lektion aufgeschoben) — gemessen gegen den Bestand **bevor** sie geschrieben wird, wie es Runde 12 bei `UNCONDITIONED_RULE` vorgemacht hat: heute trifft sie die L8-Mitteilung dreimal, die L12-Dialogzeile „Mai ist ein schöner Monat!" einmal (Dialog = CONTRACT §2, also nur Produktionsflächen prüfen) und sonst nichts. **Drittens** die fünf Mustertexte reparieren, nicht kürzen: L8 ohne die Adjektivzeile (der Leitpunkt ist ohne sie erfüllt — siehe MAJOR 1), L12 `meinen Geburtstag` → `Geburtstag` („Am Freitag feiern wir Geburtstag."), L10 `erst` → `um zehn Uhr`, L2 `marokkanisch` streichen, L4 den reflexiven Leitpunkt mit „Wann treffen wir uns?" ist durch die Aufgabenbank erzwungen und bleibt — dann aber `sich treffen` als Wortfeldeintrag in L4, damit der Kurs sagt, was er verlangt. **Viertens** die Leseregel dazu in den Kopf von `a11.js`: **ein Mustertext ist eine Produktionsfläche und wird mit demselben Maßstab gemessen wie ein Poolitem — Wörter, Konstruktionen, Fenster. Wer ihn verlängert, verlängert ihn aus dem Wortfeld.** | MAJOR |
| `CONSTRUCTION_PATTERNS` → `separable-verbs-intro`, `skip` in `src/data/curricula/constructions.js:124` ↔ RULE 15b in `scripts/validate-curriculum.mjs:1419` (nominell **harte Null**) ↔ `src/data/curricula/a11.js` L2 `sprechen.readAloud: [2, 6]` ↔ `a1.1-cp1-hoeren-3` | Der Guard: `re: /\b\w+e?[stn]?\b[^.!?]*\s(ein\|auf\|an\|aus\|mit\|zu\|vor\|nach)\s*[.!?]/g` mit `skip: (m, sentence) => !/\b(kauf\|steh\|ruf\|fang\|hör\|mach\|komm\|seh\|bring\|schlaf\|räum\|geh\|fahr\|zieh)(e\|st\|t\|en)\b/i.test(sentence)` — **eine geschlossene Liste von vierzehn Verbstämmen** · gemessen, derselbe Satz zweimal: `deferredConstructionHits(CURRICULUM_A11, 3, 'Bitte **kaufen** Sie das Brot ein.')` → `[{ slug: 'separable-verbs-intro', taught: 11, hit: 'Bitte kaufen Sie das Brot ein.' }]`, `deferredConstructionHits(CURRICULUM_A11, 3, 'Bitte **füllen** Sie das Formular aus.')` → **`[]`** · und der zweite Satz ist kein Testfall, sondern Material: `CURRICULUM_A11.lektionen[1].dialog.lines[2].de === 'Bitte füllen Sie das Formular aus.'`, und L2 hat `sprechen.readAloud: [**2**, 6]` — also **Vorlesezeile**, die `score-readaloud` bewertet · dieselbe Zeile ist `a1.1-cp1-hoeren-3`, **Diktat in Checkpoint 1**, den der Lernende **nach Lektion 3** schreibt · eigener Lauf der rohen Regex **ohne** `skip()` über alle Diktat-, Vorlese-, `pretest.model`- und `sample`-Texte der zwölf Lektionen: **genau ein** Treffer, und es ist dieser | **RULE 15b meldet eine harte Null, und die Null ist keine Messung des Materials, sondern eine Messung einer Verbliste — der eine Satz, der durchfällt, steht in zwei Produktionsflächen gleichzeitig.** Drei Messungen. **(a) Die Lücke ist mechanisch und reproduzierbar.** `füll` steht nicht auf der Liste, `kauf` schon; der identische Satzbau wird einmal gefangen und einmal nicht. Die Liste ist erkennbar aus den Sätzen gewachsen, die frühere Runden gemessen haben — genau die Bauweise, die diese Reihe seit Runde 9 als „Instanz statt Klasse" zurückweist, und diesmal im Herz derjenigen Regel, auf die sich die letzten drei Runden berufen haben. Runde 13 hat die sechs Mustertexte ausdrücklich mit „no deferred construction" beschrieben und sich dabei auf diese Null verlassen. **(b) Der Satz trägt zwei Formen und nicht eine.** „Bitte füllen Sie das Formular aus." ist eine **Satzklammer** (Vorsilbe am Satzende) — gelehrt in Lektion 11, hier in Lektion 2 vorzulesen — und ein **Sie-Imperativ**, den A1.1 in keiner notice, keiner Regelkarte und keinem Item erklärt (das Imperativmuster steht in `OFF_LIMITS_FORMS_A12` als Lektion 8 des **Folgelevels**). Im Dialog gehört beides zu CONTRACT §2 und ist richtig so: eine Beamtin sagt diesen Satz, und der Lernende soll ihn verstehen. Auf `readAloud` und im Checkpoint-Diktat ist es Produktion, und genau diese Unterscheidung ist der Grund, aus dem RULE 15b 2026-09 geschrieben wurde. **(c) Der Preis ist eine benotete Fläche.** `a1.1-cp1-hoeren-3` wird getippt und gewertet, drei Lektionen bevor der Kurs die Satzklammer auch nur erwähnt. Ich werte es als MAJOR und nicht als BLOCKER, weil ein Diktat Nachbildung ist: der Lernende hört den Satz und schreibt ihn, er muss die Regel nicht kennen. Aber er ist die eine Stelle, an der A1.1 eine nie gelehrte Struktur benotet, und die Regel, die das verhindern soll, sieht sie nicht. | **Erstens** den `skip()` von der Stammliste auf eine Formfrage umstellen: eine Satzklammer liegt vor, wenn zwischen einem **finiten Verb an Position 1 oder 2** und einer der Vorsilben am Satzende kein Satzzeichen steht — das finite Verb wird wie in `frontableOrders` an seiner Form erkannt (`/^[a-zäöüß]{2,}(?:e|st|t|en|et)$/` minus `NOT_A_VERB_RE`), nicht an seinem Stamm. Die vierzehn Stämme bleiben als **Positivfixture**, damit die Umstellung keinen bisherigen Treffer verliert, und „Der Teppich ist auf der Terrasse." bleibt die Negativsonde, die der heutige Kommentar schon nennt. **Zweitens** `Bitte füllen Sie das Formular aus.` als benannte Fixture in `tests/curricula.test.mjs`: der Guard **muss** sie als `separable-verbs-intro → L11` melden, sonst ist die Regel wieder eine Liste. **Drittens** das Fenster verschieben, nicht die Zeile umschreiben (CONTRACT §2): L2 `sprechen.readAloud: [2, 6]` → `[**4**, 6]` — Zeile 4 der L2-Dialogs ist frei und trägt das eigene Thema der Lektion; die Checkpoint-Auswahl zieht ihre Diktate aus denselben Fenstern, also folgt `a1.1-cp1-hoeren-3` automatisch. **Viertens** RULE 15b auf die **Checkpoints** ausdehnen, wo sie heute gar nicht läuft: jedes `dictation`- und `read_aloud`-Item von `buildCheckpoint` gegen `deferredConstructionHits(curriculum, checkpoint.afterLektion, text)` — ich habe alle zwanzig gemessen, nach der Umstellung des `skip()` bleibt genau dieser eine übrig, und mit dem verschobenen Fenster keiner. **Fünftens** der Satz hinter allen dreien, der in den Kopf von `constructions.js` gehört: **ein Guard, dessen Kern eine Liste ist, darf keine harte Null melden — er meldet „null über diese Liste", und die nächste Runde liest den Unterschied.** | MAJOR |

---

## Status der Befunde aus Runde 12

Verifiziert an den Artefakten (gebauter Pool, alle drei Ziehungen, die vier gebauten
Checkpoints, die gebauten Lektionsstapel, Validator-Ausgabe, Testlauf, Lint,
Duplikatprüfung), nicht am Diff.

| # | Runde-12-Befund | Status | Beleg |
|---|---|---|---|
| B1 | Vier Wortsalat-Items (drei davon benotete Checkpoint-Aufgaben) lehnen die vorangestellte Zeitangabe ab, die der pretest derselben Lektion nennt und die drei Cacheitems führen | **an den vier gemessenen Items vollständig geschlossen, an der Klasse offen** | `frontableOrders` leitet die Stellung aus dem Kanon ab statt sie zu tippen, mit drei dokumentierten Verengungen (nur Aussagesätze, nur echte Zeit-/Ortsangaben, nur subjektinitiale Kanons), und die Buildausgabe zeigt die vier Reparaturen namentlich: `extra-a11-l03-07 + Aus Marokko kommen wir.`, `extra-a11-l08-08 + Am Dienstag ist der Termin um acht Uhr. · Um acht Uhr ist der Termin am Dienstag. · Am Dienstag um acht Uhr ist der Termin.`, `extra-a11-l11-10 + Gestern habe ich gearbeitet.`, `extra-a11-l12-13 + Im Mai ist mein Geburtstag.` **Ich habe alle sechs erzeugten Formen einzeln gelesen: keine ist falsches Deutsch**, das Verb steht überall auf Position 2, das Subjekt dahinter, Partizip und Vorsilbe bleiben am Ende. Gemessen durch den echten Checker: alle sechs `correct`, in der Übung **und** in den drei benoteten Checkpoint-Kopien (`a1.1-cp1-schreiben-1`, `a1.1-cp3-schreiben-1`, `a1.1-cp4-bausteine-2`). Die drei Cacheitems stehen als `FRONTING_PROBES`-Fixture, die Gegenproben (`extra-a11-l11-01` Frage, `extra-a11-l09-16` ohne Angabe) als Negativfixture, und der Klassenlauf geht über den **gebauten** Pool und über **alle** Checkpoint-Items. **Rest, und er ist BLOCKER 1:** `FRONTABLE_ADVERBIAL_RE` ist eine geschlossene Liste, `von Beruf` steht nicht darauf, und `extra-a11-l02-07` = `a1.1-cp1-bausteine-2` lehnt „Von Beruf bin ich Lehrer." ab, während der L2-pretest genau diesen Anfang akzeptiert. |
| B2 | Drei Fehlerkorrekturen mit zwei minimalen Reparaturen werten eine davon falsch; `ambiguousCorrection` kennt die Kongruenzachse nicht | **an der Kongruenzachse geschlossen, an der Genusachse offen** | Die drei Items tragen jetzt den Cue, den der Kurs schon führte: `extra-a11-l09-14` „Korrigieren Sie **das Verb**: „Du habt Durst."", `extra-a11-l10-10` „… „Sind die Abfahrt um neun Uhr?"", `extra-a11-l10-11` „… „Haben der Fahrer Verspätung?"" — alle drei gemessen im gezogenen Stapel. `agreementAmbiguity` ist eine echte Regel und keine Liste: sie liest die beiden unregelmäßigen Paradigmen aus einer Tabelle, leitet regelmäßige Formen aus dem gemeinsamen Stamm ab, kennt Nullpluralnomen, lässt `sie` bewusst draußen (3sg und 3pl zugleich) und wird durch **jedes** der fünf Cue-Wörter (`verb`, `verbform`, `subjekt`, `artikel`, `nomen`) entschärft; Cacheitems dieser Bauart verlassen den Pool über `REASON.AMBIGUOUS_AGREEMENT`. Gemessen: `agreementAmbiguity` über alle 34 `error_correction`-Items des gebauten Pools → 0 Treffer, `extra-a11-l07-08` („Lena spielen …", nur am Verb reparierbar) bleibt unberührt. **Rest, und er ist BLOCKER 2:** die Regel gibt Artikelkorrekturen ausdrücklich an `ambiguousCorrection` ab (`DETERMINER_FORM_RE`), und die prüft nur den Wechsel der Artikelfamilie — drei L6-Items mit einem Genuspartner im eigenen Wortfeld (`Chefin/Chef`, `Kollegin/Kollege`, `Verkäuferin/Verkäufer`) fallen zwischen beide Regeln. |
| M1 | Alle sechs Mitteilungs-Beispieltexte fallen durch die Formcheck-Liste, die der Bildschirm einen Absatz darüber zeigt (14–24 Wörter gegen „25–45") | **an der Länge geschlossen, an der Sache verschlechtert** | RULE 17 ist gebaut und gut gebaut: `modelTextsPassOwnChecklist` liest Fenster und Leitpunkte aus der **Serverbank** (nicht aus der Lektion), ruft `scoreWriting` — dieselbe Funktion, die `GradedWriting.jsx` aufruft —, läuft über **beide** Zweige (Mitteilung **und** Formular, letzteres über `formularSampleValues`, das den Mustertext in die Feldmap zurückliest) und ist eine **harte Null**. Gemessen: alle zwölf Mustertexte bestehen, die sechs Mitteilungen liegen bei **41 · 37 · 39 · 37 · 35 · 37** Wörtern, alle sechs Formulare füllen jedes Feld. Zwei getippte 30-Wort-Obergrenzen sind aus der Lektion verschwunden, weil die Bank sie führt. **Und der Preis ist MAJOR 1 und MAJOR 2:** die Länge ist mit Füllsätzen bezahlt, die um die Schlüsselwörter des Checkers herum gebaut sind („Das Land ist Marokko und die Staatsangehörigkeit ist marokkanisch." nach „Ich komme aus Marokko.", „Die Nummer ist für das Handy." in L6, „Das ist der neue Tag und die neue Uhrzeit." in L8), und fünf der sechs Texte tragen jetzt Wörter, die der Kurs nie lehrt. Eine Regel, die nur die Länge misst, bekommt Länge. |
| M2 | Die RULE-15b-Reparatur der Runde 12 hat die L3-Modellantwort gegen die Figurenfakten ihres eigenen Dialogs gestellt | **geschlossen, als Klasse und mit einer Regel, die die Fakten liest statt sie zu tippen** | RULE 19 (`modelTextsPassOwnChecklist`s Nachbarin) baut die Faktentafel **aus dem Dialog**: `FACT_VERBS` (`ist`, `sind`, `kostet`, `kosten`), ein Dreitokenfenster als Wertbereich, `HEDGES` als erlaubte Zwischenwörter, die Adjektive des Levels aus dem **Wortfeld** gelesen (`levelAdjectives`) statt gelistet, und eine Genustafel aus dem Wortfeld für die Auflösung von `er`/`sie`/`es`, mit dem sauber begründeten Sonderfall, dass ein Plural, der wie sein Singular klingt (`der Euro` → `Euro`), das Genus des Singulars nicht überschreiben darf. Harte Null, kein Ratchet. L3 lautet wieder `model: 'Der Bruder ist zwanzig. **Er ist Student.**'` mit `accepted: [… 'Der Bruder', 'Die Schwester']` — die Zahl steht bei der Person, die sie im Dialog trägt, und der zweite Satz übt das Personalpronomen, das L3 lehrt. Die L6-Zeile „Die Nummer ist null vier zwei." ist zu „Die Telefonnummer ist null vier zwei drei drei acht eins." geworden und trifft den Leitpunkt wieder. **Rest, klein und als Minor geführt:** „Der Bruder ist zwanzig." nennt auf die Frage „eine Person in **Ihrer** Familie" weiterhin keinen Besitzer — das ist der Preis des Possessivzauns von L12 und war Runde 12s eigener Vorschlag. |
| M3 | Diktat und Vorlesen ziehen in drei von zwölf Lektionen dieselbe Zeile | **zu zwei Dritteln geschlossen, der Rest offen und ehrlich als Eigentümerfrage geführt** | RULE 18 (`sharedProductionLines`) vergleicht **Text** statt Index, genau wie Runde 12 es verlangt hat. Gemessen an den gebauten Lektionen: L6 `readAloud [2,6] → [4,6]` und L8 `readAloud [3,6] → [6,8]` — beide Überschneidungen weg, und beide neuen Zeilen tragen das eigene Thema ihrer Lektion („Und ein Handy?" für den unbestimmten Artikel, zwei Wochentagszeilen für die Uhrzeit). **L3 bleibt bei 1**, und der Ratchet-Kommentar rechnet vor warum: RULE 15b lässt in diesem Dialog genau drei konstruktionsfreie Zeilen (2, 5, 7), zwei disjunkte Fenster brauchen vier, und CONTRACT §2 verbietet das Umschreiben einer Dialogzeile. Der kleinstmögliche Eingriff ist benannt und dem Eigentümer vorgelegt (Zeile 3 „**Meine** Schwester ist noch jung." → „**Sie** ist noch jung."). Das ist die richtige Art, eine Eins stehen zu lassen. |
| M4 | Kein Situationsboden: „Freizeit und Hobbys" serviert im dritten Durchgang 2 von 7 Aufgaben aus dem eigenen Wortfeld | **vollständig geschlossen, als Motorregel und mit einer gelesenen Zahl** | `SITUATION_MIN = 4` steht in `buildLesson.js`, die Füllstufe sitzt zwischen der Tippschwelle und der allgemeinen Füllung, nimmt nie einen Platz, den `PRIMARY_MIN` noch braucht, lockert keine Kappe, und ein Durchgang, der unvollständig endet, wird **ohne** die Füllung neu gezogen — die vollständige Sieben gewinnt. `practiceReport` meldet `situationCount`, `tests/lesson-engine.test.mjs` hat `MAX_OFF_SITUATION_BLOCKS = 0` und eine Mutationsprobe, die beweist, dass die Stufe beißt. Meine eigene Nachmessung mit `relevanceScore(item, ownTerms, ∅)` über alle 36 Blöcke, Versuch 1/2/3: **L1 7/7/7 · L2 7/5/4 · L3 7/6/4 · L4 7/7/7 · L5 7/7/5 · L6 7/5/5 · L7 7/4/4 · L8 6/6/6 · L9 7/5/5 · L10 6/7/7 · L11 7/7/6 · L12 7/6/5** — **kein Block unter 4**, alle 36 `complete`, `relaxUsed = 0`. L7 Versuch 3 steigt von 2/7 auf 4/7. Die sechs bis acht Freizeit-Items, um die Runde 12 ausdrücklich gebeten hat, sind **nicht** geschrieben worden, und L7 liegt deshalb in beiden Wiederholungen auf dem Boden statt darüber — das ist kein Befund (vier von sieben ist die Zahl, die die Regel verspricht, und drei Plätze für die Wiederholung früherer Lektionen sind pädagogisch richtig), aber es ist die Arbeit, die der Motor nicht leisten kann. |

**Zusätzlich geprüft und weiterhin in Ordnung:** die **zwölf Schreibaufgaben** sind mit der
Serverbank deckungsgleich (Aufgabentext, Leitpunkte bzw. Formularfelder, Wortgrenzen), alle
zwölf `taskKey`s vorhanden; `anrede` in allen zwölf Sprechaufträgen `'Sie'`, `speakingAI.mjs`
setzt `'Sie'` als Default und im Systemprompt durch, die zwölf pretests sprechen alle Sie;
RULE 14, RULE 12, RULE 17 und RULE 19 als harte Regeln auf 0; RULE 13 (4 Sprechaufträge ohne
Mission) unverändert vertretbar; die vier Checkpoints: **80** Items, **0** Selbstablehnungen
über **2 463** Bewertungsflächen (252 gezogene, 374 Poolitems, 233 Handitems und alle
getippten Checkpoint-Items, jeweils mit **jeder** `accepted`-Variante), **32 von 32**
verschiedene Poolitems, **keine** doppelt genutzte Dialogzeile, **kein** Poolitem jenseits
seines Kapitelfensters, und die 16 Lesen-Aussagen weiterhin ohne Teilzeichenkette und ohne
Ein-Token-Abstand; die Diktate halten realistische Lernerschreibungen aus (ohne Kommas, ohne
Bindestrich in der Telefonnummer, ohne Apostroph in „Machs gut" — alle `correct`); **395**
Wiederholungskarten, **670** Flächen, **0** Selbstablehnungen gegen `gradeTypedReview`;
`a11.audio.js` weiterhin der leere Manifest-Stub, alle Hörflächen fallen ehrlich auf die
Computerstimme zurück (Eigentümeraufgabe, kein Befund). **Die Erinnerungsmail duzt weiterhin,
und das lasse ich als Eigentümerentscheidung stehen — kein MAJOR**, wie in Runde 5 bis 12;
die Notiz darüber im Kopf von `course-reminder.mjs` fehlt zum neunten Mal (Minor 8).

**Zu den Änderungen dieser Runde, einzeln gelesen.** Es sind bemerkenswert wenige Zeilen
Material und bemerkenswert viel Regel — 1 815 eingefügte Zeilen, davon über 1 100 in
`validate-curriculum.mjs`, `quality.js` und den vier Testdateien. Das ist genau die
Richtung, die diese Reihe seit Runde 9 verlangt. Die drei neuen Validatorregeln sind
sauber gebaut und lesen ihre Maßstäbe aus den Daten: RULE 17 holt Fenster und Leitpunkte
aus der Serverbank, RULE 18 vergleicht Text statt Index, RULE 19 baut seine Faktentafel
aus dem Dialog und seine Adjektivliste aus dem Wortfeld. `frontableOrders` und
`agreementAmbiguity` sind die beiden besten Prädikate, die `quality.js` besitzt, jedes mit
Positiv- und Negativfixtures und mit einem Lauf über die benoteten Flächen. **Das einzige
schwächere Stück ist das Material, das die Regeln bezahlt hat:** sechs neu geschriebene
Mustertexte, von denen fünf an der Lexis hängen bleiben und drei sichtbar um einen Checker
herum formuliert sind. Das ist derselbe Mechanismus wie in Runde 12, als die
RULE-15b-Reparatur zwei Modelltexte verschlechterte — und es ist der Grund, warum diese
Reihe zweimal hintereinander dieselbe Bitte formuliert: **wer einen Text ändert, um eine
Regel zu erfüllen, misst den neuen Text mit allen Regeln, nicht mit der einen.**

---

## Bewertungsübersicht (Runde 13, Runde 12 in Klammern)

| Lektion | Deutsch | Niveau | Dialog | Prüfung | Kohärenz | Σ /25 |
|---|---|---|---|---|---|---|
| 1 Hallo, ich bin … | 4 (4) | 4 (4) | 5 (5) | 4 (4) | 4 (4) | **21** (21) |
| 2 Ich bin Studentin | **3** (4) | **3** (4) | 4 (4) | **3** (4) | 3 (3) | **16** (19) |
| 3 Meine Familie | **4** (3) | 4 (4) | 4 (4) | **4** (3) | **4** (3) | **20** (17) |
| 4 Auf dem Flohmarkt | 4 (4) | 4 (4) | 5 (5) | 4 (4) | 4 (4) | **21** (21) |
| 5 Im Klassenzimmer | 5 (5) | 4 (4) | 5 (5) | 4 (4) | 4 (4) | **22** (22) |
| 6 Der erste Tag im Büro | 5 (5) | 4 (4) | 4 (4) | **3** (4) | 4 (4) | **20** (21) |
| 7 Freizeit und Hobbys | 4 (4) | 4 (4) | 5 (5) | 3 (3) | **4** (3) | **20** (19) |
| 8 Termine und Uhrzeit | **3** (4) | **3** (4) | 5 (5) | **4** (3) | **4** (3) | **19** (19) |
| 9 Im Café | **5** (4) | 4 (4) | 5 (5) | 4 (4) | 4 (4) | **22** (21) |
| 10 Am Bahnhof | 4 (4) | 4 (4) | 5 (5) | 4 (4) | 4 (4) | **21** (21) |
| 11 Mein Tag | 4 (4) | 4 (4) | 4 (4) | **4** (3) | 4 (4) | **20** (19) |
| 12 Feste feiern | 5 (5) | **3** (4) | 5 (5) | **4** (3) | 4 (4) | **21** (21) |

Kursmittel **20,3/25** (Runde 12: 20,1 · Runde 11: 20,4 · Runde 10: 20,5 · Runde 9: 20,5 ·
Runde 8: 20,4 · Runde 7: 20,3 · Runde 6: 19,9 · Runde 5: 19,7 · Runde 4: 19,9 · Runde 3:
20,4 · Runde 2: 19,8 · Runde 1: 17,2). Fünf Lektionen besser, drei schlechter, vier gleich.

**Zur Lesart.** Die Gewinner sind die Lektionen, in denen eine Motorschließung ankommt, ohne
dass ein neu geschriebener Text sie wieder aufzehrt: **L3 (17 → 20)** — die Modellantwort
steht wieder bei den Figurenfakten ihres Dialogs, und die vorangestellte Ortsangabe wird in
der Übung **und** in `a1.1-cp1-schreiben-1` akzeptiert; **L9 (21 → 22)** — die
Kongruenzkorrektur trägt ihren Cue, und L9 ist die Lektion, deren Items Runde 12 als „genau
die Lücke" gelobt hat; **L11 (19 → 20)** und **L12 (21 → 21)** — „Gestern habe ich
gearbeitet." und „Im Mai ist mein Geburtstag." sind richtig, wo sie es sein müssen, aber L12
bezahlt es mit einem Mustertext, der `meinen` ohne Akkusativregel zeigt; **L7 (19 → 20)** —
der Situationsboden hebt den dritten Durchgang von 2/7 auf 4/7. Bei den Verlierern zähle ich
zwei Gruppen. **Neu entstanden** ist nur einer: **L2 (19 → 16)**, und er ist der schwerste
Einzelabsturz dieser Reihe seit Runde 4 — die Lektion trägt BLOCKER 1 (ihr eigener pretest
widerspricht ihrem eigenen Item und ihrer eigenen benoteten Checkpoint-Aufgabe), ihr
Mustertext ist in dieser Runde zu einer Aneinanderreihung von Formularzeilen geworden, und
ihre Vorlesezeile ist der Satz, an dem RULE 15b eine Null meldet, die sie nicht hat.
**Neu gemessen, nicht neu entstanden** sind die übrigen: **L6 (21 → 20)** verliert an
BLOCKER 2, und die drei Items stehen seit dem ersten Handitem-Batch im Pool; **L8 (19 → 19)**
gewinnt an RULE 18 und der Wortstellung, was es an seinem neuen Mustertext verliert (dreimal
Adjektivdeklination in der Lektion, deren Dialog namentlich davor geschützt wurde);
**L12 (21 → 21)** dasselbe eine Etage kleiner. **Stärkste:** L5 und L9 (22). **Schwächste:**
L2 (16).

---

## Würde ich meinen Namen jetzt daraufsetzen?

**Auf den Motor, auf die Dialoge, auf die Checkpoints, auf die Regelkarten und auf die
Validatorregeln: ja. Auf den Kurs als Ganzes: nein — und zum ersten Mal in dieser Reihe aus
einem Grund, der nicht das Material ist, sondern die Bauweise der Regeln.**

Ich habe alle sechs Punkte aus Runde 12 an den Artefakten nachgeprüft: **beide BLOCKER und
drei der vier MAJOR sind an ihren gemessenen Instanzen vollständig geschlossen**, der vierte
(der Situationsboden) ist als Motorregel geschlossen und besser geschlossen als bestellt.
Die Arbeit ist gut. `frontableOrders` erzeugt sechs Wortstellungen, und ich habe keine
gefunden, die falsches Deutsch wäre — das ist bei einem mechanischen Umbau deutscher
Hauptsätze keine Selbstverständlichkeit, sondern das Ergebnis von drei sorgfältig
begründeten Verengungen. `agreementAmbiguity` liest Paradigmen statt Ids. RULE 19 baut seine
Faktentafel aus dem Dialog. Der Situationsboden zieht neu, statt eine Kappe zu lockern.
2 463 Bewertungsflächen, null Selbstablehnungen; 36 Blöcke, null Antwortlecks, null
Lockerungen, sieben verschiedene Aufgabenformen in jedem einzelnen.

**Und trotzdem: beide neuen BLOCKER sind der geschlossene BLOCKER, eine Achse weiter — und
das ist kein Zufall, sondern die vorhersehbare Folge davon, wie die Regeln gebaut sind.**
`FRONTABLE_ADVERBIAL_RE` ist eine Liste von siebzehn Angabeformen, und der Kommentar sagt
das offen. `agreementAmbiguity` ist eine Liste von zwei Paradigmen plus einer Regelendung
und reicht die Artikel weiter an eine Regel, die nur eine von zwei Artikelachsen kennt.
`CONSTRUCTION_PATTERNS.separable-verbs-intro` hat eine Liste von vierzehn Verbstämmen im
`skip()`. Jede dieser Listen ist aus den Sätzen gewachsen, die eine frühere Runde gemessen
hat, und jede fällt bei dem ersten Satz um, den keine Runde gemessen hat: `von Beruf`,
`Chefin/Chef`, `füllen`. Runde 12 hat das im vierten Punkt ihres zweiten BLOCKERs wörtlich
vorausgesagt und den Fix benannt — **einen Generator, der zu jedem typisierten Item die
zweite Kandidatenantwort mechanisch erzeugt und verlangt, dass sie akzeptiert oder vom
Prompt ausgeschlossen ist**. Gebaut wurden stattdessen zwei weitere Achsen. Der Generator
ist die Arbeit, die diese Reihe beendet; zwei weitere Achsen sind die Arbeit, die sie um
eine Runde verlängert.

Die drei MAJORs sind kleiner und hängen alle an derselben Fläche: den Texten, die der Kurs
als Muster zeigt. Dass `leitpunktKeyword` eine prüfungsgerechte Mitteilung an drei von drei
Leitpunkten durchfallen lässt, ist in dreizehn Runden niemandem aufgefallen — mir
eingeschlossen —, und es ist der Grund, aus dem der L2-Mustertext jetzt „Der Familienstand:
Ich bin ledig." sagt. Dass keine Regel die **Wörter** eines Mustertextes liest, während
`minLektion` genau diese Frage für alle 374 Poolitems beantwortet, ist die Lücke, durch die
die Adjektivdeklination ausgerechnet in die Lektion gekommen ist, deren Dialogzeile mit
einem Kommentar davor bewahrt wurde. Und dass RULE 15b eine harte Null meldet, die in
Wahrheit eine Null über vierzehn Verbstämme ist, ist derselbe Befund noch einmal, eine
Ebene tiefer.

Die Arbeit ist überschaubar und vollständig ausgeschrieben: der Kandidatengenerator für
typisierte Items (er schließt beide BLOCKER **und** macht drei bestehende Regeln
überflüssig); `leitpunktKeywords` statt `leitpunktKeyword`, mit einer Fixture aus je einer
richtigen Antwort pro Aufgabe; eine Lexiszeile in RULE 17, die `untaughtTokens` auf Muster-
und Modelltexte anwendet, plus ein `adjective-declension`-Muster in `constructions.js`; der
`skip()` der Satzklammer auf eine Formfrage statt auf eine Stammliste; RULE 15b über die
Checkpoint-Diktate; L2 `readAloud [2,6] → [4,6]`; und fünf Mustertexte, die aus dem
eigenen Wortfeld verlängert werden statt um einen Checker herum. Das ist wieder etwa ein
Tag. **Und ich bitte diesmal um eine Sache mehr als um Code: bevor die nächste Runde eine
Liste erweitert, soll sie fragen, ob die Liste durch eine Frage an die Daten ersetzbar ist.
Dreimal in diesem Bericht wäre die Antwort ja gewesen.**

---

**Minors (acht Zeilen, nicht erfasst):** (1) Die Buchstabenkarte mischt weiter Groß- und
Kleinschreibung in der Namensliste (`be, ce, de` neben `Jot, Vau, Ypsilon, Zett`, dazu
`W we` im `content` gegen „W heißt **We**" in `commonMistakes`), und **kein Item des ganzen
Pools prüft Q** (nachgemessen im gebauten Pool: 0 Treffer). Unverändert seit Runde 10.
(2) Die `-chen/-lein`-Zeile der L4-Karte („das **Mädchen**, das **Brötchen**") lehrt zum
fünften Mal eine Endungsgruppe, von der der Kurs **null** Wörter enthält (nachgezählt im
gebauten Pool: 0 Items mit `Mädchen` oder `Brötchen`); sie sollte ersatzlos fallen.
(3) Im Lesen-Teil unverändert seit Runde 10: `a1.1-cp1-lesen-3` („**Der Student** spricht
Englisch und Deutsch.", Schlüssel *Falsch*, Erklärung „Im Text steht „Bruder", nicht
„Student"") ist im Text nicht widerlegt, sondern unerwähnt — in *Start Deutsch 1* deckt
„Falsch" das ab, aber die Erklärung behauptet einen Widerspruch, den es nicht gibt; und
`a1.1-cp3-lesen-1`/`-4` sind zweimal dieselbe Umformung mit derselben Sprecherin und
demselben Verb („Ana hat Durst." / „Ana hat Hunger.") — zwei von vier Items eines Papiers.
(4) Der `UNCONDITIONED_RULE`-Lint läuft weiter nur über Poolitems; Runde 11 hat „des Pools,
**der Karten und der notices**" verlangt. Ich habe wieder beide gemessen: 0 materielle
Treffer, aber dieselben zwei **wahren** Kartensätze lösen das Prädikat aus
(`definite-articles`: „Der Artikel steht **immer** vor dem Nomen"; `possessive-articles`:
„Die höfliche Form ist Ihr und steht **immer** mit großem I") — das Carve-out für „steht
immer vor/mit <Wortart>" ist zum zweiten Mal ungeschrieben. (5) Die Vier-Buchstaben-Grenze
der Leckachse hat weiter genau einen inhaltlichen Rest: L11 Versuch 3 serviert
`extra-a11-l11-04` („Kaufst du heute ___? (einkaufen)" → `ein`) neben `8a2be339`
(„___ ihr heute **ein**? (einkaufen)"). (6) Der Topic-Slug `numbers` erzeugt im
Wiederholungsindex weiter eine Doppelkarte: `pattern:numbers` ist Zeichen für Zeichen
`pattern:verb-sein`, gemessen über alle **395** Karten die einzige Dublette. (7) **Sieben**
gezogene Items tragen als `questionEn` die deutsche Frage statt einer Übersetzung
(`34957c67`, `0fe8146d`, `dd1c3d60`, `cb35f9d0`, `da9e486f`, `9fd52b13`, `e0ac0425`);
`af4fc6ff` schreibt als einziges Item des gezogenen Stapels gerade Anführungszeichen
(`Ihr ___ "Tschüss".`) statt der deutschen; und `extra-a11-l12-17` („Mach's gut und ___ Tag
noch!") trägt den Cue „**(Adjektiv)**" über einer festen Wendung des Wortfelds — der Kurs
hat für diesen Fall die ehrlichere Formel „fester Ausdruck" und benutzt sie zweimal.
(8) L3 nutzt weiter eine Dialogzeile doppelt (RULE 18 = 1, Eigentümerentscheidung offen);
die L3-Modellantwort „Der Bruder ist zwanzig." nennt auf „eine Person in **Ihrer** Familie"
keinen Besitzer; `a11.audio.js` ist weiter der leere Manifest-Stub;
`tests/lesson-extra-items-a12.test.mjs:464` ist weiter der einzige übersprungene Test der
Suite (868 Tests, 867 grün) — korrekt geführt, aber er sollte mit A1.2 wieder anspringen;
der Rückblick sagt dem Lernenden nirgends, dass ein zweiter Durchgang **andere** Aufgaben
bringt; und die Eigentümerentscheidung zur du-Anrede der Erinnerungsmail ist zum neunten
Mal nirgends aufgeschrieben.

BLOCKER: 2 · MAJOR: 3
