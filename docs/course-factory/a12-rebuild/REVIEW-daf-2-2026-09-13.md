
# A1.2 — DaF-Fachreview #2 (2026-09-13)

Reviewer: dieselbe DaF-Lehrkraft und Prüferin für Goethe-Zertifikat A1 *Start Deutsch 1* wie in
Review #1. Zweiter Durchgang, `claude/sharp-gauss-tbxb46` @ `85d14bd` („A1.2 round 2 — personas
kept, notice forms exemplified, no Vorgriffe in production lines, exam Teile backed"). Gelesen
wurde ausschließlich der **committete** Stand in einem eigenen Worktree; die unfertige A1.1-Arbeit
im Baum ist ausgeklammert.

Neu in dieser Runde — und der Grund, warum dieses Review härter ausfällt als das erste: **der Pool
existiert.** Review #1 durfte über Items nur sagen, was die Vorgaben hergeben. Jetzt gibt es
`src/data/lessonPools/a12.json` (354 Items: 238 aus der Altbank nach `quality.js`, 116
handgeschrieben in `a12.extra.json`) und zwölf A1.2-Regelkarten. Damit ist zum ersten Mal
prüfbar, was der Lernende **wirklich bekommt**.

Gemessen, nicht angenommen:

* `src/data/curricula/a12.js`, alle zwölf Lektionen, jedes Feld — 120 Dialogzeilen, 12 notices,
  12 pretests, 12 Phonetikslots, 12 `hoeren`/`sprechen`/`schreiben`-Blöcke, 201 Wortfeldeinträge.
* **DER ECHTE ZUG:** `planPractice(CURRICULUM_A12, a12.json, 1)` und `…, 2)` über
  `src/lib/lesson/buildLesson.js` — **168 Items** (12 × 7 × 2 Versuche, 100 verschiedene).
  **Jedes einzeln** beurteilt: gegen `checkAnswer` (`src/lib/lesson/check.js`) mit
  `strict: STRICT_TOPIC.test(topic)` und `caseSensitive: isCaseTask(item)` wie `PracticeItem.jsx`
  es aufruft, gegen `drillsSlug(item, slug)` und `isUsableItem(item, { level:'a1.2' })` aus
  `src/data/lessonPools/quality.js`, und als Lehrerin: Ist die Antwort durch den **deutschen**
  Prompt bestimmt? Bestraft das Item korrektes Deutsch? Sind Formen und Wörter bis hierher
  gelehrt? Stimmt das Register?
  Maschinell sauber: **168/168** lösen ihre eigene Musterantwort als `CORRECT`, **0** Items sind
  nach `exclusionReason` unbrauchbar, **0** Items sind unter ihrem `primarySlug` abgelegt, ohne ihn
  zu üben. Das ist ein gutes Ergebnis und steht so in keinem A1.1-Review.
* **Alle 116 handgeschriebenen Items** einzeln gelesen; dazu maschinell: `accepted` bei 116/116
  vorhanden, `questionEn` bei 116/116, `caseSensitive: true` bei genau den zwei Höflichkeitsitems
  (`extra-a12-l06-10`, `extra-a12-l09-09`).
* **Die zwölf A1.2-Regelkarten** in `scripts/rule-card-overrides.mjs` **und** in der generierten
  `netlify/functions/_shared/ruleCards.mjs`, jede gegen die `notice` ihrer eigenen Lektion.
* **Der Checkpoint-Builder auf A1.2**: `buildCheckpoint({ curriculum: CURRICULUM_A12, checkpoint,
  pool: a12.json, seed: 1 })` für `a1.2-cp1 … cp4`. Er baut: **4 × 20 = 80 Items**, alle
  fünf Sektionen gefüllt. Alle 80 einzeln beurteilt.
* `node scripts/validate-curriculum.mjs a1.2`: **RULE 10: 0 · RULE 11: 156 · RULE 12: 0 ·
  RULE 13: 1 · RULE 6b: 0 · RULE 15: 0 · RULE 16: 0 · RULE 14: 0** — und jede der sieben
  Ratchet-**Zahlen** gegen die Messung gehalten, nicht nur das Urteil.
* `npm test`: **692 von 693**. Eine Zusicherung ist rot (siehe BLOCKER 1).
  `node scripts/check-duplicates.mjs`: der Astro-Zwilling ist byte-identisch.
* `src/data/writingTasks.js` + `netlify/functions/_shared/writingTasks.mjs` (zwölf `a12-l*`, Paar
  identisch) gegen `schreiben` und gegen `netlify/functions/evaluate-writing.mjs` Zeile für Zeile
  — `MIN_CHARS`, `task.register`, `isCourse`, `courseAllowance`, `COURSE_TASK_KEY_PREFIX` und der
  `insert` in `writing_submissions`.
* **Die echten Zeilen erneut über den Supabase-Connector** (read-only): `reading_lessons` a1.2
  (`order_index` 1–10, mit `title_de`/`topic`), `listening_exercises` a1.2 (`exercise_number` 1–6,
  mit `exercise_type`), `speaking_missions` a1.2 (`mission_order` 1–12, mit `target_structures`) —
  und Zeile für Zeile gegen `links`, `examTeile` und `sprechen.open.missionOrder` gehalten.
* `docs/course-factory/a12-rebuild/CONTRACT.md` in der Fassung von `85d14bd`, und
  `docs/course-standard-2026-09-12.md` §2.2–§2.4, §3.

## Urteil in einem Absatz

**Runde 2 hat sehr viel richtig gemacht, und sie hat dabei zum zweiten Mal dieselbe Bewegung
vollzogen: der Befund wurde dort repariert, wo die Regel hinsieht, und ist dorthin ausgewichen, wo
sie es nicht tut.** Was geschlossen ist, ist wirklich geschlossen und soll zuerst gesagt werden:
Ana bleibt ledig, RULE 14 läuft mit einer echten `PERSONAS_A12`-Tabelle und meldet 0; von 39
unbelegten Fettstellen sind **0** übrig, L2 schreibt jetzt *einundzwanzig* und
*vierhundertachtzig* aus, L3 heißt „den und einen" und vertagt `keinen` ausdrücklich auf L4, L11
löst das Versprechen aus L1 mit `zum`/`zur` ein; die 23 Dativtreffer vor Lektion 11 sind aus
Diktat und Nachsprechen **vollständig** verschwunden; L6 sagt „Die Hand und der Fuß **sehen gut
aus**" und „Der Rücken und der Bauch **tun weh**"; die beiden falschen Phonetikslots sind sachlich
richtig; die `pretest.accepted`-Listen tragen jetzt die Struktur statt des Satzanfangs; die vier
erfundenen Formulare sind SD1-Anmeldungen mit `Unterschrift`; L7 führt den Nominativ als Kontrast
zu L3 statt als Neueinführung, bekommt die einzige echte *Sprechen-Teil-1*-Mission des Niveaus und
löst damit das falsche Prüfungsetikett von L6 auf; die thematischen Fehlgriffe in der Datenbank
(„Am Wochenende" an der Festlektion, „Essen und Trinken" an der Arztpraxis, „Im Büro" an den
Wochenendplänen) sind alle drei korrigiert; und `anrede` steht zwölfmal da, mit Begründung. **Und
trotzdem unterschreibe ich wieder nicht, aus sechs Gründen, von denen fünf dieselbe Form haben.**
Erstens ist die Testsuite **rot**: `tests/curricula.test.mjs:961` erwartet `[4, 10, 11]` und
bekommt `[11]` — ausgerechnet die Zusicherung, die die Hauptleistung dieser Runde absichert, wurde
nicht mitgezogen, und der CONTRACT behauptet in §4, sie täte es. Zweitens sind **die zwölf
Regelkarten der Stand von Runde 1**: `ruleCards.mjs` wurde nach den notice-Reparaturen nicht neu
erzeugt, also heißt L3s Karte weiter „Der Akkusativ: den, einen, **keinen**" und übt `kein Park →
keinen Park`, während die notice derselben Lektion sagt, diese Form komme erst in Lektion 4; L1s
Karte trägt „**Neben der** Apotheke ist die Bank" — genau die Zeile, die BLOCKER 3 aus dem Diktat
entfernt hat — samt einem darauf gebauten `commonMistake`; L6s Karte sagt weiter „Ihr Auge
**sieht** gut"; L10s Karte lehrt weiter `gegen`, `um`, `teurer`, `größer`, `besser`. Sechs von
zwölf Karten widersprechen ihrer eigenen Lektion, und die Karte ist das, was
`explain-answer.mjs` auf „Erklär mir das" ausliefert. Drittens sind die Vorgriffe nicht weg,
sondern umgezogen: RULE 15 liest `hoeren.lines ∪ sprechen.readAloud` und meldet 0 — im **Pool**
tragen **21 von 96** handgeschriebenen Items vor Lektion 11 eine Dativ- oder
Wechselpräposition, **acht davon in Items, die der Lernende ganz tippt**; `extra-a12-l01-09` ist
wörtlich die gelöschte Diktatzeile („Neben der Apotheke ___ die Bank"), und `extra-a12-l07-06`
lässt „Wer ist die Frau **mit dem kurzen Haar**?" *bauen* — Dativ plus Adjektivdeklination, vier
Lektionen zu früh, und der Checkpoint nach Lektion 9 verlangt denselben Satz noch einmal.
Viertens prüft **kein einziger Ratchet den Checkpoint**, und der Checkpoint nach Lektion 3 verlangt
`keinen` („Ich habe ___ Schlüssel", „Du hast keinen Hund") — die Form, die L3s eigene Karte auf
Lektion 4 vertagt —, während der Checkpoint nach Lektion 12 „Er **ist** nach München geflogen"
verlangt, obwohl L12s Karte das haben-Perfekt lehrt und `bin` als Fehler anstreicht. Fünftens
meldet RULE 16 „0 ungedeckte Prüfungsteile", prüft aber nur, **ob ein Link existiert**, nie, ob
die verlinkte Zeile das **Format** des genannten Teils hat: gegen die echten Zeilen gehalten sind
**sechs von elf** Hören-/Lesen-Behauptungen weiterhin falsch etikettiert (L3 „Lesen Teil 3" =
Schilder → „Beim Bäcker"; L7 „Lesen Teil 2" = Anzeigen → „Meine Hobbys"; L9 und L12 ebenso; und
alle sechs Hörübungen sind `exercise_type = short_dialogues`, also sind L2s „Hören Teil 2" und L11s
„Hören Teil 3" ebenfalls unbelegt). Sechstens — und das ist der einzige Befund, der nichts mit
Didaktik zu tun hat — stimmt die offene Frage des CONTRACT §6.1 **nicht**: `COURSE_TASK_KEY_PREFIX
= 'a11-'` bedeutet nicht, dass A1.2 auf das Prüfungskontingent zahlt, sondern dass es **auf gar
kein Kontingent zahlt**; nachgerechnet in `evaluate-writing.mjs` bekommt ein kostenloser Lernender
für A1.2-Schreibaufgaben eine Grenze von 12, gezählt über Zeilen, die eine A1.2-Abgabe nie
schreibt. **Das ist wieder ein Tag Arbeit, und danach ist dieses Modul besser als A1.1 nach sechs
Runden — aber die Lehre dieser Runde muss in den CONTRACT, sonst kostet sie eine dritte: eine
Regel, die nur die Fläche liest, auf der der letzte Befund stand, verschiebt den Befund, statt ihn
zu schließen.**

**Zahlen:** 6 BLOCKER · 15 MAJOR. Minors sind nicht erfasst (fünf Zeilen am Ende).

---

## BLOCKER

| Feld | Zitat | Problem | Fix (pasteable) | Schwere |
|---|---|---|---|---|
| `tests/curricula.test.mjs:961` („A1.2 ratchets are the measured numbers") gegen `missionlessLektionen(CURRICULUM_A12)` und gegen `CONTRACT.md` §4 | `assert.deepEqual(missionlessLektionen(CURRICULUM_A12), [4, 10, 11]);` · gemessen: `[11]` · `npm test`: **692 pass, 1 fail** · CONTRACT §4: „**`tests/curricula.test.mjs` already pins the list as `[11]`**" | **Die Testsuite ist rot, und zwar an genau der Zusicherung, die die Hauptleistung dieser Runde absichert.** Die Missionsneuzuordnung ist gelandet (L2↔4, L5↔11, L4→6, L10→8, L7→9, L12→10), also ist `missionlessLektionen` korrekt `[11]` — aber der Test, der die alte Liste festnagelt, wurde nicht mitgeändert. Drei Folgen, jede für sich schwer: (a) CI (`.github/workflows/ci.yml` ruft `npm test`) ist rot, der Branch kann nicht mergen; (b) der CONTRACT behauptet in §4 das Gegenteil — **er beschreibt einen Zustand, der nicht eingetreten ist**, und §4 ist genau das Dokument, das der Integrator statt der Datei liest; (c) der Kommentar über dem Test („RULE 13 is 3 because … three Lektionen … have no mission that fits their situation") ist die Begründung, die derselbe CONTRACT eine Seite weiter als **falsch** zurückgezogen hat. Review #1 hat `npm test` mit 673/673 grün gemessen; diese Runde hat die Suite verlassen, ohne sie zu fragen. | Drei Zeilen, eine Entscheidung. In `tests/curricula.test.mjs` den Kommentarabsatz durch die richtige Begründung ersetzen und `` assert.deepEqual(missionlessLektionen(CURRICULUM_A12), [4, 10, 11]); `` → `` assert.deepEqual(missionlessLektionen(CURRICULUM_A12), [11], 'nur L11 hat keine passende Mission — kein A1.2-Sprechauftrag handelt vom Wetter'); ``, dazu `` assert.ok(r.missionlessLektionen <= 3, …) `` → `` assert.ok(r.missionlessLektionen <= 1, 'der Ratchet folgt der Messung und darf nur sinken'); `` und in `scripts/validate-curriculum.mjs` die Registry-Zeile `missionlessLektionen: 3` → `1`. Und die Regel, die die Klasse schließt, ist eine Zeile im PR-Ablauf, nicht im Code: **`npm test` gehört in denselben Commit wie die Datenänderung** — `.claude/skills/steward/SKILL.md` nennt die Gates bereits; was fehlt, ist, dass der CONTRACT §4 den Testzustand nicht behaupten darf, sondern den Befehl nennen muss, mit dem man ihn misst. | BLOCKER |
| `scripts/rule-card-overrides.mjs` (A1.2-Hälfte, Zeilen 473 ff.) und die daraus generierte `netlify/functions/_shared/ruleCards.mjs`, gegen die `notice` derselben Lektion | **L3** Karte: `titleDe: 'Der Akkusativ: den, einen, keinen'`, content: `'… der Markt → den Markt. ein Supermarkt → einen Supermarkt. kein Park → keinen Park.'`, `commonMistakes[2]`: `'Im Zentrum brauche ich keine Park.' → 'Im Zentrum brauche ich keinen Park.'` · dagegen L3 `notice`: `title: 'Der Akkusativ: den und einen'`, `'… Die Form keinen kommt in Lektion 4 dazu.'` · **L1** Karte: `'Weitere Beispiele: Die Apotheke ist rechts. Neben der Apotheke ist die Bank.'` und `commonMistakes[2]`: `'Die Bank neben der Apotheke ist.' → 'Die Bank ist neben der Apotheke.'`, dazu `'Danach kommt die Brücke.'` · **L6** Karte: `'Ana nimmt die Tablette am Abend. Ihr Auge sieht gut.'` · **L10** Karte: `'Nach für, ohne, gegen, um und durch …'`, `'Ich gehe durch das Kaufhaus. Der Bus fährt um den Platz.'`, `'Das Kleid ist teurer. Unregelmäßig: groß → größer, gut → besser.'` | **Die notices wurden repariert, die Karten nicht — und die Karte ist die zweite Stelle, an der derselbe Lernende dieselbe Regel liest.** `netlify/functions/explain-answer.mjs` importiert `ruleCard`/`ruleCardText` und gründet darauf die Antwort auf „Erklär mir das"; der Commit von Runde 2 hat `a12.js`, `CONTRACT.md` und `writingTasks` angefasst und die Karten nicht neu erzeugt. Vier Widersprüche, alle in derselben Lektion wie ihre notice: **(a) L3.** Die Karte lehrt `keinen`, die notice vertagt es. Der Lernende, der die notice liest und dann fragt, bekommt die zurückgezogene Regel — in Lektion 3, in der `keinen` in keiner Zeile vorkommt. Das ist BLOCKER 2 aus Runde 1, in der anderen Datei, unangetastet. **(b) L1.** „Neben der Apotheke ist die Bank" ist die Zeile, die BLOCKER 3 als Wechselpräposition (Standard: A2.1) aus dem Diktat entfernt hat. Sie steht jetzt **zweimal** in der Regelkarte von Lektion 1, einmal als Beispiel und einmal als Musterkorrektur — also an der Stelle, die der Lernende liest, **wenn er nicht weiterweiß**. Dazu zitiert die Karte „Danach kommt die Brücke.", eine Zeile, die es im Dialog nicht mehr gibt. **(c) L6.** „Ihr Auge **sieht** gut" ist MAJOR 1 aus Runde 1 — im Dialog zu „sehen gut aus" repariert, in der Karte wörtlich stehen geblieben. **(d) L10.** Die notice wurde auf `für/ohne/durch` gekürzt und die Komparation bekam ihr Beispiel; die Karte lehrt unverändert `gegen`, `um`, `teurer`, `größer`, `besser` — vier Formen, die in der ganzen Lektion nicht vorkommen, also genau das, was RULE 6b misst, in der Datei, die RULE 6b nicht liest. Der Kopf der Override-Datei formuliert die verletzte Regel selbst: „**A CARD NEVER TEACHES MORE THAN ITS OWN LEKTION'S `notice`**". | Erst die Karten, dann die Regel. Karten: in `scripts/rule-card-overrides.mjs` **L3** `titleDe: 'Der Akkusativ: den und einen'`, die Zeile `'der Markt → den Markt. ein Supermarkt → einen Supermarkt. kein Park → keinen Park.'` → `'der Markt → den Markt. ein Supermarkt → einen Supermarkt. Die Form keinen kommt in Lektion 4.'`, `commonMistakes[2]` → `{ wrong:'Ana kauft ein Kuchen.', correct:'Ana kauft einen Kuchen.', explanationDe:'Nach kaufen steht der Akkusativ. Maskulin wird ein zu einen.' }`, und `'Ich bezahle das Kilo Brot an der Kasse.'` → `'Ich bezahle ein Kilo Brot an der Kasse.'`. **L1** die Zeile `'Weitere Beispiele: … Neben der Apotheke ist die Bank.'` → `'Weitere Beispiele: Die Apotheke ist rechts. Rechts ist die Haltestelle.'`, `commonMistakes[2]` → `{ wrong:'Die Haltestelle am Platz ist.', correct:'Am Platz ist die Haltestelle.', explanationDe:'Das Verb steht nicht am Satzende. Im Aussagesatz steht es auf Position 2.' }`, `'Danach kommt die Brücke.'` → `'Da ist auch die Brücke.'`. **L6** `'Ihr Auge sieht gut.'` → `'Die Hand und der Fuß sehen gut aus.'`. **L10** `'Nach für, ohne, gegen, um und durch …'` → `'Nach für, ohne und durch steht immer der Akkusativ.'`, die `um`-Zeile streichen, `'Das Kleid ist teurer. Unregelmäßig: …'` → `'Die Mütze ist billiger als der Schal.'`. Danach `node scripts/build-lesson-pool.mjs a1.2` laufen lassen, damit `ruleCards.mjs` nachzieht. Regel: **RULE 6b auf die Karte ausdehnen.** `noticeFormCoverage` in `scripts/validate-curriculum.mjs` liest heute nur `l.notice.bodyDe`; ein zweiter Durchlauf über `RULE_CARDS[l.primarySlug].content` **und** `commonMistakes[].correct`, gegen denselben Input (`dialog ∪ pretest.model ∪ schreiben.sample`), findet alle vier Befunde sofort. Dazu eine harte Zusicherung in `tests/curricula.test.mjs`, die **keinen Ratchet** braucht: `assert.equal(RULE_CARDS[l.primarySlug].titleDe, l.notice.title)` für jede Lektion jedes Levels — die Titel stehen eine Zeile auseinander und L3 hätte die Runde nicht überlebt. | BLOCKER |
| RULE 15 (`producedBeforeTaught` in `scripts/validate-curriculum.mjs`) gegen `src/data/lessonPools/a12.extra.json` und gegen den echten Zug | RULE 15 liest: `` [...(l.hoeren?.lines\|\|[]), ...(l.sprechen?.readAloud\|\|[])] `` — **nur Dialogzeilen**, Ergebnis **0**. · Im Pool, vor Lektion 11: `extra-a12-l01-09` „**Neben der** Apotheke ___ die Bank. (sein)" · `extra-a12-l07-06` „Bilden Sie die Frage: [wer / sein / die Frau / **mit dem kurzen Haar**]" → `'Wer ist die Frau mit dem kurzen Haar?'` · `extra-a12-l08-01` „Hilf mir bitte **mit der** Waschmaschine!", `-02` „Warte kurz **vor der** Garage!", `-03` „Holen Sie das Sofa **aus der** Garage!", `-05` „**mit der** Waschmaschine", `-06` „**vor dem** Kühlschrank", `-09` „**mit dem** Schrank" · `extra-a12-l09-04` „**Mit dem** Taxi ___ man schnell **zum** Flughafen fahren." · `extra-a12-l01-08` „Die Straßenbahn fährt **zum** Zentrum." · `extra-a12-l05-07` „Wann kommt der Bus **zum** Konzert?" | **Der Befund ist nicht geschlossen, er ist umgezogen — von der Fläche, die Review #1 gelesen hat, auf die, die es nicht lesen konnte.** Gemessen mit derselben Tabelle, die RULE 15 benutzt (`OFF_LIMITS_FORMS_A12`, erweitert um die Wechselpräpositionen `vor/neben/auf/unter`, die dort gar nicht stehen): **21 von 96** handgeschriebenen Items vor Lektion 11 tragen eine Dativ- oder Wechselpräposition; **acht** davon sind `sentence_building` oder `error_correction`, also Items, in denen der Lernende den **ganzen Satz tippt** — dieselbe Produktionshandlung wie das Diktat, für das die Regel geschrieben wurde. Drei Einzelbefunde wiegen besonders: **(a)** `extra-a12-l01-09` ist wörtlich die Zeile, die BLOCKER 3 aus L1s Diktat genommen hat. Sie steht jetzt als getipptes `fill_blank` in derselben Lektion und wird in **beiden** Versuchen gezogen. **(b)** `extra-a12-l07-06` lässt in Lektion **7** „mit dem kurzen Haar" **bauen** — Dativ (L11) *und* Adjektivdeklination (Standard: A2.1), die keine Karte dieses Kurses erwähnt —, und `buildCheckpoint` zieht denselben Satz in `a1.2-cp3`, also in die Prüfung nach Lektion 9. **(c)** Lektion 8 ist fünf von sieben gezogenen Items lang eine Dativübung: die Bitte braucht ein Objekt, das Objekt steht in der Garage, und der Ort ist der Dativ. Das ist keine Nachlässigkeit, sondern die Bauform, die Review #1 benannt hat — nur eine Ebene tiefer: **wo ein Item eine Struktur braucht, entsteht ein Satz, der eine andere mitbringt.** Die Regel hat den Umzug nicht gesehen, weil ihre Definition den Ort nennt statt die Handlung. | Erst die Regel, dann die Items — diesmal in dieser Reihenfolge, sonst zieht der Befund weiter. **Regel:** `producedBeforeTaught` von den Dialogzeilen auf **alles, was der Lernende produziert** heben. `` const producedTexts = (spec, l, pool) => [ ...(l.hoeren?.lines\|\|[]).map(i => l.dialog.lines[i]?.de), ...(l.sprechen?.readAloud\|\|[]).map(i => l.dialog.lines[i]?.de), ...planPractice(spec.curriculum, pool, 1).get(l.nr).filter(isTypedItem).map(it => `${it.questionDe} ${it.answer}`), ...planPractice(spec.curriculum, pool, 2).get(l.nr).filter(isTypedItem).map(it => `${it.questionDe} ${it.answer}`), ].filter(Boolean); `` — dazu die Aufgabenformeln (`Bilden Sie den Satz`, `Schreiben Sie die Zahl`, `Korrigieren Sie`) vor dem Match entfernen, sonst meldet `\bden\b` die Formel selbst. `OFF_LIMITS_FORMS_A12['dative-prepositions-intro']` um `vor\|neben\|unter\|über\|hinter\|zwischen\|auf\|an\|in` + Dativdeterminer erweitern — das sind die Wechselpräpositionen, die der Standard nach A2.1 legt und die die Tabelle heute nicht kennt. Ratchet `untaughtInProduction` auf die dann gemessene Zahl, Ziel **0**, Ausnahmeliste nur `LICENSED_CHUNKS_A12`. **Items:** `extra-a12-l01-09` → `{ questionDe:'Rechts ___ die Haltestelle. (sein)', answer:'ist', accepted:['ist'], questionEn:'The stop is on the right.' }`; `extra-a12-l01-08` → `'Die Straßenbahn fährt zum Rathaus.'` (lizenzierter Chunk); `extra-a12-l05-07` → `[wann / kommen / der Bus / am Platz]` → `'Wann kommt der Bus am Platz?'`; `extra-a12-l07-06` → `[wer / sein / der Mann / auf dem Foto]` scheitert an derselben Regel — richtig ist `[wer / sein / die Frau / da]` → `'Wer ist die Frau da?'` (die Dialogzeile selbst); die sechs L8-Items auf Akkusativobjekte umstellen: `-02` → `'Warte kurz!'`, `-03` → `[holen / Sie / das Sofa]` → `'Holen Sie das Sofa!'`, `-01`/`-05`/`-09` → `'Hilf mir bitte!'` / `'Helfen Sie mir bitte!'`; `extra-a12-l09-04` → `'Das Taxi ___ schnell zum Flughafen fahren. (können)'` bleibt Vorgriff — besser `'Hier ___ man nicht schnell fahren. (dürfen)'`. | BLOCKER |
| `src/lib/checkpoint/buildCheckpoint.js` auf `CURRICULUM_A12` + `a12.json`, gegen die `notice`/Karte der Lektionen, die der Checkpoint abschließt | `a1.2-cp1` (nach L3) `bausteine[2]`: `'Ich habe ___ Schlüssel. (kein Schlüssel)'` → `'keinen'` · `a1.2-cp1` `schreiben[1]`: `'Schreiben Sie den Satz: [du / kein / Hund / haben]'` → `'Du hast keinen Hund.'` · dagegen L3 `notice`: „**Die Form keinen kommt in Lektion 4 dazu.**" · `a1.2-cp4` (nach L12) `bausteine[3]`: `'Schreiben Sie den Satz im Perfekt: [er / nach München / fliegen]'` → `'Er ist nach München geflogen.'` · dagegen L12-Karte `commonMistakes[2]`: `'Ich bin Kuchen gegessen.' → 'Ich habe Kuchen gegessen.'` und content: „**haben** steht auf Position 2" · `a1.2-cp1` `bausteine[1]`: `'Schreiben Sie die Zahl in Worten: 2026 = ___'` → `'zweitausendsechsundzwanzig'`, gegen L2s Wortfeldeintrag „**die Zahlen 20–100**" | **Der Checkpoint ist die Prüfung, die das Kapitel abschließt, und keine der sieben Regeln liest ihn.** Er baut sauber — 4 × 20, alle fünf Sektionen, `isItemCorrect` grades identisch zur Lektion —, und genau deshalb fällt auf, was er prüft. Drei Klassen: **(a) Der Kurs prüft, was er zurückgehalten hat.** `keinen` steht in **zwei** Items des Checkpoints nach Lektion 3, während L3 die Form ausdrücklich vertagt und sie in keiner ihrer zehn Zeilen vorkommt. Das ist nicht streng, das ist widersprüchlich: die Lektion sagt „kommt später", die Prüfung sagt „jetzt". **(b) Der Kurs prüft, was er verneint.** L12s Karte lehrt das haben-Perfekt und streicht `bin … gegessen` als Fehler an — und der Abschlusscheckpoint verlangt `ist … geflogen`. Ein Lernender, der die Karte befolgt, schreibt „Er **hat** nach München geflogen" und bekommt ein rotes X für die Regel, die ihm beigebracht wurde. **(c) Der Kurs prüft über seine eigene Decke.** L2 lehrt „die Zahlen 20–100"; der Checkpoint verlangt eine vierstellige Zahl. Dazu — als Formfehler derselben Bauart — sind drei von acht *Richtig/Falsch*-Distraktoren keine falschen Aussagen, sondern **ungrammatische Sätze** („Ja, **der Wohnung** ist da", „dann das **Anzeige**", „**Der Haar** ist neu"): der Generator tauscht ein Wort und trifft dabei das Genus. In *Lesen Teil 3* ist der Distraktor eine plausible Falschaussage; hier lernt der Prüfling, Grammatikfehler zu erkennen statt Inhalte. | Zwei Filter, eine Regel. (1) In `buildCheckpoint.js` bekommt der Pool-Zug denselben Horizont wie die Lektion: `` const taughtBy = (nr, spec) => new Set(spec.primaryOrder.slice(0, nr)); `` und ein `eligible`-Filter, der jedes Item verwirft, dessen Text eine Form aus `OFF_LIMITS_FORMS_A12` eines **noch nicht** abgeschlossenen Slugs trägt — dieselbe Tabelle, die BLOCKER 3 erweitert; `keinen` (L4), `ist … geflogen` (sein-Perfekt, in keinem Slug) und `2026` fallen damit heraus. Das sein-Perfekt braucht dafür einen Eintrag: `'perfekt-intro': /\b(?:ist\|sind\|bin)\s+\w+\s+ge\w{2,}en\b/gi` — der Kurs lehrt es nicht, also gehört es in **keinen** Checkpoint dieses Niveaus, und die Karte sagt das auch. (2) Den Zahlenraum an die Lektion binden: im `bausteine`-Zug Items verwerfen, deren Prompt eine Zahl > 100 enthält, solange `numbers-counting` das Wortfeld „20–100" trägt. (3) Den Distraktor-Generator auf **Inhalt** umstellen: nur Zahlen, Namen und Zeitangaben tauschen (das tut er in fünf von acht Fällen schon: „in achtzig Stunden", „in elf Minuten", „um 12 Uhr" sind richtig gebaut), nie einen Artikel oder ein Nomen — `` if (swapped.article !== original.article) retry(); ``. Regel: **RULE 20** in `scripts/validate-curriculum.mjs` — `buildCheckpoint` für alle vier Checkpoints bauen und dieselbe Vorgriffsprüfung wie RULE 15 darüber laufen lassen; Ratchet `untaughtInCheckpoint`, gemessen, Ziel 0. Solange kein Ratchet den Checkpoint liest, ist die Prüfung die einzige unbeobachtete Fläche des Moduls — und Review #1 hat aufgeschrieben, was mit unbeobachteten Flächen passiert. | BLOCKER |
| `examTeileBacked` in `scripts/validate-curriculum.mjs:894` gegen `reading_lessons` / `listening_exercises` (level a1.2, gelesen 2026-09-13) | Die Regel prüft: `` if (teil.startsWith('Lesen') && li.readingOrder == null) why = 'links.readingOrder is null'; `` — **mehr nicht.** Ergebnis 0. · Gegen die Zeilen: L3 `examTeile:['Lesen Teil 3',…]`, `readingOrder: 8` = „**Beim Bäcker**" (`topic: At the Bakery`, Fließtext) · L7 `'Lesen Teil 2'`, `readingOrder: 1` = „**Meine Hobbys**" (`Hobbies & Free Time`) · L9 `'Lesen Teil 3'`, `readingOrder: 7` = „Mit dem Zug durch Deutschland" · L12 `'Lesen Teil 1'`, `readingOrder: 2` = „Essen und Trinken" · alle sechs `listening_exercises` a1.2: `exercise_type = 'short_dialogues'` — dagegen L2 `'Hören Teil 2'` und L11 `'Hören Teil 3'` · die beiden einzigen Prüfungsformat-Texte der Datenbank (`order_index` 9 „Zwei kurze E-Mails (wie in der Prüfung, Teil 1)", 10 „Anzeigen zu Wohnen und Arbeit (wie in der Prüfung, Teil 2)") hängen richtig an L4 und L2 | **Die Hälfte des Befunds, die Review #1 mit „ein Etikett, das der Kurs nicht einlöst, gehört nicht ins Verkaufsraster" gemeint hat, ist offen — und der Ratchet meldet 0, was schlechter ist als der alte Zustand, weil er jetzt Entwarnung gibt.** Was repariert ist, ist echt repariert: L6 behauptet nicht mehr *Sprechen Teil 1*, L7 hat die Teil-1-Mission, kein Teil wird mehr ohne Link behauptet, und die drei thematischen Fehlgriffe sind weg. Was offen ist, ist die Frage, die die Spalte stellt: **„Goethe-Teil trained" (Standard §2.4) behauptet ein Format, nicht ein Thema.** *Lesen Teil 1* sind zwei kurze E-Mails, *Teil 2* sind Anzeigen, *Teil 3* sind Schilder und Aushänge; *Hören Teil 1* sind kurze Gespräche, *Teil 2* Durchsagen, *Teil 3* Ansagen am Telefon. Gemessen: **sechs von elf** Hören-/Lesen-Behauptungen zeigen auf eine Zeile anderen Formats — vier Lesen (L3, L7, L9, L12) und zwei Hören (L2, L11, beide gegen `short_dialogues`). Der Beweis, dass die Unterscheidung gemeint ist, steht in derselben Tabelle: zwei Lesetexte tragen „wie in der Prüfung, Teil 1/2" im Titel, und **genau die beiden** sind richtig verlinkt. Dazu ein Befund, der erst durch den Pool sichtbar wird: **`hoeren.kind` ist zwölfmal `dictation`, und RULE 7 erzwingt das** (`if (h.kind !== 'dictation') fail(...)`). Der eigene Hörschritt des Kurses ist also zwölfmal ein Diktat — eine Übungsform, die in *Start Deutsch 1* **nicht vorkommt** —, während fünf Lektionen „Hören Teil 1/2/3" ins Kaufraster schreiben. | Zwei Zeilen Wahrheit sind besser als sechs falsche Etiketten. (1) **Die Teil-Nummer nur behaupten, wo das Format stimmt.** In `a12.js`: L3 `'Lesen Teil 3'` → `'Lesen'`, L7 `'Lesen Teil 2'` → `'Lesen'`, L9 `'Lesen Teil 3'` → `'Lesen'`, L12 `'Lesen Teil 1'` → `'Lesen'`, L2 `'Hören Teil 2'` → `'Hören'`, L11 `'Hören Teil 3'` → `'Hören'` — und `EXAM_TEILE` in `validate-curriculum.mjs` um die beiden schlichten Werte `'Hören'`, `'Lesen'` erweitern. L4 („Lesen Teil 1" → E-Mails) und L2 („Lesen Teil 2" → Anzeigen) behalten ihre Nummer, weil die Datenbank sie deckt. (2) **Die Regel:** `examTeileBacked` bekommt die Formattabelle, die der CONTRACT ohnehin führen muss: `` const READING_FORMAT_A12 = { 9:'Lesen Teil 1', 10:'Lesen Teil 2' }; const LISTENING_FORMAT_A12 = {}; // alle sechs Zeilen sind short_dialogues `` und `` else if (/^Lesen Teil \d$/.test(teil) && READING_FORMAT_A12[li.readingOrder] !== teil) why = `readingOrder ${li.readingOrder} hat nicht das Format ${teil}`; `` — analog für Hören. Ratchet `unbackedExamTeile` auf die dann gemessene Zahl, Ziel 0. (3) **`hoeren.kind`:** RULE 7 von „muss `dictation` sein" auf „muss aus `HOEREN_KINDS` sein" lockern (`['dictation','listening-select','gap-fill','word-order','match']`, Standard §3) und mindestens **vier** verschiedene Formen je Niveau verlangen; solange zwölfmal diktiert wird, darf keine Lektion eine Hören-**Teil**-Nummer tragen. Das ist dieselbe Regel wie (1), nur von der anderen Seite: `src/data/marketing.js` — measure before you claim. | BLOCKER |
| `netlify/functions/evaluate-writing.mjs:38–43, 169–187, 252–256` gegen die zwölf `a12-l*` und gegen `CONTRACT.md` §6.1 | `const COURSE_WRITING_FREE_LIFETIME = 12;` · `const COURSE_TASK_KEY_PREFIX = 'a11-';` · `const isCourse = !!task.course;` · `const courseAllowance = isCourse && (tier === 'free_trial' \|\| tier === 'free_expired');` · `if (courseAllowance) usedQuery = usedQuery.like('task_key', `${COURSE_TASK_KEY_PREFIX}%`);` · der Insert: `.insert({ user_id, exam_key, task_key, … })` — `task_key` ist `'a12-l07'` · dagegen CONTRACT §6.1: „The twelve `a12-l*` tasks therefore do **NOT** count against `COURSE_WRITING_FREE_LIFETIME` and are **billed against the ordinary writing allowance instead**." | **Die offene Frage des CONTRACT ist falsch beantwortet, und die richtige Antwort ist schlimmer.** Nachgerechnet: `task.course` ist bei allen zwölf A1.2-Aufgaben `'a1.2'`, also ist `isCourse` **true**, also ist `courseAllowance` für einen `free_trial`/`free_expired`-Lernenden **true**, also ist `limit` = 12 lebenslang — und **nicht** das Prüfungskontingent, das der CONTRACT annimmt. Gezählt wird dieses Kontingent aber über `task_key LIKE 'a11-%'`, und eine A1.2-Abgabe schreibt `task_key = 'a12-lNN'`. **Der Zähler bewegt sich also nie.** Zwei Folgen, beide falsch, beide in derselben Zeile: (a) ein kostenloser Lernender kann A1.2-Schreibaufgaben **unbegrenzt** benoten lassen — jede Abgabe ist ein Claude-Aufruf, und der Checkpoint reicht dieselbe Aufgabe noch einmal ein (siehe MAJOR 12); (b) umgekehrt sperrt ein Lernender, der A1.1s zwölf verbraucht hat, sich damit A1.2 **vollständig** zu, obwohl er dort noch keine einzige Aufgabe abgegeben hat. Das ist kein Didaktikbefund, sondern ein Kosten- und Zugangspfad, und er steht im einzigen Dokument, das der Integrator vor der Freischaltung liest, als „may be intended". Er ist nicht intendiert: keine der beiden Wirkungen ist eine Entscheidung, die jemand treffen wollte. | Eine Zeile, und dann die Entscheidung ausschreiben. Code: `` const COURSE_TASK_KEY_PREFIX = 'a11-'; `` → `` const COURSE_TASK_KEY_PREFIXES = ['a11-', 'a12-']; `` und `` usedQuery = usedQuery.like('task_key', `${COURSE_TASK_KEY_PREFIX}%`); `` → `` usedQuery = usedQuery.or(COURSE_TASK_KEY_PREFIXES.map((p) => `task_key.like.${p}%`).join(',')); `` — dann teilen sich A1.1 und A1.2 die zwölf, was der Zahl `COURSE_WRITING_FREE_LIFETIME` entspricht. **Wenn** A1.2 stattdessen kostenpflichtig sein soll (die wahrscheinliche Absicht), ist die richtige Zeile eine andere: `` const courseAllowance = isCourse && task.course === 'a1.1' && (tier === 'free_trial' \|\| tier === 'free_expired'); `` — dann fällt A1.2 auf `WRITING_LIMITS[tier]`, und ein `free_expired`-Lernender bekommt `subscription_required` statt eines stillen Freifahrtscheins. **Eines von beidem, in `CONTRACT.md` §6.1 als Entscheidung dokumentiert, nicht als offene Frage.** Test, der die Klasse schließt, in `tests/writing-course.test.mjs`: für **jede** Aufgabe mit `course` muss der Präfix ihres `taskKey` in `COURSE_TASK_KEY_PREFIXES` stehen — `` for (const t of WRITING_TASKS.filter((x) => x.course)) assert.ok(COURSE_TASK_KEY_PREFIXES.some((p) => t.taskKey.startsWith(p)), `${t.taskKey} zählt gegen kein Kontingent`); `` — das ist die Zusicherung, die ein A1.3 nicht mehr durchrutschen lässt. | BLOCKER |

---

## MAJOR

| Feld | Zitat | Problem | Fix (pasteable) | Schwere |
|---|---|---|---|---|
| Die Ratchet-Zeilen der A1.2-Registry in `scripts/validate-curriculum.mjs:475–486` gegen die eigene Ausgabe | gemessen ↔ Ratchet: RULE 6b **0 ↔ 31** · RULE 15 **0 ↔ 1** · RULE 16 **0 ↔ 3** · RULE 13 **1 ↔ 3** · und die Tests dazu: `assert.ok(noticeFormCoverage(CURRICULUM_A12).length <= r.unexemplifiedNoticeForms)` | **Vier von sieben Ratchets stehen über ihrer Messung, und die Tests prüfen nur „Messung ≤ Ratchet" — also ist der Rückfall auf 31 unbelegte Notice-Formen grün.** Genau das hat Runde 2 geleistet: 39 → 0. Die Zahl, die diese Leistung festhalten soll, steht auf 31 (dem A1.1-Wert). Ein Ratchet, der 31 Einheiten Luft hat, ist keine Sperre, sondern eine Notiz. Review #1 hat den Fix wörtlich so formuliert: „Ratchet `MAX_UNEXEMPLIFIED_NOTICE_FORMS`, **gemessener Startwert nach den Reparaturen**, Ziel 0." | Vier Zahlen, ein Commit. In der `'a1.2'`-Zeile: `unexemplifiedNoticeForms: 0`, `untaughtInProduction: 0`, `unbackedExamTeile: 0`, `missionlessLektionen: 1` — mit dem Datum und der Messung im Kommentar, wie die anderen drei es schon tragen. Und die Zusicherungsform in `tests/curricula.test.mjs` umdrehen, damit ein zu hoher Ratchet selbst auffällt: `` assert.equal(noticeFormCoverage(CURRICULUM_A12).length, r.unexemplifiedNoticeForms, 'der Ratchet IST die Messung — senken, wenn repariert wurde'); `` statt `<=`. Dann kann eine Reparatur den Ratchet nicht mehr vergessen. | MAJOR |
| `untaughtItemTokens: 156` in derselben Registry gegen `CONTRACT.md` §4 und gegen die 156 gedruckten Paare | CONTRACT §4: „RULE 11 — hand-written items use only taught words · **A1.2: 0** · Offenders: — (the file is empty; **the ratchet must stay 0**)" · Registry-Kommentar: „It was 0 while no pool existed, which measured nothing." · aus der Ausgabe: `…: RICHTIG`, `…: Satz`, `…: Schreiben`, `…: Worten`, `…: Zahl` | **Der einzige Ratchet, den der CONTRACT ausdrücklich festnagelt, wurde von 0 auf 156 angehoben — und die Zahl misst zum Teil die Aufgabenformel statt die Lexik.** Die Begründung ist ehrlich und im Kern richtig: 0 über einer leeren Datei ist keine Messung, und A1.1 hat mit 9 → 187 dieselbe Bewegung gemacht. Zwei Einschränkungen. (a) Von den 156 Paaren erreichen **19** den Lernenden im echten Zug, **18 davon in Lektion 2** und eines in L12 (`München`) — die handgeschriebene Hälfte ist tatsächlich bei **0**, und das ist die gute Nachricht dieser Runde. (b) Ein nennenswerter Teil der 156 sind Aufgabenwörter (`Schreiben`, `Worten`, `Zahl`, `Satz`, `RICHTIG`, `Kurzform`, `Präteritum`) — Metasprache des Übungsbanks, keine Lektionslexik. Eine Zahl, die beides mischt, kann nicht sinken, ohne dass jemand die Formeln umschreibt, und dann sinkt sie aus dem falschen Grund. | Die Formeln aus der Messung nehmen, dann ist die Zahl wieder eine Aussage über Lexik: in `scripts/validate-curriculum.mjs` die Tokenisierung von RULE 11 um `LEMMA_STOPWORDS` aus `src/lib/lesson/buildLesson.js` erweitern (dort steht die Liste bereits, inklusive der Sie-Formen) plus `['richtig','satz','zahl','worten','kurzform','präteritum','text','fall','bedeutet','permission']`. Ratchet auf die dann gemessene Zahl, und im CONTRACT §4 die Zeile ersetzen: „RULE 11 · A1.2 · **<neue Zahl>**, davon 0 in `a12.extra.json` — die handgeschriebene Hälfte bleibt bei 0, die Altbank sinkt mit jeder Umschreibung." Die Zusicherung, die schon da ist und die richtige ist, bleibt: `itemLexis(...).filter(handWritten).length === 0`. | MAJOR |
| Der echte Zug für L2 gegen L2s eigenes Wortfeld, und `extra-a12-l02-01 … -09` | gezogen in L2: `8c0a525e` „Schreiben Sie die Zahl in Worten: **3000** = ___", `5cbcd867` „**340** = ___", `d6c3655e` „**2026** = ___", `68174fed` „[am Samstag / wir / nach Berlin / **fliegen**]", `b3a0ddfa` „Morgen ___ ich nach Berlin." · dagegen L2 `wortfeld[16]`: `{ de:'die Zahlen 20–100', meta:true }` · die neun handgeschriebenen L2-Items: **neunmal `fill_blank`**, siebenmal derselbe Rahmen „Ergänzen Sie: … ___ … (NN)" | **Lektion 2 ist die einzige Lektion, die im echten Zug in die Altbank greifen muss — und was sie dort holt, liegt über ihrer eigenen Decke.** In Versuch 1 sind drei von sieben Items generiert, in Versuch 2 zwei. Drei davon verlangen vier- und dreistellige Zahlwörter, während die Lektion „20–100" lehrt; zwei bringen `fliegen` mit, das Wortfeld von **L9**. Die Ursache steht im eigenen Extra-Pool: die neun handgeschriebenen L2-Items sind **eine** Aufgabenform, siebenmal wiederholt, alle `fill_blank`. `MAX_SAME_LEMMA` greift nicht (die Nomen sind verschieden), aber `PRACTICE_SIZE`-Vielfalt und `typedMin` schieben den Planer trotzdem in die Nachbartopics. Für den Lernenden heißt das: die Zahlenlektion, die Review #1 mühsam auf ausgeschriebene Zahlwörter gebracht hat, prüft im Übungsschritt „2026". | Drei Items schreiben, nicht neun umschreiben: `` { id:'extra-a12-l02-10', topic:'numbers-counting', type:'sentence_building', questionDe:'Bilden Sie den Satz: [die Miete / sein / vierhundertachtzig Euro]', answer:'Die Miete ist vierhundertachtzig Euro.', accepted:['Die Miete ist vierhundertachtzig Euro.','Die Miete ist vierhundertachtzig Euro'], questionEn:'Build the sentence about the rent.' } ``, `` { id:'extra-a12-l02-11', type:'error_correction', questionDe:'Korrigieren Sie: „Die Nachbarin ist zwanzigeins."', answer:'Die Nachbarin ist einundzwanzig.', … } ``, `` { id:'extra-a12-l02-12', type:'error_correction', questionDe:'Korrigieren Sie: „Das drei Stockwerk ist frei."', answer:'Das dritte Stockwerk ist frei.', … } ``. Regel: eine Zusicherung in `tests/curricula.test.mjs`, die für **jede** Lektion jedes Levels verlangt, dass der echte Zug (`planPractice`, Versuche 1 **und** 2) mindestens fünf der sieben Items aus `extra-<level>.json` zieht — die Altbank ist der Rest, nicht die Quelle. Heute fällt nur L2 durch, und genau darum ist die Zusicherung billig. | MAJOR |
| `extra-a12-l06-05` und `extra-a12-l06-08`, gegen `extra-a12-l06-04` und gegen L6 `canDo[0]` | `-05`: „Tim ___ am Morgen nichts, sein Bauch **ist krank**. (essen)" → `isst` · `-08`: „Korrigieren Sie: „Mein Mann schlaft nicht gut, sein Rücken **ist krank**."" → **`'Mein Mann schläft nicht gut, sein Rücken ist krank.'`** · dagegen `-04`: „Ana ___ nicht gut, ihr Rücken **tut weh**. (schlafen)" → `schläft` · `canDo[0]`: „Ich kann sagen, **was mir wehtut**." | **Die Kollokation, die MAJOR 1 aus Runde 1 im Dialog repariert hat, steht in zwei handgeschriebenen Items wieder da — und in einem davon ist sie die *richtige* Antwort.** Körperteile sind im Deutschen nicht *krank*, sie *tun weh*; `krank` ist personen- und tierbezogen. In `-08` bekommt der Lernende einen Satz zum Korrigieren, tippt die Musterlösung ab und produziert dabei genau die Interferenz, die die erste Kann-Beschreibung der Lektion abtrainieren soll. Dass `-04` dieselbe Konstruktion **richtig** macht („ihr Rücken tut weh"), beweist, dass die Reparatur bekannt war und auf ein Item angewendet wurde, nicht auf seine Geschwister — dieselbe Teilreparatur wie bei den Regelkarten. Nebenbei kehrt in `-08` „**Mein Mann**" zurück; RULE 14 sieht es nicht, weil sie Dialogzeilen liest, nicht Items, und es ist hier kein Widerspruch (kein Sprecher), aber es ist die Formulierung, wegen der es RULE 14 gibt. | `` extra-a12-l06-05 `` → `` { questionDe:'Tim ___ am Morgen nichts, sein Bauch tut weh. (essen)', answer:'isst', accepted:['isst'], questionEn:'Tim eats nothing in the morning, his stomach hurts.' } `` · `` extra-a12-l06-08 `` → `` { questionDe:'Korrigieren Sie: „Der Nachbar schlaft nicht gut, sein Rücken tut weh."', answer:'Der Nachbar schläft nicht gut, sein Rücken tut weh.', accepted:['Der Nachbar schläft nicht gut, sein Rücken tut weh.','Der Nachbar schläft nicht gut, sein Rücken tut weh'], questionEn:'Fix the verb form.' } ``. Regel: die Kollokations-Negativliste, die Review #1 als RULE 17 bestellt hat, ist **nicht geschrieben worden** und hätte beide Items gefunden. Fünf Einträge reichen für alles, was zwei Reviews gefunden haben: `` const COLLOCATION_BANS = [ { re:/\b(Kopf\|Hals\|Bauch\|Rücken\|Hand\|Fuß\|Auge\|Zahn\|Arm\|Bein)\b[^.?!]*\bist krank\b/i, why:'Körperteile tun weh, sie sind nicht krank' }, { re:/\bsieht\b(?![^.?!]*\baus\b)[^.?!]*\bgut\b/i, why:'aussehen ist trennbar: sieht gut aus' }, { re:/\bmit\s+\w+\s+Grad\b/i, why:'bei zehn Grad, nicht mit' }, { re:/\bbrauche?t?\s+(?:einen\|den)\s+(?:Supermarkt\|Markt\|Bahnhof\|Flughafen)\b/i, why:'man braucht keine Institution' }, { re:/\bdas Kilo\b/i, why:'ein Kilo Brot, nicht das Kilo Brot' } ]; `` — angewendet auf Dialogzeilen, notices, Regelkarten **und** Pool-Items. Harte Regel, kein Ratchet; die Liste wächst mit jedem Review statt mit jedem Item. | MAJOR |
| `extra-a12-l03-01, -02, -05, -06, -08, -10`, `pretest.model` von L3 und die L3-Regelkarte | `pretest.model: 'Ich brauche einen Supermarkt.'` · `dialog.lines[1]`: „Ich **brauche einen Supermarkt** und einen Markt." · `-01` „**Ana braucht den Markt** im Zentrum." · `-02` „Tim braucht einen Supermarkt im Zentrum." · `-08` „[ich / brauchen / Supermarkt]" → „Ich brauche einen Supermarkt." · `-10` „Korrigieren Sie: „Ich brauche ein Markt im Zentrum."" → „Ich brauche einen Markt im Zentrum." · `-06` „Ich bezahle ___ an der Kasse. (**das Kilo Brot**)" · Karte: „Beispiele: **Ich brauche einen Supermarkt im Zentrum.** Ich bezahle **das Kilo Brot** an der Kasse." | **Review #1 hat „Ich brauche einen Supermarkt" als den unnatürlichsten Satz des Kurses benannt und `suchen`/`nehmen`/`kaufen` vorgeschlagen; Runde 2 hat die Formulierung nicht ersetzt, sondern **vervielfacht**: sie steht jetzt in `pretest.model`, im Dialog, in fünf Items und in der Regelkarte.** Man braucht etwas *aus* dem Supermarkt oder man *muss* zum Supermarkt; „Ana braucht **den Markt**" (`-01`) ist die schlechtere Variante desselben Fehlers, weil der bestimmte Artikel eine bereits eingeführte Institution voraussetzt. Dasselbe bei „das Kilo Brot": der Dialog wurde auf „ein Kilo Brot" repariert (Zeile 4/5), Item `-06` und die Karte haben die verworfene Fassung behalten. Der Grund ist derselbe wie überall in dieser Runde: die Reparatur hat die Datei getroffen, auf die der Befund zeigte, und nicht die Formulierung. **Bei `brauchen + Institution` ist das besonders teuer, weil `brauchen` das Verb ist, an dem L3 den Akkusativ sichtbar macht — es wird also mit jeder Wiederholung eingeprägt.** | Ein Verb tauschen, überall gleich. `pretest.model` → `'Ich suche einen Supermarkt.'`, `accepted: ['Ich suche','Ich brauche','Ich gehe']` (bleibt) · `dialog.lines[1]` → `{ speaker:'Ana', de:'Ich suche einen Supermarkt und einen Markt.', en:'I am looking for a supermarket and a market.' }` (Diktatzeile und `notice.examples[0]` mitziehen) · `-01` → `'Ana sucht ___ im Zentrum. (der Markt)'` · `-02` → `'Tim sucht ___ im Zentrum. (ein Supermarkt)'` · `-08` → `'[ich / suchen / Supermarkt] (mit unbestimmtem Artikel)'` → `'Ich suche einen Supermarkt.'` · `-10` → `'Korrigieren Sie: „Ich suche ein Markt im Zentrum."'` → `'Ich suche einen Markt im Zentrum.'` · `-06` → `'Ich bezahle ___ an der Kasse. (ein Kilo Brot)'` → `'ein Kilo Brot'` · die Karte entsprechend (siehe BLOCKER 2). `suchen` verlangt denselben Akkusativ und ist die Sprachhandlung, die die Lektion überhaupt meint. Regel: siehe die `COLLOCATION_BANS` in MAJOR 4 — beide Muster stehen dort. | MAJOR |
| `checkAnswer` (`src/lib/lesson/check.js:140`) auf `extra-a12-l08-07`, `-08` und `extra-a12-l10-03`, gemessen | `-07` „Der Teppich ist auf der Terrasse. ___ ihn bitte! (holen, du)", `accepted: ['Hol','hol']` — gemessen: `'Hole'` → **WRONG**, `'hole'` → **WRONG** · `-08` „Korrigieren Sie: „Du machst den Fernseher aus!"", `accepted: ['Mach den Fernseher aus!','Mach den Fernseher aus']` — `'Mache den Fernseher aus!'` → **WRONG** · `-10-03` „Ana geht ___ das Kaufhaus und kauft eine Hose.", `accepted: ['durch']` — gemessen: `'in'` → **WRONG** | **Drei Items bestrafen korrektes Deutsch, und zwei davon bestrafen die Form, die das Lehrwerk selbst als richtig gelten lassen müsste.** (a) Beim du-Imperativ regelmäßiger Verben ist das **-e fakultativ**: *Hol!* und *Hole!*, *Mach!* und *Mache!* sind beide korrekt (die Kurzform ist die übliche, nicht die einzige). Ein Lernender, der die Vollform schreibt, bekommt ein rotes X in der Lektion, deren Karte sagt „die Endung -st fällt mit" — sie sagt nichts über das -e. Die Lektion enthält zugleich `Warte!`, wo das -e **obligatorisch** ist (Stamm auf -t), also lernt der Lernende gerade die Unterscheidung und wird für die falsche Hälfte bestraft. (b) `-10-03` ist schwerer: „Ana geht **in das** Kaufhaus und kauft eine Hose" ist die natürliche Lesart und grammatisch einwandfrei; „durch das Kaufhaus gehen und eine Hose kaufen" beschreibt einen Durchgang, kein Einkaufen. Der deutsche Prompt bestimmt die Antwort also **nicht** — die einzige Information, die `durch` erzwingt, steht in der Absicht des Autors. Zum Vergleich: `-10-01` („Ana kauft die Mütze ___ ihre Oma" → `für`) und `-10-04` sind sauber determiniert; der Unterschied ist messbar und der Autor hat ihn in vier von sieben Items getroffen. | `accepted` erweitern, wo das Deutsch zwei Formen hat, und den Prompt reparieren, wo er nicht bestimmt. `-07` → `accepted: ['Hol','hol','Hole','hole']` · `-08` → `accepted: ['Mach den Fernseher aus!','Mach den Fernseher aus','Mache den Fernseher aus!','Mache den Fernseher aus']` · `-10-03` → `` { questionDe:'Ana geht ___ das Kaufhaus zur Kasse.', answer:'durch', accepted:['durch'], questionEn:'Ana walks through the department store to the till.' } `` — mit `zur Kasse` ist der Durchgang gemeint und `in` scheidet aus. Regel in `scripts/validate-curriculum.mjs` (RULE 7-Erweiterung) für jedes handgeschriebene Item, dessen Antwort ein **Imperativ** ist: `` if (/^[A-ZÄÖÜ][a-zäöüß]+$/.test(a) && !/(t\|d\|chn\|ffn\|tm)$/.test(stem(a)) && !accepted.includes(a + 'e')) fail(`${it.id}: du-Imperativ ohne die Variante mit -e`); `` — eine Zeile, und die Klasse ist zu, auch für A1.3. | MAJOR |
| L9 `wortfeld` (16 Einträge), `canDo[3]`, `links.readingOrder: 7` — **unverändert seit Review #1** | `situation: 'Regeln im Verkehr und für die Umwelt'` · Wortfeld: `das Ticket, einsteigen, aussteigen, abfahren, ankommen, das Fahrrad, das Taxi, der Koffer, die Reise, das Flugzeug, der Flughafen, fliegen, laufen, langsam, schnell, **der Müll**` · `canDo[3]: 'Ich kann sagen, wohin der Müll kommt.'` · Dialog: „Den Müll müssen Sie nach Hause nehmen." · **neu:** `readingOrder: 7` = „**Mit dem Zug durch Deutschland**" (`topic: Train Travel`) | **Der einzige MAJOR aus Runde 1, an dem gar nichts geschehen ist — und die neue Verlinkung zementiert ihn.** Fünfzehn von sechzehn Wortfeldeinträgen sind Reiselexik; das einzige Wort, das die Situation „Regeln (Verkehr, Umwelt)" trägt, ist `der Müll`, und er ist einer der drei Einträge **ohne `wordId`**, also ohne Audio. Die Kann-Beschreibung „wohin der Müll kommt" wird von keiner Zeile beantwortet (der Dialog sagt nur, dass man ihn mitnehmen muss — das ist eine andere Auskunft), und RULE 12 meldet trotzdem 0, weil sie das Inhaltswort `Müll` findet. Runde 2 hat die Lektion mit „Mit dem Zug durch Deutschland" verlinkt: der Lesetext **passt zum Wortfeld** und damit zur Drift, nicht zur Situation. Die Lektion heißt „Regeln unterwegs" und ist eine Reiselektion mit einer Regelkarte. Für das 12×6-Raster heißt das: die Zeile verkauft „Mobilität **und Umwelt**", und Umwelt kommt nicht vor. | Wie in Runde 1 vorgeschlagen, unverändert gültig: sechs Reiseeinträge (`das Flugzeug`, `der Flughafen`, `fliegen`, `der Koffer`, `die Reise`, `das Taxi` — alle in A1.1 L10 „Am Bahnhof" bereits abgedeckt) gegen sechs Regel-/Umweltwörter tauschen, die die Goethe-A1-Liste führt: `das Schild`, `verboten`, `erlaubt`, `die Ampel`, `die Flasche`, `das Papier`. Dann trägt der Dialog seine Situation: `lines[6]` → `{ speaker:'Herr Schmidt', de:'Hier. Das Schild sagt: Rauchen ist hier verboten.', en:'Here. The sign says: smoking is forbidden here.' }`, `lines[8]` → `{ speaker:'Herr Schmidt', de:'Ja. Papier und Flaschen kommen nicht in den Müll.', en:'Yes. Paper and bottles do not go in the rubbish.' }` — und `canDo[3]` ist zum ersten Mal beantwortet. `links.readingOrder` → `null` und `examTeile` ohne Lesen-Eintrag (BLOCKER 5), solange kein A1.2-Lesetext von Regeln handelt; das ist der ehrliche Zustand und gehört in den Modulkopf. Regel: RULE 2 prüft `situationKeywords` heute nur gegen `situation`/`handlungsfeld`; dieselbe Liste zusätzlich gegen das **Wortfeld** laufen lassen und **mindestens ein Drittel** Treffer verlangen — L9 trifft 1 von 16 und ist die einzige Lektion, die durchfällt. | MAJOR |
| `sprechen.open.missionOrder` gegen `speaking_missions.target_structures` (gelesen 2026-09-13) und gegen `CONTRACT.md` §4 | CONTRACT §4: „Measured against **`target_structures`** instead …" · gebaut: L1→**M5** „Nach dem Weg fragen" (`question words wo, wie, wohin, wie lange, wie weit`) — L1 `primarySlug: basic-sentence-structure` · L6→**M1** „Anmeldung beim Hausarzt" (`verb in second position, word order: subject - verb - rest`) — L6 `primarySlug: stem-changing-verbs` · ungenutzt: **M2** „Meine Familie" (`nominative subject forms, der/die/das and mein/meine as subject, wer questions`) — das ist wörtlich L7s `primarySlug` `nominative-case` | **Die Neuzuordnung ist gelandet und ist gut — aber in zwei von zwölf Fällen ist sie wieder thematisch, und es sind genau die zwei, die Review #1 als Muster benannt hat.** M1 trägt „verb in second position" — das ist **L1s** Regelkarte — und hängt an der Arztpraxis, weil man sich dort anmeldet; M5 trägt die W-Fragen — das ist **L5s** Regelkarte — und hängt an der Wegbeschreibung, weil man dort nach dem Weg fragt. Die beiden sind vertauscht, exakt wie M4/M11 es vor der Reparatur waren. Zusätzlich liegt M2 ungenutzt, deren `target_structures` buchstabengetreu L7s Grammatikpunkt sind — L7 hat (richtig) M9 bekommen, aber damit ist die einzige strukturgenaue Mission des Niveaus übrig, während L1 und L6 die Struktur der jeweils anderen üben. Der Grund ist, dass **RULE 18 nicht geschrieben wurde**: der CONTRACT beschreibt die Messung, aber keine Regel hält sie fest, also kann die nächste Runde sie wieder aufweichen. | Zwei Zahlen tauschen: L1 `missionOrder: 5` → **`1`**, L6 `missionOrder: 1` → **`5`**? Nein — M5 („Nach dem Weg fragen") passt thematisch zu L1 und strukturell zu L5, und L5 hat mit M11 bereits die strukturgenaue Mission. Also: **L1 → `1`** (verb-second, sein eigener Slug), **L6 → `2`**? M2 ist „Meine Familie" und passt zu keiner Arztpraxis. Der ehrliche Zug ist: **L1 → 1, L5 → 5, L11 → 11, L6 → null** — L6 hat keine Mission, deren Struktur der Vokalwechsel ist, und `missionlessLektionen` wird damit `[6, 11]`, also 2. Das ist eine **schlechtere Ratchet-Zahl und ein besserer Kurs**, und genau diese Entscheidung gehört in den CONTRACT geschrieben statt verrechnet. Regel — **RULE 18**, die Review #1 bestellt hat: `` const MISSION_KEYWORDS = { 'basic-sentence-structure': /verb in second\|word order/i, 'question-words': /question words\|W-Fragen/i, 'nominative-case': /nominative/i, 'accusative-intro': /accusative direct object/i, 'negation': /negation\|kein/i, 'modal-verbs-intro': /modal verbs/i, 'imperative': /Imperativ\|requests/i, 'prepositions-accusative': /accusative prepositions/i, 'numbers-counting': /numbers\|clock times/i }; `` — `missionOrder` darf nur auf eine Mission zeigen, deren `target_structures` das Muster des `primarySlug` trifft, oder `null` sein; die zwölf Missionszeilen beim Pool-Bau mitdumpen (wie `grammar-content-cache.json`), damit CI ohne Netz prüfen kann. Ratchet `missionlessLektionen`, gemessen. | MAJOR |
| `sprechen.open.anrede` (zwölfmal `'Sie'`) gegen `speaking_missions.scenario_de` aller zwölf A1.2-Missionen | `a12.js` Modulkopf: „**DER OFFENE BRUCH, DEN DIESE DATEI NICHT SCHLIESSEN KANN:** alle zwölf `speaking_missions` auf Level a1.2 duzen in `scenario_de` („Du meldest dich … an"), während jeder `promptDe` siezt." · `netlify/functions/_shared/speakingAI.mjs`: `normalizeAnrede = (value) => (value === 'du' ? 'du' : 'Sie')` | **Das Feld ist gesetzt, die Entscheidung ist begründet, der Bruch ist unverändert da — und er trifft jetzt elf von zwölf Lektionen statt neun.** Runde 2 hat `anrede` zwölfmal auf `'Sie'` gesetzt, mit dem sehr guten Argument, dass jeder Sprechauftrag nach der Missionsneuzuordnung ein Prüfungsformat ist. Die Folge ist aber, dass der Lernende in **elf** Lektionen (alle außer L11, die keine Mission hat) auf dem Lektionsbildschirm eine Sie-Aufgabe liest und einen Klick später in einem `scenario_de` landet, das ihn duzt — vorher waren es neun. Das ist kein Vorwurf an die Entscheidung, sondern an ihre Halbierung: die Datei hat entschieden, was sie entscheiden kann, und den Rest aufgeschrieben. Aufgeschrieben ist nicht geschlossen, und der CONTRACT führt es nicht als offene Frage in §6. | Die SQL-Migration ist klein und gehört in `migrations/`: `` UPDATE speaking_missions SET scenario_de = replace(replace(replace(scenario_de, 'Du ', 'Sie '), ' dich', ' sich'), 'Frag ', 'Fragen Sie ') WHERE lower(level) = 'a1.2'; `` — danach von Hand nachlesen (zwölf Zeilen), denn `dein/deine` und die Verbendungen fallen durch die Ersetzung. Alternativ, wenn die Missionen bewusst duzen sollen: `anrede` je Lektion auf `'du'` ziehen und `promptDe` mit — aber dann stimmt das Prüfungsregister nicht mehr, also ist die Migration der richtige Weg. **Eines von beidem, und die Zeile gehört in `CONTRACT.md` §6 als Punkt 6.** Test, der die Klasse schließt: `tests/speaking-course-task.test.mjs` prüft `normalizeAnrede` in fünf Varianten und **kein Kurrikulum** — ein Fall, der für jede Lektion jedes Levels `buildTeacherSystemPrompt` gegen `sprechen.open.anrede` hält, ist zehn Zeilen und findet ein vergessenes Feld sofort. | MAJOR |
| L4 `dialog.lines[8]`/`[9]` (`[8]` ist `sprechen.readAloud[1]`) und L5 `dialog.lines[1]`, `[5]`, `[6]` (`[1]` ist `readAloud[0]`, `[5]` ist `hoeren.lines[1]`) | L4: `[8] Ana: 'Und die Rechnung? Haben Sie **keinen** Rabatt?'` → `[9] Frau Kaya: '**Ja.** Das Angebot ist gut: Sie bezahlen die Nacht nicht.'` · L5: `[1] Tim: '**Wo und wohin** fahren wir? Ich habe den Fahrplan.'` · `[5] Tim: 'Der **Ausflug ist weit**. Wie oft fährt der Bus?'` · `[6] Lena: '**Manchmal jede Stunde.** Ich bin oft zu spät.'` — und `extra-a12-l05-03` „___ fährt der Bus? — **Manchmal jede Stunde.**" → `Wie oft` | **Vier unreparierte Sprachfehler, alle in Zeilen, die der Lernende spricht oder tippt — und einer davon ist ausgerechnet der Partikel, den die Lektion lehren müsste.** (a) Auf eine **verneinte** Entscheidungsfrage antwortet man bejahend mit **`Doch`**, nicht mit `Ja`. „Haben Sie keinen Rabatt?" — „Ja. Das Angebot ist gut" ist falsches Deutsch, und es steht in **Lektion 4**, deren einziger Lerngegenstand die Verneinung ist, deren Karte `doch` mit keinem Wort erwähnt, und deren Zeile `[8]` eine **Nachsprechzeile** ist. `doch` ist A1-Stoff und in *Sprechen Teil 3* alltäglich. (b) „Wo **und wohin** fahren wir?" ist kein deutscher Satz — und es ist `readAloud[0]`, die erste Zeile, die der Lernende spricht. (c) „Der **Ausflug** ist weit" (→ *Der Weg ist weit*) ist eine **Diktatzeile**; Review #1 hat sie als Minor benannt und ausdrücklich dazugeschrieben, dass sie diktiert wird. (d) „**Manchmal jede Stunde**" ist ebenfalls unrepariert und steht jetzt zusätzlich in `extra-a12-l05-03` — wo es außerdem dazu führt, dass „Wann" als Antwort falsch ist, obwohl „Wann fährt der Bus? — Jede Stunde" ein korrekter Austausch ist. | L4 `lines[9]` → `{ speaker:'Frau Kaya', de:'Doch. Das Angebot ist gut: Sie bezahlen die Nacht nicht.', en:'Yes (there is). The offer is good: you do not pay for the night.' }` und einen Satz in die notice, der in die 60 Wörter passt, wenn der letzte gekürzt wird: `'Auf eine Frage mit nicht oder kein antwortet man mit **doch**: Haben Sie keinen Rabatt? — **Doch**.'` — `doch` steht dann im Input **und** in der Karte, und die Lektion lehrt die Verneinung vollständig. · L5 `lines[1]` → `{ speaker:'Tim', de:'Ja. Wohin fahren wir? Ich habe den Fahrplan.', en:'Yes. Where are we going? I have the timetable.' }` · `lines[5]` → `{ speaker:'Tim', de:'Der Weg ist weit. Wie oft fährt der Bus?', en:'The way is far. How often does the bus run?' }` (`der Ausflug` bleibt über `lines[8]`/`schreiben` gedeckt) · `lines[6]` → `{ speaker:'Lena', de:'Jede Stunde. Ich bin oft zu spät.', en:'Every hour. I am often late.' }` und `extra-a12-l05-03` → `'___ fährt der Bus? — Dreimal am Tag.'`, `accepted: ['Wie oft','wie oft']`. Regel: siehe `COLLOCATION_BANS` (MAJOR 4); `/\bkein\w*\b[^?]*\?/` gefolgt von `/^Ja\b/` in der nächsten Zeile ist ein sechster Eintrag und findet (a) maschinell. | MAJOR |
| L6 `dialog.lines[6]`, gezogen als Diktatzeile in `a1.2-cp2` | `[6] Frau Berger: '**Wer krank ist, schläft viel.**'` · `buildCheckpoint(a1.2-cp2).hoeren[1]`: `'Hören Sie zu und schreiben Sie den Satz.'` → **`'Am Morgen und am Abend. Wer krank ist, schläft viel.'`** | **Der Satz ist ein Nebensatz mit Verbletztstellung und korrelativem `wer` — B1-Syntax in einem A1.2-Dialog —, und der Checkpoint nach Lektion 6 lässt ihn **diktieren**.** Der Lernende hat bis hier keinen einzigen Nebensatz gesehen; keine Karte des Kurses erwähnt die Verbstellung im Nebensatz, L1s Karte sagt im Gegenteil „das Verb steht auf Position 2", und hier steht es am Ende. Der Satz ist auch für sich zweifelhaft (eine Ärztin sagt das nicht so). **Er stammt aus dem Reparaturvorschlag von Review #1** — und genau deshalb gehört er in dieses Review: ein Vorschlag des Reviewers ist eine Anregung, keine Lizenz, und die Verbstellung hat der Vorschlag nicht bedacht. Die Lektion braucht `schläft` im Input (es belegt den Vokalwechsel und ist die Form, an der die Karte ihn zeigt), aber nicht in einem Nebensatz. | `lines[6]` → `{ speaker:'Frau Berger', de:'Am Morgen und am Abend. Sie schläft dann besser.', en:'In the morning and in the evening. Then you sleep better.' }` — `schläft` bleibt, der Nebensatz ist weg, die Antwort passt weiter auf `[5]` („Wie oft nimmt man die Tablette?"). Regel in `scripts/validate-curriculum.mjs` (RULE 5-Erweiterung), die A1 und A2 gleichermaßen schützt und eine Zeile ist: `` const SUBORDINATE_RE = /\b(?:weil\|dass\|wenn\|wer\|ob\|obwohl\|damit\|als)\b[^,.?!]*,\s*\w+/i; `` — jede Dialogzeile, die trifft, ist ein Befund, solange kein Slug des Niveaus die Nebensatzstellung lehrt. Harte Regel; A1.2 hat heute genau einen Treffer. | MAJOR |
| `buildCheckpoint` über alle vier A1.2-Checkpoints, und `chapterWritingTask` | dasselbe Item in zwei Checkpoints: „Ich trinke ___ Kaffee. (der Kaffee)" (cp1 `bausteine[5]`, cp2 `bausteine[1]`) · „Schreiben Sie den Preis in Worten: 4,20 € = ___" (cp1, cp2) · „[du / kein / Hund / haben]" (cp1, cp4) · `graded_writing`: cp1 = `a12-l03`, cp2 = `a12-l06`, cp3 = `a12-l09`, cp4 = `a12-l12` — jeweils **wörtlich die Schreibaufgabe der letzten Lektion des Kapitels** | **Der Checkpoint hat keine Entdopplung über Checkpoints hinweg und keine eigene Schreibaufgabe.** (a) `usedPoolIds` ist ein `Set` **pro Aufruf**; `planPractice` trägt seine benutzten Ids über die ganze Lektionsreihe mit (das war die Lehre des Reviews vom 2026-09-12), `buildCheckpoint` baut jeden Checkpoint isoliert. Ergebnis: drei von 80 Items erscheinen in zwei der vier Prüfungen — bei 20 Items je Prüfung sind das 15 % Wiederholung in einem Kapitel, das der Lernende bestehen muss. (b) Schwerer: die benotete Schreibaufgabe **jedes** Checkpoints ist die Aufgabe, die der Lernende in der Lektion davor schon geschrieben hat. Vier von zwölf Aufgaben werden zweimal geschrieben, acht erscheinen in keiner Prüfung, und jede Wiederholung ist ein zweiter Claude-Aufruf gegen dasselbe Kontingent — das BLOCKER 6 gerade gar nicht zählt. Für die Prüfung ist es zudem wertlos: der Lernende hat den Text und das Feedback schon. | (a) `buildCheckpoint` eine `usedIds`-Menge über die Checkpoints hinweg geben — dieselbe Bauform wie `planPractice`: `` export function planCheckpoints(curriculum, pool, seed) { const used = new Set(); return curriculum.checkpoints.map((cp) => { const items = buildCheckpoint({ curriculum, checkpoint: cp, pool, seed, usedIds: used }); for (const it of items) if (it.id) used.add(it.id); return [cp.id, items]; }); } `` und `CheckpointPage` daraus lesen. (b) `chapterWritingTask(chapter, level)` soll die Aufgabe **einer anderen** Lektion des Kapitels ziehen als der letzten: `` const tasks = chapter.map((l) => l.schreiben.taskKey); return writingTaskByKey('goethe_a1', tasks[(hashSeed(checkpoint.id) % (tasks.length - 1))]); `` — dann schreibt cp1 `a12-l01` oder `a12-l02`, und die vierte Lektion bleibt für die Lektion selbst. Regel: eine Zusicherung in `tests/checkpoint.test.mjs`, die für jedes Level verlangt, dass die 80 Checkpoint-Item-Ids **paarweise verschieden** sind und dass kein `graded_writing` den `taskKey` der unmittelbar vorangehenden Lektion trägt. | MAJOR |
| L12 `dialog.lines[4]`–`[8]` und `extra-a12-l12-08` | `[4] 'Die Wurst und das Fleisch haben wir gekauft.'` · `[5] 'Und den Fisch? Tim hat ihn gekocht.'` · `[6] 'Das Ei und das Gemüse hat meine Oma gemacht.'` · `[7] 'Das Obst und die Kartoffeln habe ich gekauft.'` · `[8] 'Die Nudeln und den Reis hat Tim gekocht.'` · `extra-a12-l12-08`: „[ich / haben / **die Kartoffel** / kaufen]" → „Ich habe **die Kartoffel** gekauft." | **Die drei Einzelfehler sind repariert, die Bauform nicht: fünf aufeinanderfolgende Zeilen tragen denselben Rahmen „X und Y hat/haben … gekauft/gekocht", und die Lektion, die *erzählen* lehren soll, erzählt immer noch nicht.** Gut ist, was repariert wurde: „die **Kartoffeln**" im Plural, „**Im Kuchen war zu viel Zucker**" statt „Der Zucker war zu viel", und die Kann-Beschreibung heißt jetzt „was ich auf dem Fest **gegessen habe**" statt des nirgends gelehrten `gab`. Offen ist die Struktur: zwischen `[4]` und `[8]` gibt es keine Reaktion, keine Nachfrage, keine Wertung — vierzehn von sechzehn Wortfeldeinträgen werden in zehn Zeilen abgearbeitet, zwei pro Zeile. Und der Singular, den der Dialog abgelegt hat, lebt in `extra-a12-l12-08` weiter: „Ich habe **die Kartoffel** gekauft" sagt niemand über ein Fest. | Zwei Zeilen in Reaktionen umwandeln, das Item in den Plural: `[6]` → `{ speaker:'Lena', de:'Wirklich? Das Gemüse war sehr lecker.', en:'Really? The vegetables were very tasty.' }` · `[8]` → `{ speaker:'Lena', de:'Und was hat Tim gekocht? Den Reis und die Nudeln.', en:'And what did Tim cook? The rice and the noodles.' }` — `das Ei`, `die Wurst`, `das Fleisch` verlieren damit Deckung und gehören in `pretest.model` und `schreiben.sample`, die RULE 10 ebenfalls liest. · `extra-a12-l12-08` → `'[ich / haben / die Kartoffeln / kaufen]'` → `'Ich habe die Kartoffeln gekauft.'`. Regel: die `MAX_ORPHAN_LINES`-Idee aus Review #1 misst Anschluss, nicht Monotonie; billiger und genauer ist ein Rahmen-Zähler — `` const frame = (s) => s.replace(/[A-ZÄÖÜ][a-zäöüß]+/g, 'N'); `` und dann verlangen, dass keine Lektion mehr als **drei** Zeilen mit demselben normalisierten Rahmen trägt. L12 hat heute fünf und ist die einzige Lektion, die durchfällt. | MAJOR |
| `docs/course-standard-2026-09-12.md` §2.2, Spalte „Grammatik ceiling", Zeile **A1.2**, gegen die zwölf `notice`-Texte — **unverändert seit Review #1** | Standard A1.2: „Dativ (Präp., **Verben, Pronomen**); temporale Präp.; wollen/sollen/dürfen/müssen; Imperativ; war/**hatte**; **Perfekt untrennbar**; Komparation; **denn**; **würde**; Ordinalzahlen" · gemessen in `a12.js`: `hatte` kommt ausschließlich in `FUNCTION_WORDS` und in Kommentaren vor, `denn` ausschließlich in `FUNCTION_WORDS`, `würde` **gar nicht**, `bezahlt` (das Musterbeispiel für das untrennbare Perfekt) **gar nicht**; `helfen`, `passen`, `wehtun` werden benutzt und nie als Dativverben benannt; `mir`, `dir`, `Ihnen` stehen ab L6 im Input (`extra-a12-l06-10` verlangt `Ihnen` sogar als **Antwort**) und in keiner Karte | **Fünf von zehn Deckelpunkten sind weiterhin ungedeckt, und der Standard ist das Dokument, aus dem die öffentliche Lehrplanseite ihre Versprechen zieht.** Review #1 hat zwei Wege angeboten — §2.2 korrigieren oder zwei der fünf Punkte ohne neuen Slug einlösen — und keiner ist gegangen worden. Ein Punkt ist dabei besonders schief: `extra-a12-l06-10` lässt den Lernenden das **Dativpronomen `Ihnen`** produzieren, in Lektion 6, unter dem Topic `stem-changing-verbs`, ohne dass irgendeine Karte des Kurses den Dativ vor Lektion 11 erwähnt — die Struktur wird also geprüft, ohne je gelehrt worden zu sein, und das Item ist zusätzlich unter einem Slug abgelegt, dessen Grammatik es nicht übt. | Zwei der fünf Punkte kosten je einen Satz und sollten sofort eingelöst werden: **L12** notice um `'Verben mit be-, ver-, er- bekommen kein ge-: bezahlen → **bezahlt**.'` erweitern (bleibt unter 60 Wörtern, wenn der `war`-Satz kürzt) plus `dialog.lines[3]` → `{ speaker:'Ana', de:'Nein, ich habe Milch getrunken. Tim hat bezahlt.', en:'No, I drank milk. Tim paid.' }` — dann ist auch `bezahlen` aus L3 im Perfekt wiederholt; **L11** notice um die Dativpronomen: `'Auch die Pronomen stehen im Dativ: mit **mir**, bei **dir**, nach **Ihnen**.'` — `bei dir` steht dort bereits zweimal im Dialog, kostet also keinen Input. Danach ist der Deckel zu 7/10 gedeckt, und **§2.2 muss den Rest ausweisen**: `hatte`, `würde` und `denn` in die A2.1-Zeile verschieben oder in der A1.2-Zeile als „(nicht in diesem Kurs)" markieren. Dazu `extra-a12-l06-10` → `'Frau Chakiri, ___ Kopf tut weh? (höflich, Possessivartikel)'` → `'Ihr'` — dieselbe Höflichkeitsform, ohne Dativ, und das Item übt dann wenigstens keinen Vorgriff. Regel — **RULE 19**, die Review #1 bestellt hat und die nicht geschrieben wurde: die Deckelzeile je Level als Stichwortliste im Validator, jeder Punkt muss in mindestens einer `notice.bodyDe` desselben Niveaus vorkommen; Ratchet `MAX_UNCOVERED_CEILING`, gemessen (heute 5), Ziel 0 oder ein dokumentierter Rest. | MAJOR |
| Die 48 `canDo`-Zeilen gegen den `PROVENANCE`-Absatz im Modulkopf und gegen Standard §2.2 — **unverändert seit Review #1** | Modulkopf: „PROVENANCE. Kann-Beschreibungen **folgen** dem Goethe-Zertifikat A1 (Start Deutsch 1) Prüfungsziele/Testbeschreibung" · Standard §2.2: „3–5 Kann-Beschreibungen **quoted** from the Goethe Prüfungsziele" · gebaut, z. B. L9: `'Ich kann sagen, wohin der Müll kommt.'` — in keiner Goethe-Quelle | **48 frei formulierte Sätze tragen eine Provenienzbehauptung, die sie nicht einlösen, und sie stehen in der Spalte des 12×6-Rasters, die ein Käufer vor dem Kauf liest.** In Review #1 stand das als Minor, weil damals Wichtigeres offen war; nach einer Runde, in der die Zeile unangetastet blieb, ist es eine Claims-Frage: `src/data/marketing.js` führt die Regel, dass jede Behauptung ihre Herkunft inline trägt, und „folgen dem … Prüfungsziele" ist eine Herkunftsbehauptung. Der Unterschied zwischen „folgen" und „orientieren sich an" ist ein Wort und juristisch der ganze Abstand zwischen Zitat und Anlehnung. Dass es sich um gute Kann-Beschreibungen handelt, steht außer Frage — RULE 12 meldet 0, jede ist in ihrer Lektion geübt. | Ein Wort, oder zitieren. Modulkopf: `'Kann-Beschreibungen folgen dem Goethe-Zertifikat A1 …'` → `'Kann-Beschreibungen orientieren sich an den Handlungsfeldern des Goethe-Zertifikats A1 (Start Deutsch 1) und am BAMF-Rahmencurriculum; sie sind für dieses Modul formuliert, nicht aus den Prüfungszielen zitiert.'` — und dieselbe Formulierung überall dorthin, wo die Lehrplanseite sie ausgibt. Wer stattdessen zitieren will, braucht die Prüfungsziele als Quelle im Repo (wie `factsCheckedOn` + `sources` bei den Leitfäden, `tests/guides.test.mjs`). Regel: `tests/claims.test.mjs` um eine Zusicherung erweitern, die in Kurrikulum-Modulköpfen das Wort „folgen"/„nach den Prüfungszielen" nur zulässt, wenn im selben Modul ein `sources`-Feld steht — dieselbe Mechanik, die die Leitfäden schon haben. | MAJOR |

---

## Status der Befunde aus Runde 1

An den Artefakten geprüft, nicht am Commit-Text. **G** = geschlossen, **T** = teilweise, **O** = offen.

| # | Befund aus Review #1 | Status | Nachweis |
|---|---|---|---|
| B1 | Ana verheiratet in L6s Diktat; `personaSource: null` | **G** | `personaSource: 'a1.2'`, `PERSONAS_A12` erbt `PERSONAS_A11`, RULE 14 = 0; L6 `[3]` „Ich schlafe nicht gut." · Rest: „Mein Mann" kehrt in `extra-a12-l06-08` zurück, wo RULE 14 nicht liest (MAJOR 4) |
| B2 | RULE 6 prüft die notice-Formen nicht (39 Fettstellen ohne Beleg) | **T** | RULE 6b existiert, misst **0**; L2 schreibt Zahlwörter aus, L3 heißt „den und einen", L11 liefert `zum`/`zur`. **Aber:** die zwölf **Regelkarten** wurden nicht mitgezogen (BLOCKER 2), und L8s Sie-Imperativ wurde geschlossen, indem die **Fettung entfernt** wurde, nicht durch Input |
| B3 | Vorgriffe in Diktat und Nachsprechen (23 Dativtreffer vor L11) | **T** | RULE 15 existiert, misst **0**; L1/L2/L8/L9/L11 sind repariert. **Aber:** 21 von 96 Pool-Items vor L11 tragen den Vorgriff, 8 in getippten Items (BLOCKER 3) |
| B4 | `examTeile` ungedeckt; L6 „Sprechen Teil 1"; Zuordnungen gegen die DB | **T** | RULE 16 existiert; L6 → „Sprechen Teil 3", L7 → M9 („Sprechen Teil 1: Sich komplett vorstellen"), L4 → R09, L2 → R10, L5 → R06, L12 → R02, L8 → R04, L11 → H06, L10 → H01 — **alle thematischen Fehlgriffe korrigiert**. **Aber:** die Format-Hälfte ist offen, 6 von 11 (BLOCKER 5) |
| M1 | L6 „Ihr Auge sieht gut"; „Der Rücken ist krank" | **T** | Dialog `[8]` „sehen gut aus", `[7]` „tun weh" · Karte und zwei Items unrepariert (BLOCKER 2, MAJOR 4) |
| M2 | L8 Fernseher unter dem Dach; Kinderzimmer in der WG | **T** | `[7]` → „Der Herd ist neu, das Dach ist alt" ✓ · `das Kinderzimmer` steht weiter im Wortfeld, in `[3]` und in `extra-a12-l08-04` |
| M3 | L1 zwei unvereinbare Wege; U-Bahn-Frage unbeantwortet | **T** | `[9]` „Die U-Bahn fährt ins Zentrum" ✓, das zweite „Zuerst" ist weg ✓ · `[7]` „Da ist auch die Brücke" bleibt eine Deckungszeile ohne Anschluss |
| M4 | L12 Liste statt Erzählung; „die Kartoffel"; „zu viel"; `gab` | **T** | Plural ✓, „Im Kuchen war zu viel Zucker" ✓, canDo → „gegessen habe" ✓ · fünf Listenzeilen und der Singular im Item bleiben (MAJOR 13) |
| M5 | Phonetik L7 („LANG/KURZ/GROSS") und L11 („langes o: Sonne") | **G** | L7 `'Langer und kurzer Vokal: Haar – Arm'`, Items `['das HAAR','der ARM','der MUND']`; L11 `'Langes e, kurzes o: Schnee – Sonne'` — beide sachlich richtig, Betonungssilbe markiert, `GROSS` weg |
| M6 | `pretest.accepted` als Freibrief (L4/L6/L7, einzelne Artikel) | **G** | L4 `['Die Steckdose funktioniert nicht','Die Lampe ist nicht','Das ist nicht','Das ist kein']`, L6 `['Mein Kopf tut weh',…]`, L7 `['Mein Onkel ist',…]` — alle ≥ 2 Wörter und strukturtragend; ohne `acceptedMode`-Feld gelöst, was die bessere Lösung ist |
| M7 | Missionszuordnung nach Thema statt Struktur; RULE 13 misst falsch | **T** | Remap gelandet, RULE 13 = 1 ✓ · L1↔M5 und L6↔M1 sind wieder thematisch, M2 liegt ungenutzt, RULE 18 fehlt, der Ratchet steht auf 3, der Test ist rot (BLOCKER 1, MAJOR 8) |
| M8 | `anrede` fehlt zwölfmal | **T** | zwölfmal gesetzt, mit Begründung an jeder Zeile ✓ · der du/Sie-Bruch zu `scenario_de` ist unverändert und trifft jetzt 11 statt 9 Lektionen (MAJOR 9) |
| M9 | Grammatikdeckel §2.2 zu 5/10; L7 Nominativ nach Akkusativ | **T** | L7s Karte führt den Nominativ als **Kontrast** zu L3 („Lektion 3 hat das Objekt gezeigt: den Onkel") — genau der vorgeschlagene Weg ✓ · die fünf Deckellücken sind unverändert, RULE 19 fehlt (MAJOR 14) |
| M10 | L9 Wortfeld = Reiselexik, `canDo[3]` unbeantwortet | **O** | nichts geändert; die neue Verlinkung auf „Mit dem Zug durch Deutschland" verstärkt die Drift (MAJOR 7) |
| M11 | „die Zahlen 20–100" überschneidet A1.1s „11–100" | **O** | `a11.js` ist zu Recht nicht angefasst worden (außerhalb des Auftrags), aber die Übergabezeile fehlt im CONTRACT §6 und die `RANGE_RE`-Prüfung ist nicht geschrieben — die Zahlen 20–100 werden weiter zweimal gezählt |
| M12 | Drei erfundene Formulare („Steckbrief", zweimal „Ausflug", Feld `Aussehen`) | **G** | L7 „Anmeldung für den Sportverein" mit `['Familienname','Vorname','Geburtsdatum','Straße','Unterschrift']`, L5 „Anmeldung für die Fahrt zum Konzert", L11 „Anmeldung für den Ausflug"; `Aussehen`/`Wetter`/`Temperatur` sind weg, alle sechs Formulare enden auf `Unterschrift`; `writingTasks` in beiden Kopien identisch |
| M13 | L11 „bei dir / bei uns" gegen L8s gemeinsame WG; L4 Hotel nach L2 | **G** | `setting: 'Tim fragt Ana nach dem Wetter in Marokko.'`, `[0]` „Wie ist das Wetter im Sommer bei dir?" trägt jetzt seine Voraussetzung; L4 `setting: 'Ana übernachtet **vor dem Umzug** im Hotel.'` |
| M14 | L10 zwei Strukturen in einer Karte; `gegen`/`um`/`teurer` ohne Beleg | **T** | notice auf `für/ohne/durch` gekürzt, Komparation mit Beleg aus `[8]` ✓ · die **Regelkarte** hat alle vier Formen behalten (BLOCKER 2) |

**Bilanz:** 5 geschlossen, 12 teilweise, 2 offen. Keine der vier BLOCKER-Klassen ist zurückgekehrt; drei von vier sind dort geschlossen, wo Review #1 sie gefunden hat, und in eine Nachbardatei ausgewichen.

---

## Bewertungsübersicht (Runde 2, Runde 1 in Klammern)

Fünf Achsen zu je 5: **Deutsch** (Sprachrichtigkeit aller Texte der Lektion — Dialog, notice, Karte, Items), **Niveau** (nichts vor seiner Regel, nichts über der Decke), **Dialog** (Kohärenz, Antwortbezug, Erzählwert), **Prüfung** (Deckung der `examTeile` durch Link, Format und Mission), **Kohärenz** (Situation ↔ Wortfeld ↔ canDo ↔ Aufgabe).

| Lektion | Deutsch | Niveau | Dialog | Prüfung | Kohärenz | Σ /25 |
|---|---|---|---|---|---|---|
| 1 Wie komme ich zum Rathaus? | 4 (4) | 3 (3) | 4 (4) | 4 (4) | 4 (4) | **19** (19) |
| 2 Die Wohnungsanzeige | 4 (3) | 2 (3) | 4 (3) | 3 (4) | 4 (4) | **17** (17) |
| 3 In der Stadt unterwegs | 2 (3) | 3 (3) | 4 (4) | 3 (3) | 3 (4) | **15** (17) |
| 4 Im Hotel reklamieren | 3 (4) | 4 (4) | 4 (4) | 4 (3) | 4 (4) | **19** (19) |
| 5 Pläne für das Wochenende | 2 (3) | 4 (4) | 3 (3) | 4 (3) | 3 (4) | **16** (17) |
| 6 In der Arztpraxis | 3 (2) | 3 (4) | 4 (2) | 3 (2) | 4 (2) | **17** (12) |
| 7 Wer ist das? | 4 (3) | 2 (2) | 3 (3) | 5 (3) | 4 (3) | **18** (14) |
| 8 Im Haushalt helfen | 4 (4) | 2 (2) | 4 (2) | 4 (3) | 3 (3) | **17** (14) |
| 9 Regeln unterwegs | 4 (3) | 4 (2) | 3 (3) | 3 (2) | 2 (3) | **16** (13) |
| 10 Kleidung kaufen | 3 (3) | 4 (3) | 4 (3) | 4 (3) | 3 (4) | **18** (16) |
| 11 Wie ist das Wetter? | 3 (3) | 5 (4) | 4 (3) | 3 (2) | 5 (3) | **20** (15) |
| 12 Das Fest war schön | 4 (3) | 3 (3) | 2 (3) | 3 (3) | 4 (3) | **16** (15) |

Kursmittel **17,3/25** (Runde 1: 15,7). Zum Vergleich: A1.1 stand nach fünf Runden bei 19,7 — A1.2 hat nach zwei Runden aufgeholt, was A1.1 vier gekostet hat, und wird jetzt an einem schärferen Maßstab gemessen (die Items und die Checkpoints waren in Runde 1 gar nicht prüfbar).

**Zur Lesart:** **Stärkste: L11 (20).** Der Wetterdialog ist der einzige des Moduls, in dem Situation, Wortfeld (19 Einträge, alle Wetter), Grammatik, Karte und Kann-Beschreibungen **eine** Sache sind; die Figurenreparatur (Tim fragt Ana nach Marokko) hat nebenbei die drei `bei`-Belege begründet, und die Karte löst das Versprechen aus Lektion 1 wörtlich ein. Ihre 3 in „Prüfung" kommen vom falschen Hören-Etikett und der fehlenden Mission, beides ehrlich als Rest ausgewiesen. Dahinter **L1 und L4 (je 19)**: L1 verliert nur an der Regelkarte und am Item `l01-09`, L4 an dem einen `Doch`. **Der größte Sprung ist L6 (12 → 17)** — vier Befundklassen aus Runde 1 sind im Dialog geschlossen, und was übrig ist, steht in der Karte und in zwei Items, also außerhalb der Datei, die repariert wurde. Das ist exakt die Bewegung, die dieses Review beschreibt. **Schwächste: L3 (17 → 15)** — die einzige Lektion, die **gefallen** ist, und zwar nicht, weil sie schlechter wurde, sondern weil der Pool sichtbar gemacht hat, was vorher unsichtbar war: `brauchen + Supermarkt` steht jetzt sechsfach da, und ihre Regelkarte widerspricht ihrer eigenen notice. Dahinter L5 (16) mit vier unreparierten Sprachfehlern in Produktionszeilen, L9 (16), dessen Wortfeld unverändert an der Situation vorbeigeht, und L12 (16), dessen Dialog immer noch eine Liste ist. **Der Befund, der sich durch alle zwölf zieht,** ist nicht mehr Deckung (das war Runde 1) und nicht Sprache, sondern **Synchronität**: dieselbe Aussage steht an drei Stellen — notice, Regelkarte, Item —, und repariert wurde sie an einer.

---

## Würde ich meinen Namen jetzt daraufsetzen?

**Nein — aber näher als beim letzten Mal, und aus einem anderen Grund.** Nach Runde 1 fehlten Prädikate; jetzt gibt es sie, sie sind gut geschrieben, sie laufen, und sie melden 0. Was fehlt, ist ihre **Reichweite**. Jede der sechs Blockaden oben lässt sich in einem Satz sagen, der keine Datei nennt: „was die notice zurücknimmt, nimmt die Karte auch zurück"; „was der Lernende tippt, hat der Kurs erklärt — auch wenn er es in einem Item tippt"; „was die Prüfung abfragt, hat die Lektion davor gelehrt"; „was das Raster verspricht, hat das Format, das es nennt"; „was gezählt werden soll, wird auch gezählt"; „was repariert wurde, sagt der Test". Fünf davon standen in Review #1 schon, und sie wurden korrekt umgesetzt — für die Fläche, auf der der Befund stand. **Die Lehre dieser Runde ist deshalb nicht „mehr Regeln", sondern: eine Regel muss die Handlung benennen, nicht den Ort.** RULE 15 heißt heute „Vorgriffe in Diktat/Nachsprechen" und müsste „Vorgriffe in allem, was der Lernende produziert" heißen; dann hätte sie die 21 Pool-Items und die zwei Checkpoint-Sätze mitgefunden, ohne dass jemand sie erweitert. RULE 6b heißt „Notice-Formen ohne Beleg" und müsste „Regeltexte ohne Beleg" heißen; dann hätte sie die Karten gelesen. RULE 16 heißt „ungedeckte Prüfungsteile" und prüft „Link vorhanden"; der Name ist richtig, die Implementierung nicht.

Der zweite Satz, der aus dieser Runde mitgeht, ist unbequemer und betrifft die Arbeitsweise, nicht das Modul: **eine grüne Ausgabe ersetzt nicht `npm test`, und ein CONTRACT darf keinen Zustand behaupten, den er nicht prüft.** Der Commit sagt „Validator a1.2: 0/156/0/1, RULE 6b/15/16 = 0", und das stimmt; der CONTRACT sagt „`tests/curricula.test.mjs` already pins the list as `[11]`", und das stimmt nicht — die Suite ist seit diesem Commit rot. Beide Sätze stehen in derselben Änderung. Review #1 hat 673/673 gemessen und aufgeschrieben; das ist der Grund, warum dieser Befund jetzt eine Zeile kostet und nicht eine Woche.

Was gut ist, bleibt gut und soll die Runde nicht verlieren: **die 116 handgeschriebenen Items sind die beste Einzelarbeit in diesem Repo.** 116 von 116 mit `accepted`, 116 von 116 mit englischer Glosse, die zwei Höflichkeitsitems mit `caseSensitive: true` genau dort, wo A1.1-Review #4 es gefordert hat, **0** untaught tokens, **0** unbrauchbar nach `quality.js`, **168 von 168** lösen ihre eigene Antwort; L4s dreizehn Verneinungsitems tragen **alle** den Kontextsatz, der `NEGATION_WITHOUT_CUE` auslöst, also ist die Warnung des CONTRACT §5 („L4 zieht ohne Reserve") eingelöst und nicht nur gemeldet. Das ist der Teil des Moduls, den niemand anfassen muss.

Die Arbeit, die bleibt, ist wieder überschaubar und liegt diesmal in drei Dateien statt in zwölf Lektionen: ein Testfix und vier Ratchet-Zahlen, ein `build-lesson-pool.mjs a1.2`-Lauf nach vier Kartenreparaturen, `producedBeforeTaught` auf die Items und den Checkpoint erweitert plus etwa fünfzehn Item-Zeilen, eine Formattabelle in `examTeileBacked` und sechs ehrlichere `examTeile`-Einträge, eine Zeile in `evaluate-writing.mjs`. **Das ist ein Tag. Danach lese ich die zwölf Lektionen, die 116 Items und die vier Checkpoints noch einmal, und wenn die Karten dann sagen, was die notices sagen, unterschreibe ich für alle zwölf.**

---

**Minors (fünf Zeilen, nicht erfasst):** (1) L2 `[9]` „Die **erste** Wohnung ist frei" widerspricht weiter `[7]` „Das **dritte** Stockwerk ist frei", und „Wie ist die Miete?" (Dialog) bleibt schwächer als „Wie **hoch** ist die Miete?" (`schreiben.sample` derselben Lektion) — beide Minors aus Runde 1, unrepariert. (2) Vier Figuren, zwei Nachnamen, über Kreuz: **Herr Berg** (Vermieter, L2) ↔ **Lena Berg** (L7/L11), **Frau Berger** (Ärztin, L6) ↔ **Tim Berger** (L5); `PERSONAS_A12` prüft Tatsachen, nicht Namenskollisionen, und `extra-a12-l09-09` stellt **Herrn Berg** zusätzlich an den Bahnhof, wo Herr Schmidt arbeitet. (3) `extra-a12-l11-04` „**Bei Wind** geht Tim zur Kirche" und `-07` „**Im Gewitter** kommt Tim aus der Kirche" sind beides kein idiomatisches Deutsch (*bei Wind* ist markiert, *bei Gewitter* ist die Form), und `extra-a12-l04-11` „Der Vorhang ist **kein** am Fenster" ist kein Fehler, den ein Lernender macht. (4) L4 `situation: 'Hotel, **Termine** und Reklamation'` — Termine kommen in Wortfeld, Dialog, canDo und Schreibaufgabe nicht vor (sie sitzen in L6); L5 `situation: 'Pläne und **Wünsche**'` ohne einen einzigen Wunschausdruck; beide Minors aus Runde 1, unrepariert. (5) `die Anzeige` (L2), `funktionieren` (L4) und `der Müll` (L9) tragen weiter keine `wordId`, haben also kein Audio — `funktionieren` ist das Kernverb seiner Lektion und `der Müll` das einzige situationstragende Wort von L9; CONTRACT §6.3 führt die drei `words`-Zeilen als offene Übergabe, und §1/§5 sind überholt (sie führen `a12.json` als **MISSING** und `a12.extra.json` als `items: []`).

BLOCKER: 6 · MAJOR: 15
