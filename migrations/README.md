# Database migrations

SQL in this folder is **never applied automatically** — there is no migration
runner in the build. Apply each file by hand in the Supabase SQL editor
(Dashboard → SQL Editor → New query → paste → Run), in filename order.

## Files

| File | What it does | Status |
|---|---|---|
| `2026-08-16-fix-rls-security.sql` | Closes the self-grant-Pro RLS hole on `subscriptions`/`profiles`, locks down `webhook_logs` and `xray_usage`, narrows anonymous access to paid reading/listening content to the free tier (a1.1). | ✅ **Applied 2026-08-16** |
| `2026-08-16-enable-rls-on-unprotected-tables.sql` | Enables RLS on 13 tables that had it switched off entirely (the whole course catalogue was publicly deletable via the anon key), and revokes public EXECUTE on `debit_speaking_wallet`. | ✅ **Applied 2026-08-16** |
| `2026-08-16-content-cleanup.sql` | De-CAPS of 270 shouting `grammar_rules` rows (892 edits, correct German orthography restored); `related_slugs` populated for all 64 topics; documents the dead `grammar_introductions` table (commented DROP, owner's call). | ✅ **Applied 2026-08-16** |
| `2026-08-16-reading-lessons.md` (note) | 22 new reading lessons inserted (every level now ≥8) — source of truth lives in `content/reading/*.json` in the repo; re-seed with `scripts/seed-reading-lessons.mjs`. | ✅ **Applied 2026-08-16** |
| `2026-08-16-welcome-email-webhook.sql` | Wires the never-called `send-welcome-email` function: `pg_net` + an `AFTER INSERT` trigger on `auth.users` POSTing to the Netlify function with the `WEBHOOK_SECRET` bearer. The repo copy redacts the secret — the live function body embeds it (rotate both together). | ✅ **Applied 2026-08-16** |
| `2026-08-17-audit-remediation.sql` | Remediates `AUDIT-2026-08-16.md`: drops the PUBLIC/ALL policy on `speaking_usage` and the client write policies on the other speaking tables; allows `'placement'` in `speaking_sessions_level_check` (restoring the level test); dedupes b1.2 reading lessons and `payment_failures` (+ unique indexes); reconciles `profiles.is_subscribed`; reshuffles the skewed answer-option positions; adds `xray_usage.ip_hash` for per-IP metering. | ✅ **Applied 2026-08-17** |
| `2026-08-17-speaking-missions.md` (note) | 56 speaking missions inserted for A1.2–B2.2 (the table previously held only A1.1, so 7 of 8 levels showed an empty Speaking tab). Data-only; ordering column is `mission_order`. | ✅ **Applied 2026-08-17** |
| `2026-08-17-content-backfill.md` (note) | Six data migrations: `acceptable_answers` for all 433 fill-blanks (only 7 have real variants; `[]` would have broken every one — see the note), plurals resolved for 101 nouns (98 are mass nouns marked `'null'`), 47 duplicate vocabulary rows removed, reading-lesson CRLF/plural-marker/`word_count`/`estimated_reading_time` hygiene, `prerequisite_slugs` for 60 topics, and 3 unanswerable exercises rewritten. | ✅ **Applied 2026-08-17** |
| `2026-08-17-lifecycle-and-listening.sql` | `lifecycle_emails` ledger for the new trial lifecycle job (UNIQUE(user_id, kind) is what makes the daily cron safe to re-run); adds the `answers`/`plays_used` columns the SPA has always written to `user_listening_progress`, so listening completions finally persist and can feed the streak; consolidates that table's overlapping policies; and records the 64-title restore. | ✅ **Applied 2026-08-17** |
| `2026-08-17-narrow-anon-content.sql` | Closes audit A-09: `words`, `sentences` and `paragraphs` were fully readable with the anon key that ships in the SPA bundle. Anon now sees only the a1.1 free tier; authenticated keeps full read. `podcasts` deliberately untouched (the public RSS feed uses the anon key). Also fills the last 3 empty `prerequisite_slugs`. | ✅ **Applied 2026-08-17** |
| `2026-08-22-activation-lifecycle.sql` | Widens the `lifecycle_emails` kind CHECK for the activation sequence (`activation_d1`/`activation_d4`) and creates the service-role-only `lifecycle_customer_state` funnel view. The mailer still no-ops until `LIFECYCLE_ACTIVATION_ENABLED=true` is set in Netlify. (Row added 2026-08-31 — this file was missing from the ledger.) | ✅ **Applied 2026-08-31** via the Supabase connector (migration `activation_lifecycle_2026_08_22`) |
| `2026-08-31-purchases.sql` | One-time product purchases (course entitlements) for the telc B1 Komplettvorbereitung: widens `subscriptions.plan_type` to admit `'course'` (the included Pro window — without this the webhook fails AFTER money moved), creates `purchases` (read-own RLS, service-role writes, UNIQUE on the LS order id for webhook idempotency) and `program_progress` (own-rows checkboxes). Guarded by `tests/purchases.test.mjs`. | ✅ **Applied 2026-08-31** via the Supabase connector (migration `purchases_2026_08_31`); policies + widened CHECK verified live |
| `2026-08-31-exam-profile.sql` | Exam identity for the exam-first app (renovation Phase 4a): `profiles.exam_track` (CHECK: the four examTracks keys + `'none'`), `exam_date`, `daily_goal_target` (1–10, default 3). All three client-writable preferences like `current_level`; the privileged-column trigger is untouched (it pins only the columns it names). | ✅ **Applied 2026-08-31** via the Supabase connector (migration `exam_profile_2026_08_31`); columns + CHECKs verified live |
| `2026-08-31-exam-attempts.sql` | Mock-exam attempts (renovation Phase 5a): one row per practice run, `section_deadline` as the server-persisted timer anchor, answers jsonb autosave. Own-row SELECT/INSERT/UPDATE, deliberately **no DELETE**. | ✅ **Applied 2026-08-31** via the Supabase connector (migration `exam_attempts_2026_08_31`); 3 policies verified live |
| `2026-08-31-writing-submissions.sql` | AI-graded writing (renovation Phase 5b): submissions + feedback ledger, also what `evaluate-writing.mjs` counts to enforce the per-tier limits. SELECT-own only — **service-role writes** (every row costs an AI call). | ✅ **Applied 2026-08-31** via the Supabase connector (migration `writing_submissions_2026_08_31`); exactly one SELECT policy verified live |
| `2026-08-31-vocab-srs.sql` | Vocabulary spaced repetition (renovation Phase 6): one card per (user, word), SM-2-lite fields, `(user_id, due_at)` index. Own-row RLS on all four verbs — the `program_progress` posture. Replaces the never-persisted "Mark learned" checkbox. | ✅ **Applied 2026-08-31** via the Supabase connector (migration `vocab_srs_2026_08_31`) |
| `2026-09-02-confirmation-nudge.sql` | Admits the `confirm_nudge` kind into the `lifecycle_emails` CHECK so the one-shot confirmation nudge (`netlify/functions/confirmation-nudge.mjs`) can claim before sending — the UNIQUE(user_id, kind) lock is what makes "one reminder per account, ever" a fact rather than a promise in the copy. Aimed at the 498 signups that never confirmed their address (227 inside the 2-90 day mailable window). | **Applied 2026-09-03** via the Supabase connector (migration `confirmation_nudge_ledger_kind`); CHECK verified live. `CONFIRM_NUDGE_ENABLED=true` set in Netlify 2026-09-03 10:48 UTC - i.e. AFTER that day's 10:30 run, which therefore no-opped and claimed nothing. First live send is 2026-09-04; the job runs 10:30 UTC daily, 40 sends per run |
| `2026-09-13-admin-panel-foundation.sql` | Admin panel phase 1 (docs/admin-panel.md): `profiles.role` (CHECK, frozen by the privileged-column trigger so a user can never grant themselves a role), seeds the two owner accounts as `admin`, `pg_trgm` + four `profiles` indexes for the user directory, and the append-only `admin_audit_log` (actor never nullable, RLS on with no policies, UPDATE/DELETE/TRUNCATE revoked). Guarded by `tests/admin-rbac.test.mjs`. | ✅ **Applied 2026-09-13** via the Supabase connector (migration `admin_panel_foundation_2026_09_13`); roles, indexes, RLS and grants verified live |
| `2026-09-03-weekly-metrics.sql` | `weekly_metrics` (service-role only) + `weekly_truth_metrics()` SECURITY DEFINER with EXECUTE revoked from anon/authenticated. Powers `netlify/functions/weekly-truth.mjs` (Monday 06:00 UTC: stored history + owner email). | ✅ **Applied 2026-09-03** (via Supabase connector) |
| `2026-09-03-a1-1-reorder.sql` | A1.1 lesson order: Nouns & Gender first, alphabet fifth (the first-lesson leak); unique key dropped/re-added around the permutation. Mirrored in `src/data/grammarTopics.js` + the grammar cache; `tests/topic-order.test.mjs` pins all three. | ✅ **Applied 2026-09-03** (via Supabase connector) |
| `2026-09-05-a1-1-reading.sql` | Course Factory Wave 2, PR C: adds `reading_lessons.checks` (auto-checkable Richtig/Falsch + exam-choice items), rewrites the 8 live A1.1 lessons with `checks` populated, and inserts 2 new exam-format A1.1 lessons (order_index 9/10). Generated by `scripts/reading-from-json.mjs`; every UPDATE is guarded `AND checks IS NULL`, every INSERT `WHERE NOT EXISTS`. | **Applied 2026-09-05** via the Supabase connector (Wave 2 close); verified 10 A1.1 lessons, all with `checks` |
| `2026-09-05-a1-1-listening.sql` | Course Factory Wave 2, PR C: adds `listening_questions.acceptable_answers`, widens the `question_type` CHECK to admit `'dictation'`, patches 2 existing rows' `options` (author's late review pass), and inserts 78 new A1.1 questions (13 per exercise across the 6 A1.1 exercises, including number/time/price dictation). Generated by `scripts/listening-questions-from-json.mjs`; every INSERT is guarded `WHERE NOT EXISTS` on `(exercise_id, question_number)`. | **Applied 2026-09-05** via the Supabase connector (Wave 2 close); verified 6×23 A1.1 questions incl. dictation rows |
| `2026-09-05-a1-1-wortliste.sql` | Course Factory Wave 2, PR B (#69): 114 new A1.1 words (Länder/Sprachen, Beruf, Formular, Getränke, Uhrzeit/Termine, formal Sie) and 51 guarded fixes to live rows ("null" plurals, article-in-word, above-level examples). Generated by `scripts/words-from-json.mjs`. | **Applied 2026-09-05** via the Supabase connector; verified 339 A1.1 words, 0 "null" plurals, 0 article prefixes |
| `2026-09-05-a1-2-wortliste.sql` | Course Factory Wave 3, PR B: adds 166 new A1.2 words across 8 categories (Travel & Transport, Places in Town, Health & Doctor, Furniture, Free Time & Hobbies, Money & Shopping, Numbers 100-1000 & Ordinals, Weather), and 54 guarded fixes to live A1.2 rows (article baked into the headword, the literal string "null" as a plural, three rows repurposed into missing words, month plurals). Generated by `scripts/words-from-json.mjs`, now generalised to derive the level from its input rows instead of hardcoding `a1.1`. | **Applied 2026-09-05** via the Supabase connector in three chunks; verified 413 A1.2 words (247 + 166), 0 "null" plurals, 0 article prefixes, 2215 words total = VOCAB_WORD_COUNT |
| `2026-09-05-a1-2-new-topics.sql` | Course Factory Wave 3, PR A: adds four A1.2 grammar topics — stem-changing-verbs, imperative, perfekt-intro, dative-prepositions-intro — at topic_order 9-12 (33 rules, 40 examples, 104 exercises, ≥16 typed per topic). Generated by `scripts/grammar-topics-from-json.mjs` (create mode, unchanged from #68); re-asserts the topic_order CHECK at 1..12 (already that range since #68, so a no-op). | **Applied 2026-09-05** via the Supabase connector in 8 chunks (after the rule_type `list`→`tip` fix); verified 4 topics / 33 rules / 40 examples / 104 exercises byte-identical to `grammar-content-cache.json` |
| `2026-09-05-a1-typed-production.sql` | Course Factory Wave 3, PR A2: adds 163 typed (non-multiple_choice, `options: null`) exercises across the 16 pre-existing A1.1/A1.2 grammar topics (79 A1.1 + 84 A1.2), and upgrades `numbers-counting` with 4 new rules (100-1000, prices, ordinal numbers, phone numbers/postal codes), 4 new examples, a `topic_patch` (description/estimated_time) and one guarded `grammar_rules` content patch (the Quick Reference summary rule, id `125e153e-...`, guarded `WHERE ... AND content = <old>::jsonb`). Generated by the new EXTEND mode in `scripts/grammar-topics-from-json.mjs` (never mints a topic id, never touches the topic_order CHECK); every INSERT is guarded `WHERE NOT EXISTS (id)`. | **Applied 2026-09-05** via the Supabase connector in 7 chunks; verified 163 new exercise rows (every A1 topic ≥8 typed), numbers-counting 9 rules / 12 examples / Quick Reference at 9 points, totals 72/522/757/1045 = cache |
| `2026-09-05-a1-2-reading.sql` | Course Factory Wave 3, PR C: rewrites the 8 live A1.2 lessons with `checks` populated (5 Richtig/Falsch + 1 exam-style choice each, ≤120 words), and inserts 2 new exam-format A1.2 lessons (order_index 9/10: E-Mails Teil 1, Anzeigen Teil 2). Generated by `scripts/reading-from-json.mjs`, now generalised to derive its header comments from the input instead of hardcoding A1.1 (statement output unchanged — verified byte-identical against the committed A1.1 migration); every UPDATE is guarded `AND checks IS NULL`, every INSERT `WHERE NOT EXISTS`. Re-emits `ALTER TABLE ... ADD COLUMN IF NOT EXISTS checks jsonb` (idempotent no-op, the column already exists from the A1.1 migration). | **Applied 2026-09-05** via the Supabase connector in 3 chunks; verified 10 A1.2 lessons all with `checks` (6 items each, the Anzeigen lesson 5), 70 lessons total = READING_LESSON_COUNT |
| `2026-09-05-a1-2-listening.sql` | Course Factory Wave 3, PR C: patches 2 existing A1.2 rows' `options` (reviewer fixes) and inserts 78 new A1.2 questions (13 per exercise across the 6 A1.2 exercises, including 18 number/time/price/phone dictation items). Generated by `scripts/listening-questions-from-json.mjs`, same header-generalisation as above; every INSERT is guarded `WHERE NOT EXISTS` on `(exercise_id, question_number)`, existing-row UPDATEs keyed on the OLD `options` value. Re-emits the `acceptable_answers` column + widened `question_type` CHECK (idempotent no-op, both already live from the A1.1 migration). | **Applied 2026-09-05** via the Supabase connector in 4 chunks; verified 6×23 A1.2 questions, 18 dictation rows, both option fixes live |
| `2026-09-05-goethe-a1-exam-key.sql` | Course Factory Wave 1: widens the three exam-key CHECKs (`writing_submissions.exam_key`, `exam_attempts.exam_key`, `profiles.exam_track`) to admit `goethe_a1`, the Start Deutsch 1 track. Idempotent. (Ledger row added retroactively 2026-09-06 — the file had none.) | **Applied 2026-09-05** via the Supabase connector (ledger entry `goethe_a1_exam_key`); on `exam_attempts` superseded by the Abschlusstest rows below |
| `2026-09-05-a1-1-abschlusstest.sql` | Course Factory Wave 2, PR D (#71): widens the `exam_attempts_exam_key_check` CHECK to admit the course-test key `a1_1_abschluss` (the Abschlusstest A1.1 on the mock runner). `writing_submissions`/`profiles.exam_track` deliberately untouched. (Row added 2026-09-05 — the file was missing from this ledger.) | ✅ **Applied 2026-09-05** (per `docs/course-factory-tracker.md`, Wave 2 row D) |
| `2026-09-05-a1-2-abschlusstest.sql` | Course Factory Wave 3, PR D: re-creates the same CHECK with the full current list plus `a1_2_abschluss` (the Abschlusstest A1.2, gated on A1.2 access). Idempotent; `tests/exams.test.mjs` asserts the newest `*abschlusstest*.sql` lists every `COURSE_TESTS[].key`. | **Applied 2026-09-05** via the Supabase connector; `pg_get_constraintdef` lists all seven keys incl. `a1_2_abschluss` |
| `2026-09-06-a2-1-new-topics.sql` | Course Factory Wave 4, PR A: adds four A2.1 grammar topics — adjective-endings-intro, pronouns-accusative-dative, modal-verbs-past, temporal-prepositions — at topic_order 9-12 (36 rules, 40 examples, 104 exercises, ≥20 typed per topic). Generated by `scripts/grammar-topics-from-json.mjs` (create mode); re-asserts the topic_order CHECK at 1..12 (already that range since #68, so a no-op). | **Applied 2026-09-06** via the Supabase connector in 10 chunks; verified 4 topics / 36 rules / 40 examples / 104 exercises (86 typed) byte-identical to `grammar-content-cache.json` (md5 over rules/examples/exercises matched) |
| `2026-09-06-a2-1-typed-production.sql` | Course Factory Wave 4, PR A2: EXTEND-mode typed production for the eight pre-existing A2.1 topics (97 exercises, 12–13 per topic, options NULL + acceptable_answers) plus +3 rules / +4 examples each for possessive-pronouns, perfect-tense-haben and imperative-mood. Generated by `scripts/grammar-topics-from-json.mjs` (extend mode); no topic INSERTs, no CHECK change; every INSERT guarded by WHERE NOT EXISTS (id). | **Applied 2026-09-06** via the Supabase connector in 6 chunks; verified 97 exercises / 9 rules / 12 examples byte-identical to `grammar-content-cache.json` (md5 over the 118 new rows matched), totals 76/567/809/1246 = cache, every A2.1 topic ≥20 exercises |
| `2026-09-06-a2-1-wortliste.sql` | Course Factory Wave 4, PR B: adds 175 new A2.1 words across 8 categories (Health & Body, Home & Moving, Offices & Forms, Travel & Holidays, Clothing & Appearance, Family & Relationships, Media & Communication, Celebrations & Invitations), and 208 guarded fixes to live A2.1 rows (117 headwords with the article baked in, 29 plurals carrying an article, 5 literal-string "null" plurals, 8 singularia tantum set to NULL, 15 glosses starting with "the", 34 example sentences outside the level). Generated by `scripts/words-from-json.mjs`. | **Applied 2026-09-06** via the Supabase connector in 4 chunks; verified 423 A2.1 words (248 + 175), the 175 additions md5-identical to the source JSON, 0 "null" plurals / 0 article-in-headword / 0 article-in-plural / 0 "the …" glosses at a2.1, 2390 words total = VOCAB_WORD_COUNT |
| `2026-09-06-a2-1-reading.sql` | Course Factory Wave 4, PR C: rewrites the 8 live A2.1 lessons to ≤150 words inside the A2.1 level constraint with `checks` populated (5 Richtig/Falsch + 1 exam-style a/b/c choice each), and inserts 2 new exam-format A2.1 lessons (order_index 9/10: Goethe A2 Lesen Teil 1 Zeitungstext, Lesen Teil 3 E-Mail — 5 a/b/c checks each, no Richtig/Falsch, matching the exam). Generated by `scripts/reading-from-json.mjs`; every UPDATE is guarded `AND checks IS NULL`, every INSERT `WHERE NOT EXISTS`. Re-emits `ALTER TABLE ... ADD COLUMN IF NOT EXISTS checks jsonb` (idempotent no-op). | **Applied 2026-09-06** via the Supabase connector in 2 chunks; verified 10 A2.1 lessons all with `checks` (6/6/6/6/6/6/6/6 on the rewrites, 5/5 on the exam-format pair), max 144 words, 72 lessons total = READING_LESSON_COUNT |
| `2026-09-06-a2-1-listening.sql` | Course Factory Wave 4, PR C: patches 1 existing A2.1 row's `options` (exercise 2 question 9, a distractor price) and inserts 78 new A2.1 questions (13 per exercise across the 6 A2.1 exercises: 5 multiple_choice at 11/13/15/17/19, 5 richtig_falsch at 12/14/16/18/20, 3 dictation at 21/22/23 — 18 number/time/price dictation items on the existing audio). Generated by `scripts/listening-questions-from-json.mjs`; every INSERT is guarded `WHERE NOT EXISTS` on `(exercise_id, question_number)`, the existing-row UPDATE keyed on the OLD `options` value. Re-emits the `acceptable_answers` column + widened `question_type` CHECK (idempotent no-op). | **Applied 2026-09-06** via the Supabase connector in 2 chunks; verified 6×23 A2.1 questions (3 dictation each = 18), the exercise-2 question-9 option fix live |
| `2026-09-06-goethe-a2-track.sql` | Course Factory Wave 4, PR D1: widens `profiles_exam_track_check` and `writing_submissions_exam_key_check` to admit `goethe_a2` (the Goethe-Zertifikat A2 track: Leitfaden, `/pruefung/goethe-a2/` hub, four SMS/E-Mail writing tasks, no mock yet). `exam_attempts` is widened by PR D2's Abschlusstest migration instead. Idempotent. | **Applied 2026-09-06** via the Supabase connector (ledger `goethe_a2_exam_key`); `pg_get_constraintdef` lists `goethe_a2` in both |
| `2026-09-06-a2-1-abschlusstest.sql` | Course Factory Wave 4, PR D2: re-creates `exam_attempts_exam_key_check` with the full current list plus `goethe_a2` (the track PR D1 left out of `exam_attempts` on purpose) and `a2_1_abschluss` (the Abschlusstest A2.1, Goethe-A2 format, gated on A2.1 access). Idempotent; `tests/exams.test.mjs` reads the newest `*abschlusstest*.sql` and requires every course-test key and every mock-bearing track key in it. | **Applied 2026-09-06** via the Supabase connector (ledger `a2_1_abschlusstest_exam_key`); `pg_get_constraintdef` lists all nine keys |
| `2026-09-06-a2-2-new-topics.sql` | Course Factory Wave 5, PR A: adds four A2.2 grammar topics — konjunktiv-ii-polite, verbs-with-prepositions-intro, indirect-questions-intro, infinitive-with-zu-intro — at topic_order 9-12 (36 rules, 40 examples, 104 exercises, 20–22 typed per topic). Generated by `scripts/grammar-topics-from-json.mjs` (create mode); re-asserts the topic_order CHECK at 1..12 (already that range since #68, so a no-op). | **Applied 2026-09-06** via the Supabase connector in 10 chunks (one dropped statement in chunk 02 caught by the md5 check and re-applied idempotently); verified 4 topics / 36 rules / 40 examples / 104 exercises (86 typed) byte-identical to `grammar-content-cache.json` (md5 over rules/examples/exercises matched), totals 80/603/849/1350 = cache |
| `2026-09-06-a2-2-typed-production.sql` | Course Factory Wave 5, PR A2: EXTEND-mode typed production for the eight pre-existing A2.2 topics (80 exercises, 10 per topic, options NULL + acceptable_answers) plus +3 rules / +4 examples each for reflexive-verbs, simple-past-sein-haben, coordinating-conjunctions, comparative, superlative and future-tense (18 rules, 24 examples). Generated by `scripts/grammar-topics-from-json.mjs` (extend mode); no topic INSERTs, no CHECK change; every INSERT guarded by WHERE NOT EXISTS (id). | **Applied 2026-09-06** via the Supabase connector in 6 chunks; verified 80 exercises / 18 rules / 24 examples byte-identical to `grammar-content-cache.json` (md5 over the 122 new rows matched), totals 80/621/873/1430 = cache, every A2.2 topic ≥10 typed |
| `2026-09-06-a2-2-wortliste.sql` | Course Factory Wave 5, PR B: the A2.2 share of the Wortliste — 171 additions in 8 new categories (Personality & Character, Transport & Travel Problems, Learning & Exams, Housing & Neighbours, Household Chores, Kitchen & Cooking, Shopping & Payment, Emergencies & Safety) as guarded INSERTs, plus 271 guarded UPDATEs on 177 live A2.2 rows (152 article-in-headword, 60 article-in-plural, 15 literal "null" plurals + 1 own find, 44 example sentences rewritten inside the level constraint). Generated by `scripts/words-from-json.mjs`. | **Applied 2026-09-06** via the Supabase connector in 5 chunks; verified by SELECT: 2561 words total, 418 at a2.2, 0 article-prefixed headwords/plurals, 0 "null" plurals, the 171 new rows in their 8 categories with audio_url null (awaiting the owner's Azure run, `--table words`); 536 defect rows remain at b1.x/b2.x for later waves |
| `2026-09-07-a2-2-legacy-recut.sql` | Course Factory Wave 6, PR A: in-place re-cut of the live A2.2 grammar rows that predate the A2.2 level constraint — guarded `UPDATE … WHERE id = … AND <field> = <old>` on 150 fields across `subordinating-conjunctions`, `subordinate-word-order`, `superlative` (+ one Präteritum in `comparative`, two meta-Genitiv explanations in `infinitive-with-zu-intro`/`future-tense`): B1 subordinators, als + Präteritum, Plusquamperfekt, Genitiv, Passiv replaced by in-level equivalents; nothing added or removed. Plus 45 guarded `words` re-levels (B1/B2 "Prepositions", out-of-Wortliste "Animals") from a2.2 to b1.1/b1.2/b2.1. Generated by `scripts/recut-from-json.mjs`; the identical edit is in `grammar-content-cache.json`. | Applied 2026-09-06 via the Supabase connector (`a2_2_legacy_recut_chunk_01..03`); verified: md5 over ALL A2.2 rules/examples/exercises (107/142/262) identical to the cache, SQL ban sweep at a2.2 = 0 rows, words a2.2 418 → 373 (5 Prepositions kept, 0 Animals; b1.1 +28, b1.2 +11, b2.1 +6) |
| `2026-09-07-b1-1-new-topics.sql` | Course Factory Wave 7, PR A: adds four B1.1 grammar topics — temporal-clauses, obwohl-damit-sodass, adverbial-connectors, two-part-connectors — at topic_order 9-12 (36 rules, 40 examples, 104 exercises, 20–23 typed per topic). Generated by `scripts/grammar-topics-from-json.mjs` (create mode); re-asserts the topic_order CHECK at 1..12 (a no-op). The identical rows are in `grammar-content-cache.json`, which this PR also re-serialised to the single-line dump format (Wave 6's re-cut had written it pretty-printed; content unchanged). | **Applied 2026-09-07** via the Supabase connector in 10 chunks (`b1_1_new_topics_chunk_01..10`); verified 84 topics / 657 rules / 913 examples / 1534 exercises = cache = `marketing.js`; the four topics' 36 rules / 40 examples / 104 exercises (84 typed) md5-identical to `grammar-content-cache.json` (rules `80ed16eb…`, examples `e434f865…`, exercises `8a07cd96…`) |
| `2026-09-07-b1-1-typed-production.sql` | Course Factory Wave 7, PR A2: EXTEND-mode typed production for the eight pre-existing B1.1 topics (80 exercises, 10 per topic — 4 guided fill_blank at stage 4 + 3 sentence_building + 3 error_correction at stage 5, options NULL + acceptable_answers) plus +3 rules / +4 examples each for the five thin topics genitive-prepositions, relative-clauses-dat-gen, konjunktiv-ii-ware-hatte, infinitive-with-zu, um-zu-ohne-zu (15 rules, 20 examples), and 6 guarded rule patches (one title_de calque, five `content` fields: lowercase nouns inside glosses, one Passiv sentence, one uncapitalised sentence start). Generated by `scripts/grammar-topics-from-json.mjs` (extend mode); no topic INSERTs, no CHECK change; every INSERT guarded by WHERE NOT EXISTS (id), every UPDATE guarded on the old value. The identical rows are in `grammar-content-cache.json`. | **Applied 2026-09-08** via the Supabase connector in 6 chunks (`b1_1_typed_production_chunk_01..06`); verified md5 over ALL B1.1 rules (108) / examples (145) / exercises (269) identical to the cache (`ddfaa277…` / `9fa006b9…` / `dd50bf69…`), totals 84 / 672 / 933 / 1614 = cache = `marketing.js`, ban sweep 0 rows |
| `2026-09-07-b1-1-legacy-recut.sql` | Course Factory Wave 7, PR A2: in-place re-cut of two pre-Course-Factory B1.1 multiple-choice exercises (genitive-prepositions order 7, relative-clauses-dat-gen order 7) whose `question_de`/`question_en` were swapped and whose German named its own key — 6 guarded `UPDATE … WHERE id = … AND <field> = <old>`; keys, options, ids and order_index untouched. Generated by `scripts/recut-from-json.mjs --header`; the identical edit is in `grammar-content-cache.json`. | **Applied 2026-09-08** (`b1_1_legacy_recut_chunk_01`); covered by the same full-level exercise md5 |
| `2026-09-06-a2-2-abschlusstest.sql` | Course Factory Wave 5, PR D2: re-creates `exam_attempts_exam_key_check` with all ten keys (adds `a2_2_abschluss`) and applies two guarded content fixes on live A2.2 `speaking_missions` rows (a relative-clause + Präteritum title, a `seitdem` in target_structures — both banned at A2.2). | Applied 2026-09-06 via the Supabase connector (`a2_2_abschlusstest`); verified: `pg_get_constraintdef` lists all ten keys, both `speaking_missions` rows carry the new values |
| `2026-09-06-a2-2-reading.sql` | Course Factory Wave 5, PR C: rewrites the eight live A2.2 reading lessons inside the A2.2 level constraint (≤150 words, `checks` = 5 richtig/falsch + 1 a/b/c each, key vocabulary and open questions refreshed; UPDATE guarded by `AND checks IS NULL`) and inserts two Goethe-A2 exam-format lessons (Teil 2 Informationstafel a/b/c ×5, Teil 4 Anzeigen-Zuordnung ×5 with the "x" option; INSERT guarded on level + title_de). Generated by `scripts/reading-from-json.mjs`. | Applied 2026-09-06 via the Supabase connector (`a2_2_reading_chunk_01..03`); verified: 10 lessons at a2.2, all with checks, the 8 rewrites ≤150 words and md5-identical to the reviewed JSON |
| `2026-09-06-a2-2-listening.sql` | Course Factory Wave 5, PR C: +78 listening questions on the six live A2.2 exercises (question_number 11–23 each: 5 multiple_choice at 11/13/15/17/19, 5 richtig_falsch at 12/14/16/18/20 with 3 Richtig / 3 Falsch per position across the six, 3 dictation at 21–23 with digit/word/unit acceptable_answers); the audit of the 60 live questions found nothing to edit. Generated by `scripts/listening-questions-from-json.mjs`; re-emits the acceptable_answers column and the widened question_type CHECK idempotently. | Applied 2026-09-06 via the Supabase connector (`a2_2_listening_chunk_01..02`); verified: 6 × 23 questions at A2.2 incl. 18 dictation |

Every file above records a change that is **already live** on project
`omqyueddktqeyrrqvnyq`. They are idempotent, so re-running one is safe and is how you
would reproduce the same state on a branch or fresh database — with one exception, called
out in its own header: the answer-option reshuffle in `2026-08-17-audit-remediation.sql`
picks new positions each run (meaning is preserved; positions are not).

## Conventions

- Name new files `YYYY-MM-DD-short-description.sql`.
- Make every statement idempotent (`DROP POLICY IF EXISTS`, `CREATE OR REPLACE`,
  `ADD COLUMN IF NOT EXISTS`) so a file can be re-run safely.
- Put a header comment in each file: what it fixes, how to test, how to roll back.

## Legacy SQL at the repo root

The eleven `*.sql` files at the repo root (`supabase-grammar-schema.sql`,
`fix-*.sql`, `create-*.sql`, …) are **historical**: they were run by hand at
various points and represent how the current database state was reached. They
are kept for reference — do not re-run them blindly (some contain one-off data
fixes with hardcoded UUIDs). New schema changes belong here in `migrations/`.

## After applying a migration

Re-test the critical paths as both an anonymous visitor and a logged-in user:

1. Signup / login / onboarding (profiles INSERT still works).
2. A Lemon Squeezy test purchase or webhook replay (subscriptions written via
   service role).
3. Grammar pages while logged out (Astro build + free tier need anon SELECT).
4. Reading/listening a1.1 while logged out; higher levels while logged in.
5. Sentence X-Ray as anonymous and logged-in user.
