[build]
  # Build order: SPA first (creates dist/), then Astro, then merge Astro output
  # into the SPA dist/ so grammar files aren't wiped by the Vite build.
  # _astro/ contains Astro's JS/CSS chunks; grammar/ contains the HTML pages.
  command = "npm run build && cd astro-site && npm install && npm run build && cd .. && cp -r astro-site/dist/grammar dist/grammar && cp -r astro-site/dist/vergleich dist/vergleich && cp -r astro-site/dist/leitfaden dist/leitfaden && cp -r astro-site/dist/pricing dist/pricing && mkdir -p dist/_astro && cp -r astro-site/dist/_astro/. dist/_astro/ && cp astro-site/dist/404.html dist/404.html && mv dist/index.html dist/app.html && cp astro-site/dist/index.html dist/index.html && cp astro-site/dist/sitemap-0.xml dist/ && node scripts/prerender-spa-routes.mjs && node scripts/ping-indexnow.mjs || true"
  publish = "dist"
  functions = "netlify/functions"

[functions]
  node_bundler = "esbuild"

# 07:00 UTC daily (09:00 CET / 08:00 GMT)
[functions."daily-sentence"]
  schedule = "0 7 * * *"

# Security headers for all pages
[[headers]]
  for = "/*"
  [headers.values]
    X-Frame-Options = "DENY"
    X-Content-Type-Options = "nosniff"
    Referrer-Policy = "strict-origin-when-cross-origin"
    Permissions-Policy = "camera=(), microphone=(self), geolocation=(), payment=()"
    Strict-Transport-Security = "max-age=31536000; includeSubDomains; preload"
    X-XSS-Protection = "1; mode=block"

# Hashed bundles — filenames change on every build, safe to cache forever
[[headers]]
  for = "/assets/*"
  [headers.values]
    Cache-Control = "public, max-age=31536000, immutable"

[[headers]]
  for = "/_astro/*"
  [headers.values]
    Cache-Control = "public, max-age=31536000, immutable"

# Force lowercase grammar level URLs
[[redirects]]
  from = "/grammar/A1.1"
  to = "/grammar/a1.1"
  status = 301

[[redirects]]
  from = "/grammar/A1.2"
  to = "/grammar/a1.2"
  status = 301

[[redirects]]
  from = "/grammar/A2.1"
  to = "/grammar/a2.1"
  status = 301

[[redirects]]
  from = "/grammar/A2.2"
  to = "/grammar/a2.2"
  status = 301

[[redirects]]
  from = "/grammar/B1.1"
  to = "/grammar/b1.1"
  status = 301

[[redirects]]
  from = "/grammar/B1.2"
  to = "/grammar/b1.2"
  status = 301

[[redirects]]
  from = "/grammar/B2.1"
  to = "/grammar/b2.1"
  status = 301

[[redirects]]
  from = "/grammar/B2.2"
  to = "/grammar/b2.2"
  status = 301

# Podcast RSS Feed
[[redirects]]
  from = "/podcast-feed.xml"
  to = "/.netlify/functions/podcast-feed"
  status = 200

# Speaking API proxy
[[redirects]]
  from = "/api/speaking/*"
  to = "/.netlify/functions/:splat"
  status = 200

# Unsubscribe handler
[[redirects]]
  from = "/unsubscribe"
  to = "/.netlify/functions/unsubscribe"
  status = 200

# Return 404 for missing hashed assets (prevents serving index.html as JS/CSS)
[[redirects]]
  from = "/assets/*"
  to = "/404.html"
  status = 404

# Daily sentence trigger (test via /api/daily-sentence?test=true)
[[redirects]]
  from = "/api/daily-sentence"
  to = "/.netlify/functions/daily-sentence"
  status = 200

# Test endpoint — always sends only to zaid@deutsch-meister.de
[[redirects]]
  from = "/api/send-daily-test"
  to = "/.netlify/functions/send-daily-test"
  status = 200

# ---------------------------------------------------------------------------
# SPA route allow-list — serve the app shell (200) for every REAL client-side
# route in the React router (src/App.jsx).
#
# The app shell is dist/app.html, NOT dist/index.html. The build renames the
# Vite-built SPA shell (dist/index.html) to dist/app.html and drops the static
# Astro homepage in at dist/index.html, so "/" serves the static marketing page
# while every SPA route below rewrites to /app.html (the unchanged React shell).
#
# Netlify serves existing static files (root index.html, /pricing/, grammar/,
# vergleich/, leitfaden/, _astro/, /404.html, public assets) directly and skips
# these non-forced rewrites, so only param/hash SPA routes reach here. Anything
# NOT matched here falls through to the 404 rule below — that is what turns
# unknown URLs into real HTTP 404s instead of a 200 + SPA shell.
#
# Keep this list in sync with the <Route> paths in src/App.jsx.
# ---------------------------------------------------------------------------

# --- Exact SPA routes ---
[[redirects]]
  from = "/intro"
  to = "/app.html"
  status = 200
[[redirects]]
  from = "/login"
  to = "/app.html"
  status = 200
[[redirects]]
  from = "/signup"
  to = "/app.html"
  status = 200
[[redirects]]
  from = "/reset-password"
  to = "/app.html"
  status = 200
[[redirects]]
  from = "/update-password"
  to = "/app.html"
  status = 200
[[redirects]]
  from = "/verify-email"
  to = "/app.html"
  status = 200
[[redirects]]
  from = "/onboarding"
  to = "/app.html"
  status = 200
# /pricing is now a static Astro file in dist/pricing/ — Netlify serves that
# real file before any non-forced rewrite, so no SPA rule is needed for it.
[[redirects]]
  from = "/faq"
  to = "/app.html"
  status = 200
[[redirects]]
  from = "/ueber-uns"
  to = "/app.html"
  status = 200
[[redirects]]
  from = "/podcasts"
  to = "/app.html"
  status = 200
[[redirects]]
  from = "/level-test"
  to = "/app.html"
  status = 200
[[redirects]]
  from = "/dashboard"
  to = "/app.html"
  status = 200
[[redirects]]
  from = "/profile"
  to = "/app.html"
  status = 200
[[redirects]]
  from = "/vocabulary"
  to = "/app.html"
  status = 200
[[redirects]]
  from = "/speaking"
  to = "/app.html"
  status = 200
[[redirects]]
  from = "/analyze"
  to = "/app.html"
  status = 200

# --- Route families (bare route + params/sub-paths) ---
[[redirects]]
  from = "/subscription"
  to = "/app.html"
  status = 200
[[redirects]]
  from = "/subscription/*"
  to = "/app.html"
  status = 200
[[redirects]]
  from = "/payment/*"
  to = "/app.html"
  status = 200
# /vergleich (hub), /vergleich/babbel|duolingo|lingoda and /leitfaden/telc-b1 are
# now static Astro files in dist/ — Netlify serves those real files before these
# non-forced rewrites. These remain only as the SPA fallback for any OTHER
# /vergleich/* or /leitfaden/* path that has no static file (e.g. an unknown
# comparison slug, which the SPA redirects to the hub). The bare "/vergleich"
# exact rule was removed so it can't shadow the static hub index.
[[redirects]]
  from = "/vergleich/*"
  to = "/app.html"
  status = 200
[[redirects]]
  from = "/leitfaden/*"
  to = "/app.html"
  status = 200
[[redirects]]
  from = "/video-library"
  to = "/app.html"
  status = 200
[[redirects]]
  from = "/video-library/*"
  to = "/app.html"
  status = 200
[[redirects]]
  from = "/level/*"
  to = "/app.html"
  status = 200
[[redirects]]
  from = "/reading"
  to = "/app.html"
  status = 200
[[redirects]]
  from = "/reading/*"
  to = "/app.html"
  status = 200
[[redirects]]
  from = "/listening"
  to = "/app.html"
  status = 200
[[redirects]]
  from = "/listening/*"
  to = "/app.html"
  status = 200
[[redirects]]
  from = "/admin/*"
  to = "/app.html"
  status = 200

# Grammar is served as static Astro files in dist/grammar/ (real files win
# before this rule). This only catches non-existent grammar sub-paths, which
# the SPA's /grammar/:level(/:topicSlug) routes render as the app shell — same
# behaviour as before, so grammar deep-links keep working.
[[redirects]]
  from = "/grammar/*"
  to = "/app.html"
  status = 200

# ---------------------------------------------------------------------------
# Real 404 — final catch-all. Any request that is neither a static file nor a
# known SPA route above returns the static 404 page WITH a 404 status (not the
# old 200 + SPA shell). Must stay LAST so the allow-list wins first.
# ---------------------------------------------------------------------------
[[redirects]]
  from = "/*"
  to = "/404.html"
  status = 404
